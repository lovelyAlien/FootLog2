# Design System — FootLog

## Product Context
- **What this is:** 개인용 iOS 시간 기반 위치 체크인 & 성찰 저널 앱. 매시간(또는 설정한 빈도) 알림으로 지금 어디 있는지 기록하고, 메모/사진을 덧붙여 개인 필드 로그를 쌓는다.
- **Who it's for:** 창업자 본인 한 명 (1단계 유일 사용자, 외부 검증 없음).
- **Space/industry:** 위치 체크인(Swarm류) + 저널링(Day One류)의 교집합이지만, 소셜/탐색/게이미피케이션 요소를 전부 배제한 반대 극단.
- **Project type:** 네이티브 iOS 앱(Expo/React Native), 개인 도구.

## Aesthetic Direction
- **Direction:** Personal Field Log(개인 현장 기록장) — "팔려고 만든 게 아니라 필요해서 만든 도구"처럼 보여야 함. 라이프스타일 앱도, 소셜 앱도 아님.
- **Decoration level:** minimal — 타이포그래피와 여백이 일을 다 함.
- **Mood:** 담담하고 절제됨. 축하하지 않고, 판단하지 않고, 그저 조용히 기록됨. "여정, 기록, 정리, 담담"(창업자가 직접 고른 4단어) 전부를 매 화면이 서비스해야 함.
- **경쟁 조사 근거:** Swarm(체크인=소셜/탐색 데이터, 친구공유·근처추천)과 정반대 포지션 — FootLog는 관객이 나 하나뿐인 도구. 요즘 캄/미니멀 저널 앱(Onrise, Finch)의 웜뉴트럴+반성 프롬프트 관행은 따르되, 그 위에 "성취 언어 완전 배제"라는 더 급진적인 원칙을 얹음.

## Typography
**두 계층 분리가 핵심 원칙** — 시스템이 자동 기록한 것과 내가 직접 쓴 것을 시각적으로 구분한다.
- **UI 크롬/버튼/내비:** iOS 시스템 폰트(SF Pro) 그대로. 네이티브 개인 도구이므로 커스텀 폰트가 오히려 어색함 — "브랜드 퍼포먼스 서핏"이 아니라 "폰 안에 원래 있던 도구"처럼 느껴져야 함.
- **시간/자동 기록 메타데이터:** 모노스페이스(SF Mono 계열, tabular). 예: `09:21`. "이건 시스템이 기록했다"는 신호.
- **내가 직접 쓴 메모/저널 텍스트:** 이탤릭 세리프(Newsreader, Google Fonts — 무료, 가벼움). "이건 내가 썼다"는 신호. **이 서체는 반드시 사용자 작성 텍스트에만 사용 — UI 라벨이나 시스템 문구에는 절대 쓰지 않음.**
- **로딩:** Newsreader는 Google Fonts CDN에서 로드(`fonts.googleapis.com`), SF Pro/SF Mono는 iOS 네이티브라 로딩 불필요.
- **스케일:**
  - 화면 타이틀(오늘 날짜 등): 22px, 600
  - 장소명(리스트 행): 16px, 500, 시스템 폰트
  - 시간(모노스페이스): 15px, 500, tabular-nums
  - 메모/저널(세리프 이탤릭): 15px, 400, line-height 1.5
  - 보조 텍스트(빈 슬롯 안내 등): 13px, 400, 시스템 폰트

## Color
- **Approach:** restrained — 액센트 1개, 절대 늘리지 않음("두 번째 액센트는 게이미피케이션이 옆문으로 들어오는 것").
- **Background:** `#F4F1EA` (웜 오프화이트)
- **Surface (카드/바텀시트):** `#FBFAF6`
- **Surface soft (구분 영역):** `#ECE8DF`
- **Text primary:** `#2F302C` (진짜 검정 아님, 웜 니어블랙)
- **Text muted:** `#79786F`
- **Text faint (placeholder 등):** `#A7A49A`
- **Accent (올리브그린, 유일한 포인트):** `#7C8660` — 체크인 버튼, 현재 위치 링, 지도 마크, 이동 궤적선, **오늘 표시(멀티셀렉트 달력의 "오늘" 밑줄), 스크러버의 선택 위치 표시(선택된 틱마크 + 중앙 고정 인디케이터)**에만 사용(2026-08-23 확장 — `/plan-design-review`, 두 문서가 독립적으로 이 용도를 추가하며 드리프트가 생겨 한 번에 공식화). "좋다"는 뜻이 아니라 "지금·기록됨·살아있음"이라는 뜻으로만 씀 — 완료/실패 이분법과 절대 짝짓지 않음. 확장된 두 용도 모두 "지금 보고 있는/가리키는 지점"이라는 동일한 의미 축이라 원래 정의를 벗어나지 않음.
- **Accent soft (배경용 옅은 올리브):** `#D8DDC9`
- **Line/구분선:** `#DDD8CD`
- **지도 전용:** land `#E9E4D8`, road `#D2CDC1`, water `#DDE3DF`
- **Semantic 색상 없음:** success/warning/error/info 색상 자체를 두지 않는다 — 저장 실패 등 오류 상태도 muted 톤의 텍스트로만 표현(문서의 "저장 실패 시 사용자 대면 UI" 참고), 빨강 계열 사용 금지.
- **Dark mode:** 1단계 스코프 아님(창업자 개인 도구, 필요 시 2단계에서 표면 재설계 — 채도 낮추기보다 배경/표면 자체를 재정의할 것).

## Spacing
- **Base unit:** 8px
- **Density:** comfortable
- **Scale:** 2xs(4) xs(8) sm(12) md(16) lg(24) xl(32) 2xl(48) 3xl(64)

## Layout
- **Approach:** hybrid — 지도는 비대칭/가장자리 정렬(컨트롤은 모서리에), 설정/리스트 화면은 그리드 규칙적(iOS 표준 그룹 리스트).
- **네비게이션 셸 (2026-08-23 수정 — 아래 Decisions Log 참고):** 하단 상시 탭바(오늘 / 캘린더) 2탭. 탭바는 오늘 뷰와 캘린더 탭의 "홈" 화면(월 그리드)에서만 노출되고, 그 안에서 push되는 상세 화면(과거 단일 날짜 화면·멀티셀렉트 결과 화면 등 "기록에 집중"하는 화면)에서는 숨김 — 상세 화면에서는 이전과 동일하게 지도가 화면 전체를 차지. **하루 마무리 화면은 이 목록에 해당 없음(2026-08-23 `/plan-eng-review` 수정)** — push가 아니라 모달로 화면 전체가 탭바까지 통째로 덮으며 뜨는 방식이라, 애초에 "탭바를 숨길지" 결정 자체가 필요 없음(`docs/designs/day-end-reflection-map.md` Premise 5 참고).
- **오늘 뷰 구조:** 지도가 상단 대부분을 차지(탭바 노출 화면이라 완전한 엣지투엣지는 아님, 하단은 탭바) + 바텀시트 3단 스냅(CLOSED/DRAGGING/OPEN, 220ms). 체크인 버튼은 시트 상태와 무관하게 지도 위에 항상 플로팅, 탭바 위 레이어에 위치(가려지지 않음). 바텀시트 CLOSED 피크·OPEN 최대 높이는 탭바 상단을 기준으로 계산(화면 물리적 하단이 아님) — 탭바 높이 49pt + 세이프 에어리어 별도.
- **바텀시트 리스트:** 실제 체크인만 시간순 표시(2026-08-22 체크인 모델 자유형 재정의로 슬롯 개념 자체가 폐기됨 — 미래 슬롯도, 놓친 과거의 점선 원도 없음. 완료된 행만 화살표와 함께 존재). 오늘 아직 체크인이 없으면 리스트 영역엔 옅은 안내 텍스트 한 줄만("아직 기록이 없어요") — 지도 위에 겹쳐 띄우지 않음(대비 보장을 위해 바텀시트 표면에만 배치).
- **Border radius:** sm(4px, 뱃지 등) md(8px, 리스트 행 hover 등) lg(16px, 바텀시트 상단 모서리) full(9999px, 체크인 알약 버튼, 탭 아이콘 강조 없음 — 시스템 기본 탭바 스타일)
- **Max content width:** 해당 없음(모바일 전용, 반응형 스코프 아님 — 창업자 본인 기기 하나만 타겟).

## Motion
- **Approach:** minimal-functional — 오직 이해를 돕는 전환만. 장식적 모션 없음.
- **바텀시트 스냅:** 220ms
- **확인 핀 드롭:** 160ms — 축하하듯 튀는 바운스 없음. "측량사 십자선이 조용히 자리잡는" 느낌: ease-out, overshoot 없음.
- **저장 상태 전환:** 180ms 크로스페이드
- **Easing:** enter(ease-out) exit(ease-in) move(ease-in-out) — 전부 절제됨, bounce/spring 이징 사용 안 함.

## Decisions Log
| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-08-22 | DESIGN.md 최초 작성 (`/design-consultation`) | `/plan-design-review` Pass 5에서 지적된 "무드로만 정의된 색상/타이포" gap 해소. Codex + Claude 서브에이전트 아웃사이드 보이스 종합 |
| 2026-08-22 | 타이포그래피 3계층 분리(시스템/모노스페이스/세리프) 채택 | Claude 서브에이전트 제안 — "시스템이 기록한 것"과 "내가 쓴 것"을 시각적으로 구분하는 게 저비용 고효과 차별점이라 판단. 커스텀 폰트는 이 세리프 하나뿐(Newsreader), 나머지는 전부 시스템 네이티브 |
| 2026-08-22 | 오늘 뷰 바텀시트 리스트에서 미래(아직 안 온) 시간대 행을 완전히 제거 | 목업 리뷰 중 사용자가 지적 — 미래 빈 슬롯을 미리 다 보여주면 사용자가 시각적으로 "오늘 몇 개 채웠는지"를 암산할 수 있게 되어, CRITICAL 원칙(완료율 숫자 비노출)을 우회하는 셈. 리스트는 지나간 시간대만 표시하고 하루가 진행되며 append-only로 늘어남. 빈 슬롯(점선 원)의 의미가 "미래 예정"에서 "지나갔지만 놓친 과거"로 명확해짐 |
| 2026-08-22 | 이동 궤적선(오늘 체크인들을 잇는 얇은 점선)을 지도에 추가, 색상은 accent 재사용 | `/plan-design-review`에서 발견된 critical gap(보상 신호 전무) 해소. 거리/시간/고도 등 통계 라벨은 명시적으로 배제(목업 생성 과정에서 AI가 반복적으로 km/고도/좌표를 끼워 넣어 거부됨) |
| 2026-08-23 | Accent 색상 승인 용도를 4개에서 6개로 확장(오늘 표시 밑줄, 스크러버 선택 위치 표시 추가) | `calendar-multiselect-view.md`와 `calendar-date-scrubber.md`가 같은 세션에서 독립적으로 accent를 새 용도에 쓰기 시작한 드리프트를 Codex+Claude 서브에이전트가 각각 발견 — 두 문서를 따로 고치는 대신 DESIGN.md 자체를 갱신해 공식화. "지금 보고 있는/가리키는 지점"이라는 원래 의미 축과 일치해 새 색상 토큰 도입 없이 확장 |
| 2026-08-23 | 오늘 뷰의 "지도 엣지투엣지" 원칙을 수정 — 하단에 상시 탭바(오늘/캘린더) 도입, 지도는 상단 대부분만 차지 | `calendar-multiselect-view.md`의 진입점 미정 gap을 계기로 검토. 캘린더 아이콘/햄버거 메뉴 방식은 발견성이 낮거나(숨은 제스처) chrome을 늘리는 트레이드오프가 있었는데, 창업자가 직접 "지도 공간이 줄어드는 건 감수하겠다"고 확정 — 발견성이 가장 높은 표준 iOS 패턴을 의도적으로 선택. 대신 "기록에 집중"하는 상세 화면(T10, 멀티셀렉트 결과, 하루 마무리)에서는 탭바를 숨겨 지도가 다시 전체를 차지하도록 절충 — `calendar-date-scrubber.md`의 132pt 플로팅 카드 계산은 이 절충 덕에 재계산 없이 그대로 유효 |
| 2026-08-23 | 바텀시트 리스트의 "놓친 행 = 점선 원" 문구를 DESIGN.md에서 제거, 빈 상태 안내 텍스트 위치를 지도 위에서 바텀시트 표면으로 이동 | `/autoplan` Design Review — 같은 날(2026-08-22) 있었던 체크인 모델 자유형 재정의(문서 원본 참고) 이후 DESIGN.md가 갱신되지 않아 stale해진 것을 Codex+Claude 서브에이전트 둘 다 독립적으로 발견. 지도 위 옅은 텍스트는 명도 대비 보장이 안 돼(4.5:1 기준 위반 위험) 바텀시트로 이동 |
| 2026-08-28 | 확인 핀 형상을 점(dot) 마커에서 구글맵 스타일 물방울(teardrop) 마커로 변경 (색상은 accent/accentSoft 유지, 빨강 도입 안 함) | 창업자가 실사용 중 점 마커가 iOS 네이티브 "내 위치" 파란 점과 겹쳐 구분이 어렵다고 지적. 색만 바꾸는 대신 모양을 바꿔 해결 — 색이 같아도 모양이 다르면 겹쳐도 안 헷갈림. "빨강 계열 금지" 원칙은 그대로 유지, SVG 등 새 의존성 추가 없이 순수 View/StyleSheet 회전 기법으로 구현 |
