---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - eas.json
  - app.json
  - .gitignore
autonomous: false
requirements: [REQ-foundation-setup]
user_setup:
  - service: apple-developer
    why: "EAS Build가 iOS 개발용 인증서와 프로비저닝 프로파일을 발급하려면 Apple Developer 계정 인증이 필요하다. 2FA 코드 입력은 사람만 할 수 있다."
    env_vars: []
    dashboard_config:
      - task: "Apple Developer Program 유료 등급($99/년) 활성 상태 확인 — 무료 Personal Team은 7일 프로비저닝 만료로 로컬 SQLite 데이터가 날아간다"
        location: "developer.apple.com/account — Membership 탭"
      - task: "테스트 대상 iPhone의 UDID를 EAS 내부 배포 대상에 등록 (eas device:create 실행 시 자동 안내)"
        location: "EAS CLI 대화형 프롬프트 또는 developer.apple.com/account/resources/devices"
  - service: expo-eas
    why: "클라우드 빌드 실행 및 빌드 아티팩트 배포 링크 발급"
    env_vars: []
    dashboard_config:
      - task: "EAS 계정 `jaeseungchoun` 로그인 상태 확인 (`eas whoami`)"
        location: "터미널 / expo.dev 대시보드"

must_haves:
  truths:
    - "창업자가 EAS Dev Client 빌드를 자신의 iPhone에 설치할 수 있다"
    - "설치된 앱을 실행하면 스플래시가 걷힌 뒤 부팅 확인 화면이 표시된다"
    - "부팅 확인 화면에 DB 스키마 버전이 표시되어 SQLite 마이그레이션이 실기기에서 성공했음을 확인할 수 있다"
    - "부팅 확인 화면의 저널 샘플 문장이 이탤릭 세리프로 렌더되어 Newsreader 번들 폰트 로딩 성공을 확인할 수 있다"
    - "Dev Client가 로컬 Metro 서버에 연결되어 JS 번들을 로드한다"
  artifacts:
    - path: "eas.json"
      provides: "REQ-foundation-setup: development 빌드 프로필"
      contains: "\"developmentClient\": true"
    - path: "app.json"
      provides: "EAS 프로젝트 연결 (extra.eas.projectId)"
      contains: "projectId"
  key_links:
    - from: "eas.json"
      to: "expo-dev-client"
      via: "development 프로필의 developmentClient 플래그"
      pattern: "developmentClient"
    - from: "app.json"
      to: "EAS 프로젝트"
      via: "extra.eas.projectId"
      pattern: "projectId"
---

<objective>
EAS Dev Client 빌드 프로필을 구성하고 iOS 개발 빌드를 생성해, 창업자가 자신의 iPhone에
설치·실행할 수 있음을 실기기에서 확인한다.

Purpose: REQ-foundation-setup 및 ROADMAP Success Criteria 1("창업자가 EAS Dev Client 빌드를
자신의 iPhone에 설치하고 실행할 수 있다")을 충족한다. `react-native-maps`가 네이티브
모듈을 요구하므로 Expo Go는 사용할 수 없다는 것이 PROJECT.md의 LOCKED 제약이며, Phase 2
이후 모든 phase가 이 Dev Client 위에서 개발된다. 실기기 설치·실행 확인은 코드나 CI로
자동 검증할 수 없어(RESEARCH.md Environment Availability) human-verify가 필수다.
Output: `eas.json` development 프로필, EAS 프로젝트 연결, 창업자 iPhone에 설치된 Dev Client
빌드, Phase 1 전체 산출물의 실기기 육안 검증 결과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<known_assumptions>
RESEARCH.md의 Assumptions Log 중 이 플랜에서 실제로 검증/해소되는 항목:

- **A1 (번들 식별자):** Plan 01-01에서 `com.jaeseungchoun.footlog`로 확정했다.
  `eas build:configure`가 이 값을 그대로 쓰는지 확인하고, 다른 값으로 덮어쓰지 않는다.
- **A2 (EAS 플랜 등급):** `resourceClass: "m-medium"`이 창업자 EAS 계정 플랜에서 사용
  가능한지 **미확인**이다. 빌드가 큐에서 무한 대기하거나 리소스 클래스 오류로 실패하면
  `eas.json`에서 `ios.resourceClass`를 제거해 EAS 기본값으로 되돌린다(제거해도 development
  프로필은 정상 동작한다).
- **A3 (Newsreader 웨이트):** 실기기에서 저널 샘플 문장이 이탤릭 세리프로 보이지 않으면
  웨이트 이름이 틀린 것이다 — Plan 01-02의 `JOURNAL_FONT_FAMILY` 값을 재확인해야 한다.
  이 플랜의 human-verify 체크 항목이 그 검출 지점이다.
</known_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: eas.json development 빌드 프로필을 작성하고 EAS 프로젝트를 연결한다</name>
  <files>eas.json, app.json, .gitignore</files>
  <read_first>
    - /Users/lovelyalien/Documents/workspace/FootLog2/app.json — Plan 01-01이 확정한 `slug`, `ios.bundleIdentifier`, `platforms` 현재 값
    - /Users/lovelyalien/Documents/workspace/FootLog2/package.json — 설치된 `expo-dev-client` 존재 확인
    - /Users/lovelyalien/Documents/workspace/FootLog2/.gitignore — 자격증명/빌드 산출물 제외 항목 현재 상태
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-RESEARCH.md — "Code Examples" 의 `eas.json` 블록, "Architecture Patterns" 의 시스템 다이어그램(EAS 빌드 흐름), "Environment Availability" 표(eas-cli 20.5.1, 계정 로그인 확인됨)
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-PATTERNS.md — `eas.json` 항목의 official-recipe 블록
  </read_first>
  <action>
    1. `eas whoami`로 EAS 로그인 상태를 확인한다. 로그인되어 있지 않으면 Task 2의
       checkpoint로 넘긴다(로그인 자체가 사람이 해야 하는 인증 단계).
    2. 저장소 루트에 `eas.json`을 작성한다. 구조:
       - `cli.version`: `">= 20.5.1"` (환경에 설치된 eas-cli 버전)
       - `cli.appVersionSource`: `"remote"` (EAS가 빌드 번호를 관리 — 로컬 수동 증가 불필요)
       - `build.development`: `developmentClient: true`, `distribution: "internal"`,
         `ios.resourceClass: "m-medium"`, `channel: "development"`
       - `build.preview`와 `build.production` 프로필은 이 phase에서 만들지 않는다
         (배포는 1단계 스코프 밖 — 창업자 본인 기기 1대만 타겟).
    3. `npx eas-cli@latest build:configure --platform ios`를 실행한다. 이 커맨드는
       EAS 프로젝트를 생성/연결하고 `app.json`의 `extra.eas.projectId`와 `owner` 필드를
       채운다. 실행 후 `app.json`을 열어 다음을 확인한다:
       - `expo.ios.bundleIdentifier`가 `com.jaeseungchoun.footlog`에서 변경되지 않았을 것
       - `expo.slug`이 `footlog`에서 변경되지 않았을 것
       - `expo.extra.eas.projectId`가 UUID 형태로 채워졌을 것
       변경되었다면 Plan 01-01의 확정값으로 되돌리고 `projectId`만 남긴다.
       이 커맨드가 Apple 계정 인증을 요구하면 즉시 중단하고 Task 2로 넘긴다 —
       `build:configure` 자체는 Apple 인증 없이 완료 가능해야 한다.
    4. `.gitignore`에 `credentials.json`, `*.p8`, `*.p12`, `*.mobileprovision`, `*.ipa`,
       `.easignore` 가 포함되어 있는지 확인하고 누락분을 추가한다 — EAS 자격증명 파일이
       저장소에 커밋되는 경로를 차단한다(T-1-05).
    5. `npx eas-cli@latest build:inspect --platform ios --profile development --stage pre-build --output /tmp/eas-inspect`
       또는 `npx eas-cli@latest config --platform ios --profile development` 로
       프로필이 스키마 오류 없이 해석되는지 확인한다.
  </action>
  <verify>
    <automated>node -e "const e=require('./eas.json'); const d=e.build.development; if(d.developmentClient!==true)throw new Error('developmentClient'); if(d.distribution!=='internal')throw new Error('distribution'); const a=require('./app.json').expo; if(a.ios.bundleIdentifier!=='com.jaeseungchoun.footlog')throw new Error('bundleId drift'); if(a.slug!=='footlog')throw new Error('slug drift'); if(!a.extra||!a.extra.eas||!a.extra.eas.projectId)throw new Error('missing projectId'); console.log('eas config ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f eas.json` 이 성공한다.
    - `node -e "const d=require('./eas.json').build.development; if(d.developmentClient!==true)throw 1; if(d.distribution!=='internal')throw 2"` 가 종료코드 0으로 끝난다.
    - `node -e "const a=require('./app.json').expo; if(!/^[0-9a-f-]{36}$/.test(a.extra.eas.projectId))throw new Error('projectId not a uuid')"` 가 종료코드 0으로 끝난다.
    - `node -e "const a=require('./app.json').expo; if(a.ios.bundleIdentifier!=='com.jaeseungchoun.footlog')throw 1; if(a.slug!=='footlog')throw 2"` 가 종료코드 0으로 끝난다(01-01 확정값 무드리프트).
    - `node -e "const e=require('./eas.json'); if(e.build.production||e.build.preview)throw new Error('out-of-scope profile added')"` 가 종료코드 0으로 끝난다.
    - `grep -cE "credentials\.json|\*\.p8|\*\.p12|\*\.mobileprovision" .gitignore` 가 4 이다.
    - `git status --porcelain | grep -cE "\.p8$|\.p12$|\.mobileprovision$|credentials\.json"` 가 0 이다.
    - `npx eas-cli@latest config --platform ios --profile development` 가 스키마 오류 없이 프로필을 출력한다.
  </acceptance_criteria>
  <done>`eas.json`의 development 프로필이 유효하고, EAS 프로젝트가 연결되었으며, 자격증명 파일이 git에서 제외된다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Claude가 자동화한 것: Expo SDK 57 스캐폴드, 5개 런타임 의존성 설치, iOS 앱 설정
    (`com.jaeseungchoun.footlog` / 세로모드 / light), 디자인 토큰 상수 파일, Newsreader
    번들 폰트 등록, SQLite 마이그레이션 프레임워크, 루트 레이아웃 배선, 부팅 확인 화면,
    jest 테스트 스위트(전부 green), `eas.json` development 프로필, EAS 프로젝트 연결.

    Claude가 할 수 없는 것: Apple Developer 계정 로그인과 **2단계 인증(2FA) 코드 입력**.
    EAS Build는 iOS 개발용 인증서(Distribution Certificate)와 프로비저닝 프로파일을
    발급하기 위해 Apple 계정 인증을 요구하며, 2FA 코드는 창업자 본인의 신뢰된 기기로만
    수신된다. 자동화 경로가 존재하지 않는다.
  </what-built>
  <how-to-verify>
    아래를 저장소 루트(`/Users/lovelyalien/Documents/workspace/FootLog2`)에서 직접 실행하세요.

    1. iPhone을 내부 배포 대상으로 등록합니다(최초 1회):
       `npx eas-cli@latest device:create`
       → "Website" 방식을 선택하면 등록용 링크/QR이 나옵니다. iPhone에서 열어 프로파일을
         설치하면 UDID가 Apple Developer 계정에 등록됩니다.

    2. 개발 빌드를 실행합니다:
       `npx eas-cli@latest build --platform ios --profile development`
       → Apple Developer 계정 로그인(Apple ID + 비밀번호)과 **2FA 코드**를 물어봅니다.
       → "Generate a new Apple Distribution Certificate?" / "Generate a new provisioning
         profile?" 류의 질문에는 기존 자격증명이 없다면 모두 **Yes**로 답합니다.
       → 등록된 기기 목록이 나오면 1번에서 등록한 iPhone을 선택합니다.

    3. 빌드는 EAS 클라우드에서 진행되며 보통 10~25분 걸립니다. 완료되면 CLI가 QR 코드와
       expo.dev 다운로드 링크를 출력합니다. 이 링크를 이 세션에 알려주세요.

    확인해야 할 것:
    - `Build finished` 또는 빌드 상태가 `finished` 로 끝났는가?
    - 빌드 상세 페이지의 Bundle Identifier가 `com.jaeseungchoun.footlog` 인가?

    문제가 생겼을 때:
    - 빌드가 큐에서 오래 대기하거나 리소스 클래스 오류가 나면 → `eas.json`의
      `build.development.ios.resourceClass` 줄을 지우고 다시 실행하세요(RESEARCH.md 가정 A2).
    - "no devices registered" 오류가 나면 → 1번 단계를 먼저 완료하세요.
  </how-to-verify>
  <resume-signal>빌드 완료 후 "빌드 완료" 또는 빌드 링크를 알려주세요. 실패했다면 CLI가 출력한 에러 메시지를 붙여넣어 주세요.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    EAS Dev Client iOS 빌드가 완성되어 설치 링크가 발급되었습니다. 이 빌드에는 Phase 1의
    세 요구사항 산출물이 모두 포함되어 있습니다 — Expo/EAS 스캐폴드(REQ-foundation-setup),
    DESIGN.md 디자인 토큰(REQ-design-tokens), SQLite 마이그레이션 프레임워크
    (REQ-sqlite-migrations). 부팅 확인 화면이 세 가지를 한 화면에서 동시에 보여주도록
    만들어져 있습니다.
  </what-built>
  <how-to-verify>
    **1단계 — 설치**
    1. iPhone에서 빌드 링크(또는 QR 코드)를 엽니다.
    2. "Install" 을 눌러 Dev Client 앱을 설치합니다.
    3. 최초 실행 시 "신뢰할 수 없는 개발자" 경고가 뜨면
       설정 → 일반 → VPN 및 기기 관리 → 해당 개발자 프로파일 → "신뢰"를 누릅니다.

    **2단계 — Metro 연결**
    4. Mac의 저장소 루트에서 `npx expo start` 를 실행합니다.
    5. iPhone과 Mac이 **같은 Wi-Fi**에 있는지 확인하고, iPhone에서 방금 설치한 앱을 열어
       터미널에 표시된 개발 서버에 연결합니다(앱 안에서 서버 목록을 탭하거나 QR 스캔).

    **3단계 — 육안 확인 (각 항목을 하나씩 확인해 주세요)**
    - [ ] **앱이 실행된다** — 스플래시가 잠깐 보인 뒤 화면이 나타난다.
          (스플래시에서 멈춰 있으면 Newsreader 폰트 로딩 실패 신호입니다.)
    - [ ] **배경이 웜 오프화이트다** — 순백(#FFFFFF)이 아니라 살짝 노란 기가 도는
          아이보리(`#F4F1EA`)여야 합니다.
    - [ ] **"FootLog" 제목이 iOS 시스템 폰트(SF Pro)로 보인다** — 굵기 semibold, 22px.
    - [ ] **`schema v1` 형태의 진단 텍스트가 모노스페이스(고정폭)로 보인다** —
          숫자 `1`이 표시된다면 SQLite 마이그레이션이 실기기에서 성공한 것입니다.
          여기에 `schema v0`이나 빈 값이 보이면 마이그레이션이 실패한 것입니다.
    - [ ] **저널 샘플 문장이 이탤릭 세리프(기울어진 명조체)로 보인다** —
          제목과 확연히 다른 서체여야 합니다. 여기가 시스템 폰트로 보이면 Newsreader
          번들 로딩이 실패한 것입니다(RESEARCH.md 가정 A3 반증).
    - [ ] **어디에도 빨강/초록/노랑 같은 상태 색이 없다** — DESIGN.md에는 semantic
          컬러 자체가 없습니다.
    - [ ] **어디에도 "3/8" 같은 진행률 숫자가 없다** — CRITICAL 원칙.
    - [ ] **앱을 완전히 종료했다가 다시 열어도 `schema v1`이 그대로 보인다** —
          마이그레이션이 재실행 시에도 안전하다는 뜻입니다.

    **참고:** 이 화면은 Phase 4(오늘 뷰)가 완전히 대체할 임시 확인 화면입니다.
    디자인 완성도를 평가하는 화면이 아니라, 3개 요구사항이 실기기에서 동작하는지
    확인하는 진단 화면입니다.
  </how-to-verify>
  <resume-signal>모든 항목이 통과하면 "approved"라고 알려주세요. 체크 실패한 항목이 있으면 어떤 항목이 어떻게 달랐는지(예: "저널 문장이 이탤릭이 아니라 일반 고딕으로 보임") 설명해 주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 소스 → EAS 클라우드 빌드 서버 | 소스 전체가 서드파티 클라우드로 업로드된다 |
| Apple Developer 계정 → 코드사인 자격증명 | 인증서/프로비저닝 프로파일이 로컬과 EAS에 저장된다 |
| EAS 빌드 아티팩트 링크 → 설치 가능한 .ipa | 링크를 아는 사람은 앱을 설치할 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Spoofing | EAS Dev Client 빌드 배포 링크 | mitigate | `distribution: "internal"` + Apple Developer 유료 계정의 프로비저닝 프로파일로 등록된 UDID 기기에만 설치 가능. 추가 통제는 expo.dev 배포 링크를 공개 채널(이슈/PR 본문/스크린샷)에 공유하지 않는 것 — checkpoint 지시문이 링크를 세션 내에서만 주고받도록 안내 |
| T-1-05 | Information Disclosure | 코드사인 자격증명 파일의 저장소 커밋 | mitigate | Task 1이 `.gitignore`에 `credentials.json`, `*.p8`, `*.p12`, `*.mobileprovision`을 강제하고, acceptance criteria가 `git status`에 해당 확장자가 나타나지 않음을 검증 |
| T-1-10 | Elevation of Privilege | Apple Developer 계정 자격증명 취급 | transfer | Apple ID 로그인·2FA는 EAS CLI와 Apple의 인증 흐름에 위임된다. Claude는 어떤 단계에서도 Apple ID·비밀번호·2FA 코드를 요청하거나 저장하지 않는다 — 이 때문에 Task 2가 `checkpoint:human-action`으로 분리되어 있다 |
| T-1-11 | Tampering | 소스가 EAS 클라우드 빌드 서버로 업로드됨 | accept | Expo/EAS는 이 프로젝트의 LOCKED 빌드 파이프라인이며(PROJECT.md), 1단계 코드에는 비밀값도 개인 데이터도 없다(로컬 전용, 백엔드 없음). 로컬 Xcode 빌드로 전환하는 것은 코드사인·프로비저닝 수동 관리 비용이 훨씬 크다 |
| T-1-SC | Tampering | npm 패키지 | mitigate | 이 플랜은 신규 npm 패키지를 설치하지 않는다. `eas-cli`는 `npx`로 실행되며 RESEARCH.md 환경 확인에서 20.5.1로 검증됨 |
</threat_model>

<verification>
- `node -e` 로 `eas.json` / `app.json` 스키마 및 확정값 드리프트 검증 → 종료코드 0
- `npx eas-cli@latest config --platform ios --profile development` → 스키마 오류 0건
- `git status --porcelain` → 자격증명 파일 0건
- EAS 빌드 상태 → `finished`
- 실기기 육안 체크리스트 8항목 → 전부 통과 (human-verify)
</verification>

<success_criteria>
- `eas.json`에 development 프로필이 존재하고 `developmentClient: true` / `distribution: "internal"`이다.
- `app.json`에 EAS `projectId`가 연결되어 있고, 번들 식별자·슬러그가 Plan 01-01 확정값에서
  드리프트하지 않았다.
- EAS iOS 개발 빌드가 성공적으로 완료되었다.
- 창업자가 자신의 iPhone에 Dev Client를 설치하고 실행할 수 있다 (ROADMAP Success Criteria 1).
- 실기기에서 배경색·3계층 타이포·DB 스키마 버전이 확인되어, REQ-design-tokens와
  REQ-sqlite-migrations가 시뮬레이터가 아닌 실제 기기에서 동작함이 검증되었다.
- 자격증명 파일이 저장소에 커밋되지 않았다.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
