---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - app.json
  - .gitignore
  - jest.config.js
  - expo-env.d.ts
  - app/_layout.tsx
  - app/index.tsx
  - assets/
  - src/test-infra.smoke.test.ts
autonomous: true
requirements: [REQ-foundation-setup]

must_haves:
  truths:
    - "개발자가 저장소 루트에서 `npx tsc --noEmit`를 실행하면 타입 에러 없이 통과한다"
    - "개발자가 `npm test`를 실행하면 jest가 기동되어 최소 1개 테스트를 green으로 통과시킨다"
    - "`app/` 디렉터리에 템플릿 데모 라우트(탭/샘플 컴포넌트)가 남아 있지 않고 최소 골격만 존재한다"
    - "expo-sqlite / expo-dev-client / expo-font / expo-splash-screen / @expo-google-fonts/newsreader 가 dependencies에 설치되어 있다"
  artifacts:
    - path: "package.json"
      provides: "Expo SDK 57 의존성 + npm test 스크립트 + jest-expo peer override"
      contains: "expo-sqlite"
    - path: "tsconfig.json"
      provides: "expo/tsconfig.base 확장 + strict 모드"
      contains: "\"strict\": true"
    - path: "app.json"
      provides: "iOS 전용 / 세로모드 / 번들 식별자 설정"
      contains: "com.jaeseungchoun.footlog"
    - path: "jest.config.js"
      provides: "jest-expo 프리셋 테스트 러너 설정"
      contains: "jest-expo"
  key_links:
    - from: "package.json"
      to: "jest.config.js"
      via: "npm test 스크립트가 jest 실행"
      pattern: "\"test\":.*jest"
    - from: "jest.config.js"
      to: "jest-expo"
      via: "preset"
      pattern: "preset.*jest-expo"
---

<objective>
빈 저장소에 Expo SDK 57 프로젝트를 스캐폴드하고, Phase 1~8 전체가 의존할 런타임 의존성과
테스트 인프라(jest-expo)를 설치한다.

Purpose: 이 저장소에는 현재 `package.json`조차 없다. 나머지 모든 플랜(디자인 토큰, SQLite
마이그레이션, 루트 레이아웃 배선, EAS 빌드)이 이 플랜의 산출물 위에서만 실행될 수 있다.
또한 RESEARCH.md가 "Wave 0 Gaps"로 플래그한 테스트 프레임워크 부재를 여기서 해소해,
이후 플랜들이 `<automated>` 검증을 가질 수 있게 한다.
Output: 부팅 가능한 Expo 프로젝트 스캐폴드(`package.json`, `app.json`, `tsconfig.json`,
최소 `app/` 골격) + 동작하는 jest 테스트 러너.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@CLAUDE.md
</context>

<locked_decisions>
프로젝트 전역 LOCKED 결정 중 이 플랜에 적용되는 항목 (PROJECT.md Constraints / Key Decisions,
RESEARCH.md `<user_constraints>` — CONTEXT.md와 동일한 권위로 취급):

- **EAS Dev Client 필수, Expo Go 금지** — `react-native-maps`가 이후 phase에서 네이티브
  모듈을 요구하므로. `expo-dev-client`는 선택이 아니라 필수 의존성.
- **네이티브 iOS 전용, 세로모드 전용, 390×844pt 기준, 시스템 Dynamic Type만 지원.**
- **다크 모드는 1단계 스코프 아님** (DESIGN.md 명시) → `userInterfaceStyle`은 `light` 고정.
- **오프라인 우선: 네트워크 의존성 전무** — 런타임 CDN fetch 금지, 폰트를 포함한 모든
  에셋은 번들에 포함.
- **테마 라이브러리(Tamagui / react-native-unistyles 등) 도입 금지** — REQ-design-tokens가
  "단일 상수 파일"을 명시. 이 플랜에서 어떤 테마/UI 킷 패키지도 설치하지 않는다.

Claude's Discretion으로 위임된 항목에 대한 이 플랜의 확정값 (RESEARCH.md "Claude's Discretion"):
- iOS 번들 식별자: `com.jaeseungchoun.footlog` (EAS 계정 소유자 `jaeseungchoun` 기준 도메인 역순)
- Expo 슬러그: `footlog`, 앱 표시명: `FootLog`, URL scheme: `footlog`
- 패키지 매니저: npm 유지
- TypeScript `strict`: **true로 활성화** (RESEARCH Open Question 2 권고 채택)
- 테스트 프레임워크: 이후 플랜으로 미루지 않고 **이 Foundation phase에 포함** (VALIDATION.md의
  Wave 0 요구사항이 tokens/migrations 테스트를 이미 전제하고 있으므로)
</locked_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Expo SDK 57 프로젝트를 저장소 루트에 스캐폴드하고 데모 코드를 프루닝한다</name>
  <files>package.json, package-lock.json, tsconfig.json, app.json, .gitignore, expo-env.d.ts, app/_layout.tsx, app/index.tsx, assets/</files>
  <read_first>
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-RESEARCH.md — "Standard Stack", "Recommended Project Structure", "Common Pitfalls" 중 Pitfall 4(TypeScript 버전), "State of the Art"(expo init 폐기 → create-expo-app)
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-PATTERNS.md — "File Classification" 표의 config 행들(어떤 파일이 템플릿 생성물이고 어떤 파일이 손으로 쓰는 것인지)
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/PROJECT.md — Constraints 섹션(플랫폼/타겟 기기 제약)
    - /Users/lovelyalien/Documents/workspace/FootLog2/CLAUDE.md — 프로젝트 규칙
  </read_first>
  <action>
    저장소 루트(`/Users/lovelyalien/Documents/workspace/FootLog2`)에는 이미 `CLAUDE.md`,
    `DESIGN.md`, `TODOS.md`, `docs/`, `.planning/`, `.gstack/`, `.git/`가 존재한다.
    `create-expo-app`은 대상 디렉터리가 비어 있지 않으면 충돌 검사에서 중단될 수 있으므로,
    **임시 디렉터리에 생성한 뒤 루트로 복사하는 방식**을 사용한다.

    작업 트리에는 이 phase와 무관한 기존 uncommitted 변경(예: `TODOS.md`의 `M`)이 이미
    존재할 수 있다. **그것을 커밋하거나 stash 하지 말 것** — 사용자의 별개 진행 중 작업이다.
    이 태스크는 그 변경을 건드리지 않고, 자신이 새 변경을 추가하지 않았음만 증명하면 된다.

    0. **사전 스냅샷(반드시 다른 작업보다 먼저).** 아래를 실행해 기존 문서의 git 상태
       베이스라인을 원본 그대로 저장한다:
       `git status --porcelain -- CLAUDE.md DESIGN.md TODOS.md docs .planning > /tmp/footlog-01-01-git-before.txt`
       이 커맨드가 종료코드 0으로 끝나는지 확인한다(실패하면 이후 비교가 무의미해지므로
       원인을 먼저 해결한다). 파일이 비어 있어도 정상이다. 이 스냅샷은 acceptance criteria의
       before/after 비교에 사용된다.
    1. `TMP=$(mktemp -d)` 로 임시 경로를 만들고 `npx create-expo-app@latest "$TMP/FootLog" --template default`
       를 실행한다. `default` 템플릿은 TypeScript + `expo-router`를 기본 포함한다
       (RESEARCH.md Primary recommendation).
    2. 생성된 트리에서 다음만 저장소 루트로 복사한다: `package.json`, `package-lock.json`,
       `tsconfig.json`, `app.json`, `.gitignore`, `expo-env.d.ts`, `app/`, `assets/`,
       그리고 템플릿이 만든 프루닝 스크립트 디렉터리(`scripts/`)와 데모 소스 디렉터리
       (`components/`, `hooks/`, `constants/`) — 존재하는 것만.
       **`README.md`는 복사하지 않는다.** 기존 `CLAUDE.md`, `DESIGN.md`, `TODOS.md`,
       `docs/`, `.planning/`, `.gstack/`는 절대 덮어쓰지 않는다.
    3. 저장소 루트에서 `npm install`을 실행해 `node_modules`를 채운다.
    4. 데모 코드를 프루닝한다: `printf 'n\n' | npm run reset-project` — `default` 템플릿이
       제공하는 공식 프루닝 스크립트로, 데모 탭 라우트/컴포넌트를 **삭제**하고
       `app/_layout.tsx`(expo-router `Stack` 하나)와 `app/index.tsx`(플레이스홀더) 최소
       골격만 남긴다(`n` 응답 = `app-example/`로 옮기지 말고 삭제).
       `reset-project` 스크립트가 없거나 실패하면 수동으로 `app/(tabs)/`, `components/`,
       `hooks/`, `constants/`, `scripts/`, `app-example/`를 삭제하고 `app/_layout.tsx`에는
       `expo-router`의 `Stack` 하나만, `app/index.tsx`에는 `View` + `Text` 플레이스홀더
       하나만 남긴다. 프루닝 후 `scripts/` 디렉터리와 `package.json`의 `reset-project`
       스크립트 항목도 제거한다.
    5. `tsconfig.json`: `extends`가 `"expo/tsconfig.base"`인지 확인하고,
       `compilerOptions.strict`를 `true`로 설정한다. `compilerOptions.paths`의 `@/*` 별칭은
       템플릿 기본값 그대로 유지한다.
    6. `package.json`의 `typescript` devDependency 버전은 **템플릿이 pin한 값을 그대로 둔다**
       — `npm view typescript version`이 알려주는 7.x로 임의 상향하지 말 것
       (RESEARCH.md Pitfall 4: TS 7.x는 네이티브 포팅 메이저 릴리스라 Expo 생태계 검증 미완).
    7. `.gitignore`에 `node_modules/`, `.expo/`, `dist/`, `ios/`, `android/`, `.env*`,
       `*.log`가 포함되어 있는지 확인하고 누락분을 추가한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f package.json && test -f app.json && test -f tsconfig.json` 이 모두 성공한다.
    - `node -e "const p=require('./package.json'); if(!p.dependencies.expo) process.exit(1); if(!p.dependencies['expo-router']) process.exit(1); console.log(p.dependencies.expo)"` 가 성공하고 `expo` 버전이 `57.` 로 시작한다.
    - `node -e "const t=require('./tsconfig.json'); if(t.extends!=='expo/tsconfig.base') process.exit(1); if(t.compilerOptions.strict!==true) process.exit(1)"` 가 종료코드 0으로 끝난다.
    - `ls app/` 출력에 `(tabs)` 가 포함되지 않고, `_layout.tsx` 와 `index.tsx` 가 포함된다.
    - `test ! -d components && test ! -d hooks && test ! -d constants && test ! -d app-example` 가 성공한다.
    - `npx tsc --noEmit` 이 종료코드 0으로 끝난다.
    - 이 태스크가 기존 프로젝트 문서에 **새로운** 수정을 추가하지 않았다. 다음 한 줄이 종료코드 0으로 끝난다:
      `git status --porcelain -- CLAUDE.md DESIGN.md TODOS.md docs .planning > /tmp/footlog-01-01-git-after.txt && diff <(grep -E '^( M|M |MM)' /tmp/footlog-01-01-git-before.txt | sort) <(grep -E '^( M|M |MM)' /tmp/footlog-01-01-git-after.txt | sort)`
      (`&&`는 git 자체가 실패했을 때 빈 결과끼리 비교되어 거짓 통과하는 것을 막는다.)
      이는 "이 경로들의 M이 절대적으로 0건"을 요구하는 것이 **아니다** — 태스크 시작 전부터
      존재하던 변경(예: 사용자의 미커밋 `TODOS.md`)은 그대로 남아 있어도 통과하며,
      오직 이 태스크가 추가한 델타만 검사한다.
  </acceptance_criteria>
  <done>Expo SDK 57 스캐폴드가 저장소 루트에 존재하고, 데모 코드가 제거된 최소 `app/` 골격만 남았으며, strict 모드에서 타입 체크가 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: 런타임 의존성을 설치하고 iOS 앱 설정을 확정한다</name>
  <files>package.json, package-lock.json, app.json</files>
  <read_first>
    - /Users/lovelyalien/Documents/workspace/FootLog2/package.json — Task 1이 생성한 현재 의존성 목록
    - /Users/lovelyalien/Documents/workspace/FootLog2/app.json — Task 1이 생성한 현재 Expo 설정
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-RESEARCH.md — "Standard Stack" 표(각 패키지의 검증된 버전과 도입 이유), "Package Legitimacy Audit"(전 패키지 Approved 판정), "Installation" 커맨드 블록
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/PROJECT.md — Constraints(iOS 전용/세로모드/다크모드 제외)
  </read_first>
  <action>
    1. 다음 런타임 의존성을 **`npx expo install`로** 설치한다(버전을 직접 pin하지 말 것 —
       `expo install`이 SDK 57 호환 버전을 자동 해석한다):
       `npx expo install expo-dev-client expo-sqlite expo-font expo-splash-screen @expo-google-fonts/newsreader`
       이 5개 패키지는 RESEARCH.md의 Package Legitimacy Audit에서 전부 `Approved`
       (slopcheck `OK`, `[ASSUMED]`/`[SUS]`/`[SLOP]` 없음)이므로 별도 인간 승인 게이트가 필요 없다.
       **이 목록 외의 패키지를 추가로 설치하지 않는다** — 특히 테마/UI 라이브러리는 금지.
    2. `app.json`의 `expo` 객체를 다음 확정값으로 설정한다:
       - `name`: `"FootLog"`
       - `slug`: `"footlog"`
       - `scheme`: `"footlog"`
       - `version`: `"1.0.0"`
       - `orientation`: `"portrait"`
       - `userInterfaceStyle`: `"light"` (다크 모드는 1단계 스코프 아님)
       - `platforms`: `["ios"]` (네이티브 iOS 전용)
       - `ios.bundleIdentifier`: `"com.jaeseungchoun.footlog"`
       - `ios.supportsTablet`: `false` (창업자 iPhone 1대만 타겟)
       템플릿이 넣어둔 `plugins` 배열(`expo-router`, `expo-splash-screen` 등)과
       `newArchEnabled`, `icon`, `splash` 관련 키는 그대로 유지한다.
       `android` 블록이 있으면 제거한다(iOS 전용).
       `ios.infoPlist`의 권한 사용 문구(`NSLocationWhenInUseUsageDescription` 등)는
       **이 phase에서 작성하지 않는다** — REQ-permission-copy(Phase 2) 스코프.
    3. `npx expo install --check`를 실행해 SDK 57과 버전이 어긋난 패키지가 없는지 확인하고,
       보고되면 제안된 버전으로 맞춘다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const p=require('./package.json');const need=['expo-dev-client','expo-sqlite','expo-font','expo-splash-screen','@expo-google-fonts/newsreader'];const miss=need.filter(n=>!p.dependencies[n]);if(miss.length){console.error('missing:',miss);process.exit(1)}console.log('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');['expo-dev-client','expo-sqlite','expo-font','expo-splash-screen','@expo-google-fonts/newsreader'].forEach(n=>{if(!p.dependencies[n])throw new Error(n)})"` 가 종료코드 0으로 끝난다.
    - `node -e "const a=require('./app.json').expo;if(a.slug!=='footlog')throw 1;if(a.orientation!=='portrait')throw 2;if(a.userInterfaceStyle!=='light')throw 3;if(a.ios.bundleIdentifier!=='com.jaeseungchoun.footlog')throw 4;if(a.ios.supportsTablet!==false)throw 5;if(JSON.stringify(a.platforms)!=='[\"ios\"]')throw 6"` 가 종료코드 0으로 끝난다.
    - `node -e "const a=require('./app.json').expo;if(a.android)throw new Error('android block present')"` 가 종료코드 0으로 끝난다.
    - `node -e "const p=require('./package.json');const bad=Object.keys(p.dependencies).filter(k=>/tamagui|unistyles|nativewind|styled-components|react-native-paper/i.test(k));if(bad.length)throw new Error('theme lib installed: '+bad)"` 가 종료코드 0으로 끝난다.
    - `npx expo install --check` 가 버전 불일치를 보고하지 않는다.
  </acceptance_criteria>
  <done>Phase 1~8이 필요로 하는 5개 Expo 런타임 패키지가 SDK 57 호환 버전으로 설치되었고, `app.json`이 iOS 전용·세로모드·확정 번들 식별자로 설정되었다.</done>
</task>

<task type="auto">
  <name>Task 3: jest-expo 테스트 인프라를 구축한다 (VALIDATION.md Wave 0)</name>
  <files>package.json, jest.config.js, src/test-infra.smoke.test.ts</files>
  <read_first>
    - /Users/lovelyalien/Documents/workspace/FootLog2/package.json — Task 2 이후의 의존성/스크립트 현재 상태, 특히 설치된 `react-native` 정확한 버전
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-VALIDATION.md — Wave 0 Requirements 체크리스트, Quick/Full run command, "no watch-mode flags" 규칙, 피드백 지연 10초 목표
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-RESEARCH.md — "Common Pitfalls" 중 Pitfall 2(jest-expo ↔ SDK 57 피어 의존성 충돌과 `overrides` 해법), "Validation Architecture" 섹션
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-PATTERNS.md — `jest.config.js` / 테스트 파일 항목과 "needs a mocking strategy" 주석
  </read_first>
  <action>
    1. 테스트 의존성을 devDependency로 설치한다:
       `npm install --save-dev jest-expo@~57.0.4 jest@^30.4.2 @testing-library/react-native@^14.0.1`
       설치 중 피어 의존성 경고가 나면 2번 단계의 `overrides`를 먼저 추가한 뒤 재설치한다.
    2. **Pitfall 2 대응 (필수):** `package.json`에 최상위 `overrides` 필드를 추가한다 —
       `"overrides": { "@react-native/jest-preset": "<설치된 react-native 정확한 버전>" }`.
       버전은 `node -p "require('./package.json').dependencies['react-native']"`로 읽은
       값에서 범위 기호(`^`, `~`)를 제거한 정확한 버전을 쓴다(RESEARCH.md는 `0.86.0`을
       예상값으로 기록하고 있으나, 실제 설치된 값이 우선이다). 추가 후 `npm install` 재실행.
    3. `package.json`의 `scripts`에 다음을 추가한다:
       - `"test": "NODE_OPTIONS=--experimental-sqlite jest"`
       `NODE_OPTIONS=--experimental-sqlite`는 Plan 01-03이 `node:sqlite`의 `DatabaseSync`로
       실제 SQLite 엔진에 대해 마이그레이션을 검증하기 위해 필요하다(Node v22.21.1에서
       `node:sqlite`는 이 플래그 뒤에 있으며, 로컬에서 동작 확인됨).
       **watch 모드 스크립트는 추가하지 않는다**(VALIDATION.md sign-off 규칙).
    4. `jest.config.js`를 생성한다. 목표 설정:
       - `preset`: `'jest-expo/ios'` (단일 플랫폼 프리셋 — 이 phase에는 웹/안드로이드
         타깃이 없다). 이 프리셋 이름이 해석되지 않으면 `'jest-expo'`로 되돌린다.
       - `testMatch`: `src/` 와 `app/` 하위의 `**/*.test.ts`, `**/*.test.tsx`
       - `testPathIgnorePatterns`: `/node_modules/`, `/.expo/`
       - `clearMocks: true`
       Node 환경이 필요한 개별 테스트는 파일 상단 `@jest-environment node` docblock으로
       지정한다(Plan 01-03이 이 방식을 사용한다).
    5. `src/test-infra.smoke.test.ts`를 만든다. 이 파일은 테스트 러너 자체가 살아 있음을
       증명하는 최소 스모크 테스트로, (a) 산술 단언 하나와 (b) `NODE_OPTIONS`가 전달되어
       `require('node:sqlite')`가 던지지 않고 로드되는지 확인하는 단언 하나를 포함한다
       (후자는 `@jest-environment node` docblock이 필요하다).
    6. `npm test`가 green이 될 때까지 프리셋/transformIgnorePatterns를 조정한다.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` 가 종료코드 0으로 끝나고 출력에 `1 passed` 이상의 테스트 통과가 표시된다.
    - `test -f jest.config.js` 가 성공하고, `grep -v '^\s*//' jest.config.js | grep -c "jest-expo"` 가 1 이상이다.
    - `node -e "const p=require('./package.json');if(!/jest/.test(p.scripts.test))throw 1;if(!/experimental-sqlite/.test(p.scripts.test))throw 2;if(/--watch/.test(JSON.stringify(p.scripts)))throw 3"` 가 종료코드 0으로 끝난다.
    - `node -e "const p=require('./package.json');if(!p.overrides||!p.overrides['@react-native/jest-preset'])throw new Error('missing jest-preset override')"` 가 종료코드 0으로 끝난다.
    - `node -e "const p=require('./package.json');['jest','jest-expo','@testing-library/react-native'].forEach(n=>{if(!p.devDependencies[n])throw new Error(n)})"` 가 종료코드 0으로 끝난다.
    - `npm test` 실행 벽시계 시간이 30초 미만이다(VALIDATION.md 피드백 지연 목표).
    - `npm install` 이 `@react-native/jest-preset` 관련 peer dependency 경고 없이 완료된다.
  </acceptance_criteria>
  <done>`npm test`가 green이며, 이후 플랜들이 `<automated>` 검증에 사용할 jest 러너와 `node:sqlite` 플래그 전달 경로가 확보되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → 로컬 `node_modules` | 서드파티 코드가 개발 머신과 최종 앱 번들로 유입되는 유일한 경로 |
| 로컬 저장소 → git 원격 | 자격증명/시크릿이 유출될 수 있는 경로 |

이 phase에는 네트워크 호출, 인증, 사용자 입력 폼이 존재하지 않으므로 ASVS V2/V3/V4/V6는 N/A
(RESEARCH.md Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | `npm install` / `npx expo install` 로 유입되는 전 패키지 | mitigate | RESEARCH.md의 Package Legitimacy Audit 표에서 slopcheck 0.6.1 실행 결과 전 패키지 `OK`/`Approved`, `[ASSUMED]`/`[SUS]`/`[SLOP]` 0건 — 따라서 blocking human checkpoint 불필요. 단, Task 2 acceptance criteria가 감사 표에 없는 테마/UI 라이브러리 설치를 grep 게이트로 차단한다 |
| T-1-05 | Information Disclosure | `.gitignore` / 저장소 커밋 | mitigate | Task 1에서 `.env*`, `.expo/`, `ios/`, `android/`를 `.gitignore`에 강제 — EAS 자격증명·환경변수가 커밋되는 경로를 사전 차단 |
| T-1-06 | Tampering | `tsconfig.json` strict 비활성화로 인한 타입 우회 | mitigate | `compilerOptions.strict: true` 를 Task 1 acceptance criteria에서 기계 검증 — 이후 phase에서 조용히 꺼지는 것을 방지 |
</threat_model>

<verification>
- `npx tsc --noEmit` → 종료코드 0
- `npm test` → 종료코드 0, 스모크 테스트 통과
- `npx expo install --check` → 버전 불일치 0건
- `/tmp/footlog-01-01-git-before.txt` ↔ `/tmp/footlog-01-01-git-after.txt` 의 수정(M) 항목 diff → 종료코드 0
  (기존 프로젝트 문서 `CLAUDE.md`/`DESIGN.md`/`TODOS.md`/`docs/`/`.planning/` 에 이 플랜이
  **새로 추가한** 수정 없음. 플랜 실행 이전부터 있던 uncommitted 변경은 그대로 유지되며 위반이 아니다.)
</verification>

<success_criteria>
- Expo SDK 57 프로젝트가 저장소 루트에 스캐폴드되어 있고 strict TypeScript 체크가 통과한다.
- `expo-dev-client`, `expo-sqlite`, `expo-font`, `expo-splash-screen`,
  `@expo-google-fonts/newsreader` 5개가 SDK 57 호환 버전으로 설치되어 있다.
- `app.json`이 `com.jaeseungchoun.footlog` / iOS 전용 / 세로모드 / light 로 확정되어 있다.
- `npm test`가 green이고, VALIDATION.md의 Wave 0 "Framework install" 및 "jest.config.js"
  항목이 충족된다.
- 템플릿 데모 라우트·컴포넌트가 남아 있지 않다.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
