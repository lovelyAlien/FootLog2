---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - app/_layout.tsx
  - app/index.tsx
  - app/__tests__/foundation-wiring.test.ts
autonomous: true
requirements: [REQ-design-tokens, REQ-sqlite-migrations]

must_haves:
  truths:
    - "앱이 부팅되면 Newsreader 폰트 로딩이 끝나기 전까지 스플래시가 유지되고, 로딩 완료 후에만 화면이 그려진다"
    - "앱이 부팅되면 SQLite 마이그레이션이 정확히 한 번, DB 연결 직후에 실행된다"
    - "화면 컴포넌트가 하드코딩 hex/px 없이 `src/theme/tokens.ts`에서만 시각 값을 가져온다"
    - "부팅 확인용 화면이 DB 스키마 버전과 Newsreader 렌더 결과를 표시해 실기기에서 3개 요구사항을 눈으로 확인할 수 있다"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "폰트 스플래시 게이팅 + SQLiteProvider onInit 마이그레이션 배선"
      contains: "onInit={migrateDbIfNeeded}"
    - path: "app/index.tsx"
      provides: "토큰 소비를 실증하는 부팅 확인 플레이스홀더 화면"
      contains: "src/theme/tokens"
    - path: "app/__tests__/foundation-wiring.test.ts"
      provides: "배선 계약 회귀 가드(onInit 전용 호출, 토큰 하드코딩 금지)"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/db/migrations.ts"
      via: "SQLiteProvider onInit prop"
      pattern: "onInit=\\{migrateDbIfNeeded\\}"
    - from: "app/_layout.tsx"
      to: "src/theme/fonts.ts"
      via: "useFonts(newsreaderFonts)"
      pattern: "useFonts\\(newsreaderFonts\\)"
    - from: "app/index.tsx"
      to: "src/theme/tokens.ts"
      via: "colors/typography/spacing import"
      pattern: "theme/tokens"
---

<objective>
루트 레이아웃에서 Newsreader 폰트 로딩과 SQLite 마이그레이션을 앱 부팅 시퀀스에 배선하고,
디자인 토큰이 화면 계층에서 실제로 소비됨을 실증하는 부팅 확인 화면을 만든다.

Purpose: Plan 01-02(토큰·폰트)와 Plan 01-03(마이그레이션)이 만든 모듈은 아직 어디에서도
호출되지 않는다. 이 플랜이 둘을 앱 부팅 경로에 연결해 ROADMAP Success Criteria 2("앱
화면들이 단일 상수 파일에서 토큰을 import할 수 있다")와 3("SQLite 데이터베이스가
마이그레이션 프레임워크를 통해 초기화된다")를 관측 가능한 상태로 만든다. 또한 Plan 01-05의
실기기 human-verify가 육안으로 확인할 대상 화면을 제공한다.
Output: 배선된 `app/_layout.tsx`, 부팅 확인용 `app/index.tsx`, 배선 계약 회귀 가드 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@CLAUDE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<interfaces>
<!-- Plan 01-02 / 01-03 이 이미 만든 계약. 이 플랜은 이것들을 소비만 한다 — -->
<!-- 코드베이스를 뒤져 시그니처를 찾지 말 것. -->

src/theme/tokens.ts:
  export const colors: { background, surface, surfaceSoft, textPrimary, textMuted,
                         textFaint, accent, accentSoft, line, mapLand, mapRoad, mapWater }
  export const typography: { screenTitle, placeName, timestamp, journalEntry, helperText }
  export const spacing: { '2xs','xs','sm','md','lg','xl','2xl','3xl' }
  export const motion: { bottomSheetSnapMs, confirmPinDropMs, saveStateCrossfadeMs, easing }
  export const radius: { sm, md, lg, full }

src/theme/fonts.ts:
  export const JOURNAL_FONT_FAMILY: 'Newsreader_400Regular_Italic'
  export const newsreaderFonts: Record<string, number>   // useFonts()에 그대로 전달

src/db/migrations.ts:
  export const DATABASE_NAME = 'footlog.db'
  export const DATABASE_VERSION = 1
  export type MigratableDb = Pick<SQLiteDatabase, 'getFirstAsync'|'execAsync'|'runAsync'>
  export async function migrateDbIfNeeded(db: MigratableDb): Promise<void>

expo-sqlite (외부):
  <SQLiteProvider databaseName={string} onInit={(db: SQLiteDatabase) => Promise<void>}>
  useSQLiteContext(): SQLiteDatabase   // 자식 컴포넌트에서 DB 접근

expo-splash-screen (외부):
  SplashScreen.preventAutoHideAsync(): Promise<boolean>
  SplashScreen.hideAsync(): Promise<boolean>

@expo-google-fonts/newsreader / expo-font (외부):
  useFonts(map): [boolean, Error | null]
</interfaces>

<placeholder_screen_scope>
`app/index.tsx`는 **부팅 확인용 임시 플레이스홀더**이며 제품 UI가 아니다.
- Phase 4(REQ-today-view)가 이 화면을 오늘 뷰로 완전히 대체한다. 이 사실을 파일 상단
  주석에 명시한다.
- 탭바·바텀시트·지도·체크인 버튼 등 어떤 제품 컴포넌트도 만들지 않는다(전부 Phase 2~8 스코프).
- **진행률/완료 수치 표시 절대 금지** — PROJECT.md의 CRITICAL 등급 원칙. "N/M", "3/8" 같은
  형태의 숫자가 화면에 나타나서는 안 된다. 이 화면이 표시하는 유일한 숫자는 DB 스키마
  버전(진단값)이며, 진행률이 아니다.
- 앱 표시명 "FootLog"는 priming/설정 화면에서만 노출한다는 REQ-app-name(Phase 8) 규칙이
  있으나, 그 규칙은 "매일 보는 오늘 뷰"를 대상으로 한다. 이 임시 진단 화면은 Phase 4에서
  제거되므로 "FootLog" 문자열을 표시해도 무방하다.
</placeholder_screen_scope>

<tasks>

<task type="auto">
  <name>Task 1: 루트 레이아웃에 폰트 스플래시 게이팅과 SQLiteProvider onInit 마이그레이션을 배선한다</name>
  <files>app/_layout.tsx</files>
  <read_first>
    - /Users/lovelyalien/Documents/workspace/FootLog2/app/_layout.tsx — Plan 01-01의 프루닝 이후 현재 상태(어떤 Stack/Provider가 이미 있는지)
    - /Users/lovelyalien/Documents/workspace/FootLog2/src/db/migrations.ts — `migrateDbIfNeeded` 시그니처와 `DATABASE_NAME` 상수의 실제 export 형태
    - /Users/lovelyalien/Documents/workspace/FootLog2/src/theme/fonts.ts — `newsreaderFonts` 맵의 실제 형태
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-PATTERNS.md — `app/_layout.tsx` 섹션의 "Full pattern to copy verbatim" 코드 블록과 "Critical rule (Pitfall 3)"
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-RESEARCH.md — "Architecture Patterns" 두 번째 코드 블록(`app/_layout.tsx`), "Common Pitfalls" Pitfall 3
    - /Users/lovelyalien/Documents/workspace/FootLog2/tsconfig.json — `paths` 별칭(`@/*`) 확인 후 import 경로 스타일 결정
  </read_first>
  <action>
    01-PATTERNS.md의 `app/_layout.tsx` "Full pattern to copy verbatim" 블록을 기준으로
    작성하되, 하드코딩된 값 대신 이 프로젝트의 실제 export를 사용한다.

    1. 모듈 최상단(컴포넌트 밖)에서 `SplashScreen.preventAutoHideAsync()`를 호출한다.
    2. `RootLayout` 컴포넌트에서 `const [fontsLoaded, fontError] = useFonts(newsreaderFonts);`
       — `src/theme/fonts.ts`의 `newsreaderFonts`를 그대로 전달한다(폰트 맵을 이 파일에
       다시 적지 않는다).
    3. `useEffect(() => { if (fontsLoaded || fontError) SplashScreen.hideAsync(); },
       [fontsLoaded, fontError])` 로 스플래시를 해제하고, `!fontsLoaded && !fontError`면
       `null`을 반환해 폰트 미로딩 상태의 화면 깜빡임을 막는다.
    4. 반환 트리를 `<SQLiteProvider databaseName={DATABASE_NAME} onInit={migrateDbIfNeeded}>`로
       감싸고 그 안에 `expo-router`의 `<Stack />`을 둔다.
       `Stack.Screen`의 `options`로 `headerShown: false`를 설정한다(Phase 1에는 헤더가
       필요한 화면이 없고, DESIGN.md의 네비게이션 셸은 Phase 4 스코프).
       `<SQLiteProvider>`에 `useSuspense`를 켜지 않는다(Suspense 경계를 아직 만들지 않았다).
    5. **Pitfall 3 절대 규칙:** `migrateDbIfNeeded`는 `onInit` prop으로만 전달한다.
       컴포넌트 body에서 직접 `await`하거나 `useEffect` 안에서 호출하면 안 된다 —
       `SQLiteProvider`가 DB 연결 직후 정확히 1회 실행을 보장하는 공식 API다.
    6. 이 파일에 hex 컬러나 숫자 px 값을 하드코딩하지 않는다. 배경색이 필요하면
       `colors.background`를 import해 사용한다.
    7. `npx tsc --noEmit`로 타입 체크를 통과시킨다. `SQLiteDatabase`가 `MigratableDb`에
       할당 가능하므로 `onInit={migrateDbIfNeeded}`는 캐스트 없이 통과해야 한다 —
       캐스트가 필요해지면 그것은 `MigratableDb` 정의가 잘못됐다는 신호이므로
       Plan 01-03의 정의를 확인한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onInit={migrateDbIfNeeded}" app/_layout.tsx` 가 1 이다.
    - `grep -c "databaseName={DATABASE_NAME}" app/_layout.tsx` 가 1 이다.
    - `grep -c "useFonts(newsreaderFonts)" app/_layout.tsx` 가 1 이다.
    - `grep -c "SplashScreen.preventAutoHideAsync()" app/_layout.tsx` 가 1 이고, `grep -c "SplashScreen.hideAsync()" app/_layout.tsx` 가 1 이다.
    - `grep -v '^\s*[/*]' app/_layout.tsx | grep -cE "await migrateDbIfNeeded|migrateDbIfNeeded\(" ` 가 0 이다(직접 호출 부재 — Pitfall 3).
    - `grep -v '^\s*[/*]' app/_layout.tsx | grep -cE "#[0-9A-Fa-f]{6}"` 가 0 이다(하드코딩 컬러 부재).
    - `grep -v '^\s*[/*]' app/_layout.tsx | grep -c "useSuspense"` 가 0 이다.
    - `npx tsc --noEmit` 이 종료코드 0으로 끝나고, `app/_layout.tsx` 안에 `as ` 캐스트가 없다(`grep -c " as SQLiteDatabase\| as any" app/_layout.tsx` 가 0).
    - `npm test` 전체 스위트가 종료코드 0으로 끝난다.
  </acceptance_criteria>
  <done>앱 부팅 시 Newsreader 로딩 완료까지 스플래시가 유지되고, DB 연결 직후 마이그레이션이 `onInit`을 통해 정확히 1회 실행되도록 배선되었다.</done>
</task>

<task type="auto">
  <name>Task 2: 토큰 소비를 실증하는 부팅 확인 화면과 배선 회귀 가드 테스트를 작성한다</name>
  <files>app/index.tsx, app/__tests__/foundation-wiring.test.ts</files>
  <read_first>
    - /Users/lovelyalien/Documents/workspace/FootLog2/app/index.tsx — Plan 01-01의 프루닝이 남긴 플레이스홀더 현재 상태
    - /Users/lovelyalien/Documents/workspace/FootLog2/app/_layout.tsx — Task 1이 배선한 결과(Provider 계층 확인)
    - /Users/lovelyalien/Documents/workspace/FootLog2/src/theme/tokens.ts — 소비할 토큰의 실제 키 이름과 타입
    - /Users/lovelyalien/Documents/workspace/FootLog2/src/db/migrations.ts — `DATABASE_VERSION` 상수
    - /Users/lovelyalien/Documents/workspace/FootLog2/DESIGN.md — `## Color`, `## Typography`, `## Spacing`(플레이스홀더 화면도 토큰 밖 값을 쓰면 안 됨)
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/PROJECT.md — Context 섹션의 "진행률/완료 수치는 UI 어디에도 노출 금지" CRITICAL 원칙
  </read_first>
  <action>
    1. `app/index.tsx`를 부팅 확인 화면으로 작성한다. 파일 상단 주석에 "Phase 4의
       REQ-today-view가 이 화면을 완전히 대체한다 — 제품 UI가 아니라 Foundation phase의
       부팅/배선 확인용 임시 화면"임을 명시한다.

       화면이 표시할 것 (전부 `src/theme/tokens.ts`의 값만 사용):
       - 루트 `View`: `backgroundColor: colors.background`, `padding: spacing.lg`,
         `flex: 1`, `justifyContent: 'center'`.
       - `Text` 1: 스타일 `typography.screenTitle`, 색 `colors.textPrimary`,
         내용 `FootLog` — SF Pro 시스템 폰트 계층이 렌더됨을 확인.
       - `Text` 2: 스타일 `typography.timestamp`, 색 `colors.textMuted`,
         내용은 `useSQLiteContext()`로 읽은 `PRAGMA user_version` 값을 표시하는 진단 문자열
         (예: `schema v1`). `useEffect` 안에서 `db.getFirstAsync<{ user_version: number }>('PRAGMA user_version')`를
         호출해 state에 담고, 로딩 중에는 `colors.textFaint` 색의 자리표시 문자를 보여준다.
         이 모노스페이스 렌더는 `ui-monospace` + `tabular-nums` 토큰이 동작함을 확인시킨다.
       - `Text` 3: 스타일 `typography.journalEntry`, 색 `colors.textPrimary`,
         내용은 한 줄짜리 한국어 샘플 문장(사용자 작성 텍스트를 흉내내는 예시).
         이 렌더가 이탤릭 세리프면 Newsreader 번들 로딩이 성공한 것이다.
       - `Text` 4: 스타일 `typography.helperText`, 색 `colors.textMuted`,
         내용은 이 화면이 임시 확인 화면이라는 한 줄 안내.
       - 시각적 구분이 필요하면 `colors.line`과 `radius.md`, `spacing.*`만 사용한다.

       금지: hex 리터럴, 매직 넘버 px, `accent` 컬러 사용(DESIGN.md가 승인한 6개 용도 중
       어디에도 이 진단 화면이 포함되지 않는다), 진행률/개수 표시, 애니메이션.

    2. `app/__tests__/foundation-wiring.test.ts`를 작성한다. 파일 상단에
       `@jest-environment node` docblock을 두고, `fs.readFileSync`로 소스를 읽어
       배선 계약을 정적으로 단언한다(RN 렌더 환경 불필요, 10초 이내 실행 목표):
       - Test 1: `app/_layout.tsx` 소스가 `onInit={migrateDbIfNeeded}` 를 포함한다.
       - Test 2: `app/_layout.tsx` 소스에서 주석을 제거한 본문에 `migrateDbIfNeeded(` 형태의
         직접 호출이 존재하지 않는다(Pitfall 3 회귀 가드).
       - Test 3: `app/_layout.tsx` 소스가 `useFonts(newsreaderFonts)` 와
         `SplashScreen.preventAutoHideAsync()` 를 포함한다.
       - Test 4: `app/index.tsx` 소스가 `src/theme/tokens`(또는 `@/src/theme/tokens`)에서
         `colors`, `typography`, `spacing` 을 import한다.
       - Test 5 (핵심 가드): `app/` 과 `src/` 하위의 모든 `.ts`/`.tsx` 파일을 재귀 순회하여,
         `src/theme/tokens.ts` 를 **제외한** 어떤 파일의 주석 아닌 본문에도
         `#RRGGBB`/`#RGB` 형태의 hex 컬러 리터럴이 존재하지 않음을 단언한다.
         테스트 파일(`*.test.ts`, `*.test.tsx`)은 예외로 제외한다(토큰 값을 단언해야 하므로).
       - Test 6: `app/index.tsx` 소스의 주석 아닌 본문에 `colors.accent` 가 등장하지 않는다
         (DESIGN.md의 accent 6개 승인 용도에 이 화면이 없음).
       - Test 7: `app/index.tsx` 소스에 진행률 패턴(정규식 `\b\d+\s*\/\s*\d+\b`)이 등장하지
         않는다(PROJECT.md CRITICAL 원칙 회귀 가드).
    3. `npm test` 전체 스위트와 `npx tsc --noEmit`를 green으로 만든다.
    4. `npx expo start`가 Metro 번들러를 에러 없이 기동하는지 확인한다(번들 그래프가
       실제로 해석되는지 검증 — 30초 내 "Bundled" 로그 확인 후 종료).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` 전체 스위트가 종료코드 0으로 끝나고 `app/__tests__/foundation-wiring.test.ts`의 7개 테스트가 통과한다.
    - `npx tsc --noEmit` 이 종료코드 0으로 끝난다.
    - `grep -cE "theme/tokens" app/index.tsx` 가 1 이상이다.
    - `grep -v '^\s*[/*]' app/index.tsx | grep -cE "#[0-9A-Fa-f]{3,6}\b"` 가 0 이다.
    - `grep -v '^\s*[/*]' app/index.tsx | grep -c "colors.accent"` 가 0 이다.
    - `grep -c "useSQLiteContext" app/index.tsx` 가 1 이상이고, `grep -c "PRAGMA user_version" app/index.tsx` 가 1 이다.
    - `grep -cE "typography.screenTitle|typography.timestamp|typography.journalEntry|typography.helperText" app/index.tsx` 가 4 이상이다.
    - `grep -v '^\s*[/*]' app/index.tsx | grep -cE "\b[0-9]+\s*/\s*[0-9]+\b"` 가 0 이다(진행률 수치 부재).
    - `npx expo start` 실행 시 Metro가 번들 에러 없이 기동한다(수동 확인 후 종료; 에러 로그가 있으면 이 태스크는 미완).
  </acceptance_criteria>
  <done>부팅 확인 화면이 5개 토큰 스타일과 3계층 타이포를 실제로 소비하고 DB 스키마 버전을 표시하며, 하드코딩 컬러/진행률 수치/`onInit` 우회를 막는 회귀 가드가 테스트로 고정되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 부팅 시퀀스 → 로컬 SQLite 파일 | `SQLiteProvider`가 DB를 열고 마이그레이션을 실행하는 유일한 지점 |

이 phase의 화면에는 사용자 입력 폼도, 네트워크 호출도 없다 — ASVS V2/V3/V4/V6는 N/A.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | `app/index.tsx` 의 `PRAGMA user_version` 조회 | mitigate | 조회 SQL은 리터럴 문자열 상수이며 사용자 입력이 개입하지 않는다. 이후 phase가 이 화면 자리에 실제 쿼리를 넣을 때는 `runAsync`/`getAllAsync` + `?` 플레이스홀더를 써야 한다는 선례가 Plan 01-03에 고정되어 있다 |
| T-1-03 | Denial of Service | 마이그레이션 중복 실행으로 인한 부팅 실패 | mitigate | `migrateDbIfNeeded`를 `onInit` prop으로만 전달(Task 1). Task 2 Test 2가 직접 호출 재도입을 회귀 테스트로 차단 |
| T-1-07 | Tampering | 화면 계층의 디자인 토큰 우회(하드코딩 hex) | mitigate | Task 2 Test 5가 `src/theme/tokens.ts`를 제외한 `app/`·`src/` 전체에서 hex 리터럴을 0건으로 강제 — Phase 4~8이 토큰을 우회하는 경로를 지금 봉쇄한다 |
| T-1-02 | Information Disclosure | 폰트 로딩 시 외부 CDN 접속 | mitigate | `useFonts(newsreaderFonts)`가 Plan 01-02의 번들 폰트 맵만 사용 — 런타임 네트워크 호출 경로가 존재하지 않는다 |
| T-1-SC | Tampering | npm 패키지 | mitigate | 이 플랜은 신규 패키지를 설치하지 않는다(01-01에서 감사 완료된 패키지만 사용) |
</threat_model>

<verification>
- `npm test` → 전체 스위트 green (tokens, fonts, migrations, foundation-wiring)
- `npx tsc --noEmit` → 종료코드 0
- `npx expo start` → Metro 번들 에러 0건
- `grep -rn "onInit={migrateDbIfNeeded}" app/_layout.tsx` → 1건
- `grep -rnE "#[0-9A-Fa-f]{6}" app/ src/ --include=*.tsx --include=*.ts | grep -v "src/theme/tokens.ts" | grep -v ".test."` → 0건
</verification>

<success_criteria>
- 앱 부팅 시퀀스가 폰트 로딩 게이팅 → DB 마이그레이션 → 화면 렌더 순으로 배선되어 있다.
- `migrateDbIfNeeded`가 `SQLiteProvider`의 `onInit` 외의 경로로 호출되지 않는다.
- 화면 계층이 `src/theme/tokens.ts`에서만 시각 값을 가져오며, 하드코딩 hex가 0건이다.
- 부팅 확인 화면이 SF Pro / ui-monospace / Newsreader 3계층 타이포와 DB 스키마 버전을
  동시에 표시해, Plan 01-05의 실기기 검증이 육안으로 3개 요구사항을 확인할 수 있다.
- 진행률 수치와 accent 컬러 오남용이 회귀 테스트로 차단되어 있다.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
