---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/theme/tokens.ts
  - src/theme/tokens.test.ts
  - src/theme/fonts.ts
  - src/theme/fonts.test.ts
autonomous: true
requirements: [REQ-design-tokens]

must_haves:
  truths:
    - "모든 화면 컴포넌트가 `src/theme/tokens.ts` 하나에서 컬러·타이포·스페이싱·모션·radius 토큰을 import할 수 있다"
    - "tokens.ts의 모든 값이 DESIGN.md에 문서화된 값과 정확히 일치한다"
    - "Newsreader 이탤릭 세리프가 런타임 CDN fetch 없이 번들에서 로드되는 폰트 맵으로 export된다"
    - "`npm test`가 토큰 값 검증 테스트를 green으로 통과시킨다"
  artifacts:
    - path: "src/theme/tokens.ts"
      provides: "REQ-design-tokens: DESIGN.md 토큰 단일 상수 파일"
      exports: ["colors", "typography", "spacing", "motion", "radius"]
      min_lines: 45
    - path: "src/theme/tokens.test.ts"
      provides: "DESIGN.md 값 대조 회귀 테스트"
      contains: "#7C8660"
    - path: "src/theme/fonts.ts"
      provides: "useFonts에 전달할 Newsreader 폰트 맵"
      exports: ["newsreaderFonts", "JOURNAL_FONT_FAMILY"]
  key_links:
    - from: "src/theme/fonts.ts"
      to: "@expo-google-fonts/newsreader"
      via: "번들 폰트 import (CDN fetch 아님)"
      pattern: "@expo-google-fonts/newsreader"
    - from: "src/theme/tokens.ts"
      to: "src/theme/fonts.ts"
      via: "typography.journalEntry.fontFamily === JOURNAL_FONT_FAMILY"
      pattern: "JOURNAL_FONT_FAMILY"
---

<objective>
DESIGN.md의 타이포그래피/컬러/스페이싱/모션 토큰을 모든 화면에서 import 가능한 단일
TypeScript 상수 파일로 export하고, Newsreader 이탤릭 세리프를 번들 폰트로 등록한다.

Purpose: REQ-design-tokens 및 ROADMAP Success Criteria 2("앱 화면들이 DESIGN.md와 일치하는
단일 상수 파일에서 공유 디자인 토큰을 import할 수 있다")를 충족한다. CLAUDE.md는 DESIGN.md를
모든 시각 결정의 단일 진실 소스로 못박고 있으므로, 이 파일이 Phase 4~8의 전 화면이
하드코딩 없이 참조할 유일한 지점이 된다.
Output: `src/theme/tokens.ts`(값 상수), `src/theme/fonts.ts`(폰트 맵), 그리고 두 파일에 대한
DESIGN.md 대조 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@CLAUDE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<interfaces>
<!-- 이 플랜이 CREATE하는 계약. Plan 01-04(루트 레이아웃 배선)와 Phase 4~8의 모든 화면이 -->
<!-- 이 export들을 소비한다. 시그니처는 아래에서 벗어나지 않는다. -->

src/theme/tokens.ts (신규 — 이 플랜이 생성):
  export const colors: {
    background, surface, surfaceSoft, textPrimary, textMuted, textFaint,
    accent, accentSoft, line, mapLand, mapRoad, mapWater
  } — 전부 `#RRGGBB` 대문자 hex 문자열, `as const`
  export const typography: {
    screenTitle, placeName, timestamp, journalEntry, helperText
  } — 각 항목은 React Native `TextStyle` 호환 객체, `as const`
  export const spacing: { '2xs','xs','sm','md','lg','xl','2xl','3xl' } — number, `as const`
  export const motion: {
    bottomSheetSnapMs, confirmPinDropMs, saveStateCrossfadeMs,
    easing: { enter, exit, move }
  } — `as const`
  export const radius: { sm, md, lg, full } — number, `as const`

src/theme/fonts.ts (신규 — 이 플랜이 생성):
  export const JOURNAL_FONT_FAMILY: 'Newsreader_400Regular_Italic'
  export const newsreaderFonts: Record<string, number>
    — `useFonts(newsreaderFonts)` 에 그대로 전달 가능한 폰트 맵
</interfaces>

<design_source_of_truth>
아래 값은 `DESIGN.md`(저장소 루트)에서 그대로 옮겨온 것이며, **이 플랜에서 새 토큰을
발명하거나 값을 조정하지 않는다**(CLAUDE.md: "Do not deviate without explicit user approval",
01-PATTERNS.md: "Do not invent additional tokens beyond what DESIGN.md specifies").
실행자는 반드시 `DESIGN.md`를 직접 읽고 아래 표와 대조한 뒤 작성한다.

**Color** (DESIGN.md `## Color`)
| 토큰 | 값 | DESIGN.md 근거 |
|---|---|---|
| background | `#F4F1EA` | 웜 오프화이트 |
| surface | `#FBFAF6` | 카드/바텀시트 |
| surfaceSoft | `#ECE8DF` | 구분 영역 |
| textPrimary | `#2F302C` | 웜 니어블랙 |
| textMuted | `#79786F` | |
| textFaint | `#A7A49A` | placeholder 등 |
| accent | `#7C8660` | 올리브그린, 승인된 6개 용도 전용 |
| accentSoft | `#D8DDC9` | 배경용 옅은 올리브 |
| line | `#DDD8CD` | 구분선 |
| mapLand | `#E9E4D8` | 지도 전용 |
| mapRoad | `#D2CDC1` | 지도 전용 |
| mapWater | `#DDE3DF` | 지도 전용 |

DESIGN.md는 **semantic 색상(success/warning/error/info)을 두지 않는다** — 이 파일에
그런 키를 추가하면 안 된다. 오류는 `textMuted`로 표현한다.

**Typography** (DESIGN.md `## Typography` 스케일)
| 토큰 | fontFamily | fontSize | fontWeight | 비고 |
|---|---|---|---|---|
| screenTitle | `System` | 22 | `'600'` | 화면 타이틀(오늘 날짜 등) |
| placeName | `System` | 16 | `'500'` | 장소명(리스트 행) |
| timestamp | `ui-monospace` | 15 | `'500'` | `fontVariant: ['tabular-nums']` — 시스템이 기록한 시간 |
| journalEntry | `Newsreader_400Regular_Italic` | 15 | `'400'` | `lineHeight: 22.5` (15 × 1.5) — 사용자 작성 텍스트 전용 |
| helperText | `System` | 13 | `'400'` | 보조 텍스트 |

`ui-monospace`는 RN이 iOS에서 공식 지원하는 제네릭 패밀리로, SF Mono 폰트 파일을 별도
번들링할 필요가 없다(RESEARCH.md "Don't Hand-Roll").

**Spacing** (DESIGN.md `## Spacing`, base unit 8px)
`2xs: 4, xs: 8, sm: 12, md: 16, lg: 24, xl: 32, 2xl: 48, 3xl: 64`

**Radius** (DESIGN.md `## Layout` → Border radius)
`sm: 4, md: 8, lg: 16, full: 9999`

**Motion** (DESIGN.md `## Motion`)
`bottomSheetSnapMs: 220`, `confirmPinDropMs: 160`, `saveStateCrossfadeMs: 180`,
`easing: { enter: 'ease-out', exit: 'ease-in', move: 'ease-in-out' }`
— bounce/spring 이징 값은 **존재해서는 안 된다**.
</design_source_of_truth>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DESIGN.md 토큰을 src/theme/tokens.ts 단일 상수 파일로 전사한다</name>
  <files>src/theme/tokens.test.ts, src/theme/tokens.ts</files>
  <read_first>
    - /Users/lovelyalien/Documents/workspace/FootLog2/DESIGN.md — 전체. 특히 `## Typography`, `## Color`, `## Spacing`, `## Layout`(Border radius), `## Motion` 섹션. 이 플랜의 `<design_source_of_truth>` 표와 대조 검증할 원본
    - /Users/lovelyalien/Documents/workspace/FootLog2/CLAUDE.md — Design System 규칙("Do not deviate without explicit user approval")
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-PATTERNS.md — `src/theme/tokens.ts` 항목(전사 대상 패턴과 "새 토큰 발명 금지" 제약)
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-RESEARCH.md — "Architecture Patterns" Pattern 1, "Alternatives Considered"(테마 엔진 도입이 왜 과설계인지)
    - /Users/lovelyalien/Documents/workspace/FootLog2/jest.config.js — Plan 01-01이 만든 testMatch/preset 설정(테스트 파일 위치·확장자 규약 확인)
  </read_first>
  <behavior>
    `src/theme/tokens.test.ts`가 먼저 실패해야 하며(RED), 다음을 단언한다:
    - Test 1: `colors`의 12개 키가 `<design_source_of_truth>` Color 표의 hex 값과 정확히 일치한다(키 개수도 정확히 12개).
    - Test 2: `colors`에 `success`/`warning`/`error`/`info`/`danger` 키가 존재하지 않는다(DESIGN.md: semantic 색상 없음).
    - Test 3: `colors`의 모든 값이 `/^#[0-9A-F]{6}$/` 를 만족한다(대문자 hex).
    - Test 4: `typography.screenTitle` = fontFamily `System`, fontSize 22, fontWeight `'600'`.
    - Test 5: `typography.placeName` = `System` / 16 / `'500'`.
    - Test 6: `typography.timestamp` = `ui-monospace` / 15 / `'500'` 이고 `fontVariant`가 `['tabular-nums']`.
    - Test 7: `typography.journalEntry` = `Newsreader_400Regular_Italic` / 15 / `'400'` 이고 `lineHeight === 22.5`.
    - Test 8: `typography.helperText` = `System` / 13 / `'400'`.
    - Test 9: `spacing`이 정확히 `{'2xs':4,'xs':8,'sm':12,'md':16,'lg':24,'xl':32,'2xl':48,'3xl':64}` 와 깊은 동등하다.
    - Test 10: `radius`가 정확히 `{sm:4,md:8,lg:16,full:9999}` 와 깊은 동등하다.
    - Test 11: `motion.bottomSheetSnapMs === 220`, `motion.confirmPinDropMs === 160`, `motion.saveStateCrossfadeMs === 180`.
    - Test 12: `motion.easing` = `{enter:'ease-out',exit:'ease-in',move:'ease-in-out'}` 이고, `motion`을 JSON 직렬화한 문자열에 `bounce` 또는 `spring`이 포함되지 않는다.
    - Test 13 (엣지케이스): `tokens.ts` 소스를 `fs.readFileSync`로 읽어, `export const` 로 시작하는 최상위 export가 정확히 `colors|typography|spacing|motion|radius` 5개뿐임을 확인한다(토큰 발명 방지).
  </behavior>
  <action>
    먼저 `DESIGN.md`를 직접 읽어 이 플랜의 `<design_source_of_truth>` 표와 값이 어긋나는
    항목이 없는지 확인한다. 어긋나면 **DESIGN.md를 우선**하고 SUMMARY에 불일치를 기록한다.

    1. `src/theme/tokens.test.ts`를 먼저 작성하고 `npm test -- src/theme/tokens.test.ts`가
       모듈 미존재로 실패(RED)하는 것을 확인한다. 파일 상단에 `@jest-environment node`
       docblock을 둔다(순수 값 객체 테스트이므로 RN 환경 불필요).
    2. `src/theme/tokens.ts`를 작성한다. 파일 최상단에 출처 주석을 남긴다 —
       DESIGN.md에서 전사했다는 사실과 전사 기준일. 다섯 개의 named export
       (`colors`, `typography`, `spacing`, `motion`, `radius`)를 전부 `as const`로 선언한다.
       default export를 만들지 않는다.
       - `colors`: 위 표의 12개 키만. 각 값 옆에 DESIGN.md의 용도 설명을 짧은 주석으로 남기고,
         `accent`에는 "정확히 6개 승인된 용도로만 사용 — DESIGN.md Color 섹션 참고" 주석을 단다.
       - `typography`: 5개 스타일. `timestamp.fontVariant`는 `['tabular-nums'] as const`,
         `journalEntry.fontFamily`는 `src/theme/fonts.ts`가 export할
         `JOURNAL_FONT_FAMILY`와 동일한 리터럴 `'Newsreader_400Regular_Italic'`.
         (Task 2에서 두 파일을 실제로 연결한다. 이 태스크에서는 리터럴로 둔다.)
       - `spacing`, `radius`, `motion`: 위 표 그대로.
       React Native 타입(`TextStyle` 등)을 import해 타입 애노테이션을 붙이지 말 것 —
       `as const`의 리터럴 타입 추론이 좁아져 테스트 단언이 깨진다. 순수 값 객체로 둔다.
    3. `npm test -- src/theme/tokens.test.ts`가 green이 될 때까지 반복한다(GREEN).
    4. `npx tsc --noEmit`로 타입 에러가 없는지 확인한다.
  </action>
  <verify>
    <automated>npm test -- src/theme/tokens.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/theme/tokens.test.ts` 가 종료코드 0으로 끝나고 13개 이상의 단언 테스트가 통과한다.
    - `grep -c "as const" src/theme/tokens.ts` 가 5 이상이다.
    - `grep -v '^\s*//' src/theme/tokens.ts | grep -cE "^export const (colors|typography|spacing|motion|radius) ="` 가 정확히 5 이다.
    - `grep -v '^\s*//' src/theme/tokens.ts | grep -cE "export default"` 가 0 이다.
    - `grep -v '^\s*//' src/theme/tokens.ts | grep -icE "success|warning|error:|danger|#(FF0000|F00)"` 가 0 이다(semantic/빨강 색상 부재).
    - `grep -v '^\s*//' src/theme/tokens.ts | grep -icE "bounce|spring"` 가 0 이다.
    - `grep -c "#7C8660" src/theme/tokens.ts` 가 1 이상이고, `grep -c "#F4F1EA" src/theme/tokens.ts` 가 1 이상이다.
    - `node -e "const t=require('./src/theme/tokens.ts')" ` 대신 `npx tsc --noEmit` 이 종료코드 0으로 끝난다.
    - `grep -v '^\s*//' src/theme/tokens.ts | grep -cE "from 'react-native'|from \"react-native\""` 가 0 이다(순수 값 객체 유지).
  </acceptance_criteria>
  <done>DESIGN.md의 컬러 12개·타이포 5종·스페이싱 8단계·radius 4단계·모션 4항목이 `src/theme/tokens.ts` 하나에서 `as const`로 export되고, 값 대조 테스트가 green이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Newsreader 이탤릭 세리프를 번들 폰트 맵으로 등록하고 tokens.ts와 연결한다</name>
  <files>src/theme/fonts.ts, src/theme/fonts.test.ts, src/theme/tokens.ts</files>
  <read_first>
    - /Users/lovelyalien/Documents/workspace/FootLog2/src/theme/tokens.ts — Task 1이 만든 현재 상태, 특히 `typography.journalEntry.fontFamily` 리터럴
    - /Users/lovelyalien/Documents/workspace/FootLog2/DESIGN.md — `## Typography` 의 Newsreader 관련 문단("이 서체는 반드시 사용자 작성 텍스트에만 사용", "로딩: Google Fonts CDN에서 로드")
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/phases/01-foundation/01-RESEARCH.md — "Summary" 문단의 CDN↔번들 기술적 간극 설명, "Anti-Patterns to Avoid"의 "Newsreader를 CDN에서 fetch로 구현 시도" 항목, "Assumptions Log" A3(웨이트 가정)
    - /Users/lovelyalien/Documents/workspace/FootLog2/.planning/PROJECT.md — Constraints의 "오프라인 우선: 1단계는 네트워크 의존성 전무"
    - /Users/lovelyalien/Documents/workspace/FootLog2/node_modules/@expo-google-fonts/newsreader/package.json — 설치된 패키지가 실제로 export하는 엔트리 확인용
  </read_first>
  <behavior>
    `src/theme/fonts.test.ts`가 다음을 단언한다:
    - Test 1: `JOURNAL_FONT_FAMILY === 'Newsreader_400Regular_Italic'`.
    - Test 2: `newsreaderFonts`가 `JOURNAL_FONT_FAMILY`를 키로 가지며, 그 값이 `undefined`/`null`이 아니다(번들 에셋 모듈 참조가 실제로 해석됨).
    - Test 3: `newsreaderFonts`의 키 집합이 `[JOURNAL_FONT_FAMILY]` 하나뿐이다(DESIGN.md가 명시한 웨이트는 15px/400 이탤릭 하나 — RESEARCH.md A3).
    - Test 4 (교차검증): `typography.journalEntry.fontFamily === JOURNAL_FONT_FAMILY` — tokens.ts와 fonts.ts가 서로 드리프트하지 않음을 보장.
    - Test 5 (엣지케이스): `fonts.ts` 소스를 `fs.readFileSync`로 읽어 `fonts.googleapis.com`, `fetch(`, `http://`, `https://` 문자열이 코드 라인(주석 제외)에 존재하지 않음을 확인 — 오프라인 우선 원칙 위반 차단.
  </behavior>
  <action>
    **중요 — DESIGN.md 문구와 구현의 의도적 차이:** DESIGN.md는 Newsreader를 "Google Fonts
    CDN(fonts.googleapis.com)에서 로드"한다고 적고 있으나 이는 웹 사고방식이며, React
    Native/Expo에서는 런타임 CDN fetch가 오프라인 우선 원칙(PROJECT.md Constraints)과 정면
    충돌한다. `@expo-google-fonts/newsreader` npm 패키지가 **동일한 폰트 파일을 앱 번들에
    포함**하므로 시각적 결과는 DESIGN.md 의도와 동일하다. 구현은 번들 방식으로 하되,
    이 차이를 `fonts.ts` 최상단 주석과 SUMMARY에 명시적으로 기록한다(디자인 의도 변경 아님,
    로딩 메커니즘만 플랫폼에 맞게 치환).

    1. `src/theme/fonts.test.ts`를 먼저 작성해 RED를 확인한다(`@jest-environment node` docblock).
    2. `src/theme/fonts.ts`를 작성한다:
       - `@expo-google-fonts/newsreader`에서 `Newsreader_400Regular_Italic`을 import한다.
         설치된 패키지의 실제 export 이름은 `node_modules/@expo-google-fonts/newsreader`에서
         확인한다(서브패스 export 형태일 수 있음).
       - `export const JOURNAL_FONT_FAMILY = 'Newsreader_400Regular_Italic' as const;`
       - `export const newsreaderFonts = { [JOURNAL_FONT_FAMILY]: Newsreader_400Regular_Italic };`
         — 이 객체는 Plan 01-04의 `app/_layout.tsx`에서 `useFonts(newsreaderFonts)`로
         그대로 소비된다.
       - 최상단 주석: 이 파일이 유일한 커스텀 폰트 등록 지점이며, 이 서체는 사용자 작성
         텍스트(메모/저널) 전용이고 UI 라벨/시스템 문구에는 절대 사용하지 않는다는
         DESIGN.md 규칙을 남긴다.
    3. `src/theme/tokens.ts`의 `typography.journalEntry.fontFamily`를 하드코딩 리터럴에서
       `JOURNAL_FONT_FAMILY` import 참조로 교체한다. 이때 `as const` 추론이 유지되도록
       `JOURNAL_FONT_FAMILY`를 `as const` 리터럴 타입으로 선언해 둔다. Task 1의
       `tokens.test.ts` Test 7과 Test 13이 계속 green이어야 한다(Test 13의 최상위 export
       개수 규칙은 `tokens.ts`에만 적용되며, import 추가는 이를 위반하지 않는다).
    4. `npm test`(전체 스위트) + `npx tsc --noEmit`가 모두 green이 될 때까지 반복한다.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` 전체 스위트가 종료코드 0으로 끝난다(Task 1의 tokens 테스트 포함 전부 green).
    - `grep -c "@expo-google-fonts/newsreader" src/theme/fonts.ts` 가 1 이상이다.
    - `grep -v '^\s*[/*]' src/theme/fonts.ts | grep -icE "fonts\.googleapis\.com|fetch\(|https?://"` 가 0 이다.
    - `grep -c "JOURNAL_FONT_FAMILY" src/theme/tokens.ts` 가 1 이상이다(tokens.ts가 fonts.ts를 참조).
    - `grep -v '^\s*//' src/theme/tokens.ts | grep -c "'Newsreader_400Regular_Italic'"` 가 0 이다(리터럴 중복 제거 완료).
    - `npx tsc --noEmit` 이 종료코드 0으로 끝난다.
  </acceptance_criteria>
  <done>`newsreaderFonts`가 `useFonts`에 바로 전달 가능한 형태로 export되고, `tokens.ts`의 저널 타이포가 그 폰트 패밀리를 단일 출처로 참조하며, 런타임 CDN fetch 경로가 코드에 존재하지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음) | 이 플랜은 정적 상수와 번들 에셋만 다룬다 — 네트워크·사용자 입력·인증 경계가 존재하지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Information Disclosure | `src/theme/fonts.ts` 의 런타임 폰트 로딩 | mitigate | DESIGN.md 문구를 문자 그대로 구현해 `fonts.googleapis.com`을 런타임 fetch하면 앱 사용 사실이 외부 CDN에 노출되고 오프라인 원칙도 깨진다. Task 2의 acceptance criteria가 소스 내 `https?://`·`fetch(` 문자열을 grep 게이트로 차단하고, `@expo-google-fonts/newsreader` 번들 방식만 허용한다 |
| T-1-07 | Tampering | `src/theme/tokens.ts` 값 드리프트 | mitigate | AI 생성 목업이 반복적으로 semantic 컬러(빨강 오류색)와 진행률 UI를 재도입한 이력이 있다(PROJECT.md Key Decisions). Task 1 Test 2/Test 13 + grep 게이트가 semantic 컬러 키와 미승인 토큰 추가를 회귀 테스트로 봉쇄한다 |
| T-1-SC | Tampering | npm 패키지 | mitigate | 이 플랜은 신규 패키지를 설치하지 않는다(01-01에서 감사 완료된 `@expo-google-fonts/newsreader`만 사용). 테마 라이브러리 도입 금지는 01-01 Task 2의 grep 게이트가 이미 강제 |
</threat_model>

<verification>
- `npm test` → 전체 스위트 green (tokens 13+ 단언, fonts 5 단언)
- `npx tsc --noEmit` → 종료코드 0
- `grep -rn "#7C8660" src/ app/ --include=*.ts --include=*.tsx` → `src/theme/tokens.ts` 외의
  파일에 accent hex가 하드코딩되어 있지 않다
- DESIGN.md 직접 대조: 실행자가 읽은 DESIGN.md 값과 `<design_source_of_truth>` 표 사이에
  불일치가 있었다면 SUMMARY에 기록되어 있다
</verification>

<success_criteria>
- `src/theme/tokens.ts`가 `colors`/`typography`/`spacing`/`motion`/`radius` 5개를 `as const`로
  export하며, 모든 값이 DESIGN.md와 정확히 일치한다.
- semantic 색상 키와 bounce/spring 이징 값이 존재하지 않는다.
- Newsreader가 번들 폰트로 등록되어 `useFonts`에 전달 가능한 `newsreaderFonts` 맵이 export된다.
- `typography.journalEntry.fontFamily`가 `JOURNAL_FONT_FAMILY` 단일 출처를 참조한다.
- `npm test` 전체 스위트가 green이다.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
