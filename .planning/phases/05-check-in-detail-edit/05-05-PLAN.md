---
phase: 05-check-in-detail-edit
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - src/today/pendingDelete.ts
  - src/today/pendingDelete.test.ts
  - src/today/UndoSnackbar.tsx
  - src/today/content.ts
  - src/today/CheckinListRow.tsx
  - src/today/TodayBottomSheet.tsx
  - src/today/__tests__/todayUi.test.ts
  - src/app/(tabs)/index/index.tsx
  - src/app/__tests__/checkin-wiring.test.ts
autonomous: true
requirements: [REQ-checkin-detail-base, REQ-checkin-swipe-delete]

must_haves:
  truths:
    - "오늘 뷰 바텀시트의 체크인 행을 탭하면 해당 체크인의 상세화면이 열린다"
    - "행을 왼쪽으로 스와이프하면 오른쪽에서 테라코타(colors.pin) 삭제 어포던스가 나오고, 임계값을 넘기면 확인 다이얼로그 없이 리스트에서 즉시 사라진다"
    - "삭제 직후 4초 동안 \"실행취소\"를 누르면 체크인이 리스트로 되돌아오고 DB에서 지워지지 않는다"
    - "4초가 지나면 실제로 DB에서 삭제되며, 첨부 사진 파일도 함께 정리된다"
    - "삭제 대기 중에 화면을 떠나면 삭제가 취소되지 않고 즉시 확정된다"
    - "메모/사진 유무와 무관하게 모든 체크인 행에 동일하게 적용된다"
  artifacts:
    - path: "src/today/pendingDelete.ts"
      provides: "지연 삭제(4초) 컨트롤러 — 타이머/확정/취소 순수 로직"
      contains: "export function createPendingDeleteController"
    - path: "src/today/UndoSnackbar.tsx"
      provides: "4초 undo 스낵바 프레젠테이셔널 컴포넌트"
      contains: "export function UndoSnackbar"
    - path: "src/today/pendingDelete.test.ts"
      provides: "지연 삭제 타이밍/취소/언마운트 커밋 유닛 테스트"
  key_links:
    - from: "src/today/CheckinListRow.tsx"
      to: "react-native-gesture-handler/ReanimatedSwipeable"
      via: "스와이프 제스처"
      pattern: "ReanimatedSwipeable"
    - from: "src/app/(tabs)/index/index.tsx"
      to: "checkinRepo.deleteCheckin"
      via: "지연 삭제 컨트롤러의 onCommit"
      pattern: "deleteCheckin\\("
    - from: "src/app/(tabs)/index/index.tsx"
      to: "expo-router push /[id]"
      via: "행 탭 → 상세화면"
      pattern: "router\\.push"
---

<objective>
Phase 4가 D-03에 따라 **의도적으로 탭 불가능하게** 남겨뒀던 리스트 행을 뒤집어 상세화면
진입점으로 만들고, 같은 행에 스와이프 삭제 + 4초 undo 스낵바를 얹는다.

핵심 설계는 **지연 삭제(delayed commit)**다: 스와이프 즉시 DB `DELETE`를 실행하고 undo 시
재삽입하는 방식이 아니라, UI에서만 즉시 숨기고 실제 `DELETE`는 4초 타이머 만료 시점에
실행한다. 재삽입 방식은 원래 row의 `created_at`/`id` 메타데이터를 복원해야 해서 더
복잡하다(05-UI-SPEC.md가 이미 지연 삭제를 권장, 05-RESEARCH.md §Alternatives Considered가
확정).

Purpose: 이 plan은 기존 회귀 가드 **1개**를 의도적으로 반전시킨다 —
`src/today/__tests__/todayUi.test.ts`의 Test 1("Pressable/onPress가 등장하지 않는다")은
Phase 4가 D-03에 따라 만든 계약이고, Phase 5가 그 계약을 명시적으로 바꾼다. 이 테스트를
"고장난 테스트"로 오해해 조용히 지우는 일이 없도록 반전 근거를 테스트 파일 주석에 남기는
것까지가 이 plan의 범위다.

반면 같은 파일의 **Test 6("`colors.accent`가 등장하지 않는다")은 반전 대상이 아니다** —
삭제 어포던스 배경이 `colors.pin`(테라코타 `#B85C38`)이므로 이 단언은 수정 없이 그대로
통과해야 한다. DESIGN.md 2026-08-31 18:10 갱신이 accent의 승인 용도를 캘린더 탭 전용
2개(오늘 표시 밑줄, 스크러버 선택 위치)로 좁히면서 체크인 관련 색상 정체성을 전부 Pin으로
이전했고, 2026-09-01 Decisions Log가 이 어포던스를 명시적으로 Pin으로 확정했다. 대신
"`colors.pin`이 정확히 1회, 삭제 어포던스 배경에만 등장한다"는 **새 색상 가드를 추가**한다.
Output: 탭 가능·스와이프 가능한 리스트 행 + 지연 삭제 컨트롤러 + undo 스낵바.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-check-in-detail-edit/05-CONTEXT.md
@.planning/phases/04-today-view/04-CONTEXT.md
@.planning/phases/05-check-in-detail-edit/05-RESEARCH.md
@.planning/phases/05-check-in-detail-edit/05-PATTERNS.md
@.planning/phases/05-check-in-detail-edit/05-UI-SPEC.md
@DESIGN.md
</context>

<interfaces>
<!-- 실행자가 코드베이스를 뒤지지 않아도 되도록 미리 추출한 계약 -->

05-02-PLAN.md가 만든 함수(이 plan이 소비):
```ts
export async function deleteCheckin(db: MigratableDb, id: string): Promise<void>;
// src/checkin/config.ts
export type PhotoStorageDeps = {
  copyIntoDocumentDirectory(sourceUri: string, fileName: string): Promise<string>;
  deleteFile(uri: string): Promise<void>;
};
```

`src/today/CheckinListRow.tsx`의 **현재** 계약(이 plan이 바꾼다):
```tsx
export const LIST_ROW_MIN_HEIGHT = 44;
export type CheckinListRowProps = { checkin: CheckinRow };
export function CheckinListRow({ checkin }: CheckinListRowProps)  // 순수 View, 탭 불가
```
파일 헤더 8~13줄에 D-01/D-02/D-03 계약 주석이 있고, D-03이 "이 행은 탭 불가능하다"고
명시한다 — 이 주석 블록을 이번 phase의 반전 근거로 **교체**해야 한다.

`src/today/TodayBottomSheet.tsx`의 현재 props(확장 대상):
```tsx
export type TodayBottomSheetProps = {
  checkins: CheckinRow[];
  containerHeight: number;
  animatedPosition: SharedValue<number>;
};
// renderItem={({ item }) => <CheckinListRow checkin={item} />}
```

`src/today/content.ts`의 현재 상수(여기에 문구를 추가한다):
```ts
export const TODAY_COPY = {
  tabToday: '오늘', tabCalendar: '캘린더',
  calendarPlaceholder: '캘린더는 곧 추가돼요',
  emptyState: '아직 기록이 없어요 · 체크인하면 지도가 채워져요',
} as const;
```

스와이프 제스처 API(05-RESEARCH.md Pattern 3, `node_modules` 소스로 검증):
```tsx
import ReanimatedSwipeable from 'react-native-gesture-handler/ReanimatedSwipeable';
import { SwipeDirection } from 'react-native-gesture-handler';
// props: friction, rightThreshold, overshootRight, renderRightActions,
//        onSwipeableOpen(direction), activeOffsetX, failOffsetY
```
구 `Swipeable`(class)는 소스에 `@deprecated`로 표시돼 있다 — 절대 import하지 않는다.

토큰(05-UI-SPEC.md 확정):
- 삭제 어포던스: 폭 `72`, 배경 **`colors.pin`**(`#B85C38` 테라코타 = 체크인 정체성 색).
  `colors.accent`(올리브)가 **아니다** — DESIGN.md 2026-08-31 18:10 갱신이 accent를 캘린더 탭
  전용 2개로 좁히고 체크인 관련 색을 전부 Pin으로 이전했으며, 2026-09-01 Decisions Log가 이
  어포던스를 Pin으로 확정했다. `colors.pinSoft`도 아니다 — pinSoft는 "저장된 핀/궤적선"용
  옅은 톤이라 어포던스 배경으로 쓰면 삭제 의미가 흐려진다.
  아이콘은 `SymbolView name="trash"` + `tintColor={colors.surface}`, `accessibilityLabel` "삭제"
- 스낵바: 배경 `colors.textPrimary`, 텍스트 `colors.surface`,
  `typography.placeName`, 내부 좌우 패딩 `spacing.md`, 크로스페이드
  `motion.saveStateCrossfadeMs`(= 180)
- `SymbolView` import 선례: `src/components/CheckinActionCard.tsx:25`

**주의 — 건드리면 안 되는 기존 회귀 가드:**
`src/app/__tests__/checkin-wiring.test.ts` Test 9와 Test 39가
"`(tabs)/index/index.tsx` 소스에 `react-native-gesture-handler`가 등장하지 않는다"를 단언한다.
스와이프 제스처는 반드시 `src/today/CheckinListRow.tsx` 안에만 두고, 부모 화면 파일에는
`react-native-gesture-handler`를 **절대 import하지 않는다**. 이 두 테스트는 유효한 계약이므로
수정 대상이 아니다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 지연 삭제 컨트롤러(순수 로직) + undo 스낵바 컴포넌트 + 문구 상수</name>
  <files>src/today/pendingDelete.ts, src/today/pendingDelete.test.ts, src/today/UndoSnackbar.tsx, src/today/content.ts</files>
  <read_first>
    - src/today/content.ts (TODAY_COPY — 같은 스타일로 문구 추가)
    - src/components/LocationDeniedBanner.tsx (프레젠테이셔널 컴포넌트 계약 — 내부 absolute positioning 금지, 배치는 부모)
    - src/components/CheckinActionCard.tsx (25줄 SymbolView import, Pressable + accessibilityRole/accessibilityLabel 관례)
    - src/theme/tokens.ts (colors.textPrimary / colors.surface / typography.placeName / motion.saveStateCrossfadeMs / spacing)
    - src/today/trajectory.test.ts (src/today/ 하위 순수 로직 테스트의 기존 스타일)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pattern 7 + Assumption A1/A2, Open Question #1/#3)
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§Undo 스낵바)
  </read_first>
  <behavior>
    - `request(item)` 후 `delayMs` 경과 → `onCommit(item)`이 정확히 1회 호출된다
    - `request(item)` 후 `undo()` → 타이머가 만료돼도 `onCommit`이 호출되지 않는다
    - `request(a)` 직후 `request(b)` → `a`가 즉시 커밋되고(단일 스낵바 원칙), `b`만 대기 상태로 남는다
    - `request(item)` 후 `dispose()` → `onCommit(item)`이 **즉시 호출된다**(취소가 아니라 확정 — 화면 이탈 시 삭제가 조용히 부활하는 것을 막는다)
    - `dispose()`를 대기 항목 없이 호출해도 `onCommit`이 호출되지 않는다
    - `undo()`를 대기 항목 없이 호출해도 throw하지 않는다
    - `onChange(pendingId | null)`가 대기 상태 진입/해제 시마다 호출된다
    - `UNDO_WINDOW_MS`가 정확히 `4000`이다
  </behavior>
  <action>
**1) `src/today/pendingDelete.ts` 신규 — 지연 삭제 컨트롤러(순수 로직).**
RN/네이티브 모듈을 import하지 않는 순수 TS 모듈로 만든다(`@jest-environment node`에서
그대로 로드 가능해야 한다 — `src/today/trajectory.ts`와 같은 성격).

```
export const UNDO_WINDOW_MS = 4000;
export type PendingDeleteItem = { id: string; photoPath: string | null };
export type PendingDeleteController = {
  request(item: PendingDeleteItem): void;
  undo(): void;
  dispose(): void;
};
export function createPendingDeleteController(args: {
  delayMs?: number;                                  // 기본값 UNDO_WINDOW_MS
  onCommit: (item: PendingDeleteItem) => void;
  onChange: (pendingId: string | null) => void;
}): PendingDeleteController;
```
내부는 `setTimeout` 하나와 현재 대기 항목 하나만 유지한다(큐잉하지 않는다 — 05-RESEARCH.md
Assumption A2의 "단일 스낵바 + 즉시 확정" 원칙, iOS 메일 앱 관례와 동일).

**`dispose()`가 `clearTimeout`만 하고 끝나지 않는 것이 이 모듈의 핵심**이다 — 대기 항목이
있으면 **즉시 `onCommit`을 호출**한 뒤 타이머를 정리한다. 흔한 "언마운트 시 대기 중인 비동기
작업을 취소한다" 반사(이 저장소의 `isMountedRef` 가드가 대표적)와 정반대이므로, 왜 반대인지
(사용자가 "실행취소"를 누르지 않았는데도 화면을 떠났다는 이유로 삭제가 조용히 취소되면 다음
로드에서 지워졌어야 할 행이 부활한다 — 05-RESEARCH.md Assumption A1)를 함수 위에 굵직한
주석으로 남긴다. 이 주석이 없으면 다음 실행자/리뷰어가 "더 일반적인 패턴"으로 되돌릴 위험이
매우 높다.

`4000`이라는 숫자는 이 파일의 `UNDO_WINDOW_MS`에만 존재한다 — 스낵바나 화면에 재선언하지
않는다.

**2) `src/today/pendingDelete.test.ts` 신규.** `/** @jest-environment node */` 도크블록 +
`jest.useFakeTimers()`로 위 `<behavior>` 8개 케이스를 전부 검증한다. `jest.advanceTimersByTime`
으로 시간을 진행시키고, `onCommit`/`onChange`는 `jest.fn()`으로 호출 횟수와 인자를 단언한다.
특히 "dispose 시 즉시 커밋" 케이스는 `advanceTimersByTime` 없이 `onCommit`이 호출됐음을
단언해야 한다(타이머 만료가 아니라 즉시 실행임을 구분).

**3) `src/today/content.ts`에 문구를 추가한다.** `TODAY_COPY`에 같은 `as const` 스타일로:
- `deletedSnackbar`: `'삭제했어요'`
- `undoCta`: `'실행취소'`
- `deleteAffordanceLabel`: `'삭제'`
05-UI-SPEC.md §Copywriting Contract에서 그대로 전사한다(문구를 여기서 발명하지 않는다).

**4) `src/today/UndoSnackbar.tsx` 신규 — 프레젠테이셔널 컴포넌트.**
Props: `{ visible: boolean; onUndo: () => void }`. `visible`이 거짓이면 `null`을 반환한다
(disable이 아니라 미마운트 — 이 저장소의 프레젠테이셔널 계약).
렌더: 가로 `View`(배경 `colors.textPrimary`, `borderRadius: radius.md`, 좌우 패딩
`spacing.md`, 세로 패딩 `spacing.sm`) 안에 좌측 `TODAY_COPY.deletedSnackbar` 텍스트
(`typography.placeName`, `colors.surface`)와 우측 `Pressable`
(`accessibilityRole="button"`, `accessibilityLabel={TODAY_COPY.undoCta}`,
`minHeight: 44` 히트 영역, 라벨은 밑줄 + `colors.surface`).
등장·퇴장 크로스페이드는 `motion.saveStateCrossfadeMs`를 재사용한다 — 새 모션 토큰을
발명하지 않는다. 애니메이션에 `react-native`의 `Animated`를 쓰되, 만약 reanimated를 쓴다면
default export를 반드시 `Reanimated`로 바인딩한다(`(tabs)/index/index.tsx:39-43` 주석의
shadowing 규약).
**내부에 `position: 'absolute'`를 두지 않는다** — 화면 하단·탭바 위라는 배치는 부모가
결정한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/today/pendingDelete.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const UNDO_WINDOW_MS = 4000" src/today/pendingDelete.ts`가 1이다
    - `grep -v '^//' src/today/pendingDelete.ts | grep -Ec "react-native|expo-"`가 0이다 (순수 TS 모듈)
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/today/pendingDelete.test.ts`가 종료 코드 0이고 8개 이상의 테스트가 통과한다
    - `dispose()` 즉시 커밋 테스트가 존재한다: `grep -c "dispose" src/today/pendingDelete.test.ts`가 2 이상이다
    - 가드 확인: `pendingDelete.ts`의 `dispose()`에서 `onCommit` 호출을 `clearTimeout`만 남기도록 임시 변경하면 위 jest 명령이 실패한다(확인 후 원복)
    - `grep -v '^//' src/today/UndoSnackbar.tsx | grep -Ec "position: 'absolute'"`가 0이다
    - `grep -v '^//' src/today/UndoSnackbar.tsx | grep -c "motion.saveStateCrossfadeMs"`가 1이다
    - `grep -v '^//' src/today/UndoSnackbar.tsx | grep -Ec "#[0-9a-fA-F]{3,6}\b|\b4000\b"`가 0이다 (hex 리터럴/기간 상수 중복 없음)
    - `grep -Ec "deletedSnackbar|undoCta|deleteAffordanceLabel" src/today/content.ts`가 3이다
    - `npx tsc --noEmit`이 종료 코드 0이다
  </acceptance_criteria>
  <done>지연 삭제 컨트롤러가 8개 동작 케이스로 유닛 테스트되고(특히 dispose 즉시 커밋), 스낵바 컴포넌트와 문구 상수가 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: CheckinListRow를 탭 가능 + 스와이프 삭제 가능하게 바꾸고 Phase 4 D-03 계약을 반전한다</name>
  <files>src/today/CheckinListRow.tsx, src/today/TodayBottomSheet.tsx, src/today/__tests__/todayUi.test.ts</files>
  <read_first>
    - src/today/CheckinListRow.tsx (전체 74줄 — 특히 헤더 8~13줄의 D-01/D-02/D-03 계약 주석과 27~35줄 fontVariant 얕은 복사 관용구)
    - src/today/TodayBottomSheet.tsx (전체 — props 타입과 renderItem)
    - src/today/__tests__/todayUi.test.ts (전체 114줄 — 반전 대상 Test 1의 정확한 현재 단언, 그리고 **유지 대상**인 Test 6(`colors.accent` 미등장)의 단언)
    - DESIGN.md (§Color + Decisions Log 2026-08-31 18:10 / 2026-09-01 — 어포던스 색이 왜 accent가 아니라 Pin인지)
    - src/components/CheckinActionCard.tsx (25줄 SymbolView import, Pressable + accessibilityRole="button" + accessibilityLabel 관례)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pattern 3, Pitfall 3)
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§스와이프 삭제, §CheckinListRow 탭 가능 전환)
    - .planning/phases/04-today-view/04-CONTEXT.md (D-03 — 무엇을 뒤집는지)
  </read_first>
  <action>
**1) `CheckinListRow.tsx`의 props를 확장한다:**
`{ checkin: CheckinRow; onPress: (id: string) => void; onDeleteRequest: (checkin: CheckinRow)
=> void }`. 네비게이션 자체는 이 컴포넌트가 소유하지 않는다 — `expo-router`를 여기서
import하지 않고 콜백만 받는다(프레젠테이셔널 계약: "화면별 로직을 내부에 갖지 않는다").

**2) 기존 행 콘텐츠(`View style={styles.row}` 서브트리)를 `Pressable`로 감싼다.**
`onPress={() => onPress(checkin.id)}`, `accessibilityRole="button"`,
`accessibilityLabel`은 시각과 메모 미리보기를 조합해 구성한다. 탭 피드백은 iOS 표준
하이라이트 수준(`pressed` 시 배경 `colors.surfaceSoft`)만 준다 — 새 리플/그림자 효과를
발명하지 않는다. **chevron/">"/화살표/밑줄 등 탭 가능함을 암시하는 시각 요소를 추가하지
않는다**(05-UI-SPEC.md 확정: 스와이프 삭제 어포던스와 시각적으로 섞이지 않게, 리스트 밀도
유지).

**3) 그 `Pressable`을 다시 `ReanimatedSwipeable`로 감싼다.**
import 경로는 정확히 `react-native-gesture-handler/ReanimatedSwipeable`(default) +
`import { SwipeDirection } from 'react-native-gesture-handler'`. 구 `Swipeable` class를
import하지 않는다(`@deprecated`).
Props: `friction={2}`, `rightThreshold={40}`, `overshootRight={false}`,
`activeOffsetX={[-10, 10]}`, `failOffsetY={[-5, 5]}`.
`activeOffsetX`/`failOffsetY`를 명시하는 이유(부모 `BottomSheetFlatList`의 세로 팬 제스처와
경합할 수 있다는 커뮤니티 보고 — 05-RESEARCH.md Pitfall 3, MEDIUM confidence, 05-07-PLAN.md가
시뮬레이터로 실측 검증한다)를 주석으로 남긴다. 세로 이동이 먼저 감지되면 이 제스처를 포기해
시트 스크롤에 양보한다.
`renderRightActions`: 폭 `72`, 배경 `colors.pin`(테라코타 — accent가 **아니다**, 위
`<interfaces>` 토큰 절의 근거 참고), 중앙에
`SymbolView name="trash" tintColor={colors.surface}`,
`accessibilityLabel={TODAY_COPY.deleteAffordanceLabel}`.
`onSwipeableOpen={(direction) => { if (direction === SwipeDirection.RIGHT)
onDeleteRequest(checkin); }}` — 별도 확인 다이얼로그 없이 스와이프 자체가 확정 행동이다
(iOS 네이티브 관례, 05-UI-SPEC.md).

**4) 파일 헤더 주석 블록(8~13줄)을 교체한다.** D-03의 "이 행은 탭 불가능하다" 문장을
그대로 두면 코드와 주석이 정면으로 모순된다. Phase 4 D-03이 무엇이었고 Phase 5가 왜 그걸
뒤집는지(상세화면 진입점이 생김 — 05-CONTEXT.md/05-UI-SPEC.md), 그리고 chevron 미추가
결정은 그대로 유지된다는 점을 날짜와 함께 남긴다(이 저장소의 "2026-08-28 추가" 스타일).
D-01(장소명 미렌더)/D-02(시간+메모 미리보기만) 계약은 변경 없이 유지한다.

**5) `TodayBottomSheet.tsx`에 콜백을 스레딩한다.** props에
`onRowPress: (id: string) => void`와 `onDeleteRequest: (checkin: CheckinRow) => void`를
추가하고 `renderItem`에서 `<CheckinListRow checkin={item} onPress={onRowPress}
onDeleteRequest={onDeleteRequest} />`로 전달한다. 시트 자신은 삭제/네비게이션 로직을 갖지
않는다(순수 전달). 시트에 `colors.pin`도 `colors.accent`도 도입하지 않는다 — 어포던스는 행
안에 있다.

**6) `src/today/__tests__/todayUi.test.ts`의 Phase 4 D-03 계약을 반전하고, 색상 가드를 넓힌다.**
- **Test 1(반전 대상)** — 현재 "Pressable/TouchableOpacity/onPress/chevron이 등장하지
  않는다"를, "`Pressable`과 `onPress`가 등장한다(탭 가능 — Phase 5 D-03 반전) **그러나**
  chevron과 `TouchableOpacity`는 여전히 등장하지 않는다"로 바꾼다. chevron 부재 단언은 반드시
  살려둔다 — 그건 Phase 5도 유지하기로 한 결정이다.
- **Test 6(반전하지 않는다 — 한 글자도 고치지 않는다)** — "`colors.accent`가 등장하지
  않는다"는 그대로 둔다. 삭제 어포던스가 `colors.pin`을 쓰므로 이 단언은 수정 없이 통과해야
  하며, DESIGN.md 2026-08-31 갱신(accent = 캘린더 탭 전용 2개) 이후 오히려 더 강한 의미를
  갖는다. **이 테스트가 빨간불이 되면 어포던스 색을 잘못 넣은 것이다 — 테스트가 아니라 코드를
  고쳐야 한다**는 점을 테스트 위 주석에 명시한다.
- **새 색상 가드를 추가한다** — "`colors.pin`이 정확히 1회 등장하고 그 사용처가 삭제 어포던스
  배경이다"(정규식으로 `deleteAffordance` 스타일 블록 안의 `backgroundColor: colors.pin`을
  확인). `colors.pinSoft`는 등장하지 않는다는 단언도 함께 둔다 — pinSoft는 "저장된 핀"용
  옅은 톤이라 어포던스에 잘못 쓰이면 삭제 의미가 흐려진다. 카운트 정규식은 반드시
  `/colors\.pin\b/`처럼 단어 경계를 써서 `pinSoft`가 `pin` 카운트에 섞이지 않게 한다.
- Test 1 위와 새 색상 가드 위에 **근거 주석**을 남긴다: Test 1은 고장난 게 아니라 Phase 4
  D-03 계약을 Phase 5가 명시적으로 뒤집은 것이며(근거: 05-CONTEXT.md, 05-UI-SPEC.md),
  어포던스 색이 Pin인 근거는 DESIGN.md Decisions Log 2026-09-01(accent→Pin 전환)이다. 이
  주석이 없으면 다음 리뷰어가 "회귀 가드를 약화시켰다"거나 "왜 accent가 아니지"라고 판단한다.
- 새 테스트를 추가한다: `ReanimatedSwipeable`이 `react-native-gesture-handler/ReanimatedSwipeable`
  경로로 import된다 / 구 `Swipeable`을 단독 import하지 않는다 / `activeOffsetX`와
  `failOffsetY`가 등장한다(Pitfall 3 완화책 회귀 가드) / 어포던스 폭 `72`가 등장한다 /
  `SwipeDirection.RIGHT` 비교가 존재한다.
- `TodayBottomSheet` 블록에도 새 테스트를 추가한다: `onRowPress`/`onDeleteRequest`를
  `CheckinListRow`에 전달한다 / 시트 코드에 `deleteCheckin`이나 `router`가 등장하지 않는다
  (순수 전달 계약).
- 기존 Test 15(시트에 `colors.accent` 없음), Test 16(absolute 없음), Test 11
  (`GestureHandlerRootView` 없음)은 그대로 유지되어야 한다 — 반전 대상이 아니다.
  Test 15 옆에 "시트에 `colors.pin`도 등장하지 않는다"는 단언을 하나 더 둔다(어포던스는 행
  안에만 있다는 계약의 색상 측면 가드).

모든 소스 문자열 단언은 `stripComments`를 통과한 코드에 대해서만 수행한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/today/__tests__/todayUi.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -c "react-native-gesture-handler/ReanimatedSwipeable"`가 1이다
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -Ec "^import \{ Swipeable"`가 0이다 (deprecated class 미사용)
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -c "activeOffsetX"`가 1이고 `grep -c "failOffsetY"`가 1이다
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -c "SwipeDirection.RIGHT"`가 1이다
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -c "colors.accent"`가 0이다 (accent는 캘린더 탭 전용 — 이 phase는 어디에도 쓰지 않는다)
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -Ec "colors\.pin([^S]|$)"`가 정확히 1이다 (삭제 어포던스 배경 1곳)
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -c "colors.pinSoft"`가 0이다 (저장된 핀용 옅은 톤을 어포던스에 쓰지 않는다)
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -Eic "chevron|TouchableOpacity"`가 0이다
    - `grep -v '^//' src/today/CheckinListRow.tsx | grep -Ec "expo-router|router\."`가 0이다 (네비게이션은 부모 소유)
    - `grep -c "탭 불가능" src/today/CheckinListRow.tsx`가 0이다 (모순된 헤더 주석이 교체됨)
    - `grep -v '^//' src/today/TodayBottomSheet.tsx | grep -Ec "onRowPress|onDeleteRequest"`가 4 이상이다 (타입 2 + 전달 2)
    - `grep -v '^//' src/today/TodayBottomSheet.tsx | grep -Ec "deleteCheckin|router\.|colors\.accent|colors\.pin"`가 0이다
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/today/__tests__/todayUi.test.ts`가 종료 코드 0이고 실패 0건이다
    - 반전 사유 주석 존재: `grep -Ec "D-03|반전" src/today/__tests__/todayUi.test.ts`가 2 이상이다
    - Pin 색상 근거 주석 존재: `grep -Ec "2026-09-01|colors\.pin" src/today/__tests__/todayUi.test.ts`가 2 이상이다
    - Test 6이 **수정되지 않았다**: `grep -c "Test 6: colors.accent가 등장하지 않는다" src/today/__tests__/todayUi.test.ts`가 1이다
    - 가드 확인: `CheckinListRow.tsx`에서 `activeOffsetX`를 임시로 지우면 위 jest 명령이 실패한다(확인 후 원복)
    - `npx tsc --noEmit`이 종료 코드 0이다
  </acceptance_criteria>
  <done>리스트 행이 `Pressable`(chevron 없음) + `ReanimatedSwipeable`(`colors.pin` 테라코타 어포던스, 제스처 경합 완화 옵션 포함)로 감싸졌고, Phase 4 D-03 계약(Test 1)이 근거 주석과 함께 반전되고 accent 계약(Test 6)은 수정 없이 통과하며, 시트가 콜백을 순수 전달한다.</done>
</task>

<task type="auto">
  <name>Task 3: 오늘 화면에 행 탭 네비게이션 + 지연 삭제/undo 상태 + 스낵바 배치를 배선한다</name>
  <files>src/app/(tabs)/index/index.tsx, src/app/__tests__/checkin-wiring.test.ts</files>
  <read_first>
    - src/app/(tabs)/index/index.tsx (207줄 todayCheckins state, 397줄 reloadTodayCheckins, 900~940줄 AppState 리스너 + useCallback 관례, 1077~1085줄 TodayBottomSheet 렌더 지점, 22~60줄 import 블록)
    - src/today/pendingDelete.ts (Task 1 산출물 — 컨트롤러 API)
    - src/today/UndoSnackbar.tsx (Task 1 산출물)
    - src/checkin/deps.ts (defaultPhotoStorageDeps — 이 화면이 이미 import 중)
    - src/app/__tests__/checkin-wiring.test.ts (특히 Test 9/Test 39 — gesture-handler 미사용 계약. 이 계약을 깨지 않아야 한다)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pattern 7, Pitfall 6, Open Question #1/#2)
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§Undo 스낵바 위치)
  </read_first>
  <action>
**1) 행 탭 → 상세화면 네비게이션.** `expo-router`의 `router`(또는 `useRouter()`)를 이 화면에
import하고 `handleRowPress = useCallback((id: string) => router.push({ pathname: '/[id]',
params: { id } }), [])`를 만들어 `TodayBottomSheet`의 `onRowPress`로 넘긴다. 경로를 문자열
템플릿(`` `/${id}` ``)이 아니라 `pathname`+`params` 객체 형태로 쓴다 — 동적 세그먼트를
expo-router가 직접 인코딩하게 해서 id에 특수문자가 있어도 경로가 깨지지 않게 한다.
이 화면은 `(tabs)/index/_layout.tsx`의 nested Stack 안에 있으므로 push 후에도 탭바가
유지된다.

**2) 지연 삭제 상태를 소유한다.**
- `const [hiddenIds, setHiddenIds] = useState<Set<string>>(new Set())` — 삭제 대기 중인 행을
  리스트에서 숨긴다.
- `const [pendingId, setPendingId] = useState<string | null>(null)` — 스낵바 표시 여부.
- 컨트롤러를 `useRef`로 한 번만 만든다(`createPendingDeleteController`). `onChange`는
  `setPendingId`로 연결하고, 대기 해제 시 `hiddenIds`에서도 제거한다. `onCommit`은 아래 3)의
  커밋 로직을 호출한다.
- `useEffect` cleanup에서 `controller.dispose()`를 호출한다 — **이것이 언마운트 시 삭제를
  즉시 확정하는 경로**다. `clearTimeout`으로 바꾸지 말 것(Task 1의 컨트롤러 주석 참고).

**3) 커밋 로직(`onCommit`).** 순서를 반드시 지킨다:
`runWithSingleRetry(() => deleteCheckin(db, item.id))` → 성공이면 (a) `item.photoPath`가
있을 때 `defaultPhotoStorageDeps.deleteFile(item.photoPath)`를 **non-blocking**으로 호출하고
실패 시 `console.error`만 남긴다(DB row는 이미 지워졌으므로 파일 삭제 실패는 고아 파일만
남길 뿐 데이터 유실이 아니다), (b) `reloadTodayCheckins()`로 목록을 새로 읽는다.
실패(2회 다)면 별도 재시도 UI를 띄우지 않고 `console.error`만 남긴 뒤 `hiddenIds`에서 해당
id를 제거한다 — 다음 `reloadTodayCheckins`에서 행이 다시 나타나는 것 자체가 사용자에게
"삭제되지 않았다"는 자연스러운 피드백이 된다. 이미 스낵바가 사라진 뒤라 새 오류 UI를 띄우는
것이 더 혼란스럽고, 요구사항 문서 어디에도 이 실패 UX가 정의돼 있지 않다
(05-RESEARCH.md Open Question #1의 권장안을 채택). 이 판단 근거를 주석으로 남긴다.
사진 파일 정리 여부도 요구사항에 명시가 없던 gap이며, 정리하는 쪽을 택한 근거
(고아 파일 누적으로 인한 저장공간 누수 방지 — 05-RESEARCH.md Assumption A3 / Open
Question #2)를 함께 주석으로 남긴다.

**4) 리스트에서 대기 항목을 숨긴다.** `TodayBottomSheet`에 넘기는 `checkins`를
`todayCheckins.filter((c) => !hiddenIds.has(c.id))`로 감싼다(`useMemo`로 안정화). 지도 핀과
궤적선도 같은 필터된 배열을 쓸지 결정한다 — **쓴다**: 리스트에서 사라졌는데 지도에 핀이
남아 있으면 undo 창 동안 상태가 어긋나 보인다. 기존 `trajectoryCoordinates`의 `useMemo`
입력도 필터된 배열로 바꾼다.

**5) 스낵바를 배치한다.** `UndoSnackbar`를 화면 하단, **탭바 위**에 배치한다 — 배치
(absolute positioning, bottom 오프셋)는 부모인 이 화면이 소유한다(컴포넌트 계약). 기존
플로팅 버튼들이 쓰는 `insets`/`floatingButtonStyle` 계산 관례를 참고해 안전 영역을 존중한다.
`visible={pendingId !== null}`, `onUndo={() => controller.undo()}`.

**6) `react-native-gesture-handler`를 이 파일에 절대 import하지 않는다** —
`checkin-wiring.test.ts` Test 9/Test 39가 이를 단언하고 있고, 그 계약은 여전히 유효하다
(스와이프 제스처는 `CheckinListRow.tsx` 안에만 존재). 기존 Test 9/39를 수정하지 않는다.

**7) `checkin-wiring.test.ts`에 새 배선 가드를 추가한다**(가장 큰 Test 번호 뒤로 이어 붙이고
`it('Test N (2026-08-31 추가): ...')` 관례를 따른다):
- `router.push`와 `pathname: '/[id]'`가 등장한다(행 탭 진입점).
- `createPendingDeleteController`가 등장하고, cleanup에 `dispose()`가 등장한다.
- `clearTimeout`이 등장하지 않는다 — 언마운트 시 "취소"가 아니라 "즉시 커밋"이라는 A1
  계약의 회귀 가드. 이 단언의 근거를 주석으로 명시한다.
- `deleteCheckin`이 `runWithSingleRetry`로 감싸여 있다.
- `deleteFile`이 등장하고, 그 호출이 `deleteCheckin` 호출보다 **뒤 인덱스**에 있다
  (DB 삭제 성공 후에만 파일 정리 — Pitfall 5와 같은 계열의 순서 계약).
- `hiddenIds`(또는 동등한 필터)가 `TodayBottomSheet`에 넘기는 배열과 지도 핀/궤적선 배열
  **양쪽 모두**에 적용된다.
- `react-native-gesture-handler`가 여전히 등장하지 않는다(기존 Test 9/39를 재확인하는 중복
  단언은 추가하지 않는다 — 이미 있다).
- `4000` 리터럴이 이 파일에 등장하지 않는다(`UNDO_WINDOW_MS` 단일 출처).
모든 단언은 `stripComments`를 통과한 코드에 대해 수행한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -c "router.push"`가 1이다
    - `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -c "pathname: '/\[id\]'"`가 1이다
    - `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -c "createPendingDeleteController"`가 1이다
    - `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -c "dispose()"`가 1이다
    - `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -c "clearTimeout"`가 0이다
    - `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -c "react-native-gesture-handler"`가 0이다
    - `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -c "\b4000\b"`가 0이다
    - `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -c "deleteFile"`가 1이다
    - 순서 확인: `grep -v '^//' "src/app/(tabs)/index/index.tsx" | grep -n "deleteCheckin\|deleteFile"`의 출력에서 `deleteCheckin` 줄 번호 < `deleteFile` 줄 번호
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-wiring.test.ts`가 종료 코드 0이고, 기존 Test 9/Test 39가 여전히 통과한다
    - 가드 확인: `dispose()`를 `clearTimeout`으로 임시 대체하면 `checkin-wiring.test.ts`가 실패한다(확인 후 원복)
    - `npm test` 전체 스위트가 종료 코드 0이다
    - `npx tsc --noEmit`이 종료 코드 0이다
  </acceptance_criteria>
  <done>행 탭이 `/[id]`로 push하고, 스와이프가 4초 지연 삭제를 시작하며, undo가 되돌리고, 만료/언마운트 시 DB 삭제 + 사진 파일 정리가 순서대로 실행되고, 스낵바가 탭바 위에 뜬다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 리스트 행 스와이프(사용자 제스처) → SQLite DELETE | 파괴적 작업이 확인 다이얼로그 없이 트리거되는 경계 |
| 삭제 커밋 → `documentDirectory` 파일 삭제 | DB 값(`photo_path`)이 파일시스템 삭제 인자로 쓰이는 경계 |
| 리스트 행 탭 → 라우트 파라미터(`id`) | 사용자 선택 id가 URL 세그먼트로 인코딩되는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | 지연 삭제 언마운트 처리 — 취소로 잘못 구현 시 삭제된 체크인이 조용히 부활 | mitigate | `dispose()`가 `clearTimeout`이 아니라 **즉시 커밋**하도록 구현하고, `pendingDelete.test.ts` 유닛 테스트와 `checkin-wiring.test.ts`의 "`clearTimeout` 미등장" 단언 **두 겹**으로 고정한다(05-RESEARCH.md Assumption A1) |
| T-05-14 | Tampering | `deleteFile(photo_path)` 경로 인자 | mitigate | 삭제 대상 uri는 항상 DB `photo_path` 값이며 `photos.ts`가 `crypto.randomUUID()` 기반 파일명으로 `documentDirectory`에 만든 것이다 — 사용자 자유 입력이 아니다. 순서 계약(DB DELETE 성공 후에만 파일 삭제)을 wiring 테스트가 인덱스 비교로 게이트해, 삭제 실패 시 파일만 먼저 지워지는 상태를 막는다 |
| T-05-15 | Tampering | `router.push`의 id 세그먼트 | mitigate | 문자열 템플릿 대신 `{ pathname: '/[id]', params: { id } }` 객체 형태를 쓴다 — expo-router가 세그먼트 인코딩을 담당해 id에 `/`·`?` 등이 있어도 경로가 조작되지 않는다 |
| T-05-16 | Denial of Service | 백그라운드 전환 중 `setTimeout` 정지로 4초 보장이 깨짐 | accept | iOS가 앱 suspend 중 JS 타이머 카운트를 멈춘다 — 데이터 유실은 없고(타이머는 복귀 후 반드시 발동해 커밋된다) "정확히 4초"라는 체감 타이밍만 어긋난다. 방어 로직 없이 허용한다(05-RESEARCH.md Pitfall 6). 이 판단 근거를 컨트롤러 주석에 남긴다 |
| T-05-17 | Repudiation | 확인 다이얼로그 없는 파괴적 삭제 | mitigate | 4초 undo 창이 확인 다이얼로그를 대체하는 안전망이며(iOS 네이티브 스와이프 삭제 관례, REQ-checkin-swipe-delete 명시), DB `DELETE`는 그 창이 만료된 뒤에만 실행된다 — 되돌릴 수 없는 상태로 진입하기 전에 항상 4초의 취소 기회가 있다 |
| T-05-SC | Tampering | npm/pip/cargo installs | N/A | 신규 패키지 설치 없음. `react-native-gesture-handler`는 Phase 3부터 루트 `GestureHandlerRootView`로 이미 설치·사용 중인 기존 의존성이다 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/today/` 그린 (pendingDelete 유닛 + todayUi 계약)
- `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-wiring.test.ts` 그린 (기존 Test 9/39 포함)
- `npm test` 전체 스위트 그린
- `npx tsc --noEmit` 통과
- 의도적 파손 실험 3건(dispose→clearTimeout / activeOffsetX 제거 / 어포던스 배경을 `colors.pin`→`colors.accent`로 교체)에서 테스트가 실제로 실패한다. 세 번째 실험은 Test 6(accent 미등장)과 새 Pin 가드 **양쪽**을 동시에 빨간불로 만들어야 한다
</verification>

<success_criteria>
- 행 탭으로 상세화면 진입, 왼쪽 스와이프로 `colors.pin`(테라코타) 삭제 어포던스, 4초 undo가 전부 동작한다
- 언마운트 시 대기 중인 삭제가 취소가 아니라 확정된다(두 겹 테스트로 고정)
- Phase 4의 D-03 계약 반전(Test 1)이 근거 주석과 함께 명시적으로 문서화됐고, accent 계약(Test 6)은 수정 없이 그대로 통과한다
- `(tabs)/index/index.tsx`에 `react-native-gesture-handler`가 여전히 등장하지 않는다
</success_criteria>

<output>
Create `.planning/phases/05-check-in-detail-edit/05-05-SUMMARY.md` when done
</output>
