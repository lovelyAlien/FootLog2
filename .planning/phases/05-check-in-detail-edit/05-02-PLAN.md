---
phase: 05-check-in-detail-edit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/checkin/checkinRepo.ts
  - src/checkin/checkinRepo.test.ts
  - src/checkin/localDate.ts
  - src/checkin/localDate.test.ts
  - src/checkin/config.ts
  - src/checkin/deps.ts
  - src/checkin/testing/fakePhotoStorage.ts
autonomous: true
requirements: [REQ-checkin-detail-base, REQ-checkin-detail-layout, REQ-checkin-swipe-delete]

must_haves:
  truths:
    - "화면이 SQL을 직접 쓰지 않고도 체크인 1건을 id로 조회할 수 있다"
    - "화면이 SQL을 직접 쓰지 않고도 체크인 1건을 삭제할 수 있다"
    - "상세화면 헤더가 쓸 로컬 월/일 문자열(예: \"8월 31일\")을 Intl로 얻을 수 있다"
    - "화면이 expo-file-system을 직접 import하지 않고도 사진 파일을 삭제할 수 있다"
  artifacts:
    - path: "src/checkin/checkinRepo.ts"
      provides: "getCheckinById / deleteCheckin"
      contains: "export async function getCheckinById"
    - path: "src/checkin/localDate.ts"
      provides: "formatLocalMonthDay"
      contains: "export function formatLocalMonthDay"
    - path: "src/checkin/config.ts"
      provides: "PhotoStorageDeps.deleteFile 포트"
      contains: "deleteFile"
    - path: "src/checkin/deps.ts"
      provides: "deleteFile 실제 구현(new File(uri).delete())"
      contains: "deleteFile"
  key_links:
    - from: "src/checkin/deps.ts"
      to: "expo-file-system File API"
      via: "new File(uri).delete()"
      pattern: "new File\\(uri\\)"
    - from: "src/checkin/testing/fakePhotoStorage.ts"
      to: "PhotoStorageDeps"
      via: "타입 구현(더블이 새 포트를 만족)"
      pattern: "deleteFile"
---

<objective>
상세화면과 스와이프 삭제가 소비할 데이터/유틸 레이어를 먼저 확장한다: 체크인 단건
조회(`getCheckinById`), 체크인 삭제(`deleteCheckin`), 로컬 월/일 포맷
(`formatLocalMonthDay`), 그리고 사진 파일 삭제 포트(`PhotoStorageDeps.deleteFile`).

이 네 가지는 전부 **이미 존재하는 파일에 같은 관례로 함수를 하나씩 더하는 작업**이며
(05-PATTERNS.md 판정: 전부 exact 매치 아날로그), 05-RESEARCH.md Code Examples가 구현체를
이미 확정해 뒀다. 05-01-PLAN.md(라우트 재구성)와 파일이 전혀 겹치지 않아 같은 wave에서
병렬로 실행된다.

Purpose: 05-RESEARCH.md Pitfall 4가 경고한 "포트에 삭제 함수가 없어 화면이
`expo-file-system`을 직접 import하게 되는" 격리 위반을 사전에 차단하고, 상세화면 plan이
비즈니스 로직에만 집중할 수 있게 바닥을 깐다.
Output: 4개 신규 함수/포트 + 각각의 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-check-in-detail-edit/05-RESEARCH.md
@.planning/phases/05-check-in-detail-edit/05-PATTERNS.md
</context>

<interfaces>
<!-- 코드베이스에서 직접 추출한 현재 계약 — 실행자는 이걸 그대로 확장한다 -->

`src/checkin/checkinRepo.ts`의 기존 시그니처(변경 없이 유지):
```ts
export async function runWithSingleRetry<T>(attempt: () => Promise<T>): Promise<{ ok: true; value: T } | { ok: false }>;
export async function commitCheckin(db: MigratableDb, params: NewCheckinParams): Promise<SaveResult>;
export async function getLatestCheckinCoordinate(db: MigratableDb): Promise<{ lat: number; lng: number } | null>;
export async function getTodayCheckins(db: MigratableDb, localDateKey: string): Promise<CheckinRow[]>;
export async function updateCheckinNoteAndPhoto(db: MigratableDb, id: string, args: { note: string | null; photoPath: string | null; now: string }): Promise<void>;
```

`src/checkin/localDate.ts`의 기존 시그니처:
```ts
export function resolveTimeZone(): string;
export function resolveLocalDateKey(date: Date, timeZone?: string): string;
export function toIsoTimestamp(date?: Date): string;
export function formatLocalTime(isoTimestamp: string, timeZone?: string): string;
```

`src/checkin/config.ts`의 현재 포트(확장 대상):
```ts
export type PhotoStorageDeps = {
  copyIntoDocumentDirectory(sourceUri: string, fileName: string): Promise<string>;
};
```

`src/checkin/deps.ts`의 현재 구현(같은 객체에 메서드를 하나 더한다):
```ts
export const defaultPhotoStorageDeps: PhotoStorageDeps = {
  async copyIntoDocumentDirectory(sourceUri, fileName) {
    const sourceFile = new File(sourceUri);
    const destinationFile = new File(Paths.document, fileName);
    await sourceFile.copy(destinationFile);
    return destinationFile.uri;
  },
};
```

`src/checkin/testing/fakePhotoStorage.ts`의 현재 더블 타입(같이 확장해야 컴파일된다):
```ts
export type FakePhotoStorage = PhotoStorageDeps & {
  __copies(): Array<{ source: string; fileName: string }>;
  __setShouldThrow(shouldThrow: boolean): void;
};
```

테스트 관례: `src/checkin/*.test.ts`는 파일 최상단에 `/** @jest-environment node */` 도크블록을
두고, DB 테스트는 `createTestDb`(`../db/testing/nodeSqliteAdapter`) + `migrateDbIfNeeded`로
실제 SQLite 엔진을 쓴다. 타임존 의존 테스트는 반드시 `timeZone` 인자를 명시적으로 넘긴다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkinRepo에 getCheckinById / deleteCheckin을 추가한다</name>
  <files>src/checkin/checkinRepo.ts, src/checkin/checkinRepo.test.ts</files>
  <read_first>
    - src/checkin/checkinRepo.ts (전체 145줄 — 헤더 주석의 "SQL은 이 파일에만" 규약, 기존 getTodayCheckins/updateCheckinNoteAndPhoto 스타일)
    - src/checkin/checkinRepo.test.ts (기존 테스트 구조 — createTestDb/migrateDbIfNeeded/validParams 헬퍼를 그대로 재사용)
    - src/db/schema.ts (CheckinRow 필드 정의)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (§Code Examples "체크인 단건 조회 + 삭제")
  </read_first>
  <behavior>
    - `getCheckinById(db, id)`: 존재하는 id → 해당 `CheckinRow` 반환(모든 컬럼 포함)
    - `getCheckinById(db, id)`: 존재하지 않는 id → `null` 반환(undefined 아님)
    - `deleteCheckin(db, id)`: 해당 row가 사라지고, 같은 id로 `getCheckinById`가 `null`을 반환한다
    - `deleteCheckin(db, id)`: 다른 체크인 row는 영향받지 않는다(같은 날짜의 다른 id는 `getTodayCheckins`에 그대로 남는다)
    - `deleteCheckin(db, id)`: 존재하지 않는 id로 호출해도 throw하지 않는다(멱등)
  </behavior>
  <action>
`src/checkin/checkinRepo.ts` 끝에 두 함수를 추가한다. 05-RESEARCH.md §Code Examples가 확정한
구현을 그대로 쓴다:

- `getCheckinById(db: MigratableDb, id: string): Promise<CheckinRow | null>` —
  `db.getFirstAsync<CheckinRow>('SELECT * FROM checkins WHERE id = ?', id)`의 결과에
  `?? null`을 적용해 반환한다(`getLatestCheckinCoordinate`가 이미 쓰는 것과 동일한 관용구).
- `deleteCheckin(db: MigratableDb, id: string): Promise<void>` —
  `db.runAsync('DELETE FROM checkins WHERE id = ?', id)`.

파라미터 바인딩(`?`)만 쓰고 문자열 보간(`${...}`)을 SQL에 절대 쓰지 않는다 — 이 파일이
이미 지키는 규약이자 T-05-03 완화책이다. 트랜잭션으로 감싸지 않는다: 두 함수 모두 단일
문장이라 `commitCheckin`의 BEGIN/COMMIT 패턴이 필요 없다.

`deleteCheckin`에는 사진 파일 정리를 넣지 않는다 — 이 함수는 DB row만 책임진다. 첨부 사진
파일(`photo_path`) 정리는 호출자(오늘 화면의 지연 삭제 커밋 경로, 05-05-PLAN.md)가
`PhotoStorageDeps.deleteFile`로 별도 수행한다. 그 이유를 함수 위 주석에 남긴다(05-RESEARCH.md
Assumption A3 / Open Question #2에 대한 이 phase의 확정 답이며, 다음 phase가 이 함수에
파일 I/O를 밀어 넣는 것을 막는다).

`src/checkin/checkinRepo.test.ts`에 위 `<behavior>` 5개 케이스를 `describe('getCheckinById')` /
`describe('deleteCheckin')` 블록으로 추가한다. 기존 파일의 `createTestDb` +
`migrateDbIfNeeded` + `validParams()` 헬퍼를 재사용하고 새 헬퍼를 만들지 않는다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/checkinRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/checkin/checkinRepo.ts | grep -c "export async function getCheckinById"`가 1이다
    - `grep -v '^//' src/checkin/checkinRepo.ts | grep -c "export async function deleteCheckin"`가 1이다
    - `grep -v '^//' src/checkin/checkinRepo.ts | grep -c '\${'`가 0이다 (SQL 문자열 보간 부재)
    - `grep -v '^//' src/checkin/checkinRepo.ts | grep -c "expo-file-system"`가 0이다 (파일 I/O가 repo에 새지 않음)
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/checkinRepo.test.ts`가 종료 코드 0이고, `getCheckinById`/`deleteCheckin` describe 블록에서 최소 5개 테스트가 통과한다
    - 존재하지 않는 id로 `deleteCheckin`을 호출하는 테스트가 rejects 없이 통과한다
  </acceptance_criteria>
  <done>두 함수가 checkinRepo.ts에 존재하고, 5개 동작 케이스가 실제 SQLite 엔진 테스트로 그린이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: localDate에 formatLocalMonthDay를 추가한다</name>
  <files>src/checkin/localDate.ts, src/checkin/localDate.test.ts</files>
  <read_first>
    - src/checkin/localDate.ts (전체 49줄 — 헤더 주석의 "수동 Date 파싱 금지, Intl 전담" 규약과 기존 formatLocalTime 스타일)
    - src/checkin/localDate.test.ts (타임존 인자를 항상 명시하는 테스트 관례)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (§Code Examples "로컬 날짜 헤더 포맷")
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§Typography — 헤더 타이틀은 로컬 날짜, 예 "8월 31일")
  </read_first>
  <behavior>
    - `formatLocalMonthDay('2026-08-31T12:00:00Z', 'Asia/Seoul')` → `'8월 31일'`
    - 자정 경계: `formatLocalMonthDay('2026-08-31T16:00:00Z', 'Asia/Seoul')` → `'9월 1일'` (UTC 16시 = KST 다음날 01시)
    - 다른 타임존: 같은 ISO 값을 `'America/New_York'`으로 포맷하면 서울 결과와 다른 날짜를 낸다
    - 반환 문자열에 연도가 포함되지 않는다 (`2026` 미포함)
  </behavior>
  <action>
`src/checkin/localDate.ts` 끝에 `formatLocalMonthDay(isoTimestamp: string, timeZone: string =
resolveTimeZone()): string`을 추가한다. 05-RESEARCH.md §Code Examples가 확정한 구현 그대로
`new Intl.DateTimeFormat('ko-KR', { timeZone, month: 'long', day: 'numeric' }).format(new
Date(isoTimestamp))`를 반환한다.

로케일이 `formatLocalTime`의 `'en-GB'`가 아니라 `'ko-KR'`인 이유(한국어 "8월 31일" 표기를
얻기 위함이며, 시간 표시와 달리 이 문자열은 사용자에게 한국어로 노출된다)를 함수 위 주석에
남긴다 — 이 파일이 이미 `en-CA`/`en-GB` 선택 근거를 주석으로 남기는 관례를 따른다.

수동 `Date` 파싱, 문자열 슬라이싱, 월 이름 배열 하드코딩을 절대 쓰지 않는다(파일 헤더 규약).

`src/checkin/localDate.test.ts`에 `describe('formatLocalMonthDay')` 블록으로 위 4개 케이스를
추가한다. 모든 케이스에서 `timeZone` 인자를 명시적으로 넘겨 CI 머신 타임존에 의존하지 않게
한다(이 파일의 기존 관례).
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/localDate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/checkin/localDate.ts | grep -c "export function formatLocalMonthDay"`가 1이다
    - `grep -v '^//' src/checkin/localDate.ts | grep -c "ko-KR"`가 1이다
    - `grep -v '^//' src/checkin/localDate.ts | grep -Ec "getMonth\(\)|getDate\(\)|slice\(|substring\("`가 0이다 (수동 파싱 부재)
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/localDate.test.ts`가 종료 코드 0이고 `formatLocalMonthDay` 블록의 4개 케이스가 전부 통과한다
    - 자정 경계 테스트(`2026-08-31T16:00:00Z` / `Asia/Seoul` → `'9월 1일'`)가 존재하고 통과한다
  </acceptance_criteria>
  <done>`formatLocalMonthDay`가 Intl만으로 구현돼 있고, 자정 경계를 포함한 4개 케이스가 그린이다.</done>
</task>

<task type="auto">
  <name>Task 3: PhotoStorageDeps에 deleteFile 포트를 추가하고 구현/테스트 더블을 맞춘다</name>
  <files>src/checkin/config.ts, src/checkin/deps.ts, src/checkin/testing/fakePhotoStorage.ts</files>
  <read_first>
    - src/checkin/config.ts (34~41줄 — PhotoStorageDeps 타입과 "왜 좁힌 함수 포트인가" 주석)
    - src/checkin/deps.ts (1~6줄 헤더의 "런타임 네이티브 import는 이 파일만" 규약 + 68~78줄 defaultPhotoStorageDeps)
    - src/checkin/testing/fakePhotoStorage.ts (전체 — 더블의 __copies/__setShouldThrow 관례)
    - src/checkin/__tests__/nativeDeps.test.ts (네이티브 import 격리 회귀 가드가 무엇을 스캔하는지)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pitfall 4)
  </read_first>
  <action>
세 파일을 한 태스크로 묶는다 — 타입/구현/더블이 동시에 바뀌지 않으면 `tsc --noEmit`이
깨지므로 원자적으로 처리해야 한다.

1. `src/checkin/config.ts`: 기존 `PhotoStorageDeps` 타입에 `deleteFile(uri: string):
   Promise<void>` 메서드를 **추가**한다(평행한 새 타입을 만들지 않는다). 왜 좁힌 함수
   포트인지에 대한 기존 주석 블록에 이 메서드도 같은 근거로 포함됨을 한 줄 덧붙인다.
2. `src/checkin/deps.ts`: `defaultPhotoStorageDeps` 객체 리터럴에 `async deleteFile(uri) {
   const file = new File(uri); await file.delete(); }`를 추가한다. `expo-file-system/legacy`의
   `deleteAsync`나 문자열 경로 접합을 쓰지 않는다 — 이 파일이 이미 채택한 새 클래스
   API(`File`/`Paths`)만 쓴다(05-RESEARCH.md §State of the Art: `deleteAsync`는 최상위
   import에서 런타임 throw하는 경고 스텁이다). `File`은 이미 이 파일에 import돼 있으므로
   새 import를 추가하지 않는다.
3. `src/checkin/testing/fakePhotoStorage.ts`: 더블에 `deleteFile` 구현을 추가한다. 실제
   파일시스템을 건드리지 않고 삭제 호출 기록만 메모리에 남기며, 기존 `__copies()`와 대칭인
   `__deletions(): string[]` 인스펙터를 노출한다(`FakePhotoStorage` 타입에도 추가).
   `__setShouldThrow(true)`가 켜져 있으면 `deleteFile`도 throw하게 해 복사와 동일한 실패
   주입 경로를 유지한다. 파일 헤더의 "실제 네이티브 모듈과 다르게 동작하는 지점" 목록에
   삭제 관련 항목(존재하지 않는 파일 삭제 시 실제 모듈의 동작을 재현하지 않는다)을 추가한다.

`src/checkin/` 하위의 어떤 파일도 새로 `expo-file-system`을 import하지 않는다 —
`nativeDeps.test.ts`의 격리 가드가 이를 이미 강제하고 있으므로, 이 태스크는 그 가드를
그린으로 유지한 채 통과해야 한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/__tests__/nativeDeps.test.ts src/checkin/testing/fakeCheckinDeps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/checkin/config.ts | grep -c "deleteFile(uri: string): Promise<void>"`가 1이다
    - `grep -v '^//' src/checkin/deps.ts | grep -c "new File(uri)"`가 1이다
    - `grep -v '^//' src/checkin/deps.ts | grep -Ec "deleteAsync|expo-file-system/legacy"`가 0이다
    - `grep -v '^//' src/checkin/testing/fakePhotoStorage.ts | grep -Ec "deleteFile|__deletions"`가 3 이상이다
    - `grep -v '^//' src/checkin/testing/fakePhotoStorage.ts | grep -c "expo-file-system"`가 0이다 (더블은 네이티브를 import하지 않는다)
    - `npx tsc --noEmit`이 종료 코드 0이다 (타입/구현/더블 3자 정합)
    - `nativeDeps.test.ts`의 "런타임 네이티브 import는 deps.ts 하나뿐" 단언이 여전히 통과한다
  </acceptance_criteria>
  <done>`PhotoStorageDeps`가 `deleteFile`를 노출하고, 실구현(`new File(uri).delete()`)과 테스트 더블(`__deletions()`)이 모두 타입을 만족하며 `tsc --noEmit`과 격리 가드가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 화면 → SQLite (`checkinRepo.ts`) | id 문자열이 SQL 문장에 도달하는 경계 — 파라미터 바인딩으로만 통과시킨다 |
| 화면 → 파일시스템 (`PhotoStorageDeps.deleteFile`) | uri 문자열이 `documentDirectory` 파일 삭제에 도달하는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | `getCheckinById` / `deleteCheckin`의 SQL 문장 | mitigate | ASVS V5 — `?` 파라미터 바인딩만 사용하고 템플릿 보간을 금지한다. acceptance criteria가 `checkinRepo.ts` 전체에서 `${` 출현 수 0을 게이트(주석 제외 후 카운트) |
| T-05-04 | Tampering | `deleteFile(uri)`의 경로 인자 | mitigate | 이 포트에 넘어오는 uri는 항상 DB `photo_path` 컬럼 값이며, 그 값은 `photos.ts`가 `crypto.randomUUID()` 기반 파일명으로 `documentDirectory`에 복사할 때 생성한 것이다 — 사용자 자유 입력이 경로에 들어오는 경로가 없다(기존 `photos.ts` 원칙 유지). 새 경로 조작 표면을 만들지 않기 위해 `deleteFile`은 문자열 접합/`..` 정규화를 일절 수행하지 않고 받은 uri를 그대로 `new File()`에 넘긴다 |
| T-05-05 | Information Disclosure | `src/checkin/` 하위 파일의 네이티브 모듈 직접 import 확산 | mitigate | `deps.ts` 단일 소유 규약 유지 — `nativeDeps.test.ts` 격리 가드를 그린으로 유지하는 것이 Task 3의 acceptance criteria |
| T-05-SC | Tampering | npm/pip/cargo installs | N/A | 신규 패키지 설치 없음(05-RESEARCH.md §Package Legitimacy Audit — 적용 대상 없음). 이 plan에서 `npm install`을 실행하지 않는다 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/` 전부 그린
- `npx tsc --noEmit` 통과
- `checkinRepo.ts`에 SQL 문자열 보간 0건, `expo-file-system` import 0건
</verification>

<success_criteria>
- `getCheckinById` / `deleteCheckin` / `formatLocalMonthDay` / `PhotoStorageDeps.deleteFile` 네 가지가 전부 존재하고 각각 테스트로 고정됐다
- 화면 레이어가 이 phase에서 SQL이나 `expo-file-system`을 직접 만질 필요가 없어졌다
</success_criteria>

<output>
Create `.planning/phases/05-check-in-detail-edit/05-02-SUMMARY.md` when done
</output>
