---
phase: 05-check-in-detail-edit
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - src/checkin/CheckinDetailScreen.tsx
  - src/checkin/checkinFlow.ts
  - src/app/__tests__/checkin-detail-wiring.test.ts
autonomous: true
requirements: [REQ-checkin-detail-base, REQ-checkin-detail-layout, REQ-checkin-detail-flush, REQ-maps-deeplink]

must_haves:
  truths:
    - "사용자가 상세화면에서 메모를 자유롭게 편집할 수 있다"
    - "메모를 수정한 뒤 인앱 뒤로가기/스와이프백을 하면 3버튼 경고 다이얼로그가 뜬다(계속 편집 / 저장하지 않고 나가기 / 저장하고 나가기)"
    - "메모가 수정되지 않았으면 경고 없이 그대로 나간다"
    - "앱이 백그라운드로 전환되면 경고 없이 조용히 메모가 저장된다"
    - "\"지도 앱에서 열기\"를 누르면 저장되지 않은 메모가 먼저 flush된 뒤 지도 앱이 열린다"
    - "메모 저장이 두 번 다 실패하면 \"저장하지 못했어요\" + \"다시 시도\"가 메모 아래 인라인으로 보인다"
  artifacts:
    - path: "src/checkin/checkinFlow.ts"
      provides: "CHECKIN_DETAIL_COPY — 미저장 경고 다이얼로그 + 지도 앱 열기 문구 단일 출처"
      contains: "CHECKIN_DETAIL_COPY"
    - path: "src/checkin/CheckinDetailScreen.tsx"
      provides: "메모 편집/저장, beforeRemove 경고, AppState flush, Maps 딥링크"
      contains: "beforeRemove"
  key_links:
    - from: "src/checkin/CheckinDetailScreen.tsx"
      to: "checkinRepo.updateCheckinNoteAndPhoto"
      via: "runWithSingleRetry로 감싼 flush"
      pattern: "runWithSingleRetry"
    - from: "src/checkin/CheckinDetailScreen.tsx"
      to: "React Navigation pop 파이프라인"
      via: "navigation.addListener('beforeRemove')"
      pattern: "addListener\\('beforeRemove'"
    - from: "src/checkin/CheckinDetailScreen.tsx"
      to: "iOS 시스템 Maps 앱"
      via: "Linking.openURL(maps.apple.com URL)"
      pattern: "maps\\.apple\\.com"
    - from: "src/checkin/CheckinDetailScreen.tsx"
      to: "OS 생명주기"
      via: "AppState.addEventListener('change')"
      pattern: "AppState\\.addEventListener"
---

<objective>
상세화면에 **편집 계층**을 붙인다: 메모 TextInput + 명시적 저장, 인앱 이탈 시 3버튼 미저장
경고(D-01), `AppState` 백그라운드 전환 시 조용한 flush(D-02), 그리고 flush를 선행시키는
"지도 앱에서 열기" 딥링크.

이 phase의 핵심 결정이 여기 모여 있다: **같은 "저장" 함수를 서로 다른 트리거가 서로 다른
방식으로 부른다.** 인앱 이탈은 경고 후 사용자 선택에 따라, OS 백그라운드 전환은 경고 없이
조용히, 딥링크는 호출 직전에 명시적으로. D-01과 D-02는 모순이 아니라 서로 다른 트리거에
대한 서로 다른 처리다(05-CONTEXT.md 확정).

Purpose: Day-end 회고(Phase 7)의 "5초 디바운스 자동저장, 경고 없음" 모델과 **의도적으로
다른** 모델이다 — 사용자가 논의에서 product-design.md T13의 명시적 경고 방식을 직접
선택했다(D-01). 구현 중 회고 패턴으로 "통일"하려는 유혹을 반드시 거부해야 한다.
Output: 완결된 메모 편집 흐름 + 3개 저장 트리거 + 딥링크 + 저장 실패 UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-check-in-detail-edit/05-CONTEXT.md
@.planning/phases/05-check-in-detail-edit/05-RESEARCH.md
@.planning/phases/05-check-in-detail-edit/05-PATTERNS.md
@.planning/phases/05-check-in-detail-edit/05-UI-SPEC.md
@DESIGN.md
</context>

<interfaces>
<!-- 실행자가 코드베이스를 뒤지지 않아도 되도록 미리 추출한 계약 -->

재사용할 기존 함수(시그니처 변경 없음):
```ts
// src/checkin/checkinRepo.ts
export async function runWithSingleRetry<T>(attempt: () => Promise<T>): Promise<{ ok: true; value: T } | { ok: false }>;
export async function updateCheckinNoteAndPhoto(db: MigratableDb, id: string,
  args: { note: string | null; photoPath: string | null; now: string }): Promise<void>;
// src/checkin/localDate.ts
export function toIsoTimestamp(date?: Date): string;
```

기존 문구 상수(그대로 재사용 — 새로 만들지 않는다):
```ts
// src/checkin/checkinFlow.ts:120-134
CHECKIN_COPY.notePlaceholder      // '지금 여기서 뭘 하고 있나요?'
CHECKIN_COPY.saveFailedHeadline   // '저장하지 못했어요'
CHECKIN_COPY.saveFailedHelper     // '저장 공간을 확인해주세요'
CHECKIN_COPY.retryCta             // '다시 시도'
```

`CheckinActionCard.tsx`의 SAVE_FAILED 분기가 쓰는 시각 구성(복제 대상):
`typography.screenTitle` 헤드라인 → `spacing.xs` 간격 → `typography.helperText` +
`colors.textMuted` 보조문구 → `spacing.md` 간격 → `primaryButton` Pressable
(`accessibilityRole="button"`, `accessibilityLabel={CHECKIN_COPY.retryCta}`,
라벨은 `typography.placeName`).
`CheckinActionCard.tsx`의 노트 입력부: `<TextInput multiline value onChangeText onBlur
placeholder={CHECKIN_COPY.notePlaceholder} placeholderTextColor={colors.textFaint}
style={[typography.journalEntry, styles.noteInput]} />`.

`src/app/(tabs)/index/index.tsx`의 기존 AppState 리스너(복제할 관용구, 918~939줄):
```tsx
useEffect(() => {
  const subscription = AppState.addEventListener('change', (nextAppState) => {
    if (nextAppState === 'active') { /* ... */ return; }
    const current = stateRef.current;
    if (canEditNoteAndPhoto(current)) { flushNoteAndPhoto(); }
  });
  return () => subscription.remove();
}, [flushNoteAndPhoto, reloadTodayCheckins]);
```

`beforeRemove` 관용구(05-RESEARCH.md Pattern 4가 `expo-router/build/react-navigation/core/
useOnPreventRemove.js` 소스로 검증 완료 — 헤더 뒤로가기 버튼과 엣지 스와이프백 **둘 다**
동일한 POP 액션 파이프라인에 물려 있어 우회 경로가 없다):
```tsx
const sub = navigation.addListener('beforeRemove', (e) => {
  if (!isDirtyRef.current) return;
  e.preventDefault();
  Alert.alert(/* title */, undefined, [ /* 3 buttons */ ]);
});
// 재개는 navigation.dispatch(e.data.action)
```

Maps 딥링크 URL(product-design.md:874가 이미 확정한 값):
`http://maps.apple.com/?ll=${lat},${lng}` — `canOpenURL` 사전 체크는 생략한다(iOS 시스템
앱이라 항상 존재, product-design.md:943 "해당 없음").
`expo-linking`은 이미 `src/checkin/permissions.ts`가 `openSettings()`로 쓰는 중이다.

`CheckinRow.lat` / `CheckinRow.lng`는 SQLite `REAL` 컬럼이며 TypeScript 타입도 `number`다 —
사용자가 자유 입력한 문자열이 아니다(T-05-08 참고).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 메모 편집 + 명시적 저장(flushNoteAndPhoto) + 저장 실패 인라인 UI</name>
  <files>src/checkin/checkinFlow.ts, src/checkin/CheckinDetailScreen.tsx</files>
  <read_first>
    - src/checkin/CheckinDetailScreen.tsx (05-03-PLAN.md 산출물 — 사진 블록 뒤에 메모 슬롯을 이어 붙인다)
    - src/checkin/checkinFlow.ts (117~134줄 CHECKIN_COPY — 새 상수를 같은 파일 같은 스타일로 추가)
    - src/components/CheckinActionCard.tsx (122~145줄 노트 입력, 145~172줄 SAVE_FAILED 분기, 216~250줄 noteInput/primaryButton 스타일)
    - src/checkin/checkinRepo.ts (31~49줄 runWithSingleRetry 주석 — "Phase 5가 그대로 재사용하도록 설계됨")
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§저장 트리거 매트릭스, §수정 저장 실패 UI, §Typography)
  </read_first>
  <behavior>
    - 메모를 한 글자라도 바꾸면 dirty 플래그가 참이 된다
    - 텍스트필드 blur(포커스만 이탈)만으로는 아무 것도 저장되지 않고 dirty 상태가 유지된다 — Phase 3 캡처 시점의 "blur 즉시 커밋"과 의도적으로 다르다(D-01)
    - `flushNoteAndPhoto()` 성공 시 dirty 플래그가 거짓으로 돌아가고 저장 실패 UI가 사라진다
    - `updateCheckinNoteAndPhoto`가 2회 연속 throw하면 저장 실패 UI가 나타나고 dirty 플래그는 참으로 남는다
    - "다시 시도"를 누르면 같은 flush가 재실행된다
    - 빈 문자열 메모는 `null`로 저장된다(기존 `(tabs)/index/index.tsx`의 `state.note || null` 관례와 동일)
  </behavior>
  <action>
**1) `src/checkin/checkinFlow.ts`에 `CHECKIN_DETAIL_COPY` 상수를 추가한다.** `CHECKIN_COPY`
바로 아래에 같은 `as const` 스타일로 두고, 05-UI-SPEC.md §Copywriting Contract가 확정한
문구를 그대로 전사한다(문구를 여기서 발명하지 않는다 — 이 저장소의 단일 출처 규약):
- `unsavedTitle`: `'저장하지 않은 변경사항이 있어요'`
- `keepEditing`: `'계속 편집'`
- `discardAndLeave`: `'저장하지 않고 나가기'`
- `saveAndLeave`: `'저장하고 나가기'`
- `openInMaps`: `'지도 앱에서 열기'`
새 문구를 `CHECKIN_COPY`에 섞지 않고 별도 상수로 분리하는 이유(체크인 캡처 흐름의 문구와
상세화면 편집 흐름의 문구를 구분해, 한쪽 변경이 다른 쪽 회귀 가드를 건드리지 않게 함)를
주석으로 남긴다.

**2) `CheckinDetailScreen.tsx`에 메모 상태를 추가한다.**
- `const [note, setNote] = useState('')` — 체크인 로드 시 `checkin.note ?? ''`로 초기화.
- `const isDirtyRef = useRef(false)` — 최신 dirty 값을 리스너가 재구독 없이 읽기 위한 ref
  미러(`(tabs)/index/index.tsx`의 `stateRef` 관용구와 동일한 이유). `noteRef`도 같이 두어
  flush가 최신 메모 값을 읽게 한다.
- `onChangeText`: `setNote(next)` + `noteRef.current = next` + `isDirtyRef.current = true`.
- `onBlur`: **아무 것도 하지 않는다.** 핸들러 자체를 붙이지 않는다(05-UI-SPEC.md 저장 트리거
  매트릭스: "텍스트필드 blur → 아무 것도 하지 않는다"). 왜 Phase 3의 blur 커밋 패턴을
  따르지 않는지 주석으로 명시한다 — 이게 D-01의 핵심이라 나중에 "일관성" 명목으로
  되돌려지기 가장 쉬운 지점이다.

**3) `flushNoteAndPhoto`를 `useCallback`으로 구현한다.**
`runWithSingleRetry(() => updateCheckinNoteAndPhoto(db, checkinId, { note: noteRef.current ||
null, photoPath: <현재 photo_path 상태>, now: toIsoTimestamp() }))`를 호출한다. 결과가
`{ ok: true }`면 `isDirtyRef.current = false`로 내리고 저장 실패 state를 해제한다.
`{ ok: false }`면 저장 실패 state를 세우고 dirty는 **참으로 유지**한다(아직 저장 안 됨).
deps 배열은 `[db, checkinId]`처럼 안정적인 값만 담아 매 키 입력마다 재생성되지 않게 한다 —
Task 2의 AppState 리스너가 이 함수를 deps로 쓰기 때문에, 매 렌더 새 함수가 되면 리스너가
키 입력마다 재구독된다.

**4) 메모 슬롯을 렌더한다(레이아웃 5번, 사진 블록 아래 `spacing.lg` 간격 뒤).**
`TextInput multiline`, `value={note}`, `onChangeText`,
`placeholder={CHECKIN_COPY.notePlaceholder}`, `placeholderTextColor={colors.textFaint}`,
스타일 `[typography.journalEntry, styles.noteInput]`. `noteInput`은 `minHeight: 96`,
`backgroundColor: colors.surface`, `borderRadius: radius.md`, 내부 패딩 `spacing.md`.
`typography.journalEntry`는 이 TextInput **한 곳에만** 쓴다 — 버튼 라벨/헤더/안내 문구에
절대 쓰지 않는다(Phase 3/4 계승 원칙).

**5) 저장 실패 UI를 메모 바로 아래 인라인으로 렌더한다.**
저장 실패 state가 참일 때만 마운트한다(disable이 아니라 조건부 마운트 — 이 저장소의
프레젠테이셔널 계약). `CheckinActionCard.tsx`의 SAVE_FAILED 분기와 **시각적으로 동일한**
구성을 복제한다: `typography.screenTitle` + `CHECKIN_COPY.saveFailedHeadline` →
`typography.helperText`/`colors.textMuted` + `CHECKIN_COPY.saveFailedHelper` →
`primaryButton` 스타일 Pressable(`accessibilityRole="button"`,
`accessibilityLabel={CHECKIN_COPY.retryCta}`)이 `flushNoteAndPhoto`를 재호출.

**컴포넌트 재사용 범위 결정(05-CONTEXT.md Claude's Discretion에 대한 확정 답):**
`CheckinActionCard`를 import해 재사용하지 **않고** 스타일/문구만 복제한다. 이유:
`CheckinActionCard`는 `checkinFlow` 상태 머신의 `phase` prop에 렌더를 분기하는
캡처 흐름 전용 컴포넌트라, 상태 머신이 없는 상세화면(D-01은 평범한 boolean dirty 추적)이
쓰려면 phase를 가짜로 만들어 넘겨야 한다 — 그건 재사용이 아니라 오염이다. 문구 상수
(`CHECKIN_COPY.*`)는 그대로 공유하므로 문구 드리프트는 발생하지 않는다. 이 판단 근거를
저장 실패 블록 위 주석에 남긴다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-detail-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/checkin/checkinFlow.ts | grep -c "CHECKIN_DETAIL_COPY"`가 1이다
    - `grep -c "저장하지 않은 변경사항이 있어요" src/checkin/checkinFlow.ts`가 1이고, 같은 문자열이 `src/checkin/CheckinDetailScreen.tsx`에는 0회 등장한다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "runWithSingleRetry"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "updateCheckinNoteAndPhoto"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "onBlur"`가 0이다 (blur 저장 없음 — D-01)
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "typography.journalEntry"`가 1이다 (메모 한 곳 전용)
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -Ec "saveFailedHeadline|saveFailedHelper|retryCta"`가 3이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -Ec "setTimeout|debounce"`가 0이다 (디바운스 자동저장 미채택 — D-01)
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "isDirtyRef"`가 3 이상이다 (set/clear/read)
    - `npx tsc --noEmit`이 종료 코드 0이다
  </acceptance_criteria>
  <done>메모를 편집하면 dirty가 서고, `flushNoteAndPhoto`가 단일 재시도로 저장하며, 2회 실패 시 인라인 실패 UI가 마운트된다. blur나 타이머로 저장되는 경로가 소스에 존재하지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: "지도 앱에서 열기" 딥링크(flush 선행) + AppState 백그라운드 조용한 flush</name>
  <files>src/checkin/CheckinDetailScreen.tsx</files>
  <read_first>
    - src/checkin/CheckinDetailScreen.tsx (Task 1 산출물 — flushNoteAndPhoto가 이미 존재)
    - src/app/(tabs)/index/index.tsx (912~939줄 — 복제할 AppState 리스너 관용구와 'active' 분기 가드)
    - src/checkin/permissions.ts (expo-linking을 이미 쓰는 기존 지점 — import 경로 관례)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pattern 5, Pattern 6, Anti-Patterns의 "AppState 리스너를 active 분기 없이 등록")
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§저장 트리거 매트릭스, §레이아웃 구조 3번 슬롯)
  </read_first>
  <action>
**1) "지도 앱에서 열기" 버튼을 레이아웃 3번 슬롯(정적 지도 아래, `spacing.sm` 간격 뒤,
사진 위)에 렌더한다.** `Pressable` + `accessibilityRole="button"` +
`accessibilityLabel={CHECKIN_DETAIL_COPY.openInMaps}`. 라벨 텍스트는
`typography.placeName` + `colors.textMuted`, 배경 없는 텍스트 버튼, 좌측 정렬,
`minHeight: 44`(44pt 터치 타겟). **`colors.accent`도 `colors.pin`도 쓰지 않는다** — accent는
DESIGN.md 2026-08-31 18:10 갱신 이후 캘린더 탭 전용 2개 용도로 좁혀져 이 phase에서는 아예
쓰이지 않고, `colors.pin`(테라코타)은 이 phase에서 스와이프 삭제 어포던스 1곳 전용이다
(05-05-PLAN.md). 이 버튼은 "가벼운 보조 액션"이므로 muted 톤을 유지한다
(05-UI-SPEC.md §Color).

**2) 핸들러:** `flushNoteAndPhoto()`를 먼저 `await`한 뒤
`Linking.openURL(\`http://maps.apple.com/?ll=${checkin.lat},${checkin.lng}\`)`를 호출한다.
`canOpenURL` 사전 체크를 넣지 않는다(iOS 시스템 앱이라 항상 존재 —
product-design.md:943). URL 템플릿에는 `checkin.lat`과 `checkin.lng` 두 개의 `number`
값만 보간한다 — 메모/장소명 등 다른 필드나 사용자 입력 문자열을 절대 URL에 넣지 않는다
(T-05-08).

Task 3의 `AppState` flush가 별도로도 안전망이 되지만, 딥링크 호출 직전에도 명시적으로 한 번
flush하는 이유(두 경로 모두 안전망이 되게 하는 DRY 다중 트리거 원칙 —
day-end-reflection-map.md와 동일)를 주석으로 남긴다. 이중 저장은 같은 값을 두 번 쓰는 것뿐이라
부작용이 없다.

**3) `AppState` 리스너를 추가한다(REQ-checkin-detail-flush / D-02).**
`useEffect`에서 `AppState.addEventListener('change', ...)`를 구독하고 cleanup에서
`subscription.remove()`한다. 콜백은 **반드시** `if (nextAppState === 'active') return;`
가드로 시작한다 — `active`로 **돌아올 때도** 콜백이 불리므로 이 가드가 없으면 포그라운드
복귀 시 의도치 않은 저장이 발생한다(05-RESEARCH.md Anti-Patterns). 가드를 통과하면
`if (isDirtyRef.current) flushNoteAndPhoto();`만 수행한다 — **다이얼로그/Alert를 절대 띄우지
않는다**(D-02: OS 백그라운드 전환은 조용히 flush, 경고는 인앱 이탈 전용). 오늘 뷰 리스너에
있는 `active` 분기의 알림 배너/리로드 로직은 상세화면에 해당 없으므로 복제하지 않는다.

deps 배열은 `[flushNoteAndPhoto]` 하나만 둔다 — Task 1에서 `flushNoteAndPhoto`를 안정적인
`useCallback`으로 만든 이유가 바로 이것이며(매 키 입력마다 재구독 방지), `isDirtyRef`/
`noteRef` ref 미러 관용구가 최신 값 접근을 담당한다. 이 근거를 주석으로 남긴다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-detail-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "maps.apple.com"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "canOpenURL"`가 0이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "CHECKIN_DETAIL_COPY.openInMaps"`가 1 이상이다
    - URL 보간 인자 검증: `grep -o 'll=\${[^`]*' src/checkin/CheckinDetailScreen.tsx`의 출력에 `checkin.lat`과 `checkin.lng`만 등장하고 `note`/`placeName`/입력 state 식별자가 등장하지 않는다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "AppState.addEventListener"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "nextAppState === 'active'"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "subscription.remove()"`가 1이다
    - AppState 콜백 본문에 `Alert`가 없다: `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | sed -n '/AppState.addEventListener/,/^\s*}, \[/p' | grep -c "Alert"`가 0이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "colors.accent"`가 0이다
    - `npx tsc --noEmit`이 종료 코드 0이다
  </acceptance_criteria>
  <done>딥링크 버튼이 muted 톤으로 3번 슬롯에 있고 flush 후 Apple Maps URL을 열며, AppState 리스너가 `active` 가드를 갖추고 dirty일 때만 조용히 flush한다.</done>
</task>

<task type="auto">
  <name>Task 3: beforeRemove 3버튼 미저장 경고(D-01) + wiring 테스트를 5요소 레이아웃으로 확장</name>
  <files>src/checkin/CheckinDetailScreen.tsx, src/app/__tests__/checkin-detail-wiring.test.ts</files>
  <read_first>
    - src/checkin/CheckinDetailScreen.tsx (Task 1/2 산출물)
    - src/app/__tests__/checkin-detail-wiring.test.ts (05-03-PLAN.md가 만든 5개 describe 블록 — 레이아웃 순서 단언을 확장하고 새 블록을 추가한다)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pattern 4 — 검증 근거와 정확한 코드 형태)
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§미저장 경고 다이얼로그, §iOS 네이티브 컴포넌트 기본값 함정)
  </read_first>
  <action>
**1) `beforeRemove` 리스너를 추가한다.** `useNavigation()`(expo-router 공개 API)의
`addListener('beforeRemove', ...)`를 `useEffect`에서 구독한다. `usePreventRemove`는
`expo-router` 최상위 export에 없는 내부 구현이므로 쓰지 않는다(05-RESEARCH.md
§Alternatives Considered).

콜백 로직:
- `if (!isDirtyRef.current) return;` — dirty가 아니면 그대로 나가게 둔다.
- dirty면 `e.preventDefault()` 후 `Alert.alert(CHECKIN_DETAIL_COPY.unsavedTitle, undefined,
  [...])`로 **3버튼** 다이얼로그를 띄운다:
  1. `CHECKIN_DETAIL_COPY.keepEditing` — `style: 'default'`, `onPress` 없음(다이얼로그만 닫힘)
  2. `CHECKIN_DETAIL_COPY.discardAndLeave` — `style: 'default'`, `onPress`에서
     `navigation.dispatch(e.data.action)`만 호출(저장하지 않음)
  3. `CHECKIN_DETAIL_COPY.saveAndLeave` — `style: 'default'`, `onPress`에서
     `flushNoteAndPhoto()` 후 `navigation.dispatch(e.data.action)`

**세 버튼 모두 `style: 'default'`다 — `style: 'destructive'`를 절대 쓰지 않는다.** iOS는
`destructive` 버튼을 자동으로 빨간 텍스트로 렌더하는데, DESIGN.md는 UI 전역에서 빨강 계열
시맨틱 색상을 금지한다(Phase 4가 발견한 "탭바 기본 파란 틴트"와 같은 종류의 플랫폼 기본값
함정). 이 금지 사유를 Alert 호출부 주석에 명시한다.

`e.data.action`을 나중에 그대로 `navigation.dispatch`하면 원래 막았던 pop이 재실행된다 —
React Navigation의 표준 "preventing going back" 관용구다. deps 배열은
`[navigation, flushNoteAndPhoto]`.

이 리스너가 헤더 뒤로가기 버튼과 엣지 스와이프백 **둘 다**를 커버한다는 근거(둘 다 동일한
POP 액션 디스패치 파이프라인에 물려 있으며, 05-RESEARCH.md Pattern 4가
`expo-router/build/react-navigation/core/useOnPreventRemove.js` 소스로 직접 확인함)를
주석으로 남긴다 — UI-SPEC이 "구현 시 확인 필요"로 남겼던 항목의 해소 기록이다.

**2) `checkin-detail-wiring.test.ts`를 확장한다.**
- 기존 `describe('상세화면 고정 레이아웃 순서 (REQ-checkin-detail-layout)')`의 3요소 순서
  단언을 **5요소**로 확장한다: `formatLocalTime` < `MapView` < `openInMaps` < `photo_path`
  < `notePlaceholder` 순서로 `indexOf` 인덱스가 증가한다. 05-03-PLAN.md가 남긴 "5요소로
  확장한다"는 주석을 이 변경으로 소진하고 주석을 갱신한다.
- 새 `describe('미저장 경고 (D-01)')`:
  `addListener('beforeRemove'` 등장 / `e.preventDefault()` 등장 /
  `navigation.dispatch(e.data.action)`이 2회 등장(저장 안 함 + 저장 후 나가기) /
  `CHECKIN_DETAIL_COPY`의 3개 버튼 키가 전부 등장 /
  **`style: 'destructive'`가 0회 등장**(빨강 금지 회귀 가드) /
  `isDirtyRef` 조기 반환 가드 존재.
- 새 `describe('AppState 백그라운드 flush (REQ-checkin-detail-flush, D-02)')`:
  `AppState.addEventListener('change'` 등장 / `nextAppState === 'active'` 가드 존재 /
  `subscription.remove()` cleanup 존재 / AppState 콜백 블록 안에 `Alert`가 등장하지 않음
  (D-02: 조용한 flush — 인앱 경고와 혼선되는 회귀를 막는다).
- 새 `describe('Maps 딥링크 (REQ-maps-deeplink)')`:
  `maps.apple.com` 등장 / `canOpenURL` 미등장 / 딥링크 핸들러 안에서 `flushNoteAndPhoto`
  호출이 `Linking.openURL` 호출보다 **앞선 인덱스**에 있다(flush 선행 계약) /
  URL 템플릿 보간 인자가 `checkin.lat`/`checkin.lng`뿐이다.
- 새 `describe('자동저장 미채택 (D-01 vs Phase 7)')`:
  `CheckinDetailScreen.tsx` 코드에 `setTimeout`/`debounce`/`onBlur`가 등장하지 않는다 —
  Phase 7의 디바운스 자동저장 패턴으로 "통일"되는 회귀를 막는 가드. 왜 이 화면만 다른
  모델인지(D-01, 사용자가 명시적으로 선택함) 주석으로 남긴다.

모든 단언은 `stripComments`를 통과한 코드에 대해서만 수행한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-detail-wiring.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "addListener('beforeRemove'"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "e.preventDefault()"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "e.data.action"`가 2다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "destructive"`가 0이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "style: 'default'"`가 3이다
    - `grep -c "describe(" src/app/__tests__/checkin-detail-wiring.test.ts`가 10이다 (기존 5 + 신규 5)
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-detail-wiring.test.ts`가 종료 코드 0이고 실패 0건이다
    - 가드가 실제로 무는지 확인: Alert의 "저장하지 않고 나가기" 버튼에 `style: 'destructive'`를 임시로 넣으면 위 jest 명령이 실패한다(확인 후 원복)
    - 가드가 실제로 무는지 확인: 딥링크 핸들러에서 `flushNoteAndPhoto()` 호출을 `Linking.openURL` 뒤로 임시로 옮기면 위 jest 명령이 실패한다(확인 후 원복)
    - `npm test` 전체 스위트가 종료 코드 0이다
  </acceptance_criteria>
  <done>3버튼 미저장 경고가 전부 `default` 스타일로 동작하고, wiring 테스트 10개 describe 블록이 5요소 레이아웃 순서·D-01·D-02·딥링크 flush 선행·자동저장 미채택을 전부 회귀 가드로 고정하며, 두 건의 의도적 파손 실험에서 실제로 실패한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력(메모 텍스트) → SQLite | 자유 입력 문자열이 DB에 쓰이는 경계 |
| 앱 → iOS 시스템 Maps 앱(URL scheme) | 앱 경계 밖으로 URL이 나가는 경계 |
| OS 생명주기(AppState) → 저장 로직 | 사용자 의도가 아닌 이벤트가 쓰기를 유발하는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | `http://maps.apple.com/?ll=` URL 보간 | mitigate | ASVS V5 — URL 템플릿에 `checkin.lat`/`checkin.lng`(SQLite `REAL` 컬럼, TS `number` 타입) **두 값만** 보간한다. 메모 등 사용자 자유 입력 문자열은 URL 구성에 일절 참여하지 않으며, Task 2/3의 acceptance criteria가 보간 인자 목록을 문자열 검사로 게이트한다 |
| T-05-09 | Tampering | 메모 텍스트 → `updateCheckinNoteAndPhoto` | mitigate | 기존 `checkinRepo.ts`의 `?` 파라미터 바인딩 UPDATE를 그대로 쓴다 — 이 화면은 SQL 문자열을 조립하지 않는다. 05-02-PLAN.md의 `${` 부재 게이트가 repo 측을 커버한다 |
| T-05-10 | Repudiation | `beforeRemove` 우회로 인한 초안 소실("경고를 못 봤다") | mitigate | 05-RESEARCH.md Pattern 4가 `useOnPreventRemove.js` 소스로 검증: `beforeRemove`는 UI 트리거(헤더 버튼/엣지 제스처)가 아니라 액션 디스패치 파이프라인에 물려 있어 두 경로 모두 동일하게 발동한다 — 우회 경로 없음. 추가 안전망으로 `AppState` 백그라운드 flush(D-02)가 OS 강제 전환 경로를 별도로 막는다 |
| T-05-11 | Denial of Service | `AppState` 리스너가 `active` 복귀 시에도 쓰기를 유발 | mitigate | 콜백 최상단 `if (nextAppState === 'active') return;` 가드를 필수화하고 wiring 테스트로 고정한다(05-RESEARCH.md Anti-Patterns) |
| T-05-12 | Information Disclosure | 저장 실패 UI가 내부 오류/경로를 노출 | mitigate | 실패 UI 문구는 `CHECKIN_COPY.saveFailedHeadline`/`saveFailedHelper` 고정 문자열만 쓴다 — 예외 메시지/스택/파일 경로를 화면에 렌더하지 않는다(`console.error`로만 남긴다) |
| T-05-SC | Tampering | npm/pip/cargo installs | N/A | 신규 패키지 설치 없음(05-RESEARCH.md §Package Legitimacy Audit). `expo-linking`은 `permissions.ts`가 이미 쓰고 있는 기존 의존성이다 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-detail-wiring.test.ts` 그린 (10개 describe)
- `npm test` 전체 스위트 그린
- `npx tsc --noEmit` 통과
- 의도적 파손 실험 3건(destructive 스타일 주입 / flush 순서 뒤집기 / pointerEvents 제거)에서 테스트가 실제로 실패
</verification>

<success_criteria>
- 메모 편집 → 인앱 이탈 시 3버튼 경고, OS 백그라운드 전환 시 조용한 flush, 딥링크 시 flush 선행 — 세 트리거가 각각 다르게 동작한다
- Alert 어디에도 `destructive` 스타일이 없다
- `setTimeout`/`debounce`/`onBlur` 기반 자동저장 경로가 소스에 존재하지 않는다(D-01)
</success_criteria>

<output>
Create `.planning/phases/05-check-in-detail-edit/05-04-SUMMARY.md` when done
</output>
