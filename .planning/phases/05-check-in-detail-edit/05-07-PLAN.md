---
phase: 05-check-in-detail-edit
plan: 07
type: execute
wave: 5
depends_on: [05-05, 05-06]
files_modified:
  - src/today/CheckinListRow.tsx
  - src/today/__tests__/todayUi.test.ts
autonomous: false
requirements: [REQ-checkin-detail-layout, REQ-checkin-swipe-delete]

must_haves:
  truths:
    - "전체 테스트 스위트와 타입체크가 그린이다"
    - "바텀시트 안에서 리스트 행을 가로로 스와이프하면 삭제 어포던스만 반응하고, 세로로 드래그하면 시트만 움직인다(제스처 경합 없음)"
    - "상세화면 정적 지도 미리보기의 마커가 체크인 위치에 pinSoft 색으로 정확히 렌더된다"
    - "행 탭 → 상세화면 push 후에도 탭바가 계속 보인다"
  artifacts:
    - path: ".planning/phases/05-check-in-detail-edit/05-07-SUMMARY.md"
      provides: "시뮬레이터 검증 항목별 결과 + 사용자 확인이 필요한 항목 구분"
  key_links:
    - from: "src/today/CheckinListRow.tsx"
      to: "@gorhom/bottom-sheet 세로 팬 제스처"
      via: "activeOffsetX / failOffsetY 튜닝값"
      pattern: "activeOffsetX"
---

<objective>
이 phase가 만든 **MEDIUM confidence 지점**과 **정적 분석으로 잡을 수 없는 시각/인터랙션
결과**를 실제로 실행해 확인한다.

05-RESEARCH.md 전체에서 유일하게 확신도가 낮은 항목은 `ReanimatedSwipeable`(가로 팬)을
`BottomSheetFlatList`(세로 팬이 이미 지배) 안에 중첩할 때의 제스처 경합이다 —
`@gorhom/bottom-sheet` GitHub 이슈 #1267/#1300으로만 확인됐고 이 저장소에서 실측된 적이
없다. `activeOffsetX={[-10, 10]}`/`failOffsetY={[-5, 5]}` 완화책을 넣어뒀지만, 그 값이
실제로 맞는지는 **돌려봐야만** 안다.

Purpose: CLAUDE.md의 시뮬레이터 우선 검증 원칙 — 이 phase의 검증 항목은 네이티브 모듈
구성이 바뀌지 않은 JS 레이어 인터랙션이라 iOS Simulator로 충분히 재현 가능하다. 사용자에게
무조건 넘기지 않고 Claude가 먼저 직접 확인한 뒤, 시뮬레이터로 원천 재현 불가능한 항목만
구분해 넘긴다.
Output: 항목별 검증 결과 + (필요 시) 제스처 임계값 튜닝 + 사용자 최종 확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05-check-in-detail-edit/05-VALIDATION.md
@.planning/phases/05-check-in-detail-edit/05-RESEARCH.md
@.planning/phases/05-check-in-detail-edit/05-UI-SPEC.md
@DESIGN.md
</context>

<interfaces>
<!-- 검증 절차와 판정 기준 -->

**시뮬레이터 실행 절차(CLAUDE.md 확정 순서):**
1. iOS Simulator 패널 attach
2. 가장 최근 DerivedData 빌드를 launch(없으면 빌드)
3. 이미 실행 중인 Metro dev server(`--dev-client`)에 연결
4. 스크린샷/탭으로 항목별 검증

이 phase는 신규 네이티브 패키지를 설치하지 않았으므로(05-RESEARCH.md §Package Legitimacy
Audit) 기존 dev client 빌드를 그대로 재사용할 수 있다 — 재빌드가 필요하지 않다.

**제스처 튜닝 파라미터(경합이 관측될 때만 조정):**
`src/today/CheckinListRow.tsx`의 `ReanimatedSwipeable`
- `activeOffsetX={[-10, 10]}` — 가로 이동이 이 값을 넘어야 스와이프 제스처가 활성화된다.
  가로 스와이프가 잘 안 잡히면 값을 줄이고(예: `[-5, 5]`), 시트 세로 스크롤 중 행이 옆으로
  밀리면 값을 키운다(예: `[-20, 20]`).
- `failOffsetY={[-5, 5]}` — 세로 이동이 먼저 이 값을 넘으면 스와이프를 포기하고 시트의 세로
  스크롤에 양보한다. 시트가 잘 안 끌리면 값을 줄이고, 스와이프 도중 시트가 따라 움직이면
  값을 키운다.

**시뮬레이터로 원천 재현 불가능한 범주(사용자에게 넘길 항목):**
실기기의 압력/속도 감지 미세 차이, 실제 조도에서의 색 인지, 실기기 고유 프레임 드랍.
이 phase에는 GPS 정확도 수렴이나 카메라 촬영 품질처럼 구조적으로 재현 불가능한 항목은
없다(위치는 이미 저장된 고정 좌표를 읽기만 한다).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 전체 게이트 실행 + 시뮬레이터로 인터랙션/시각 항목을 직접 검증한다</name>
  <files>src/today/CheckinListRow.tsx, src/today/__tests__/todayUi.test.ts</files>
  <read_first>
    - CLAUDE.md (§실기기 확인이 필요한 검증 단계 — 시뮬레이터 우선 원칙과 보고 규약)
    - .planning/phases/05-check-in-detail-edit/05-VALIDATION.md (§Manual-Only Verifications 2개 항목, §Sampling Rate의 Phase gate)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pitfall 3 — 무엇이 왜 MEDIUM인지)
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§정적 지도 미리보기, §스와이프 삭제, §Undo 스낵바 — 시각 판정 기준)
    - src/today/CheckinListRow.tsx (튜닝 대상 제스처 파라미터)
  </read_first>
  <action>
**1) Phase gate를 먼저 돌린다.** `npm test`(전체 스위트)와 `npx tsc --noEmit`이 모두 그린인지
확인한다. 하나라도 red면 시뮬레이터로 넘어가지 말고 원인을 해결한다 — 05-VALIDATION.md
§Sampling Rate의 Phase gate 정의다.

**2) 시뮬레이터를 띄우고 아래 8개 항목을 순서대로 직접 확인한다.** 각 항목마다 스크린샷 또는
관측 결과를 기록한다. 항목별로 PASS/FAIL과 근거를 남긴다.

*진입/네비게이션*
- (A) 오늘 뷰에서 체크인 행을 탭하면 상세화면이 push로 열린다.
- (B) 상세화면에서 **탭바가 계속 보인다**(nested Stack이 제대로 동작 — 05-01/05-03의 핵심
  가정). 탭바가 사라지면 라우트 구조가 잘못된 것이다.
- (C) 상세화면 헤더에 해당 체크인의 로컬 날짜(예: "8월 31일")가 뜨고, 오늘 뷰에는 빈
  네비게이션 바가 생기지 않는다(Pitfall 2 회귀 확인).

*정적 지도 미리보기*
- (D) 마커가 체크인 좌표 위치에 렌더되고, 색이 `pinSoft`(연한 테라코타)이지 `pin`(진한
  테라코타)이 아니다. teardrop 모양의 뾰족한 끝이 좌표를 가리킨다.
- (E) 지도를 스크롤/줌/회전/탭해도 **아무 반응이 없다**(5중 잠금이 실제로 동작).

*스와이프 삭제 — 이 plan의 핵심 검증 항목(Pitfall 3, MEDIUM)*
- (F) 바텀시트를 **세로로** 드래그하면 시트만 움직이고 행이 옆으로 밀리지 않는다.
- (G) 리스트 행을 **가로로** 스와이프하면 오른쪽에서 테라코타(`colors.pin` `#B85C38`) 삭제
  어포던스가 나오고,
  이때 시트가 세로로 따라 움직이지 않는다. 임계값을 넘기면 확인 없이 행이 사라지고 4초
  스낵바가 탭바 위에 뜬다.
- (H) "실행취소"를 누르면 행이 되돌아온다. 누르지 않고 4초를 기다리면 스낵바가 사라지고,
  앱을 재시작해도 그 체크인이 없다(DB에서 실제 삭제됨).

**3) (F)/(G)에서 경합이 관측되면 `activeOffsetX`/`failOffsetY`를 위 `<interfaces>`의 방향
지침대로 조정하고 다시 확인한다.** 값을 바꿨다면 그 값과 조정 근거(어떤 증상을 보고
어느 방향으로 얼마나 움직였는지)를 `CheckinListRow.tsx` 주석에 남기고,
`todayUi.test.ts`의 해당 회귀 가드가 새 값으로도 통과하는지 확인한다(가드는 파라미터의
**존재**를 단언하므로 값 변경으로 깨지지 않아야 정상 — 깨진다면 가드가 값을 하드코딩하고
있는 것이니 존재 단언으로 완화한다). 조정이 불필요했다면 "튜닝 없이 초기값으로 통과"를
명시적으로 기록한다.

**4) 결과를 SUMMARY에 기록할 때 CLAUDE.md 보고 규약을 따른다:** 어느 항목을 Claude가
시뮬레이터로 직접 확인했고 어느 항목이 사용자 확인이 필요한지 **구분해서** 명시한다.
시뮬레이터에서 재현되지 않은 항목이 있다면 "재현 불가"와 "PASS"를 혼동해 적지 않는다.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`가 종료 코드 0이고 실패 0건이다
    - `npx tsc --noEmit`이 종료 코드 0이다
    - SUMMARY에 (A)~(H) 8개 항목이 각각 PASS/FAIL과 근거(스크린샷 또는 관측 서술)와 함께 기록돼 있다
    - SUMMARY에 "Claude가 시뮬레이터로 직접 확인한 항목"과 "사용자 확인이 필요한 항목"이 별도 목록으로 구분돼 있다
    - 제스처 튜닝 여부가 명시돼 있다 — 조정했다면 `grep -c "activeOffsetX" src/today/CheckinListRow.tsx`가 여전히 1이고 조정 근거 주석이 존재하며, 조정하지 않았다면 "초기값 통과"가 SUMMARY에 기록돼 있다
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/today/__tests__/todayUi.test.ts`가 종료 코드 0이다(튜닝 후에도 가드 유지)
  </acceptance_criteria>
  <done>전체 스위트와 타입체크가 그린이고, 8개 인터랙션/시각 항목이 시뮬레이터에서 항목별로 검증됐으며, 제스처 경합 여부와 튜닝 결과가 기록됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 최종 확인 — 실기기 감각과 시각 톤</name>
  <files>(코드 변경 없음 — 사용자 확인 게이트)</files>
  <read_first>
    - .planning/phases/05-check-in-detail-edit/05-07-SUMMARY.md (Task 1이 기록한 항목별 시뮬레이터 검증 결과 — 사용자에게 먼저 제시한다)
    - CLAUDE.md (§실기기 확인이 필요한 검증 단계 — 무엇만 사용자에게 넘기는지)
    - DESIGN.md (§Color — 빨강 금지 원칙 / Decisions Log 2026-08-31 18:10 + 2026-09-01 — 스와이프 삭제 어포던스가 accent(올리브)에서 Pin(테라코타)으로 전환된 근거)
  </read_first>
  <action>
Task 1의 시뮬레이터 검증 결과를 먼저 사용자에게 제시한 뒤, 아래 `<how-to-verify>`의 5개
항목을 실기기에서 확인해 달라고 요청하고 응답을 기다린다. 이 태스크는 코드를 변경하지
않는다 — 사용자가 문제를 보고하면 그 내용을 SUMMARY의 "사용자 피드백" 절에 기록하고,
수정이 필요하면 별도 gap 처리로 넘긴다(이 태스크 안에서 즉흥 수정하지 않는다).
사용자가 "승인"하면 phase를 완료 처리한다.
  </action>
  <what-built>
Phase 5 전체(체크인 상세화면 + 메모 편집/미저장 경고/백그라운드 flush/지도 앱 딥링크 +
사진 교체·삭제 + 리스트 스와이프 삭제 + 4초 undo 스낵바)가 구현됐고, Task 1에서 Claude가
iOS Simulator로 (A)~(H) 8개 항목을 직접 확인했습니다.

시뮬레이터로 확인할 수 없는 나머지는 **실기기의 제스처 감각과 실제 화면에서의 색 인지**
두 범주뿐입니다.
  </what-built>
  <how-to-verify>
Task 1의 SUMMARY에 기록된 항목별 결과를 먼저 확인해 주세요. 그 다음 실기기에서:

1. **스와이프 감각** — 오늘 뷰 바텀시트에서 체크인 행을 왼쪽으로 스와이프해 보세요.
   손가락 속도를 빠르게/천천히 바꿔가며 몇 번 시도해, iOS 기본 앱(메일 등)의 스와이프
   삭제와 비교했을 때 임계값과 스프링 감각이 어색하지 않은지 봐 주세요.
   같은 리스트에서 시트를 세로로 스크롤할 때 행이 옆으로 밀리지 않는지도 함께요.
2. **삭제 어포던스 색** — 스와이프해서 나오는 삭제 배경이 Pin 테라코타(`#B85C38`)입니다.
   실제 화면 밝기에서 "삭제"로 읽히면서도 경고/위험 느낌(빨강)이 아닌지 봐 주세요.
   DESIGN.md의 빨강 금지 원칙을 지키되 삭제 의미가 전달돼야 합니다. 3번 항목의 상세화면
   마커(`pinSoft`, 연한 테라코타)와 번갈아 보면서 두 톤이 서로 뭉개지지 않고 "진한 쪽 =
   삭제 액션 / 옅은 쪽 = 저장된 핀"으로 구분되는지도 함께 봐 주세요(2026-09-01
   accent→Pin 전환으로 새로 생긴 인접 관계입니다).
3. **정적 지도 마커 색** — 체크인 행을 탭해 상세화면에 들어가면 지도 미리보기에 마커가
   하나 있습니다. 이 마커가 오늘 뷰 지도의 "저장된 체크인 핀"과 **같은 색**으로 보이는지
   봐 주세요(같은 체크인이 화면마다 다른 색으로 보이면 안 됩니다).
4. **미저장 경고 다이얼로그 톤** — 상세화면에서 메모를 조금 고친 뒤 뒤로가기를 누르면
   3버튼 다이얼로그가 뜹니다. 세 버튼 어디에도 빨간 글자가 없어야 하고, 문구가 담담한지
   ("정말", 느낌표, 경고 아이콘 없음) 봐 주세요.
5. **"지도 앱에서 열기"** — 메모를 고친 상태에서 이 버튼을 누르면 지도 앱이 열립니다.
   앱으로 돌아왔을 때 고쳤던 메모가 **그대로 남아 있는지** 확인해 주세요(경고 없이 조용히
   저장됐어야 합니다).

문제가 있으면 어느 항목의 무엇이 어떻게 다른지 알려주세요.
  </how-to-verify>
  <resume-signal>"승인"이라고 입력하시거나, 문제가 있는 항목과 증상을 알려주세요</resume-signal>
  <acceptance_criteria>
    - Task 1의 SUMMARY 항목별 결과가 사용자에게 제시됐다
    - 사용자가 5개 실기기 항목에 대해 "승인" 또는 구체적 문제 보고로 응답했다
    - 사용자 응답 내용이 SUMMARY의 "사용자 피드백" 절에 그대로 기록됐다
    - 이 태스크에서 소스 파일이 변경되지 않았다: `git status --porcelain src/ | wc -l`이 0이다
  </acceptance_criteria>
  <done>사용자가 실기기 감각(스와이프 임계값/스프링)과 시각 톤(삭제 어포던스 색, 마커 색 일치, Alert 빨강 부재)을 확인하고 승인했거나, 문제 항목이 SUMMARY에 기록됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (신규 없음) | 이 plan은 검증과 제스처 임계값 튜닝만 수행한다 — 새 입력 표면이나 데이터 경로를 만들지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Repudiation | 시뮬레이터에서 재현 불가한 항목이 "PASS"로 잘못 기록됨 | mitigate | SUMMARY 작성 시 "Claude 직접 확인"과 "사용자 확인 필요"를 **별도 목록으로 분리**하도록 acceptance criteria가 요구한다(CLAUDE.md 보고 규약) — "재현 불가"와 "통과"를 뭉뚱그리는 것을 구조적으로 막는다 |
| T-05-23 | Tampering | 제스처 튜닝 과정에서 회귀 가드를 값 고정으로 잘못 강화 | mitigate | Task 1이 가드를 파라미터의 **존재** 단언으로 유지하도록 명시한다 — 값을 하드코딩하는 가드는 정당한 튜닝을 막는 잘못된 가드이므로 완화 대상이다 |
| T-05-SC | Tampering | npm/pip/cargo installs | N/A | 신규 패키지 설치 없음. 기존 dev client 빌드를 그대로 재사용하며 재빌드도 필요하지 않다 |
</threat_model>

<verification>
- `npm test` 전체 스위트 그린
- `npx tsc --noEmit` 통과
- (A)~(H) 8개 시뮬레이터 항목 기록 완료
- 사용자 최종 확인 승인
</verification>

<success_criteria>
- Phase gate(전체 스위트 + 타입체크)가 그린이다
- MEDIUM confidence였던 제스처 경합이 실측으로 해소됐다(경합 없음 확인 또는 튜닝 후 해소)
- 시뮬레이터 확인 항목과 사용자 확인 항목이 SUMMARY에서 명확히 구분됐다
- 사용자가 실기기 감각/색 인지 항목을 승인했다
</success_criteria>

<output>
Create `.planning/phases/05-check-in-detail-edit/05-07-SUMMARY.md` when done
</output>
