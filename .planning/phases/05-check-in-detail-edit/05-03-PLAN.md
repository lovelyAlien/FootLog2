---
phase: 05-check-in-detail-edit
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/app/(tabs)/index/_layout.tsx
  - src/app/(tabs)/index/[id].tsx
  - src/app/(tabs)/index/index.tsx
  - src/checkin/CheckinDetailScreen.tsx
  - src/checkin/config.ts
  - src/app/__tests__/checkin-detail-wiring.test.ts
autonomous: true
requirements: [REQ-checkin-detail-base, REQ-checkin-detail-layout]

must_haves:
  truths:
    - "`/[id]` 라우트로 체크인 상세화면이 열리며 탭바가 계속 보인다"
    - "상세화면 헤더 타이틀에 해당 체크인의 로컬 날짜(예: \"8월 31일\")가 표시된다"
    - "상세화면 최상단에 체크인 시각이 모노스페이스로 표시된다"
    - "시각 아래에 해당 체크인 좌표를 중심으로 한 정적 지도 미리보기가 pinSoft 마커 1개와 함께 보이며, 스크롤/줌/회전/탭 어느 것으로도 반응하지 않는다"
    - "사진이 있으면 이미지가, 없으면 \"사진 추가\" 빈 슬롯이 보인다"
    - "상세화면에 체크인 전체 삭제 버튼이 존재하지 않는다(D-05)"
  artifacts:
    - path: "src/app/(tabs)/index/[id].tsx"
      provides: "체크인 상세 라우트(얇은 래퍼)"
      contains: "useLocalSearchParams"
      max_lines: 25
    - path: "src/checkin/CheckinDetailScreen.tsx"
      provides: "상세화면 본체 — Phase 6이 다른 라우트에서 재사용할 수 있게 라우트 파일 밖에 둔다"
      contains: "export function CheckinDetailScreen"
    - path: "src/app/__tests__/checkin-detail-wiring.test.ts"
      provides: "상세화면 레이아웃/토큰/D-05 회귀 가드"
  key_links:
    - from: "src/app/(tabs)/index/[id].tsx"
      to: "src/checkin/CheckinDetailScreen.tsx"
      via: "import + id/db prop 전달"
      pattern: "CheckinDetailScreen"
    - from: "src/checkin/CheckinDetailScreen.tsx"
      to: "checkinRepo.getCheckinById"
      via: "단건 조회"
      pattern: "getCheckinById\\("
    - from: "src/checkin/CheckinDetailScreen.tsx"
      to: "colors.pinSoft"
      via: "정적 지도 마커 색상"
      pattern: "colors\\.pinSoft"
---

<objective>
체크인 상세화면의 라우트 셸과 **표시 계층**을 만든다: `[id]` 라우트 등록, 얇은 라우트
래퍼, 그리고 `CheckinDetailScreen.tsx` 본체의 상단 3요소(시각 → 정적 지도 미리보기 → 사진).

이 plan은 화면을 "열고 보이게" 하는 데까지만 책임진다. 편집 계층(메모 입력·저장·미저장
경고·AppState flush·"지도 앱에서 열기" 딥링크)은 05-04-PLAN.md가, 사진 교체/삭제
인터랙션은 05-06-PLAN.md가 같은 파일에 이어 붙인다 — 세 plan이 하나의 파일을 순차 wave로
나눠 갖는다.

Purpose: 05-RESEARCH.md가 확정한 "라우트 파일은 얇은 래퍼, 실제 JSX는 `src/checkin/`의
컴포넌트"라는 구조를 지금 잡아두면 Phase 6(캘린더 과거 날짜 뷰)이 자기 탭의 nested
stack에서 라우트 파일 하나만 추가해 같은 화면을 재사용할 수 있다. 지금 라우트 파일 안에
JSX를 넣으면 Phase 6이 전체를 복제해야 한다.
Output: 진입 가능한 상세화면 + 고정 레이아웃 상단 3요소 + 신규 회귀 가드 테스트 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-check-in-detail-edit/05-CONTEXT.md
@.planning/phases/05-check-in-detail-edit/05-RESEARCH.md
@.planning/phases/05-check-in-detail-edit/05-PATTERNS.md
@.planning/phases/05-check-in-detail-edit/05-UI-SPEC.md
@DESIGN.md
</context>

<interfaces>
<!-- 실행자가 코드베이스를 뒤지지 않아도 되도록 미리 추출한 계약 -->

05-02-PLAN.md가 만든 새 함수(이 plan이 소비):
```ts
export async function getCheckinById(db: MigratableDb, id: string): Promise<CheckinRow | null>;
export function formatLocalMonthDay(isoTimestamp: string, timeZone?: string): string;
```

기존 함수/상수(그대로 재사용):
```ts
// src/checkin/localDate.ts
export function formatLocalTime(isoTimestamp: string, timeZone?: string): string; // '09:21'
// src/checkin/checkinFlow.ts
export const CHECKIN_COPY = { photoPlaceholderLabel: '사진 추가', /* ... */ } as const;
// src/theme/tokens.ts
colors.background '#F4F1EA' / colors.surface '#FBFAF6' / colors.surfaceSoft '#ECE8DF'
colors.line / colors.textPrimary / colors.textMuted / colors.pinSoft '#DDC0AC'
spacing.xs 8 / spacing.sm 12 / spacing.md 16 / spacing.lg 24
radius.md 8
typography.timestamp (15px/500, fontVariant readonly 튜플) / typography.helperText (13px/400)
```

`src/app/(tabs)/index/index.tsx`(오늘 뷰)에서 그대로 가져올 상수/스타일:
```ts
const MAP_REGION_DELTA = 0.01;             // 지도 region delta — 같은 값을 재선언하지 말고 export해 재사용
pinWrapper: { width: 28, height: 34, alignItems: 'center' }
pinDrop: { width: 28, height: 28, borderTopLeftRadius: 14, borderTopRightRadius: 14,
           borderBottomRightRadius: 14, borderBottomLeftRadius: 0, transform: [{ rotate: '-45deg' }] }
pinSaved: { backgroundColor: colors.pinSoft }
```

이미지 렌더는 `expo-image`의 `Image`를 쓴다(`react-native`의 `Image`가 아님) —
`src/components/CheckinActionCard.tsx:24`가 이미 확립한 관례:
```tsx
import { Image } from 'expo-image';
import { SymbolView } from 'expo-symbols';
<Image source={{ uri: photo.uri }} style={styles.photoThumbnail} />
<SymbolView name="camera" tintColor={colors.textMuted} />
```

`typography.timestamp.fontVariant`는 `as const` readonly 튜플이라 RN `TextStyle`에 그대로
넣을 수 없다 — `src/today/CheckinListRow.tsx:32-35`가 쓰는 "소비 측 얕은 복사" 관용구를
그대로 복제한다(`as` 캐스트로 우회 금지).

라우트 래퍼가 쓸 훅:
```tsx
import { useLocalSearchParams, useNavigation } from 'expo-router';
import { useSQLiteContext } from 'expo-sqlite';
const { id } = useLocalSearchParams<{ id: string }>();
const db = useSQLiteContext();
```

기존 wiring 테스트 관례(신규 테스트 파일이 따라야 함): `/** @jest-environment node */`
도크블록 + `fs.readFileSync` + `stripComments`(`src/test-utils/stripComments`) + 정규식 단언.
`src/app/__tests__/tabs-wiring.test.ts`가 레퍼런스 구조다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: [id] 라우트를 Stack에 등록하고 얇은 라우트 래퍼를 만든다</name>
  <files>src/app/(tabs)/index/_layout.tsx, src/app/(tabs)/index/[id].tsx</files>
  <read_first>
    - src/app/(tabs)/index/_layout.tsx (05-01-PLAN.md 산출물 — index 스크린만 등록돼 있다)
    - src/app/(tabs)/index/index.tsx (useSQLiteContext 사용 지점 — 같은 provider/훅을 쓴다)
    - .planning/phases/05-check-in-detail-edit/05-PATTERNS.md (§`src/app/(tabs)/index/[id].tsx` — 그대로 옮겨 쓸 형태)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pattern 1, Pitfall 2, §Recommended Project Structure의 "얇은 래퍼" 근거)
  </read_first>
  <action>
`src/app/(tabs)/index/_layout.tsx`에 `<Stack.Screen name="[id]" options={{ headerShown:
true }} />`를 추가한다. `headerShown: true`를 **명시**하는 이유(이 Stack은 루트의
`screenOptions={{ headerShown: false }}`를 상속하지 않는 별도 인스턴스이며, 반대로 이
Stack의 다른 스크린은 `headerShown: false`라 기본값에 기댈 수 없다 — 05-RESEARCH.md
Pitfall 2)를 주석으로 남긴다. `title`은 여기서 정적으로 지정하지 않는다 — 체크인마다 다른
로컬 날짜라서 화면 컴포넌트가 데이터 로드 후 `navigation.setOptions`로 채운다(Task 2).

새 파일 `src/app/(tabs)/index/[id].tsx`를 만든다. 이 파일은 **오직 3가지만** 한다:
`useLocalSearchParams<{ id: string }>()`로 `id`를 읽고, `useSQLiteContext()`로 `db`를 얻고,
`<CheckinDetailScreen db={db} checkinId={id} />`를 반환한다. 화면 JSX·스타일·state·
데이터 조회를 이 파일에 절대 넣지 않는다. 파일 헤더 주석에 그 이유를 남긴다: Phase 6은
`(tabs)/calendar/...`라는 **다른 탭의 다른 nested stack**에서 진입하므로 이 라우트 파일을
그대로 push할 수 없고, 반드시 별도 라우트 파일에서 같은 `CheckinDetailScreen`을 import해야
한다 — 화면 본체가 라우트 파일 안에 있으면 Phase 6이 전체를 복제하게 된다
(05-RESEARCH.md §Recommended Project Structure).

이 파일 하나로는 컴파일되지 않는다(`CheckinDetailScreen`이 아직 없음) — Task 2와 함께
커밋해도 되지만, 실행 순서상 Task 2의 스텁을 먼저 만들어 두는 것이 편하다면 그렇게 해도
된다. 다만 Task 2 완료 시점에는 반드시 실제 구현이어야 한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "src/app/(tabs)/index/[id].tsx"`
    - `grep -v '^//' "src/app/(tabs)/index/_layout.tsx" | grep -c 'name="\[id\]"'`가 1이다
    - `grep -v '^//' "src/app/(tabs)/index/_layout.tsx" | grep -c "headerShown: true"`가 1이다
    - `grep -v '^//' "src/app/(tabs)/index/[id].tsx" | grep -c "useLocalSearchParams"`가 1이다
    - `grep -v '^//' "src/app/(tabs)/index/[id].tsx" | grep -c "CheckinDetailScreen"`가 2다 (import 1 + 사용 1)
    - `grep -v '^//' "src/app/(tabs)/index/[id].tsx" | grep -Ec "StyleSheet|useState|useEffect|getCheckinById"`가 0이다 (래퍼에 화면 로직 없음)
    - `grep -vc '^\s*//' "src/app/(tabs)/index/[id].tsx"`가 25 이하다
    - `npx tsc --noEmit`이 종료 코드 0이다
  </acceptance_criteria>
  <done>`[id]` 스크린이 `headerShown: true`로 Stack에 등록됐고, 라우트 파일은 id/db를 읽어 `CheckinDetailScreen`에 넘기기만 하는 25줄 이하 래퍼다.</done>
</task>

<task type="auto">
  <name>Task 2: CheckinDetailScreen에 조회 + 시각/정적지도/사진 고정 레이아웃을 구현한다</name>
  <files>src/checkin/CheckinDetailScreen.tsx</files>
  <read_first>
    - src/app/(tabs)/index/index.tsx (1005~1050줄 MapView/Marker 사용부, 1180~1218줄 pinWrapper/pinDrop/pinSaved 스타일, 22~60줄 import 관례 — teardrop 기하를 재도출하지 말고 그대로 복제)
    - src/components/CheckinActionCard.tsx (1~30줄 프레젠테이셔널 계약 주석, 98~120줄 사진 슬롯 JSX, 216~230줄 photoBox/photoThumbnail 스타일)
    - src/today/CheckinListRow.tsx (27~35줄 — typography.timestamp.fontVariant 얕은 복사 관용구)
    - src/checkin/checkinRepo.ts (getCheckinById 시그니처)
    - src/checkin/localDate.ts (formatLocalTime / formatLocalMonthDay)
    - src/db/schema.ts (CheckinRow 필드명 — lat/lng/photo_path/timestamp_utc)
    - src/theme/tokens.ts (colors/spacing/radius/typography 전체)
    - .planning/phases/05-check-in-detail-edit/05-UI-SPEC.md (§레이아웃 구조, §정적 지도 미리보기, §Color)
    - .planning/phases/05-check-in-detail-edit/05-RESEARCH.md (Pattern 2)
  </read_first>
  <action>
새 파일 `src/checkin/CheckinDetailScreen.tsx`를 만든다.
Props: `{ db: MigratableDb; checkinId: string }`. Named export `CheckinDetailScreen`.

**데이터 로드:** `useEffect`에서 `getCheckinById(db, checkinId)`를 호출해 로컬 state에
담는다. 로드 전에는 `null`을 반환하고(빈 화면), 조회 결과가 `null`이면(존재하지 않는 id)
역시 `null`을 반환한다 — 이 화면은 "이미 존재하는 체크인 1건"에 대한 화면이라 빈 상태
개념이 없다(05-UI-SPEC.md §Copywriting Contract "Empty state: 해당 없음"). 언마운트 후
setState를 막는 `isMountedRef` 가드를 넣는다(`(tabs)/index/index.tsx`의
`handlePickPhoto`가 이미 쓰는 관용구).

**헤더 타이틀:** 체크인 로드 완료 후 `useEffect`에서 `useNavigation()`의
`setOptions({ title: formatLocalMonthDay(checkin.timestamp_utc) })`를 호출한다. 커스텀 타이포
토큰을 적용하지 않고 iOS 시스템 기본 네비게이션 타이틀 스타일을 그대로 쓴다
(05-UI-SPEC.md §Typography — Phase 4 탭바 라벨과 동일한 보수적 기본값 원칙).

**루트 컨테이너:** `ScrollView`, `backgroundColor: colors.background`, 좌우 패딩
`spacing.lg`. 컴포넌트 내부에 `position: 'absolute'`를 쓰지 않는다(이 저장소의 프레젠테이셔널
계약 — 배치는 부모가 결정). 파일 헤더에 그 계약 주석을 `CheckinActionCard.tsx` 스타일로
남기고, 이 화면이 왜 `src/app/` 라우트 파일이 아니라 `src/checkin/`에 있는지(Phase 6
재사용)도 함께 적는다.

**렌더 순서(위→아래, 고정 — REQ-checkin-detail-layout):**
1. **시각** — `formatLocalTime(checkin.timestamp_utc)`를 `typography.timestamp` +
   `colors.textPrimary`로 단독 한 줄. `fontVariant` readonly 튜플은 `CheckinListRow.tsx`의
   얕은 복사 관용구로 브리징한다.
2. (`spacing.lg` 간격)
3. **정적 지도 미리보기** — `react-native-maps`의 `MapView`. `style`은 `width: '100%'`,
   `height: 160`, `borderRadius: radius.md`. `region`은 체크인 좌표 중심 +
   `latitudeDelta`/`longitudeDelta` 모두 `MAP_REGION_DELTA`. **인터랙션 전면 차단:**
   `scrollEnabled={false}` `zoomEnabled={false}` `rotateEnabled={false}`
   `pitchEnabled={false}`에 더해 `pointerEvents="none"`까지 지정한다 — 개별 플래그를 다
   잠가도 남을 수 있는 제스처 인식 자체를 원천 차단해 "탭해도 아무 동작 없음"이라는
   UI-SPEC 계약을 확실히 지킨다. 내부에 `Marker` 1개(`anchor={{ x: 0.5, y: 1 }}`)를 두고
   `pinWrapper`/`pinDrop`/`pinSaved` 스타일을 오늘 뷰에서 **그대로 복제**한다(teardrop
   기하를 재도출하지 않는다). 마커 색은 반드시 `colors.pinSoft` — 이 화면이 보여주는
   체크인은 항상 이미 저장이 끝난 과거 기록이라 오늘 뷰의 "저장된 핀" 규칙과 같아야
   한다(`colors.pin`은 진행 중 확인 핀 전용).
   `MAP_REGION_DELTA` 값 `0.01`을 이 파일에 재선언하지 말고, `(tabs)/index/index.tsx`의
   기존 상수를 `src/checkin/config.ts`로 옮겨 양쪽이 같은 상수를 import하게 한다
   (이 저장소의 "값을 두 곳에 중복 선언하지 않는다" 규약).
4. (`spacing.lg` 간격)
5. **사진** — `checkin.photo_path`가 있으면 `expo-image`의 `Image`로 전체 너비, 비율 유지,
   `maxHeight: 240`, `borderRadius: radius.md`. 없으면 높이 160px 빈 슬롯:
   `backgroundColor: colors.surfaceSoft`, `borderWidth: 1` + `borderColor: colors.line`,
   `borderRadius: radius.md`, 중앙에 `SymbolView name="camera" tintColor={colors.textMuted}`
   + `CHECKIN_COPY.photoPlaceholderLabel`(`typography.helperText`, `colors.textMuted`).
   **이 plan에서는 표시 전용이다** — `Pressable`로 감싸거나 `onPress`를 붙이지 않는다.
   사진 교체/삭제 인터랙션(D-03/D-04)은 05-06-PLAN.md가 추가한다. 그 사실을 주석으로
   남겨 다음 실행자가 중복 구현하지 않게 한다.

**이 plan에서 렌더하지 않는 것(다음 plan 소관 — 스텁도 넣지 않는다):**
"지도 앱에서 열기" 버튼과 메모 TextInput은 05-04-PLAN.md가 3·5번 슬롯에 추가한다.
동작 없는 껍데기 버튼을 미리 렌더하지 않는다. 그 사실을 사진 블록 뒤에 주석으로 남긴다.

**D-05 준수:** 이 화면에 체크인 전체 삭제 버튼/액션을 만들지 않는다. 삭제는 오늘 뷰 리스트
스와이프로만 제공되며(05-05-PLAN.md), 상세화면은 편집 전용 공간으로 유지한다.

**토큰 규율:** hex 컬러 리터럴을 쓰지 않고 전부 `tokens.ts`에서 가져온다. `colors.accent`를
이 화면에서 쓰지 않는다 — DESIGN.md 2026-08-31 18:10 갱신 이후 accent의 승인 용도는 캘린더 탭
전용 2개(오늘 표시 밑줄, 스크러버 선택 위치)로 좁혀졌고 체크인 관련 색상 정체성은 전부
`colors.pin`/`colors.pinSoft`로 이전됐다. **이 phase는 accent를 어떤 파일에서도 쓰지 않는다** —
스와이프 삭제 어포던스(05-05-PLAN.md)조차 `colors.pin`을 쓴다(DESIGN.md Decisions Log
2026-09-01, 05-UI-SPEC.md §색상 결정).
한국어 문자열을 JSX에 직접 하드코딩하지 않고 `CHECKIN_COPY` 상수를 참조한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "export function CheckinDetailScreen"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "getCheckinById("`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "formatLocalTime("`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "formatLocalMonthDay("`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx`에서 `scrollEnabled={false}`, `zoomEnabled={false}`, `rotateEnabled={false}`, `pitchEnabled={false}`, `pointerEvents="none"`이 각각 1회 이상 등장한다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "colors.pinSoft"`가 1이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "colors.pin\b"`가 0이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "colors.accent"`가 0이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -Ec "#[0-9a-fA-F]{3,6}\b"`가 0이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -Ec "position: 'absolute'"`가 0이다
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "MAP_REGION_DELTA"`가 1 이상이고, `grep -c "MAP_REGION_DELTA = 0.01" src/checkin/CheckinDetailScreen.tsx`가 0이다 (재선언 없이 import)
    - `grep -c "MAP_REGION_DELTA" src/checkin/config.ts`가 1 이상이고 `grep -c "MAP_REGION_DELTA = 0.01" "src/app/(tabs)/index/index.tsx"`가 0이다 (상수가 한 곳으로 통합됨)
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -Ec "삭제|trash"`가 0이다 (D-05 — 상세화면에 삭제 진입점 없음)
    - `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -c "photoPlaceholderLabel"`가 1이다 (문구 하드코딩 대신 상수 참조)
    - `npx tsc --noEmit`이 종료 코드 0이다
    - 렌더 순서 확인: `grep -v '^//' src/checkin/CheckinDetailScreen.tsx | grep -n "formatLocalTime\|MapView\|photo_path"`의 출력에서 `formatLocalTime` 줄 번호 < `MapView` 줄 번호 < `photo_path` 조건 분기 줄 번호
  </acceptance_criteria>
  <done>`CheckinDetailScreen`이 체크인을 id로 조회해 헤더 날짜/시각/잠긴 정적 지도(pinSoft 마커)/사진(또는 빈 슬롯)을 고정 순서로 렌더하며, 삭제 진입점이 없고 `tsc --noEmit`이 통과한다.</done>
</task>

<task type="auto">
  <name>Task 3: checkin-detail-wiring 회귀 가드 테스트 파일을 신규 생성한다</name>
  <files>src/app/__tests__/checkin-detail-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/tabs-wiring.test.ts (신규 파일이 복제할 구조 — readSource 헬퍼, stripComments, describe/it 명명, `it('Test N: ...')` 관례)
    - src/today/__tests__/todayUi.test.ts (src/ 하위 컴포넌트 소스를 읽는 정적 분석 변형 — CheckinDetailScreen.tsx를 읽는 데 이 형태가 더 가깝다)
    - src/checkin/CheckinDetailScreen.tsx (Task 2 산출물)
    - src/app/(tabs)/index/[id].tsx (Task 1 산출물)
    - .planning/phases/05-check-in-detail-edit/05-VALIDATION.md (Per-Task Verification Map — REQ-checkin-detail-layout 행)
  </read_first>
  <action>
`src/app/__tests__/checkin-detail-wiring.test.ts`를 신규 생성한다. 파일 최상단에
`/** @jest-environment node */` 도크블록을 두고, `fs.readFileSync` + `stripComments`
(`../../test-utils/stripComments`)로 소스를 읽는 정적 분석만 수행한다(RN 렌더 없음).
`(tabs)/index/[id].tsx`, `(tabs)/index/_layout.tsx`, `src/checkin/CheckinDetailScreen.tsx`
세 소스를 읽는 상수를 파일 상단에 선언한다. **모든 문자열 단언은 반드시 `stripComments`를
통과한 코드에 대해서만 수행한다** — 주석에 등장하는 토큰이 단언을 스스로 만족시키거나
부정 단언을 깨뜨리는 것을 막는다.

이 plan에서 넣을 단언(05-04/05-06-PLAN.md가 같은 파일에 이어서 추가할 것이므로 `describe`
블록을 목적별로 나눠 둔다):

`describe('상세 라우트 구조 계약')`
- `(tabs)/index/[id].tsx`가 존재하고, `_layout.tsx`가 `name="[id]"`를 등록한다.
- `_layout.tsx`에서 `[id]` 등록에 `headerShown: true`가 함께 등장한다(Pitfall 2 가드).
- `[id].tsx`에 `StyleSheet`/`getCheckinById`/`useState`가 등장하지 않는다 — 얇은 래퍼 계약.
- `[id].tsx`가 `CheckinDetailScreen`을 `src/checkin/`에서 import한다 — Phase 6 재사용
  구조 가드(화면 JSX가 라우트 파일로 되돌아오는 회귀를 막는다).

`describe('상세화면 고정 레이아웃 순서 (REQ-checkin-detail-layout)')`
- `formatLocalTime` 등장 위치 인덱스 < `MapView` 등장 위치 인덱스 < `photo_path` 등장 위치
  인덱스. `String.prototype.indexOf`로 인덱스를 비교하는 방식으로 순서를 단언한다.
  05-04-PLAN.md가 딥링크 버튼과 메모를 추가하면 이 단언을 5요소로 확장한다는 주석을 남긴다.
- `formatLocalMonthDay`가 등장한다(헤더 타이틀).

`describe('정적 지도 미리보기 인터랙션 잠금')`
- `scrollEnabled={false}` / `zoomEnabled={false}` / `rotateEnabled={false}` /
  `pitchEnabled={false}` / `pointerEvents="none"` 5개가 전부 등장한다.
- `MAP_REGION_DELTA = 0.01` 재선언이 등장하지 않는다(상수 중복 선언 가드).

`describe('상세화면 색상/토큰 규율')`
- `colors.pinSoft`가 등장하고 `colors.pin`(단어 경계)은 등장하지 않는다.
- `colors.accent`가 등장하지 않는다(accent는 DESIGN.md 2026-08-31 갱신 이후 캘린더 탭 전용 —
  이 phase가 만드는 어떤 파일에도 등장하지 않는다).
- hex 컬러 리터럴이 등장하지 않는다.
- `position: 'absolute'`가 등장하지 않는다(프레젠테이셔널 계약).

`describe('D-05: 상세화면에 체크인 삭제 진입점이 없다')`
- `CheckinDetailScreen.tsx` 코드에 `deleteCheckin`, `trash`, `'삭제'` 문자열이 등장하지
  않는다. 이 단언은 05-06-PLAN.md가 사진 삭제(`'사진 삭제'` 접근성 라벨)를 추가할 때
  체크인 전체 삭제와 구분되도록 조정될 예정이라는 주석을 남긴다 — 그때도 `deleteCheckin`
  부재 단언은 반드시 유지한다(그것이 D-05의 본질이다).
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-detail-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/__tests__/checkin-detail-wiring.test.ts`
    - `head -3 src/app/__tests__/checkin-detail-wiring.test.ts | grep -c "@jest-environment node"`가 1이다
    - `grep -c "stripComments" src/app/__tests__/checkin-detail-wiring.test.ts`가 2 이상이다 (import + 사용)
    - `grep -c "describe(" src/app/__tests__/checkin-detail-wiring.test.ts`가 5다
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-detail-wiring.test.ts`가 종료 코드 0이고 실패 0건이다
    - 가드가 실제로 무는지 확인: `CheckinDetailScreen.tsx`에서 `pointerEvents="none"`을 임시로 지우면 위 jest 명령이 실패한다(확인 후 원복)
    - 가드가 실제로 무는지 확인: `CheckinDetailScreen.tsx`의 `colors.pinSoft`를 `colors.pin`으로 임시 변경하면 위 jest 명령이 실패한다(확인 후 원복)
    - `npm test` 전체 스위트가 종료 코드 0이다
  </acceptance_criteria>
  <done>신규 wiring 테스트 파일이 5개 describe 블록으로 라우트 구조/레이아웃 순서/지도 잠금/토큰 규율/D-05를 고정하고, 두 건의 의도적 파손 실험에서 실제로 실패한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL 경로 → 화면 (`useLocalSearchParams`의 `id`) | 라우트 파라미터가 DB 조회 인자로 흘러 들어가는 경계 |
| 화면 → Apple MapKit (지도 타일 요청) | 체크인 좌표가 OS 네이티브 지도 컴포넌트로 나가는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | `useLocalSearchParams`의 `id` → `getCheckinById` | mitigate | ASVS V5 — `id`는 `checkinRepo.getCheckinById`의 `?` 파라미터 바인딩으로만 SQL에 도달한다(05-02-PLAN.md에서 게이트됨). 화면은 SQL 문자열을 조립하지 않으며 `CheckinDetailScreen.tsx`에 SQL 텍스트가 없음을 wiring 테스트가 간접 확인한다(`getCheckinById` 호출만 존재) |
| T-05-06 | Information Disclosure | 존재하지 않는/타 사용자 id로의 접근 | accept | 1단계는 단일 사용자·로컬 전용 앱이라 접근 제어(ASVS V4) 대상이 아니다. 존재하지 않는 id는 `getCheckinById`가 `null`을 반환하고 화면이 `null`을 렌더해 조용히 아무것도 보여주지 않는다 — 오류 메시지로 내부 상태를 노출하지 않는다 |
| T-05-07 | Denial of Service | 잠기지 않은 `MapView`가 무한 타일 요청 | accept | `region`이 고정돼 있고 모든 제스처가 비활성(`pointerEvents="none"` 포함)이라 사용자가 팬/줌으로 타일 요청을 유발할 수 없다. 최초 1회 타일 로드는 Phase 3/4에서 이미 확립된 기존 의존성이며 이 phase가 새 네트워크 표면을 만들지 않는다 |
| T-05-SC | Tampering | npm/pip/cargo installs | N/A | 신규 패키지 설치 없음(05-RESEARCH.md §Package Legitimacy Audit). 이 plan에서 `npm install`을 실행하지 않는다 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/checkin-detail-wiring.test.ts` 그린
- `npm test` 전체 스위트 그린
- `npx tsc --noEmit` 통과
</verification>

<success_criteria>
- `(tabs)/index/[id]` 라우트가 존재하며 25줄 이하의 얇은 래퍼다
- `CheckinDetailScreen`이 시각 → 정적 지도 → 사진 순서로 렌더하고, 지도는 5가지 방법으로 잠겨 있다
- 상세화면 어디에도 체크인 삭제 진입점이 없다(D-05)
- 신규 wiring 테스트가 위 계약을 전부 회귀 가드로 고정한다
</success_criteria>

<output>
Create `.planning/phases/05-check-in-detail-edit/05-03-SUMMARY.md` when done
</output>
