---
phase: 06-calendar-tab
plan: 08
type: execute
wave: 5
depends_on: ["06-03", "06-05", "06-06", "06-07"]
files_modified:
  - .planning/phases/06-calendar-tab/06-VALIDATION.md
autonomous: false
requirements: [REQ-calendar-grid, REQ-past-date-view, REQ-date-scrubber, REQ-settings-screen]

must_haves:
  truths:
    - "전체 테스트 스위트가 green이고 타입 체크가 통과한다"
    - "Phase 4가 남긴 fence 테스트(Test 13/14)가 red로 방치되지도, 통째로 삭제되지도 않았다"
    - "제스처 체감 항목(월 스와이프 · 스크러버 드래그 · 시트 강제 접힘)이 시뮬레이터에서 실제로 확인됐다"
    - "시뮬레이터로 재현 불가능한 항목만 사용자에게 넘겨졌다"
  artifacts:
    - path: ".planning/phases/06-calendar-tab/06-VALIDATION.md"
      provides: "Per-Task Verification Map이 실제 플랜/태스크 번호로 채워지고 nyquist_compliant가 갱신된 검증 계약"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/06-calendar-tab/06-VALIDATION.md"
      to: "06-01~06-07 PLAN.md"
      via: "Task/Plan/Wave 열 채우기"
      pattern: "06-0"
---

<objective>
Phase 6 전체를 게이트한다: 자동 검증(전체 스위트 + 타입 체크 + fence 테스트 갱신 확인)을 돌리고,
자동화로 잡히지 않는 제스처 체감 항목을 iOS 시뮬레이터에서 Claude가 직접 확인한 뒤, 시뮬레이터로
원천 재현 불가능한 항목만 창업자에게 넘긴다.

Purpose: 06-VALIDATION.md가 "Task/Plan/Wave 열은 TBD — 플래너가 채운다"로 남겨둔 계약을 닫고,
CLAUDE.md의 "실기기 확인이 필요한 검증 단계" 규칙(무조건 사용자에게 넘기지 말고 시뮬레이터로
먼저 확인)을 이 phase에 적용한다.
Output: 갱신된 06-VALIDATION.md + 시뮬레이터 확인 결과 + 사용자 승인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calendar-tab/06-VALIDATION.md
@.planning/phases/06-calendar-tab/06-UI-SPEC.md
@CLAUDE.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 자동 검증 + 06-VALIDATION.md 검증 표 채우기</name>
  <files>.planning/phases/06-calendar-tab/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-calendar-tab/06-VALIDATION.md (전체 — Per-Task Verification Map의 TBD 열, Wave 0 체크리스트, Validation Sign-Off 체크박스)
    - .planning/phases/06-calendar-tab/06-01-PLAN.md ~ 06-07-PLAN.md (각 플랜의 task 이름과 `<automated>` 명령 — 표에 채울 실제 값)
    - src/app/__tests__/tabs-wiring.test.ts (Test 13/14가 실제로 갱신됐는지 확인)
  </read_first>
  <action>
    (1) `npm test`(전체 스위트)와 `npx tsc --noEmit`을 실행하고 결과를 기록한다. 실패가 있으면
    해당 플랜의 SUMMARY와 함께 원인을 보고하고 이 태스크를 실패로 처리한다 — 실패를 무시하고
    체크포인트로 넘어가지 않는다.

    (2) `06-VALIDATION.md`의 Per-Task Verification Map에서 `TBD`로 남은 Task/Plan/Wave 열을 실제
    값으로 채운다(예: `06-02 Task 2 / 06-02 / wave 1`). 각 행의 `File Exists` 열을 `✅`로,
    `Status` 열을 `✅ done`으로 갱신한다. 행이 실제 구현과 어긋나면 행을 고치되 요구사항 ID는
    바꾸지 않는다.

    (3) Wave 0 Requirements 체크리스트 6개를 실제 생성된 테스트 파일 기준으로 체크한다.
    `src/calendar/DateScrubber.test.ts`로 적혀 있던 항목은 실제 파일명
    `src/calendar/scrubberRange.test.ts`로 정정한다(순수 함수 모듈로 분리된 결과).

    (4) Validation Sign-Off 체크박스 6개를 검토해 체크하고, frontmatter의 `nyquist_compliant`를
    `true`, `wave_0_complete`를 `true`, `status`를 `approved`로 갱신한다.

    (5) fence 테스트 확인을 명시적으로 기록한다: `tabs-wiring.test.ts`의 Test 13/14가
    (a) 여전히 파일에 존재하고, (b) 본문이 "부재 단언"에서 "존재 단언"으로 바뀌었으며,
    (c) 통과한다는 것을 확인한 결과를 문서에 한 줄로 남긴다(06-RESEARCH.md Pitfall 2의 명시적 게이트).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`가 실패 0건으로 끝난다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
    - `grep -c "TBD" .planning/phases/06-calendar-tab/06-VALIDATION.md`가 0이다.
    - `grep -c "nyquist_compliant: true" .planning/phases/06-calendar-tab/06-VALIDATION.md`가 1이다.
    - `grep -c "DateScrubber.test.ts" .planning/phases/06-calendar-tab/06-VALIDATION.md`가 0이다(실제 파일명으로 정정됨).
    - `npm test -- src/app/__tests__/tabs-wiring.test.ts -t "Test 13"` 과 `-t "Test 14"` 가 각각 통과한다.
  </acceptance_criteria>
  <done>전체 자동 검증이 green이고, 검증 계약 문서가 실제 구현 기준으로 채워졌다.</done>
</task>

<task type="auto">
  <name>Task 2: iOS 시뮬레이터 직접 확인 — 제스처/애니메이션 체감 항목</name>
  <files>.planning/phases/06-calendar-tab/06-VALIDATION.md</files>
  <read_first>
    - CLAUDE.md §실기기 확인이 필요한 검증 단계 (시뮬레이터 우선 원칙과 순서)
    - .planning/phases/06-calendar-tab/06-VALIDATION.md §Manual-Only Verifications (3개 항목)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md (색/타이포/치수 계약 — 스크린샷 대조 기준)
    - docs/designs/calendar-date-scrubber.md Premise 8/10/11
  </read_first>
  <action>
    CLAUDE.md가 정한 순서를 따른다: 시뮬레이터 패널 attach → 가장 최근 DerivedData 빌드 launch
    (없으면 빌드) → 이미 실행 중인 Metro dev server(`--dev-client`)에 연결 → 항목별 확인.
    이 phase는 네이티브 모듈 구성을 바꾸지 않았으므로(신규 패키지 0개) 기존 빌드를 그대로 쓸 수 있다.

    확인할 항목과 준비 조건:
    1. **월 그리드** — 캘린더 탭 진입. 이번 달 그리드가 보이고 주가 일요일부터 시작하는지,
       오늘 셀에 accent 밑줄이 있는지, 기록 있는 날/없는 날의 톤이 구분되는지 스크린샷으로 확인.
    2. **월 이동 2경로(D-05)** — 헤더 `<`/`>` 탭과 그리드 좌우 스와이프가 같은 결과를 내는지.
    3. **과거 날짜 진입** — 기록이 있는 날짜 셀 탭 → 지도+시트가 열리고 **체크인 버튼이 없으며**
       **하단 탭바가 사라지는지**. 뒤로 나가면 탭바가 돌아오는지. 행을 탭해 상세화면에 들어가면
       탭바가 다시 보이는지(06-05의 `useFocusEffect` 방식이 의도대로 동작하는지).
    4. **기록 없는 날** — 기록이 없는 날짜를 탭해 `'이 날은 기록이 없어요'`가 시트 표면에 보이고
       지도 위에 겹치지 않는지.
    5. **스크러버(테스트 데이터 필요)** — 서로 다른 최소 3일에 체크인이 있어야 스크러버가 보인다.
       시뮬레이터에 데이터가 부족하면 `simctl location set`으로 위치를 바꿔가며 체크인을 만들거나,
       앱 DB에 직접 행을 넣어 상태를 만든 뒤 확인한다. 확인 항목: 드래그 시 지도/시트가 실시간으로
       바뀌는지, 손을 뗀 자리에서 즉시 멈추는지(관성 없음), 첫 체크인 날짜/오늘 경계에서 튕김 없이
       멈추는지, 스크러버에 손이 닿는 순간 시트가 접히고 손을 뗀 뒤에도 자동으로 다시 열리지 않는지.
    6. **스크러버 숨김** — 기록이 1일뿐인 상태에서 과거 날짜 화면에 스크러버가 아예 없는지.
    7. **설정 화면** — Today 뷰 상단 햄버거 탭 → 설정 화면이 열리고 **탭바가 유지되는지**,
       3개 행이 보이는지, 알림 빈도 탭 시 액션시트 3개 옵션이 뜨는지, 빈도를 '끔'으로 바꾼 뒤
       앱을 백그라운드→포그라운드 전환하고 다시 설정 화면에 들어왔을 때 여전히 '끔'인지
       (06-06 Pitfall 5 배선의 실동작 확인).
    8. **DESIGN.md 대조** — 스크린샷에서 빨강/초록/파랑 계열 시맨틱 색이 없는지, accent(올리브)가
       오늘 밑줄과 스크러버 선택 표시 외에 등장하지 않는지, 진행률/개수 수치가 어디에도 없는지.

    각 항목의 결과(확인됨/문제 발견/시뮬레이터 재현 불가)를 `06-VALIDATION.md`의
    §Manual-Only Verifications 표에 "Claude 시뮬레이터 확인" 열을 추가해 기록한다. 문제를 발견하면
    고치지 말고 정확한 증상과 화면/파일을 기록한 뒤 Task 3 체크포인트에서 보고한다 —
    이 플랜은 검증 플랜이며 구현을 바꾸지 않는다.
  </action>
  <verify>
    <automated>grep -c "Claude 시뮬레이터 확인" .planning/phases/06-calendar-tab/06-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-VALIDATION.md`의 §Manual-Only Verifications 표에 "Claude 시뮬레이터 확인" 열이 존재하고, 위 8개 항목 각각에 대해 확인됨/문제/재현불가 중 하나가 기록돼 있다.
    - 항목 3(탭바 숨김·복원)과 항목 5(시트 강제 접힘)에 대해 스크린샷 근거가 문서에 언급돼 있다.
    - 시뮬레이터로 재현 불가능하다고 판단한 항목마다 그 이유가 한 줄로 적혀 있다(예: 실기기 고유 프레임 특성).
    - 발견된 문제가 있으면 증상 + 해당 파일/플랜 번호가 기록돼 있고, 이 플랜에서 코드가 수정되지 않았다(`git status`에 `src/` 변경 없음).
  </acceptance_criteria>
  <done>제스처/애니메이션 체감 항목이 Claude 주도로 시뮬레이터에서 확인되고 결과가 문서화됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 창업자 확인 — 시뮬레이터로 덮이지 않는 항목</name>
  <what-built>
    캘린더 탭 월 그리드(오늘 밑줄 · 기록 유무 무채색 톤 · 일요일 시작 · 스와이프+화살표 월 이동),
    과거 날짜 읽기전용 지도+시트(체크인 버튼 없음 · 탭바 숨김), 플로팅 날짜 스크러버(실시간 드래그 ·
    모멘텀 없음 · 경계 하드 클램프 · 터치 시 시트 강제 접힘 · 기록 0~1일이면 숨김),
    설정 화면(알림 빈도 · 하루 마무리 알림 토글 · 버전) + Today 뷰 햄버거 진입점.
    전체 테스트 스위트 green, 타입 체크 통과, Task 2에서 Claude가 시뮬레이터로 8개 항목을 직접 확인함.
  </what-built>
  <how-to-verify>
    Task 2의 시뮬레이터 확인 결과 요약을 먼저 읽고, 아래 **실기기에서만 판단 가능한** 항목만 확인해주세요:

    1. 실기기에서 캘린더 탭 → 그리드를 좌우로 스와이프해 월 이동이 손가락을 따라오는 느낌인지
       (시뮬레이터 트랙패드 제스처로는 체감 판단이 어려운 항목).
    2. 기록이 여러 날 쌓인 실제 데이터로 과거 날짜 화면에서 스크러버를 드래그했을 때
       - 지도/리스트가 지연 없이 따라오는지(프레임 드랍 없음),
       - 손을 뗀 자리에서 미끄러짐 없이 딱 멈추는지,
       - 첫 체크인 날짜/오늘 경계에서 튕김 없이 멈추는지.
    3. 실제 화면/조도에서 기록 있는 날(`#79786F`)과 없는 날(`#A7A49A`)의 톤 차이가 구분되는지,
       오늘 accent 밑줄이 과하지 않게 보이는지(DESIGN.md "담담한 톤" 기준).
    4. 설정에서 알림 빈도를 '3시간마다'로 바꾼 뒤 몇 시간 지나 실제 알림이 그 간격으로 오는지
       (시뮬레이터에서 실제 반복 캘린더 트리거 발화를 관찰할 수 없는 항목 — 시간이 걸리므로
       나중에 확인하고 문제가 있으면 별도로 알려주셔도 됩니다).

    Task 2에서 문제가 보고됐다면 그 항목도 함께 봐주세요.
  </how-to-verify>
  <action>
    Task 2의 시뮬레이터 확인 결과 요약(항목별 확인됨/문제/재현불가)을 사용자에게 먼저 제시하고,
    위 `<how-to-verify>`의 실기기 전용 4개 항목만 확인 요청한 뒤 응답을 기다린다. 승인 전에
    다음 단계로 진행하지 않는다. 사용자가 문제를 보고하면 이 플랜에서 고치지 말고 증상과
    해당 파일/플랜 번호를 정리해 gap 처리(`/gsd:plan-phase --gaps`) 대상으로 넘긴다.
  </action>
  <resume-signal>"승인"이라고 입력하거나 발견한 문제를 알려주세요</resume-signal>
  <done>창업자가 실기기 전용 항목을 확인하고 승인했거나, 발견된 문제가 gap으로 기록됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 검증 결과 → phase 완료 판정 | 자동 검증이 green이라는 사실만으로 제스처 체감 계약이 충족됐다고 결론 내리면 안 되는 지점. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Repudiation | 검증 기록 | mitigate | 어느 항목을 Claude가 시뮬레이터로 직접 확인했고 어느 항목이 사용자 확인 몫인지 `06-VALIDATION.md`에 명시적으로 구분해 기록한다(CLAUDE.md 4항 요구). |
| T-06-SC | Tampering | npm 설치 | accept | 신규 패키지 없음 — 이 플랜은 코드도 의존성도 바꾸지 않는다. |
</threat_model>

<verification>
- `npm test` 전체 스위트 green, `npx tsc --noEmit` 통과.
- `06-VALIDATION.md`에 TBD가 남아 있지 않다.
- 시뮬레이터 확인 결과와 사용자 확인 몫이 문서에서 구분된다.
</verification>

<success_criteria>
- Phase 6의 4개 요구사항(REQ-calendar-grid / past-date-view / date-scrubber / settings-screen)이
  자동 검증 + 시뮬레이터 확인 + 창업자 확인 3단계를 모두 통과했다.
- Phase 4 fence 테스트가 갱신된 상태로 green이다.
- 검증 계약 문서가 실제 구현을 반영한다.
</success_criteria>

<output>
Create `.planning/phases/06-calendar-tab/06-08-SUMMARY.md` when done
</output>
