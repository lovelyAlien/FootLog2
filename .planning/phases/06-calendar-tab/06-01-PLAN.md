---
phase: 06-calendar-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/migrations.ts
  - src/db/migrations.test.ts
  - src/settings/config.ts
  - src/settings/settingsRepo.ts
  - src/settings/settingsRepo.test.ts
autonomous: true
requirements: [REQ-settings-screen]

must_haves:
  truths:
    - "앱을 껐다 켜도 사용자가 고른 알림 빈도와 하루 마무리 알림 토글 값이 그대로 남아 있다"
    - "설정 row가 아직 없는 신규 설치에서도 알림 설정 읽기가 실패하지 않고 Phase 2 기본값(hourly / 하루마무리 켜짐 / 21시)으로 동작한다"
    - "기존 사용자의 checkins/drafts 데이터가 마이그레이션 v3 이후에도 그대로 보존된다"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "AppSettingsRow 타입 + CREATE_APP_SETTINGS_TABLE_SQL DDL"
      contains: "CREATE TABLE IF NOT EXISTS app_settings"
    - path: "src/db/migrations.ts"
      provides: "DATABASE_VERSION 3 + currentDbVersion === 2 append 블록"
      contains: "DATABASE_VERSION = 3"
    - path: "src/settings/settingsRepo.ts"
      provides: "app_settings 단일 row upsert/read + NotificationFrequency 런타임 가드"
      exports: ["getSettingsRow", "upsertSettings", "resolveNotificationSettings", "isNotificationFrequency"]
    - path: "src/settings/settingsRepo.test.ts"
      provides: "node:sqlite 실엔진 왕복 테스트"
  key_links:
    - from: "src/settings/settingsRepo.ts"
      to: "src/db/schema.ts"
      via: "AppSettingsRow 타입 import"
      pattern: "AppSettingsRow"
    - from: "src/settings/settingsRepo.ts"
      to: "src/notifications/config.ts"
      via: "NotificationSettings / PHASE2_NOTIFICATION_SETTINGS 재사용"
      pattern: "PHASE2_NOTIFICATION_SETTINGS"
    - from: "src/db/migrations.ts"
      to: "src/db/schema.ts"
      via: "CREATE_APP_SETTINGS_TABLE_SQL import"
      pattern: "CREATE_APP_SETTINGS_TABLE_SQL"
---

<objective>
설정 화면(D-01/D-02)이 올라앉을 영속화 계층을 만든다. `app_settings` 단일 row 테이블을
`drafts`와 동일한 고정 PK 패턴으로 추가하고, DATABASE_VERSION을 2→3으로 올리며,
`NotificationSettings`를 읽고 쓰는 repo를 만든다.

Purpose: `src/notifications/config.ts`가 주석으로 남긴 gap("Phase 2는 이 값을 영속화하지
않는다 — 설정 UI와 영속화는 Phase 6 소관")을 닫는다. 이 계층이 없으면 설정 화면은 값을
바꿔도 앱 재시작 시 전부 되돌아간다.
Output: `app_settings` 테이블 + `src/settings/settingsRepo.ts` + 실엔진 테스트 2종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calendar-tab/06-CONTEXT.md
@.planning/phases/06-calendar-tab/06-RESEARCH.md
@.planning/phases/06-calendar-tab/06-PATTERNS.md
@.claude/skills/test-driven-development/SKILL.md
</context>

<interfaces>
<!-- 실행자는 아래 계약을 그대로 소비한다. 코드베이스 탐색 불필요. -->

src/notifications/config.ts (기존, 변경 없음):
- `export type NotificationFrequency = 'hourly' | 'every3h' | 'off'`
- `export type NotificationSettings = { checkinFrequency: NotificationFrequency; dailyReflectionEnabled: boolean; dailyReflectionHour: number }`
- `export const PHASE2_NOTIFICATION_SETTINGS: NotificationSettings` — `{ checkinFrequency: 'hourly', dailyReflectionEnabled: true, dailyReflectionHour: 21 }`

src/db/migrations.ts (기존):
- `export const DATABASE_VERSION = 2` (이 플랜이 3으로 올린다)
- `export type MigratableDb = Pick<SQLiteDatabase, 'getFirstAsync' | 'execAsync' | 'runAsync' | 'getAllAsync'>`
- `export async function migrateDbIfNeeded(db: MigratableDb): Promise<void>`

src/db/testing/nodeSqliteAdapter.ts (기존):
- `createTestDb(path?: string)` → `{ db, raw, close }`

src/checkin/config.ts (기존, 상수 명명 선례):
- `export const DRAFT_ROW_ID = 'draft'`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app_settings 스키마 + 마이그레이션 v3</name>
  <files>src/db/schema.ts, src/db/migrations.ts, src/db/migrations.test.ts</files>
  <read_first>
    - src/db/schema.ts (기존 전체 — `DraftRow` + `CREATE_DRAFTS_TABLE_SQL` 블록이 그대로 복제할 형태)
    - src/db/migrations.ts (기존 전체 — append 규칙 주석과 T-1-01 보안 주석)
    - src/db/migrations.test.ts (기존 전체 — `DRAFTS_COLUMNS` 배열 단언 패턴, Test 1의 `expect(DATABASE_VERSION).toBe(2)`, Test 15의 버전업 보존 테스트)
    - .planning/phases/06-calendar-tab/06-PATTERNS.md (§`src/db/schema.ts`, §`src/db/migrations.ts` 절)
  </read_first>
  <behavior>
    - migrations.test.ts Test 1: 빈 DB 마이그레이션 후 `user_version`이 3이며 `expect(DATABASE_VERSION).toBe(3)`이다.
    - 신규 테스트 A: 마이그레이션 후 `sqlite_master`에 `app_settings` 테이블이 존재한다.
    - 신규 테스트 B: `app_settings`가 정확히 4개 컬럼(`id`, `checkin_frequency`, `daily_reflection_enabled`, `updated_at`)을 갖는다.
    - 신규 테스트 C: `id` 없이 insert하면 실패하고, `checkin_frequency`/`daily_reflection_enabled`/`updated_at`에 NULL을 넣으면 실패한다.
    - 신규 테스트 D: `user_version = 2`인 기존 기기를 업그레이드해도 기존 `checkins`/`drafts` row가 보존되고 `app_settings`만 추가된다(Test 15의 v1→v2 테스트와 동일 형태).
    - 신규 테스트 E: 마이그레이션 직후 `app_settings`는 0행이다(기본 row를 마이그레이션 시점에 seed하지 않는다).
  </behavior>
  <action>
    `src/db/schema.ts` 끝에 `AppSettingsRow` 인터페이스와 `CREATE_APP_SETTINGS_TABLE_SQL`을
    추가한다. 컬럼은 정확히 `id TEXT PRIMARY KEY NOT NULL`, `checkin_frequency TEXT NOT NULL`,
    `daily_reflection_enabled INTEGER NOT NULL`, `updated_at TEXT NOT NULL` 4개다. `dailyReflectionHour`는
    컬럼으로 두지 않는다 — day-end-reflection-map.md Premises #4가 1단계에서 시각을 하드코딩(21시)으로
    고정했고 사용자 조정 UI가 없기 때문이며, 이 근거를 주석으로 남긴다. 인덱스는 만들지 않는다
    (`drafts`와 동일한 단일 row 근거). `DraftRow` 블록의 주석 밀도와 형식을 그대로 따른다.

    `src/db/migrations.ts`에서 `DATABASE_VERSION`을 3으로 올리고, `CREATE_APP_SETTINGS_TABLE_SQL`을
    import한 뒤 기존 `if (currentDbVersion === 1)` 블록 **아래에** `if (currentDbVersion === 2) { await
    db.execAsync(CREATE_APP_SETTINGS_TABLE_SQL); currentDbVersion = 3; }` 블록을 append한다. 기존
    `=== 0` / `=== 1` 블록은 한 글자도 수정하지 않는다(파일 자체 주석의 migration_discipline #2).
    append 안내 주석(`// if (currentDbVersion === 2) { ... }` 예시 줄)은 다음 phase용으로
    `currentDbVersion === 3` 예시로 갱신해 남긴다. `PRAGMA user_version` 보간 한 줄은 그대로 둔다 —
    migrations.test.ts Test 8이 `${` 출현 횟수를 1로 게이트한다.

    `src/db/migrations.test.ts`에서 Test 1의 `expect(DATABASE_VERSION).toBe(2)`를 3으로 고치고 테스트
    이름을 실제 생성 테이블 4종에 맞게 갱신한 뒤, 파일 상단의 `DRAFTS_COLUMNS` 배열 옆에
    `APP_SETTINGS_COLUMNS` 배열을 추가하고 위 behavior의 신규 테스트 A~E를 파일 끝에 append한다.
    기존 Test 2~15는 수정하지 않는다.
  </action>
  <verify>
    <automated>npm test -- src/db/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/db/migrations.test.ts`가 전부 통과한다.
    - `grep -n "DATABASE_VERSION = 3" src/db/migrations.ts`가 정확히 1줄 출력한다.
    - `grep -c "currentDbVersion === 2" src/db/migrations.ts`가 1 이상이고, `currentDbVersion === 0`/`=== 1` 블록 본문은 `git diff src/db/migrations.ts`에서 변경 hunk에 포함되지 않는다.
    - `grep -v '^\s*//' src/db/migrations.ts | grep -c '\${'`가 정확히 1이다(T-1-01 선례 유지).
    - `src/db/schema.ts`가 `checkin_frequency TEXT NOT NULL`과 `daily_reflection_enabled INTEGER NOT NULL`을 포함한다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>마이그레이션 v3가 app_settings 테이블을 만들고, 기존 데이터 보존과 컬럼 계약이 실엔진 테스트로 검증된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: settingsRepo — 단일 row upsert/read + 빈도 런타임 가드</name>
  <files>src/settings/config.ts, src/settings/settingsRepo.ts, src/settings/settingsRepo.test.ts</files>
  <read_first>
    - src/checkin/draftRepo.ts (전체 — `INSERT OR REPLACE` upsert + `SELECT ... WHERE id = ?` + `row ?? null` 관용구)
    - src/checkin/draftRepo.test.ts (전체 — `createTestDb`/`migrateDbIfNeeded`/try-finally `close()` 스캐폴드, "두 번 upsert해도 1행" 단언)
    - src/notifications/config.ts (전체 — `NotificationFrequency`/`NotificationSettings`/`PHASE2_NOTIFICATION_SETTINGS` 계약과 Phase 6 소관 주석)
    - src/db/schema.ts (Task 1이 방금 추가한 `AppSettingsRow`)
    - src/checkin/config.ts 63~69줄 (`DRAFT_ROW_ID` 상수 선언 위치/주석 형식)
  </read_first>
  <behavior>
    - `isNotificationFrequency('hourly' | 'every3h' | 'off')`는 true, `'daily'`/`''`/`null`/`undefined`/숫자는 false.
    - `resolveNotificationSettings(null)`은 `PHASE2_NOTIFICATION_SETTINGS`와 깊은 동등이다(신규 설치 경로).
    - `resolveNotificationSettings({ id:'settings', checkin_frequency:'every3h', daily_reflection_enabled:0, updated_at })`는 `{ checkinFrequency:'every3h', dailyReflectionEnabled:false, dailyReflectionHour:21 }`이다.
    - `resolveNotificationSettings`에 `checkin_frequency: 'garbage'`인 row가 오면 throw하지 않고 `checkinFrequency`만 `'hourly'`로 폴백한다(T-06-01 fail-safe).
    - `upsertSettings` → `getSettingsRow` 왕복이 저장한 값을 그대로 돌려준다.
    - `upsertSettings`를 서로 다른 값으로 두 번 호출해도 `SELECT COUNT(*) FROM app_settings`가 1이며 마지막 값이 남는다.
    - `upsertSettings`에 `checkinFrequency`가 union 밖 값인 객체를 (타입 캐스트로) 넘기면 rejects 한다 — 쓰기 시점 거부(T-06-01).
  </behavior>
  <action>
    `src/settings/config.ts`를 새로 만들고 `export const SETTINGS_ROW_ID = 'settings';`를 선언한다
    (`DRAFT_ROW_ID` 선례와 동일하게, 단일 row를 PK 제약으로 강제한다는 근거를 주석으로 남긴다).
    이 파일에는 런타임 import를 두지 않는다.

    `src/settings/settingsRepo.ts`를 새로 만들고 다음 4개를 export 한다:
    `isNotificationFrequency(value: unknown): value is NotificationFrequency`,
    `resolveNotificationSettings(row: AppSettingsRow | null): NotificationSettings`,
    `getSettingsRow(db: MigratableDb): Promise<AppSettingsRow | null>`,
    `upsertSettings(db: MigratableDb, settings: NotificationSettings, now: string): Promise<void>`.
    - `isNotificationFrequency`는 `['hourly','every3h','off']` 리터럴 배열 기반으로 판정한다. zod/joi를
      도입하지 않는다(06-RESEARCH.md Security Domain V5 — 3값 enum에 검증 라이브러리는 과설계).
    - `resolveNotificationSettings`는 순수 함수다(db 인자 없음). row가 null이거나 `checkin_frequency`가
      union 밖이면 해당 필드만 `PHASE2_NOTIFICATION_SETTINGS` 값으로 폴백하고, `dailyReflectionHour`는
      항상 `PHASE2_NOTIFICATION_SETTINGS.dailyReflectionHour`를 쓴다(컬럼이 없으므로).
      `daily_reflection_enabled`는 `=== 1` 비교로 boolean화한다.
    - `upsertSettings`는 먼저 `isNotificationFrequency(settings.checkinFrequency)`를 확인하고 false면
      `throw new Error(...)`로 즉시 거부한 뒤, `INSERT OR REPLACE INTO app_settings (id,
      checkin_frequency, daily_reflection_enabled, updated_at) VALUES (?, ?, ?, ?)`를 `SETTINGS_ROW_ID`와
      함께 파라미터 바인딩으로 실행한다. 문자열 보간 금지(T-06-01).
    - `now`는 호출자가 넘긴다 — repo 안에서 `new Date()`를 부르지 않는다(테스트 결정성, 이 저장소의
      `NewCheckinParams.timestampUtc` 선례와 동일).
    - 파일 헤더 주석에 "SQL 문자열은 이 파일에만 존재한다 — SettingsScreen.tsx에 절대 등장하지
      않는다"는 계약을 `checkinRepo.ts`/`draftRepo.ts` 헤더와 같은 문구로 남긴다.

    `src/settings/settingsRepo.test.ts`는 `@jest-environment node` 헤더 + `createTestDb()` +
    `migrateDbIfNeeded(db)` + try/finally `close()` 스캐폴드로 위 behavior 전부를 커버한다.
    COUNT 단언은 `draftRepo.test.ts`가 쓰는 `raw.prepare('SELECT COUNT(*) as c FROM app_settings')`
    형태를 그대로 따른다.
  </action>
  <verify>
    <automated>npm test -- src/settings/settingsRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/settings/settingsRepo.test.ts`가 전부 통과하며, behavior 목록 7개 각각에 대응하는 `it(...)`이 존재한다.
    - `grep -c "INSERT OR REPLACE INTO app_settings" src/settings/settingsRepo.ts`가 1이다.
    - `grep -c 'app_settings' src/settings/SettingsScreen.tsx 2>/dev/null` 는 파일 부재로 실패해야 한다(이 플랜은 화면을 만들지 않는다).
    - `grep -v '^\s*//' src/settings/settingsRepo.ts | grep -c '\${'`가 0이다(SQL 보간 없음).
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>설정 값을 SQLite 단일 row에 저장/복원할 수 있고, union 밖 빈도 값은 쓰기 시점에 거부되며 읽기 시점에 안전하게 폴백된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 설정 화면 → settingsRepo → SQLite | 사용자가 선택한 값이 영속 저장소로 넘어가는 지점. 이 phase에서 유일하게 새로 생기는 쓰기 경로. |
| 기존 설치 DB → 마이그레이션 러너 | 이미 데이터가 있는 기기의 스키마를 변경하는 지점. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `settingsRepo.upsertSettings` / `resolveNotificationSettings` | mitigate | `isNotificationFrequency` 닫힌 union 가드로 쓰기 시점 거부 + 읽기 시점 `'hourly'` 폴백. SQL은 전부 `?` 파라미터 바인딩. |
| T-06-06 | Denial of Service | `migrateDbIfNeeded` v2→v3 | mitigate | append-only 블록만 추가하고 기존 블록 미수정 — 실엔진 테스트 D가 기존 checkins/drafts 데이터 보존을 검증. `CREATE TABLE IF NOT EXISTS`로 재실행 멱등. |
| T-06-SC | Tampering | npm 설치 | accept | 이 phase는 신규 패키지를 추가하지 않는다(06-RESEARCH.md §Package Legitimacy Audit). 설치 태스크가 없으므로 legitimacy 게이트 대상이 없다. 계획 중 새 패키지가 필요해지면 audit 프로토콜을 먼저 통과시킬 것. |
</threat_model>

<verification>
- `npm test` 전체 스위트가 green이다(기존 마이그레이션 테스트 회귀 없음).
- `npx tsc --noEmit` 통과.
</verification>

<success_criteria>
- `app_settings` 테이블이 마이그레이션 v3로 생성되고, v2 기기 업그레이드 시 기존 데이터가 보존된다.
- `settingsRepo`가 `NotificationSettings` 단위로 읽고 쓰며, 신규 설치에서는 Phase 2 기본값으로 폴백한다.
- 잘못된 빈도 문자열은 저장되지 않고, 이미 저장돼 있어도 읽기 시 안전하게 폴백된다.
</success_criteria>

<output>
Create `.planning/phases/06-calendar-tab/06-01-SUMMARY.md` when done
</output>
