---
phase: 06-calendar-tab
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/calendar/content.ts
  - src/calendar/CalendarGridScreen.tsx
  - src/app/(tabs)/calendar/_layout.tsx
  - src/app/(tabs)/calendar/index.tsx
  - src/app/(tabs)/calendar.tsx
  - src/today/content.ts
  - src/app/__tests__/tabs-wiring.test.ts
  - src/app/__tests__/calendar-wiring.test.ts
autonomous: true
requirements: [REQ-calendar-grid]

must_haves:
  truths:
    - "캘린더 탭을 열면 플레이스홀더 문구 대신 이번 달 월 그리드가 보인다"
    - "오늘 날짜 셀에 accent 밑줄이 표시된다"
    - "기록이 있는 날은 textMuted, 없는 날은 textFaint 무채색 톤으로 구분된다"
    - "좌우 스와이프와 헤더 화살표 버튼 둘 다로 이전/다음 달로 이동할 수 있다"
    - "한 주가 일요일부터 시작한다"
    - "월 그리드 화면에서도 하단 탭바가 계속 보인다"
  artifacts:
    - path: "src/calendar/content.ts"
      provides: "CALENDAR_COPY 문구 단일 출처(요일 헤더/화살표 라벨/과거날짜 빈 상태/스크러버 캡션)"
      contains: "CALENDAR_COPY"
    - path: "src/calendar/CalendarGridScreen.tsx"
      provides: "월 그리드 화면 컴포넌트"
      min_lines: 120
    - path: "src/app/(tabs)/calendar/_layout.tsx"
      provides: "캘린더 탭 전용 nested Stack"
      contains: "headerShown: false"
    - path: "src/app/(tabs)/calendar/index.tsx"
      provides: "월 그리드 얇은 라우트 래퍼"
    - path: "src/app/__tests__/calendar-wiring.test.ts"
      provides: "캘린더 라우트/그리드 정적 소스 분석 회귀 가드"
  key_links:
    - from: "src/calendar/CalendarGridScreen.tsx"
      to: "src/checkin/checkinRepo.ts"
      via: "getCheckinDateKeysInRange 월 범위 조회"
      pattern: "getCheckinDateKeysInRange"
    - from: "src/calendar/CalendarGridScreen.tsx"
      to: "src/calendar/monthGrid.ts"
      via: "buildMonthGrid / shiftMonth / formatMonthHeader"
      pattern: "buildMonthGrid"
    - from: "src/calendar/CalendarGridScreen.tsx"
      to: "src/app/(tabs)/calendar/[date]"
      via: "router.push 날짜 셀 탭"
      pattern: "router\\.push"
---

<objective>
Phase 4가 남긴 캘린더 탭 플레이스홀더(`src/app/(tabs)/calendar.tsx` 한 줄 텍스트)를 실제 월
그리드로 교체하고, 캘린더 탭을 nested Stack 구조로 승격한다(이후 과거 날짜 화면을 push하면서
탭바를 제어할 수 있게).

Purpose: REQ-calendar-grid의 본체. D-04(무채색 기록 유무 톤), D-05(스와이프+화살표 병행),
D-06(일요일 시작)이 실제 화면으로 구현되는 지점이다.
Output: `src/calendar/CalendarGridScreen.tsx` + 캘린더 탭 nested Stack 라우트 2개 +
`calendar-wiring.test.ts` 신규 + Phase 4가 남긴 fence 테스트 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calendar-tab/06-CONTEXT.md
@.planning/phases/06-calendar-tab/06-RESEARCH.md
@.planning/phases/06-calendar-tab/06-PATTERNS.md
@.planning/phases/06-calendar-tab/06-UI-SPEC.md
@DESIGN.md
</context>

<interfaces>
<!-- 06-02가 만든 계약. 실행자는 그대로 소비한다. -->

src/calendar/monthGrid.ts:
- `export type YearMonth = { year: number; month: number }` (month 1~12)
- `export type MonthGridCell = { dateKey: string; dayOfMonth: number; inCurrentMonth: boolean }`
- `buildMonthGrid(ym: YearMonth): MonthGridCell[]` — 길이 35 또는 42, 일요일 시작
- `monthRangeBounds(ym: YearMonth): { startDateKey: string; endDateKey: string }`
- `shiftMonth(ym: YearMonth, delta: number): YearMonth`
- `formatMonthHeader(ym: YearMonth): string` — `'2026년 9월'`
- `yearMonthOf(dateKey: string): YearMonth`

src/checkin/checkinRepo.ts:
- `getCheckinDateKeysInRange(db, startDateKey, endDateKey): Promise<string[]>` — 기록 있는 고유 날짜 키 오름차순

src/checkin/localDate.ts:
- `resolveLocalDateKey(date: Date, timeZone?: string): string`

src/theme/tokens.ts:
- `colors.background '#F4F1EA'`, `colors.surface '#FBFAF6'`, `colors.textPrimary '#2F302C'`,
  `colors.textMuted '#79786F'`, `colors.textFaint '#A7A49A'`, `colors.accent '#7C8660'`, `colors.line '#DDD8CD'`
- `typography.screenTitle {22,600}`, `typography.helperText {13,400}`, `spacing`, `radius`, `motion`

src/app/(tabs)/index/_layout.tsx (구조적 쌍둥이 — 그대로 복제할 형태):
```
<Stack>
  <Stack.Screen name="index" options={{ headerShown: false }} />
  <Stack.Screen name="[id]" options={{ headerShown: true }} />
</Stack>
```
이 Stack은 루트 `_layout.tsx`의 `screenOptions={{ headerShown: false }}`를 **상속하지 않는다**.

src/app/(tabs)/index/[id].tsx (얇은 라우트 래퍼 계약):
```
const db = useSQLiteContext();
return <SomeScreen db={db} ... />;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 캘린더 탭 nested Stack 라우트 + CALENDAR_COPY 문구 단일 출처</name>
  <files>src/calendar/content.ts, src/app/(tabs)/calendar/_layout.tsx, src/app/(tabs)/calendar/index.tsx, src/app/(tabs)/calendar.tsx, src/today/content.ts</files>
  <read_first>
    - src/app/(tabs)/calendar.tsx (삭제 대상 플레이스홀더 — 무엇을 대체하는지 확인)
    - src/app/(tabs)/index/_layout.tsx (전체 — nested Stack 구조와 headerShown 상속 함정 주석)
    - src/app/(tabs)/index/[id].tsx (전체 — 얇은 라우트 래퍼 계약 주석)
    - src/today/content.ts (전체 — `X_COPY = { ... } as const` 형식과 "문구를 여기서 발명하지 않는다" 규약, `calendarPlaceholder` 키)
    - src/app/(tabs)/_layout.tsx (전체 — `<Tabs.Screen name="calendar" />` 등록이 폴더화 이후에도 그대로 유효함을 확인)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Copywriting Contract
  </read_first>
  <action>
    (1) `src/calendar/content.ts`를 새로 만들고 `CALENDAR_COPY`를 `as const`로 export 한다.
    문구는 06-UI-SPEC.md §Copywriting Contract에서 그대로 전사하며 이 phase의 캘린더 관련 문구를
    **전부** 여기에 담는다(뒤 플랜들이 이 파일을 다시 수정하지 않도록):
    - `weekdayHeaders: ['일','월','화','수','목','금','토']` (D-06 일요일 시작)
    - `prevMonthLabel: '이전 달'`, `nextMonthLabel: '다음 달'` (아이콘 전용 버튼 VoiceOver 라벨)
    - `pastDateEmptyState: '이 날은 기록이 없어요'` (오늘 뷰의 `TODAY_COPY.emptyState`와 의도적으로
      다르다 — 과거 날짜에는 "체크인하면 채워져요" CTA가 성립하지 않는다는 근거를 주석으로 남긴다)
    - `scrubberCaption: '← 드래그해서 날짜 이동 → · 오늘 이후로는 못 넘어감'`
      (`docs/designs/calendar-date-scrubber.md` Visual Design Decisions 원문 그대로, 가운뎃점은 U+00B7)
    파일 헤더 주석은 `src/today/content.ts`의 "승인된 문서에서 그대로 전사한다" 문구 형식을 따른다.

    (2) `src/app/(tabs)/calendar.tsx`를 삭제하고 `src/app/(tabs)/calendar/` 폴더를 만든다.
    `_layout.tsx`는 `CalendarStackLayout`을 default export 하며 `<Stack>` 안에
    `<Stack.Screen name="index" options={{ headerShown: false }} />` 하나만 등록한다. 과거 날짜
    화면(`[date]`) 등록은 그 라우트 파일이 실제로 생기는 06-04에서 추가한다 — 존재하지 않는
    라우트를 미리 등록하지 않는다. 헤더 주석에 `(tabs)/index/_layout.tsx`와 동일한 근거
    (루트 Stack의 `headerShown: false`를 상속하지 않으므로 스크린마다 명시해야 한다)를 남긴다.

    (3) `src/app/(tabs)/calendar/index.tsx`는 얇은 래퍼다 — `useSQLiteContext()`로 db를 얻어
    `<CalendarGridScreen db={db} />`를 렌더하는 것 외에 JSX/스타일/state/조회를 두지 않는다.

    (4) `src/today/content.ts`에서 이제 소비자가 사라진 `calendarPlaceholder` 키를 제거한다.
    `tabToday`/`tabCalendar`(탭바가 계속 사용)와 나머지 키는 그대로 둔다. 제거 근거
    (Phase 6이 플레이스홀더 화면을 실제 그리드로 교체)를 주석 한 줄로 남긴다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f "src/app/(tabs)/calendar.tsx"` 가 참이다.
    - `test -f "src/app/(tabs)/calendar/_layout.tsx" && test -f "src/app/(tabs)/calendar/index.tsx"` 가 참이다.
    - `grep -c 'name="index"' "src/app/(tabs)/calendar/_layout.tsx"`가 1이고, `grep -c '\[date\]' "src/app/(tabs)/calendar/_layout.tsx"`가 0이다.
    - `grep -c "CalendarGridScreen" "src/app/(tabs)/calendar/index.tsx"`가 1 이상이고, 같은 파일에 `StyleSheet`/`useState`가 등장하지 않는다.
    - `grep -c "calendarPlaceholder" src/today/content.ts`가 0이다.
    - `grep -c "scrubberCaption" src/calendar/content.ts`가 1이고 문구가 `docs/designs/calendar-date-scrubber.md`의 원문과 문자 단위로 일치한다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>캘린더 탭이 nested Stack 폴더 구조로 승격됐고, 이 phase의 캘린더 문구가 단일 출처에 모였다.</done>
</task>

<task type="auto">
  <name>Task 2: CalendarGridScreen — 월 그리드 렌더 + 월 이동 + 기록 유무 톤</name>
  <files>src/calendar/CalendarGridScreen.tsx</files>
  <read_first>
    - src/calendar/monthGrid.ts (06-02 산출물 — 소비할 함수 시그니처 전부)
    - src/checkin/checkinRepo.ts (`getCheckinDateKeysInRange` 시그니처와 반환 형태)
    - src/app/(tabs)/index/index.tsx 405~445줄 (`reloadTodayCheckins` + `useEffect` + `useFocusEffect` + `isMountedRef` 조회 관용구)
    - src/theme/tokens.ts (전체 — colors/typography/spacing/radius/motion, accent 예산 주석)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Component Contracts 1. Month Grid (셀 상태표 6행, 헤더 레이아웃, 44×44pt 셀)
    - DESIGN.md §Color (accent 승인 용도 2곳 — 이 화면은 그중 "오늘 밑줄" 하나만 쓴다)
  </read_first>
  <action>
    `src/calendar/CalendarGridScreen.tsx`에 `export type CalendarGridScreenProps = { db: MigratableDb }`와
    `export function CalendarGridScreen({ db })`를 만든다.

    상태/데이터:
    - `const [visibleMonth, setVisibleMonth] = useState<YearMonth>(() => yearMonthOf(resolveLocalDateKey(new Date())))`
    - `const todayKey = useMemo(() => resolveLocalDateKey(new Date()), [])`
    - `const [recordedDateKeys, setRecordedDateKeys] = useState<Set<string>>(new Set())`
    - `reloadMonthPresence`를 `useCallback([db, visibleMonth])`로 만들고 `monthRangeBounds(visibleMonth)`로
      얻은 두 키를 `getCheckinDateKeysInRange`에 넘겨 결과를 `new Set(...)`으로 저장한다. 하루씩 반복
      조회하지 않는다. `isMountedRef` 가드 + `.catch(console.error)`는 `(tabs)/index/index.tsx`의
      `reloadTodayCheckins` 관용구를 그대로 복제한다(프로미스를 조용히 삼키지 않는다).
    - `useEffect(reloadMonthPresence)` + `useFocusEffect(useCallback(reloadMonthPresence, ...))` 둘 다 배선한다
      — 다른 탭에서 체크인을 남기고 돌아왔을 때 그리드가 갱신돼야 한다.

    레이아웃(06-UI-SPEC.md §Component Contracts 1 그대로):
    - 헤더 행 높이 44: `<` 버튼(44×44pt, `SymbolView name="chevron.left"`, `accessibilityLabel={CALENDAR_COPY.prevMonthLabel}`)
      — 월/연 라벨(`formatMonthHeader(visibleMonth)`, `typography.screenTitle`, 가운데 정렬) —
      `>` 버튼(44×44pt, `chevron.right`, `CALENDAR_COPY.nextMonthLabel`). 두 버튼은
      `setVisibleMonth((m) => shiftMonth(m, -1 | +1))`을 호출한다(D-05 접근성 보조 경로).
    - 요일 헤더 행: `CALENDAR_COPY.weekdayHeaders` 7개, 13px/400, `colors.textFaint`.
    - 그리드: `buildMonthGrid(visibleMonth)` 결과를 7열로 렌더한다. 각 셀 최소 44×44pt.
    - 화면 배경 `colors.background`.

    셀 상태(정확히 이 매핑만 구현한다):
    | 조건 | 숫자 색 | 추가 처리 |
    |---|---|---|
    | `dateKey === todayKey` | `colors.textPrimary` | 숫자 아래 `colors.accent` 밑줄 뷰 |
    | `inCurrentMonth && recordedDateKeys.has(dateKey)` | `colors.textMuted` | 없음 |
    | `inCurrentMonth && !has` | `colors.textFaint` | 없음 |
    | `!inCurrentMonth` | `colors.textFaint` | `Pressable`로 감싸지 않는다(비인터랙티브 정렬용 셀) |
    D-04 근거(무채색 2단 톤 재사용, 새 색상 토큰 추가 금지)를 주석으로 남긴다. `colors.accent`는
    오늘 밑줄 **한 곳에서만** 등장해야 한다 — 셀 배경/선택 원/헤더 등 다른 용도로 절대 쓰지 않는다.

    인터랙션:
    - `inCurrentMonth` 셀 탭 → `router.push({ pathname: '/calendar/[date]', params: { date: cell.dateKey } })`.
      미래 날짜도 특별 처리 없이 동일하게 탭 가능하다(D-01 canonical_refs). `pathname`+`params` 객체
      형태를 쓴다(`(tabs)/index/index.tsx` `handleRowPress`와 동일 근거).
    - 좌우 스와이프로 월 이동(D-05 기본 제스처): `react-native-gesture-handler`의
      `Gesture.Pan()` + `GestureDetector`로 그리드 영역을 감싸고, `onEnd`에서 `event.translationX`의
      부호와 임계값(가로 40px 이상, 세로 이동보다 클 때)으로 `shiftMonth(±1)`을 `runOnJS`로 호출한다.
      `.activeOffsetX([-10, 10])`로 세로 스크롤/탭과의 경합을 줄인다. 드래그 멀티셀렉트는 구현하지
      않는다 — 롱프레스/드래그 선택 코드가 이 파일에 등장해선 안 된다(Phase 2 유예 경계).
    - 진행률/개수 수치를 어떤 형태로도 렌더하지 않는다(PROJECT.md CRITICAL). 셀에 체크인 **개수**를
      쓰지 말 것 — 기록 유무는 색 톤으로만 표현한다.

    문구는 전부 `CALENDAR_COPY`를 참조한다 — JSX에 한글 리터럴을 하드코딩하지 않는다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getCheckinDateKeysInRange" src/calendar/CalendarGridScreen.tsx`가 1 이상이고 `grep -c "getTodayCheckins" src/calendar/CalendarGridScreen.tsx`가 0이다(하루씩 N번 조회 금지).
    - `grep -c "colors.accent" src/calendar/CalendarGridScreen.tsx`가 정확히 1이다.
    - `grep -c "colors.textMuted" src/calendar/CalendarGridScreen.tsx` ≥ 1 이고 `grep -c "colors.textFaint" src/calendar/CalendarGridScreen.tsx` ≥ 1 이다.
    - `grep -Ec "buildMonthGrid|shiftMonth|formatMonthHeader" src/calendar/CalendarGridScreen.tsx` ≥ 3 이다.
    - `grep -Ec "CALENDAR_COPY\.(weekdayHeaders|prevMonthLabel|nextMonthLabel)" src/calendar/CalendarGridScreen.tsx` ≥ 3 이고, 같은 파일에 `'일'`/`'이전 달'` 같은 한글 리터럴이 직접 등장하지 않는다.
    - `grep -Ec "onLongPress|multiSelect|멀티셀렉트" src/calendar/CalendarGridScreen.tsx`가 0이다.
    - `grep -c "tabBarStyle" src/calendar/CalendarGridScreen.tsx`가 0이다(탭바는 기본값으로 보인다 — 06-RESEARCH.md Pitfall 1).
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>캘린더 탭 홈이 실제 월 그리드를 렌더하고, 오늘 밑줄·기록 유무 톤·월 이동 2경로가 전부 동작한다.</done>
</task>

<task type="auto">
  <name>Task 3: 회귀 가드 — calendar-wiring.test.ts 신설 + Phase 4 fence 테스트 갱신</name>
  <files>src/app/__tests__/calendar-wiring.test.ts, src/app/__tests__/tabs-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/tabs-wiring.test.ts (전체 — 파일 상단 `readSource`/`stripComments` 스캐폴드, Test 1/5/11/12/13/14/15의 정확한 현재 단언)
    - src/app/__tests__/checkin-detail-wiring.test.ts (전체 — 라우트 래퍼 계약을 정적 소스 분석으로 검증하는 최신 선례)
    - src/calendar/CalendarGridScreen.tsx (Task 2 산출물 — 단언 대상 식별자 확인)
    - src/app/(tabs)/calendar/_layout.tsx, src/app/(tabs)/calendar/index.tsx (Task 1 산출물)
    - .planning/phases/06-calendar-tab/06-RESEARCH.md §Common Pitfalls Pitfall 2 (fence 테스트를 편집해야 하는 근거)
  </read_first>
  <action>
    (A) `src/app/__tests__/tabs-wiring.test.ts`를 편집한다. 파일을 삭제하거나 통째로 갈아엎지 않는다 —
    Phase 4가 남긴 의도적 fence 테스트를 Phase 6 스코프에 맞게 **갱신**하는 것이다(각 변경 옆에
    "Phase 4 D-07 경계를 Phase 6이 의도적으로 반전" 근거 주석을 남긴다):
    - 상단 `const calendarSource = readSource(path.join('(tabs)', 'calendar.tsx'))` →
      `path.join('(tabs)', 'calendar', 'index.tsx')`로 바꾸고, `calendarLayoutSource`도 함께 읽는다.
    - Test 1: `(tabs)/calendar.tsx` 존재 단언을 `(tabs)/calendar/_layout.tsx` + `(tabs)/calendar/index.tsx`
      존재 단언으로 교체하고, `(tabs)/calendar.tsx`가 더 이상 존재하지 **않음**을 추가로 단언한다.
    - Test 5(탭 스크린 2개, name="calendar")는 그대로 통과해야 한다 — 수정하지 않는다.
    - Test 11: `calendar.tsx`가 `TODAY_COPY.calendarPlaceholder`를 참조한다는 단언을 제거하고,
      `(tabs)/calendar/index.tsx`가 `CalendarGridScreen`에 위임하며 `StyleSheet`/`useState`를 갖지
      않는다(얇은 래퍼 계약)는 단언으로 교체한다.
    - Test 12: 플레이스홀더 타이포/색 단언을 제거하고, `CalendarGridScreen.tsx`가 `colors.accent`를
      정확히 1회만 참조한다(오늘 밑줄 전용, accent 예산)는 단언으로 교체한다.
    - Test 13: "Phase 6 범위 식별자 미등장" 단언 5개를 전부 제거하고, 반대로
      `CalendarGridScreen.tsx`가 `Pressable`과 `buildMonthGrid`를 포함한다는 단언으로 교체한다.
    - Test 14는 이 플랜에서 **건드리지 않는다** — 햄버거 진입점은 06-07이 만들며 그 플랜이 이 테스트를
      갱신한다. (이 파일을 두 플랜이 서로 다른 wave에서 편집한다.)
    - Test 15: `calendarPlaceholder` 키 단언을 제거하고 `tabToday`/`tabCalendar` 단언은 유지한다.

    (B) `src/app/__tests__/calendar-wiring.test.ts`를 새로 만든다. `tabs-wiring.test.ts`와 동일한
    기법(`@jest-environment node`, `fs.readFileSync` + `stripComments`, 정규식 단언)을 쓴다.
    이 플랜이 커버할 단언(뒤 플랜이 같은 파일에 append 한다):
    - `(tabs)/calendar/_layout.tsx`가 `expo-router`에서 `Stack`을 import하고 `<Stack.Screen name="index"`에
      `headerShown: false`를 명시한다(06-RESEARCH.md Pitfall: 명시 안 하면 빈 네비바가 생김).
    - `(tabs)/calendar/index.tsx`가 `useSQLiteContext`로 db를 얻어 `CalendarGridScreen`에 넘기고,
      `StyleSheet`/`useState`/`MapView`를 포함하지 않는다.
    - `CalendarGridScreen.tsx`가 `getCheckinDateKeysInRange`를 참조하고 `getTodayCheckins`는 참조하지 않는다.
    - `CalendarGridScreen.tsx`의 `colors.accent` 출현이 정확히 1회다.
    - `CalendarGridScreen.tsx`가 `CALENDAR_COPY.weekdayHeaders`/`prevMonthLabel`/`nextMonthLabel`을
      참조하고, 한글 리터럴 `'이전 달'`/`'다음 달'`을 직접 포함하지 않는다.
    - `CalendarGridScreen.tsx`에 `tabBarStyle`이 등장하지 않는다(Pitfall 1 — 탭바 조작은 과거 날짜
      화면 한 곳에서만).
    - `src/calendar/content.ts`가 `CALENDAR_COPY`를 `as const`로 export하고 4개 키를 갖는다.
    모든 소스 단언은 `stripComments`를 통과한 문자열에 대해 수행한다 — 주석 안 문구가 단언을
    스스로 만족/위반시키는 자기무효화를 피한다.
  </action>
  <verify>
    <automated>npm test -- src/app/__tests__/calendar-wiring.test.ts src/app/__tests__/tabs-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/app/__tests__/calendar-wiring.test.ts src/app/__tests__/tabs-wiring.test.ts`가 전부 통과한다.
    - `npm test` 전체 스위트가 green이다(다른 wiring 테스트 회귀 없음).
    - `grep -c "calendar.tsx'" src/app/__tests__/tabs-wiring.test.ts` 결과가 0이다(플랫 파일 참조가 남아 있지 않다).
    - `grep -c "Test 14" src/app/__tests__/tabs-wiring.test.ts`가 1이고, Test 14 본문은 `git diff`의 변경 hunk에 포함되지 않는다.
    - `grep -c "stripComments" src/app/__tests__/calendar-wiring.test.ts`가 1 이상이다.
  </acceptance_criteria>
  <done>캘린더 라우트/그리드 계약이 정적 소스 분석으로 고정됐고, Phase 4가 남긴 fence 테스트가 red로 방치되지 않고 갱신됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 그리드 화면 상태(가시 월) → 범위 쿼리 | 앱 내부 생성 값이지만 SQL 인자로 들어간다. |
| 그리드 셀 탭 → 라우트 파라미터 | 그리드가 만든 `dateKey`가 다음 화면의 쿼리 인자가 된다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | `CalendarGridScreen` → `getCheckinDateKeysInRange` | mitigate | 화면은 SQL을 갖지 않는다(문자열이 `checkinRepo.ts`에만 존재). 범위 값은 `monthRangeBounds`가 만든 `YYYY-MM-DD`뿐이며 파라미터 바인딩으로 전달된다. |
| T-06-07 | Information Disclosure | 월 그리드 셀 렌더 | mitigate | 셀에 체크인 **개수**나 진행률 수치를 절대 렌더하지 않는다(PROJECT.md CRITICAL). 기록 유무는 무채색 2단 톤으로만 표현한다 — acceptance criteria가 게이트. |
| T-06-SC | Tampering | npm 설치 | accept | 신규 패키지 없음 — 설치 태스크 자체가 없다(06-RESEARCH.md §Package Legitimacy Audit). |
</threat_model>

<verification>
- `npm test` 전체 스위트 green.
- `npx tsc --noEmit` 통과.
- `src/app/(tabs)/calendar.tsx`가 삭제되고 폴더 라우트 2개가 존재한다.
</verification>

<success_criteria>
- 캘린더 탭이 플레이스홀더가 아닌 월 그리드를 보여준다.
- 오늘 accent 밑줄 / 기록 유무 무채색 톤 / 일요일 시작 / 스와이프+화살표 월 이동이 전부 구현됐다.
- Phase 4 fence 테스트가 Phase 6 스코프에 맞게 갱신됐고 스위트가 green이다.
</success_criteria>

<output>
Create `.planning/phases/06-calendar-tab/06-03-SUMMARY.md` when done
</output>
