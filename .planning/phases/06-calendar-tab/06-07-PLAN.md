---
phase: 06-calendar-tab
plan: 07
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - src/calendar/DateScrubber.tsx
  - src/calendar/PastDateScreen.tsx
  - src/today/TodayBottomSheet.tsx
  - src/app/__tests__/calendar-wiring.test.ts
autonomous: true
requirements: [REQ-date-scrubber]

must_haves:
  truths:
    - "과거 날짜 화면에서 플로팅 가로 스크러버를 드래그하면 지도와 시트 내용이 실시간으로 그 날짜로 바뀐다"
    - "스크러버에 손이 닿는 순간 바텀시트가 CLOSED로 접혀 지도가 보인다"
    - "첫 체크인 날짜와 오늘 경계에서 튕김 없이 즉시 멈춘다"
    - "손을 떼면 그 자리에서 바로 멈춘다(관성 없음)"
    - "기록이 있는 날이 0~1일이면 스크러버가 아예 보이지 않는다"
    - "스크러버 드래그 표면이 44pt 이상이다"
  artifacts:
    - path: "src/calendar/DateScrubber.tsx"
      provides: "플로팅 가로 날짜 스크러버(제스처 + 눈금 + 고정 인디케이터)"
      min_lines: 120
    - path: "src/calendar/PastDateScreen.tsx"
      provides: "스크러버 통합 — 시트 강제 접힘, 날짜 전환 재조회, 가시성 게이트"
      contains: "DateScrubber"
    - path: "src/today/TodayBottomSheet.tsx"
      provides: "선택적 sheetRef prop(부모가 imperative snapToIndex 호출 가능)"
      contains: "sheetRef"
  key_links:
    - from: "src/calendar/DateScrubber.tsx"
      to: "src/calendar/scrubberRange.ts"
      via: "indexForTranslation / clampIndex / 치수 상수"
      pattern: "indexForTranslation"
    - from: "src/calendar/PastDateScreen.tsx"
      to: "@gorhom/bottom-sheet ref"
      via: "snapToIndex(0) 강제 접힘"
      pattern: "snapToIndex"
    - from: "src/calendar/PastDateScreen.tsx"
      to: "src/checkin/checkinRepo.ts"
      via: "getCheckinHistorySummary 가시성 게이트 + 범위 시작점"
      pattern: "getCheckinHistorySummary"
---

<objective>
`docs/designs/calendar-date-scrubber.md`(CLEARED)가 확정한 플로팅 날짜 스크러버를 구현하고 과거
날짜 화면에 통합한다 — 실시간 1:1 드래그, 모멘텀 없음, 경계 하드 클램프, 터치 시 시트 강제 접힘,
기록 0~1일이면 숨김.

Purpose: REQ-date-scrubber. 원본 문서의 T1(CRITICAL, 시트 강제 접힘)·T2(하드 클램프)·T3(44×44pt)·
T4(헤더 44pt)를 실제 코드로 만드는 유일한 플랜이다.
Output: `src/calendar/DateScrubber.tsx` + `PastDateScreen` 통합 + 회귀 가드 확장.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calendar-tab/06-CONTEXT.md
@.planning/phases/06-calendar-tab/06-RESEARCH.md
@.planning/phases/06-calendar-tab/06-PATTERNS.md
@.planning/phases/06-calendar-tab/06-UI-SPEC.md
@docs/designs/calendar-date-scrubber.md
@DESIGN.md
</context>

<interfaces>
<!-- 실행자는 아래 계약을 그대로 소비한다. -->

src/calendar/scrubberRange.ts (06-02 산출물 — 모든 판정/치수의 단일 출처):
- `buildScrubberDateKeys(earliestDateKey: string, todayDateKey: string): string[]` (오름차순, 양끝 포함)
- `clampIndex(index: number, length: number): number`
- `indexForTranslation(startIndex: number, translationX: number, length: number, tickSpacingPx?: number): number`
  — 오른쪽 드래그(+translationX) = 과거로 이동. 이미 클램프된 값을 반환한다(오버슈트 없음).
- `shouldShowScrubber(distinctDateCount: number): boolean` (>= 2)
- `SCRUBBER_BOTTOM_OFFSET_PT = 132`, `SCRUBBER_HEADER_HEIGHT_PT = 44`,
  `SCRUBBER_TOUCH_SURFACE_HEIGHT_PT = 44`, `SCRUBBER_TICK_SPACING_PX = 24`, `SCRUBBER_MIN_DISTINCT_DATES = 2`

src/checkin/checkinRepo.ts (06-02 산출물):
- `getCheckinHistorySummary(db): Promise<{ earliestDateKey: string | null; distinctDateCount: number }>`
- `getCheckinDateKeysInRange(db, startDateKey, endDateKey): Promise<string[]>` (눈금 밀도 표시용)

src/calendar/content.ts (06-03 산출물):
- `CALENDAR_COPY.scrubberCaption === '← 드래그해서 날짜 이동 → · 오늘 이후로는 못 넘어감'`

src/today/TodayBottomSheet.tsx (기존 + 06-05가 `emptyText` 추가 — 이 플랜이 `sheetRef` 하나 더 추가):
- 내부적으로 `@gorhom/bottom-sheet`의 `<BottomSheet snapPoints={[closedPeak, openPeak]} ...>`를 렌더한다.
  CLOSED가 스냅 인덱스 0이다.
- `motion.bottomSheetSnapMs = 220`을 `useBottomSheetTimingConfigs`로 이미 적용하고 있다 — 새 모션 토큰 없음.

src/today/CheckinListRow.tsx (gesture-handler 선례 — 라이브러리가 이미 링크·동작함을 증명):
- `import ReanimatedSwipeable from 'react-native-gesture-handler/ReanimatedSwipeable'`
- 이 플랜은 그보다 낮은 수준 API(`Gesture` / `GestureDetector`)를 쓴다 — `ReanimatedSwipeable`은
  열기/닫기 스와이프용이라 연속 스크럽에 맞지 않는다.

src/theme/tokens.ts:
- `colors.surface '#FBFAF6'`, `colors.line '#DDD8CD'`, `colors.accent '#7C8660'`,
  `colors.textMuted '#79786F'`, `radius.lg = 16`, `typography.helperText {13,400}`, `motion.bottomSheetSnapMs = 220`

docs/designs/calendar-date-scrubber.md 눈금 색 계약:
- 기록 없는 날 = `colors.line`(얇고 짧음), 기록 있는 날 = `#C7C2B4`(조금 더 길고 진함 — 이 phase에서
  유일하게 토큰에 없는 hex, CLEARED 문서 원문 출처), 선택된 날 + 중앙 고정 인디케이터 = `colors.accent`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DateScrubber 컴포넌트 — 제스처 · 눈금 · 고정 인디케이터</name>
  <files>src/calendar/DateScrubber.tsx, src/today/TodayBottomSheet.tsx</files>
  <read_first>
    - docs/designs/calendar-date-scrubber.md (전체 — Premise 8/9/10/11/12, Visual Design Decisions, T1~T4)
    - src/calendar/scrubberRange.ts (06-02 산출물 — 소비할 함수/상수 전부)
    - src/today/CheckinListRow.tsx 1~60줄 (gesture-handler import 경로와 이 저장소의 제스처 선례, 활성화 오프셋 주석)
    - src/today/TodayBottomSheet.tsx (전체 — `BottomSheet` 렌더 지점과 props 계약, 배치는 부모 책임 규약)
    - src/theme/tokens.ts (전체)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Component Contracts 3. Date Scrubber
    - .planning/phases/06-calendar-tab/06-RESEARCH.md §Common Pitfalls Pitfall 3, Pitfall 6
  </read_first>
  <action>
    (A) `src/today/TodayBottomSheet.tsx`에 선택적 prop `sheetRef?: React.Ref<BottomSheet>` 하나를 추가하고
    `<BottomSheet ref={sheetRef} ...>`로 전달한다. 오늘 뷰는 이 prop을 넘기지 않으므로 동작이 불변임을
    주석으로 명시한다. 시트 내부에 스크러버 관련 로직/스타일을 넣지 않는다(시트는 배치·화면 로직을
    갖지 않는다는 기존 컴포넌트 계약 유지).

    (B) `src/calendar/DateScrubber.tsx`를 만든다.
    ```
    export type DateScrubberProps = {
      dateKeys: string[];              // buildScrubberDateKeys 결과(오름차순)
      recordedDateKeys: Set<string>;   // 눈금 밀도 표시용
      selectedIndex: number;
      onScrubStart: () => void;        // 부모가 시트를 CLOSED로 접는다
      onIndexChange: (index: number) => void;
    };
    ```
    - 제스처: `Gesture.Pan()` + `GestureDetector`.
      `.onBegin()`에서 `runOnJS(onScrubStart)()` — 손이 닿는 즉시(이동 전) 시트를 접는다(T1 CRITICAL).
      `.onUpdate((e) => { ... })`에서 `dragStartIndex`(shared value)와 `e.translationX`로
      `indexForTranslation(...)`을 계산해 이전 값과 다를 때만 `runOnJS(onIndexChange)(next)`를 부른다
      (프레임마다 같은 값을 다시 올려보내지 않는다). 인덱스 계산·클램프를 이 파일에서 다시 구현하지
      않고 `scrubberRange.ts` 함수를 호출한다.
      `.onBegin()`에서 현재 `selectedIndex`를 shared value에 저장한다.
    - `decay`/`withSpring`/`withDecay`/velocity 기반 후처리를 절대 넣지 않는다 — 모멘텀 없음(Premise 10).
      `.onEnd()`는 아무 것도 하지 않거나 드래그 시작값만 정리한다. 경계 러버밴딩/오버슈트 애니메이션도 없다.
    - 히트 영역: 개별 눈금에 `hitSlop`을 주지 않는다(Pitfall 6). `GestureDetector`가 감싸는 컨테이너
      자체의 높이를 `SCRUBBER_TOUCH_SURFACE_HEIGHT_PT`(44) 이상으로 준다.
    - 렌더:
      - 플로팅 카드: `backgroundColor: colors.surface`, `borderRadius: radius.lg`, 옅은 그림자.
        **배치(absolute/bottom)는 이 컴포넌트가 갖지 않는다** — 부모(PastDateScreen)가 결정한다
        (`TodayBottomSheet`/`CheckinListRow`와 동일한 재사용 컴포넌트 계약).
      - 헤더 영역 높이 `SCRUBBER_HEADER_HEIGHT_PT`(44).
      - 눈금: `selectedIndex`를 중심으로 좌우로 `SCRUBBER_TICK_SPACING_PX` 간격으로 배치한다.
        중앙은 항상 고정된 accent 인디케이터 선이고 눈금 쪽이 움직인다(Premise 9 — 인디케이터가
        움직이는 방식이 아니다). 눈금 색: 기록 없음 `colors.line`(얇고 짧음), 기록 있음 `#C7C2B4`
        (조금 더 길고 진함 — CLEARED 문서 원문값이며 이 phase의 유일한 신규 hex임을 주석에 명시),
        선택된 날 `colors.accent`(굵고 긴 눈금).
      - 캡션: `CALENDAR_COPY.scrubberCaption`, `typography.helperText`(13px/400), `colors.textMuted`.
        문구를 이 파일에 리터럴로 쓰지 않는다.
      - 날짜 숫자/개수/배지를 렌더하지 않는다(Premise 3 — 밀도로만 구분).
    - `colors.accent`는 선택 눈금과 중앙 인디케이터 두 용도 외에 등장하지 않는다(accent 예산).
    - 접근성: 순수 드래그라 VoiceOver 대체 경로는 캘린더 그리드가 담당한다(Premise 12) —
      그 근거를 주석으로 남기고, 스크러버 컨테이너에 `accessible={false}`를 두어 VoiceOver가
      의미 없는 눈금 요소를 하나씩 읽지 않게 한다.
  </action>
  <verify>
    <automated>npm test -- src/calendar/scrubberRange.test.ts src/app/__tests__/today-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "indexForTranslation|clampIndex" src/calendar/DateScrubber.tsx` ≥ 1 이고, 같은 파일에 `Math.min(`/`Math.max(`를 이용한 자체 클램프 구현이 없다.
    - `grep -Ec "withDecay|withSpring|decay\(|velocity" src/calendar/DateScrubber.tsx`가 0이다(모멘텀 금지).
    - `grep -c "hitSlop" src/calendar/DateScrubber.tsx`가 0이고 `grep -c "SCRUBBER_TOUCH_SURFACE_HEIGHT_PT" src/calendar/DateScrubber.tsx`가 1 이상이다(Pitfall 6).
    - `grep -c "GestureDetector" src/calendar/DateScrubber.tsx`가 1 이상이고 `grep -c "ReanimatedSwipeable" src/calendar/DateScrubber.tsx`가 0이다.
    - `grep -c "CALENDAR_COPY.scrubberCaption" src/calendar/DateScrubber.tsx`가 1이다.
    - `grep -Ec "position: 'absolute'|bottom:" src/calendar/DateScrubber.tsx`가 0이다(배치는 부모 책임).
    - `grep -c "sheetRef" src/today/TodayBottomSheet.tsx`가 1 이상이고, `npm test -- src/app/__tests__/today-wiring.test.ts`가 통과한다(오늘 뷰 회귀 없음).
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>스크러버 컴포넌트가 실시간 1:1 드래그로 인덱스를 올려보내고, 관성/러버밴딩/개별 hitSlop 없이 44pt 드래그 표면을 갖는다.</done>
</task>

<task type="auto">
  <name>Task 2: PastDateScreen 통합 — 시트 강제 접힘 · 날짜 전환 재조회 · 가시성 게이트</name>
  <files>src/calendar/PastDateScreen.tsx</files>
  <read_first>
    - src/calendar/PastDateScreen.tsx (06-05 산출물 전체 — 현재 조회/시트/삭제 배선)
    - src/calendar/DateScrubber.tsx (Task 1 산출물 — props 계약)
    - src/calendar/scrubberRange.ts (06-02 산출물)
    - src/checkin/checkinRepo.ts (`getCheckinHistorySummary`, `getCheckinDateKeysInRange`)
    - docs/designs/calendar-date-scrubber.md Premise 8/11/13 (위치 132pt, 0~1일이면 숨김, 시트 상태와 무관한 고정 배치)
    - src/today/TodayBottomSheet.tsx (Task 1이 추가한 `sheetRef` prop)
  </read_first>
  <action>
    (1) 활성 날짜를 화면 상태로 승격한다: `const [activeDateKey, setActiveDateKey] = useState(dateKey)`.
    `dateKey` prop이 바뀌면 `useEffect`로 동기화한다. 기존의 조회·헤더 타이틀·행 탭 push(`params.date`)
    가 전부 `dateKey` 대신 `activeDateKey`를 쓰도록 바꾼다. `router.setParams`로 라우트 파라미터를
    되쓰지 않는다 — 파라미터↔상태 양방향 동기화 루프를 만들지 않기 위함이며 그 이유를 주석으로 남긴다.

    (2) 스크러버 데이터: 마운트 시 `getCheckinHistorySummary(db)`를 1회 호출해
    `{ earliestDateKey, distinctDateCount }`를 상태로 둔다.
    - `const todayKey = useMemo(() => resolveLocalDateKey(new Date()), [])`
    - `const scrubberDateKeys = useMemo(() => (earliestDateKey ? buildScrubberDateKeys(earliestDateKey, todayKey) : []), ...)`
      — 범위는 첫 체크인 날짜 ~ 오늘이며 미래로 넘어가지 않는다(Premise 5).
    - 눈금 밀도용 `recordedDateKeys`는 `getCheckinDateKeysInRange(db, earliestDateKey, todayKey)` 1회
      결과를 `Set`으로 만든다(하루씩 반복 조회 금지).
    - `selectedIndex = scrubberDateKeys.indexOf(activeDateKey)`. `activeDateKey`가 범위 밖(미래 날짜를
      그리드에서 탭한 경우)이면 스크러버를 렌더하지 않는다 — 그 근거를 주석으로 남긴다.
    - 렌더 조건: `shouldShowScrubber(distinctDateCount) && scrubberDateKeys.length > 0 && selectedIndex >= 0`.
      조건 미충족 시 컴포넌트를 트리에서 아예 빼며(숨김 스타일이 아니라 미마운트), 그 화면은
      스크러버 없이 정상 동작한다(Premise 11).

    (3) 배치: 스크러버를 `position: 'absolute'`, `bottom: SCRUBBER_BOTTOM_OFFSET_PT`, 좌우 `spacing.md`로
    이 화면이 배치한다. 시트보다 위 레이어에 두어 시트 상태(CLOSED/OPEN)와 무관하게 항상 같은
    화면 위치에 뜬다(Premise 8). 132 같은 숫자 리터럴을 쓰지 않고 `SCRUBBER_BOTTOM_OFFSET_PT`를 참조한다.

    (4) 시트 강제 접힘(T1, CRITICAL): `const sheetRef = useRef<BottomSheet>(null)`를 만들어
    `TodayBottomSheet`에 `sheetRef={sheetRef}`로 넘기고,
    `const handleScrubStart = useCallback(() => { sheetRef.current?.snapToIndex(0); }, [])`를
    `DateScrubber`의 `onScrubStart`에 연결한다. 220ms 스냅은 시트가 이미 갖고 있으므로 새 애니메이션
    설정을 만들지 않는다. 손을 뗀 뒤 시트를 자동으로 다시 열지 않는다(원본 문서 명시) — `onEnd`에서
    `snapToIndex(1)` 류 호출을 넣지 않는다.

    (5) 날짜 전환: `onIndexChange={(i) => setActiveDateKey(scrubberDateKeys[i])}`. 드래그 중 매 변경마다
    `getTodayCheckins(db, activeDateKey)` 재조회가 걸리도록 기존 조회 `useCallback` 의존성에
    `activeDateKey`를 넣는다. 타이머 기반 디바운스를 넣지 않는다 — 인덱스가 실제로 바뀔 때만
    콜백이 오므로(Task 1 계약) 프레임당 중복 호출이 없다. 조회가 늦게 도착해 이전 날짜 결과가
    최신 상태를 덮어쓰지 않도록, 응답 적용 전에 요청 시점의 날짜 키가 현재 `activeDateKey`와
    같은지 확인하는 가드를 넣는다(기존 `isMountedRef` 가드와 함께).
  </action>
  <verify>
    <automated>npm test -- src/app/__tests__/calendar-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "snapToIndex(0)" src/calendar/PastDateScreen.tsx`가 1이고 `grep -c "snapToIndex(1)" src/calendar/PastDateScreen.tsx`가 0이다.
    - `grep -c "SCRUBBER_BOTTOM_OFFSET_PT" src/calendar/PastDateScreen.tsx`가 1 이상이고 `grep -c "bottom: 132" src/calendar/PastDateScreen.tsx`가 0이다.
    - `grep -c "shouldShowScrubber" src/calendar/PastDateScreen.tsx`가 1이다.
    - `grep -c "getCheckinHistorySummary" src/calendar/PastDateScreen.tsx`가 1이다.
    - `grep -c "activeDateKey" src/calendar/PastDateScreen.tsx` ≥ 4 이고, `getTodayCheckins` 호출 인자가 `dateKey`가 아니라 `activeDateKey`다.
    - `grep -Ec "setTimeout|debounce" src/calendar/PastDateScreen.tsx`가 0이다(스크럽 디바운스 금지 — 대기 중 삭제 타이머는 `pendingDelete.ts` 안에 있다).
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>스크러버를 드래그하면 시트가 접히고 지도·리스트가 실시간으로 그 날짜로 바뀌며, 기록이 0~1일이면 스크러버가 마운트되지 않는다.</done>
</task>

<task type="auto">
  <name>Task 3: calendar-wiring.test.ts 확장 — 스크러버 계약 회귀 가드</name>
  <files>src/app/__tests__/calendar-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/calendar-wiring.test.ts (06-03/06-05 산출물 — 기존 describe 블록)
    - src/calendar/DateScrubber.tsx, src/calendar/PastDateScreen.tsx (Task 1~2 산출물)
    - docs/designs/calendar-date-scrubber.md Implementation Tasks T1~T4 (단언이 대응해야 할 계약)
    - .planning/phases/06-calendar-tab/06-VALIDATION.md §Manual-Only Verifications (자동화로 못 잡는 항목 경계)
  </read_first>
  <action>
    기존 파일에 describe 블록을 append 한다(앞 플랜들의 단언은 수정하지 않는다). 각 `it()` 제목에
    대응하는 원본 문서 태스크 번호(T1~T4) 또는 Premise 번호를 남긴다:
    - (T1) `PastDateScreen.tsx`가 `snapToIndex(0)`을 포함하고 `snapToIndex(1)`은 포함하지 않는다.
    - (T2) `DateScrubber.tsx`가 `indexForTranslation`을 참조하고, `withDecay`/`withSpring`/`velocity`를
      포함하지 않는다(모멘텀·러버밴딩 부재).
    - (T3) `DateScrubber.tsx`가 `SCRUBBER_TOUCH_SURFACE_HEIGHT_PT`를 참조하고 `hitSlop`을 포함하지 않는다.
    - (T4/Premise 8) `PastDateScreen.tsx`가 `SCRUBBER_BOTTOM_OFFSET_PT`를 참조하고 숫자 리터럴 `132`를
      직접 포함하지 않으며, `DateScrubber.tsx`가 `SCRUBBER_HEADER_HEIGHT_PT`를 참조한다.
    - (Premise 11) `PastDateScreen.tsx`가 `shouldShowScrubber`를 참조한다.
    - (Premise 3) `DateScrubber.tsx`에 체크인 개수/진행률을 표시하는 식별자가 없다 —
      `checkins.length` 같은 카운트 렌더가 등장하지 않는다.
    - (accent 예산) `DateScrubber.tsx`의 `colors.accent` 참조가 2회 이하이고, 프로젝트 전체에서
      `colors.accent`를 포함하는 `src/` 파일이 `CalendarGridScreen.tsx`와 `DateScrubber.tsx`
      두 개뿐이다. 파일 목록은 `fs` 재귀 순회로 수집하고 각 파일은 `stripComments` 후 검사한다
      (`src/theme/tokens.ts`의 토큰 **정의**는 예외로 제외한다).
    - `DateScrubber.tsx`가 `CALENDAR_COPY.scrubberCaption`을 참조하고 캡션 한글 리터럴을 직접
      포함하지 않는다.
  </action>
  <verify>
    <automated>npm test -- src/app/__tests__/calendar-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/app/__tests__/calendar-wiring.test.ts`가 전부 통과한다.
    - `npm test` 전체 스위트가 green이다.
    - 테스트 파일이 T1/T2/T3/T4 각각에 대응하는 `it(...)`을 포함한다.
    - accent 예산 단언이 `src/theme/tokens.ts`를 제외 처리한다(토큰 정의를 위반으로 세지 않는다).
  </acceptance_criteria>
  <done>스크러버의 CLEARED 계약 4건(강제 접힘·하드 클램프·44pt·132pt)과 accent 예산이 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 제스처 입력 → 날짜 인덱스 → 조회 | 사용자 손가락 움직임이 조회 인자를 결정하는 지점. 인덱스가 범위를 벗어나면 정의되지 않은 날짜 키가 SQL로 흘러갈 수 있다. |
| 워클릿 스레드 → JS 스레드 | `runOnJS` 경계에서 상태 갱신이 일어난다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | `DateScrubber` → `activeDateKey` → `getTodayCheckins` | mitigate | 인덱스는 `indexForTranslation`이 항상 `clampIndex`를 거쳐 반환하고, 화면은 `scrubberDateKeys[i]`(내부 생성 배열)에서만 날짜 키를 얻는다 — 임의 문자열이 쿼리에 도달할 경로가 없다. `selectedIndex < 0`이면 스크러버 자체를 마운트하지 않는다. |
| T-06-14 | Denial of Service | 드래그 중 재조회 폭주 | mitigate | 인덱스가 실제로 변할 때만 `runOnJS` 콜백이 발생하며(프레임당 중복 호출 없음), 조회는 단일 인덱스 쿼리 1회다. 늦게 도착한 응답이 최신 상태를 덮어쓰지 않도록 요청 날짜 키 일치 가드를 둔다. |
| T-06-07 | Information Disclosure | 스크러버 눈금 렌더 | mitigate | 눈금은 기록 유무를 밀도로만 표현하고 개수/진행률 수치를 렌더하지 않는다(PROJECT.md CRITICAL, Premise 3) — 테스트가 게이트. |
| T-06-SC | Tampering | npm 설치 | accept | 신규 패키지 없음 — `react-native-gesture-handler`/`react-native-reanimated`/`@gorhom/bottom-sheet` 모두 이미 설치·사용 중이다. |
</threat_model>

<verification>
- `npm test` 전체 스위트 green.
- `npx tsc --noEmit` 통과.
- 드래그 감각/시트 접힘 타이밍/월 스와이프 같은 제스처 체감 항목은 자동 검증 불가 — 06-08에서
  시뮬레이터로 확인한다(06-VALIDATION.md §Manual-Only Verifications).
</verification>

<success_criteria>
- 스크러버가 실시간 1:1로 날짜를 바꾸고, 경계에서 튕김 없이 멈추며, 관성이 없다.
- 스크러버 터치 즉시 시트가 CLOSED로 접히고 자동으로 다시 열리지 않는다.
- 기록이 0~1일이면 스크러버가 트리에 존재하지 않는다.
- 이 phase 전체에서 accent 사용처가 정확히 2곳(오늘 밑줄, 스크러버 선택 표시)이다.
</success_criteria>

<output>
Create `.planning/phases/06-calendar-tab/06-07-SUMMARY.md` when done
</output>
