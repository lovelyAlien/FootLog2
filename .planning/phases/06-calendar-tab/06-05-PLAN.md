---
phase: 06-calendar-tab
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - src/app/(tabs)/calendar/_layout.tsx
  - src/app/(tabs)/calendar/[date]/index.tsx
  - src/app/(tabs)/calendar/[date]/[id].tsx
  - src/calendar/PastDateScreen.tsx
  - src/today/TodayBottomSheet.tsx
  - src/app/__tests__/calendar-wiring.test.ts
autonomous: true
requirements: [REQ-past-date-view]

must_haves:
  truths:
    - "월 그리드에서 날짜를 탭하면 그날의 지도+바텀시트 화면이 열린다"
    - "그 화면에는 체크인 버튼이 없다"
    - "그 화면에서는 하단 탭바가 숨겨지고, 뒤로 나가면 다시 보인다"
    - "그날 기록이 없으면 '이 날은 기록이 없어요'가 시트 위에 보인다(지도 위가 아니라)"
    - "행을 탭하면 Phase 5의 체크인 상세화면이 그대로 열리고, 그 화면에서는 탭바가 다시 보인다"
    - "조작된 날짜 파라미터로는 화면이 열리지 않고 조용히 되돌아간다"
  artifacts:
    - path: "src/calendar/PastDateScreen.tsx"
      provides: "과거 날짜 읽기전용 지도+시트 화면"
      min_lines: 150
    - path: "src/app/(tabs)/calendar/[date]/index.tsx"
      provides: "과거 날짜 라우트 래퍼 + date 파라미터 검증"
      contains: "isValidLocalDateKey"
    - path: "src/app/(tabs)/calendar/[date]/[id].tsx"
      provides: "캘린더 스택 전용 체크인 상세 라우트 래퍼(CheckinDetailScreen 재사용)"
      contains: "CheckinDetailScreen"
    - path: "src/today/TodayBottomSheet.tsx"
      provides: "선택적 emptyText prop 추가(기본값은 기존 TODAY_COPY.emptyState — 오늘 뷰 동작 불변)"
      contains: "emptyText"
  key_links:
    - from: "src/calendar/PastDateScreen.tsx"
      to: "src/checkin/checkinRepo.ts"
      via: "getTodayCheckins(db, dateKey) 그대로 재사용"
      pattern: "getTodayCheckins"
    - from: "src/calendar/PastDateScreen.tsx"
      to: "src/today/TodayBottomSheet.tsx"
      via: "바텀시트 컴포넌트 재사용"
      pattern: "TodayBottomSheet"
    - from: "src/calendar/PastDateScreen.tsx"
      to: "부모 Tabs 네비게이터"
      via: "navigation.getParent().setOptions({ tabBarStyle })"
      pattern: "getParent"
---

<objective>
캘린더 그리드에서 날짜를 탭했을 때 열리는 과거 날짜 화면을 만든다 — 그날의 지도 핀 + 바텀시트
리스트를 보여주되 체크인 버튼은 없고, 이 phase에서 유일하게 탭바를 숨기는 화면이다.

Purpose: REQ-past-date-view. 새 상세화면을 만들지 않고 Phase 4의 시트/행 컴포넌트와 Phase 5의
`CheckinDetailScreen`을 그대로 재사용하는 것이 이 플랜의 핵심 제약이다 — 06-RESEARCH.md가 반복해서
경고한 "두 디렉터리 건너에 이미 있는 걸 다시 유도하는" 위험 지점.
Output: `src/calendar/PastDateScreen.tsx` + 캘린더 스택 라우트 2개 + 회귀 가드 확장.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calendar-tab/06-CONTEXT.md
@.planning/phases/06-calendar-tab/06-RESEARCH.md
@.planning/phases/06-calendar-tab/06-PATTERNS.md
@.planning/phases/06-calendar-tab/06-UI-SPEC.md
@DESIGN.md
</context>

<interfaces>
<!-- 실행자는 아래 계약을 그대로 소비한다. 새 조회 함수/새 시트 컴포넌트를 만들지 않는다. -->

src/checkin/checkinRepo.ts (기존, 변경 없음):
- `getTodayCheckins(db: MigratableDb, localDateKey: string): Promise<CheckinRow[]>`
  — 이름만 "Today"일 뿐 임의의 `localDateKey`를 받는다. 파일 주석이 Phase 6 재사용을 명시한다.
  결과는 `timestamp_utc` 오름차순 정렬돼 있다(재정렬 불필요).

src/today/TodayBottomSheet.tsx (기존 — 이 플랜이 선택적 `emptyText` prop 하나만 추가한다):
```
export type TodayBottomSheetProps = {
  checkins: CheckinRow[];
  containerHeight: number;                 // 부모가 onLayout으로 잰 콘텐츠 영역 높이
  animatedPosition: SharedValue<number>;   // 부모 소유 SharedValue, 시트가 써 넣는다
  onRowPress: (id: string) => void;
  onDeleteRequest: (checkin: CheckinRow) => void;
};
```
- 시트는 배치(absolute positioning)를 갖지 않는다 — 부모가 결정한다.
- snapPoints는 `[closedPeak, openPeak]`이며 CLOSED가 인덱스 0이다.
- `checkins.length === 0`이면 시트 표면에 `TODAY_COPY.emptyState`를 렌더한다.

src/today/pendingDelete.ts (기존):
- `createPendingDeleteController({ onCommit: (item: PendingDeleteItem) => void; onChange: (id: string | null) => void })`
- `PendingDeleteItem = { id: string; photoPath: string | null }`, `UNDO_WINDOW_MS = 4000`
- 컨트롤러는 `request(item)` / `undo()` / `dispose()`를 갖는다. `dispose()`는 대기 중 삭제를 **확정**한다.

src/today/UndoSnackbar.tsx (기존):
- `UndoSnackbarProps = { visible: boolean; onUndo: () => void }`

src/today/trajectory.ts (기존):
- `buildTrajectoryCoordinates(checkins: CheckinRow[]): TrajectoryCoordinate[]`

src/calendar/monthGrid.ts (06-02):
- `isValidLocalDateKey(value: unknown): value is string` — `YYYY-MM-DD` + 실재 날짜 round-trip
- `formatDateKeyTitle(dateKey: string): string` — `'2026년 9월 1일'`

src/calendar/content.ts (06-03):
- `CALENDAR_COPY.pastDateEmptyState === '이 날은 기록이 없어요'`

src/checkin/config.ts:
- `MAP_REGION_DELTA = 0.01`

src/checkin/CheckinDetailScreen.tsx (Phase 5, 변경 없음):
- `<CheckinDetailScreen db={db} checkinId={id} />`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 캘린더 스택 라우트 2개 + date 파라미터 검증</name>
  <files>src/app/(tabs)/calendar/_layout.tsx, src/app/(tabs)/calendar/[date]/index.tsx, src/app/(tabs)/calendar/[date]/[id].tsx</files>
  <read_first>
    - src/app/(tabs)/calendar/_layout.tsx (06-03 산출물 — 현재 `index` 하나만 등록돼 있다)
    - src/app/(tabs)/index/[id].tsx (전체 — 얇은 래퍼 계약 주석, 특히 "Phase 6은 다른 탭의 다른 nested stack에서 진입하므로 반드시 별도 라우트 파일에서 같은 CheckinDetailScreen을 import해야 한다"는 문장)
    - src/app/(tabs)/index/_layout.tsx (전체 — 스크린마다 `headerShown`을 명시해야 하는 근거)
    - src/calendar/monthGrid.ts (`isValidLocalDateKey` 시그니처)
    - .planning/phases/06-calendar-tab/06-RESEARCH.md §Architecture Patterns Pattern 3(탭바 숨김) + §Security Domain(라우트 파라미터 검증)
  </read_first>
  <action>
    폴더 구조를 `src/app/(tabs)/calendar/[date]/index.tsx`(과거 날짜 화면) +
    `src/app/(tabs)/calendar/[date]/[id].tsx`(그 화면에서 진입하는 체크인 상세)로 만든다.
    `calendar/[date].tsx`(플랫 파일) + `calendar/[id].tsx` 조합을 쓰지 않는 이유를 `_layout.tsx`
    주석에 남긴다: 같은 레벨의 동적 세그먼트 두 개는 `/calendar/<무언가>`를 날짜인지 체크인 id인지
    구분할 수 없어 라우트가 모호해진다. 중첩하면 상세 경로가 `/calendar/2026-09-01/<id>`가 되어
    "어느 날짜에서 들어온 상세인지"까지 URL이 표현한다.

    `_layout.tsx`의 `<Stack>`에 스크린 2개를 **추가**한다(기존 `index` 등록은 유지):
    `<Stack.Screen name="[date]/index" options={{ headerShown: true }} />`,
    `<Stack.Screen name="[date]/[id]" options={{ headerShown: true }} />`.
    두 화면 모두 title은 여기서 정적으로 주지 않는다 — 각 화면이 데이터 로드 후
    `navigation.setOptions`로 채운다(`(tabs)/index/_layout.tsx`의 `[id]` 주석과 동일 근거).
    `[date]/index`의 `headerShown: true`는 `calendar-date-scrubber.md` Premise 8이 명시한 44pt 헤더다.

    `[date]/index.tsx` — 얇은 래퍼 + fail-closed 검증만 한다:
    - `const { date } = useLocalSearchParams<{ date: string }>()`
    - `if (!isValidLocalDateKey(date)) { return <Redirect href="/calendar" />; }` —
      조작된/깨진 파라미터가 쿼리나 화면 상태에 도달하기 전에 캘린더 홈으로 되돌린다(T-06-02).
      throw하거나 빈 화면을 남기지 않는다. `Redirect`는 `expo-router`에서 import한다
      (`(tabs)/index/index.tsx`가 priming 리다이렉트에 쓰는 것과 같은 컴포넌트).
    - 통과하면 `const db = useSQLiteContext();` 후 `<PastDateScreen db={db} dateKey={date} />`만 렌더한다.
      JSX/스타일/state/조회를 이 파일에 두지 않는다.

    `[date]/[id].tsx` — `(tabs)/index/[id].tsx`와 같은 형태로 `id`와 `db`를 읽어
    `<CheckinDetailScreen db={db} checkinId={id} />`를 렌더한다. 화면 본체를 복제하지 않고 동일
    컴포넌트를 import한다. 헤더 주석에 "Phase 5 상세화면을 캘린더 스택에서 재사용하기 위한 두 번째
    진입점이며, 화면 로직은 한 곳에만 존재한다"는 계약을 남긴다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "src/app/(tabs)/calendar/[date]/index.tsx" && test -f "src/app/(tabs)/calendar/[date]/[id].tsx"` 가 참이다.
    - `grep -c 'name="\[date\]/index"' "src/app/(tabs)/calendar/_layout.tsx"`가 1이고 `grep -c 'name="\[date\]/\[id\]"' "src/app/(tabs)/calendar/_layout.tsx"`가 1이다.
    - `grep -c "isValidLocalDateKey" "src/app/(tabs)/calendar/[date]/index.tsx"`가 1 이상이고 같은 파일에 `Redirect`가 등장한다.
    - `grep -c "CheckinDetailScreen" "src/app/(tabs)/calendar/[date]/[id].tsx"`가 1 이상이고, 그 파일에 `MapView`/`useState`/`StyleSheet`가 등장하지 않는다.
    - `grep -Ec "StyleSheet|useState|MapView" "src/app/(tabs)/calendar/[date]/index.tsx"`가 0이다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>과거 날짜와 그 날짜의 체크인 상세가 캘린더 스택 안에서 모호함 없이 라우팅되고, 잘못된 날짜 파라미터는 화면에 도달하지 못한다.</done>
</task>

<task type="auto">
  <name>Task 2: PastDateScreen — 읽기전용 지도+시트, 체크인 버튼 없음, 탭바 숨김</name>
  <files>src/calendar/PastDateScreen.tsx</files>
  <read_first>
    - src/app/(tabs)/index/index.tsx 1130~1270줄 (지도+핀+궤적선+시트 합성부, 플로팅 버튼 배치, `handleContainerLayout`/`containerHeight`/`sheetPosition` 관용구)
    - src/app/(tabs)/index/index.tsx 405~560줄 (`reloadTodayCheckins`, `filteredTodayCheckins`, `commitPendingDelete`, `pendingDeleteController`, `handleDeleteRequest`, `handleUndoDelete`, `handleRowPress` — 이 플랜이 그대로 옮겨올 배선)
    - src/today/TodayBottomSheet.tsx (전체 — props 계약과 배치는 부모 책임이라는 규약)
    - src/today/pendingDelete.ts (전체 — `dispose()`가 취소가 아니라 확정이라는 계약)
    - src/checkin/deps.ts (`defaultPhotoStorageDeps.deleteFile`)
    - src/calendar/content.ts (`CALENDAR_COPY.pastDateEmptyState`)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Component Contracts 2. Past-Date View
    - .planning/phases/06-calendar-tab/06-RESEARCH.md §Common Pitfalls Pitfall 1(탭바 기본값)
  </read_first>
  <action>
    `export type PastDateScreenProps = { db: MigratableDb; dateKey: string }`,
    `export function PastDateScreen({ db, dateKey })`.

    데이터:
    - `getTodayCheckins(db, dateKey)`를 그대로 쓴다 — `getCheckinsByDate` 같은 새 함수를 만들지
      않는다(06-RESEARCH.md 명시 anti-pattern). `dateKey`가 바뀌면 다시 조회한다(`useCallback([db, dateKey])`).
    - `useEffect` + `useFocusEffect` 둘 다로 재조회한다(상세화면에서 삭제/편집하고 돌아왔을 때 반영).
    - 지연 삭제 대기 항목을 리스트·지도 핀·궤적선 세 곳에서 함께 숨기는 `hiddenIds` +
      `filteredCheckins` 관용구를 `(tabs)/index/index.tsx`에서 그대로 옮겨온다.

    레이아웃(오늘 뷰의 합성을 그대로 따르되 두 가지가 다르다):
    - 루트 `<View onLayout>`으로 `containerHeight`를 재고, `MapView`를 `StyleSheet.absoluteFill`로 깐다.
      `showsUserLocation`은 켜지 않는다 — 과거 날짜를 보는 화면에서 현재 위치 점은 의미가 없다.
      초기 region은 그날 첫 체크인 좌표 + `MAP_REGION_DELTA`로 잡고, 체크인이 0건이면 region을
      지정하지 않는다.
    - 저장된 체크인 핀(`colors.pinSoft` 계열, 오늘 뷰의 `pinSaved` 스타일과 동일한 모양)과
      `buildTrajectoryCoordinates`로 만든 `Polyline`(`colors.pinSoft`, 폭 2)을 렌더한다.
    - `TodayBottomSheet`를 그대로 재사용한다: `checkins={filteredCheckins}`, `containerHeight`,
      `animatedPosition={sheetPosition}`(이 화면이 소유하는 `useSharedValue(0)`), `onRowPress`,
      `onDeleteRequest`. 새 시트 컴포넌트를 만들지 않는다.
    - **차이 1 — 체크인 버튼을 렌더하지 않는다.** `CheckinActionCard`/`CHECKIN_COPY.checkinCta`/
      체크인 캡처 상태머신(`checkinFlow`)을 이 파일에서 import하지 않는다. 재센터 버튼도 두지 않는다.
    - **차이 2 — 탭바를 숨긴다.** `useFocusEffect`(`useLayoutEffect`가 아니라)로
      `navigation.getParent()?.setOptions({ tabBarStyle: { display: 'none' } })`를 걸고, cleanup에서
      `{ display: 'flex' }`로 복원한다. `useFocusEffect`를 쓰는 이유를 주석으로 남긴다: 이 화면 위로
      체크인 상세화면이 push되면 blur가 발생해 탭바가 복원되므로, "과거 날짜 뷰는 탭바 숨김 /
      체크인 상세화면은 탭바 유지"라는 `footlog-product-design.md` 네비게이션 계약 두 줄을 한 번에
      만족한다. 이 저장소에서 탭바를 조작하는 파일은 이 파일 하나뿐이어야 한다(06-RESEARCH.md Pitfall 1).
    - 빈 상태: 시트가 `checkins.length === 0`일 때 `TODAY_COPY.emptyState`를 렌더하므로, 이 화면은
      `TodayBottomSheet`에 선택적 `emptyText` prop을 추가하는 대신 **`TodayBottomSheet`에
      `emptyText?: string` prop을 하나 추가**하고(기본값은 기존 `TODAY_COPY.emptyState`라 오늘 뷰
      동작은 불변) 여기서 `CALENDAR_COPY.pastDateEmptyState`를 넘긴다. 지도 위에 빈 상태 문구를
      절대 겹쳐 그리지 않는다(대비 안전 규칙).
    - 헤더 타이틀: `useEffect`로 `navigation.setOptions({ title: formatDateKeyTitle(dateKey) })`.
      뒤로가기는 기본 헤더 버튼을 쓴다(미저장 경고가 없는 화면이라 커스텀 `headerLeft`가 불필요).

    삭제/undo: `(tabs)/index/index.tsx`의 `commitPendingDelete` + `commitPendingDeleteRef` +
    `createPendingDeleteController` + 언마운트 시 `dispose()` + `UndoSnackbar` 배선을 그대로 옮겨온다
    (06-UI-SPEC.md가 `CheckinListRow`를 "unchanged"로 재사용한다고 확정했으므로 스와이프 삭제
    어포던스가 이 화면에도 존재하며, 삭제가 실제로 커밋되는 경로가 반드시 있어야 한다).
    `dispose()`가 취소가 아니라 확정이라는 계약을 주석으로 남긴다.

    행 탭: `router.push({ pathname: '/calendar/[date]/[id]', params: { date: dateKey, id } })`.

    진행률/개수 수치를 어떤 형태로도 렌더하지 않는다. 문구는 전부 상수 모듈 참조.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getTodayCheckins" src/calendar/PastDateScreen.tsx`가 1 이상이고 `grep -c "getCheckinsByDate" src/calendar/PastDateScreen.tsx`가 0이다.
    - `grep -c "TodayBottomSheet" src/calendar/PastDateScreen.tsx`가 1 이상이다(새 시트 컴포넌트 없음).
    - `grep -Ec "CheckinActionCard|checkinCta|checkinFlow|handleCheckinPress" src/calendar/PastDateScreen.tsx`가 0이다(체크인 버튼 부재).
    - `grep -c "getParent" src/calendar/PastDateScreen.tsx`가 1 이상이고, `grep -rl "tabBarStyle" src/ --include='*.tsx' | grep -v PastDateScreen | grep -v "(tabs)/_layout.tsx"` 가 아무 것도 출력하지 않는다.
    - `grep -c "useFocusEffect" src/calendar/PastDateScreen.tsx`가 1 이상이다.
    - `grep -c "CALENDAR_COPY.pastDateEmptyState" src/calendar/PastDateScreen.tsx`가 1이다.
    - `grep -Ec "createPendingDeleteController|UndoSnackbar" src/calendar/PastDateScreen.tsx` ≥ 2 이다.
    - `npm test -- src/app/__tests__/tabs-wiring.test.ts src/app/__tests__/today-wiring.test.ts` 가 통과한다(`TodayBottomSheet`에 prop을 추가해도 오늘 뷰 회귀가 없다).
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>과거 날짜 화면이 그날의 지도+시트를 보여주고, 체크인 버튼이 없으며, 이 화면에서만 탭바가 숨겨진다.</done>
</task>

<task type="auto">
  <name>Task 3: calendar-wiring.test.ts 확장 — 과거 날짜 화면 계약 회귀 가드</name>
  <files>src/app/__tests__/calendar-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/calendar-wiring.test.ts (06-03 산출물 — 기존 describe 블록과 소스 로딩 스캐폴드)
    - src/calendar/PastDateScreen.tsx, src/app/(tabs)/calendar/[date]/index.tsx, src/app/(tabs)/calendar/[date]/[id].tsx (Task 1~2 산출물)
    - .planning/phases/06-calendar-tab/06-VALIDATION.md §Per-Task Verification Map (T-06-02 행)
  </read_first>
  <action>
    기존 `calendar-wiring.test.ts`에 describe 블록을 **append** 한다(기존 그리드 단언은 수정하지 않는다).
    새 소스 상수 3개(`pastDateRouteSource`, `pastDateDetailRouteSource`, `pastDateScreenSource`)를
    `stripComments`로 정제해 읽고 다음을 단언한다:
    - `[date]/index.tsx`가 `isValidLocalDateKey`로 검증하고 실패 시 `Redirect`한다(T-06-02 fail-closed).
    - `[date]/index.tsx`와 `[date]/[id].tsx` 둘 다 `StyleSheet`/`MapView`/`useState`를 갖지 않는다(얇은 래퍼 계약).
    - `[date]/[id].tsx`가 `CheckinDetailScreen`을 import한다(화면 본체 복제 금지).
    - `PastDateScreen.tsx`가 `getTodayCheckins`를 참조하고 `getCheckinsByDate`는 참조하지 않는다.
    - `PastDateScreen.tsx`가 `TodayBottomSheet`를 참조한다.
    - `PastDateScreen.tsx`에 `CheckinActionCard`/`checkinCta`/`checkinFlow`가 등장하지 않는다
      (REQ-past-date-view의 "체크인 버튼 없음" 가드).
    - `PastDateScreen.tsx`가 `getParent`와 `tabBarStyle`을 참조한다(탭바 숨김 배선 존재).
    - `src/` 전체에서 `tabBarStyle`을 포함하는 파일이 `PastDateScreen.tsx`와 `(tabs)/_layout.tsx`
      두 개뿐이다(Pitfall 1 — 다른 화면이 탭바를 만지지 않는다). 파일 목록은 `fs.readdirSync`
      재귀 순회로 수집하고, 각 파일은 `stripComments` 후 검사한다.
    - `PastDateScreen.tsx`가 `CALENDAR_COPY.pastDateEmptyState`를 참조하고 한글 리터럴
      `'이 날은 기록이 없어요'`를 직접 포함하지 않는다.
    - `PastDateScreen.tsx`가 `createPendingDeleteController`와 `dispose`를 참조한다(삭제 확정 경로 존재).
  </action>
  <verify>
    <automated>npm test -- src/app/__tests__/calendar-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/app/__tests__/calendar-wiring.test.ts`가 전부 통과한다.
    - `npm test` 전체 스위트가 green이다.
    - 테스트 파일이 "체크인 버튼 부재" 단언과 "tabBarStyle 파일 2개 제한" 단언을 각각 포함한다.
    - 06-03이 만든 그리드 관련 `it(...)` 블록은 `git diff`의 변경 hunk에 포함되지 않는다.
  </acceptance_criteria>
  <done>과거 날짜 화면의 세 가지 계약(체크인 버튼 없음 · 탭바 숨김 단일 지점 · 파라미터 fail-closed)이 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 라우트 파라미터(`date`, `id`) → 화면/쿼리 | 딥링크나 잘못된 push로 임의 문자열이 들어올 수 있는 지점. |
| 과거 날짜 화면 → 지연 삭제 커밋 | 사용자 데이터를 영구 삭제하는 경로가 이 화면에도 생긴다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | `calendar/[date]/index.tsx` | mitigate | `isValidLocalDateKey` 통과 전에는 `PastDateScreen`을 마운트하지 않고 `/calendar`로 `Redirect`(fail closed). SQL 인자는 검증된 `YYYY-MM-DD`뿐. |
| T-06-09 | Tampering | `calendar/[date]/[id].tsx` | mitigate | `id`는 `getCheckinById`의 파라미터 바인딩으로만 흘러가며(기존 Phase 5 경로 재사용), 라우트 파일은 문자열 조립을 하지 않는다. `router.push`는 `pathname`+`params` 객체 형태를 써 인코딩을 expo-router에 맡긴다(T-05-15 선례). |
| T-06-10 | Denial of Service | 지연 삭제 컨트롤러 | mitigate | 언마운트 시 `dispose()`로 대기 중 삭제를 **확정**한다 — 화면 이탈로 삭제가 조용히 취소돼 되살아나는 상태를 만들지 않는다(pendingDelete.ts 계약, T-05-13 선례). |
| T-06-SC | Tampering | npm 설치 | accept | 신규 패키지 없음 — 설치 태스크가 없다. |
</threat_model>

<verification>
- `npm test` 전체 스위트 green.
- `npx tsc --noEmit` 통과.
- `src/` 안에서 `tabBarStyle`을 언급하는 파일이 `PastDateScreen.tsx`와 `(tabs)/_layout.tsx` 둘뿐이다.
</verification>

<success_criteria>
- 그리드에서 날짜를 탭하면 그날의 지도+시트가 열리고 체크인 버튼이 없다.
- 그 화면에서만 탭바가 숨겨지고, 상세화면으로 들어가면 다시 보인다.
- 잘못된 날짜 파라미터는 화면에 도달하지 못한다.
</success_criteria>

<output>
Create `.planning/phases/06-calendar-tab/06-05-SUMMARY.md` when done
</output>
