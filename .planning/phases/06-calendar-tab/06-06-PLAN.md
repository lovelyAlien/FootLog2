---
phase: 06-calendar-tab
plan: 06
type: execute
wave: 3
depends_on: ["06-04"]
files_modified:
  - src/app/(tabs)/index/settings.tsx
  - src/app/(tabs)/index/_layout.tsx
  - src/app/(tabs)/index/index.tsx
  - src/app/_layout.tsx
  - src/app/__tests__/settings-wiring.test.ts
  - src/app/__tests__/tabs-wiring.test.ts
autonomous: true
requirements: [REQ-settings-screen]

must_haves:
  truths:
    - "Today 뷰 상단의 햄버거 아이콘(≡)을 탭하면 설정 화면이 열린다"
    - "설정 화면에서는 하단 탭바가 계속 보인다"
    - "캘린더 탭 홈에는 햄버거 아이콘이 없다 (D-03)"
    - "사용자가 알림 빈도를 '끔'으로 바꾼 뒤 앱을 백그라운드→포그라운드 전환해도 매시간 알림이 되살아나지 않는다"
  artifacts:
    - path: "src/app/(tabs)/index/settings.tsx"
      provides: "설정 라우트 얇은 래퍼"
      contains: "SettingsScreen"
    - path: "src/app/(tabs)/index/_layout.tsx"
      provides: "Today 스택에 settings 스크린 등록"
      contains: "name=\"settings\""
    - path: "src/app/_layout.tsx"
      provides: "포그라운드 자가진단이 영속 설정을 읽어 넘기는 배선"
      contains: "runForegroundNotificationCheck("
  key_links:
    - from: "src/app/(tabs)/index/index.tsx"
      to: "src/app/(tabs)/index/settings.tsx"
      via: "햄버거 Pressable → router.push('/settings')"
      pattern: "router\\.push"
    - from: "src/app/_layout.tsx"
      to: "src/settings/settingsRepo.ts"
      via: "getSettingsRow + resolveNotificationSettings"
      pattern: "resolveNotificationSettings"
---

<objective>
설정 화면을 앱에 실제로 연결한다: Today 뷰 상단 햄버거 아이콘(D-03) → 설정 라우트 push,
그리고 06-RESEARCH.md Pitfall 5가 경고한 조용한 회귀 — 루트 레이아웃의 포그라운드 자가진단이
여전히 하드코딩 기본값을 쓰는 문제 — 를 함께 닫는다.

Purpose: 설정 화면이 값을 저장해도 `runForegroundNotificationCheck()`가 인자 없이 호출되면
포그라운드 복귀마다 자가진단이 "매시간"을 기대값으로 보고 사용자가 끈 트리거를 고아로 판정해
되살린다. 진입점과 이 배선을 같은 플랜에 두는 이유다.
Output: 설정 라우트 + 햄버거 진입점 + 자가진단 설정 배선 + 회귀 가드 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calendar-tab/06-CONTEXT.md
@.planning/phases/06-calendar-tab/06-RESEARCH.md
@.planning/phases/06-calendar-tab/06-PATTERNS.md
@.planning/phases/06-calendar-tab/06-UI-SPEC.md
@DESIGN.md
</context>

<interfaces>
<!-- 실행자는 아래 계약을 그대로 소비한다. -->

src/settings/SettingsScreen.tsx (06-04 산출물):
- `export function SettingsScreen({ db }: { db: MigratableDb })`

src/settings/content.ts (06-04 산출물):
- `SETTINGS_COPY.screenTitle === '설정'`, `SETTINGS_COPY.settingsEntryLabel === '설정'`(햄버거 VoiceOver 라벨)

src/settings/settingsRepo.ts (06-01 산출물):
- `getSettingsRow(db): Promise<AppSettingsRow | null>`
- `resolveNotificationSettings(row: AppSettingsRow | null): NotificationSettings` (순수)

src/notifications/registry.ts (기존 — 시그니처가 이미 인자를 받는다):
```
export async function runForegroundNotificationCheck(
  settings: NotificationSettings = PHASE2_NOTIFICATION_SETTINGS,
  deps: NotificationDeps = defaultNotificationDeps
): Promise<SelfHealReport | null>
```
기본 인자가 있어 인자 없이 호출해도 컴파일/실행이 되므로 아무 것도 깨지지 않은 채 잘못 동작한다
(06-RESEARCH.md Pitfall 5). 현재 `src/app/_layout.tsx`는 인자 없이 호출한다.

src/notifications/permissions.ts (기존):
- `subscribeToForegroundActive(callback: () => void): () => void` — 이미 AppState 래퍼이며,
  루트 레이아웃에서 `AppState.addEventListener`를 다시 부르면 리스너가 두 개가 된다(파일 주석).

src/app/(tabs)/index/index.tsx (기존, 이 플랜이 아이콘 하나만 추가):
- 상단 컨테이너: `<View style={[styles.bannerStack, { paddingTop: insets.top }]}>` (1183줄 부근)
- 소형 아이콘 버튼 선례(재센터 버튼, 1239~1253줄): `Pressable` + `accessibilityRole="button"` +
  `accessibilityLabel` + `SymbolView` + `tintColor`
- `router`는 `expo-router`에서 이미 import돼 있다.

src/app/(tabs)/index/_layout.tsx (기존):
```
<Stack>
  <Stack.Screen name="index" options={{ headerShown: false }} />
  <Stack.Screen name="[id]" options={{ headerShown: true }} />
</Stack>
```

src/app/__tests__/tabs-wiring.test.ts (Test 14, 이 플랜이 갱신 대상):
```
it('Test 14: (tabs)/index.tsx와 (tabs)/_layout.tsx 어디에도 설정 진입점 관련 식별자와 햄버거 문자가 등장하지 않는다', ...)
  → headerRight / /\bsettings\b/i / '≡' 부재를 단언 (Phase 4 D-08 경계 fence)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 설정 라우트 등록 + Today 뷰 햄버거 진입점</name>
  <files>src/app/(tabs)/index/settings.tsx, src/app/(tabs)/index/_layout.tsx, src/app/(tabs)/index/index.tsx</files>
  <read_first>
    - src/app/(tabs)/index/[id].tsx (전체 — 얇은 라우트 래퍼 계약)
    - src/app/(tabs)/index/_layout.tsx (전체 — `headerShown`을 스크린마다 명시해야 하는 근거 주석)
    - src/app/(tabs)/index/index.tsx 1180~1260줄 (`bannerStack` 상단 컨테이너, 재센터 버튼 `Pressable`+`SymbolView` 형태)
    - src/app/(tabs)/index/index.tsx 20~95줄 (import 블록 — `router`/`SymbolView`가 이미 들어와 있음을 확인)
    - src/checkin/CheckinDetailScreen.tsx 55~57줄 (`SMALL_ICON_HIT_SLOP` 값과 근거 주석)
    - src/settings/content.ts (06-04 산출물 — `SETTINGS_COPY.screenTitle`, `settingsEntryLabel`)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Component Contracts 4. Settings Screen (진입점 문단)
    - src/theme/tokens.ts (accent 예산 주석)
  </read_first>
  <action>
    (1) `src/app/(tabs)/index/settings.tsx`를 만든다 — `useSQLiteContext()`로 db를 얻어
    `<SettingsScreen db={db} />`를 렌더하는 얇은 래퍼. `useLocalSearchParams`는 필요 없다(파라미터 없음).
    헤더 주석에 `[id].tsx`와 동일한 "라우트 파일은 db 배선과 화면 렌더만 한다" 계약을 남긴다.

    (2) `src/app/(tabs)/index/_layout.tsx`의 `<Stack>`에
    `<Stack.Screen name="settings" options={{ headerShown: true, title: SETTINGS_COPY.screenTitle }} />`를
    추가한다. 기존 `index`/`[id]` 등록은 수정하지 않는다. 이 화면을 Today 탭의 nested Stack 안에 두는
    이유(같은 스택이므로 탭바가 기본으로 유지된다 — 설정 화면은 체크인 상세화면과 동급, D-07)를
    주석으로 남긴다. `tabBarStyle`을 여기서 절대 조작하지 않는다.

    (3) `src/app/(tabs)/index/index.tsx`의 `bannerStack` 상단 컨테이너 안(배너들과 같은
    `insets.top` 패딩 영역)에 햄버거 버튼을 추가한다:
    - `<Pressable onPress={handleSettingsPress} accessibilityRole="button"
      accessibilityLabel={SETTINGS_COPY.settingsEntryLabel} hitSlop={SMALL_ICON_HIT_SLOP}
      style={styles.settingsButton}><SymbolView name="line.3.horizontal" tintColor={colors.textPrimary} /></Pressable>`
    - `const handleSettingsPress = useCallback(() => { router.push('/settings'); }, []);`
    - `tintColor`에 `colors.accent`를 절대 쓰지 않는다(accent 예산은 이 phase에서 캘린더 2곳 전용).
    - 유니코드 문자 `≡`를 코드에 직접 쓰지 않는다 — SF Symbol 이름(`line.3.horizontal`)으로 렌더한다
      (이 저장소의 아이콘 규약이자, 갱신될 Test 14가 심볼 이름을 단언하기 때문).
    - `styles.settingsButton`은 44×44pt 터치 타겟을 만족하도록 `alignItems`/`justifyContent` 중앙 정렬 +
      최소 크기를 준다. 배너가 떠 있을 때도 아이콘이 가려지지 않도록 `bannerStack` 안에서
      아이콘 행을 배너들보다 위쪽에 배치한다.
    - 캘린더 탭에는 이 아이콘을 절대 추가하지 않는다(D-03) — 이 플랜은 `src/calendar/` 아래 파일을
      건드리지 않는다.
    - 파일에 이미 있는 체크인 캡처/지도 로직은 수정하지 않는다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "src/app/(tabs)/index/settings.tsx"` 가 참이고 그 파일에 `SettingsScreen`이 등장하며 `StyleSheet`/`useState`는 등장하지 않는다.
    - `grep -c 'name="settings"' "src/app/(tabs)/index/_layout.tsx"`가 1이다.
    - `grep -c "line.3.horizontal" "src/app/(tabs)/index/index.tsx"`가 1이다.
    - `grep -c "≡" "src/app/(tabs)/index/index.tsx"`가 0이다.
    - `grep -c "router.push('/settings')" "src/app/(tabs)/index/index.tsx"`가 1이다.
    - `grep -c "SETTINGS_COPY.settingsEntryLabel" "src/app/(tabs)/index/index.tsx"`가 1이다.
    - `grep -rc "line.3.horizontal" src/calendar/ 2>/dev/null | grep -v ':0' | wc -l` 이 0이다(캘린더 쪽에 햄버거 없음, D-03).
    - `grep -c "tabBarStyle" "src/app/(tabs)/index/_layout.tsx" "src/app/(tabs)/index/settings.tsx"` 결과가 모두 0이다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>Today 뷰 상단 햄버거를 탭하면 탭바를 유지한 채 설정 화면이 push된다.</done>
</task>

<task type="auto">
  <name>Task 2: 포그라운드 자가진단이 영속 설정을 읽도록 배선(Pitfall 5)</name>
  <files>src/app/_layout.tsx</files>
  <read_first>
    - src/app/_layout.tsx (전체 — 배선 규칙 주석: 자가진단 호출은 이 파일의 단일 AppState 리스너에서만, `AppState.addEventListener` 중복 금지, 훅을 조기 반환보다 위에 둘 것)
    - src/notifications/registry.ts 128~155줄 (`runForegroundNotificationCheck` 시그니처와 기본 인자)
    - src/settings/settingsRepo.ts (06-01 산출물 — `getSettingsRow` / `resolveNotificationSettings`)
    - .planning/phases/06-calendar-tab/06-RESEARCH.md §Common Pitfalls Pitfall 5 (구체적 회귀 시나리오)
  </read_first>
  <action>
    현재 `runCheck`는 `runForegroundNotificationCheck()`를 인자 없이 부른다 — 기본 인자
    `PHASE2_NOTIFICATION_SETTINGS`가 적용돼, 사용자가 "끔"/"3시간마다"를 골라도 포그라운드 복귀마다
    자가진단이 "매시간"을 기대값으로 보고 사용자의 트리거를 고아로 판정해 취소·재생성한다.

    `runCheck`를 영속 설정을 먼저 읽는 async 클로저로 바꾼다:
    `const row = await getSettingsRow(db); await runForegroundNotificationCheck(resolveNotificationSettings(row));`
    - `db` 접근: 이 `useEffect`는 `SQLiteProvider` **바깥**의 `RootLayout` 본문에 있으므로
      `useSQLiteContext()`를 여기서 쓸 수 없다. `SQLiteProvider`의 자식으로 렌더되는 작은 내부
      컴포넌트(예: `NotificationSelfHealGate`)를 이 파일 안에 만들어 그 안에서 `useSQLiteContext()`와
      `useEffect`(마운트 1회 호출 + `subscribeToForegroundActive` 구독)를 수행하고,
      `<SQLiteProvider ...>` 자식 트리 최상단에 렌더한다. 이 컴포넌트는 UI를 렌더하지 않는다(`null` 반환).
      기존 배선 규칙 주석(자가진단 호출 지점은 이 파일 하나, AppState 리스너 중복 금지)을 그대로
      유지하고, 이동한 이유를 주석에 추가한다.
    - 설정 읽기 실패 시에도 자가진단이 아예 안 도는 상태를 만들지 않는다 — `getSettingsRow` 실패는
      `console.error`로 남기고 `resolveNotificationSettings(null)`(= Phase 2 기본값)로 진행한다.
    - 기존의 "프로미스를 조용히 삼키지 않는다" 계약(`.catch((error) => console.error('[notifications] foreground check failed', error))`)과
      "마운트 시 1회 호출 + 포그라운드 구독 반환" 구조를 그대로 보존한다.
    - `runForegroundNotificationCheck`의 두 번째 인자(deps)는 넘기지 않는다 — 기본
      `defaultNotificationDeps`가 프로덕션 경로다.
  </action>
  <verify>
    <automated>npm test -- src/app/__tests__/notification-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "runForegroundNotificationCheck()" src/app/_layout.tsx`가 0이다(무인자 호출이 남아 있지 않다).
    - `grep -c "resolveNotificationSettings" src/app/_layout.tsx`가 1 이상이고 `grep -c "getSettingsRow" src/app/_layout.tsx`가 1 이상이다.
    - `grep -c "AppState.addEventListener" src/app/_layout.tsx`가 0이다(리스너 중복 금지 규약 유지).
    - `grep -c "subscribeToForegroundActive" src/app/_layout.tsx`가 1이다.
    - `npm test -- src/app/__tests__/notification-wiring.test.ts`가 통과한다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>콜드스타트와 포그라운드 복귀 시 자가진단이 사용자가 저장한 알림 설정을 기대값으로 쓴다.</done>
</task>

<task type="auto">
  <name>Task 3: 회귀 가드 — settings-wiring 확장 + tabs-wiring Test 14 갱신</name>
  <files>src/app/__tests__/settings-wiring.test.ts, src/app/__tests__/tabs-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/settings-wiring.test.ts (06-04 산출물 — 기존 describe 블록과 소스 로딩 스캐폴드)
    - src/app/__tests__/tabs-wiring.test.ts (전체 — Test 14의 현재 단언과 그 docstring, 06-03이 갱신한 나머지 테스트들)
    - src/app/(tabs)/index/index.tsx, src/app/(tabs)/index/settings.tsx, src/app/(tabs)/index/_layout.tsx, src/app/_layout.tsx (Task 1~2 산출물)
    - .planning/phases/06-calendar-tab/06-RESEARCH.md §Common Pitfalls Pitfall 2 / Pitfall 5
  </read_first>
  <action>
    (A) `tabs-wiring.test.ts`의 **Test 14만** 갱신한다(다른 테스트는 건드리지 않는다). Phase 4 D-08이
    "설정 진입점은 이 phase에서 만들지 않는다"는 경계를 fence로 심어둔 테스트이며, D-01/D-03이 그
    경계를 이번 phase에 의도적으로 반전했다는 근거를 `it()` 제목과 주석에 남긴다. 새 단언:
    - `(tabs)/index/index.tsx`가 `line.3.horizontal`과 `router.push('/settings')`를 포함한다.
    - `(tabs)/index/index.tsx`가 유니코드 `≡`를 포함하지 않는다(SF Symbol 이름으로만 렌더).
    - `(tabs)/_layout.tsx`에는 여전히 `headerRight`/설정 관련 식별자가 없다 — 설정은 세 번째 탭으로
      승격되지 않았다(원본 제품 원칙 유지).

    (B) `settings-wiring.test.ts`에 describe 블록을 append 한다(06-04가 만든 단언은 수정하지 않는다):
    - `(tabs)/index/settings.tsx`가 `useSQLiteContext`와 `SettingsScreen`을 참조하고
      `StyleSheet`/`useState`를 갖지 않는다(얇은 래퍼 계약).
    - `(tabs)/index/_layout.tsx`가 `name="settings"` 스크린을 등록하고 `headerShown: true`를 명시한다.
    - `(tabs)/index/_layout.tsx`와 `(tabs)/index/settings.tsx` 어디에도 `tabBarStyle`이 없다
      (설정 화면은 탭바 유지 — 06-RESEARCH.md Pitfall 1).
    - `src/app/_layout.tsx`에 `runForegroundNotificationCheck()`(무인자 호출)가 등장하지 않고,
      `resolveNotificationSettings`를 참조한다(Pitfall 5 회귀 가드).
    - `src/app/_layout.tsx`에 `AppState.addEventListener`가 등장하지 않는다(리스너 중복 금지).
    - `src/calendar/` 아래 어떤 파일도 `line.3.horizontal`을 포함하지 않는다(D-03 — 햄버거는 Today 뷰 전용).
    모든 단언은 `stripComments` 후 소스에 대해 수행한다.
  </action>
  <verify>
    <automated>npm test -- src/app/__tests__/settings-wiring.test.ts src/app/__tests__/tabs-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/app/__tests__/settings-wiring.test.ts src/app/__tests__/tabs-wiring.test.ts`가 전부 통과한다.
    - `npm test` 전체 스위트가 green이다.
    - `tabs-wiring.test.ts`의 Test 14가 여전히 존재하며(삭제 아님), 본문이 "부재 단언"에서 "존재 단언"으로 바뀌었다.
    - `settings-wiring.test.ts`가 `runForegroundNotificationCheck()` 무인자 호출 부재 단언을 포함한다.
  </acceptance_criteria>
  <done>Phase 4 fence 테스트가 red로 방치되지 않고 갱신됐고, Pitfall 5 회귀가 테스트로 차단됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 영속 설정(SQLite) → 포그라운드 자가진단 → OS 알림 스케줄러 | 저장된 값이 네이티브 예약 트리거의 기대 집합을 결정하는 지점. |
| Today 뷰 → 설정 라우트 push | 앱 내부 네비게이션(외부 입력 없음). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | `src/app/_layout.tsx` 자가진단 호출부 | mitigate | `runForegroundNotificationCheck`에 영속 설정을 명시적으로 넘긴다. 무인자 호출 부재를 `settings-wiring.test.ts`가 회귀 가드한다 — 사용자가 끈 알림이 포그라운드 복귀로 되살아나는 경로를 차단. |
| T-06-11 | Denial of Service | 설정 읽기 실패 시 자가진단 | mitigate | `getSettingsRow` 실패를 `console.error` 후 Phase 2 기본값으로 진행 — 읽기 실패가 자가진단 자체를 건너뛰게 만들지 않는다. |
| T-06-12 | Elevation of Privilege | 설정 화면 배치 | accept | 설정은 탭으로 승격되지 않고 Today 뷰 push 화면으로만 접근된다(원본 제품 원칙). 단일 사용자 로컬 앱이라 접근 제어 요구가 없다 — `(tabs)/_layout.tsx`에 설정 식별자가 없음을 테스트가 고정. |
| T-06-SC | Tampering | npm 설치 | accept | 신규 패키지 없음 — 설치 태스크가 없다. |
</threat_model>

<verification>
- `npm test` 전체 스위트 green.
- `npx tsc --noEmit` 통과.
- `src/app/_layout.tsx`에 무인자 `runForegroundNotificationCheck()` 호출이 남아 있지 않다.
</verification>

<success_criteria>
- Today 뷰 햄버거 → 설정 화면 진입 경로가 동작하고 탭바가 유지된다.
- 캘린더 탭과 탭바에는 설정 진입점이 없다(D-03/원본 원칙).
- 저장된 알림 설정이 포그라운드 자가진단의 기대값으로 실제 사용된다.
</success_criteria>

<output>
Create `.planning/phases/06-calendar-tab/06-06-SUMMARY.md` when done
</output>
