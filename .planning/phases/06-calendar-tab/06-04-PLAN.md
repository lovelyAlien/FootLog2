---
phase: 06-calendar-tab
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/settings/content.ts
  - src/settings/SettingsScreen.tsx
  - src/app/__tests__/settings-wiring.test.ts
autonomous: true
requirements: [REQ-settings-screen]

must_haves:
  truths:
    - "설정 화면에 알림 빈도 · 하루 마무리 알림 토글 · 버전 정확히 3개 항목이 보인다"
    - "알림 빈도를 바꾸면 값이 저장되고 실제 알림 트리거가 새 빈도로 재구성된다"
    - "하루 마무리 알림 토글은 아무것도 저장된 적 없을 때 켜짐으로 보인다"
    - "버전 행은 app.json의 값을 그대로 보여주며 탭할 수 없다"
    - "설정 화면 어디에도 '전체 데이터 삭제' 항목이 없다"
  artifacts:
    - path: "src/settings/content.ts"
      provides: "SETTINGS_COPY + 빈도 액션시트 옵션/취소인덱스/인덱스→값 매핑"
      exports: ["SETTINGS_COPY", "FREQUENCY_ACTION_SHEET_OPTIONS", "FREQUENCY_ACTION_SHEET_CANCEL_INDEX", "FREQUENCY_BY_ACTION_SHEET_INDEX", "FREQUENCY_LABEL_BY_VALUE"]
    - path: "src/settings/SettingsScreen.tsx"
      provides: "iOS 그룹 리스트 스타일 설정 화면 컴포넌트"
      min_lines: 150
    - path: "src/app/__tests__/settings-wiring.test.ts"
      provides: "설정 화면 계약 정적 소스 분석 회귀 가드"
  key_links:
    - from: "src/settings/SettingsScreen.tsx"
      to: "src/settings/settingsRepo.ts"
      via: "getSettingsRow / resolveNotificationSettings / upsertSettings"
      pattern: "upsertSettings"
    - from: "src/settings/SettingsScreen.tsx"
      to: "src/notifications/scheduling.ts"
      via: "applyNotificationSettings 호출(재구현 아님)"
      pattern: "applyNotificationSettings"
    - from: "src/settings/SettingsScreen.tsx"
      to: "expo-constants"
      via: "Constants.expoConfig?.version 버전 표시"
      pattern: "expoConfig"
---

<objective>
D-01/D-02가 확정한 설정 화면 본체를 만든다 — 알림 빈도(매시간/3시간마다/끔), 하루 마무리 알림
토글(기본 켜짐), 버전 정보 3개 항목. 라우트 배선과 햄버거 진입점은 06-06이 담당하므로 이
플랜은 컴포넌트와 문구, 그리고 그 계약을 고정하는 테스트만 만든다.

Purpose: 설정 화면은 Phase 7의 회고 알림 토글(REQ-reflection-notification)이 살 자리이기도 하다 —
Phase 7 착수 전에 이 화면이 존재해야 한다는 의존관계가 06-CONTEXT.md canonical_refs에 명시돼 있다.
Output: `src/settings/SettingsScreen.tsx` + `src/settings/content.ts` + `settings-wiring.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calendar-tab/06-CONTEXT.md
@.planning/phases/06-calendar-tab/06-RESEARCH.md
@.planning/phases/06-calendar-tab/06-PATTERNS.md
@.planning/phases/06-calendar-tab/06-UI-SPEC.md
@DESIGN.md
</context>

<interfaces>
<!-- 06-01이 만든 계약 + 기존 코드 계약. 실행자는 그대로 소비한다. -->

src/settings/settingsRepo.ts (06-01 산출물):
- `getSettingsRow(db: MigratableDb): Promise<AppSettingsRow | null>`
- `resolveNotificationSettings(row: AppSettingsRow | null): NotificationSettings` — 순수, row가 null이면 `PHASE2_NOTIFICATION_SETTINGS`
- `upsertSettings(db: MigratableDb, settings: NotificationSettings, now: string): Promise<void>` — union 밖 빈도는 throw
- `isNotificationFrequency(value: unknown): value is NotificationFrequency`

src/notifications/config.ts (기존):
- `type NotificationFrequency = 'hourly' | 'every3h' | 'off'`
- `type NotificationSettings = { checkinFrequency; dailyReflectionEnabled: boolean; dailyReflectionHour: number }`
- `PHASE2_NOTIFICATION_SETTINGS` — `{ 'hourly', true, 21 }`

src/notifications/scheduling.ts (기존, 재구현 금지):
- `applyNotificationSettings(settings: NotificationSettings, deps: NotificationDeps): Promise<void>`
  — 라이브 트리거를 읽고 `ALL_MANAGED_IDS` 안에서만 취소-후-등록 순서로 델타를 적용한다. Phase 2가 이미
  고아 트리거 버그를 잡아둔 코드이므로 절대 포크하지 않는다.

src/notifications/deps.ts (기존):
- `defaultNotificationDeps: NotificationDeps`

src/checkin/photos.ts 29~37줄 (액션시트 상수 소유 선례 — 이 형태를 그대로 복제):
- `PHOTO_ACTION_SHEET_OPTIONS`(옵션 배열), `PHOTO_ACTION_SHEET_CANCEL_INDEX`(취소 인덱스),
  `PHOTO_SOURCE_BY_ACTION_SHEET_INDEX`(인덱스→값, 취소 자리는 `null`)
- 실제 `ActionSheetIOS.showActionSheetWithOptions` 호출은 화면이 담당하고, 상수는 config 모듈이 소유한다.

src/checkin/CheckinDetailScreen.tsx (props/헤더 선례):
- `export type CheckinDetailScreenProps = { db: MigratableDb; checkinId: string }`
- `const SMALL_ICON_HIT_SLOP = { top: 8, bottom: 8, left: 8, right: 8 }`
- `navigation.setOptions({ headerLeft: () => <Pressable ... accessibilityLabel={...} hitSlop={SMALL_ICON_HIT_SLOP}><SymbolView name="chevron.left" tintColor={colors.textPrimary} /></Pressable> })`
- 언마운트 후 setState 방지 `isMountedRef` 관용구

app.json:
- `expo.version === "1.0.0"` — `Constants.expoConfig?.version`이 읽는 단일 출처
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SETTINGS_COPY + 빈도 액션시트 상수 단일 출처</name>
  <files>src/settings/content.ts</files>
  <read_first>
    - src/today/content.ts (전체 — `X_COPY = {...} as const` 형식과 "승인된 문서에서 그대로 전사" 규약)
    - src/checkin/photos.ts 20~40줄 (액션시트 옵션/취소인덱스/인덱스→값 3종 세트 소유 패턴)
    - src/checkin/checkinFlow.ts 120~134줄 (`CHECKIN_COPY`의 `saveFailedHeadline: '저장하지 못했어요'`, `retryCta: '다시 시도'` — 저장 실패 문구를 새로 발명하지 않고 같은 어휘를 쓰기 위한 참조)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Copywriting Contract (설정 관련 행 전부)
  </read_first>
  <action>
    `src/settings/content.ts`를 새로 만든다. 런타임 import는 두지 않고 `NotificationFrequency`만
    타입 전용 import 한다(`src/notifications/config.ts`의 규약과 동일).

    `SETTINGS_COPY`를 `as const`로 export 하며 06-UI-SPEC.md에서 그대로 전사한다:
    `screenTitle: '설정'`, `backLabel: '뒤로'`, `sectionNotifications: '알림'`, `sectionInfo: '정보'`,
    `rowFrequency: '알림 빈도'`, `rowDailyReflection: '하루 마무리 알림'`, `rowVersion: '버전'`,
    `frequencyHourly: '매시간'`, `frequencyEvery3h: '3시간마다'`, `frequencyOff: '끔'`,
    `actionSheetCancel: '취소'`, `saveFailed: '저장하지 못했어요'`, `retryCta: '다시 시도'`,
    `settingsEntryLabel: '설정'`(햄버거 아이콘 VoiceOver 라벨 — 06-06이 소비).
    `saveFailed`/`retryCta`는 `CHECKIN_COPY.saveFailedHeadline`/`retryCta`와 같은 문구를 의도적으로
    재사용하는 것임을 주석으로 남긴다(앱 전역 저장 실패 어휘 통일, 06-UI-SPEC.md §Copywriting Contract).

    액션시트 3종 세트를 `photos.ts` 패턴 그대로 export 한다 — 문자열을 다시 리터럴로 쓰지 않고
    `SETTINGS_COPY` 값에서 조립한다:
    - `FREQUENCY_ACTION_SHEET_OPTIONS = [SETTINGS_COPY.frequencyHourly, SETTINGS_COPY.frequencyEvery3h, SETTINGS_COPY.frequencyOff, SETTINGS_COPY.actionSheetCancel] as const`
    - `FREQUENCY_ACTION_SHEET_CANCEL_INDEX = 3`
    - `FREQUENCY_BY_ACTION_SHEET_INDEX: readonly (NotificationFrequency | null)[] = ['hourly','every3h','off', null]`
    - `FREQUENCY_LABEL_BY_VALUE: Readonly<Record<NotificationFrequency, string>>` — 현재 선택값을 행
      trailing 텍스트로 렌더할 때 쓴다.
    화면이 액션시트 인덱스를 하드코딩하지 않게 이 모듈이 인덱스→값 매핑을 소유한다는 근거를
    `photos.ts`와 동일한 문구로 주석에 남긴다.

    "전체 데이터 삭제" 관련 문구를 절대 넣지 않는다 — 승인된 목업의 4번째 행은 D-02가 명시적으로
    제외했다(2단계 유예, PROJECT.md Out of Scope). 그 근거를 주석으로 남겨 이후에 실수로 되살아나지
    않게 한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const SETTINGS_COPY" src/settings/content.ts`가 1이고 같은 선언이 `} as const;`로 닫힌다.
    - `grep -Ec "전체 데이터 삭제|deleteAll|삭제" src/settings/content.ts`가 0이다.
    - `grep -c "FREQUENCY_ACTION_SHEET_CANCEL_INDEX = 3" src/settings/content.ts`가 1이다.
    - `FREQUENCY_BY_ACTION_SHEET_INDEX`의 길이가 `FREQUENCY_ACTION_SHEET_OPTIONS`의 길이와 같고 마지막 원소가 `null`이다.
    - `grep -c "from 'react-native'\|from 'expo-" src/settings/content.ts`가 0이다(런타임 import 없음).
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>설정 화면 문구와 액션시트 옵션이 단일 출처에 모였고, 제외 결정(전체 데이터 삭제)이 코드 레벨에서 지켜진다.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsScreen — 3개 항목 그룹 리스트 + 저장 후 알림 재구성</name>
  <files>src/settings/SettingsScreen.tsx</files>
  <read_first>
    - src/settings/settingsRepo.ts (06-01 산출물 — 소비할 4개 함수 시그니처)
    - src/settings/content.ts (Task 1 산출물)
    - src/notifications/scheduling.ts 114~142줄 (`applyNotificationSettings` 호출 계약 — 취소-후-등록 순서를 절대 재구현하지 않는다)
    - src/notifications/config.ts (전체 — `NotificationSettings` 필드와 `PHASE2_NOTIFICATION_SETTINGS`)
    - src/checkin/CheckinDetailScreen.tsx 55~135줄 및 380~400줄 (props 형태, `isMountedRef` 가드, `navigation.setOptions({ headerLeft })` 커스텀 뒤로가기 패턴, `SMALL_ICON_HIT_SLOP`)
    - src/app/(tabs)/index/index.tsx 의 `ActionSheetIOS.showActionSheetWithOptions` 호출부 (photos 상수 3종을 소비하는 방식)
    - src/theme/tokens.ts (전체)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Component Contracts 4. Settings Screen + §Color Pitfall guard
  </read_first>
  <action>
    `export type SettingsScreenProps = { db: MigratableDb }`, `export function SettingsScreen({ db })`.

    데이터 로드:
    - `const [settings, setSettings] = useState<NotificationSettings>(PHASE2_NOTIFICATION_SETTINGS)`로
      시작하고, 마운트 시 `getSettingsRow(db)` → `resolveNotificationSettings(row)` 결과를 반영한다.
      `isMountedRef` 가드와 `.catch(console.error)`는 `CheckinDetailScreen.tsx`의 로드 관용구를 복제한다.
      row가 아직 없어도 기본값(매시간 / 하루마무리 켜짐)이 보이므로 별도 로딩 상태를 두지 않는다.

    쓰기 경로(두 행이 공유하는 단 하나의 함수):
    - `const persist = useCallback(async (next: NotificationSettings) => { ... }, [db])` 하나만 만든다.
      순서는 반드시 (1) `upsertSettings(db, next, new Date().toISOString())`,
      (2) 성공 시 `applyNotificationSettings(next, defaultNotificationDeps)`,
      (3) 성공 시 `setSettings(next)` + `setSaveFailed(false)`.
      어느 단계든 실패하면 `setSaveFailed(true)`로 두고 화면 상태(`settings`)를 바꾸지 않는다 —
      저장되지 않은 값이 켜진 것처럼 보이면 안 된다. 프로미스를 조용히 삼키지 않는다(`console.error`).
      취소/재등록 델타 로직을 이 파일에 절대 재구현하지 않는다 — `applyNotificationSettings` 호출뿐이다.

    행 1 "알림 빈도":
    - `Pressable` 행, 좌측 라벨 `SETTINGS_COPY.rowFrequency`(16px/500 `placeName`), 우측
      `FREQUENCY_LABEL_BY_VALUE[settings.checkinFrequency]`(16px/400 `colors.textMuted`) + `chevron.right` SymbolView.
    - 탭하면 `ActionSheetIOS.showActionSheetWithOptions({ options: [...FREQUENCY_ACTION_SHEET_OPTIONS], cancelButtonIndex: FREQUENCY_ACTION_SHEET_CANCEL_INDEX }, (index) => { const next = FREQUENCY_BY_ACTION_SHEET_INDEX[index]; if (!next) return; persist({ ...settings, checkinFrequency: next }); })`.
      인덱스 숫자를 직접 하드코딩하지 않는다. `destructiveButtonIndex`를 절대 지정하지 않는다 —
      iOS가 빨간 텍스트로 렌더해 DESIGN.md의 "시맨틱 색상 없음" 원칙을 깬다(CheckinDetailScreen의
      Alert 버튼 스타일 주석과 동일 근거).

    행 2 "하루 마무리 알림":
    - 좌측 라벨 `SETTINGS_COPY.rowDailyReflection`, 우측 react-native `Switch`
      (`value={settings.dailyReflectionEnabled}`, `onValueChange={(v) => persist({ ...settings, dailyReflectionEnabled: v })}`).
      chevron 없음. `trackColor`/`thumbColor`에 `colors.accent`를 쓰지 않는다(accent 예산 위반) —
      iOS 기본 스위치 색을 그대로 두거나 `colors.textMuted` 계열만 쓴다.
    - Phase 7의 회고 기능이 아직 없어도 이 토글은 지금 동작한다는 근거(Phase 2가 이미
      `daily_reflection` 타입 스케줄링을 지원, D-02)를 주석으로 남긴다.

    행 3 "버전":
    - 좌측 라벨 `SETTINGS_COPY.rowVersion`, 우측 `Constants.expoConfig?.version ?? ''`.
      `Pressable`로 감싸지 않고 chevron도 없다 — 이 화면의 유일한 읽기 전용 행이다.
      `'1.0.0'` 같은 버전 문자열을 하드코딩하지 않는다(`app.json`의 `expo.version`이 단일 출처).

    섹션/스타일:
    - 섹션 헤더 2개: `SETTINGS_COPY.sectionNotifications`("알림", 행 1~2 위), `SETTINGS_COPY.sectionInfo`("정보", 행 3 위).
      `typography.helperText` + **`colors.textMuted`**. `colors.accent`를 절대 쓰지 않는다 —
      승인 목업의 올리브톤 섹션 헤더는 accent 예산 확정(2026-08-23/08-31) 이전 산출물이라 그대로
      따르면 안 된다(06-RESEARCH.md Pitfall 4). 이 근거를 주석으로 남긴다.
    - 화면 배경 `colors.background`, 행 배경 `colors.surface`, 행 최소 높이 44, 행 좌우 패딩 `spacing.md`,
      섹션 간격 `spacing.lg`, 구분선 `colors.line`. `@expo/ui`의 네이티브 `List`/`Section`을 import하지
      않는다(06-RESEARCH.md 명시 반려 — 네이티브 시스템 색이 웜 팔레트와 충돌).
    - 헤더 뒤로가기: `navigation.setOptions({ headerLeft: ... })`로 `CheckinDetailScreen`과 동일한
      커스텀 `Pressable` + `chevron.left` + `accessibilityLabel={SETTINGS_COPY.backLabel}` +
      `hitSlop`(8px)을 배선한다. 미저장 경고 로직은 없다 — 이 화면은 변경 즉시 저장이다.
    - 저장 실패 시 해당 섹션 아래에 `SETTINGS_COPY.saveFailed` 문구(muted)와 마지막 시도를 다시
      실행하는 `SETTINGS_COPY.retryCta` `Pressable`을 렌더한다. 빨강/초록/노랑 계열 색을 쓰지 않는다.
    - `tabBarStyle`을 이 파일에서 절대 건드리지 않는다 — 설정 화면은 탭바를 유지한다(D-07,
      06-RESEARCH.md Pitfall 1).
    - JSX에 한글 리터럴을 하드코딩하지 않는다 — 전부 `SETTINGS_COPY` 참조.
    - 진행률/개수 수치를 어떤 형태로도 노출하지 않는다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applyNotificationSettings" src/settings/SettingsScreen.tsx`가 1 이상이고, 같은 파일에 `cancelScheduledNotificationAsync`/`scheduleNotificationAsync`가 등장하지 않는다(재구현 금지).
    - `grep -c "upsertSettings" src/settings/SettingsScreen.tsx`가 1이다(쓰기 경로가 하나뿐).
    - `grep -c "colors.accent" src/settings/SettingsScreen.tsx`가 0이다.
    - `grep -c "expoConfig" src/settings/SettingsScreen.tsx`가 1이고 `grep -c "'1\.0\.0'" src/settings/SettingsScreen.tsx`가 0이다.
    - `grep -c "@expo/ui" src/settings/SettingsScreen.tsx`가 0이다.
    - `grep -c "tabBarStyle" src/settings/SettingsScreen.tsx`가 0이다.
    - `grep -Ec "destructiveButtonIndex|전체 데이터" src/settings/SettingsScreen.tsx`가 0이다.
    - `grep -Ec "SETTINGS_COPY\.(rowFrequency|rowDailyReflection|rowVersion|sectionNotifications|sectionInfo)" src/settings/SettingsScreen.tsx` ≥ 5 이다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>설정 화면이 3개 항목을 렌더하고, 값 변경이 SQLite 저장 → 기존 알림 재구성 함수 호출 순서로 이어진다.</done>
</task>

<task type="auto">
  <name>Task 3: settings-wiring.test.ts — 설정 화면 계약 회귀 가드</name>
  <files>src/app/__tests__/settings-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/tabs-wiring.test.ts (전체 — `@jest-environment node` + `readSource`/`stripComments` 스캐폴드와 단언 스타일)
    - src/app/__tests__/notification-wiring.test.ts (전체 — 알림 모듈 계약을 정적 소스 분석으로 검증한 선례)
    - src/settings/SettingsScreen.tsx, src/settings/content.ts (Task 1~2 산출물 — 단언 대상 식별자)
  </read_first>
  <action>
    `src/app/__tests__/settings-wiring.test.ts`를 새로 만든다. `tabs-wiring.test.ts`와 동일한 기법
    (`@jest-environment node`, `fs.readFileSync` + `stripComments`, 정규식/문자열 단언)을 쓰고,
    모든 단언은 주석을 제거한 소스에 대해 수행한다(자기무효화 방지).

    이 플랜이 담을 단언(라우트/햄버거 관련 단언은 06-06이 같은 파일에 append 한다):
    - `SETTINGS_COPY`가 `as const`로 export되고 D-02가 요구한 3개 행 라벨
      (`'알림 빈도'`, `'하루 마무리 알림'`, `'버전'`)을 정확히 그 문구로 갖는다.
    - `content.ts`/`SettingsScreen.tsx` 어디에도 `'전체 데이터 삭제'` 문구가 없다(D-02 제외 결정 고정).
    - `FREQUENCY_ACTION_SHEET_OPTIONS`가 4개 원소이고 `FREQUENCY_ACTION_SHEET_CANCEL_INDEX`가 3이며
      `FREQUENCY_BY_ACTION_SHEET_INDEX`의 마지막 원소가 `null`이다.
    - `SettingsScreen.tsx`가 `applyNotificationSettings`를 참조하고, `cancelScheduledNotificationAsync`/
      `scheduleNotificationAsync`는 참조하지 않는다(재구현 금지 계약).
    - `SettingsScreen.tsx`가 `upsertSettings`와 `resolveNotificationSettings`를 참조한다.
    - `SettingsScreen.tsx`에 `colors.accent`가 등장하지 않는다(accent 예산 — Pitfall 4).
    - `SettingsScreen.tsx`에 `@expo/ui` import가 없다.
    - `SettingsScreen.tsx`에 `tabBarStyle`이 등장하지 않는다(설정 화면은 탭바 유지).
    - `SettingsScreen.tsx`가 `expoConfig`를 참조하고 버전 문자열 리터럴(`/\d+\.\d+\.\d+/`)을 갖지 않는다.
    - `SettingsScreen.tsx`가 `Switch`를 렌더하고, 그 기본값 경로가 `PHASE2_NOTIFICATION_SETTINGS`를
      경유한다(`resolveNotificationSettings` 참조로 확인).
    - `SettingsScreen.tsx`에 SQL 키워드(`SELECT`/`INSERT`/`app_settings`)가 등장하지 않는다
      (SQL은 repo 파일에만 존재한다는 저장소 규약).
    각 `it()` 제목에 대응하는 결정 ID(D-01/D-02) 또는 pitfall 번호를 남긴다.
  </action>
  <verify>
    <automated>npm test -- src/app/__tests__/settings-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/app/__tests__/settings-wiring.test.ts`가 전부 통과한다.
    - `npm test` 전체 스위트가 green이다.
    - `grep -c "stripComments" src/app/__tests__/settings-wiring.test.ts`가 1 이상이다.
    - 테스트 파일이 `'전체 데이터 삭제'` 부재 단언과 `colors.accent` 부재 단언을 각각 포함한다.
  </acceptance_criteria>
  <done>설정 화면의 스코프 경계(3개 행, 삭제 항목 없음)와 재사용 계약(applyNotificationSettings 호출)이 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력(액션시트 선택 / 스위치) → settingsRepo → SQLite | 이 phase가 새로 만드는 유일한 사용자 쓰기 경로. |
| 설정 값 → `applyNotificationSettings` → OS 알림 스케줄러 | 앱 상태가 네이티브 예약 트리거를 바꾸는 지점. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `SettingsScreen.persist` → `upsertSettings` | mitigate | 화면은 `FREQUENCY_BY_ACTION_SHEET_INDEX`가 돌려준 닫힌 union 값만 넘기고, `upsertSettings`가 쓰기 시점에 한 번 더 `isNotificationFrequency`로 검증한다. SQL은 화면에 존재하지 않는다. |
| T-06-04 | Information Disclosure | 알림 콘텐츠 | mitigate | `scheduling.ts`의 `contentFor(id)`는 id로만 본문을 만든다 — 이 플랜은 "메모 미리보기" 류 설정 항목을 추가하지 않는다. 설정 화면에 체크인 내용/개수/진행률을 렌더하지 않는다. |
| T-06-08 | Denial of Service | `applyNotificationSettings` 호출 실패 | mitigate | 저장/재구성 실패 시 화면 상태를 바꾸지 않고 `saveFailed` 문구 + 재시도 경로를 노출한다 — 실패를 조용히 삼켜 "껐다고 믿는데 계속 울리는" 상태를 만들지 않는다. |
| T-06-SC | Tampering | npm 설치 | accept | 신규 패키지 없음. `expo-constants`는 이미 설치돼 있고(첫 사용일 뿐) 설치 태스크가 없다. |
</threat_model>

<verification>
- `npm test` 전체 스위트 green.
- `npx tsc --noEmit` 통과.
</verification>

<success_criteria>
- 설정 화면 컴포넌트가 3개 항목을 계약대로 렌더하고 accent를 쓰지 않는다.
- 값 변경이 SQLite 저장 → `applyNotificationSettings` 호출 순서로 이어지고, 실패는 사용자에게 보인다.
- 스코프 제외 항목(전체 데이터 삭제)이 테스트로 차단됐다.
</success_criteria>

<output>
Create `.planning/phases/06-calendar-tab/06-04-SUMMARY.md` when done
</output>
