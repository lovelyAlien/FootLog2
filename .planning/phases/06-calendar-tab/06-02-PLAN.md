---
phase: 06-calendar-tab
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/checkin/checkinRepo.ts
  - src/checkin/checkinRepo.test.ts
  - src/calendar/monthGrid.ts
  - src/calendar/monthGrid.test.ts
  - src/calendar/scrubberRange.ts
  - src/calendar/scrubberRange.test.ts
autonomous: true
requirements: [REQ-calendar-grid, REQ-past-date-view, REQ-date-scrubber]

must_haves:
  truths:
    - "월 그리드가 한 달치 셀을 일요일 시작 7열로 계산할 수 있다"
    - "한 달에 기록이 있는 날짜 목록을 단일 범위 쿼리 1회로 알아낼 수 있다"
    - "스크러버가 첫 체크인 날짜~오늘 범위를 벗어나지 않도록 인덱스를 하드 클램프할 수 있다"
    - "기록이 있는 날이 0~1일이면 스크러버를 숨겨야 한다고 판정할 수 있다"
    - "조작된 날짜 문자열이 쿼리에 도달하기 전에 거부된다"
  artifacts:
    - path: "src/calendar/monthGrid.ts"
      provides: "월 그리드 셀 생성 · 월 이동 · 헤더 포맷 · date key 검증(순수 함수)"
      exports: ["buildMonthGrid", "monthRangeBounds", "shiftMonth", "formatMonthHeader", "yearMonthOf", "isValidLocalDateKey", "formatDateKeyTitle"]
    - path: "src/calendar/scrubberRange.ts"
      provides: "스크러버 범위/클램프/가시성 순수 함수 + 치수 상수"
      exports: ["buildScrubberDateKeys", "clampIndex", "indexForTranslation", "shouldShowScrubber", "SCRUBBER_BOTTOM_OFFSET_PT", "SCRUBBER_HEADER_HEIGHT_PT", "SCRUBBER_TOUCH_SURFACE_HEIGHT_PT", "SCRUBBER_TICK_SPACING_PX", "SCRUBBER_MIN_DISTINCT_DATES"]
    - path: "src/checkin/checkinRepo.ts"
      provides: "월 범위 기록 유무 조회 + 전체 기록 요약(첫 날짜/고유 날짜 수)"
      exports: ["getCheckinDateKeysInRange", "getCheckinHistorySummary"]
  key_links:
    - from: "src/calendar/monthGrid.ts"
      to: "src/checkin/localDate.ts"
      via: "resolveLocalDateKey / resolveTimeZone 재사용"
      pattern: "resolveLocalDateKey"
    - from: "src/checkin/checkinRepo.ts"
      to: "checkins.local_date_key 인덱스"
      via: "BETWEEN ? AND ? 범위 쿼리"
      pattern: "local_date_key BETWEEN"
---

<objective>
캘린더 탭 전체가 올라앉을 순수 로직 계층을 만든다: 월 그리드 날짜 산수, 스크러버 범위/클램프
산수, 그리고 "이 달에 기록이 있는 날짜"를 단일 범위 쿼리로 뽑는 repo 함수.

Purpose: 화면 컴포넌트가 날짜 산수와 SQL을 직접 들고 있으면 이 저장소의 테스트 전략(순수
모듈은 실유닛 테스트, 화면은 정적 소스 분석)이 성립하지 않는다. 그리드/스크러버의 모든
"판정"을 여기서 끝내고 컴포넌트는 얇은 소비자로 남긴다.
Output: `src/calendar/monthGrid.ts`, `src/calendar/scrubberRange.ts`, `checkinRepo` 조회 함수 2개
+ 각각의 유닛 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calendar-tab/06-CONTEXT.md
@.planning/phases/06-calendar-tab/06-RESEARCH.md
@.planning/phases/06-calendar-tab/06-PATTERNS.md
@.planning/phases/06-calendar-tab/06-UI-SPEC.md
@docs/designs/calendar-date-scrubber.md
@.claude/skills/test-driven-development/SKILL.md
</context>

<interfaces>
<!-- 실행자는 아래 계약을 그대로 소비한다. 코드베이스 탐색 불필요. -->

src/checkin/localDate.ts (기존, 변경 없음 — 이 플랜이 재사용):
- `export function resolveTimeZone(): string`
- `export function resolveLocalDateKey(date: Date, timeZone?: string): string` — `Intl.DateTimeFormat('en-CA', { timeZone })`, 결과는 `YYYY-MM-DD`
- `export function formatLocalMonthDay(isoTimestamp: string, timeZone?: string): string` — `ko-KR`, `{ month:'long', day:'numeric' }`

src/db/migrations.ts (기존):
- `export type MigratableDb = Pick<SQLiteDatabase, 'getFirstAsync' | 'execAsync' | 'runAsync' | 'getAllAsync'>`

src/checkin/checkinRepo.ts (기존, 이 플랜이 함수만 추가):
- `export async function getTodayCheckins(db: MigratableDb, localDateKey: string): Promise<CheckinRow[]>` — 임의 날짜 키를 받는다. **과거 날짜 뷰가 그대로 재사용하므로 새 조회 함수를 만들지 않는다.**
- 인덱스 `idx_checkins_local_date_key ON checkins(local_date_key)`가 이미 존재한다.

docs/designs/calendar-date-scrubber.md 확정 수치:
- 카드 하단 오프셋 132pt (CLOSED 피크 100pt + 여백 32pt), 헤더 44pt, 드래그 표면 44×44pt
- 범위 = 첫 체크인 날짜 ~ 오늘, 경계에서 러버밴딩 없는 하드 클램프, 모멘텀 없음
- 기록 있는 날이 0~1일이면 스크러버 자체를 숨김
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkinRepo — 월 범위 기록 유무 조회 + 기록 요약</name>
  <files>src/checkin/checkinRepo.ts, src/checkin/checkinRepo.test.ts</files>
  <read_first>
    - src/checkin/checkinRepo.ts (전체 — SQL은 이 파일에만 둔다는 헤더 계약, `getTodayCheckins`/`getLatestCheckinCoordinate`의 파라미터 바인딩·`row ?? null` 관용구)
    - src/checkin/checkinRepo.test.ts (전체 — `createTestDb`/`migrateDbIfNeeded` 스캐폴드와 기존 테스트 번호 체계)
    - src/db/schema.ts (`CheckinRow`, `CREATE_CHECKINS_INDEXES_SQL`)
  </read_first>
  <behavior>
    - `getCheckinDateKeysInRange(db, '2026-09-01', '2026-09-30')`는 그 범위에 체크인이 있는 **고유** `local_date_key`만 오름차순 배열로 반환한다(같은 날 3건이면 1개).
    - 범위 밖 날짜(`2026-08-31`, `2026-10-01`)의 체크인은 결과에 포함되지 않는다.
    - 해당 범위에 체크인이 하나도 없으면 빈 배열을 반환한다(throw 아님).
    - `getCheckinHistorySummary(db)`는 체크인이 전혀 없을 때 `{ earliestDateKey: null, distinctDateCount: 0 }`을 반환한다.
    - 서로 다른 3일에 체크인이 있고 그중 하루에 2건이면 `{ earliestDateKey: <가장 이른 날짜>, distinctDateCount: 3 }`을 반환한다.
  </behavior>
  <action>
    `src/checkin/checkinRepo.ts` 끝에 조회 함수 2개를 추가한다(기존 함수는 수정하지 않는다).

    `getCheckinDateKeysInRange(db: MigratableDb, startDateKey: string, endDateKey: string): Promise<string[]>` —
    `SELECT DISTINCT local_date_key FROM checkins WHERE local_date_key BETWEEN ? AND ? ORDER BY local_date_key ASC`
    를 `getAllAsync<{ local_date_key: string }>`로 실행하고 `.map((r) => r.local_date_key)`로 평탄화한다.
    하루씩 N번 `getTodayCheckins`를 부르는 루프를 절대 만들지 않는다(06-RESEARCH.md §Don't Hand-Roll).
    `local_date_key`가 `YYYY-MM-DD` 사전식 정렬 = 시간순이라는 점이 이 쿼리 성립 근거이며, 기존
    `idx_checkins_local_date_key` 인덱스가 이 접근 패턴을 위해 이미 존재한다는 사실을 주석으로 남긴다.

    `getCheckinHistorySummary(db: MigratableDb): Promise<{ earliestDateKey: string | null; distinctDateCount: number }>` —
    `SELECT MIN(local_date_key) AS earliest_date_key, COUNT(DISTINCT local_date_key) AS distinct_date_count FROM checkins`
    를 `getFirstAsync`로 1회 실행한다. 스크러버 가시성 게이트(기록 0~1일이면 숨김)와 스크롤 범위
    시작점을 한 쿼리로 함께 얻기 위한 함수임을 주석으로 명시한다(06-RESEARCH.md Assumption A4).
    빈 테이블에서 `MIN(...)`은 NULL을 돌려주므로 `?? null`로 정규화하고 `distinctDateCount`는 `?? 0`으로 정규화한다.

    두 함수 모두 `?` 파라미터 바인딩만 쓴다 — 문자열 보간 금지(T-06-03).

    `src/checkin/checkinRepo.test.ts`에 위 behavior 5개를 커버하는 테스트를 파일 끝에 append한다.
    기존 테스트는 수정하지 않는다.
  </action>
  <verify>
    <automated>npm test -- src/checkin/checkinRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/checkin/checkinRepo.test.ts`가 전부 통과하고 behavior 5개에 대응하는 `it(...)`이 존재한다.
    - `grep -c "SELECT DISTINCT local_date_key" src/checkin/checkinRepo.ts`가 1이다.
    - `grep -c "COUNT(DISTINCT local_date_key)" src/checkin/checkinRepo.ts`가 1이다.
    - `grep -v '^\s*//' src/checkin/checkinRepo.ts | grep -c '\${'`가 0이다.
    - `git diff src/checkin/checkinRepo.ts`의 변경 hunk에 `getTodayCheckins` 본문이 포함되지 않는다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>한 달치 기록 유무와 전체 기록 요약을 각각 SQL 1회로 얻을 수 있고, 실엔진 테스트가 이를 검증한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: monthGrid — 일요일 시작 월 그리드 산수 + date key 검증</name>
  <files>src/calendar/monthGrid.ts, src/calendar/monthGrid.test.ts</files>
  <read_first>
    - src/checkin/localDate.ts (전체 — "수동 Date 파싱/UTC 오프셋 산수 금지, Intl.DateTimeFormat이 타임존 변환 전담" 헤더 계약과 en-CA/ko-KR 로케일 선택 근거)
    - src/checkin/localDate.test.ts (전체 — 순수 함수 유닛 테스트의 `@jest-environment node` 헤더와 단언 형식)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Component Contracts 1. Month Grid (셀 상태표, 주 시작 요일, 헤더 문구 형식)
  </read_first>
  <behavior>
    - `buildMonthGrid({ year: 2026, month: 9 })`의 길이는 7의 배수이며(35 또는 42), 첫 셀의 요일은 일요일이다(D-06).
    - 2026년 9월(1일이 화요일)의 그리드에서 인덱스 0,1 셀은 `inCurrentMonth: false`(8월 말일 채움), 인덱스 2 셀은 `{ dateKey: '2026-09-01', dayOfMonth: 1, inCurrentMonth: true }`이다.
    - 그리드에서 `inCurrentMonth: true`인 셀 수가 그 달의 실제 일수와 같다(9월 30, 2월(윤년 아님) 28, 2028년 2월 29).
    - 모든 셀의 `dateKey`가 `YYYY-MM-DD` 형식이고 인접 셀끼리 정확히 하루 차이다(경계 채움 셀 포함).
    - `monthRangeBounds({ year: 2026, month: 9 })`는 `{ startDateKey: '2026-09-01', endDateKey: '2026-09-30' }`이다.
    - `shiftMonth({ year: 2026, month: 12 }, 1)`은 `{ year: 2027, month: 1 }`, `shiftMonth({ year: 2026, month: 1 }, -1)`은 `{ year: 2025, month: 12 }`이다.
    - `formatMonthHeader({ year: 2026, month: 9 })`는 `'2026년 9월'`이다.
    - `yearMonthOf('2026-09-01')`은 `{ year: 2026, month: 9 }`이다.
    - `formatDateKeyTitle('2026-09-01')`은 `'2026년 9월 1일'`이다.
    - `isValidLocalDateKey`: `'2026-09-01'` → true. `'2026-9-1'`, `'2026-13-01'`, `'2026-02-30'`, `''`, `'2026-09-01; DROP TABLE checkins'`, `undefined`, `null`, `12345` → 전부 false(T-06-02 fail-closed).
  </behavior>
  <action>
    `src/calendar/monthGrid.ts`를 새로 만든다. `localDate.ts`와 동일하게 네이티브 모듈을 import 하지
    않는 순수 함수 모듈이며(`@jest-environment node`에서 로드 가능), 헤더 주석에 그 계약과 "월 이동/
    셀 열거는 타임존 변환이 아니라 달력 좌표 계산이므로 `Date.UTC` + `timeZone: 'UTC'` 고정으로
    수행한다 — 사용자 로컬 타임존을 여기에 섞으면 월 경계가 기기 시각에 따라 흔들린다"는 근거를 남긴다.

    export 목록과 시그니처:
    - `export type YearMonth = { year: number; month: number }` (month는 1~12)
    - `export type MonthGridCell = { dateKey: string; dayOfMonth: number; inCurrentMonth: boolean }`
    - `export const WEEKDAY_COUNT = 7`
    - `buildMonthGrid(ym: YearMonth): MonthGridCell[]`
    - `monthRangeBounds(ym: YearMonth): { startDateKey: string; endDateKey: string }`
    - `shiftMonth(ym: YearMonth, delta: number): YearMonth`
    - `formatMonthHeader(ym: YearMonth): string`
    - `yearMonthOf(dateKey: string): YearMonth`
    - `formatDateKeyTitle(dateKey: string): string`
    - `isValidLocalDateKey(value: unknown): value is string`

    구현 규약:
    - 셀 `dateKey` 생성은 `resolveLocalDateKey(new Date(Date.UTC(y, mIndex, d)), 'UTC')`로 한다 —
      수동 문자열 zero-padding 조합을 새로 만들지 않고 `localDate.ts`의 en-CA 포맷터를 재사용한다.
    - 첫 셀 계산은 `new Date(Date.UTC(y, mIndex, 1)).getUTCDay()`(일요일=0)만큼 이전 달로 되감는다.
      주 시작 요일 상수를 `const WEEK_START_DAY = 0`로 두고 D-06 근거를 주석에 남긴다.
    - 마지막 주는 항상 7칸을 채운다(다음 달 앞부분으로 패딩). 6주가 필요한 달은 42칸, 아니면 35칸.
    - `formatMonthHeader`/`formatDateKeyTitle`은 `new Intl.DateTimeFormat('ko-KR', { timeZone: 'UTC', ... })`를
      쓴다(`localDate.ts`의 `formatLocalMonthDay`가 ko-KR을 고른 것과 같은 근거 — 사용자에게 그대로
      노출되는 한국어 문구). 월 이름 배열 하드코딩 금지.
    - `isValidLocalDateKey`는 `/^\d{4}-\d{2}-\d{2}$/` 정규식 통과 + `Date.UTC`로 재구성한 값이 원문과
      round-trip 일치(즉 `2026-02-30` 같은 존재하지 않는 날짜를 거부)까지 확인한다. 이 함수가
      `calendar/[date]` 라우트 파라미터의 유일한 방어선이라는 점(T-06-02)을 주석에 명시한다.

    `src/calendar/monthGrid.test.ts`는 `localDate.test.ts`와 동일한 `@jest-environment node` 헤더로
    위 behavior 전부를 커버한다.
  </action>
  <verify>
    <automated>npm test -- src/calendar/monthGrid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/calendar/monthGrid.test.ts`가 전부 통과하고 behavior 항목마다 대응하는 `it(...)`이 존재한다.
    - `src/calendar/monthGrid.ts`가 `expo-`/`react-native` 로 시작하는 import를 하나도 갖지 않는다(`grep -c "from 'expo\|from 'react-native" src/calendar/monthGrid.ts` == 0).
    - `grep -c "resolveLocalDateKey" src/calendar/monthGrid.ts`가 1 이상이다(포맷터 재사용).
    - `npm test -- src/calendar/monthGrid.test.ts -t "isValidLocalDateKey"`가 통과하며 `'2026-02-30'` 거부 케이스를 포함한다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>월 그리드 셀·월 이동·헤더 문구·날짜 키 검증이 전부 순수 함수로 존재하고 테스트로 고정된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: scrubberRange — 범위 생성 · 하드 클램프 · 가시성 게이트 + 치수 상수</name>
  <files>src/calendar/scrubberRange.ts, src/calendar/scrubberRange.test.ts</files>
  <read_first>
    - docs/designs/calendar-date-scrubber.md (전체 — Premise 5/8/10/11, Implementation Tasks T1~T4의 확정 수치)
    - src/calendar/monthGrid.ts (Task 2 산출물 — `resolveLocalDateKey` 재사용 방식과 순수 모듈 계약)
    - .planning/phases/06-calendar-tab/06-UI-SPEC.md §Component Contracts 3. Date Scrubber
  </read_first>
  <behavior>
    - `buildScrubberDateKeys('2026-08-30', '2026-09-02')`는 `['2026-08-30','2026-08-31','2026-09-01','2026-09-02']`(오름차순, 양끝 포함)이다.
    - `buildScrubberDateKeys(x, x)`는 길이 1 배열이다.
    - `buildScrubberDateKeys('2026-09-02', '2026-08-30')`(시작 > 끝)은 빈 배열을 반환한다(throw 아님).
    - 월/연 경계를 넘는 범위(`'2025-12-30'` ~ `'2026-01-02'`)에서 날짜가 하루씩 정확히 증가한다.
    - `clampIndex(-5, 10) === 0`, `clampIndex(99, 10) === 9`, `clampIndex(3, 10) === 3`, `clampIndex(0, 0) === 0`.
    - `indexForTranslation(5, 0, 10)`은 5다(움직이지 않으면 제자리).
    - `indexForTranslation(5, +SCRUBBER_TICK_SPACING_PX * 2, 10)`은 3이다(손가락을 오른쪽으로 끌면 눈금이 따라와 더 **이전** 날짜가 중앙 인디케이터 아래로 온다).
    - `indexForTranslation(5, -SCRUBBER_TICK_SPACING_PX * 2, 10)`은 7이다.
    - `indexForTranslation(0, +SCRUBBER_TICK_SPACING_PX * 50, 10)`은 0이고, `indexForTranslation(9, -SCRUBBER_TICK_SPACING_PX * 50, 10)`은 9다 — 경계를 넘어가지 않고 즉시 멈춘다(러버밴딩/오버슈트 값이 반환되지 않는다, Premise 10).
    - `shouldShowScrubber(0) === false`, `shouldShowScrubber(1) === false`, `shouldShowScrubber(2) === true` (Premise 11).
    - `SCRUBBER_BOTTOM_OFFSET_PT === 132`, `SCRUBBER_HEADER_HEIGHT_PT === 44`, `SCRUBBER_TOUCH_SURFACE_HEIGHT_PT === 44`, `SCRUBBER_MIN_DISTINCT_DATES === 2`.
  </behavior>
  <action>
    `src/calendar/scrubberRange.ts`를 새로 만든다 — `monthGrid.ts`와 동일한 순수 모듈 계약
    (네이티브 import 없음, `@jest-environment node` 로드 가능).

    치수 상수를 이 파일이 소유한다(컴포넌트에 숫자 리터럴을 흩뿌리지 않는다). 각 상수 옆에
    `calendar-date-scrubber.md`의 출처(Premise 8 / T3 / T4 / Premise 11)를 주석으로 남긴다:
    `SCRUBBER_BOTTOM_OFFSET_PT = 132`, `SCRUBBER_HEADER_HEIGHT_PT = 44`,
    `SCRUBBER_TOUCH_SURFACE_HEIGHT_PT = 44`, `SCRUBBER_MIN_DISTINCT_DATES = 2`,
    `SCRUBBER_TICK_SPACING_PX = 24`.
    `SCRUBBER_TICK_SPACING_PX`만 원본 문서에 없는 값이다 — 06-CONTEXT.md가 "시각적 스크롤 창
    크기는 Claude 재량"으로 남긴 항목(`calendar-date-scrubber.md` Open Questions)이며, 24px 간격이면
    일반적인 iPhone 폭(약 390pt)에서 카드 안에 좌우 약 7일씩 보인다는 근거를 주석에 남긴다.

    함수:
    - `buildScrubberDateKeys(earliestDateKey: string, todayDateKey: string): string[]` — 두 키를
      `Date.UTC` 기준 정수 일수로 바꿔 하루씩 증가시키며 `resolveLocalDateKey(..., 'UTC')`로 키를 만든다
      (`monthGrid.ts`와 동일 규약, 수동 문자열 산수 금지). 시작 > 끝이면 빈 배열.
    - `clampIndex(index: number, length: number): number` — `length <= 0`이면 0, 아니면
      `Math.min(Math.max(index, 0), length - 1)`.
    - `indexForTranslation(startIndex: number, translationX: number, length: number, tickSpacingPx = SCRUBBER_TICK_SPACING_PX): number` —
      `clampIndex(startIndex - Math.round(translationX / tickSpacingPx), length)`. 부호 규약("눈금 쪽이
      손가락을 따라 움직이고 중앙 인디케이터는 고정"이라 오른쪽 드래그 = 과거로 이동, Premise 9)을
      주석으로 명시한다. 감쇠/스프링/관성 항을 절대 넣지 않는다(Premise 10).
    - `shouldShowScrubber(distinctDateCount: number): boolean` — `distinctDateCount >= SCRUBBER_MIN_DISTINCT_DATES`.

    `src/calendar/scrubberRange.test.ts`가 위 behavior 전부를 커버한다.
  </action>
  <verify>
    <automated>npm test -- src/calendar/scrubberRange.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/calendar/scrubberRange.test.ts`가 전부 통과하고 behavior 항목마다 대응하는 `it(...)`이 존재한다.
    - `grep -c "SCRUBBER_BOTTOM_OFFSET_PT = 132" src/calendar/scrubberRange.ts`가 1이다.
    - `grep -c "SCRUBBER_TOUCH_SURFACE_HEIGHT_PT = 44" src/calendar/scrubberRange.ts`가 1이다.
    - `grep -Ec "decay|spring|momentum|velocity" src/calendar/scrubberRange.ts`가 0이다(모멘텀 없음 계약).
    - `grep -c "from 'expo\|from 'react-native" src/calendar/scrubberRange.ts`가 0이다.
    - `npx tsc --noEmit`이 에러 없이 끝난다.
  </acceptance_criteria>
  <done>스크러버의 범위·클램프·가시성 판정과 확정 치수가 순수 모듈에 존재하며, 경계 넘김이 테스트로 차단된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 라우트 파라미터(`calendar/[date]`) → 조회 함수 | 딥링크/네비게이션 파라미터로 들어온 문자열이 SQL 인자가 되는 지점. 이 플랜이 검증 함수를 제공하고, 06-04가 라우트에서 호출한다. |
| 화면 상태(가시 월) → 범위 쿼리 | 앱 내부에서만 생성되는 값이지만 SQL 인자로 들어간다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | `isValidLocalDateKey` (monthGrid.ts) | mitigate | `YYYY-MM-DD` 정규식 + `Date.UTC` round-trip 검증으로 fail-closed. 06-04의 `calendar/[date]` 라우트가 쿼리 전에 반드시 호출한다. |
| T-06-03 | Tampering | `getCheckinDateKeysInRange` / `getCheckinHistorySummary` | mitigate | `BETWEEN ? AND ?` 파라미터 바인딩만 사용, SQL 문자열 보간 0회(acceptance criteria가 게이트). SQL은 `checkinRepo.ts`에만 존재. |
| T-06-SC | Tampering | npm 설치 | accept | 신규 패키지 없음(06-RESEARCH.md §Package Legitimacy Audit) — 설치 태스크 자체가 없다. |
</threat_model>

<verification>
- `npm test` 전체 스위트 green.
- `npx tsc --noEmit` 통과.
- `src/calendar/` 아래 두 모듈 모두 네이티브 모듈 import 0건(Node 테스트 환경 로드 가능).
</verification>

<success_criteria>
- 월 그리드/스크러버의 모든 날짜 판정이 순수 함수로 존재하고 유닛 테스트로 고정됐다.
- 한 달치 기록 유무를 SQL 1회로 얻는 경로가 있고, 하루씩 N번 조회하는 경로는 존재하지 않는다.
- 조작된 날짜 문자열을 거부하는 단일 검증 함수가 존재한다.
</success_criteria>

<output>
Create `.planning/phases/06-calendar-tab/06-02-SUMMARY.md` when done
</output>
