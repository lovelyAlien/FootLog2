---
phase: 09-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/build.gradle.kts
  - backend/settings.gradle.kts
  - backend/gradle/wrapper/gradle-wrapper.properties
  - backend/gradlew
  - backend/gradlew.bat
  - backend/compose.yaml
  - backend/.gitignore
  - backend/src/main/kotlin/com/footlog/backend/BackendApplication.kt
  - backend/src/main/resources/application.yml
  - backend/src/main/resources/application-local.yml
  - backend/src/main/resources/application-staging.yml
  - backend/src/test/kotlin/com/footlog/backend/TestcontainersConfiguration.kt
  - backend/src/test/kotlin/com/footlog/backend/TestBackendApplication.kt
  - backend/src/test/kotlin/com/footlog/backend/BackendApplicationTests.kt
autonomous: true
requirements:
  - REQ-backend-scaffold

must_haves:
  truths:
    - "개발자가 `cd backend && ./gradlew compileKotlin`을 실행하면 Kotlin 컴파일이 성공한다"
    - "런타임 클래스패스에 Flyway(core + postgresql 어댑터), PostgreSQL JDBC, Hibernate ORM이 실제로 해석되어 존재한다"
    - "어떤 설정 파일에도 DB 비밀값이 평문으로 존재하지 않는다 — 환경변수 참조만 있다"
    - "Hibernate는 스키마를 생성하지 않고 검증만 한다(ddl-auto=validate)"
    - "actuator는 health 엔드포인트만 노출하도록 설정되어 있다"
  artifacts:
    - path: "backend/build.gradle.kts"
      provides: "Spring Boot 4.1.1 + Kotlin + Gradle Kotlin DSL 빌드 정의, allOpen/noarg JPA 플러그인 설정, Flyway/Testcontainers 의존성"
      contains: "spring-boot-starter-flyway"
    - path: "backend/compose.yaml"
      provides: "로컬 PostgreSQL 컨테이너 정의(D-12)"
      contains: "postgres"
    - path: "backend/src/main/resources/application.yml"
      provides: "공통 프로파일 — ddl-auto=validate, actuator health-only 노출"
      contains: "validate"
    - path: "backend/src/main/resources/application-staging.yml"
      provides: "스테이징 프로파일 — DataSource를 환경변수로만 주입(D-11)"
      contains: "DATABASE_URL"
    - path: "backend/src/main/kotlin/com/footlog/backend/BackendApplication.kt"
      provides: "Spring Boot 엔트리포인트"
      contains: "@SpringBootApplication"
  key_links:
    - from: "backend/build.gradle.kts"
      to: "org.flywaydb:flyway-database-postgresql"
      via: "runtimeClasspath 의존성 해석"
      pattern: "flyway-database-postgresql"
    - from: "backend/build.gradle.kts"
      to: "jakarta.persistence.Entity"
      via: "allOpen 컴파일러 플러그인 설정"
      pattern: "allOpen"
    - from: "backend/src/main/resources/application-staging.yml"
      to: "환경변수 DATABASE_URL/DATABASE_USERNAME/DATABASE_PASSWORD"
      via: "Spring 프로퍼티 플레이스홀더"
      pattern: "\\$\\{DATABASE_"
---

<objective>
저장소 루트 하위 `backend/` 폴더에 Spring Boot 4.1.1(Kotlin, Gradle Kotlin DSL) 프로젝트를
공식 `start.spring.io` 생성기로 스캐폴딩하고, Boot 4 특유의 함정(Flyway 스타터 필수화,
Kotlin+JPA allOpen 누락)을 빌드 설정에서 미리 막고, D-11이 요구하는 Spring Profiles
3종(공통/local/staging)을 만든다.

Purpose: 이후 모든 Phase 9 플랜(마이그레이션, 엔티티, Docker, CI)이 올라설 빌드/설정 기반을
만든다. 이 플랜이 끝나면 "컴파일되는 Spring Boot 프로젝트"가 존재하지만, 아직 스키마도
엔티티도 없다(그건 09-02/09-04 소관).
Output: `backend/` Gradle 프로젝트 전체 + `application{,-local,-staging}.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-foundation/09-CONTEXT.md
@.planning/phases/09-backend-foundation/09-RESEARCH.md
@.planning/phases/09-backend-foundation/09-PATTERNS.md
@.planning/phases/09-backend-foundation/09-VALIDATION.md

**사전 조건:** Docker Desktop 데몬이 기동 중이어야 한다(09-RESEARCH.md Environment
Availability에서 확인됨: Docker 20.10.12, Compose v2.2.3, JDK 21.0.6). Gradle 최초 실행은
배포판/의존성 다운로드로 수 분이 걸릴 수 있으므로 Bash 호출 timeout을 600000ms로 잡을 것.

**버전 재확인 결과(계획 시점 2026-09-02 실측):** `start.spring.io/metadata/client`의
`bootVersion.default`는 여전히 `4.1.1.RELEASE`다. 09-RESEARCH.md의 버전 표(Kotlin 2.3.21,
Gradle 9.7.1, Hibernate 7.4.1.Final, Flyway 12.4.0, PostgreSQL JDBC 42.7.11)는 그대로 유효하다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: start.spring.io 공식 생성기로 backend/ 스캐폴딩</name>
  <files>backend/build.gradle.kts, backend/settings.gradle.kts, backend/gradlew, backend/gradlew.bat, backend/gradle/wrapper/gradle-wrapper.properties, backend/compose.yaml, backend/.gitignore, backend/src/main/kotlin/com/footlog/backend/BackendApplication.kt, backend/src/test/kotlin/com/footlog/backend/TestcontainersConfiguration.kt, backend/src/test/kotlin/com/footlog/backend/TestBackendApplication.kt, backend/src/test/kotlin/com/footlog/backend/BackendApplicationTests.kt</files>
  <read_first>
    - .planning/phases/09-backend-foundation/09-RESEARCH.md (`## Standard Stack`의 스캐폴딩 curl 명령과 버전 표, `## Architecture Patterns`의 Recommended Project Structure)
    - .planning/phases/09-backend-foundation/09-CONTEXT.md (D-05 단일 저장소 `backend/` 폴더, D-08 Gradle Kotlin DSL, D-12 Docker Compose)
    - .gitignore (저장소 루트 — Expo용 무시 규칙만 있고 JVM/Gradle 항목이 없음을 확인)
  </read_first>
  <action>
저장소 루트에서 09-RESEARCH.md `## Standard Stack`에 실측 검증된 스캐폴딩 명령을 그대로
실행한다. 파라미터는 다음 값을 정확히 사용한다(임의로 바꾸지 않는다):
`type=gradle-project-kotlin`, `language=kotlin`, `bootVersion=4.1.1`, `baseDir=backend`,
`groupId=com.footlog`, `artifactId=backend`, `name=backend`, `packageName=com.footlog.backend`,
`javaVersion=21`, `dependencies=web,data-jpa,postgresql,flyway,validation,actuator,docker-compose,testcontainers`.
`curl`로 `starter.zip`을 내려받아 저장소 루트에서 `unzip`하면 `backend/` 폴더가 생성된다.
zip 파일은 작업 후 삭제한다.

생성 후 다음을 확인만 하고 손대지 않는다(09-PATTERNS.md가 "생성물이지 직접 작성 대상이
아니다"라고 명시한 파일들): `TestcontainersConfiguration.kt`, `TestBackendApplication.kt`,
`compose.yaml`. `compose.yaml`이 생성되지 않았다면 `postgres:17` 이미지, 서비스명 `postgres`,
`POSTGRES_DB=backend`/`POSTGRES_USER=backend`/`POSTGRES_PASSWORD=secret`(로컬 전용 개발값),
포트 `5432` 매핑으로 직접 작성한다.

생성된 `BackendApplicationTests.kt`에 `@Import(TestcontainersConfiguration::class)`가 없으면
추가한다 — 없으면 컨텍스트 로딩 테스트가 DataSource 없이 실패한다.

`backend/.gitignore`가 생성되었는지 확인하고 `build/`, `.gradle/`가 무시 목록에 있는지
확인한다. 없으면 두 항목을 추가한다(루트 `.gitignore`는 수정하지 않는다 — 백엔드 무시 규칙은
`backend/` 스코프에 둔다).

`./gradlew --version`을 실행해 wrapper가 Gradle 9.x를 내려받고 정상 동작하는지 확인한다.
  </action>
  <verify>
    <automated>cd backend && ./gradlew --version && test -f build.gradle.kts && test -f settings.gradle.kts && test -f compose.yaml && test -f src/main/kotlin/com/footlog/backend/BackendApplication.kt</automated>
  </verify>
  <acceptance_criteria>
    - `test -d backend/src/main/kotlin/com/footlog/backend` 가 성공한다
    - `grep -cE 'org\.springframework\.boot.*4\.1\.1' backend/build.gradle.kts` 결과가 1 이상
    - `grep -cE 'gradle-9\.' backend/gradle/wrapper/gradle-wrapper.properties` 결과가 1
    - `grep -cE 'kotlin.*2\.3' backend/build.gradle.kts` 결과가 1 이상
    - `grep -c 'postgres' backend/compose.yaml` 결과가 1 이상
    - `grep -c 'TestcontainersConfiguration' backend/src/test/kotlin/com/footlog/backend/BackendApplicationTests.kt` 결과가 1 이상
    - `grep -vE '^\s*#' backend/.gitignore | grep -c 'build'` 결과가 1 이상
    - 저장소 루트에 `starter.zip`/`backend.zip` 잔여 파일이 없다(`ls backend.zip starter.zip` 이 실패)
  </acceptance_criteria>
  <done>`backend/` Gradle 프로젝트가 존재하고 `./gradlew --version`이 Gradle 9.x를 출력한다.</done>
</task>

<task type="auto">
  <name>Task 2: build.gradle.kts 경화 — allOpen + Flyway 스타터 + 테스트 의존성</name>
  <files>backend/build.gradle.kts</files>
  <read_first>
    - backend/build.gradle.kts (Task 1이 생성한 현재 상태 — 어떤 플러그인/의존성이 이미 있는지 먼저 확인)
    - .planning/phases/09-backend-foundation/09-RESEARCH.md (`## Standard Stack` 표, `## Architecture Patterns` Pattern 1의 allOpen 블록, `## Common Pitfalls` Pitfall 1·2, `## State of the Art`의 Boot4 테스트 스타터 분리 항목)
  </read_first>
  <action>
생성된 `build.gradle.kts`를 다음 4가지 관점에서 점검하고, 빠진 것만 추가한다. **이미 생성기가
넣어준 좌표/버전은 절대 다시 쓰지 않는다** — Boot BOM이 버전을 관리하므로 명시적 버전을
붙이지 않는다.

1. Kotlin JPA 플러그인 + allOpen(09-RESEARCH.md Pitfall 1): `kotlin("plugin.jpa")`가 적용되어
   있는지 확인하고, `allOpen { annotation("jakarta.persistence.Entity"); annotation("jakarta.persistence.MappedSuperclass"); annotation("jakarta.persistence.Embeddable") }`
   블록이 없으면 추가한다. 이 블록이 없으면 엔티티가 `final`로 컴파일되어 Hibernate 지연로딩
   프록시가 동작하지 않는다.
2. Flyway(D-03 마이그레이션 프레임워크, D-01 PostgreSQL 대상 어댑터 / 09-RESEARCH.md Pitfall 2 — Boot 4 breaking change): `implementation("org.springframework.boot:spring-boot-starter-flyway")`와
   `implementation("org.flywaydb:flyway-database-postgresql")`이 모두 있어야 한다.
   `org.flywaydb:flyway-core` 단독으로는 Boot 4에서 마이그레이션이 조용히 실행되지 않는다.
3. 테스트 의존성: 생성기가 넣은 테스트 스타터를 그대로 유지한다. Boot 4는 단일
   `spring-boot-starter-test`가 아니라 모듈별 `*-test` 스타터로 분리되어 있으므로, 이후 플랜에서
   필요한 모듈(`spring-boot-starter-data-jpa-test`, `spring-boot-starter-flyway-test`,
   `spring-boot-starter-webmvc-test`, `spring-boot-starter-actuator-test`)이 부족하면 그때 추가한다 —
   지금 추측으로 미리 넣지 않는다. `spring-boot-testcontainers`와 `org.testcontainers:postgresql`
   테스트 의존성은 생성기 산출물 그대로 유지한다.
4. `spring-boot-docker-compose`는 `developmentOnly` 스코프여야 한다(09-RESEARCH.md Pitfall 3 —
   스테이징/운영 jar에 포함되면 안 된다). `tasks.withType<Test> { useJUnitPlatform() }`이
   있는지 확인한다.

수정 후 `./gradlew dependencies --configuration runtimeClasspath`를 실행해 실제 해석 결과를
확인한다(D-03 Flyway 12.x, D-01 PostgreSQL JDBC 42.7.x, D-04 Hibernate ORM 7.4.x). 좌표가 해석되지 않으면
추측으로 고치지 말고 `start.spring.io` 생성 결과와 대조한다.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileKotlin && ./gradlew dependencies --configuration runtimeClasspath | grep -E "flyway-core|flyway-database-postgresql|org.postgresql:postgresql|hibernate-core"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'spring-boot-starter-flyway' backend/build.gradle.kts` 결과가 1 이상
    - `grep -c 'flyway-database-postgresql' backend/build.gradle.kts` 결과가 1 이상
    - `grep -c 'jakarta.persistence.Entity' backend/build.gradle.kts` 결과가 1 이상 (allOpen 블록)
    - `grep -c 'plugin.jpa' backend/build.gradle.kts` 결과가 1 이상
    - `grep -c 'developmentOnly' backend/build.gradle.kts` 결과가 1 이상이며 같은 줄에 `spring-boot-docker-compose`가 포함된다
    - `grep -c 'useJUnitPlatform' backend/build.gradle.kts` 결과가 1 이상
    - `cd backend && ./gradlew dependencies --configuration runtimeClasspath` 출력에 `org.flywaydb:flyway-core`와 `flyway-database-postgresql`이 모두 등장한다
    - `cd backend && ./gradlew compileKotlin` 이 성공 종료한다
  </acceptance_criteria>
  <done>Boot 4의 Flyway 스타터/allOpen 함정이 빌드 설정 수준에서 막혀 있고 Kotlin 컴파일이 통과한다.</done>
</task>

<task type="auto">
  <name>Task 3: Spring Profiles 3종(공통/local/staging) 작성</name>
  <files>backend/src/main/resources/application.yml, backend/src/main/resources/application-local.yml, backend/src/main/resources/application-staging.yml</files>
  <read_first>
    - backend/src/main/resources/application.properties 또는 application.yml (생성기 산출물 — `.properties`로 생성됐다면 `.yml`로 전환하고 원본은 삭제한다)
    - .planning/phases/09-backend-foundation/09-RESEARCH.md (`## Architecture Patterns` Pattern 3 — 세 파일의 검증된 내용, `## Security Domain` — actuator 노출 잠금과 비밀값 관리)
    - .planning/phases/09-backend-foundation/09-CONTEXT.md (D-09 로컬+스테이징 범위, D-11 Spring Profiles + 환경변수, D-12 Docker Compose)
  </read_first>
  <action>
09-RESEARCH.md Pattern 3의 세 YAML을 그대로 작성한다. 생성기가 `application.properties`를
만들었다면 삭제하고 `application.yml`로 대체한다.

`application.yml`(공통): `spring.application.name: backend`,
`spring.jpa.hibernate.ddl-auto: validate`(스키마 소유자는 Flyway뿐 — `update`/`create`는 금지),
`spring.jpa.open-in-view: false`, `management.endpoints.web.exposure.include: health`
(env/beans/heapdump 등 다른 actuator 엔드포인트는 노출하지 않는다 — T-9-02).

`application-local.yml`: `spring.docker.compose.enabled: true`만 둔다. DataSource 접속정보는
`spring-boot-docker-compose`가 `compose.yaml`에서 자동 주입하므로 하드코딩하지 않는다(D-12).

`application-staging.yml`: `spring.datasource.url/username/password`를 각각
`${DATABASE_URL}`, `${DATABASE_USERNAME}`, `${DATABASE_PASSWORD}` 환경변수 참조로만 쓴다.
평문 비밀값을 한 글자도 넣지 않는다(D-11, T-9-01). `spring-boot-docker-compose`는
`developmentOnly` 스코프라 스테이징 jar에 없다는 점을 파일 상단 주석에 남긴다(Pitfall 3).

세 파일 모두 어떤 키가 왜 그 값인지 짧은 한글 주석을 남긴다 — 특히 `ddl-auto: validate`가
Flyway 단일 소유권을 지키기 위한 것임을 명시한다.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileKotlin && test -f src/main/resources/application.yml && test -f src/main/resources/application-local.yml && test -f src/main/resources/application-staging.yml && ! test -f src/main/resources/application.properties</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^[^#]*ddl-auto:' backend/src/main/resources/application.yml` 출력에 `validate`가 포함되고 `update`/`create`/`create-drop`은 어느 application*.yml에도 없다
    - `grep -E '^[^#]*include:' backend/src/main/resources/application.yml` 출력이 `health`만 포함한다(`"*"`, `env`, `beans` 없음)
    - `grep -c 'DATABASE_URL' backend/src/main/resources/application-staging.yml` 결과가 1
    - `grep -RhE '^[^#]*password:' backend/src/main/resources/application*.yml | grep -v '\${' | wc -l` 결과가 `0` (평문 비밀값 없음)
    - `grep -E '^[^#]*docker' backend/src/main/resources/application-local.yml` 출력에 `compose`가 포함된다
  </acceptance_criteria>
  <done>세 프로파일 파일이 존재하고, 비밀값 하드코딩이 0건이며, Hibernate가 스키마를 검증만 하도록 고정되어 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발자 로컬 → git 저장소 | 설정 파일에 평문 DB 비밀값이 커밋될 수 있는 경계 |
| 외부 네트워크 → Spring Boot HTTP | 이번 phase에서 노출되는 유일한 표면은 `/actuator/*` |
| Maven Central → 로컬/CI 빌드 | 서드파티 코드가 빌드에 유입되는 공급망 경계 |
| 애플리케이션 프로세스 → PostgreSQL | JDBC 자격증명이 오가는 경계 |

이번 phase에는 인증 로직(V2), 세션(V3), 도메인 API 엔드포인트(V4)가 존재하지 않는다 —
09-RESEARCH.md `## Security Domain`이 명시한 대로 해당 ASVS 카테고리는 적용 대상이 아니다.
V5(입력 검증)는 `spring-boot-starter-validation`을 미리 포함해 Phase 10+ 대비만 한다.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Information Disclosure | `application-{local,staging}.yml` | mitigate | 비밀값은 `${DATABASE_PASSWORD}` 등 환경변수 참조만 허용(D-11). 게이트: `grep -RhE '^[^#]*password:' backend/src/main/resources/application*.yml \| grep -v '\${' \| wc -l` 이 0 |
| T-9-02 | Information Disclosure | Spring Boot Actuator | mitigate | `management.endpoints.web.exposure.include: health`로 health만 노출. `/actuator/env`·`/actuator/beans` 비노출은 09-05 HealthCheckSmokeTest가 404로 재확인 |
| T-9-03 | Tampering | Hibernate ddl-auto | mitigate | `ddl-auto: validate` 고정 — 런타임에 JPA가 DDL을 실행할 수 없게 하고 스키마 소유권을 Flyway로 단일화 |
| T-9-SC | Tampering | Maven Central 의존성(공급망) | mitigate | 좌표를 추측하지 않고 공식 `start.spring.io` 생성 결과를 그대로 사용하고 `./gradlew dependencies` 실측으로 해석 확인. 09-RESEARCH.md `## Package Legitimacy Audit`이 전 좌표를 `[VERIFIED]`로 판정했고 `[ASSUMED]`/`[SUS]`가 없다 — JVM/Maven 생태계라 slopcheck(npm/pip/cargo) 대상이 아니므로 인간 검증 체크포인트를 걸지 않는다 |
</threat_model>

<verification>
- `cd backend && ./gradlew compileKotlin` 성공
- `cd backend && ./gradlew dependencies --configuration runtimeClasspath | grep -E "flyway-core|flyway-database-postgresql|hibernate-core|org.postgresql:postgresql"` 4개 항목 모두 출력
- `grep -RhE '^[^#]*password:' backend/src/main/resources/application*.yml | grep -v '\${' | wc -l` 결과 0
- 이 시점에는 `./gradlew build` 전체 통과를 기대하지 않는다 — 마이그레이션(09-02)과 엔티티(09-04)가 아직 없기 때문. 전체 그린은 09-06 게이트에서 확인한다.
</verification>

<success_criteria>
- `backend/`에 Spring Boot 4.1.1 + Kotlin + Gradle Kotlin DSL 프로젝트가 존재하고 컴파일된다
- Boot 4의 Flyway 스타터 필수화와 Kotlin allOpen 함정이 빌드 설정에서 이미 해결되어 있다
- 공통/local/staging 세 프로파일이 존재하고 비밀값 하드코딩이 0건이다
- actuator 노출이 health 하나로 잠겨 있고 Hibernate가 스키마를 변경하지 못한다
</success_criteria>

<output>
Create `.planning/phases/09-backend-foundation/09-01-SUMMARY.md` when done
</output>
