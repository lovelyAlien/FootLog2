---
phase: 09-backend-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-02
  - 09-04
files_modified:
  - backend/src/test/kotlin/com/footlog/backend/HealthCheckSmokeTest.kt
  - backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt
  - backend/build.gradle.kts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-backend-scaffold
  - REQ-backend-db-schema

must_haves:
  truths:
    - "앱이 기동되면 GET /actuator/health가 200과 UP 상태를 반환한다"
    - "/actuator/env, /actuator/beans 같은 다른 actuator 엔드포인트는 노출되지 않는다"
    - "staging 프로파일로도 애플리케이션이 기동된다 — DataSource가 환경변수만으로 주입된다"
    - "REQUIREMENTS.md가 Phase 9~12의 원자 요구사항 8개를 실제로 담고 있고 Traceability 표가 이를 반영한다"
  artifacts:
    - path: "backend/src/test/kotlin/com/footlog/backend/HealthCheckSmokeTest.kt"
      provides: "REQ-backend-scaffold 성공 기준 1(기동 확인) 자동 검증 + actuator 노출 잠금 검증"
      min_lines: 30
    - path: "backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt"
      provides: "staging 프로파일 + 환경변수 DataSource 주입 경로 자동 검증(D-09/D-11)"
      min_lines: 30
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 9~12 원자 요구사항 8개 + Traceability 반영"
      contains: "REQ-backend-scaffold"
  key_links:
    - from: "backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt"
      to: "backend/src/main/resources/application-staging.yml"
      via: "@ActiveProfiles(\"staging\") + DATABASE_* 프로퍼티 주입"
      pattern: "DATABASE_URL"
    - from: "backend/src/test/kotlin/com/footlog/backend/HealthCheckSmokeTest.kt"
      to: "/actuator/health"
      via: "TestRestTemplate HTTP 요청"
      pattern: "/actuator/health"
---

<objective>
"서버가 실제로 기동된다"를 자동 검증으로 못 박고(로컬 프로파일 + staging 프로파일 양쪽),
actuator 노출이 health 하나로 잠겨 있음을 테스트로 확인하며, 계획 중 발견된 문서 공백
(REQUIREMENTS.md에 Phase 9~12 원자 요구사항이 없음)을 메운다.

Purpose: REQ-backend-scaffold의 성공 기준("로컬/스테이징 환경에서 빌드·기동")을 사람 눈이
아니라 CI가 매번 검증하게 만든다. 동시에 09-CONTEXT.md가 canonical reference로 지목한
`REQUIREMENTS.md §백엔드/인증/클라우드` 섹션이 실제로는 존재하지 않는 상태를 해소한다.
Output: 스모크/스테이징 테스트 2종 + 갱신된 REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-backend-foundation/09-CONTEXT.md
@.planning/phases/09-backend-foundation/09-RESEARCH.md
@.planning/phases/09-backend-foundation/09-VALIDATION.md

**문서 공백(계획 단계에서 발견):** 09-CONTEXT.md `<canonical_refs>`는
`.planning/REQUIREMENTS.md §백엔드/인증/클라우드 (Phase 9~12) — REQ-backend-scaffold,
REQ-backend-db-schema`를 참조하라고 지시하지만, 실제 REQUIREMENTS.md에는 그 섹션이 없고
연기 상태의 단일 버킷 `REQ-phase2-backend`만 존재한다. ROADMAP.md에는 Phase 9~12의 요구사항
ID 8개가 이미 쓰여 있으므로, 이 플랜은 새 요구사항을 발명하는 것이 아니라 ROADMAP에 이미
있는 8개를 REQUIREMENTS.md에 반영하는 작업이다. (Phase 6에서도 REQ-settings-screen에 같은
공백이 있었고 동일한 방식으로 해소했다 — STATE.md Blockers/Concerns 참고.)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HealthCheckSmokeTest — 기동 + actuator 노출 잠금 검증</name>
  <files>backend/src/test/kotlin/com/footlog/backend/HealthCheckSmokeTest.kt, backend/build.gradle.kts</files>
  <behavior>
    - Test 1: `GET /actuator/health`가 HTTP 200을 반환하고 응답 본문에 `"status":"UP"`이 포함된다
    - Test 2: `GET /actuator/env`가 404를 반환한다(노출 목록에 없음 — T-9-02)
    - Test 3: `GET /actuator/beans`가 404를 반환한다
  </behavior>
  <read_first>
    - backend/src/main/resources/application.yml (`management.endpoints.web.exposure.include` 설정 현황)
    - backend/src/test/kotlin/com/footlog/backend/EntityPersistenceTest.kt (09-04이 쓴 Testcontainers 주입 형태 재사용)
    - backend/src/test/kotlin/com/footlog/backend/TestcontainersConfiguration.kt
    - .planning/phases/09-backend-foundation/09-RESEARCH.md (`## Validation Architecture` Phase Requirements → Test Map, `## Security Domain` actuator 과다 노출 항목)
  </read_first>
  <action>
`HealthCheckSmokeTest.kt`를 `com.footlog.backend` 패키지에 작성한다.
`@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)` +
`@Import(TestcontainersConfiguration::class)`로 실제 서블릿 컨테이너와 PostgreSQL을 띄우고,
`TestRestTemplate`으로 세 엔드포인트를 호출해 위 `<behavior>` 3건을 단언한다.

Test 2/3은 "노출되지 않는다"를 401/403이 아니라 **404**로 단언한다 — Spring Boot Actuator는
`exposure.include`에 없는 엔드포인트를 아예 매핑하지 않기 때문이다. 이 두 단언은 보안 통제
T-9-02(정보 노출)의 회귀 게이트라는 점을 테스트 주석에 남긴다.

컴파일에 웹 계층 테스트 지원이 필요하면 Boot 4의 해당 모듈 테스트 스타터를
`build.gradle.kts`에 추가한다(예: `spring-boot-starter-webmvc-test`,
`spring-boot-starter-actuator-test`). 버전은 명시하지 않는다. 좌표가 해석되지 않으면 추측하지
말고 `./gradlew dependencies` 출력과 대조한다.

TDD 순서: 테스트를 먼저 작성해 실행하고, 실패하면 실패 원인을 보고 설정(노출 목록 등)을
고친다. 이미 09-01이 설정을 올바르게 넣어 첫 실행부터 통과할 수 있는데, 그 경우
`exposure.include` 값을 일시적으로 `"*"`로 바꿔 Test 2/3이 실제로 실패하는지 한 번 확인한 뒤
원복해 게이트가 살아 있음을 증명한다(그 로그를 SUMMARY에 남긴다).
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*HealthCheckSmokeTest"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/src/test/kotlin/com/footlog/backend/HealthCheckSmokeTest.kt` 성공
    - `grep -c '/actuator/health' backend/src/test/kotlin/com/footlog/backend/HealthCheckSmokeTest.kt` 결과가 1 이상
    - `grep -c '/actuator/env' backend/src/test/kotlin/com/footlog/backend/HealthCheckSmokeTest.kt` 결과가 1 이상
    - `grep -c 'RANDOM_PORT' backend/src/test/kotlin/com/footlog/backend/HealthCheckSmokeTest.kt` 결과가 1 이상
    - `cd backend && ./gradlew test --tests "*HealthCheckSmokeTest"` 가 성공한다
    - SUMMARY에 `exposure.include`를 일시적으로 넓혔을 때 Test 2/3이 실패했다는 로그가 인용되어 있다(게이트가 실제로 작동함을 증명)
  </acceptance_criteria>
  <done>기동 확인과 actuator 노출 잠금이 자동 테스트로 고정됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StagingProfileBootTest — 환경변수 기반 DataSource 기동 검증</name>
  <files>backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt</files>
  <behavior>
    - Test 1: `staging` 프로파일 활성 상태에서 애플리케이션 컨텍스트가 정상 기동된다 — DataSource가 `DATABASE_URL`/`DATABASE_USERNAME`/`DATABASE_PASSWORD` 프로퍼티만으로 구성된다
    - Test 2: staging 프로파일에서도 Flyway가 V1~V3를 적용해 `checkins` 테이블 조회가 성공한다
    - Test 3: staging 프로파일에서도 `GET /actuator/health`가 200이고 `/actuator/env`는 404다(노출 잠금이 프로파일과 무관하게 유지됨)
  </behavior>
  <read_first>
    - backend/src/main/resources/application-staging.yml (검증 대상 — `${DATABASE_URL}` 등 플레이스홀더 구성)
    - backend/src/test/kotlin/com/footlog/backend/TestcontainersConfiguration.kt (이 테스트는 `@ServiceConnection` 자동 주입을 **쓰지 않는다** — staging의 명시적 DataSource 설정 경로를 검증해야 하므로 컨테이너를 직접 선언한다)
    - .planning/phases/09-backend-foundation/09-RESEARCH.md (`## Common Pitfalls` Pitfall 3 — docker-compose 모듈은 developmentOnly라 스테이징에 효과가 없다)
    - .planning/phases/09-backend-foundation/09-CONTEXT.md (D-09 로컬+스테이징 범위, D-11 비밀값은 환경변수로만)
  </read_first>
  <action>
`StagingProfileBootTest.kt`를 작성한다. `@SpringBootTest(webEnvironment = RANDOM_PORT)`와
`@ActiveProfiles("staging")`을 붙이고, `TestcontainersConfiguration`을 import하는 대신 이
테스트 클래스 안에서 `PostgreSQLContainer`를 companion object의 static 필드로 직접 선언·기동한다.
그런 다음 `@DynamicPropertySource`로 `DATABASE_URL`(컨테이너의 jdbcUrl),
`DATABASE_USERNAME`(username), `DATABASE_PASSWORD`(password) 세 프로퍼티를 등록한다 —
이렇게 하면 `application-staging.yml`의 `${DATABASE_URL}` 플레이스홀더가 실제로 해석되는지를
검증하게 된다. `@ServiceConnection`을 쓰면 DataSource가 자동 주입되어 이 검증이 무의미해지므로
쓰지 않는다.

Pitfall 3(개발 전용 docker-compose 모듈은 스테이징에 없음)을 클래스 주석에 한글로 남겨,
"로컬에서 됐으니 스테이징도 되겠지"라는 가정이 왜 위험한지 다음 사람이 알 수 있게 한다.

TDD 순서: 테스트를 먼저 실행한다. `application-staging.yml`에 문제가 있으면(예: 플레이스홀더
오타, 누락된 키) 여기서 기동 실패로 드러난다 — 그 실패를 고친 뒤 GREEN을 만든다.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*StagingProfileBootTest"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt` 성공
    - `grep -c '@ActiveProfiles' backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt` 결과가 1 이며 값이 `staging`이다
    - `grep -c 'DynamicPropertySource' backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt` 결과가 1 이상
    - `grep -c 'DATABASE_URL' backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt` 결과가 1 이상
    - `grep -c 'ServiceConnection' backend/src/test/kotlin/com/footlog/backend/StagingProfileBootTest.kt` 결과가 0
    - `cd backend && ./gradlew test --tests "*StagingProfileBootTest"` 가 성공한다
  </acceptance_criteria>
  <done>staging 프로파일이 환경변수만으로 기동됨이 자동 테스트로 증명된다.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md에 Phase 9~12 원자 요구사항 반영</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (현재 구조: `## v1 Requirements` → 카테고리별 `### 제목` + `- [ ] **REQ-id** (Mn, 원본태스크): 설명` 불릿 / `## v2 Requirements` 안의 `REQ-phase2-backend` 단일 버킷 / 파일 끝의 `## Traceability` 표 `| Requirement | Phase | Status |`)
    - .planning/ROADMAP.md (Phase 9~12 절 — 요구사항 ID 8개와 Success Criteria의 원문. 설명 문구는 여기서 그대로 가져오고 새로 발명하지 않는다)
    - .planning/phases/09-backend-foundation/09-CONTEXT.md (`<canonical_refs>` — 이 섹션을 참조하라고 지시한 출처)
  </read_first>
  <action>
REQUIREMENTS.md에 다음 3가지를 반영한다.

(1) `## v1 Requirements`와 `## v2 Requirements` 사이에 새 최상위 섹션
`## 백엔드/인증/클라우드 (Phase 9~12)`를 추가한다. 짧은 리드 문단에 이 트랙이 1단계
로드맵(Phase 1~8)과 병행하는 독립 트랙이며 2026-09-01 사용자 명시 승인으로 착수됐음을 쓴다.
그 아래에 ROADMAP.md Phase 9~12의 Success Criteria 문장을 근거로 8개 요구사항을 기존
불릿 형식(`- [ ] **REQ-id**: 설명`)으로 쓴다. M-id/원본 태스크 ID는 없으므로 붙이지 않고,
대신 각 항목 끝에 담당 phase를 표기한다:
`REQ-backend-scaffold`(Phase 9, Spring Boot(Kotlin) 프로젝트 초기화 + 로컬/스테이징 빌드·기동),
`REQ-backend-db-schema`(Phase 9, 클라이언트 Checkin/DailyReflection 대응 서버 테이블 + 버전관리
마이그레이션), `REQ-auth-kakao-oauth`(Phase 10, 카카오 OAuth2/PKCE 로그인 완료 시 계정 생성/조회),
`REQ-auth-session-token`(Phase 10, 인증 토큰 발급 + 클라이언트 안전 저장·갱신·재사용),
`REQ-storage-s3-upload`(Phase 11, 인증된 사용자의 사진을 서버 경유로 S3 호환 스토리지 업로드),
`REQ-storage-access-control`(Phase 11, 소유자만 접근 가능한 서명 URL 또는 동등한 접근 제어),
`REQ-sync-local-first`(Phase 12, 오프라인 작성분의 로컬 즉시 반영 + 온라인 복귀 시 자동 동기화),
`REQ-sync-conflict-resolution`(Phase 12, 다중 기기 충돌의 결정론적 해소 전략).
Phase 9의 두 항목은 이 phase에서 구현 중이므로 체크박스 상태를 나머지와 동일하게 `[ ]`로
두고, 완료 표시는 `/gsd:verify-work` 이후 절차에 맡긴다.

(2) `## v2 Requirements`의 `REQ-phase2-backend` 불릿을 유지하되, 이제 위 8개로 분해되어
Phase 9~12 트랙으로 이동했다는 문장과 `→ ## 백엔드/인증/클라우드 (Phase 9~12) 참고`를
덧붙인다. 항목 자체를 삭제하지 않는다 — 과거 결정의 추적성을 남긴다.

(3) 파일 끝 `## Traceability` 표에 8개 행을 추가한다: `| REQ-backend-scaffold | Phase 9 | Pending |`
형태로, Phase 9 두 개는 `Pending`, Phase 10~12 여섯 개도 `Pending`.

ROADMAP.md는 이 태스크에서 수정하지 않는다(Phase 9 요구사항 ID가 이미 기재되어 있다).
  </action>
  <verify>
    <automated>grep -c 'REQ-backend-scaffold\|REQ-backend-db-schema\|REQ-auth-kakao-oauth\|REQ-auth-session-token\|REQ-storage-s3-upload\|REQ-storage-access-control\|REQ-sync-local-first\|REQ-sync-conflict-resolution' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## 백엔드/인증/클라우드 (Phase 9~12)' .planning/REQUIREMENTS.md` 결과가 1
    - 8개 ID 각각에 대해 `grep -c '<ID>' .planning/REQUIREMENTS.md` 결과가 2 이상(요구사항 불릿 1회 + Traceability 표 1회)
    - `grep -c 'REQ-phase2-backend' .planning/REQUIREMENTS.md` 결과가 1 이상이며 해당 줄(또는 바로 다음 줄)에 분해되었다는 안내가 포함된다
    - `grep -cE '^\| REQ-backend-scaffold \| Phase 9 \|' .planning/REQUIREMENTS.md` 결과가 1
    - `grep -cE '^\| REQ-sync-conflict-resolution \| Phase 12 \|' .planning/REQUIREMENTS.md` 결과가 1
    - 기존 v1 요구사항 불릿 수가 변하지 않았다(`grep -c '^- \[' .planning/REQUIREMENTS.md`가 편집 전보다 정확히 8 증가)
  </acceptance_criteria>
  <done>09-CONTEXT.md가 참조하라고 지시한 REQUIREMENTS.md 백엔드 섹션이 실제로 존재하고 Traceability가 8개 요구사항을 반영한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 네트워크 → Spring Boot HTTP | 이번 phase가 노출하는 유일한 표면(`/actuator/*`) — 무엇이 매핑되는지가 곧 공격 표면 |
| 배포 환경 변수 → 애플리케이션 설정 | staging DataSource 자격증명이 프로세스로 들어오는 경계 |

인증(V2)/세션(V3)/접근제어(V4)는 여전히 적용 대상이 아니다 — actuator health는 인증 없이
접근 가능한 것이 표준이며 민감 정보를 담지 않는다(`show-details` 기본값은 never).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 (검증) | Information Disclosure | Spring Boot Actuator | mitigate | 09-01이 건 `exposure.include: health` 설정이 실제로 작동하는지를 `HealthCheckSmokeTest` Test 2/3(`/actuator/env`·`/actuator/beans` 404 단언)로 회귀 게이트화 |
| T-9-17 | Information Disclosure | `/actuator/health` 응답 상세 | accept | `management.endpoint.health.show-details` 기본값이 `never`라 DB 접속 정보/컴포넌트 상세가 노출되지 않는다. 값을 `always`로 바꾸지 않는 한 위험 없음 — 바꿀 경우 Phase 10에서 인증 게이트를 함께 걸어야 한다 |
| T-9-01 (검증) | Information Disclosure | staging DataSource 자격증명 | mitigate | `StagingProfileBootTest`가 환경변수 주입만으로 기동됨을 증명 — 설정 파일에 평문 자격증명이 필요 없다는 사실이 실행으로 확인된다(D-11) |
| T-9-18 | Repudiation | 요구사항 문서 공백 | mitigate | 09-CONTEXT.md가 참조하는 REQUIREMENTS.md 섹션 부재를 그대로 두면 이후 phase가 "무엇을 만들기로 했는지"를 추적할 수 없다. Task 3이 ROADMAP의 8개 ID를 문서에 고정한다 |
</threat_model>

<verification>
- `cd backend && ./gradlew test --tests "*HealthCheckSmokeTest" --tests "*StagingProfileBootTest"` 성공
- REQUIREMENTS.md에 8개 요구사항 + Traceability 8행 존재
- SUMMARY에 actuator 노출 게이트가 실제로 실패를 잡는다는 실험 로그가 인용됨
</verification>

<success_criteria>
- REQ-backend-scaffold 성공 기준 1(로컬/스테이징 기동)이 사람 확인이 아니라 자동 테스트로 검증된다
- actuator 노출 잠금이 회귀 게이트를 갖는다
- REQUIREMENTS.md가 Phase 9~12 요구사항의 단일 출처로 기능한다
</success_criteria>

<output>
Create `.planning/phases/09-backend-foundation/09-05-SUMMARY.md` when done
</output>
