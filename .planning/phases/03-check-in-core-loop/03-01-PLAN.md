---
phase: 03-check-in-core-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/checkin/config.ts
  - src/checkin/deps.ts
  - src/checkin/testing/fakeLocation.ts
  - src/checkin/testing/fakeImagePicker.ts
  - src/checkin/testing/fakePhotoStorage.ts
  - src/checkin/testing/fakeCheckinDeps.test.ts
  - src/checkin/__tests__/nativeDeps.test.ts
autonomous: true
requirements: [REQ-checkin-core, REQ-checkin-confirm-pin, REQ-location-denied-flow]
user_setup: []

must_haves:
  truths:
    - "expo-location / expo-image-picker / expo-file-system / expo-crypto / react-native-maps 5종이 Expo SDK 57 호환 버전으로 package.json에 존재한다"
    - "app.json의 한국어 iOS 권한 문구 3종이 설치 과정에서 덮어써지지 않았다"
    - "@jest-environment node 유닛 테스트가 위 네이티브 패키지를 로드하지 않고도 checkin 모듈 타입을 검증할 수 있다"
    - "D-03/D-04 드래프트 계약이 쓸 DRAFT_ROW_ID 고정 PK 상수가 config.ts 단일 출처에 존재한다"
  artifacts:
    - path: "src/checkin/config.ts"
      provides: "LocationDeps / ImagePickerDeps / PhotoStorageDeps / CryptoDeps 타입 + 캡처 상수 (타입 전용 import만)"
      contains: "export type LocationDeps"
    - path: "src/checkin/deps.ts"
      provides: "네이티브 패키지 런타임 import 유일 지점 + default deps 4종"
      exports: ["defaultLocationDeps", "defaultImagePickerDeps", "defaultPhotoStorageDeps", "defaultCryptoDeps"]
    - path: "src/checkin/testing/fakeLocation.ts"
      provides: "LocationDeps를 만족하는 인메모리 더블 (권한/좌표/지연 제어)"
      exports: ["createFakeLocation"]
    - path: "src/checkin/testing/fakeImagePicker.ts"
      provides: "ImagePickerDeps를 만족하는 인메모리 더블"
      exports: ["createFakeImagePicker"]
    - path: "src/checkin/testing/fakePhotoStorage.ts"
      provides: "PhotoStorageDeps를 만족하는 인메모리 파일 복사 더블"
      exports: ["createFakePhotoStorage"]
  key_links:
    - from: "src/checkin/config.ts"
      to: "expo-location"
      via: "import type만 (런타임 import 금지)"
      pattern: "import type \\* as Location from 'expo-location'"
    - from: "src/checkin/deps.ts"
      to: "expo-location / expo-image-picker / expo-file-system / expo-crypto"
      via: "런타임 import 유일 지점"
      pattern: "^import \\* as Location from 'expo-location'"
---

<objective>
Phase 3가 필요로 하는 네이티브 모듈 5종을 SDK 57 호환 버전으로 설치하고, Phase 2 알림 모듈이 이미 확립한 DI 3파일 분리 패턴(config.ts 타입 전용 / deps.ts 런타임 import 유일 지점 / testing/fake*.ts 더블)을 `src/checkin/`에 그대로 복제한다.

Purpose: 이후 모든 Phase 3 plan(위치 캡처, 사진, 리포지토리, 화면)이 `@jest-environment node` 유닛 테스트로 검증 가능하려면 네이티브 모듈이 타입 경계 뒤에 갇혀 있어야 한다. 이 골격이 없으면 모든 후속 테스트가 네이티브 바인딩을 로드하다 깨진다.
Output: 설치된 5개 패키지, `src/checkin/config.ts`, `src/checkin/deps.ts`, 테스트 더블 3종, 격리 회귀 가드 테스트 2종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-check-in-core-loop/03-RESEARCH.md
@.planning/phases/03-check-in-core-loop/03-PATTERNS.md

<interfaces>
<!-- 복제 대상 패턴 — Phase 2 알림 모듈이 이미 확립한 계약. 그대로 따른다. -->

src/notifications/config.ts (타입 전용 import 규약):
```typescript
// 런타임 import를 두지 않는다 — expo-notifications는 타입 전용 import로만 참조한다.
import type * as Notifications from 'expo-notifications';

export type NotificationDeps = Pick<
  typeof Notifications,
  | 'scheduleNotificationAsync'
  | 'cancelScheduledNotificationAsync'
  | 'getAllScheduledNotificationsAsync'
  | 'getPermissionsAsync'
  | 'requestPermissionsAsync'
>;

export const CALENDAR_TRIGGER_TYPE = 'calendar' as const;
```

src/notifications/deps.ts (런타임 import 유일 지점 + 컴파일타임 정합성 단언):
```typescript
import * as Notifications from 'expo-notifications';
import type { NotificationDeps } from './config';
import { CALENDAR_TRIGGER_TYPE } from './config';

export const defaultNotificationDeps: NotificationDeps = {
  scheduleNotificationAsync: Notifications.scheduleNotificationAsync,
  // ...
};

const _calendarTriggerTypeAssertion: typeof CALENDAR_TRIGGER_TYPE =
  Notifications.SchedulableTriggerInputTypes.CALENDAR;
void _calendarTriggerTypeAssertion;
```

src/notifications/testing/fakeNotifications.ts (더블 구조 + 단일 캐스트 규약):
```typescript
import type { NotificationDeps } from '../config';

type PermissionsStatus = Awaited<ReturnType<NotificationDeps['getPermissionsAsync']>>;

export type FakeNotifications = NotificationDeps & {
  __setPermission(status: 'granted' | 'denied' | 'undetermined'): void;
};

export function createFakeNotifications(): FakeNotifications {
  const store = new Map<string, StoredNotification>();
  let permissionStatus: FakePermissionStatus = 'undetermined';
  function buildPermissionsStatus(status: FakePermissionStatus): PermissionsStatus {
    return { status, granted: status === 'granted', canAskAgain: status === 'undetermined', expires: 'never' } as unknown as PermissionsStatus;
  }
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 네이티브 모듈 5종 설치 + app.json 권한 문구 회귀 확인</name>
  <files>package.json, package-lock.json, src/checkin/__tests__/nativeDeps.test.ts</files>
  <read_first>
    - package.json (현재 expo-* 의존성이 전부 `~57.x.x` 범위로 고정된 관례 확인)
    - app.json (ios.infoPlist의 한국어 권한 문구 3종 + expo.plugins 배열 현재 상태)
    - src/notifications/infoPlist.test.ts (Test 6이 `locationWhenInUsePermission`/`cameraPermission`/`photosPermission` 옵션 부재를 이미 게이트하고 있음 — 이 테스트가 이번 설치로 깨지면 안 된다)
    - .planning/phases/03-check-in-core-loop/03-RESEARCH.md §Standard Stack, §Package Legitimacy Audit
  </read_first>
  <action>
    `npx expo install expo-location expo-image-picker expo-file-system expo-crypto react-native-maps` 로 5개 패키지를 설치한다. `npm install <pkg>`를 직접 쓰지 않는다 — SDK 57과 검증되지 않은 최신(58 canary) 버전이 깔릴 수 있다.

    기대 버전 범위(03-RESEARCH.md §Standard Stack, 2026-08-27 npm 레지스트리 확인값): `expo-location` ~57.0.14, `expo-image-picker` ~57.0.14, `expo-file-system` ~57.0.6, `expo-crypto` ~57.0.2, `react-native-maps` 1.29.0. 실제 설치 결과가 이 범위와 다르면 SUMMARY에 편차를 기록하되 `npx expo install`이 고른 값을 우선한다.

    설치 후 app.json을 확인한다. `npx expo install`이 `expo.plugins` 배열에 `expo-location`/`expo-image-picker` config plugin을 자동 추가했다면 **되돌린다** — 이 프로젝트는 권한 문구를 `ios.infoPlist`에 직접 넣는 방식(Phase 2 D-03 확정)이고, config plugin의 기본 옵션(`locationWhenInUsePermission` 등)이 한국어 문구를 영어 기본값으로 조용히 덮어쓴다. `react-native-maps`는 iOS Apple Maps 기본 provider를 쓰므로 plugin/API 키 설정이 필요 없다.

    `src/checkin/__tests__/nativeDeps.test.ts`를 새로 만들어(`@jest-environment node`) package.json을 `fs.readFileSync` + `JSON.parse`로 정적으로 읽고 5개 패키지가 dependencies에 존재하며 expo-* 4종의 버전 문자열이 `~57.`로 시작함을 단언한다. src/notifications/infoPlist.test.ts와 동일한 정적 분석 패턴을 따른다.
  </action>
  <verify>
    <automated>npm test -- src/checkin/__tests__/nativeDeps.test.ts src/notifications/infoPlist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');['expo-location','expo-image-picker','expo-file-system','expo-crypto','react-native-maps'].forEach(k=>{if(!p.dependencies[k])throw new Error(k)})"` 가 에러 없이 종료한다
    - `grep -c '"expo-location"\|"expo-image-picker"\|"expo-file-system"\|"expo-crypto"\|"react-native-maps"' package.json` 이 5를 출력한다
    - `npm test -- src/notifications/infoPlist.test.ts` 6개 테스트 전부 green (특히 Test 6: plugins에 permission 옵션 부재)
    - `node -e "const a=require('./app.json');const s=JSON.stringify(a.expo.plugins);if(/expo-location|expo-image-picker/.test(s))throw new Error('config plugin added')"` 가 에러 없이 종료한다
    - `src/checkin/__tests__/nativeDeps.test.ts`가 존재하고 green
  </acceptance_criteria>
  <done>5개 네이티브 패키지가 SDK 57 호환 버전으로 설치되고, app.json의 한국어 권한 문구 3종과 plugins 배열이 설치 전과 동일하며, 설치 회귀 가드 테스트가 green이다.</done>
</task>

<task type="auto">
  <name>Task 2: src/checkin/config.ts + deps.ts — DI 3파일 분리 골격</name>
  <files>src/checkin/config.ts, src/checkin/deps.ts</files>
  <read_first>
    - src/notifications/config.ts (타입 전용 import 헤더 규약 + `Pick<typeof SDK, ...>` 관례 + 상수 출처 주석 스타일)
    - src/notifications/deps.ts (런타임 import 유일 지점 계약 주석 + 컴파일타임 정합성 단언 패턴)
    - src/db/migrations.ts (`MigratableDb = Pick<SQLiteDatabase, ...>` 좁힘 관례)
    - .planning/phases/03-check-in-core-loop/03-RESEARCH.md §Architecture Patterns Pattern 1, §Common Pitfalls Pitfall 2
  </read_first>
  <action>
    `src/checkin/config.ts`를 만든다. 파일 상단에 notifications/config.ts와 동일한 "런타임 import를 두지 않는다" 계약 주석을 단다. 다음을 export한다:

    - `LocationDeps = Pick<typeof Location, 'getForegroundPermissionsAsync' | 'requestForegroundPermissionsAsync' | 'getCurrentPositionAsync' | 'getLastKnownPositionAsync'>` (`import type * as Location from 'expo-location'`)
    - `ImagePickerDeps = Pick<typeof ImagePicker, 'launchCameraAsync' | 'launchImageLibraryAsync' | 'requestCameraPermissionsAsync' | 'requestMediaLibraryPermissionsAsync'>` (`import type * as ImagePicker from 'expo-image-picker'`)
    - `CryptoDeps = Pick<typeof Crypto, 'randomUUID'>` (`import type * as Crypto from 'expo-crypto'`)
    - `PhotoStorageDeps = { copyIntoDocumentDirectory(sourceUri: string, fileName: string): string }` — 클래스 기반 `File`/`Paths`를 `Pick`으로 노출하지 않고 **함수 포트 하나로 좁힌다**. 이유를 주석으로 명시할 것: `expo-file-system` SDK 54+ 새 API는 클래스 생성자(`new File(...)`)라 `Pick<typeof FileSystem, 'File' | 'Paths'>` 더블이 생성자 형태를 흉내 내야 해 테스트 더블이 불필요하게 복잡해진다. 반환값은 목적지 파일의 uri 문자열.
    - 상수: `CAPTURE_TIMEOUT_MS = 5000`, `LAST_KNOWN_MAX_AGE_MS = 5 * 60 * 1000`, `DRAFT_ROW_ID = 'draft'`, `LOCATION_ACCURACY_BALANCED = 3 as const`. 각 값의 출처를 주석으로 단다(5000 = product-design.md T5 확정값, DRAFT_ROW_ID = 03-CONTEXT.md D-04 고정 PK).

    `src/checkin/deps.ts`를 만든다. notifications/deps.ts와 동일한 "이 파일이 src/checkin/ 안에서 해당 패키지들을 런타임 import 하는 유일한 파일이다" 계약 주석을 헤더에 단다. `expo-location`, `expo-image-picker`, `expo-crypto`를 네임스페이스 런타임 import하고 `expo-file-system`에서 `File`, `Paths`를 import한다. 다음을 export한다: `defaultLocationDeps`, `defaultImagePickerDeps`, `defaultCryptoDeps`, `defaultPhotoStorageDeps`.

    `defaultPhotoStorageDeps.copyIntoDocumentDirectory`는 `new File(sourceUri)`를 만들고 `new File(Paths.document, fileName)`을 목적지로 `copy()`한 뒤 목적지 `uri`를 반환한다. 문자열 경로 접합(`+`)이나 `expo-file-system/legacy` import를 쓰지 않는다(03-RESEARCH.md Pitfall 2).

    notifications/deps.ts의 `_calendarTriggerTypeAssertion` 패턴을 그대로 복제해, `LOCATION_ACCURACY_BALANCED` 리터럴이 실제 `Location.Accuracy.Balanced` enum 값과 일치하는지 컴파일타임에 단언하는 `const _accuracyAssertion: typeof LOCATION_ACCURACY_BALANCED = Location.Accuracy.Balanced; void _accuracyAssertion;` 를 추가한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import type" src/checkin/config.ts` 가 1 이상이고, `grep -v '^//' src/checkin/config.ts | grep -c "^import \* as"` 가 0이다 (config.ts에 런타임 import 없음)
    - `grep -c "LocationDeps\|ImagePickerDeps\|CryptoDeps\|PhotoStorageDeps" src/checkin/config.ts` 가 4 이상
    - `grep -c "CAPTURE_TIMEOUT_MS = 5000" src/checkin/config.ts` 가 1
    - `grep -c "DRAFT_ROW_ID = 'draft'" src/checkin/config.ts` 가 1
    - `grep -v '^//' src/checkin/deps.ts | grep -c "expo-file-system/legacy"` 가 0
    - `grep -c "_accuracyAssertion" src/checkin/deps.ts` 가 2 이상 (선언 + void)
    - `npx tsc --noEmit` 이 exit code 0
  </acceptance_criteria>
  <done>config.ts는 타입 전용 import만 갖고 4개 deps 타입 + 4개 상수를 export하며, deps.ts만 네이티브 패키지를 런타임 import하고 4개 default deps를 조립하며, `tsc --noEmit`이 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 테스트 더블 3종 + 런타임 import 격리 회귀 가드</name>
  <files>src/checkin/testing/fakeLocation.ts, src/checkin/testing/fakeImagePicker.ts, src/checkin/testing/fakePhotoStorage.ts, src/checkin/testing/fakeCheckinDeps.test.ts, src/checkin/__tests__/nativeDeps.test.ts</files>
  <read_first>
    - src/notifications/testing/fakeNotifications.ts (클로저 store 패턴, `__set*` 제어 메서드, "실제 네이티브와 다르게 동작하는 지점" 주석 규율, 단일 캐스트 경계)
    - src/notifications/testing/fakeNotifications.test.ts (더블 자체를 검증하는 테스트 형태)
    - src/app/__tests__/foundation-wiring.test.ts (디렉터리 스캔 기반 격리 회귀 가드 패턴 — `collectSourceFiles` 구현 참고)
    - src/checkin/config.ts (Task 2 산출물 — 더블이 만족해야 할 타입)
  </read_first>
  <behavior>
    - `createFakeLocation()`은 `LocationDeps`를 타입으로 만족하며, `__setPermission('denied')` 후 `getForegroundPermissionsAsync()`가 `status: 'denied', granted: false`를 반환한다
    - `createFakeLocation().__setDelayMs(6000)` 후 `getCurrentPositionAsync()`는 6초 뒤에 resolve된다 (5초 타임아웃 레이스를 결정적으로 재현하기 위함)
    - `createFakeLocation().__setLastKnown(null)` 후 `getLastKnownPositionAsync()`가 `null`을 반환한다
    - `createFakeImagePicker().__setNextResult({ canceled: true })` 후 `launchCameraAsync()`가 `canceled: true`를 반환한다
    - `createFakePhotoStorage().copyIntoDocumentDirectory('file:///tmp/a.jpg', 'camera-x.jpg')`가 document 접두사가 붙은 목적지 uri를 반환하고 `__copies()`에 (source, fileName) 쌍이 기록된다
  </behavior>
  <action>
    `src/checkin/testing/fakeLocation.ts`, `fakeImagePicker.ts`, `fakePhotoStorage.ts` 3개 더블을 fakeNotifications.ts 구조 그대로 만든다. 각 파일은 해당 네이티브 패키지를 **전혀 import하지 않고**, `../config`에서 타입 전용으로 `LocationDeps`/`ImagePickerDeps`/`PhotoStorageDeps`를 가져와 `Awaited<ReturnType<...>>` / `Parameters<...>`로 필요한 SDK 타입을 유도한다. SDK의 nominal enum 타입(`PermissionStatus`, `Accuracy`)과 구조적 리터럴 사이의 불일치는 fakeNotifications.ts의 `buildPermissionsStatus`와 동일하게 **함수 한 곳에서만** `as unknown as` 캐스트한다.

    `createFakeLocation()`이 노출할 제어 메서드: `__setPermission(status)`, `__setPosition({ lat, lng, accuracyMeters } | null)`, `__setLastKnown({ lat, lng, accuracyMeters } | null)`, `__setDelayMs(ms)`, `__currentPositionCallCount()`, `__requestCallCount()`. `requestForegroundPermissionsAsync`는 fakeNotifications와 동일하게 `undetermined`일 때만 상태를 `granted`로 바꾼다(iOS 실제 동작 모사).

    `createFakeImagePicker()`가 노출할 제어 메서드: `__setNextResult(result)`, `__setPermission(source, status)`, `__lastLaunched()` (`'camera' | 'library' | null`).

    `createFakePhotoStorage()`가 노출할 제어 메서드: `__copies()` (기록된 복사 목록), `__setShouldThrow(boolean)`. `copyIntoDocumentDirectory`는 `file:///fake-document/${fileName}` 형태의 uri를 반환한다.

    각 파일 헤더에 "실제 네이티브 모듈과 다르게 동작하는 지점"을 fakeNotifications.ts 규율대로 번호 목록으로 명시한다(예: 이 더블은 iOS의 위치 서비스 전역 꺼짐 상태를 재현하지 않는다).

    `src/checkin/testing/fakeCheckinDeps.test.ts`(`@jest-environment node`)를 만들어 위 behavior 5개를 검증한다.

    `src/checkin/__tests__/nativeDeps.test.ts`(Task 1에서 만든 파일)에 격리 회귀 가드 테스트를 추가한다: `src/checkin/` 하위의 모든 non-test `.ts`/`.tsx` 파일을 재귀 수집해, `stripComments` 적용 후 `from 'expo-location'` / `'expo-image-picker'` / `'expo-file-system'` / `'expo-crypto'` 를 런타임(`import type`이 아닌) 형태로 import하는 파일이 `src/checkin/deps.ts` 하나뿐임을 단언한다. foundation-wiring.test.ts의 notifications 버전(Test 9)과 동일한 구조를 쓴다.
  </action>
  <verify>
    <automated>npm test -- src/checkin</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/checkin/testing/fakeCheckinDeps.test.ts` 5개 이상 테스트 green
    - `npm test -- src/checkin/__tests__/nativeDeps.test.ts` green, 격리 가드 테스트 포함
    - `grep -v '^//' src/checkin/testing/fakeLocation.ts | grep -c "expo-location"` 가 0
    - `grep -v '^//' src/checkin/testing/fakeImagePicker.ts | grep -c "expo-image-picker"` 가 0
    - `grep -c "__setDelayMs" src/checkin/testing/fakeLocation.ts` 가 1 이상
    - `npx tsc --noEmit` 이 exit code 0
  </acceptance_criteria>
  <done>3개 더블이 각자의 Deps 타입을 컴파일타임에 만족하고, 네이티브 패키지를 import하지 않으며, 격리 회귀 가드가 `src/checkin/deps.ts`만을 유일한 런타임 import 지점으로 강제한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → 빌드 | 5개 신규 패키지가 프로젝트 의존성 트리에 들어옴 |
| Expo config plugin → app.json | 설치 스크립트가 권한 문구를 자동 변경할 수 있음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SC | Tampering | npm install (expo-location, expo-image-picker, expo-file-system, expo-crypto, react-native-maps) | mitigate | 03-RESEARCH.md §Package Legitimacy Audit에서 slopcheck 0.6.1이 5개 전부 `[OK]` 판정 + `expo/expo` 공식 모노레포/`react-native-maps` 캐노니컬 리포로 provenance 확인 완료. `[ASSUMED]`/`[SUS]`/`[SLOP]` 항목 0건이므로 blocking-human 체크포인트 불필요. `npx expo install`로 SDK 검증 버전만 설치(임의 latest 금지) |
| T-3-07 | Tampering | app.json ios.infoPlist 권한 문구 | mitigate | config plugin 자동 추가 시 되돌리고, `src/notifications/infoPlist.test.ts` Test 6(permission 옵션 부재 게이트)을 acceptance criteria로 강제 |
| T-3-08 | Information Disclosure | 테스트 더블이 실제 좌표/사진 경로를 다룸 | accept | 더블은 인메모리 리터럴만 다루며 실제 기기 데이터에 접근하지 않음 — 저위험 |
</threat_model>

<verification>
- `npm test` 전체 스위트 green (Phase 1/2 기존 테스트 회귀 없음)
- `npx tsc --noEmit` exit 0
- `git diff app.json` 결과가 비어 있다 (권한 문구/plugins 변경 없음)
</verification>

<success_criteria>
- 5개 네이티브 패키지가 package.json dependencies에 SDK 57 호환 버전으로 존재
- `src/checkin/config.ts`(타입 전용) + `src/checkin/deps.ts`(런타임 import 유일 지점) 계약이 자동 테스트로 강제됨
- 3개 테스트 더블이 각자 Deps 타입을 만족하며 네이티브 모듈 없이 동작
- app.json 한국어 권한 문구 3종 무변경
</success_criteria>

<output>
Create `.planning/phases/03-check-in-core-loop/03-01-SUMMARY.md` when done
</output>
