---
phase: 03-check-in-core-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/migrations.ts
  - src/db/migrations.test.ts
autonomous: true
requirements: [REQ-checkin-confirm-pin]
user_setup: []

must_haves:
  truths:
    - "앱을 업데이트해도 기존 checkins/daily_reflections 데이터가 지워지지 않은 채 drafts 테이블이 추가된다"
    - "drafts 테이블은 고정 PK 'draft' 단일 row 패턴을 스키마 레벨에서 지원한다 (D-03 SQLite 영속화 / D-04 단일 row)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "CREATE_DRAFTS_TABLE_SQL DDL 상수 + DraftRow 행 타입"
      contains: "CREATE TABLE IF NOT EXISTS drafts"
    - path: "src/db/migrations.ts"
      provides: "DATABASE_VERSION 2 + currentDbVersion === 1 마이그레이션 블록"
      contains: "DATABASE_VERSION = 2"
    - path: "src/db/migrations.test.ts"
      provides: "drafts 테이블 컬럼 계약 + v1→v2 데이터 보존 회귀 테스트"
  key_links:
    - from: "src/db/migrations.ts"
      to: "CREATE_DRAFTS_TABLE_SQL"
      via: "currentDbVersion === 1 블록의 execAsync"
      pattern: "currentDbVersion === 1"
---

<objective>
03-CONTEXT.md D-03/D-04가 확정한 SQLite `drafts` 테이블을 Phase 1의 `PRAGMA user_version` 마이그레이션 프레임워크에 추가하고, `DATABASE_VERSION`을 1에서 2로 올린다.

Purpose: 확인 핀 구간의 드래프트를 AsyncStorage가 아닌 SQLite에 두는 것이 D-03의 핵심이며, 이 테이블 없이는 REQ-checkin-confirm-pin의 드래프트 영속화(날짜 경계 만료·저장 시 삭제·단일 드래프트·권한 변경 강건성) 4가지 엣지케이스를 구현할 수 없다. 또한 이 plan은 Phase 1이 만든 마이그레이션 프레임워크가 실제로 "기존 데이터를 지우지 않고 테이블을 추가할 수 있는지"를 처음으로 실증하는 자리다(ROADMAP Phase 1 Success Criteria 3).
Output: `drafts` 테이블 DDL, v1→v2 마이그레이션 블록, 데이터 보존 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-check-in-core-loop/03-CONTEXT.md
@.planning/phases/03-check-in-core-loop/03-RESEARCH.md
@.planning/phases/03-check-in-core-loop/03-PATTERNS.md

<interfaces>
<!-- 확장 대상 기존 코드. 아래 계약을 그대로 이어받는다. -->

src/db/schema.ts (현재 export — 이 파일에 상수 1개 + 타입 1개를 append한다):
```typescript
export type LocationSource =
  | 'gps_auto' | 'gps_dragged' | 'gps_low_accuracy_fallback'
  | 'manual_denied' | 'manual_no_signal';

export interface CheckinRow { /* 13 columns */ }
export const CREATE_CHECKINS_TABLE_SQL = `...`;
export const CREATE_DAILY_REFLECTIONS_TABLE_SQL = `...`;
export const CREATE_CHECKINS_INDEXES_SQL = `...`;
```

src/db/migrations.ts (현재 구조 — `=== 0` 블록은 절대 수정 금지):
```typescript
export const DATABASE_NAME = 'footlog.db';
export const DATABASE_VERSION = 1;
export type MigratableDb = Pick<SQLiteDatabase, 'getFirstAsync' | 'execAsync' | 'runAsync'>;

export async function migrateDbIfNeeded(db: MigratableDb): Promise<void> {
  const result = await db.getFirstAsync<{ user_version: number }>('PRAGMA user_version');
  let currentDbVersion = result?.user_version ?? 0;
  if (currentDbVersion >= DATABASE_VERSION) return;
  if (currentDbVersion === 0) { /* checkins/daily_reflections/index 생성 */ currentDbVersion = 1; }
  // 새 블록은 여기에 append
  await db.execAsync(`PRAGMA user_version = ${currentDbVersion}`); // 유일한 문자열 보간 허용 지점
}
```

src/db/testing/nodeSqliteAdapter.ts (테스트 DB — 재사용, 신규 작성 불필요):
```typescript
export function createTestDb(path?: string): { db: MigratableDb; raw: DatabaseSync; close: () => void };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CREATE_DRAFTS_TABLE_SQL DDL + DraftRow 타입 (schema.ts 확장)</name>
  <files>src/db/schema.ts, src/db/migrations.test.ts</files>
  <read_first>
    - src/db/schema.ts (전체 — `CREATE_CHECKINS_TABLE_SQL`의 들여쓰기/컬럼 순서/주석 스타일을 그대로 복제할 대상)
    - src/db/migrations.test.ts (Test 2/Test 3의 "정확히 N개 컬럼" 계약 테스트 형태와 `CHECKINS_COLUMNS` 상수 배열 패턴)
    - .planning/phases/03-check-in-core-loop/03-RESEARCH.md §Architecture Patterns Pattern 4 (drafts 스키마 설계안 원문)
  </read_first>
  <behavior>
    - `migrateDbIfNeeded` 실행 후 `sqlite_master`에 `drafts` 테이블이 존재한다
    - `PRAGMA table_info(drafts)`가 정확히 9개 컬럼을 반환한다: id, lat, lng, accuracy_meters, location_source, local_date_key, timezone_at_capture, created_at, updated_at
    - `id`가 PRIMARY KEY이고 NOT NULL이다 (`INSERT INTO drafts (lat, ...)` 처럼 id를 빼면 실패)
    - `lat`/`lng`/`location_source`/`local_date_key`/`timezone_at_capture`/`created_at`/`updated_at`에 NULL을 넣으면 NOT NULL 제약 위반으로 실패한다
    - `accuracy_meters`에는 NULL을 넣을 수 있다
  </behavior>
  <action>
    `src/db/schema.ts` 하단에 다음을 append한다(기존 상수/타입은 수정하지 않는다):

    `DraftRow` 인터페이스 — 필드: `id: string`, `lat: number`, `lng: number`, `accuracy_meters: number | null`, `location_source: LocationSource`, `local_date_key: string`, `timezone_at_capture: string`, `created_at: string`, `updated_at: string`. 기존 `LocationSource` 타입을 재사용한다 — 새 enum을 발명하지 않는다.

    `CREATE_DRAFTS_TABLE_SQL` 상수 — `CREATE TABLE IF NOT EXISTS drafts`로 시작하고 컬럼은 다음 순서/제약: `id TEXT PRIMARY KEY NOT NULL`, `lat REAL NOT NULL`, `lng REAL NOT NULL`, `accuracy_meters REAL`, `location_source TEXT NOT NULL`, `local_date_key TEXT NOT NULL`, `timezone_at_capture TEXT NOT NULL`, `created_at TEXT NOT NULL`, `updated_at TEXT NOT NULL`. `CREATE_CHECKINS_TABLE_SQL`과 동일한 템플릿 리터럴 들여쓰기 스타일을 쓴다.

    주석으로 명시할 것: (1) `id`는 항상 `'draft'` 고정값이며 이것이 03-CONTEXT.md D-04의 "항상 최대 1개" 계약을 스키마 레벨에서 강제하는 방식이라는 것, (2) `local_date_key`는 날짜 경계 만료 판정(product-design.md T24 edge case 1)용이라는 것, (3) `note`/`photo_path`가 없는 이유 — 드래프트 구간은 "GPS 캡처 완료 ~ 확인 탭" 사이이며 메모/사진 입력은 저장 성공 이후 화면이라 드래프트에 담길 일이 없다(03-RESEARCH.md Pattern 4). 인덱스는 만들지 않는다 — 단일 row 테이블이라 PK만으로 충분하다.

    `src/db/migrations.test.ts`에 `DRAFTS_COLUMNS` 상수 배열(9개)을 추가하고, 위 behavior 5개를 검증하는 테스트를 기존 Test 2/Test 4 패턴 그대로 append한다. 기존 테스트 번호를 재사용하지 말고 Test 10 이후로 이어붙인다.
  </action>
  <verify>
    <automated>npm test -- src/db/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS drafts" src/db/schema.ts` 가 1
    - `grep -c "CREATE_DRAFTS_TABLE_SQL" src/db/schema.ts` 가 1 이상
    - `grep -c "DraftRow" src/db/schema.ts` 가 1 이상
    - `grep -c "note\|photo_path" src/db/schema.ts` 결과가 기존 checkins 정의 범위에서만 증가하고 drafts DDL 블록 안에는 0 (drafts는 메모/사진 컬럼을 갖지 않음)
    - `npm test -- src/db/migrations.test.ts` 전체 green, drafts 관련 신규 테스트 5개 포함
  </acceptance_criteria>
  <done>`drafts` 9컬럼 DDL과 `DraftRow` 타입이 schema.ts에 존재하고, 컬럼 계약/NOT NULL 제약이 실제 SQLite 엔진 테스트로 검증된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DATABASE_VERSION 2 + v1→v2 마이그레이션 블록 + 데이터 보존 회귀</name>
  <files>src/db/migrations.ts, src/db/migrations.test.ts</files>
  <read_first>
    - src/db/migrations.ts (전체 — `=== 0` 블록, PRAGMA 보간 보안 주석, 확장 안내 주석 위치)
    - src/db/migrations.test.ts (Test 1은 `expect(DATABASE_VERSION).toBe(1)`을 단언하고 있어 이번 변경으로 반드시 갱신해야 한다. Test 5/Test 7의 데이터 보존 검증 형태, Test 8의 문자열 보간 게이트)
    - .planning/phases/03-check-in-core-loop/03-PATTERNS.md §`src/db/migrations.ts` (확장 패턴 원문)
  </read_first>
  <behavior>
    - 빈 DB에 `migrateDbIfNeeded`를 실행하면 `user_version`이 2가 되고 checkins/daily_reflections/drafts 3개 테이블이 모두 생성된다
    - `user_version = 1` 상태(checkins에 row 1건 존재)에서 `migrateDbIfNeeded`를 실행하면 drafts 테이블이 새로 생기고 **기존 checkins row가 그대로 남아 있다**
    - `migrateDbIfNeeded`를 연속 2회 실행해도 결과가 동일하다(idempotent)
    - `user_version`이 이미 2 이상이면 `execAsync`를 한 번도 호출하지 않고 즉시 반환한다
    - `migrations.ts` 소스에서 주석을 제거했을 때 템플릿 보간(`${`) 출현 횟수가 정확히 1이다 (PRAGMA user_version 한 줄만 예외 — T-1-01 선례)
  </behavior>
  <action>
    `src/db/migrations.ts`를 수정한다:
    - `DATABASE_VERSION`을 `1`에서 `2`로 올린다.
    - 파일 상단 import 목록에 `CREATE_DRAFTS_TABLE_SQL`을 추가한다.
    - 기존 `if (currentDbVersion === 0) { ... }` 블록은 **한 글자도 수정하지 않는다**(migration_discipline #2 — 이미 그 버전을 통과한 기기는 변경분을 받지 못한다).
    - 기존 확장 안내 주석 자리에 새 블록을 append한다: `if (currentDbVersion === 1) { await db.execAsync(CREATE_DRAFTS_TABLE_SQL); currentDbVersion = 2; }`. `execAsync`를 개별 호출로 분리하고 DDL 문자열을 템플릿 보간으로 조합하지 않는다(01-03의 T-1-01 선례 — Test 8 게이트가 `${` 출현을 정확히 1로 강제한다).
    - 확장 안내 주석은 다음 phase가 이어붙일 수 있도록 `if (currentDbVersion === 2)` 예시로 갱신해 유지한다.

    `src/db/migrations.test.ts`를 수정한다:
    - Test 1의 `expect(DATABASE_VERSION).toBe(1)`을 `toBe(2)`로 갱신하고, 같은 테스트에서 `tableNames`에 `drafts`도 포함됨을 단언하도록 확장한다. 테스트 이름의 "user_version을 1로 올린다"도 2로 갱신한다.
    - Test 6(즉시 반환)이 `user_version`을 DATABASE_VERSION보다 크게 세팅하는 방식이라면 새 값 기준으로 여전히 유효한지 확인하고 필요 시 갱신한다.
    - 위 behavior 중 "user_version = 1 상태에서 업그레이드 시 기존 checkins row 보존"을 검증하는 신규 테스트를 추가한다. 절차: `createTestDb()` → `migrateDbIfNeeded` 대신 `CREATE_CHECKINS_TABLE_SQL`/`CREATE_DAILY_REFLECTIONS_TABLE_SQL`/`CREATE_CHECKINS_INDEXES_SQL`을 직접 실행하고 `PRAGMA user_version = 1`로 세팅해 v1 기기를 재현 → checkins에 row 1건 insert → `migrateDbIfNeeded(db)` → drafts 테이블 존재 + checkins row가 여전히 1건 + `user_version`이 2임을 단언.
  </action>
  <verify>
    <automated>npm test -- src/db</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DATABASE_VERSION = 2" src/db/migrations.ts` 가 1
    - `grep -c "currentDbVersion === 1" src/db/migrations.ts` 가 1
    - `grep -c "currentDbVersion === 0" src/db/migrations.ts` 가 1 (기존 블록 그대로 유지)
    - `git diff src/db/migrations.ts` 에서 `currentDbVersion === 0` 블록 내부 라인이 변경되지 않았다
    - `grep -c 'DATABASE_VERSION).toBe(1)' src/db/migrations.test.ts` 가 0
    - `npm test -- src/db` 전체 green (기존 Test 1~9 + 신규 테스트 포함)
    - `npm test` 전체 스위트 green (Phase 1/2 회귀 없음)
  </acceptance_criteria>
  <done>`DATABASE_VERSION`이 2이고, v1 기기가 데이터 손실 없이 drafts 테이블을 받으며, 문자열 보간 게이트(Test 8)가 여전히 green이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 코드 → SQLite 파일 | DDL/DML이 로컬 DB 파일을 변경 |
| 기존 설치 기기 → 새 앱 버전 | 마이그레이션이 기존 사용자 데이터를 다룸 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | `src/db/migrations.ts` DDL 실행 | mitigate | DDL을 템플릿 보간으로 조합하지 않고 `CREATE_DRAFTS_TABLE_SQL` 상수를 `execAsync`에 그대로 전달. 기존 Test 8(주석 제거 후 `${` 출현 횟수 정확히 1)이 회귀 게이트로 강제 (T-1-01 선례) |
| T-3-11 | Denial of Service (데이터 손실) | v1 기기 업그레이드 시 기존 checkins/daily_reflections 데이터 | mitigate | `if (currentDbVersion === 0)` 블록 불변 + `CREATE TABLE IF NOT EXISTS`만 사용(DROP/ALTER 없음) + v1→v2 데이터 보존 회귀 테스트를 acceptance criterion으로 강제 |
| T-3-12 | Tampering | `drafts.id` 다중 row 삽입으로 단일 드래프트 계약 우회 | mitigate | `id TEXT PRIMARY KEY NOT NULL` + 호출부의 `INSERT OR REPLACE ... VALUES ('draft', ...)` 고정 PK 패턴(03-04에서 구현)이 스키마 레벨에서 단일 row를 강제 |
| T-3-SC | Tampering | npm/pip/cargo installs | mitigate | 이 plan은 신규 패키지를 설치하지 않음 — 03-RESEARCH.md §Package Legitimacy Audit이 phase 전체 5개 패키지를 이미 `[OK]`로 판정했고 설치는 03-01이 담당 |
</threat_model>

<verification>
- `npm test -- src/db` green
- `npm test` 전체 스위트 green
- `npx tsc --noEmit` exit 0
</verification>

<success_criteria>
- `drafts` 테이블이 9개 컬럼 계약대로 생성되고 NOT NULL 제약이 실제 엔진에서 강제됨
- `DATABASE_VERSION`이 2이고 v1→v2 업그레이드가 기존 데이터를 보존함
- 기존 `currentDbVersion === 0` 블록이 무변경
- SQL 문자열 보간 게이트(Test 8)가 여전히 green
</success_criteria>

<output>
Create `.planning/phases/03-check-in-core-loop/03-03-SUMMARY.md` when done
</output>
