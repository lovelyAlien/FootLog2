---
phase: 03-check-in-core-loop
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/checkin/permissions.ts
  - src/checkin/permissions.test.ts
  - src/components/LocationDeniedBanner.tsx
  - src/components/__tests__/locationUi.test.ts
autonomous: true
requirements: [REQ-location-denied-flow]
user_setup: []

must_haves:
  truths:
    - "위치 권한이 거부되면 사용자는 알림 거부와 동일한 조용한 회색 배너와 설정 딥링크를 본다 (오류/경고색 없음)"
    - "앱이 포그라운드로 돌아올 때마다 위치 권한 상태가 재확인되어 배너가 자동으로 사라진다"
    - "위치 권한이 undetermined일 때만 실제 OS 프롬프트가 뜬다 — 이미 거부된 뒤 반복 호출하지 않는다"
  artifacts:
    - path: "src/checkin/permissions.ts"
      provides: "위치 권한 조회/요청/배너 판정/포그라운드 재확인 훅"
      exports: ["fetchLocationPermission", "requestLocationPermission", "shouldShowLocationDeniedBanner", "useLocationPermissionBanner"]
    - path: "src/components/LocationDeniedBanner.tsx"
      provides: "위치 권한 거부 조용한 배너 (배치는 부모가 결정)"
      exports: ["LocationDeniedBanner"]
  key_links:
    - from: "src/components/LocationDeniedBanner.tsx"
      to: "src/checkin/permissions.ts"
      via: "useLocationPermissionBanner 훅"
      pattern: "useLocationPermissionBanner"
    - from: "src/checkin/permissions.ts"
      to: "AppState 'active' 이벤트"
      via: "notifications/permissions.ts의 subscribeToForegroundActive 재사용"
      pattern: "subscribeToForegroundActive"
---

<objective>
위치 권한 조회/요청/거부 배너를 Phase 2 알림 권한 모듈과 **동일한 계약**으로 구현한다. REQ-location-denied-flow가 요구하는 "알림 거부와 동일한 배너 패턴"을 문구만 바꿔 복제하고, Phase 3가 `requestForegroundPermissionsAsync()` 호출의 소유자임을 코드로 확정한다.

Purpose: 03-RESEARCH.md Open Questions #2가 "Phase 3 vs Phase 4의 위치 권한 요청 호출 책임 경계"를 열린 질문으로 남겼다. **이 계획이 그 경계를 확정한다: Phase 3가 소유한다.** 체크인 버튼이 동작하려면 권한 요청이 필수이고, Phase 4(REQ-onboarding-empty-state)는 그 위에 온보딩 내러티브/empty state 문구만 얹는다.
Output: `src/checkin/permissions.ts`, `src/components/LocationDeniedBanner.tsx`, 그리고 두 파일의 계약 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-check-in-core-loop/03-CONTEXT.md
@.planning/phases/03-check-in-core-loop/03-RESEARCH.md
@.planning/phases/03-check-in-core-loop/03-UI-SPEC.md
@.planning/phases/03-check-in-core-loop/03-PATTERNS.md

<interfaces>
<!-- 복제 대상: Phase 2 알림 권한 모듈. 구조를 그대로 따르고 문구/SDK만 치환한다. -->

src/notifications/permissions.ts (전체 계약):
```typescript
export type PermissionSnapshot = {
  status: 'granted' | 'denied' | 'undetermined';
  granted: boolean;
  canAskAgain: boolean;
};
export type AppStateLike = {
  addEventListener(type: 'change', handler: (state: string) => void): { remove(): void };
};
export async function fetchNotificationPermission(deps?): Promise<PermissionSnapshot>;
export async function requestNotificationPermission(deps?): Promise<PermissionSnapshot>;
export function shouldShowDeniedBanner(permission: PermissionSnapshot | null): boolean;
export function subscribeToForegroundActive(handler: () => void, appState?: AppStateLike): () => void;
export function useNotificationPermissionBanner(): { showBanner: boolean; openSettings: () => void };
```

src/components/NotificationDeniedBanner.tsx (복제 대상 컴포넌트 계약):
```typescript
// 재사용 가능한 독립 컴포넌트 계약: 화면별 로직이나 위치 지정(absolute positioning)을
// 내부에 갖지 않는다 — 배치는 항상 부모가 결정한다.
export function NotificationDeniedBanner() {
  const { showBanner, openSettings } = permissions.useNotificationPermissionBanner();
  if (!showBanner) return null;
  return (
    <Pressable onPress={openSettings} accessibilityRole="button"
      accessibilityLabel="알림이 꺼져있어요. 설정에서 켜기" style={styles.banner}>
      <Text style={[typography.helperText, styles.text]}>알림이 꺼져있어요 · 설정에서 켜기</Text>
    </Pressable>
  );
}
const styles = StyleSheet.create({
  banner: { backgroundColor: colors.surface, minHeight: 44,
    paddingHorizontal: spacing.md, paddingVertical: spacing.xs, justifyContent: 'center' },
  text: { color: colors.textMuted },
});
```

src/checkin/config.ts (03-01 산출물):
```typescript
export type LocationDeps = Pick<typeof Location,
  'getForegroundPermissionsAsync' | 'requestForegroundPermissionsAsync'
  | 'getCurrentPositionAsync' | 'getLastKnownPositionAsync'>;
```
</interfaces>

**확정 문구(03-UI-SPEC.md §Copywriting Contract):** 배너 텍스트 `위치 권한이 꺼져있어요 · 설정에서 켜기`, accessibilityLabel `위치 권한이 꺼져있어요. 설정에서 켜기`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/checkin/permissions.ts — 위치 권한 조회/요청/배너 판정/포그라운드 재확인</name>
  <files>src/checkin/permissions.ts, src/checkin/permissions.test.ts</files>
  <read_first>
    - src/notifications/permissions.ts (전체 — 가드절 스타일, `PermissionSnapshot` 3필드 정규화, isMounted 가드 + `.catch(console.error)` 규약, 훅 구조)
    - src/notifications/permissions.test.ts (전체 — `@jest-environment node`, `createFakeAppState` 인라인 더블, `jest.mock()` 미사용 규약)
    - src/checkin/config.ts, src/checkin/deps.ts (03-01 산출물 — `LocationDeps`, `defaultLocationDeps`)
    - src/checkin/testing/fakeLocation.ts (03-01 산출물 — `__setPermission`, `__requestCallCount`)
    - .planning/phases/03-check-in-core-loop/03-RESEARCH.md §Architecture Patterns Pattern 3, §Common Pitfalls Pitfall 3
  </read_first>
  <behavior>
    - `fetchLocationPermission(fake)`가 SDK 응답에서 `status`/`granted`/`canAskAgain` 3필드만 뽑은 새 객체를 반환한다 (플랫폼 전용 필드 미통과)
    - `__setPermission('undetermined')` 상태에서 `requestLocationPermission(fake)`를 호출하면 `requestForegroundPermissionsAsync`가 정확히 1회 호출된다
    - `__setPermission('denied')` 상태에서 `requestLocationPermission(fake)`를 호출하면 `requestForegroundPermissionsAsync`가 **0회** 호출되고 현재 스냅샷이 그대로 반환된다 (iOS는 거부 후 재프롬프트가 불가능 — 무의미한 반복 호출 방지)
    - `shouldShowLocationDeniedBanner(null)`이 `false`를 반환한다 (초기 프레임 깜빡임 방지)
    - `shouldShowLocationDeniedBanner({ status: 'undetermined', ... })`이 `false`, `{ status: 'denied', ... }`이 `true`를 반환한다
    - `subscribeToForegroundActive(handler, fakeAppState)` 후 fakeAppState가 `'active'`를 emit하면 handler가 호출되고, `'background'`에서는 호출되지 않으며, 반환된 unsubscribe 호출 후에는 더 이상 호출되지 않는다
  </behavior>
  <action>
    `src/checkin/permissions.ts`를 만든다. `src/notifications/permissions.ts`의 구조를 그대로 복제하되 다음을 치환한다:

    - `NotificationDeps` → `LocationDeps`, `defaultNotificationDeps` → `defaultLocationDeps`
    - `getPermissionsAsync` → `getForegroundPermissionsAsync`, `requestPermissionsAsync` → `requestForegroundPermissionsAsync`
    - `requestForegroundPermissionsAsync`는 인자를 받지 않는다(알림의 `{ ios: { allowAlert... } }` 옵션 객체가 없다)
    - priming 세션 플래그(`markPrimingDismissed`/`shouldShowPriming`/`resetPrimingSession`)는 **복제하지 않는다** — 위치 권한에는 별도 priming 화면이 없고, 권한 요청은 "체크인" 첫 탭 시점의 맥락적 요청이다(REQ-onboarding-empty-state와 일치, 03-UI-SPEC.md Scope Boundary)

    export할 것: `PermissionSnapshot`(notifications와 동일 형태로 이 파일에 재선언), `fetchLocationPermission(deps = defaultLocationDeps)`, `requestLocationPermission(deps = defaultLocationDeps)`, `shouldShowLocationDeniedBanner(permission)`, `useLocationPermissionBanner()`.

    `subscribeToForegroundActive`는 **재구현하지 않고** `../notifications/permissions`에서 import해 재사용한다 — 동일한 `AppState` 래퍼를 두 벌 만들 이유가 없다. 재사용한다는 사실과 이유를 주석에 명시한다.

    `requestLocationPermission`은 notifications의 가드절 형태를 그대로 따른다: 먼저 `fetchLocationPermission`으로 현재 상태를 읽고 `status !== 'undetermined'`면 즉시 반환, `undetermined`일 때만 실제 요청. 이 가드가 없으면 이미 거부된 사용자에게 아무 효과 없는 호출을 반복하게 된다.

    `useLocationPermissionBanner()`는 notifications 훅과 동일하게 `isMounted` 가드 + `.catch(console.error)` 규약을 지키고, 마운트 시 1회 조회 + `subscribeToForegroundActive`로 포그라운드 복귀마다 재조회하며, `{ showBanner, openSettings: () => Linking.openSettings() }`를 반환한다.

    파일 헤더 주석에 다음 두 가지를 반드시 명시한다:
    (1) **권한 요청 책임 경계 확정** — `requestForegroundPermissionsAsync()` 호출은 Phase 3가 소유한다. Phase 4(REQ-onboarding-empty-state)는 이 위에 온보딩 문구/empty state만 얹으며 요청 호출을 다시 만들지 않는다(03-RESEARCH.md Open Questions #2 해소).
    (2) **iOS 단순화** — 위치 권한은 "Location Services 전역 꺼짐"과 "앱별 거부"를 구분하지 않기로 의도적으로 단순화했다(PROJECT.md Context, product-design.md). 따라서 배너 판정은 알림과 동일하게 `status === 'denied'` 하나만 본다.

    `src/checkin/permissions.test.ts`(`@jest-environment node`)를 만든다. `src/notifications/permissions.test.ts`의 `createFakeAppState` 인라인 더블 패턴을 그대로 복제하고(실제 `react-native`의 `AppState`를 import하지 않는다), `createFakeLocation()`으로 위 behavior 6개를 검증한다. `jest.mock()`을 쓰지 않는다 — 모든 의존성은 파라미터 주입이다.
  </action>
  <verify>
    <automated>npm test -- src/checkin/permissions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/checkin/permissions.test.ts` 6개 이상 테스트 green
    - `grep -c "getForegroundPermissionsAsync\|requestForegroundPermissionsAsync" src/checkin/permissions.ts` 가 2 이상
    - `grep -v '^//' src/checkin/permissions.ts | grep -c "expo-location"` 가 0 (deps.ts를 통해서만 접근)
    - `grep -c "subscribeToForegroundActive" src/checkin/permissions.ts` 가 1 이상이고 `grep -c "export function subscribeToForegroundActive" src/checkin/permissions.ts` 가 0 (재구현이 아니라 재사용)
    - `grep -c "markPrimingDismissed\|shouldShowPriming" src/checkin/permissions.ts` 가 0
    - `grep -c "Open Questions #2\|Phase 3가 소유" src/checkin/permissions.ts` 가 1 이상 (책임 경계 주석 존재)
    - `npx tsc --noEmit` 이 exit code 0
  </acceptance_criteria>
  <done>위치 권한 조회/요청/배너 판정/포그라운드 재확인이 알림 모듈과 동일한 계약으로 구현되고, undetermined 가드와 배너 판정이 자동 테스트로 검증된다.</done>
</task>

<task type="auto">
  <name>Task 2: LocationDeniedBanner.tsx + UI 계약 회귀 테스트</name>
  <files>src/components/LocationDeniedBanner.tsx, src/components/__tests__/locationUi.test.ts</files>
  <read_first>
    - src/components/NotificationDeniedBanner.tsx (전체 — 복제 대상. 헤더의 "배치는 항상 부모가 결정한다" 계약 주석, 네임스페이스 import 패턴, 불투명 배경 근거 주석, 스타일 값)
    - src/components/__tests__/notificationUi.test.ts (전체 — 정적 소스 분석 계약 테스트 패턴, `stripComments` 사용법)
    - src/theme/tokens.ts (`colors.surface`, `colors.textMuted`, `spacing.md`, `spacing.xs`, `typography.helperText`)
    - .planning/phases/03-check-in-core-loop/03-UI-SPEC.md §위치 권한 거부 배너 (확정 문구/스타일/접근성 계약)
    - src/checkin/permissions.ts (Task 1 산출물 — `useLocationPermissionBanner`)
  </read_first>
  <action>
    `src/components/LocationDeniedBanner.tsx`를 `NotificationDeniedBanner.tsx`의 구조를 그대로 복제해 만든다. 치환 지점은 3곳뿐이다:
    - import: `../notifications/permissions` → `../checkin/permissions` (네임스페이스 import `import * as permissions from ...` 형태 유지 — 정적 계약 테스트가 훅 식별자를 호출부 1곳에서만 세게 하기 위함)
    - 훅: `permissions.useNotificationPermissionBanner()` → `permissions.useLocationPermissionBanner()`
    - 문구: 본문 텍스트 `위치 권한이 꺼져있어요 · 설정에서 켜기`, `accessibilityLabel="위치 권한이 꺼져있어요. 설정에서 켜기"`

    스타일은 원본과 동일하게 유지한다: `backgroundColor: colors.surface`(불투명 — 지도 위 오버레이라도 opacity/rgba 금지, 4.5:1 대비 보장), `minHeight: 44`, `paddingHorizontal: spacing.md`, `paddingVertical: spacing.xs`, `justifyContent: 'center'`, 텍스트 `typography.helperText` + `color: colors.textMuted`. 아이콘 없음, 경고색 없음, `colors.accent` 미사용.

    헤더 주석에 원본의 "배치는 항상 부모가 결정한다(absolute positioning을 내부에 갖지 않는다)" 계약을 그대로 계승하고, Phase 3에서는 최소 지도 화면 상단에서 `NotificationDeniedBanner` **바로 아래**에 스택된다는 것, 그리고 Phase 4가 오늘 뷰를 만들 때 두 배너 모두 지도 상단으로 이관된다는 것을 명시한다(03-UI-SPEC.md 확정).

    `src/components/__tests__/locationUi.test.ts`(`@jest-environment node`)를 `notificationUi.test.ts` 패턴으로 만든다. `fs.readFileSync`로 `LocationDeniedBanner.tsx` 소스를 읽어 다음을 단언한다:
    - 확정 카피 `위치 권한이 꺼져있어요 · 설정에서 켜기`를 정확히 포함한다 (가운뎃점 `·` 문자 포함)
    - `useLocationPermissionBanner`를 참조한다
    - `colors.surface`와 `colors.textMuted`를 사용한다
    - `minHeight: 44`를 포함한다
    - `accessibilityRole="button"`과 `accessibilityLabel`을 포함한다
    - `stripComments` 적용 후 `expo-symbols` / `Image` / `Icon` / `colors.accent` / `opacity` / `rgba`가 등장하지 않는다
    - `stripComments` 적용 후 `position: 'absolute'`가 등장하지 않는다 (배치는 부모 결정 계약)
    - `colors.mapLand` / `colors.mapRoad` / `colors.mapWater`가 등장하지 않는다
  </action>
  <verify>
    <automated>npm test -- src/components</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/components/__tests__/locationUi.test.ts` 8개 이상 테스트 green
    - `npm test -- src/components/__tests__/notificationUi.test.ts` 여전히 green (기존 배너 회귀 없음)
    - `grep -c "위치 권한이 꺼져있어요 · 설정에서 켜기" src/components/LocationDeniedBanner.tsx` 가 1
    - `grep -v '^//' src/components/LocationDeniedBanner.tsx | grep -c "colors.accent"` 가 0
    - `grep -v '^//' src/components/LocationDeniedBanner.tsx | grep -c "position: 'absolute'"` 가 0
    - `npm test` 전체 스위트 green (`src/app/__tests__/foundation-wiring.test.ts`의 hex 컬러 회귀 가드 포함)
  </acceptance_criteria>
  <done>`LocationDeniedBanner`가 알림 배너와 동일한 톤/스타일/접근성 계약으로 존재하고, 확정 문구와 "배치는 부모 결정" 계약이 자동 회귀 테스트로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS 권한 시스템 → 앱 | 권한 상태 응답이 앱 UI 분기를 결정 |
| 앱 → OS 설정 앱 | `Linking.openSettings()` 딥링크 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-14 | Spoofing | 배너가 iOS 시스템 알림/권한 다이얼로그를 흉내 내 사용자를 오인시킴 | mitigate | 배너는 앱 자체 타이포(`typography.helperText`) + `colors.surface` 단색 표면 + 아이콘/경고색 없음으로 시스템 다이얼로그(중앙 카드 + 파란 텍스트 버튼)를 모사하지 않음. Phase 2 priming 화면의 T-02-16 완화와 동일 원칙. 정적 계약 테스트가 아이콘/accent 미사용을 게이트 |
| T-3-15 | Denial of Service (prompt fatigue) | 이미 거부된 사용자에게 `requestForegroundPermissionsAsync`를 반복 호출 | mitigate | `requestLocationPermission`의 `status !== 'undetermined'` 가드절 + `__requestCallCount() === 0` 자동 테스트 (Phase 2 T-02-10과 동일 완화) |
| T-3-16 | Information Disclosure | 배너가 지도 위 반투명 오버레이로 얹혀 텍스트 대비가 무너짐 | mitigate | `backgroundColor: colors.surface` 불투명 고정, `opacity`/`rgba` 사용 금지를 정적 테스트로 게이트 (Phase 2 Pitfall 6 계승) |
| T-3-SC | Tampering | npm/pip/cargo installs | mitigate | 이 plan은 신규 패키지를 설치하지 않음 — 03-RESEARCH.md §Package Legitimacy Audit이 phase 전체 5개 패키지를 이미 `[OK]`로 판정했고 설치는 03-01이 담당 |
</threat_model>

<verification>
- `npm test -- src/checkin src/components` green
- `npm test` 전체 스위트 green
- `npx tsc --noEmit` exit 0
</verification>

<success_criteria>
- 위치 권한이 `denied`일 때만 배너가 뜨고 `undetermined`/`granted`/`null`에서는 뜨지 않음
- 포그라운드 복귀 시 권한 재확인이 동작
- 이미 거부된 상태에서 OS 프롬프트를 재호출하지 않음
- 배너 문구/스타일/접근성 계약이 정적 테스트로 고정됨
- `requestForegroundPermissionsAsync()` 소유권이 Phase 3임이 소스 주석에 명시됨
</success_criteria>

<output>
Create `.planning/phases/03-check-in-core-loop/03-05-SUMMARY.md` when done
</output>
