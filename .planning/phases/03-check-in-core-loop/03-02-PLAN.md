---
phase: 03-check-in-core-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/checkin/fallbackLocation.ts
  - src/checkin/fallbackLocation.test.ts
autonomous: false
requirements: [REQ-location-denied-flow, REQ-checkin-confirm-pin]
user_setup: []

must_haves:
  truths:
    - "3단계 폴백 체인이 전부 실패해도 앱이 창업자의 실제 생활권 좌표로 지도를 띄운다 (0,0 같은 무의미한 좌표가 아님)"
    - "폴백 좌표값은 창업자가 직접 제공한 값이며 계획/구현 에이전트가 지어낸 값이 아니다 (D-07)"
  artifacts:
    - path: "src/checkin/fallbackLocation.ts"
      provides: "D-07 최종 폴백 좌표 상수 + 좌표 유효성 판정 헬퍼"
      exports: ["FALLBACK_COORDINATE", "isValidCoordinate"]
    - path: "src/checkin/fallbackLocation.test.ts"
      provides: "좌표가 placeholder(0,0)로 남지 않았음을 강제하는 게이트 테스트"
  key_links:
    - from: "src/checkin/fallbackLocation.ts"
      to: "창업자 제공 좌표값"
      via: "하드코딩 상수 + 출처 주석"
      pattern: "FALLBACK_COORDINATE"
---

<objective>
03-CONTEXT.md D-07이 정의한 "3단계 폴백 체인이 모두 실패했을 때 쓸 고정 기본 좌표"를 창업자에게 직접 확인받아 상수 모듈로 고정한다.

Purpose: 03-RESEARCH.md Open Questions #3이 명시적으로 "이 리서치는 값을 임의로 만들어내지 않는다 — 계획 단계에서 `checkpoint:human-verify` 태스크로 창업자에게 직접 확인받을 것"으로 남긴 유일한 미확정 값이다. 위치 권한도 없고 과거 체크인도 없는 최초 실행 시 지도가 어디를 보여줄지를 결정한다.
Output: `src/checkin/fallbackLocation.ts`(창업자 확정 좌표 + 유효성 헬퍼)와 placeholder 잔존을 막는 게이트 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-check-in-core-loop/03-CONTEXT.md
@.planning/phases/03-check-in-core-loop/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-07 최종 폴백 좌표값 확정 (창업자 입력 필수)</name>
  <read_first>
    - .planning/phases/03-check-in-core-loop/03-CONTEXT.md §"위치 완전 실패 시 최종 폴백 좌표" (D-07 원문)
    - .planning/phases/03-check-in-core-loop/03-RESEARCH.md §Open Questions #3
  </read_first>
  <decision>
    앱이 (1) 위치 권한이 없고 (2) 저장된 체크인이 하나도 없고 (3) 지도가 표시한 마지막 좌표도 없는 최초 실행 상황에서 지도를 띄울 "고정 기본 좌표"의 실제 위도/경도 값.
  </decision>
  <context>
    D-07은 "창업자 본인의 집/자주 가는 고정 장소 좌표를 하드코딩한다"로 이미 결정됐고, 남은 것은 숫자 값뿐이다. 리서치와 계획 단계 모두 이 값을 지어내지 않기로 명시했다(03-RESEARCH.md Open Questions #3). 1단계는 창업자 1인용 로컬 앱이라 이 좌표가 외부에 노출되지 않는다(SQLite/소스 모두 기기 로컬, PROJECT.md의 "기기 분실 시 전체 기록 소실 리스크 감수" 결정과 동일 범주).

    좌표를 얻는 방법: iOS 지도 앱에서 해당 위치를 길게 눌러 핀을 찍고 "위치 공유" → 좌표 확인, 또는 Google Maps 웹에서 우클릭 시 표시되는 `37.xxxxxx, 127.xxxxxx` 형식 값.
  </context>
  <options>
    <option id="provide-coordinate">
      <name>실제 좌표 제공 (권장, D-07 원문 그대로)</name>
      <pros>최초 실행 시 지도가 실제 생활권을 보여줘 "이 앱이 내 위치를 안다"는 첫인상이 유지된다</pros>
      <cons>소스 코드에 개인 생활권 좌표가 평문으로 남는다(1단계 로컬 전용 앱이므로 이미 수용된 리스크)</cons>
    </option>
    <option id="city-center">
      <name>거주 도시 중심 좌표로 대체 (프라이버시 절충)</name>
      <pros>집 주소 수준의 정밀도를 소스에 남기지 않는다</pros>
      <cons>D-07이 명시한 "실제 생활권" 의도에서 한 단계 멀어진다 — 다만 "0,0보다 낫다"는 핵심 의도는 유지</cons>
    </option>
  </options>
  <files>(리포지토리 파일 변경 없음 — 창업자 입력 수집 및 SUMMARY 기록)</files>
  <action>
    위 `<decision>`/`<options>`를 창업자에게 그대로 제시하고 실제 위도/경도 숫자 두 개를 수집한다. 값을 추정하거나 대신 만들어내지 않는다 — 03-RESEARCH.md Open Questions #3이 명시적으로 금지한 지점이다. 수집한 값은 Task 2에서 `src/checkin/fallbackLocation.ts`의 `FALLBACK_COORDINATE`로 하드코딩되며, 창업자가 선택한 옵션 id(`provide-coordinate` / `city-center`)를 SUMMARY에 기록한다. 창업자가 값을 제공하기 전까지 Task 2를 시작하지 않는다.
  </action>
  <verify>
    <human-check>창업자가 제공한 lat/lng 두 숫자가 대화에 명시적으로 등장하고, 0,0이 아니며 유효 범위 안이다</human-check>
  </verify>
  <acceptance_criteria>
    - 창업자가 `latitude, longitude` 형태의 구체적 숫자 두 개를 제공했다
    - 제공된 latitude가 -90~90, longitude가 -180~180 범위 안이다
    - 제공된 값이 `0, 0`이 아니다
    - 선택한 옵션 id(`provide-coordinate` 또는 `city-center`)가 SUMMARY에 기록된다
  </acceptance_criteria>
  <resume-signal>좌표를 `37.123456, 127.123456` 형식으로 입력하거나, 옵션 id(`provide-coordinate` / `city-center`)와 함께 값을 알려주세요. 이 값 없이는 Task 2를 진행할 수 없습니다.</resume-signal>
  <done>창업자가 제공한 실제 lat/lng 값과 선택한 옵션 id가 확보되어 Task 2가 하드코딩할 수 있는 상태다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fallbackLocation.ts 상수 모듈 + placeholder 잔존 게이트 테스트</name>
  <files>src/checkin/fallbackLocation.ts, src/checkin/fallbackLocation.test.ts</files>
  <read_first>
    - src/theme/tokens.ts (값의 출처를 주석으로 명시하고 "여기서 값을 발명하지 않는다"를 헤더에 두는 이 저장소의 상수 모듈 규약)
    - src/db/schema.ts (순수 상수/타입만 담아 Node 테스트 환경에서 그대로 로드되는 파일 스타일)
    - .planning/phases/03-check-in-core-loop/03-CONTEXT.md (D-07 원문 — 주석에 인용할 근거)
  </read_first>
  <behavior>
    - `FALLBACK_COORDINATE.lat`이 `0`이 아니고 `FALLBACK_COORDINATE.lng`도 `0`이 아니다 (placeholder 잔존 게이트)
    - `FALLBACK_COORDINATE.lat`이 -90 이상 90 이하, `FALLBACK_COORDINATE.lng`가 -180 이상 180 이하다
    - `isValidCoordinate({ lat: 91, lng: 0 })`이 `false`를 반환한다
    - `isValidCoordinate({ lat: Number.NaN, lng: 0 })`이 `false`를 반환한다
    - `isValidCoordinate(FALLBACK_COORDINATE)`이 `true`를 반환한다
  </behavior>
  <action>
    `src/checkin/fallbackLocation.ts`를 만든다. 네이티브 모듈을 import하지 않는 순수 상수/함수 모듈이다(db/schema.ts와 동일 성격).

    export할 것:
    - `FALLBACK_COORDINATE = { lat: <Task 1에서 창업자가 제공한 값>, lng: <창업자가 제공한 값> } as const`
    - `isValidCoordinate(c: { lat: number; lng: number }): boolean` — `Number.isFinite` 통과 + lat이 -90..90 + lng이 -180..180일 때만 true

    파일 헤더 주석에 반드시 다음을 명시한다: (1) 03-CONTEXT.md D-07이 출처라는 것, (2) 값이 창업자가 Task 1 체크포인트에서 직접 제공한 것이며 에이전트가 생성한 값이 아니라는 것, (3) 이 좌표는 3단계 폴백 체인(최근 checkin 좌표 → 지도 마지막 표시 좌표 → 이 상수)의 마지막 단계에서만 쓰인다는 것. tokens.ts 헤더와 동일하게 "여기서 값을 발명하지 않는다" 톤을 유지한다.

    `src/checkin/fallbackLocation.test.ts`(`@jest-environment node`)를 만들어 위 behavior 5개를 검증한다. 특히 `0,0` 금지 테스트는 "placeholder가 실제 값으로 교체되지 않은 채 머지되는 것"을 막는 게이트이므로 테스트 이름에 그 의도를 명시한다.

    좌표 숫자를 하드코딩 hex 컬러 회귀 가드(`src/app/__tests__/foundation-wiring.test.ts` Test 5)가 오탐하지 않는지 확인한다 — 좌표는 hex가 아니므로 문제없지만, 진행률 패턴 가드(`\b\d+\s*/\s*\d+\b`)에 걸리는 `37 / 127` 같은 표기를 주석에 쓰지 않는다.
  </action>
  <verify>
    <automated>npm test -- src/checkin/fallbackLocation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/checkin/fallbackLocation.test.ts` 5개 테스트 green
    - `grep -c "FALLBACK_COORDINATE" src/checkin/fallbackLocation.ts` 가 1 이상
    - `node -e "const s=require('fs').readFileSync('src/checkin/fallbackLocation.ts','utf8');if(/lat:\s*0\s*,/.test(s)||/lng:\s*0\s*[,}]/.test(s))throw new Error('placeholder coordinate still present')"` 가 에러 없이 종료
    - `grep -c "D-07" src/checkin/fallbackLocation.ts` 가 1 이상 (출처 주석 존재)
    - `grep -v '^//' src/checkin/fallbackLocation.ts | grep -c "expo-"` 가 0 (네이티브 의존 없음)
    - `npx tsc --noEmit` 이 exit code 0
  </acceptance_criteria>
  <done>창업자가 제공한 실제 좌표가 `FALLBACK_COORDINATE`로 고정되고, 0,0 placeholder 잔존을 막는 자동 게이트 테스트가 green이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 창업자 입력 → 소스 코드 | 개인 생활권 좌표가 리포지토리에 평문으로 들어감 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-09 | Information Disclosure | `src/checkin/fallbackLocation.ts`의 하드코딩 좌표 | accept | 1단계는 창업자 1인용 로컬 앱이며 리포지토리도 개인 소유. PROJECT.md가 이미 "기기 분실 시 전체 기록 소실 리스크 감수"를 수용된 리스크로 결정함 — 동일 범주. Task 1 체크포인트가 `city-center` 절충안을 옵션으로 제시해 창업자가 정밀도를 스스로 낮출 수 있게 함 |
| T-3-10 | Tampering | placeholder 좌표(0,0)가 실제 값으로 교체되지 않고 머지됨 | mitigate | `fallbackLocation.test.ts`가 lat/lng 0 및 범위 밖 값을 자동 실패시킴 (blocking acceptance criterion) |
| T-3-SC | Tampering | npm/pip/cargo installs | mitigate | 이 plan은 신규 패키지를 설치하지 않음 — 03-RESEARCH.md §Package Legitimacy Audit이 phase 전체 5개 패키지를 이미 `[OK]`로 판정했고 설치는 03-01이 담당 |
</threat_model>

<verification>
- `npm test -- src/checkin/fallbackLocation.test.ts` green
- `npx tsc --noEmit` exit 0
- SUMMARY에 창업자가 선택한 옵션 id와 좌표 제공 사실이 기록됨 (좌표 자체는 소스에만 존재)
</verification>

<success_criteria>
- `FALLBACK_COORDINATE`가 창업자 확정 실제 좌표를 담고 있고 0,0이 아니다
- `isValidCoordinate`가 범위 밖/NaN 좌표를 거부한다
- placeholder 잔존 시 테스트가 자동으로 실패한다
</success_criteria>

<output>
Create `.planning/phases/03-check-in-core-loop/03-02-SUMMARY.md` when done
</output>
