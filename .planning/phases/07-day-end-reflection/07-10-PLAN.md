---
phase: 07-day-end-reflection
plan: 10
type: execute
wave: 6
depends_on: ["07-03", "07-07", "07-09"]
files_modified:
  - .planning/phases/07-day-end-reflection/07-VALIDATION.md
autonomous: false
requirements: [REQ-reflection-base, REQ-reflection-autosave, REQ-reflection-save-failure-ui, REQ-reflection-copy-fix, REQ-reflection-notification, REQ-reflection-today-entry, REQ-past-reflection-edit]

must_haves:
  truths:
    - "전체 테스트 스위트와 타입체크가 green이다"
    - "Claude가 시뮬레이터에서 모달 프레젠테이션·자동저장·진입 행·과거 날짜 편집·설정 액션시트를 직접 확인했다"
    - "시뮬레이터로 재현 불가능한 항목(실기기 알림 자연 발화 + 콜드스타트 딥링크)만 창업자에게 넘어갔다"
    - "07-VALIDATION.md의 Per-Task Verification Map이 실제 결과로 채워졌다"
  artifacts:
    - path: ".planning/phases/07-day-end-reflection/07-VALIDATION.md"
      provides: "검증 결과가 반영된 validation 계약"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "07-VALIDATION.md Per-Task Verification Map"
      to: "실제 plan/task ID"
      via: "Status 컬럼 갱신"
      pattern: "✅"
---

<objective>
Phase 7 전체 게이트. 자동 검증(전체 스위트 + 타입체크 + 소스 계약)을 돌리고, 시뮬레이터로
검증 가능한 항목은 Claude가 **직접** 확인한 뒤, 시뮬레이터로 원천 재현 불가능한 항목만
창업자에게 넘긴다(프로젝트 CLAUDE.md "실기기 확인이 필요한 검증 단계" 규칙).

Purpose: 07-RESEARCH.md가 MEDIUM confidence로 남긴 세 가지 신규 영역(모달 라우팅,
알림 탭 딥링크, `BottomSheetFlatList`의 `ListHeaderComponent` 지원 — Assumptions Log A2)은
정적 테스트로는 증명되지 않는다. 이 플랜이 그 셋을 실제 실행으로 확인한다.
Output: 검증 결과가 반영된 07-VALIDATION.md + 창업자 승인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/07-day-end-reflection/07-CONTEXT.md
@.planning/phases/07-day-end-reflection/07-RESEARCH.md
@.planning/phases/07-day-end-reflection/07-UI-SPEC.md
@.planning/phases/07-day-end-reflection/07-VALIDATION.md

<interfaces>
전체 스위트: `npm test` (= `NODE_OPTIONS=--experimental-sqlite jest`)
타입체크: `npx tsc --noEmit`
린트: `npm run lint`

시뮬레이터 절차(프로젝트 CLAUDE.md): iOS Simulator 패널 attach → 가장 최근 DerivedData
빌드 launch(없으면 빌드) → 이미 실행 중인 Metro dev server(`--dev-client`)에 연결 →
스크린샷/탭으로 항목별 검증. **이 phase는 새 네이티브 모듈을 추가하지 않았으므로
(package.json 불변) EAS Dev Client 재빌드가 필요 없다** — 기존 빌드에 JS만 새로 붙는다.

시뮬레이터 알림 발화: `xcrun simctl push`로 즉시 알림을 밀어 넣을 수 있지만, 그것은
**앱이 스케줄한 반복 캘린더 트리거의 자연 발화가 아니다** — 딥링크 경로는 확인할 수
있어도 "실제 시각에 실제로 발화하는가"는 확인할 수 없다. 이 구분을 결과 보고에 명시한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 자동 게이트</name>
  <files>.planning/phases/07-day-end-reflection/07-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-day-end-reflection/07-VALIDATION.md (Per-Task Verification Map, Wave 0 Requirements, Validation Sign-Off 체크리스트)
    - .planning/phases/07-day-end-reflection/07-01-SUMMARY.md ~ 07-09-SUMMARY.md (각 플랜이 실제로 만든 파일/테스트 이름)
  </read_first>
  <action>
    `npm test`, `npx tsc --noEmit`, `npm run lint`를 순서대로 돌린다. 실패가 있으면
    수정하고 다시 돌린다(수정이 원인 조사를 필요로 한 버그였다면, 프로젝트 CLAUDE.md 규칙에
    따라 검증 완료 후 `/ce-compound`를 실행해 `docs/solutions/`에 학습을 남긴다).

    이어서 phase 전역 계약을 한 번에 확인한다:
    - `git diff --exit-code package.json package-lock.json` — 신규 패키지 0건(T-07-SC).
    - `git diff --exit-code src/notifications/` — 알림 모듈 불변(스케줄링은 이미 파라미터화되어 있어 수정 불필요).
    - 저장소 전체에서 `tabBarStyle`을 조작하는 파일이 여전히 `src/calendar/PastDateScreen.tsx` 하나뿐이다.
    - `src/` 전체에서 템플릿 보간을 포함한 SQL이 없다 — `migrations.ts`의 `PRAGMA user_version` 한 줄이 유일한 예외로 남아 있다.
    - `src/reflection/`, `src/today/ReflectionEntryRow.tsx`에 `colors.accent`(accentSoft 제외) 출현 0건.

    `.planning/phases/07-day-end-reflection/07-VALIDATION.md`의 Per-Task Verification Map을
    아래 매핑으로 채운다(Task ID / Plan / Wave 컬럼의 TBD를 실제 값으로 교체하고 Status를
    실행 결과로 갱신한다):
    - REQ-reflection-base / unit(node:sqlite) → `07-02 Task 1` / plan `07-02` / wave `1`
    - REQ-reflection-base / wiring(모달 라우트·정적 지도) → `07-08 Task 3` / plan `07-08` / wave `4`
    - REQ-reflection-autosave / unit(fake timers) → `07-02 Task 2` / plan `07-02` / wave `1`
    - REQ-reflection-autosave / wiring(AppState flush) → `07-04 Task 3` / plan `07-04` / wave `2`
    - REQ-reflection-save-failure-ui / wiring → `07-04 Task 3` / plan `07-04` / wave `2`
    - REQ-reflection-copy-fix / wiring(today) → `07-09 Task 3` / plan `07-09` / wave `5`
    - REQ-reflection-notification / unit(migrations + settingsRepo) → `07-01 Task 1·2` / plan `07-01` / wave `1`
    - REQ-reflection-notification / wiring(설정 4번째 행) → `07-03 Task 3` / plan `07-03` / wave `2`
    - REQ-reflection-today-entry / wiring → `07-06 Task 2` + `07-09 Task 3` / plan `07-06, 07-09` / wave `1, 5`
    - REQ-past-reflection-edit / wiring → `07-07 Task 2` / plan `07-07` / wave `3`
    Wave 0 Requirements 5개 체크박스를 실제 파일 존재 여부로 채우고, Validation Sign-Off
    체크리스트를 갱신하며 frontmatter의 `wave_0_complete`를 `true`로 바꾼다
    (`nyquist_compliant`는 Task 2 완료 후 갱신한다).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run lint && git diff --exit-code package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` 전체 스위트 green(스킵/실패 0건).
    - `npx tsc --noEmit` 이 0으로 종료.
    - `npm run lint` 가 0으로 종료.
    - `git diff --exit-code package.json package-lock.json` 이 0으로 종료.
    - `git diff --exit-code src/notifications/` 이 0으로 종료.
    - `grep -rl 'tabBarStyle' src | wc -l` 이 `1`이고 그 파일이 `src/calendar/PastDateScreen.tsx`다.
    - `grep -rn '\${' src/db/migrations.ts | grep -v '^\s*//' | wc -l` 이 `1`.
    - `grep -rn 'colors\.accent\b' src/reflection src/today/ReflectionEntryRow.tsx | wc -l` 이 `0`.
    - 07-VALIDATION.md에 `TBD` 문자열이 0건 남아 있다: `grep -c 'TBD' .planning/phases/07-day-end-reflection/07-VALIDATION.md` 가 `0`.
    - 07-VALIDATION.md frontmatter가 `wave_0_complete: true`다.
  </acceptance_criteria>
  <done>자동 검증 층(테스트/타입/린트/계약 grep)이 전부 green이고 validation 문서가 실제 결과로 채워졌다.</done>
</task>

<task type="auto">
  <name>Task 2: 시뮬레이터 자체 검증 (Claude가 직접 수행)</name>
  <files>.planning/phases/07-day-end-reflection/07-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (전체 — 각 항목의 시각/문구 기대값)
    - .planning/phases/07-day-end-reflection/07-RESEARCH.md (Assumptions Log A2 — `ListHeaderComponent` 지원 여부가 시뮬레이터 최우선 확인 대상인 이유, Pitfall 3 — 라우트 문자열은 타입체크만으로 신뢰하지 않는다)
    - .planning/phases/07-day-end-reflection/07-VALIDATION.md (Manual-Only Verifications 표)
  </read_first>
  <action>
    프로젝트 CLAUDE.md 절차대로 iOS Simulator에 attach하고 최근 DerivedData 빌드를 실행해
    Metro dev server에 연결한 뒤, 아래 항목을 **직접 탭하며** 확인한다. 각 항목마다
    스크린샷을 남긴다.

    A. **A2 가정 검증(최우선)** — 오늘 뷰 바텀시트에서 "오늘 돌아보기" 행이 리스트 최상단에
       실제로 렌더되는가. 체크인이 **0건인 상태**(예: 시뮬레이터 새 설치 또는 오늘 체크인
       삭제)에서도 보이는가. 빈 상태 문구가 리스트 안에 함께 보이는가.
       → 렌더되지 않으면 `BottomSheetFlatList`가 `ListHeaderComponent`를 실제로 무시한다는
       뜻이므로 즉시 중단하고, 07-RESEARCH.md A2가 제시한 대안(시트 바깥 별도 View로 렌더 후
       시각적으로 인접시키기)으로 gap closure 플랜을 요청한다.
    B. **모달 프레젠테이션** — 진입 행을 탭하면 화면이 아래→위로 슬라이드하며 **탭바까지
       덮는가**(탭바가 보이면 push로 열린 것이라 실패). ✕만 있고 날짜 타이틀이 없는가(D-03).
       아래로 스와이프해 닫히는가.
    C. **정적 지도** — 모달의 지도가 드래그/줌/회전에 반응하지 않는가. 그날 체크인 핀과
       (2건 이상일 때) 궤적선이 보이는가. 체크인 1건일 때 핀만 보이는가(graceful degradation).
    D. **자동저장** — 프롬프트에 입력하고 아무것도 누르지 않은 채 5초 이상 기다린 뒤 모달을
       닫고 다시 열어 값이 남아 있는가. 입력 직후 즉시 ✕로 닫고 다시 열어도 값이 남아 있는가
       (닫기 flush). 입력 후 홈으로 백그라운드 전환했다가 돌아와도 값이 남아 있는가
       (AppState flush). 저장 성공 시 아무 문구도 뜨지 않는가(조용한 저장).
    E. **진입 행 D-02** — 회고를 작성한 뒤 오늘 뷰로 돌아왔을 때 "오늘 돌아보기" 행의
       모습이 작성 전과 **완전히 동일한가**(체크마크/뱃지/색 변화 없음).
    F. **과거 날짜 인라인 편집** — 캘린더 탭 → 과거 날짜 → 시트를 올려 리스트 아래 프롬프트
       2칸이 보이는가. 입력이 되는가. 스크러버로 날짜를 옮기면 내용이 그 날짜 값으로
       바뀌는가. 옮기기 직전 입력한 값이 **이전 날짜**에 남아 있는가(다시 그 날짜로 돌아와
       확인). 이 화면에 "오늘의 흔적" 헤더와 40×40 썸네일이 **없는가**(D-04).
    G. **설정 4번째 행** — 설정 화면에 "회고 알림 시각" 행이 토글 바로 아래에 보이는가.
       탭하면 19~23시 + 취소 액션시트가 뜨는가. 고른 값이 행 오른쪽에 반영되는가. 앱을
       완전히 종료했다 재실행해도 그 값이 유지되는가(07-01 영속 경로 확인).
       하루 마무리 토글을 꺼도 이 행이 흐려지지 않고 여전히 동작하는가.
    H. **알림 탭 딥링크(부분 검증)** — `xcrun simctl push`로 `DAILY_REFLECTION_ID`
       identifier를 갖는 알림을 밀어 넣고 탭했을 때 회고 모달이 열리는가. 앱을 완전히 종료한
       상태에서도 동일한가(콜드스타트 경로). 모달이 두 장 쌓이지 않는가.
       → **주의:** 이것은 딥링크 경로 검증일 뿐 "실제 시각에 자연 발화하는가"의 검증이
       아니다. 그 부분은 Task 3으로 넘긴다.
    I. **DESIGN.md 정합성** — 프롬프트 답변 텍스트가 Newsreader 세리프로, 캡션 라벨이 시스템
       폰트로 렌더되는가. accent(테라코타/캘린더 강조색)가 회고 화면 어디에도 없는가.
       저장 실패 문구를 재현할 수 있으면(예: 개발 중 일시적 오류 주입) 빨간색/경고 아이콘
       없이 muted 톤인가 — 재현이 어려우면 코드 리뷰로 대체하고 그 사실을 기록한다.

    결과를 07-VALIDATION.md에 "시뮬레이터 검증 결과" 섹션으로 추가한다. 항목별로 확인 방법
    (직접 탭 / 코드 리뷰 대체)과 결과를 적고, frontmatter `nyquist_compliant`를 `true`로
    갱신한다. **어느 항목을 Claude가 시뮬레이터로 직접 확인했고 어느 항목이 사용자 확인
    대상인지 명확히 구분해 적는다**(프로젝트 CLAUDE.md 요구사항).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - A~I 9개 항목 각각에 대해 07-VALIDATION.md의 새 "시뮬레이터 검증 결과" 섹션에 결과 행이 존재한다(PASS / FAIL / 코드 리뷰 대체 중 하나).
    - A 항목이 PASS다(체크인 0건 상태에서 진입 행이 보인다) — 07-RESEARCH.md Assumptions Log A2가 해소됐음이 문서에 기록됐다.
    - B 항목 결과에 "탭바가 보이지 않았다"는 관찰이 명시적으로 기록됐다.
    - F 항목 결과에 날짜 전환 전후 값 귀속 확인 결과가 기록됐다(T-07-05 완화 실증).
    - H 항목 결과에 "자연 발화가 아닌 simctl push 기반 부분 검증"이라는 한계가 명시됐다.
    - 07-VALIDATION.md frontmatter가 `nyquist_compliant: true`다.
    - `npm test`와 `npx tsc --noEmit`이 여전히 green이다(시뮬레이터 검증 중 수정이 있었다면 재실행 결과 기준).
  </acceptance_criteria>
  <done>시뮬레이터로 재현 가능한 모든 항목이 Claude에 의해 직접 확인·기록됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 창업자 실기기 확인 — 회고 알림 자연 발화</name>
  <files>없음 — 이 태스크는 코드를 수정하지 않는다(창업자 관찰 결과는 07-10-SUMMARY.md에 기록한다)</files>
  <action>실행을 멈추고 아래 &lt;what-built&gt;/&lt;how-to-verify&gt; 내용을 창업자에게 그대로 제시한 뒤 응답을 기다린다. 시뮬레이터로 원천 재현이 불가능한 두 항목(반복 캘린더 트리거의 실제 시각 자연 발화, 잠금화면에서 콜드스타트 딥링크)만 이 게이트로 넘긴다 — 나머지는 Task 2에서 Claude가 이미 시뮬레이터로 직접 확인했다. 창업자가 어긋난 항목을 보고하면 임의로 고치지 말고 원인을 조사해 gap closure 플랜으로 제안한다.</action>
  <what-built>
    Phase 7 전체: 하루 마무리 회고 모달(정적 지도 + 체크인 리스트 + 프롬프트 2칸 + 조용한
    자동저장 + 저장 실패 시 작은 인라인 문구), 오늘 뷰 "오늘 돌아보기" 상시 진입 행,
    과거 날짜 뷰 인라인 회고 편집, 설정 화면 "회고 알림 시각" 행(19~23시), 회고 알림 탭 →
    모달 딥링크.

    Claude가 시뮬레이터로 이미 직접 확인한 항목(Task 2): 모달이 탭바까지 덮으며 열리고
    닫히는 것, 정적 지도 잠금, 자동저장 3경로(5초 디바운스 / 닫기 / 백그라운드 전환),
    "오늘 돌아보기" 행이 체크인 0건에서도 최상단에 보이는 것과 작성 후에도 모습이 변하지
    않는 것, 과거 날짜 인라인 편집과 날짜 전환 시 값 귀속, 설정 액션시트와 재시작 후 값
    유지, `simctl push`로 밀어 넣은 알림 탭 시 모달이 열리는 것.

    아래 두 항목만 시뮬레이터로 원천 재현이 불가능해 실기기 확인이 필요합니다.
  </what-built>
  <how-to-verify>
    1. 최신 JS 번들이 올라간 Dev Client를 iPhone에서 실행합니다(새 네이티브 모듈이 없어
       EAS 재빌드는 필요하지 않습니다 — 기존 빌드 그대로 사용하시면 됩니다).
    2. 설정 → "회고 알림 시각"에서 **지금으로부터 몇 분 뒤**에 해당하는 시각을 고릅니다
       (예: 지금이 20:52면 "21시"). 하루 마무리 알림 토글이 켜져 있는지 확인합니다.
    3. 앱을 완전히 종료하고(앱 스위처에서 위로 밀어 종료) 기기를 잠급니다.
    4. 그 시각이 되면 잠금화면에 "오늘 돌아보기" 알림이 뜨는지 확인합니다.
       → 알림에 회고 내용이나 체크인 개수 같은 사적인 정보가 **보이지 않아야** 합니다
         (제목만 보여야 합니다).
    5. 잠금화면에서 그 알림을 탭합니다. 앱이 콜드스타트로 실행되면서 **곧바로 회고
       화면이 열리는지** 확인합니다(오늘 뷰가 먼저 뜬 뒤 아무 일도 없으면 실패입니다).
       모달이 두 장 겹쳐 쌓이지 않는지도 확인합니다.
    6. 회고 화면에서 두 칸에 실제로 몇 문장 적어보시고, 화면 아래에서 위로 밀어 닫은 뒤
       다시 열어 내용이 남아 있는지 확인합니다. 저장 중이라는 안내나 저장 버튼이 없다는
       점(조용한 자동저장)이 실제 사용에서 불안하게 느껴지는지도 알려주세요.
    7. 다음 날 같은 시각에도 알림이 다시 오는지 확인해주시면 반복 트리거까지 검증됩니다
       (선택 — 시간이 걸리는 항목이라 나중에 알려주셔도 됩니다).
  </how-to-verify>
  <resume-signal>"approved"라고 알려주시거나, 어긋난 항목을 알려주세요(4~7번 각각에 대해 관찰하신 그대로 적어주시면 됩니다)</resume-signal>
  <verify>
    <automated>MISSING — 실기기 알림 자연 발화는 자동화 불가(07-VALIDATION.md Manual-Only Verifications 1행). 자동화 가능한 부분(딥링크 라우팅)은 07-08 Task 3 wiring 테스트와 07-10 Task 2 항목 H가 이미 커버한다.</automated>
  </verify>
  <done>창업자가 4~7번 항목을 관찰하고 "approved" 또는 구체적 불일치를 회신했으며, 그 결과가 07-10-SUMMARY.md에 Claude 시뮬레이터 확인 항목과 구분되어 기록됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 정적 테스트가 증명하는 것 ↔ 실제 런타임 동작 | 이 저장소는 화면을 정적 소스 분석으로만 검증한다 — 타입체크와 grep 게이트가 통과해도 런타임에 실패할 수 있는 영역(라우트 문자열, 서드파티 prop 지원 여부)이 남는다 |
| 시뮬레이터가 재현하는 것 ↔ 실기기 고유 동작 | OS 알림 스케줄러의 실제 타이밍/잠금화면 렌더링은 시뮬레이터로 재현되지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-25 | Denial of Service | 라우트 문자열의 런타임 실패 | mitigate | 타입체크 통과를 신뢰하지 않고 시뮬레이터에서 실제로 탭해 모달이 열리는 것을 확인한다(Task 2 항목 B/H) — STATE.md가 기록한 Phase 6 라우트 버그 3건의 재발 방지책 |
| T-07-27 | Denial of Service | `BottomSheetFlatList`의 `ListHeaderComponent` 미지원 가능성 | mitigate | Task 2 항목 A를 최우선 검증으로 지정하고, 실패 시 즉시 중단 + 07-RESEARCH.md A2의 대안으로 gap closure를 요청하는 경로를 미리 정의했다 |
| T-07-23 | Information Disclosure | 잠금화면 알림 노출 | mitigate | 실기기 체크포인트 4번 단계가 "제목만 보이고 회고 내용/개수가 보이지 않는다"를 사람 눈으로 확인한다(Phase 2가 확립한 원칙의 실증) |
| T-07-SC | Tampering | npm/pip/cargo 설치 | mitigate | Task 1이 `git diff --exit-code package.json package-lock.json`으로 phase 전체에 걸쳐 신규 패키지가 0건임을 최종 게이트한다 |
</threat_model>

<verification>
- `npm test` / `npx tsc --noEmit` / `npm run lint` 전부 green
- 시뮬레이터 항목 A~I 결과가 07-VALIDATION.md에 기록됨
- 창업자 실기기 확인 승인
</verification>

<success_criteria>
- Phase 7 Success Criteria 5개(ROADMAP.md)가 전부 관찰로 확인됐다.
- 시뮬레이터 검증 항목과 실기기 전용 항목이 문서에서 구분돼 있다.
- 07-VALIDATION.md에 TBD가 남아 있지 않고 `nyquist_compliant: true`/`wave_0_complete: true`다.
</success_criteria>

<output>
Create `.planning/phases/07-day-end-reflection/07-10-SUMMARY.md` when done — 어느 항목을
Claude가 시뮬레이터로 확인했고 어느 항목이 창업자 실기기 확인이었는지 구분해 기록한다.
커밋 메시지는 한글 Conventional Commits(`test(07-10): ...` 또는 `docs(07-10): ...`), AI 서명 트레일러 금지.
</output>
