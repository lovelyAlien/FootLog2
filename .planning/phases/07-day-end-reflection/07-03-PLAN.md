---
phase: 07-day-end-reflection
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/settings/content.ts
  - src/settings/SettingsScreen.tsx
  - src/app/__tests__/settings-wiring.test.ts
autonomous: true
requirements: [REQ-reflection-notification]

must_haves:
  truths:
    - "설정 화면 알림 섹션에 '회고 알림 시각' 행이 '하루 마무리 알림' 토글 바로 아래에 보인다"
    - "그 행을 탭하면 19/20/21/22/23시 + 취소 액션시트가 열리고, 고른 값이 행 오른쪽에 표시된다"
    - "선택한 시각이 네이티브 알림 재등록 성공 후에만 SQLite에 커밋된다"
    - "하루 마무리 알림 토글이 꺼져 있어도 이 행은 흐려지지 않고 동일하게 동작한다"
  artifacts:
    - path: "src/settings/content.ts"
      provides: "REFLECTION_HOUR 액션시트 4종 상수 + rowReflectionHour 라벨"
      contains: "REFLECTION_HOUR_OPTIONS"
    - path: "src/settings/SettingsScreen.tsx"
      provides: "4번째 행 + handleReflectionHourPress"
      contains: "handleReflectionHourPress"
    - path: "src/app/__tests__/settings-wiring.test.ts"
      provides: "4행 계약/액션시트 상수/persist 단일 쓰기경로 회귀 가드"
  key_links:
    - from: "src/settings/SettingsScreen.tsx"
      to: "persist()"
      via: "handleReflectionHourPress가 기존 persist를 그대로 호출"
      pattern: "persist\\(\\{ \\.\\.\\.settings, dailyReflectionHour"
    - from: "src/settings/SettingsScreen.tsx"
      to: "REFLECTION_HOUR_BY_ACTION_SHEET_INDEX"
      via: "액션시트 인덱스→시각 매핑 소비"
      pattern: "REFLECTION_HOUR_BY_ACTION_SHEET_INDEX"
---

<objective>
D-05의 UI 절반을 만든다: 설정 화면에 "회고 알림 시각" 행(4번째 행)을 추가하고, 기존
"알림 빈도" 행과 동일한 `ActionSheetIOS` 패턴으로 19~23시 중 하나를 고르게 한다.
새 네이티브 의존성(`@react-native-community/datetimepicker`)을 추가하지 않는다 — D-05가
EAS Dev Client 재빌드를 피하기 위해 명시적으로 액션시트 방식을 선택했다.

Purpose: 06-UI-SPEC.md가 확정했던 "정확히 3개 행" 예산을 4개로 명시적으로 확장하는
지점이다(07-UI-SPEC.md §Component Contracts 4). 스케줄링 로직(`scheduling.ts`)은
이미 `settings.dailyReflectionHour`로 파라미터화되어 있어 **수정하지 않는다**.
Output: 확장된 설정 문구 상수, 4행 설정 화면, 회귀 가드.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-day-end-reflection/07-CONTEXT.md
@.planning/phases/07-day-end-reflection/07-RESEARCH.md
@.planning/phases/07-day-end-reflection/07-PATTERNS.md
@.planning/phases/07-day-end-reflection/07-UI-SPEC.md
@.planning/phases/07-day-end-reflection/07-01-SUMMARY.md

<interfaces>
<!-- 실행자가 필요로 하는 기존 계약. 코드베이스 탐색 없이 이 값들을 그대로 쓴다. -->

src/settings/content.ts (기존 FREQUENCY 4종 세트 — 복제할 정확한 형태):
- `FREQUENCY_ACTION_SHEET_OPTIONS`(as const 배열, 마지막 원소가 `SETTINGS_COPY.actionSheetCancel`)
- `FREQUENCY_ACTION_SHEET_CANCEL_INDEX = 3`
- `FREQUENCY_BY_ACTION_SHEET_INDEX: readonly (NotificationFrequency | null)[]`
- `FREQUENCY_LABEL_BY_VALUE: Readonly<Record<NotificationFrequency, string>>`
- `SETTINGS_COPY.actionSheetCancel === '취소'`

src/settings/SettingsScreen.tsx (수정 대상, 기존 계약):
- `persist(next: NotificationSettings)` — 순서: `applyNotificationSettings(next, defaultNotificationDeps)` →
  `settingsRepo.upsertSettings(db, next, new Date().toISOString())` → `setSettings(next)` / `setSaveFailed(false)`.
  실패 시 `setSaveFailed(true)`만. **시그니처를 바꾸지 않는다.**
- `handleFrequencyPress` — `ActionSheetIOS.showActionSheetWithOptions({ options: [...], cancelButtonIndex }, cb)`
- 렌더 구조: `sectionNotifications` 헤더 → `<View style={styles.section}>` 안에
  빈도 `Pressable` 행 → `<View style={styles.divider} />` → 하루마무리 `Switch` 행.
- 스타일: `row`(minHeight 44), `rowTrailing`, `rowLabel`, `rowValue`, `divider`.
- 액션시트 옵션에 강조(destructive) 스타일을 절대 지정하지 않는다(iOS가 빨강으로 렌더 → DESIGN.md semantic 색상 금지 위반).

src/notifications/config.ts (수정하지 않음):
- `NotificationSettings.dailyReflectionHour: number` 필드가 이미 존재한다.

src/app/__tests__/settings-wiring.test.ts (확장 대상):
- `@jest-environment node` docblock, `fs.readFileSync` + `stripComments`,
  `content.ts`는 직접 import해 값 자체를 단언하고 `SettingsScreen.tsx`는 정적 소스로만 검증.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REFLECTION_HOUR 액션시트 상수 4종 추가</name>
  <files>src/settings/content.ts</files>
  <read_first>
    - src/settings/content.ts (전체 — `SETTINGS_COPY` 키 목록과 `FREQUENCY_*` 4종 세트, "여기서 값을 발명하지 않는다" 주석 규약)
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Copywriting Contract — 후보 목록과 라벨 원문)
    - .planning/phases/07-day-end-reflection/07-RESEARCH.md (Pattern 6 — 초안 상수)
  </read_first>
  <action>
    `SETTINGS_COPY`에 `rowReflectionHour: '회고 알림 시각'`를 추가한다(기존 키는 건드리지
    않는다). 그 아래에 `FREQUENCY_*` 4종 세트와 **구조적으로 동일한** 회고 시각 세트를
    추가한다:
    `REFLECTION_HOUR_OPTIONS = ['19시', '20시', '21시', '22시', '23시', SETTINGS_COPY.actionSheetCancel] as const`,
    `REFLECTION_HOUR_CANCEL_INDEX = 5`,
    `REFLECTION_HOUR_BY_ACTION_SHEET_INDEX: readonly (number | null)[] = [19, 20, 21, 22, 23, null]`,
    `REFLECTION_HOUR_LABEL_BY_VALUE: Readonly<Record<number, string>>`(19~23 → '19시'~'23시').
    라벨 문자열을 두 군데에 중복 하드코딩하지 않도록 `REFLECTION_HOUR_OPTIONS` 원소와
    `REFLECTION_HOUR_LABEL_BY_VALUE` 값이 같은 리터럴에서 파생되게 구성한다.
    21시가 후보에 반드시 포함되는 이유(`PHASE2_NOTIFICATION_SETTINGS.dailyReflectionHour`
    기본값이자 창업자 현재 설정 — 빠지면 기존 설정이 목록 밖 값이 되어 trailing 라벨이
    비게 된다)를 주석으로 남긴다. 이 파일은 런타임 import 없이 타입 전용 import만 유지한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 0으로 종료.
    - `grep -c 'REFLECTION_HOUR_OPTIONS\|REFLECTION_HOUR_CANCEL_INDEX\|REFLECTION_HOUR_BY_ACTION_SHEET_INDEX\|REFLECTION_HOUR_LABEL_BY_VALUE' src/settings/content.ts` 가 4 이상.
    - `grep -c "'21시'" src/settings/content.ts` 가 1 이상.
    - `grep -c '^import' src/settings/content.ts` 결과의 모든 import가 `import type`이다(`grep '^import' src/settings/content.ts | grep -vc 'import type'` 가 `0`).
    - `git diff src/settings/content.ts` 에 기존 `FREQUENCY_*` 상수와 기존 `SETTINGS_COPY` 키 값의 변경이 없다.
  </acceptance_criteria>
  <done>회고 시각 액션시트가 필요로 하는 옵션/취소인덱스/인덱스→값/값→라벨 4종이 content.ts 단일 출처에 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 설정 화면 4번째 행 + 액션시트 핸들러</name>
  <files>src/settings/SettingsScreen.tsx</files>
  <read_first>
    - src/settings/SettingsScreen.tsx (전체 — `persist`의 네이티브-우선/DB-나중 순서와 그 이유를 적은 주석, `handleFrequencyPress`, 빈도 행 JSX와 `divider` 관용구, 스타일 정의)
    - src/settings/content.ts (Task 1이 방금 추가한 `REFLECTION_HOUR_*`)
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Component Contracts 4 — 행 위치/트레일링/활성화 상태 규칙)
    - .planning/phases/07-day-end-reflection/07-PATTERNS.md (`src/settings/SettingsScreen.tsx` 절 — 복제할 행 JSX)
  </read_first>
  <action>
    `handleReflectionHourPress`를 `useCallback`으로 추가한다 —
    `ActionSheetIOS.showActionSheetWithOptions({ options: [...REFLECTION_HOUR_OPTIONS],
    cancelButtonIndex: REFLECTION_HOUR_CANCEL_INDEX }, (buttonIndex) => { ... })` 안에서
    `REFLECTION_HOUR_BY_ACTION_SHEET_INDEX[buttonIndex]`를 읽고, `null`/`undefined`면 즉시
    return, 아니면 `persist({ ...settings, dailyReflectionHour: nextHour })`를 호출한다.
    `handleFrequencyPress`가 쓰는 `if (!next) return;` 형태를 그대로 복사하면 안 된다 —
    시각 값은 숫자이고 `0`이 falsy라 유효값을 삼킬 수 있다(현재 후보 목록엔 0이 없지만
    목록이 확장되면 즉시 버그가 된다). `null`/`undefined` 명시 비교를 쓴다.
    `persist`의 시그니처/본문/호출 순서는 절대 바꾸지 않는다 — 이 화면의 유일한 쓰기
    경로를 하나로 유지하는 06-04 계약이다. 액션시트에 destructive 스타일 옵션을 지정하지 않는다.

    렌더 트리에서 기존 "하루 마무리 알림" `Switch` 행 **바로 아래**, 같은
    `<View style={styles.section}>` 블록 안에 `<View style={styles.divider} />`와 새 행을
    추가한다. 새 행은 빈도 행 JSX의 구조적 복제다: `Pressable`(`style={styles.row}`,
    `onPress={handleReflectionHourPress}`, `accessibilityRole="button"`,
    `accessibilityLabel={SETTINGS_COPY.rowReflectionHour}`) 안에
    `<Text style={styles.rowLabel}>{SETTINGS_COPY.rowReflectionHour}</Text>` +
    `<View style={styles.rowTrailing}>` 안에
    `<Text style={styles.rowValue}>{REFLECTION_HOUR_LABEL_BY_VALUE[settings.dailyReflectionHour]}</Text>`와
    `<SymbolView name="chevron.right" tintColor={colors.textMuted} />`.
    새 스타일을 만들지 않는다 — 기존 `row`/`rowTrailing`/`rowLabel`/`rowValue`/`divider`만
    재사용한다. 토글이 꺼져 있어도 disabled/dimmed 상태를 도입하지 않는다(07-UI-SPEC.md
    §Component Contracts 4 "활성화 상태" 확정 — 이 앱에 비활성 행 토큰이 없고, 미리 정해둔
    시각이 토글 재활성화 시 그대로 쓰이는 게 자연스럽다). 그 근거를 한 줄 주석으로 남긴다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 0으로 종료.
    - `grep -c 'handleReflectionHourPress' src/settings/SettingsScreen.tsx` 가 2 (정의 1 + JSX 사용 1).
    - `grep -c 'persist(' src/settings/SettingsScreen.tsx` 로 센 호출 지점이 4개다(정의 1 + handleRetry 1 + handleFrequencyPress 1 + handleReflectionHourPress 1) — 병렬 쓰기 경로가 생기지 않았다.
    - `grep -c 'StyleSheet.create' src/settings/SettingsScreen.tsx` 가 `1`이고, `git diff src/settings/SettingsScreen.tsx` 의 `styles` 객체에 새 키가 추가되지 않았다.
    - `grep -c 'settingsRepo.upsertSettings' src/settings/SettingsScreen.tsx` 가 `1` — DB 쓰기 지점이 여전히 `persist` 안 한 곳뿐이다.
    - `grep -c 'colors.accent' src/settings/SettingsScreen.tsx` 가 `0`.
    - `grep -c 'disabled' src/settings/SettingsScreen.tsx` 가 `0`.
  </acceptance_criteria>
  <done>설정 화면 알림 섹션이 빈도/토글/시각 3행이 되고, 시각 선택이 기존 persist 경로로만 저장된다.</done>
</task>

<task type="auto">
  <name>Task 3: 설정 4행 계약 회귀 가드 확장</name>
  <files>src/app/__tests__/settings-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/settings-wiring.test.ts (전체 — describe 블록 구성, `content.ts` 직접 import 단언 vs `SettingsScreen.tsx` 정적 소스 단언 구분, 기존 "정확히 3개 행" 단언이 어디 있는지)
    - src/settings/content.ts, src/settings/SettingsScreen.tsx (Task 1/2 결과)
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Component Contracts 4)
  </read_first>
  <action>
    기존 파일 말미에 새 `describe` 블록을 append한다(기존 단언은 수정하지 않되, 06-04가
    남긴 "정확히 3개 행" 계열 단언이 있다면 그 단언만 4개 행으로 갱신하고, 갱신 이유를
    07-UI-SPEC.md §Component Contracts 4의 예산 확장 근거와 함께 주석으로 남긴다).
    새 단언:
    (1) `REFLECTION_HOUR_OPTIONS`가 정확히 6개 원소이고 마지막이 `SETTINGS_COPY.actionSheetCancel`이다.
    (2) `REFLECTION_HOUR_CANCEL_INDEX`가 `REFLECTION_HOUR_OPTIONS.length - 1`과 같다.
    (3) `REFLECTION_HOUR_BY_ACTION_SHEET_INDEX`가 `[19, 20, 21, 22, 23, null]`이고 길이가 `REFLECTION_HOUR_OPTIONS`와 같다.
    (4) `REFLECTION_HOUR_BY_ACTION_SHEET_INDEX`의 non-null 값 전부가 `REFLECTION_HOUR_LABEL_BY_VALUE`에 키로 존재한다.
    (5) `21`이 후보에 포함된다(기존 기본값 보존 게이트).
    (6) `SettingsScreen.tsx` 코드(주석 제외)에 `ActionSheetIOS.showActionSheetWithOptions(`가 정확히 2회 등장한다.
    (7) `SettingsScreen.tsx` 코드에 `settingsRepo.upsertSettings(`가 정확히 1회 등장한다(단일 쓰기 경로).
    (8) `SettingsScreen.tsx` 코드에 액션시트 인덱스 숫자 리터럴이 하드코딩되어 있지 않다 — `cancelButtonIndex:` 뒤에 숫자 리터럴이 오지 않고 상수 식별자가 온다.
    (9) `SettingsScreen.tsx` 코드에 `SETTINGS_COPY.rowReflectionHour`가 등장하고, 한국어 리터럴 `'회고 알림 시각'`은 등장하지 않는다(문구 단일 출처).
    (10) `SettingsScreen.tsx` 코드에 `disabled`가 등장하지 않는다(토글 꺼짐 시 dimmed 금지).
    모든 grep 계열 단언은 `stripComments` 적용본을 대상으로 한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/settings-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/settings-wiring.test.ts` 전부 통과(기존 단언 포함).
    - 새 describe 블록에 최소 10개의 `it(` 가 추가됐다.
    - 테스트 파일이 `REFLECTION_HOUR_OPTIONS` 등 상수를 `../../settings/content`에서 직접 import해 값을 단언한다(정규식으로 문자열만 훑지 않는다).
  </acceptance_criteria>
  <done>설정 4행 계약과 액션시트 상수 정합성이 회귀 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS 액션시트 콜백(`buttonIndex`) → 앱 상태 | OS가 돌려주는 인덱스를 앱이 값으로 해석하는 경계 — 범위 밖 인덱스가 오면 `undefined`가 흘러들 수 있다 |
| 설정 화면 → 네이티브 알림 스케줄러 / SQLite | 두 개의 서로 다른 영속 저장소를 순서대로 갱신하는 경계(부분 실패 시 불일치 위험) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | `handleReflectionHourPress`의 인덱스→시각 변환 | mitigate | 인덱스 매핑을 화면이 아니라 `content.ts`가 소유하고, 콜백에서 `null`/`undefined` 명시 비교로 가드한다(`!next` truthy 검사 금지 — 숫자 0을 삼킨다). 매핑 배열 길이와 옵션 배열 길이 일치를 wiring 테스트가 게이트 |
| T-07-08 | Repudiation | 알림 스케줄과 DB 설정값 불일치 | mitigate | 기존 `persist()`의 네이티브-우선/DB-나중 순서를 그대로 재사용한다. 새 쓰기 경로를 만들지 않으므로 자가진단(`NotificationSelfHealGate`)이 커밋된 DB 값을 사용자 모르게 재적용하는 시나리오가 새로 생기지 않는다. wiring 테스트가 `upsertSettings` 호출 1회를 게이트 |
| T-07-09 | Information Disclosure | 잠금화면 알림 내용 | accept | 이 플랜은 알림 콘텐츠를 변경하지 않는다 — `NOTIFICATION_CONTENT.dailyReflection`은 Phase 2가 확정한 고정 제목만 쓰고 회고 본문을 노출하지 않는다. 발화 시각만 바뀐다 |
| T-07-SC | Tampering | npm/pip/cargo 설치 | mitigate | D-05가 `@react-native-community/datetimepicker` 설치를 명시적으로 회피했다. `git diff --exit-code package.json`이 변경 없음을 게이트한다 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/settings-wiring.test.ts` green
- `npx tsc --noEmit` 통과
- `git diff --exit-code package.json src/notifications/scheduling.ts` (둘 다 변경 없음 — 스케줄링은 이미 파라미터화되어 있어 수정 불필요)
</verification>

<success_criteria>
- 설정 알림 섹션에 "회고 알림 시각" 행이 존재하고 19~23시를 고를 수 있다.
- 선택값이 기존 `persist()` 경로로만 저장된다(병렬 쓰기 경로 0개).
- `src/notifications/scheduling.ts`가 수정되지 않았다.
</success_criteria>

<output>
Create `.planning/phases/07-day-end-reflection/07-03-SUMMARY.md` when done.
커밋 메시지는 한글 Conventional Commits(`feat(07-03): ...`), AI 서명 트레일러 금지.
</output>
