---
phase: 07-day-end-reflection
plan: 07
type: execute
wave: 3
depends_on: ["07-04", "07-06"]
files_modified:
  - src/calendar/PastDateScreen.tsx
  - src/app/__tests__/calendar-wiring.test.ts
autonomous: true
requirements: [REQ-past-reflection-edit]

must_haves:
  truths:
    - "과거 날짜 뷰의 체크인 리스트 아래에 그날의 회고 프롬프트 2칸이 인라인으로 보이고 편집할 수 있다"
    - "스크러버로 날짜를 바꾸면 프롬프트 내용이 그 날짜의 회고로 다시 로드된다"
    - "날짜를 바꾸기 직전의 미저장 답변은 이전 날짜 레코드에 저장된다"
    - "이 화면은 회고 모달과 동일한 프롬프트 UI/저장 로직을 쓰되, '오늘의 흔적' 헤더와 40×40 썸네일은 갖지 않는다"
  artifacts:
    - path: "src/calendar/PastDateScreen.tsx"
      provides: "인라인 회고 편집 섹션(시트 푸터)"
      contains: "ReflectionPrompts"
  key_links:
    - from: "src/calendar/PastDateScreen.tsx"
      to: "src/reflection/useReflectionDraft.ts"
      via: "activeDateKey를 인자로 전달"
      pattern: "useReflectionDraft\\(db, activeDateKey\\)"
    - from: "src/calendar/PastDateScreen.tsx"
      to: "src/today/TodayBottomSheet.tsx"
      via: "ListFooterComponent 슬롯"
      pattern: "ListFooterComponent"
---

<objective>
Phase 6이 만든 과거 날짜 읽기전용 화면에 **편집 가능한** 회고 프롬프트 2칸을 인라인으로
추가한다(D-04 — 별도 모달이 아니라 같은 화면 안, 오늘의 전체화면 모달과 다른 프레젠테이션).
원본 회고 문서의 "과거 회고는 읽기전용" 결정을 뒤집는 REQ-past-reflection-edit의 실체다.

Purpose: 자동저장/디바운스/실패 UI를 재구현하지 않고 07-04의 공유 훅·컴포넌트를 그대로
소비해, 오늘 모달과 과거 날짜 뷰가 시각·동작 양쪽에서 드리프트하지 않게 한다.
Output: 확장된 과거 날짜 화면 + calendar-wiring 회귀 가드.

**이 화면의 기존 요소는 건드리지 않는다**: 탭바 숨김(06-UI-SPEC 확정), 리스트 썸네일 없음
(`calendar-date-scrubber.md` 기존 결정), 스크러버 동작, 스와이프 삭제, undo 스낵바.
"오늘의 흔적" 섹션 헤더도 이 화면에는 넣지 않는다(D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/07-day-end-reflection/07-CONTEXT.md
@.planning/phases/07-day-end-reflection/07-RESEARCH.md
@.planning/phases/07-day-end-reflection/07-PATTERNS.md
@.planning/phases/07-day-end-reflection/07-UI-SPEC.md
@.planning/phases/07-day-end-reflection/07-04-SUMMARY.md
@.planning/phases/07-day-end-reflection/07-06-SUMMARY.md

<interfaces>
<!-- 선행 플랜 산출물 + 기존 화면 계약. 탐색 없이 그대로 쓴다. -->

src/reflection/useReflectionDraft.ts (07-04):
- `useReflectionDraft(db: MigratableDb, dateKey: string): ReflectionDraftBinding`
- `dateKey`가 바뀌면 이전 날짜 draft를 flush한 뒤 새 날짜 회고를 로드한다(내부에서 처리됨 —
  이 화면이 별도 flush 배선을 추가할 필요가 없다). 늦게 도착한 로드 응답이 최신 날짜를
  덮어쓰지 못하게 하는 가드도 훅 안에 있다.
- 반환: `{ newPlaceAnswer, freeReflection, onChangeNewPlaceAnswer, onChangeFreeReflection, saveFailed, onRetry, flush }`

src/reflection/ReflectionPrompts.tsx (07-04):
- `ReflectionPrompts({ newPlaceAnswer, freeReflection, onChangeNewPlaceAnswer,
  onChangeFreeReflection, saveFailed, onRetry })` — 캡션 라벨 2개 + 멀티라인 입력칸 2개
  (minHeight 44/120) + 조건부 저장 실패 문구 1세트 전부 포함.

src/today/TodayBottomSheet.tsx (07-06 확장):
- 선택적 prop `ListFooterComponent?: ReactElement | null` (리스트 최하단 슬롯),
  `ListHeaderComponent?: ReactElement | null`
- 체크인 0건에서도 리스트가 마운트되며 빈 상태는 `ListEmptyComponent`로 렌더된다 —
  즉 이 화면은 체크인이 0건인 과거 날짜에서도 회고 프롬프트를 보여줄 수 있다.

src/calendar/PastDateScreen.tsx (수정 대상, 현재):
- `activeDateKey` state(스크러버 드래그로 갱신) + `activeDateKeyRef` 경합 가드 + `isMountedRef`
- `sheetRef: useRef<BottomSheet>(null)`, `handleScrubStart`가 `sheetRef.current?.snapToIndex(0)`으로 시트를 접는다
- `<TodayBottomSheet sheetRef checkins containerHeight animatedPosition onRowPress onDeleteRequest emptyText={CALENDAR_COPY.pastDateEmptyState} />`
- 탭바 숨김은 `useFocusEffect` + `navigation.getParent()?.setOptions({ tabBarStyle })` — **건드리지 않는다**
- 스냅 인덱스: 0 = CLOSED(핸들+행 1개), 1 = OPEN

src/theme/tokens.ts: `spacing.lg`(24), `spacing.md`(16)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 과거 날짜 뷰에 인라인 회고 프롬프트 배선</name>
  <files>src/calendar/PastDateScreen.tsx</files>
  <read_first>
    - src/calendar/PastDateScreen.tsx (전체 — `activeDateKey` 흐름, `reloadCheckins` 경합 가드, `sheetRef`/`handleScrubStart`, `TodayBottomSheet` 호출부, 탭바 `useFocusEffect`)
    - src/reflection/useReflectionDraft.ts, src/reflection/ReflectionPrompts.tsx (07-04 산출물)
    - src/today/TodayBottomSheet.tsx (07-06 산출물 — 새 슬롯 prop의 정확한 타입)
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Component Contracts 3 — 배치/차이점/탭바 규칙)
    - .planning/phases/07-day-end-reflection/07-RESEARCH.md (Pattern 5)
  </read_first>
  <action>
    `const reflection = useReflectionDraft(db, activeDateKey);`를 호출한다 — 라우트
    파라미터 `dateKey`가 아니라 **스크러버로 갱신되는 `activeDateKey`**를 넘긴다(스크럽 중
    보고 있는 날짜의 회고를 편집해야 한다. `handleRowPress`가 이미 같은 이유로
    `activeDateKey`를 쓴다). 날짜 전환 시의 flush/재로드는 훅이 이미 처리하므로 이 화면에
    별도의 flush `useEffect`나 AppState 리스너를 추가하지 않는다 — 추가하면 같은 draft가
    두 경로로 저장된다. 이 판단을 주석으로 남긴다.

    시트 푸터 요소를 `useMemo`로 만들어 `TodayBottomSheet`의 `ListFooterComponent`에
    전달한다. 푸터는 `<View style={styles.reflectionSection}>` 안에 `<ReflectionPrompts>`
    하나만 담는다(패딩: 좌우 `spacing.md`, 상단 `spacing.lg`, 하단 `spacing.lg`).
    **인라인 화살표 컴포넌트(`ListFooterComponent={() => <.../>}`)를 쓰지 않는다** — 매
    렌더마다 새 컴포넌트 타입이 만들어져 `TextInput`이 언마운트/재마운트되고 타이핑 중
    키보드 포커스가 끊긴다. 반드시 **엘리먼트**를 전달한다. 이 이유를 주석으로 남긴다.
    `useMemo` 의존성에는 `reflection`이 노출하는 값/핸들러를 정확히 넣는다.

    "오늘의 흔적" 섹션 헤더와 40×40 썸네일을 이 화면에 넣지 않는다(D-04 — 과거 날짜 뷰
    리스트엔 썸네일 없음이라는 기존 결정 유지). `REFLECTION_COPY.sectionTitle`을 import하지
    않는다.

    키보드가 프롬프트를 가리지 않도록, 푸터 래퍼 `View`에 `onTouchStart` 핸들러를 달아
    `sheetRef.current?.snapToIndex(1)`(OPEN)을 호출한다 — 스크러버가 이미 쓰는 동일한
    imperative `sheetRef` 관용구의 반대 방향이다(스크러버는 접고, 프롬프트 터치는 편다).
    `TodayBottomSheet`를 수정하지 않고 이 화면 안에서 해결한다.

    기존 코드(탭바 숨김, 스크러버, 지도, 스와이프 삭제, undo 스낵바, `reloadCheckins`)는
    한 줄도 수정하지 않는다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 0으로 종료.
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__` 기존 스위트 전부 green.
    - `grep -c 'useReflectionDraft(db, activeDateKey)' src/calendar/PastDateScreen.tsx` 가 정확히 `1`.
    - `grep -c 'dateKey)' src/calendar/PastDateScreen.tsx` 로 확인 시 `useReflectionDraft(db, dateKey)` 형태(라우트 파라미터 직접 사용)가 등장하지 않는다.
    - `grep -c 'ListFooterComponent={() =>' src/calendar/PastDateScreen.tsx` 가 `0` — 엘리먼트를 전달했다.
    - `grep -c 'AppState' src/calendar/PastDateScreen.tsx` 가 `0` — 중복 flush 경로가 없다.
    - `grep -c 'sectionTitle\|REFLECTION_THUMBNAIL' src/calendar/PastDateScreen.tsx` 가 `0`.
    - `grep -c 'TextInput' src/calendar/PastDateScreen.tsx` 가 `0` — 프롬프트를 재구현하지 않았다.
    - `git diff src/calendar/PastDateScreen.tsx` 에 `tabBarStyle` 블록, `handleScrubStart`, `reloadCheckins`, `commitPendingDelete` 의 변경이 없다.
  </acceptance_criteria>
  <done>과거 날짜 뷰에서 그날의 회고를 시트 하단에서 바로 편집할 수 있고, 스크러버로 날짜를 옮기면 내용이 따라 바뀐다.</done>
</task>

<task type="auto">
  <name>Task 2: 과거 날짜 회고 편집 계약 회귀 가드</name>
  <files>src/app/__tests__/calendar-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/calendar-wiring.test.ts (전체 — `readSrcSource` 헬퍼, describe 구성, `stripComments` 적용 관례)
    - src/calendar/PastDateScreen.tsx (Task 1 결과)
    - .planning/phases/07-day-end-reflection/07-VALIDATION.md (Per-Task Verification Map — REQ-past-reflection-edit 행)
  </read_first>
  <action>
    기존 파일 말미에 새 describe 블록을 append한다(기존 단언은 수정하지 않는다).
    `stripComments` 적용본에 대해:
    (1) `useReflectionDraft(db, activeDateKey)`가 정확히 1회 등장한다(라우트 파라미터가 아닌 활성 날짜 사용).
    (2) `ReflectionPrompts`가 등장하고 `TextInput`은 등장하지 않는다(공유 컴포넌트 재사용, 재구현 금지).
    (3) `ListFooterComponent`가 등장하고, `ListFooterComponent={() =>` 형태가 등장하지 않는다(엘리먼트 전달 게이트 — 인라인 컴포넌트는 타이핑 중 포커스를 잃게 만든다).
    (4) `AppState`가 등장하지 않는다(중복 flush 경로 금지).
    (5) `sectionTitle`이 등장하지 않는다(D-04 — 이 화면엔 "오늘의 흔적" 헤더 없음).
    (6) 40×40 썸네일 관련 식별자가 등장하지 않는다(D-04 — 과거 날짜 뷰 리스트엔 썸네일 없음).
    (7) `tabBarStyle`을 조작하는 블록이 여전히 정확히 1회 존재한다(이 파일이 저장소에서 탭바를 만지는 유일한 파일이라는 06-RESEARCH.md Pitfall 1 계약 유지).
    (8) SQL 키워드(`INSERT `/`SELECT `/`UPDATE `/`DELETE `)가 등장하지 않는다.
    (9) 한글 문자열 리터럴이 등장하지 않는다(문구 단일 출처 — 기존 계약이 이미 있으면 그 단언을 중복하지 말고 이 항목은 생략한다).
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/calendar-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/calendar-wiring.test.ts` 전부 통과(기존 단언 포함).
    - 새 describe 블록에 최소 8개의 `it(` 이 추가됐다.
    - `git diff src/app/__tests__/calendar-wiring.test.ts` 에서 기존 `it(` 블록 본문이 변경되지 않았다.
  </acceptance_criteria>
  <done>과거 날짜 회고 편집의 재사용 계약과 D-04 차이점(헤더/썸네일 없음)이 회귀 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 스크러버 드래그(빠른 날짜 전환) → 회고 로드/저장 | 사용자 입력과 비동기 로드/지연 저장이 교차하는 경계 — 잘못 다루면 다른 날짜에 답변이 기록된다 |
| 시트 푸터 슬롯 → TextInput 생명주기 | 렌더 방식에 따라 입력 컴포넌트가 재마운트되어 입력 중 데이터/포커스를 잃을 수 있는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | 날짜 전환 시 회고 귀속 | mitigate | `useReflectionDraft`에 `activeDateKey`를 넘기고, draft가 자신의 `dateKey`/`id`를 들고 다니므로 전환 직전 flush가 이전 날짜 레코드에 기록된다. 화면이 별도 flush 경로를 만들지 않는다(wiring 테스트가 `AppState` 부재를 게이트) |
| T-07-18 | Repudiation | 입력 중 재마운트로 인한 답변 소실 | mitigate | `ListFooterComponent`에 인라인 화살표 컴포넌트가 아니라 `useMemo` 엘리먼트를 전달한다. wiring 테스트가 `ListFooterComponent={() =>` 패턴 부재를 게이트 |
| T-07-19 | Elevation of Privilege | 읽기전용 화면 계약 확대 | accept | 이 화면은 원래 읽기전용이었으나 REQ-past-reflection-edit가 회고 편집만 명시적으로 허용한다. 체크인 자체의 편집 경로는 추가되지 않으며(상세화면 진입은 기존 경로 그대로), 회고 쓰기는 `reflectionRepo`의 파라미터 바인딩 경로만 통과한다 |
| T-07-20 | Denial of Service | 과거 날짜 화면의 기존 기능 회귀 | mitigate | 탭바 숨김/스크러버/스와이프 삭제/undo 코드를 수정하지 않으며, `git diff` 기반 acceptance criteria와 기존 `src/app/__tests__` 스위트 green이 이를 게이트 |
| T-07-SC | Tampering | npm/pip/cargo 설치 | mitigate | 신규 패키지 없음. `git diff --exit-code package.json`이 변경 없음을 게이트 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__` green
- `npx tsc --noEmit` 통과
- `git diff --exit-code package.json src/reflection/ src/today/TodayBottomSheet.tsx` — 이 플랜은 선행 플랜 산출물을 수정하지 않는다
</verification>

<success_criteria>
- 과거 날짜 뷰에서 회고를 편집할 수 있고, 날짜 전환이 데이터를 섞지 않는다.
- 프롬프트 UI/저장 로직이 회고 모달과 물리적으로 동일한 코드다(복제 0건).
- D-04의 차이점(헤더 없음/썸네일 없음)이 지켜졌다.
</success_criteria>

<output>
Create `.planning/phases/07-day-end-reflection/07-07-SUMMARY.md` when done.
커밋 메시지는 한글 Conventional Commits(`feat(07-07): ...`), AI 서명 트레일러 금지.
</output>
