---
phase: 07-day-end-reflection
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/reflection/content.ts
  - src/reflection/reflectionRepo.ts
  - src/reflection/reflectionRepo.test.ts
  - src/reflection/autosaveController.ts
  - src/reflection/autosaveController.test.ts
autonomous: true
requirements: [REQ-reflection-base, REQ-reflection-autosave, REQ-reflection-save-failure-ui, REQ-reflection-copy-fix]

must_haves:
  truths:
    - "회고 두 답변이 하나의 daily_reflections 레코드로 함께 저장되고, 같은 날짜로 다시 저장하면 새 행이 생기지 않고 갱신된다"
    - "저장이 실패하면 자동으로 한 번 재시도하고, 그래도 실패하면 ok:false를 돌려준다(예외를 던지지 않는다)"
    - "타이핑을 멈추고 5초가 지나면 저장 함수가 정확히 1회 호출된다"
    - "flush()는 타이머를 기다리지 않고 즉시 마지막 draft로 저장한다"
    - "회고 화면의 모든 한국어 문구가 REFLECTION_COPY 한 곳에서만 나온다"
  artifacts:
    - path: "src/reflection/content.ts"
      provides: "REFLECTION_COPY 문구 단일 출처"
      contains: "REFLECTION_COPY"
    - path: "src/reflection/reflectionRepo.ts"
      provides: "getReflectionByDate / upsertReflection"
      exports: ["getReflectionByDate", "upsertReflection"]
    - path: "src/reflection/autosaveController.ts"
      provides: "createAutosaveController + REFLECTION_AUTOSAVE_DEBOUNCE_MS"
      exports: ["createAutosaveController", "REFLECTION_AUTOSAVE_DEBOUNCE_MS"]
    - path: "src/reflection/reflectionRepo.test.ts"
      provides: "실 SQLite 엔진 CRUD/upsert/재시도 검증"
    - path: "src/reflection/autosaveController.test.ts"
      provides: "fake timers 디바운스/flush/dispose 검증"
  key_links:
    - from: "src/reflection/reflectionRepo.ts"
      to: "src/checkin/checkinRepo.ts"
      via: "runWithSingleRetry import"
      pattern: "runWithSingleRetry"
    - from: "src/reflection/reflectionRepo.ts"
      to: "daily_reflections 테이블"
      via: "파라미터 바인딩 SQL"
      pattern: "daily_reflections"
---

<objective>
회고 기능의 순수 로직 레이어를 만든다: 문구 단일 출처(`content.ts`), `daily_reflections`
CRUD(`reflectionRepo.ts`), 5초 디바운스 자동저장 컨트롤러(`autosaveController.ts`).
세 파일 모두 React/네이티브 모듈을 import하지 않는 순수 모듈이라 `@jest-environment node`
로 완전히 검증 가능하다 — 이후 모든 회고 화면(모달, 과거 날짜 뷰)이 이 레이어 위에 얹힌다.

Purpose: 07-VALIDATION.md의 Wave 0 gap 2개(`reflectionRepo.test.ts`,
`autosaveController.test.ts`)를 이 플랜이 닫는다. 화면을 만들기 전에 저장/디바운스
계약을 테스트로 못박아, 화면 플랜이 UI에만 집중할 수 있게 한다.
Output: `src/reflection/` 도메인 디렉토리 5개 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-day-end-reflection/07-CONTEXT.md
@.planning/phases/07-day-end-reflection/07-RESEARCH.md
@.planning/phases/07-day-end-reflection/07-PATTERNS.md
@.planning/phases/07-day-end-reflection/07-UI-SPEC.md

<interfaces>
<!-- 실행자가 필요로 하는 기존 계약. 코드베이스 탐색 없이 이 값들을 그대로 쓴다. -->

src/db/schema.ts:
- `export interface DailyReflectionRow { id: string; date: string; new_place_answer: string | null; free_reflection: string | null; created_at: string; updated_at: string; }`
- 테이블 `daily_reflections`는 Phase 1(마이그레이션 v1)에서 이미 생성됨. `date`에 UNIQUE 제약이 이미 있다.

src/db/migrations.ts:
- `export type MigratableDb = Pick<SQLiteDatabase, 'getFirstAsync' | 'execAsync' | 'runAsync' | 'getAllAsync'>;`

src/checkin/checkinRepo.ts:
- `export async function runWithSingleRetry<T>(attempt: () => Promise<T>): Promise<{ ok: true; value: T } | { ok: false }>`
  (헤더 주석이 도메인 비의존/범용 재사용 의도를 이미 명시)
- `commitCheckin`의 트랜잭션 셰이프: `BEGIN` → `runAsync` → `COMMIT`, catch에서 `ROLLBACK`
  시도 후 `console.error` 남기고 rethrow.

src/today/pendingDelete.ts (설계 원형, import 아님):
- `export const UNDO_WINDOW_MS = 4000;`
- `createPendingDeleteController({ delayMs?, onCommit, onChange })` — 단일 타이머 클로저,
  `dispose()`가 취소가 아니라 즉시 확정.

src/settings/content.ts (문구 재사용 출처):
- `SETTINGS_COPY.saveFailed === '저장하지 못했어요'`, `SETTINGS_COPY.retryCta === '다시 시도'`

테스트 하네스: `import { createTestDb } from '../db/testing/nodeSqliteAdapter';`
(`const { db, raw, close } = createTestDb();`) + `migrateDbIfNeeded(db)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REFLECTION_COPY 문구 단일 출처 + reflectionRepo CRUD</name>
  <files>src/reflection/content.ts, src/reflection/reflectionRepo.ts, src/reflection/reflectionRepo.test.ts</files>
  <read_first>
    - src/checkin/checkinRepo.ts (`runWithSingleRetry`, `commitCheckin` 트랜잭션 셰이프, `getLatestCheckinCoordinate`의 `row ?? null` 관용구, 헤더의 "SQL은 repo에만 / `?` 바인딩만" 계약)
    - src/checkin/checkinRepo.test.ts (`createTestDb` + `migrateDbIfNeeded` 하네스, 인라인 fake db로 실패/재시도 경로를 검증하는 패턴)
    - src/today/content.ts, src/settings/content.ts (`X_COPY = {...} as const` 관용구와 "문구를 발명하지 않고 승인 문서에서 전사한다" 주석 규약)
    - src/db/schema.ts (`DailyReflectionRow`, `CREATE_DAILY_REFLECTIONS_TABLE_SQL`)
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Copywriting Contract — 전사할 문구 원본)
    - .planning/phases/07-day-end-reflection/07-RESEARCH.md (Pattern 2, Don't Hand-Roll 표)
  </read_first>
  <behavior>
    - `getReflectionByDate(db, '2026-09-02')`가 행이 없으면 `null`을 반환한다.
    - `upsertReflection`으로 저장한 뒤 `getReflectionByDate`가 같은 `new_place_answer`/`free_reflection`을 돌려준다.
    - 같은 `date`로 `upsertReflection`을 두 번 호출하면 `daily_reflections`의 행 수가 1이고, 두 번째 값이 남으며, `id`와 `created_at`은 첫 호출 값이 보존되고 `updated_at`만 갱신된다.
    - 빈 문자열 답변은 컬럼에 `NULL`로 저장된다(두 필드 모두 빈 문자열이면 두 컬럼 모두 NULL).
    - `runAsync`가 항상 throw하는 fake db로 호출하면 `attempt`가 정확히 2회 실행되고 결과가 `{ ok: false }`이며 예외가 밖으로 새지 않는다.
    - INSERT가 실패한 뒤에도 트랜잭션이 열린 채 남지 않는다(ROLLBACK 시도 흔적이 fake db 호출 로그에 있다).
  </behavior>
  <action>
    `src/reflection/content.ts`를 만들고 `export const REFLECTION_COPY = {...} as const`로
    다음 키를 07-UI-SPEC.md §Copywriting Contract에서 **그대로 전사**한다(문구를 발명하지
    않는다): `promptNewPlace: '새로 가본 곳이 있었나요?'`,
    `promptFreeReflection: '오늘에 대해'`, `sectionTitle: '오늘의 흔적'`,
    `emptyState: '아직 기록이 없어요'`, `saveFailed: '저장하지 못했어요'`,
    `retryCta: '다시 시도'`, `closeLabel: '닫기'`, `todayEntryRow: '오늘 돌아보기'`.
    `emptyState`가 `TODAY_COPY.emptyState`("아직 기록이 없어요 · 체크인하면 지도가
    채워져요")와 **의도적으로 다르다**는 근거(이 화면은 체크인 유도 CTA가 목적이 아니다)를
    주석으로 남긴다. `saveFailed`/`retryCta`는 `SETTINGS_COPY`와 같은 문구를 재사용하되
    값을 여기 리터럴로 전사한다(런타임 import 없는 순수 상수 모듈 규약 유지 —
    `src/settings/content.ts`와 동일). 이 파일은 런타임 import를 두지 않는다.

    `src/reflection/reflectionRepo.ts`를 만들고 다음을 export한다:
    `export type ReflectionSaveParams = { id: string; date: string; newPlaceAnswer: string; freeReflection: string; now: string; }`,
    `export async function getReflectionByDate(db: MigratableDb, date: string): Promise<DailyReflectionRow | null>`,
    `export async function upsertReflection(db: MigratableDb, params: ReflectionSaveParams): Promise<{ ok: true } | { ok: false }>`.
    `runWithSingleRetry`는 `../checkin/checkinRepo`에서 **그대로 import해 재사용**한다 —
    재구현하거나 공용 위치로 옮기지 않는다(07-RESEARCH.md Open Question #1의 권고를 이
    플랜이 확정한다: 아직 소비처가 2곳이라 Rule of Three 기준으로 이동 시점이 아니며,
    `checkin/`으로의 파일 이동은 이 phase의 변경 표면을 불필요하게 넓힌다). 이 결정을
    파일 헤더 주석에 근거와 함께 남긴다.
    `upsertReflection`의 본문은 `commitCheckin`의 셰이프를 그대로 복제한다:
    `runWithSingleRetry` 안에서 `BEGIN` → `SELECT id, created_at FROM daily_reflections
    WHERE date = ?` → 존재하면 `UPDATE ... SET new_place_answer = ?, free_reflection = ?,
    updated_at = ? WHERE id = ?`, 없으면 `INSERT INTO daily_reflections (id, date,
    new_place_answer, free_reflection, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?)` →
    `COMMIT`, catch에서 `ROLLBACK` 시도(실패 시 `console.error`) 후 rethrow.
    SQLite `ON CONFLICT ... DO UPDATE` 문법은 쓰지 않는다 — 이 저장소가 한 번도 쓴 적이
    없어 select-then-branch가 리뷰 일관성과 검증된 형태 양쪽에서 낫다(07-RESEARCH.md
    Don't Hand-Roll 표). 빈 문자열은 `|| null`로 정규화한다. 모든 SQL은 `?` 플레이스홀더
    바인딩만 사용하고 템플릿 보간을 쓰지 않는다(T-07-04).

    `src/reflection/reflectionRepo.test.ts`를 `@jest-environment node` docblock으로 만들고
    위 behavior 6종을 검증한다. 성공 경로는 `createTestDb` + `migrateDbIfNeeded` 실엔진으로,
    실패/재시도 경로는 `checkinRepo.test.ts`가 쓰는 인라인 fake `MigratableDb`(호출 카운터 +
    지정 횟수까지 throw)로 검증한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/reflection/reflectionRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/reflection/reflectionRepo.test.ts` 전부 통과.
    - `grep -vn '^\s*//' src/reflection/reflectionRepo.ts | grep -c '\${'` 가 `0`.
    - `grep -c "from '../checkin/checkinRepo'" src/reflection/reflectionRepo.ts` 가 `1` — `runWithSingleRetry`를 재구현하지 않았다.
    - `grep -c 'ON CONFLICT' src/reflection/reflectionRepo.ts` 가 `0`.
    - `grep -c 'REFLECTION_COPY' src/reflection/content.ts` 가 1 이상이고, 파일에 `import` 문이 0건이다: `grep -c '^import' src/reflection/content.ts` 가 `0`.
    - `node -e "const c=require('fs').readFileSync('src/reflection/content.ts','utf8'); ['새로 가본 곳이 있었나요?','오늘에 대해','오늘의 흔적','아직 기록이 없어요','저장하지 못했어요','다시 시도','닫기','오늘 돌아보기'].forEach(s=>{if(!c.includes(s))throw new Error('missing: '+s)})"` 가 성공.
  </acceptance_criteria>
  <done>회고 레코드가 날짜 기준으로 정확히 1행 유지되며 upsert되고, 저장 실패는 1회 재시도 후 ok:false로 조용히 반환된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5초 디바운스 자동저장 컨트롤러</name>
  <files>src/reflection/autosaveController.ts, src/reflection/autosaveController.test.ts</files>
  <read_first>
    - src/today/pendingDelete.ts (전체 — 단일 타이머 클로저 구조, `dispose()`가 취소가 아니라 확정이라는 계약과 그 이유를 설명한 헤더 주석 스타일)
    - src/today/pendingDelete.test.ts (fake timers 사용 패턴 — `jest.useFakeTimers()` / `advanceTimersByTime` / `afterEach` 복구)
    - .planning/phases/07-day-end-reflection/07-RESEARCH.md (Pattern 4 — 목표 구현체, "디바운스 vs unmount flush 경합" 서술)
    - .planning/phases/07-day-end-reflection/07-PATTERNS.md (`src/reflection/autosaveController.ts` 절 — 의미 역전 설명)
  </read_first>
  <behavior>
    - `notify(draft)` 후 `REFLECTION_AUTOSAVE_DEBOUNCE_MS`(5000)만큼 시간이 지나면 `onSave`가 정확히 1회, 그 draft로 호출된다.
    - 4999ms 시점에는 `onSave`가 아직 호출되지 않았다.
    - `notify`를 연속 3회 호출한 뒤 5000ms가 지나면 `onSave`는 1회만, **마지막** draft로 호출된다(타이머 리셋).
    - `notify` 후 즉시 `flush()`를 부르면 타이머 만료를 기다리지 않고 `onSave`가 1회 호출되고, 이후 5000ms가 더 지나도 추가 호출이 없다.
    - `notify` 없이 `flush()`를 부르면 `onSave`가 호출되지 않는다(no-op).
    - `flush()` 후 아무 `notify` 없이 `flush()`를 또 부르면 `onSave`가 추가로 호출되지 않는다(같은 draft 중복 저장 방지).
    - `notify` 후 `dispose()`를 부르면 `onSave`가 호출되지 않고(취소), 이후 5000ms가 지나도 호출되지 않는다.
    - `debounceMs` 인자를 넘기면 그 값이 사용된다.
  </behavior>
  <action>
    `src/reflection/autosaveController.ts`를 만든다. React/RN/네이티브 모듈을 import하지
    않는 순수 TS 모듈이어야 한다(`@jest-environment node`에서 로드 가능 — `pendingDelete.ts`와
    동일 제약). export:
    `export const REFLECTION_AUTOSAVE_DEBOUNCE_MS = 5000;`,
    `export type ReflectionDraft = { dateKey: string; id: string; newPlaceAnswer: string; freeReflection: string; };`,
    `export type AutosaveController = { notify(draft: ReflectionDraft): void; flush(): void; dispose(): void; };`,
    `export function createAutosaveController(args: { onSave: (draft: ReflectionDraft) => void; debounceMs?: number }): AutosaveController`.

    **`ReflectionDraft`에 `dateKey`와 `id`를 포함시키는 것이 이 플랜의 핵심 설계 결정이다**
    (07-RESEARCH.md Pattern 4의 초안은 두 답변 필드만 담았다): 과거 날짜 뷰에서 스크러버로
    날짜를 바꾸면 아직 만료되지 않은 디바운스 타이머가 남아 있을 수 있고, 저장 시점에
    "현재 화면의 날짜"를 읽으면 **이전 날짜에 쓴 답변이 새 날짜 레코드에 기록되는 데이터
    오염**이 발생한다. draft가 자신의 날짜와 레코드 id를 함께 들고 다니면 이 경합이
    구조적으로 불가능해진다. 이 근거를 파일 헤더 주석에 명시한다.

    내부 구조는 `pendingDelete.ts`의 단일 타이머 클로저를 그대로 따르되 의미가 반대다:
    이 컨트롤러는 취소 가능한 지연 확정이 아니라 **취소 불가능한 지연 저장**이라 `undo()`가
    없고, 대신 `dispose()`는 **타이머만 정리하고 저장하지 않는다**(`pendingDelete.dispose()`가
    즉시 확정하는 것과 정반대). 화면 언마운트 직전의 강제 저장은 호출자가 `flush()`를
    명시적으로 부르는 책임이며(모달의 닫기 핸들러가 `flush()` 후 `router.back()`),
    `dispose()`는 그 뒤의 타이머 누수 정리용이다 — 이 두 메서드의 역할 분리와
    `pendingDelete.ts`와의 의미 역전을 헤더 주석에 반드시 적는다.
    `flush()`는 저장 후 보관 중인 draft를 비워 같은 값이 두 번 저장되지 않게 한다.

    `src/reflection/autosaveController.test.ts`를 `@jest-environment node` docblock으로 만들고
    위 behavior 8종을 `jest.useFakeTimers()`로 검증한다(`pendingDelete.test.ts`의
    `beforeEach`/`afterEach` 타이머 복구 관용구 그대로).
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/reflection/autosaveController.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/reflection/autosaveController.test.ts` 8개 케이스 전부 통과.
    - `npx tsc --noEmit` 이 0으로 종료.
    - `grep -c "from 'react" src/reflection/autosaveController.ts` 가 `0` — 순수 모듈이다.
    - `grep -c 'dateKey' src/reflection/autosaveController.ts` 가 1 이상 — `ReflectionDraft`가 날짜를 들고 다닌다.
    - `grep -c 'undo' src/reflection/autosaveController.ts` 가 `0`.
    - `grep -n 'REFLECTION_AUTOSAVE_DEBOUNCE_MS = 5000' src/reflection/autosaveController.ts` 가 1건 반환 — 리터럴 5000이 화면 코드로 새지 않는다.
  </acceptance_criteria>
  <done>디바운스/즉시 flush/중복 저장 방지/타이머 정리 4가지 계약이 fake timers 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 회고 자유 텍스트(사용자 입력) → reflectionRepo → SQLite | 이 phase에서 유일하게 길이/형식 제약이 없는 사용자 입력이 DB 쓰기 경계를 넘는 지점 |
| 디바운스 타이머 → 저장 콜백 | 시간 축을 사이에 둔 경계 — 콜백이 실행되는 시점의 화면 상태와 draft가 만들어진 시점의 상태가 다를 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | `reflectionRepo.upsertReflection` / `getReflectionByDate` | mitigate | 회고 자유 텍스트를 포함한 모든 값은 `db.runAsync(sql, ...params)` `?` 바인딩으로만 전달한다(ASVS V5). acceptance criteria가 이 파일의 `${` 출현 0을 게이트한다. `migrations.ts`의 `PRAGMA user_version` 보간 예외는 이 파일에 적용되지 않는다 |
| T-07-05 | Tampering | `autosaveController` draft ↔ 대상 날짜 | mitigate | `ReflectionDraft`가 `dateKey`와 `id`를 함께 보유해, 지연 저장이 만료될 때 "그 draft가 쓰여진 날짜"에만 기록된다. 스크러버로 날짜를 바꿔도 이전 날짜의 답변이 다른 날짜 레코드를 덮어쓸 수 없다 |
| T-07-06 | Denial of Service | 저장 실패 시 무한 재시도 | accept | `runWithSingleRetry`는 정확히 2회 시도 후 `ok:false`로 종료한다 — 백오프 루프가 없어 배터리/디스크를 소진하지 않는다. 재시도 재개는 사용자의 명시적 "다시 시도" 탭에서만 발생한다 |
| T-07-SC | Tampering | npm/pip/cargo 설치 | mitigate | 이 phase는 신규 패키지를 설치하지 않는다(07-RESEARCH.md Package Legitimacy Audit "해당 없음"). `git diff --exit-code package.json`이 변경 없음을 게이트한다 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/reflection` 전부 green
- `npx tsc --noEmit` 통과
- `git diff --exit-code package.json` (변경 없음)
</verification>

<success_criteria>
- `src/reflection/` 3개 소스 + 2개 테스트가 존재하고 전부 green.
- 07-VALIDATION.md Wave 0 항목 중 `reflectionRepo.test.ts` / `autosaveController.test.ts` 두 개가 닫혔다.
- 회고 문구가 `REFLECTION_COPY` 한 곳에만 존재한다.
</success_criteria>

<output>
Create `.planning/phases/07-day-end-reflection/07-02-SUMMARY.md` when done.
커밋은 RED/GREEN 사이클마다 나눈다(`test(07-02): ...` → `feat(07-02): ...`), 한글 Conventional Commits, AI 서명 트레일러 금지.
</output>
