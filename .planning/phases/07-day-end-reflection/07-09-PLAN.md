---
phase: 07-day-end-reflection
plan: 09
type: execute
wave: 5
depends_on: ["07-06", "07-08"]
files_modified:
  - src/today/ReflectionEntryRow.tsx
  - src/app/(tabs)/index/index.tsx
  - src/app/__tests__/today-wiring.test.ts
autonomous: true
requirements: [REQ-reflection-today-entry, REQ-reflection-copy-fix]

must_haves:
  truths:
    - "오늘 뷰 바텀시트 리스트 최상단에 '오늘 돌아보기' 행이 항상 보인다(체크인 0건에서도)"
    - "그 행을 탭하면 회고 모달이 열린다"
    - "그 행은 그날 회고를 이미 썼는지와 무관하게 항상 똑같이 보인다(체크마크/뱃지/색 변화 없음)"
    - "오늘 뷰 어디에도 체크인 개수나 진행률 수치가 표시되지 않는다"
  artifacts:
    - path: "src/today/ReflectionEntryRow.tsx"
      provides: "accentSoft 고정 배경 진입 행"
      exports: ["ReflectionEntryRow"]
    - path: "src/app/(tabs)/index/index.tsx"
      provides: "시트 헤더 슬롯 배선 + /reflection 진입"
      contains: "ReflectionEntryRow"
  key_links:
    - from: "src/app/(tabs)/index/index.tsx"
      to: "/reflection 라우트"
      via: "절대 경로 router.push"
      pattern: "router.push\\('/reflection'\\)"
    - from: "src/app/(tabs)/index/index.tsx"
      to: "src/today/TodayBottomSheet.tsx"
      via: "ListHeaderComponent 슬롯"
      pattern: "ListHeaderComponent"
---

<objective>
오늘 뷰 바텀시트 리스트 최상단에 "오늘 돌아보기" 진입 행을 상시 노출하고, 탭하면 회고
모달을 연다. 동시에 오늘 뷰에 체크인 개수/진행률 수치가 없다는 것을 회귀 테스트로 못박는다.

Purpose: REQ-reflection-today-entry(0건에서도 항상 보임) + REQ-reflection-copy-fix(개수
미표시). 07-06이 만든 `ListHeaderComponent` 슬롯을 채우는 마지막 소비처이며, 07-08이
등록한 `/reflection` 라우트가 존재해야 절대 경로 push가 타입/런타임 양쪽에서 유효하다.
Output: 진입 행 컴포넌트, 오늘 화면 배선, 회귀 가드.

**D-02가 이 플랜의 가장 미묘한 제약이다**: 그날 회고를 이미 작성했는지를 **조회하지도,
표시하지도 않는다**. 체크마크/뱃지/색상 변화 일체 없음 — PROJECT.md의 "진행률/완료 수치
UI 노출 금지" CRITICAL 원칙이 숫자뿐 아니라 "완료 여부"를 신호하는 모든 시각 장치에
동일하게 적용된다. `accentSoft` 배경은 "이 행은 종류가 다르다"는 **고정** 스타일이지
상태 신호가 아니다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/07-day-end-reflection/07-CONTEXT.md
@.planning/phases/07-day-end-reflection/07-RESEARCH.md
@.planning/phases/07-day-end-reflection/07-PATTERNS.md
@.planning/phases/07-day-end-reflection/07-UI-SPEC.md
@.planning/phases/07-day-end-reflection/07-06-SUMMARY.md
@.planning/phases/07-day-end-reflection/07-08-SUMMARY.md

<interfaces>
<!-- 선행 산출물 + 기존 계약. 탐색 없이 그대로 쓴다. -->

src/today/TodayBottomSheet.tsx (07-06 확장):
- 선택적 `ListHeaderComponent?: ReactElement | null` — 리스트 최상단 슬롯
- 체크인 0건에서도 리스트가 마운트되고 빈 상태는 `ListEmptyComponent`로 렌더된다
  → 헤더 행은 0건에서도 보인다(REQ-reflection-today-entry의 핵심 조건).

src/today/CheckinListRow.tsx:
- `export const LIST_ROW_MIN_HEIGHT = 44;`
- 탭 가능함을 암시하는 chevron/화살표를 두지 않는다는 05-UI-SPEC 확정 계약(이 행도 동일).

src/reflection/content.ts (07-02): `REFLECTION_COPY.todayEntryRow === '오늘 돌아보기'`
(주의: `NOTIFICATION_CONTENT.dailyReflection.title`도 같은 문자열이지만 별개 출처다 —
알림 문구는 Phase 2 소관이므로 이 플랜에서 참조/수정하지 않는다.)

src/theme/tokens.ts: `colors.accentSoft`(#D8DDC9), `colors.textPrimary`,
`typography.placeName`(16/500), `spacing.lg`(24), `spacing.sm`(12)

src/app/(tabs)/index/index.tsx (수정 대상, 현재):
- 1260줄 부근 `<TodayBottomSheet checkins={filteredTodayCheckins} containerHeight={containerHeight}
  animatedPosition={sheetPosition} onRowPress={handleRowPress} onDeleteRequest={handleDeleteRequest} />`
- 같은 파일 안 네비게이션 관용구: 같은 탭 nested Stack 안 이동은 상대 경로
  (`router.push('./settings')`, `router.push({ pathname: './[id]', params: { id } })`).
  **회고 모달은 탭 그룹 밖 루트 Stack 라우트이므로 절대 경로 `'/reflection'`을 써야 한다**
  (07-RESEARCH.md Pitfall 3 — 상대 경로를 그대로 복사하면 조용히 잘못된 경로로 풀린다).
- `today-wiring.test.ts`가 `getTodayCheckins(` 1회, `reloadTodayCheckins()` 6회 같은
  호출 횟수 단언을 이미 걸어두고 있다 — 이 플랜은 그 호출 수를 바꾸지 않는다.

src/app/__tests__/today-wiring.test.ts: 07-06이 `TodayBottomSheet` 슬롯 계약 describe를
이미 추가해뒀다. 이 플랜은 그 아래에 append한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: "오늘 돌아보기" 진입 행 컴포넌트</name>
  <files>src/today/ReflectionEntryRow.tsx</files>
  <read_first>
    - src/today/CheckinListRow.tsx (행 컴포넌트 계약 — `LIST_ROW_MIN_HEIGHT`, "네비게이션을 내부에 갖지 않고 콜백만 받는다", chevron 미사용 근거)
    - src/reflection/content.ts (`REFLECTION_COPY.todayEntryRow`)
    - src/theme/tokens.ts (`colors.accentSoft`, `typography.placeName`, `spacing`)
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Component Contracts 1 — 배치/시각/인터랙션, §Color accentSoft 절)
    - DESIGN.md (§Color — accent 승인 용도 2개가 캘린더 탭 전용이라는 점)
  </read_first>
  <action>
    `src/today/ReflectionEntryRow.tsx`에
    `export type ReflectionEntryRowProps = { onPress: () => void }` /
    `export function ReflectionEntryRow({ onPress }: ReflectionEntryRowProps)`를 만든다.
    `expo-router`를 이 파일에서 import하지 않는다 — 네비게이션은 부모가 콜백으로 넘긴다
    (`CheckinListRow.tsx`가 세운 재사용 컴포넌트 계약).

    렌더: `Pressable`(`onPress`, `accessibilityRole="button"`,
    `accessibilityLabel={REFLECTION_COPY.todayEntryRow}`) 안에 `Text` 하나.
    스타일 — `minHeight: LIST_ROW_MIN_HEIGHT`(`CheckinListRow`에서 import, 44를 다시
    선언하지 않는다), `backgroundColor: colors.accentSoft`(**고정**),
    `paddingHorizontal: spacing.lg`, `paddingVertical: spacing.sm`,
    `justifyContent: 'center'`. 텍스트는 `typography.placeName` + `colors.textPrimary`.
    chevron/화살표/뱃지/체크마크를 넣지 않는다.

    props에 완료 여부/회고 존재 여부를 나타내는 필드를 **두지 않는다**(D-02). 이 컴포넌트는
    회고 데이터를 알지 못한다 — 배경색이 상태 신호가 아니라 고정 스타일이라는 근거를 파일
    헤더 주석에 명시한다. `colors.accent`(accentSoft가 아닌)를 쓰지 않는다.
    한국어 문자열 리터럴을 직접 쓰지 않는다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 0으로 종료.
    - `grep -c 'colors.accentSoft' src/today/ReflectionEntryRow.tsx` 가 `1`.
    - `grep -c 'colors\.accent\b' src/today/ReflectionEntryRow.tsx` 가 `0`.
    - `grep -c 'LIST_ROW_MIN_HEIGHT' src/today/ReflectionEntryRow.tsx` 가 `2`(import 1 + 사용 1).
    - `grep -c 'expo-router\|SymbolView\|chevron' src/today/ReflectionEntryRow.tsx` 가 `0`.
    - `grep -vn '^\s*//' src/today/ReflectionEntryRow.tsx | grep -cP '[가-힣]'` 가 `0`.
    - `grep -Ec 'completed|hasReflection|isDone|done' src/today/ReflectionEntryRow.tsx` 가 `0` — 완료 상태 prop이 없다.
  </acceptance_criteria>
  <done>완료 여부를 알지 못하는, 항상 동일한 모습의 진입 행 컴포넌트가 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: 오늘 화면에 진입 행 배선</name>
  <files>src/app/(tabs)/index/index.tsx</files>
  <read_first>
    - src/app/(tabs)/index/index.tsx (`TodayBottomSheet` 호출부 1250~1270줄, `handleRowPress`/`router.push('./settings')` 네비게이션 관용구, `useCallback` 사용 패턴)
    - src/today/ReflectionEntryRow.tsx (Task 1 결과)
    - src/today/TodayBottomSheet.tsx (07-06 — `ListHeaderComponent` prop 타입)
    - .planning/phases/07-day-end-reflection/07-RESEARCH.md (Pitfall 3 — 절대/상대 경로)
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Component Contracts 1)
  </read_first>
  <action>
    `handleReflectionEntryPress`를 `useCallback`으로 추가해 `router.push('/reflection')`
    **절대 경로**를 호출한다. 같은 파일의 기존 `'./settings'` / `'./[id]'` 상대 경로 관용구를
    복사하지 않는다 — 회고 모달은 이 탭의 nested Stack 밖(루트 Stack)에 있어 상대 경로가
    잘못 풀린다. 이 근거를 주석으로 남긴다.

    `useMemo`로 `<ReflectionEntryRow onPress={handleReflectionEntryPress} />` **엘리먼트**를
    만들어 `TodayBottomSheet`의 `ListHeaderComponent`에 전달한다. 인라인 화살표 컴포넌트
    (`ListHeaderComponent={() => <.../>}`)를 쓰지 않는다 — 매 렌더마다 새 컴포넌트 타입이
    생겨 불필요한 재마운트를 유발한다.

    회고 존재 여부를 조회하지 않는다 — `getReflectionByDate`나 `reflectionRepo`를 이 파일에서
    import하지 않는다(D-02). 기존 `getTodayCheckins`/`reloadTodayCheckins` 호출 횟수를
    바꾸지 않는다(`today-wiring.test.ts`가 정확한 횟수를 이미 게이트하고 있다).
    바텀시트 마운트 게이트(D-04, `showActionCard` 분기), 플로팅 버튼 오프셋
    (`floatingButtonStyle`), 스와이프 삭제/undo 배선은 한 줄도 수정하지 않는다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 0으로 종료.
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__` 기존 스위트 전부 green(특히 `today-wiring.test.ts`의 호출 횟수 단언).
    - `grep -c "router.push('/reflection')" 'src/app/(tabs)/index/index.tsx'` 가 `1`.
    - `grep -c "'./reflection'" 'src/app/(tabs)/index/index.tsx'` 가 `0`.
    - `grep -c 'ListHeaderComponent={() =>' 'src/app/(tabs)/index/index.tsx'` 가 `0`.
    - `grep -c 'ReflectionEntryRow' 'src/app/(tabs)/index/index.tsx'` 가 `2`(import 1 + 엘리먼트 1).
    - `grep -c 'getReflectionByDate\|reflectionRepo' 'src/app/(tabs)/index/index.tsx'` 가 `0`.
    - `git diff 'src/app/(tabs)/index/index.tsx'` 에 `floatingButtonStyle`, `showActionCard` 분기, `handleDeleteRequest` 의 변경이 없다.
  </acceptance_criteria>
  <done>오늘 뷰 시트 최상단에 진입 행이 상시 노출되고 탭하면 회고 모달이 열린다.</done>
</task>

<task type="auto">
  <name>Task 3: 진입 행 + 개수 미표시 회귀 가드</name>
  <files>src/app/__tests__/today-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/today-wiring.test.ts (07-06이 추가한 시트 슬롯 describe 포함 전체 — 헬퍼와 단언 스타일)
    - src/today/ReflectionEntryRow.tsx, src/app/(tabs)/index/index.tsx (Task 1/2 결과)
    - .planning/phases/07-day-end-reflection/07-VALIDATION.md (REQ-reflection-today-entry / REQ-reflection-copy-fix 행)
  </read_first>
  <action>
    기존 파일 말미에 새 describe 블록을 append한다(기존 단언은 수정하지 않는다).
    `stripComments` 적용본에 대해:
    (1) `index.tsx`에 `router.push('/reflection')`이 정확히 1회, `'./reflection'`은 0회 등장한다(절대 경로 게이트 — STATE.md Phase 6 라우트 버그 재발 방지).
    (2) `index.tsx`에 `ListHeaderComponent`가 등장하고 `ListHeaderComponent={() =>` 형태는 등장하지 않는다.
    (3) `index.tsx`에 `getReflectionByDate`/`reflectionRepo`가 등장하지 않는다(D-02 — 완료 여부를 조회하지 않는다).
    (4) `ReflectionEntryRow.tsx`에 `colors.accentSoft`가 1회 등장하고 `colors.accent`(정확히 그 식별자)는 0회 등장한다(DESIGN.md accent 예산 게이트).
    (5) `ReflectionEntryRow.tsx`에 `completed`/`hasReflection`/`isDone` 같은 완료 상태 식별자가 등장하지 않는다(D-02).
    (6) `ReflectionEntryRow.tsx`에 `chevron`/`SymbolView`가 등장하지 않는다.
    (7) `ReflectionEntryRow.tsx`에 `expo-router` import가 없다(콜백만 받는 컴포넌트 계약).
    (8) `REFLECTION_COPY.todayEntryRow`가 정확히 `'오늘 돌아보기'`이며 숫자나 개수 접미사를 포함하지 않는다(정규식 `/\d/` 불일치).
    (9) **개수 미표시 게이트(REQ-reflection-copy-fix)**: `index.tsx`와 `TodayBottomSheet.tsx` 코드 어디에도 `checkins.length`를 화면에 렌더하는 패턴(`{...length}` JSX 보간)이 등장하지 않는다. `TODAY_COPY`의 어떤 값에도 숫자 자리표시자(`{}`/`%d`/`${`)가 없다.
    (10) `TODAY_COPY`에 개수/진행률을 뜻하는 새 키가 추가되지 않았다 — `TODAY_COPY` 키 집합이 기존 6개 그대로다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/today-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/today-wiring.test.ts` 전부 통과(기존 + 07-06 단언 포함).
    - 새 describe 블록에 최소 10개의 `it(` 이 추가됐다.
    - `git diff src/app/__tests__/today-wiring.test.ts` 에서 기존 `it(` 블록 본문이 변경되지 않았다.
  </acceptance_criteria>
  <done>진입 행의 D-02 계약과 "오늘 뷰에 개수 표시 없음" 계약이 회귀 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 오늘 뷰 리스트 렌더 → 사용자가 매일 보는 화면 | 제품 원칙(진행률/완료 수치 노출 금지)이 깨지면 즉시 매일 보이는 표면에 노출되는 경계 |
| 탭 nested Stack → 루트 Stack 라우트 | 경로 해석 방식이 다른 두 네비게이션 영역의 경계 — 상대/절대 경로 혼동이 조용한 실패를 만든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Information Disclosure | 완료 여부/개수의 UI 노출 | mitigate | 진입 행 컴포넌트가 회고 데이터를 아예 받지 않고(props에 상태 필드 없음), 오늘 화면이 `getReflectionByDate`를 import하지 않는다. wiring 테스트가 두 조건과 `TODAY_COPY` 키 집합 불변을 게이트한다 |
| T-07-25 | Denial of Service | 잘못된 라우트로 인한 조용한 진입 실패 | mitigate | 절대 경로 `'/reflection'`만 허용하고 상대 경로 부재를 테스트로 게이트한다. 타입체크만으로 신뢰하지 않고 07-10에서 시뮬레이터로 실제 탭 검증한다(STATE.md Phase 6 선례) |
| T-07-26 | Tampering | 리스트 데이터 오염 | mitigate | 진입 행을 `checkins` 배열에 합성하지 않고 `ListHeaderComponent` 슬롯으로 렌더한다 — 스와이프 삭제 핸들러가 체크인이 아닌 항목을 받는 경로가 없다(07-06이 이미 게이트) |
| T-07-SC | Tampering | npm/pip/cargo 설치 | mitigate | 신규 패키지 없음. `git diff --exit-code package.json`이 변경 없음을 게이트 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__` green
- `npx tsc --noEmit` 통과
- `git diff --exit-code package.json src/today/TodayBottomSheet.tsx src/today/content.ts`
</verification>

<success_criteria>
- 체크인 0건에서도 "오늘 돌아보기" 행이 시트 리스트 최상단에 보인다.
- 탭하면 `/reflection` 모달이 열린다.
- 완료 상태 신호와 개수 표시가 코드/문구/테스트 세 층에서 모두 차단됐다.
</success_criteria>

<output>
Create `.planning/phases/07-day-end-reflection/07-09-SUMMARY.md` when done.
커밋 메시지는 한글 Conventional Commits(`feat(07-09): ...`), AI 서명 트레일러 금지.
</output>
