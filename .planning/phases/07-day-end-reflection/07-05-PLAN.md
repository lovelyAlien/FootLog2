---
phase: 07-day-end-reflection
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - src/reflection/ReflectionModal.tsx
  - src/app/__tests__/reflection-wiring.test.ts
autonomous: true
requirements: [REQ-reflection-base, REQ-reflection-autosave, REQ-reflection-save-failure-ui, REQ-reflection-copy-fix]

must_haves:
  truths:
    - "회고 화면 최상단에 ✕ 닫기 버튼만 있고 날짜 타이틀이 없다"
    - "그날의 체크인 핀과 궤적선이 조작 불가능한 정적 지도로 보인다"
    - "'오늘의 흔적' 아래에 그날 체크인이 시각·메모·40×40 썸네일로 나열되고, 0건이면 '아직 기록이 없어요'가 보인다"
    - "그 아래 프롬프트 2칸에 답을 쓰면 조용히 자동저장되고, 실패하면 폼 하단에 문구 1세트가 나온다"
    - "✕를 누르거나 아래로 스와이프해 닫으면 미저장 답변이 저장된 뒤 화면이 닫힌다"
  artifacts:
    - path: "src/reflection/ReflectionModal.tsx"
      provides: "회고 화면 본체(라우트 파일 밖)"
      exports: ["ReflectionModal"]
      min_lines: 120
    - path: "src/app/__tests__/reflection-wiring.test.ts"
      provides: "ReflectionModal 배선 회귀 가드(확장)"
  key_links:
    - from: "src/reflection/ReflectionModal.tsx"
      to: "src/checkin/checkinRepo.ts getTodayCheckins"
      via: "그날 체크인 단일 공유 쿼리 재사용"
      pattern: "getTodayCheckins"
    - from: "src/reflection/ReflectionModal.tsx"
      to: "src/reflection/useReflectionDraft.ts"
      via: "자동저장 훅 소비"
      pattern: "useReflectionDraft"
    - from: "src/reflection/ReflectionModal.tsx"
      to: "src/reflection/ReflectionPrompts.tsx"
      via: "공유 프롬프트 컴포넌트 렌더"
      pattern: "ReflectionPrompts"
---

<objective>
하루 마무리 회고 화면 본체(`ReflectionModal.tsx`)를 만든다. 위→아래 단일 스크롤 컬럼으로
✕ 닫기 → 정적 지도(핀+궤적선) → "오늘의 흔적" 헤더 → 읽기전용 체크인 리스트(40×40 썸네일)
→ 프롬프트 2칸(07-04 공유 컴포넌트) → 조건부 저장 실패 문구를 렌더한다.

Purpose: REQ-reflection-base의 실체. 새 쿼리를 만들지 않고 오늘 뷰가 이미 쓰는
`getTodayCheckins` + `buildTrajectoryCoordinates`를 그대로 재사용하며, 자동저장은
07-04의 `useReflectionDraft`를 소비하기만 한다 — 이 화면은 데이터 로직을 소유하지 않는다.
Output: 화면 컴포넌트 1개, 확장된 wiring 회귀 가드.

**라우트 등록은 이 플랜의 범위가 아니다** — `src/app/reflection.tsx`와 `_layout.tsx`
`presentation: 'modal'` 등록, 알림 탭 딥링크는 07-07이 담당한다(`CheckinDetailScreen.tsx`가
세운 "화면 본체는 라우트 파일 밖" 계약과 동일한 분리).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/07-day-end-reflection/07-CONTEXT.md
@.planning/phases/07-day-end-reflection/07-RESEARCH.md
@.planning/phases/07-day-end-reflection/07-PATTERNS.md
@.planning/phases/07-day-end-reflection/07-UI-SPEC.md
@.planning/phases/07-day-end-reflection/07-04-SUMMARY.md

<interfaces>
<!-- 07-04가 만든 계약 + 기존 재사용 대상. 탐색 없이 그대로 쓴다. -->

src/reflection/useReflectionDraft.ts (07-04):
- `useReflectionDraft(db: MigratableDb, dateKey: string): ReflectionDraftBinding`
- `ReflectionDraftBinding = { newPlaceAnswer; freeReflection; onChangeNewPlaceAnswer;
  onChangeFreeReflection; saveFailed; onRetry; flush }`
- 언마운트/날짜 전환 시 자동 flush를 이미 수행한다(스와이프 닫기 경로가 여기에 커버된다).

src/reflection/ReflectionPrompts.tsx (07-04):
- `ReflectionPrompts({ newPlaceAnswer, freeReflection, onChangeNewPlaceAnswer,
  onChangeFreeReflection, saveFailed, onRetry })` — 프롬프트 2칸 + 실패 UI 전부 포함.

src/reflection/content.ts: `REFLECTION_COPY.sectionTitle`('오늘의 흔적'),
`.emptyState`('아직 기록이 없어요'), `.closeLabel`('닫기')

src/checkin/checkinRepo.ts:
- `getTodayCheckins(db: MigratableDb, localDateKey: string): Promise<CheckinRow[]>`
  — 이름만 "Today"일 뿐 임의 날짜 키를 받으며 `timestamp_utc` 오름차순 정렬을 이미 보장한다
  (헤더 주석이 Phase 6/7 재사용을 명시). **새 조회 함수를 만들지 않는다.**

src/today/trajectory.ts: `buildTrajectoryCoordinates(checkins: CheckinRow[]): TrajectoryCoordinate[]`
— 2건 미만이면 `[]`를 반환한다(핀만 렌더되는 graceful degradation).

src/checkin/localDate.ts: `resolveLocalDateKey(date)`, `formatLocalTime(timestampUtc)`
src/checkin/config.ts: `MAP_REGION_DELTA`
src/db/schema.ts: `CheckinRow`(`id` `timestamp_utc` `lat` `lng` `note` `photo_path` 등)

정적 지도 잠금 조합(`CheckinDetailScreen.tsx`가 쓰는 그대로):
`scrollEnabled={false} zoomEnabled={false} rotateEnabled={false} pitchEnabled={false} pointerEvents="none"`

궤적선 렌더(`PastDateScreen.tsx`가 쓰는 그대로):
`trajectoryCoordinates.length >= 2 && <Polyline coordinates={...} strokeColor={colors.pinSoft} strokeWidth={2} />`
핀: `Marker` + `anchor={{ x: 0.5, y: 1 }}` + `pinWrapper`/`pinDrop`(28×34 물방울, `colors.pinSoft`)

src/theme/tokens.ts: `spacing`(2xs 4 / xs 8 / sm 12 / md 16 / lg 24 / xl 32),
`radius.md`(8), `typography.timestamp`(모노스페이스, tabular-nums),
`typography.journalEntry`, `typography.helperText`, `colors.background` `colors.surface`
`colors.textPrimary` `colors.textMuted` `colors.pinSoft`

src/settings/SettingsScreen.tsx: `SMALL_ICON_HIT_SLOP = { top: 8, bottom: 8, left: 8, right: 8 }`
(작은 아이콘 버튼의 44pt 히트 영역 관용구)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 회고 화면 상단부 — 닫기 버튼 / 정적 지도 / 읽기전용 체크인 리스트</name>
  <files>src/reflection/ReflectionModal.tsx</files>
  <read_first>
    - src/checkin/CheckinDetailScreen.tsx (잠긴 정적 지도 블록, 커스텀 헤더 아이콘 버튼 + `SMALL_ICON_HIT_SLOP`, 화면 본체가 라우트 밖에 있는 파일 구조, 스크롤/키보드 처리)
    - src/calendar/PastDateScreen.tsx (`Polyline` + `Marker` 렌더 조합, `pinWrapper`/`pinDrop` 스타일, `getTodayCheckins` 재로드 `useCallback` 패턴, `isMountedRef` 가드)
    - src/today/CheckinListRow.tsx (재사용하지 **않을** 컴포넌트 — 시각/메모 렌더 방식과 `LIST_ROW_MIN_HEIGHT`만 참고)
    - src/today/trajectory.ts
    - src/reflection/content.ts
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Component Contracts 2 레이아웃 1~4번, §Spacing Exceptions 40×40/44×44)
    - .planning/phases/07-day-end-reflection/07-PATTERNS.md (`src/reflection/ReflectionModal.tsx` 절 — 복제할 코드 조각들)
  </read_first>
  <action>
    `src/reflection/ReflectionModal.tsx`를 만들고
    `export type ReflectionModalProps = { db: MigratableDb }` /
    `export function ReflectionModal({ db }: ReflectionModalProps)`를 정의한다. 라우트
    파일이 아니라 화면 본체다(`CheckinDetailScreen.tsx`와 동일 계약).

    날짜: `const dateKey = useMemo(() => resolveLocalDateKey(new Date()), [])` — 이 화면은
    항상 "오늘"이다(D-03). 알림 탭 시점의 날짜 판정도 이 경로를 그대로 쓴다(07-RESEARCH.md
    Pitfall 2 / Assumptions Log A1: 반복 캘린더 트리거는 콘텐츠가 등록 시점에 고정되어
    알림 페이로드로 날짜를 전달할 수 없으므로, 탭 시점의 로컬 날짜로 판정한다). 이 근거를
    주석으로 남긴다.

    데이터: `getTodayCheckins(db, dateKey)`를 `useCallback`/`useEffect`로 마운트 시 1회
    호출하고 `isMountedRef` 가드로 state에 반영한다. **새 조회 함수를 만들지 않는다.**
    `buildTrajectoryCoordinates(checkins)`로 궤적 좌표를 파생한다(정렬을 다시 하지 않는다 —
    쿼리가 이미 시간순 보장).

    레이아웃 — 화면 전체를 세로 스크롤 가능한 단일 컬럼으로 구성한다(`ScrollView`, 배경
    `colors.background`, 좌우 패딩 `spacing.md`). 상단은 안전 영역을 존중한다
    (`useSafeAreaInsets`).
    (1) 헤더: 좌상단에 `Pressable` + `SymbolView name="xmark"`
    (`tintColor={colors.textPrimary}`), `hitSlop`으로 44×44pt 히트 영역을 확보하고
    `accessibilityRole="button"`, `accessibilityLabel={REFLECTION_COPY.closeLabel}`.
    `onPress`는 Task 2가 채울 `handleClose`에 연결한다(이 태스크에서는 `router.back()`만
    호출하는 임시 구현이 아니라, Task 2에서 flush를 추가할 자리임을 주석으로 표시하고
    지금은 `router.back()`을 호출한다). **타이틀/날짜 텍스트를 넣지 않는다(D-03).**
    (2) 지도: `MapView`(고정 높이, 화면 상단 영역 — `PastDateScreen`처럼 절대 채움이 아니라
    스크롤 컬럼 안의 고정 높이 블록으로 둔다. 높이는 명명 상수 `MAP_BLOCK_HEIGHT`로
    선언한다). `region`은 첫 체크인 좌표 + `MAP_REGION_DELTA`로 계산하고, 체크인이 0건이면
    `region`을 넘기지 않는다(`PastDateScreen`의 `initialRegion` 분기와 동일 판단). 잠금
    조합 5종(`scrollEnabled`/`zoomEnabled`/`rotateEnabled`/`pitchEnabled` 전부 `false`,
    `pointerEvents="none"`)을 그대로 적용한다 — 이 지도는 조작 불가능하다(Premise #2).
    `showsUserLocation`을 켜지 않는다. 궤적선/핀은 `PastDateScreen`과 동일 형태로 렌더한다.
    (3) 섹션 헤더: `REFLECTION_COPY.sectionTitle`을 `typography.helperText` +
    `colors.textMuted`로. **개수/진행률 수치를 절대 붙이지 않는다**(REQ-reflection-copy-fix,
    PROJECT.md CRITICAL 원칙).
    (4) 리스트: 이 파일 안에 **신규 read-only 행 컴포넌트** `ReflectionCheckinRow`를 정의한다
    (`CheckinListRow`를 재사용하지 않는다 — 그 컴포넌트는 탭 진입과 스와이프 삭제를
    전제하고 썸네일 슬롯이 없어 이 화면의 계약과 정면으로 다르다. 이 근거를 주석으로
    남긴다). 각 행: `formatLocalTime(checkin.timestamp_utc)`을 `typography.timestamp`로,
    `checkin.note`가 있으면 `typography.journalEntry` + `numberOfLines={1}`로,
    `checkin.photo_path`가 있으면 우측에 40×40 정사각 `Image`(`expo-image`,
    `contentFit="cover"`, `borderRadius: radius.md`). 40은 명명 상수
    `REFLECTION_THUMBNAIL_SIZE`로 선언한다. 탭/스와이프 핸들러를 붙이지 않는다.
    리스트는 `ScrollView` 안이므로 중첩 `FlatList`를 쓰지 않고 `checkins.map(...)`으로
    렌더한다. `checkins.length === 0`이면 `REFLECTION_COPY.emptyState`를
    `typography.helperText` + `colors.textMuted`로 렌더한다.

    `colors.accent`를 쓰지 않는다. 한국어 문자열 리터럴을 직접 쓰지 않는다(전부
    `REFLECTION_COPY` 경유). `navigation.getParent()?.setOptions({ tabBarStyle: ... })`
    같은 탭바 조작 코드를 절대 넣지 않는다 — 이 화면은 루트 Stack 모달로 탭바를 통째로
    덮으므로 불필요하고, 탭바를 조작하는 파일은 저장소에서 `PastDateScreen.tsx`
    하나뿐이라는 06-RESEARCH.md Pitfall 1 계약을 깨뜨린다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 0으로 종료.
    - `grep -c 'getTodayCheckins(' src/reflection/ReflectionModal.tsx` 가 정확히 `1`.
    - `grep -vn '^\s*//' src/reflection/ReflectionModal.tsx | grep -Ec 'INSERT |SELECT |UPDATE |DELETE '` 가 `0`.
    - `grep -c 'tabBarStyle' src/reflection/ReflectionModal.tsx` 가 `0`.
    - `grep -c 'colors.accent' src/reflection/ReflectionModal.tsx` 가 `0`.
    - `grep -c 'CheckinListRow' src/reflection/ReflectionModal.tsx` 가 `0`.
    - `grep -c 'scrollEnabled={false}\|zoomEnabled={false}\|rotateEnabled={false}\|pitchEnabled={false}' src/reflection/ReflectionModal.tsx` 가 `4`.
    - `grep -c 'REFLECTION_THUMBNAIL_SIZE\|MAP_BLOCK_HEIGHT' src/reflection/ReflectionModal.tsx` 가 3 이상.
    - `grep -vn '^\s*//' src/reflection/ReflectionModal.tsx | grep -cP '[가-힣]'` 가 `0`.
  </acceptance_criteria>
  <done>그날의 지도와 체크인 목록이 조작 불가능한 읽기전용 상단부로 렌더된다.</done>
</task>

<task type="auto">
  <name>Task 2: 프롬프트/자동저장 배선 + 닫기 시 강제 저장</name>
  <files>src/reflection/ReflectionModal.tsx</files>
  <read_first>
    - src/reflection/ReflectionModal.tsx (Task 1 결과)
    - src/reflection/useReflectionDraft.ts, src/reflection/ReflectionPrompts.tsx (07-04 산출물 — 정확한 props/반환 계약)
    - src/checkin/CheckinDetailScreen.tsx (키보드 회피 처리 — 이 저장소가 이미 쓰는 방식을 그대로 맞춘다)
    - .planning/phases/07-day-end-reflection/07-UI-SPEC.md (§Component Contracts 2 레이아웃 5~7번 + 자동저장 배선 절)
  </read_first>
  <action>
    `const draft = useReflectionDraft(db, dateKey);`를 호출하고, 리스트 아래에
    `<ReflectionPrompts ... />`를 `draft`의 7개 필드를 그대로 전달해 렌더한다. 프롬프트
    블록 위 간격은 `spacing.lg`. **이 파일에 프롬프트 UI나 실패 문구 JSX를 다시 쓰지
    않는다** — 07-04의 공유 컴포넌트만 소비한다(과거 날짜 뷰와의 시각 드리프트 방지).

    `handleClose`를 `useCallback`으로 정의해 `draft.flush()`를 먼저 부르고 `router.back()`을
    호출하도록 Task 1의 ✕ 버튼 `onPress`를 교체한다. 아래로 스와이프해 닫는 경로에는 별도
    리스너를 달지 않는다 — `useReflectionDraft`가 언마운트 cleanup에서 이미 flush하므로
    `navigation.addListener('beforeRemove')`를 추가하면 같은 draft가 두 경로로 저장될 수
    있다. 이 판단(스와이프 닫기는 훅의 언마운트 flush가 커버한다)을 주석으로 남긴다.
    `CheckinDetailScreen.tsx`의 미저장 경고 `Alert` 패턴을 절대 복제하지 않는다 — 05-CONTEXT.md
    D-01/D-02가 상세화면(명시적 flush 모델)과 회고(자동저장 모델)를 **의도적으로 다른
    모델**로 확정했다. 그 근거를 주석으로 남긴다.

    키보드: 프롬프트2(120pt)가 화면 하단에 있어 키보드가 입력칸을 가릴 수 있다.
    `CheckinDetailScreen.tsx`가 이미 쓰는 키보드 회피 방식을 그대로 맞춰 적용하고,
    `ScrollView`에 `keyboardShouldPersistTaps="handled"`를 준다(실패 문구의 "다시 시도"
    링크가 키보드가 올라온 상태에서도 첫 탭에 반응해야 한다).

    새 모션/트랜지션 코드를 넣지 않는다 — 모달 진입/퇴장은 iOS 네이티브 기본 전환에
    위임한다(07-UI-SPEC.md 확정, `motion` 토큰은 모달 전환용이 아니다).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 0으로 종료.
    - `grep -c 'useReflectionDraft(' src/reflection/ReflectionModal.tsx` 가 정확히 `1`.
    - `grep -c '<ReflectionPrompts' src/reflection/ReflectionModal.tsx` 가 정확히 `1`.
    - `grep -c 'TextInput' src/reflection/ReflectionModal.tsx` 가 `0` — 프롬프트 입력칸을 재구현하지 않았다.
    - `grep -c 'beforeRemove' src/reflection/ReflectionModal.tsx` 가 `0`.
    - `grep -c 'Alert' src/reflection/ReflectionModal.tsx` 가 `0`.
    - `grep -c 'draft.flush()' src/reflection/ReflectionModal.tsx` 가 1 이상이며, 같은 `handleClose` 안에서 `router.back()`보다 먼저 등장한다.
    - `grep -c 'keyboardShouldPersistTaps' src/reflection/ReflectionModal.tsx` 가 `1`.
    - `grep -c 'motion\.' src/reflection/ReflectionModal.tsx` 가 `0`.
  </acceptance_criteria>
  <done>답변이 5초 디바운스·백그라운드 전환·닫기 3경로로 자동저장되고, 실패 시 폼 하단 문구 1세트가 나타난다.</done>
</task>

<task type="auto">
  <name>Task 3: ReflectionModal 배선 회귀 가드 추가</name>
  <files>src/app/__tests__/reflection-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/reflection-wiring.test.ts (07-04가 만든 파일 — 헬퍼와 describe 구성)
    - src/reflection/ReflectionModal.tsx (Task 1/2 결과)
    - .planning/phases/07-day-end-reflection/07-VALIDATION.md (Per-Task Verification Map)
  </read_first>
  <action>
    07-04가 만든 파일 말미에 `ReflectionModal.tsx`를 대상으로 하는 새 describe 블록을
    append한다(기존 단언은 수정하지 않는다). `stripComments` 적용본에 대해:
    (1) `getTodayCheckins(` 가 정확히 1회 등장한다(단일 공유 쿼리 계약).
    (2) SQL 키워드(`INSERT `/`SELECT `/`UPDATE `/`DELETE `)가 등장하지 않는다.
    (3) `tabBarStyle`이 등장하지 않는다(모달은 탭바를 조작하지 않는다).
    (4) `colors.accent`가 등장하지 않는다.
    (5) `CheckinListRow`가 등장하지 않는다(신규 read-only 행 사용).
    (6) 지도 잠금 4종 prop(`scrollEnabled={false}` `zoomEnabled={false}` `rotateEnabled={false}` `pitchEnabled={false}`)이 모두 등장한다.
    (7) `useReflectionDraft(` 와 `<ReflectionPrompts` 가 각각 정확히 1회 등장한다.
    (8) `TextInput`이 등장하지 않는다(프롬프트 재구현 금지).
    (9) `beforeRemove`와 `Alert`가 등장하지 않는다(자동저장 모델 게이트 — 상세화면 모델 혼입 금지).
    (10) `handleClose` 안에서 `draft.flush()`가 `router.back()`보다 먼저 나온다(정규식으로 순서 검증).
    (11) 한글 문자열 리터럴이 등장하지 않는다(문구 단일 출처).
    (12) `REFLECTION_COPY.sectionTitle` 근처에 개수 보간(`{checkins.length}`, `${` 문자열 보간)이 없다 — 진행률 수치 노출 금지 게이트.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/reflection-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/reflection-wiring.test.ts` 전부 통과(07-04 단언 포함).
    - 새 describe 블록에 최소 12개의 `it(` 이 추가됐다.
    - `git diff src/app/__tests__/reflection-wiring.test.ts` 에서 07-04가 만든 기존 `it(` 블록 본문이 변경되지 않았다.
  </acceptance_criteria>
  <done>모달의 단일 쿼리/탭바 미조작/accent 금지/자동저장 모델 계약이 회귀 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite 체크인 데이터 → 지도/리스트 렌더 | 저장된 좌표/메모/사진 경로가 렌더 트리로 들어오는 경계 |
| 사용자 회고 입력 → useReflectionDraft → SQLite | 자유 텍스트 쓰기 경계(07-04가 소유, 이 화면은 통과만) |
| 화면 종료 제스처(✕/스와이프) → 저장 트리거 | 명시적 저장 버튼이 없으므로 종료 경로가 곧 저장 경로다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | `ReflectionModal`의 데이터 접근 | mitigate | 이 화면은 SQL을 직접 쓰지 않고 `getTodayCheckins`(파라미터 바인딩된 repo 함수)만 호출한다. wiring 테스트가 SQL 키워드 부재와 `getTodayCheckins` 1회 호출을 게이트한다 |
| T-07-12 | Repudiation | 닫기 경로의 저장 유실 | mitigate | ✕는 `flush()` 후 `router.back()`, 스와이프 닫기는 훅의 언마운트 cleanup flush가 커버한다. 두 경로가 같은 저장 함수를 통과하며 wiring 테스트가 호출 순서를 게이트 |
| T-07-14 | Information Disclosure | 진행률/완료 수치 노출 | mitigate | 섹션 헤더에 개수 보간을 금지하고 wiring 테스트가 이를 게이트한다(PROJECT.md CRITICAL 원칙 — 이 phase에서 유일하게 카운트가 붙기 쉬운 지점) |
| T-07-15 | Elevation of Privilege | 정적 지도의 의도치 않은 상호작용 | mitigate | 잠금 prop 4종 + `pointerEvents="none"`으로 지도에서 체크인 생성/이동 같은 부작용 경로가 열리지 않는다. wiring 테스트가 4종 prop 존재를 게이트 |
| T-07-SC | Tampering | npm/pip/cargo 설치 | mitigate | 신규 패키지 없음(`expo-image`/`react-native-maps`/`expo-symbols` 전부 기설치). `git diff --exit-code package.json`이 변경 없음을 게이트 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/reflection-wiring.test.ts src/reflection` green
- `npx tsc --noEmit` 통과
- `git diff --exit-code package.json` (변경 없음)
</verification>

<success_criteria>
- 화면 본체가 라우트 파일 밖 `src/reflection/ReflectionModal.tsx`에 있다.
- 새 조회 함수/새 프롬프트 UI/새 실패 UI를 만들지 않았다(전부 재사용).
- 탭바 조작 코드와 미저장 경고 Alert가 없다.
</success_criteria>

<output>
Create `.planning/phases/07-day-end-reflection/07-05-SUMMARY.md` when done.
커밋 메시지는 한글 Conventional Commits(`feat(07-05): ...`), AI 서명 트레일러 금지.
</output>
