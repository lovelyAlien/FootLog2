---
phase: 07-day-end-reflection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/migrations.ts
  - src/db/migrations.test.ts
  - src/settings/settingsRepo.ts
  - src/settings/settingsRepo.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-reflection-notification]

must_haves:
  truths:
    - "user_version=3 기기를 업그레이드하면 app_settings에 daily_reflection_hour 컬럼이 생기고 기존 row의 값이 21로 채워진다"
    - "설정 저장 시 사용자가 고른 회고 알림 시각이 SQLite에 영속되고 앱 재시작 후 그대로 읽힌다"
    - "잘못된(범위 밖/비정수) 시각 값은 쓰기 시점에 거부되고, 읽기 시점에는 21로 폴백해 앱이 죽지 않는다"
    - "REQUIREMENTS.md/ROADMAP.md가 D-05 스코프 확장(시각 선택 UI)을 반영한다"
  artifacts:
    - path: "src/db/migrations.ts"
      provides: "DATABASE_VERSION 4 + currentDbVersion===3 ALTER TABLE 블록"
      contains: "daily_reflection_hour"
    - path: "src/db/schema.ts"
      provides: "AppSettingsRow.daily_reflection_hour 타입"
      contains: "daily_reflection_hour"
    - path: "src/settings/settingsRepo.ts"
      provides: "daily_reflection_hour 읽기/쓰기"
      contains: "daily_reflection_hour"
    - path: "src/db/migrations.test.ts"
      provides: "v3→v4 캐스케이드 + 기본값 백필 회귀 테스트"
    - path: "src/settings/settingsRepo.test.ts"
      provides: "dailyReflectionHour 왕복/폴백/거부 테스트"
  key_links:
    - from: "src/settings/settingsRepo.ts"
      to: "app_settings.daily_reflection_hour"
      via: "getFirstAsync/runAsync 파라미터 바인딩"
      pattern: "daily_reflection_hour"
    - from: "src/settings/settingsRepo.ts"
      to: "NotificationSettings.dailyReflectionHour"
      via: "resolveNotificationSettings 변환"
      pattern: "dailyReflectionHour"
---

<objective>
D-05(회고 알림 시각 선택) 기능의 영속 레이어를 만든다. `app_settings` 테이블에
`daily_reflection_hour` 컬럼을 마이그레이션 v4로 추가하고, `settingsRepo`가 그 값을
읽고 쓰게 확장한다. 그리고 원본 문서/REQUIREMENTS.md가 "시각 변경 UI는 스코프 밖"으로
못박아 둔 문구를 D-05 결정에 맞게 갱신한다(Phase 6 D-01 선례 — 스코프 gap을 문서에
반영하지 않으면 커버리지 게이트가 이 스코프를 통째로 놓친다).

Purpose: 이 플랜이 없으면 07-03의 설정 UI가 고른 시각이 앱 재시작 시 21시로 되돌아간다
(현재 `resolveNotificationSettings`는 `dailyReflectionHour`를 항상 하드코딩 상수로
반환한다).
Output: 마이그레이션 v4, 확장된 settingsRepo, 회귀 테스트, 갱신된 요구사항 문서.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-day-end-reflection/07-CONTEXT.md
@.planning/phases/07-day-end-reflection/07-RESEARCH.md
@.planning/phases/07-day-end-reflection/07-PATTERNS.md

<interfaces>
<!-- 실행자가 필요로 하는 기존 계약. 코드베이스 탐색 없이 이 값들을 그대로 쓴다. -->

src/db/migrations.ts (현재):
- `export const DATABASE_VERSION = 3;`
- `export type MigratableDb = Pick<SQLiteDatabase, 'getFirstAsync' | 'execAsync' | 'runAsync' | 'getAllAsync'>;`
- 캐스케이드 블록: `currentDbVersion === 0` (checkins/daily_reflections/indexes) → 1,
  `=== 1` (drafts) → 2, `=== 2` (`CREATE_APP_SETTINGS_TABLE_SQL`) → 3.
- 파일 말미에 다음 블록을 어디에 append할지 알려주는 주석이 이미 존재한다.
- 마지막 줄의 `PRAGMA user_version = ${currentDbVersion}` 한 곳만 템플릿 보간이 허용된다.

src/db/schema.ts (현재): `AppSettingsRow`는 `id`/`checkin_frequency`/
`daily_reflection_enabled`/`updated_at` 4필드. `CREATE_APP_SETTINGS_TABLE_SQL`은
이미 출하된 DDL 문자열이다.

src/notifications/config.ts (수정하지 않음):
- `export type NotificationSettings = { checkinFrequency: NotificationFrequency; dailyReflectionEnabled: boolean; dailyReflectionHour: number; };`
- `PHASE2_NOTIFICATION_SETTINGS.dailyReflectionHour === 21`

src/settings/settingsRepo.ts (현재):
- `export function isNotificationFrequency(value: unknown): value is NotificationFrequency`
- `export function resolveNotificationSettings(row: AppSettingsRow | null): NotificationSettings`
  — 현재 `dailyReflectionHour: PHASE2_NOTIFICATION_SETTINGS.dailyReflectionHour`로 하드코딩
- `export async function getSettingsRow(db: MigratableDb): Promise<AppSettingsRow | null>`
- `export async function upsertSettings(db: MigratableDb, settings: NotificationSettings, now: string): Promise<void>`
  — `INSERT OR REPLACE INTO app_settings (id, checkin_frequency, daily_reflection_enabled, updated_at) VALUES (?, ?, ?, ?)`
- `SETTINGS_ROW_ID`는 `src/settings/config.ts`가 소유

테스트 하네스: `import { createTestDb } from '../db/testing/nodeSqliteAdapter';`
(`const { db, raw, close } = createTestDb();`), 파일 상단 `@jest-environment node` docblock.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app_settings.daily_reflection_hour 마이그레이션 v4 + 타입</name>
  <files>src/db/schema.ts, src/db/migrations.ts, src/db/migrations.test.ts</files>
  <read_first>
    - src/db/migrations.ts (전체 — 캐스케이드 구조, 헤더의 migration_discipline 주석, append 지점 주석)
    - src/db/schema.ts (AppSettingsRow 인터페이스와 CREATE_APP_SETTINGS_TABLE_SQL 주변)
    - src/db/migrations.test.ts (Test D/Test E — user_version=2 기기 업그레이드 캐스케이드 검증 패턴, Test 8 — 템플릿 보간 1회 게이트)
    - .planning/phases/07-day-end-reflection/07-RESEARCH.md (Pattern 7, Pitfall 5, Assumptions Log A3)
  </read_first>
  <behavior>
    - Test F: `migrateDbIfNeeded`를 새 DB에 돌리면 `PRAGMA table_info(app_settings)`가 정확히 5개 컬럼(`id`, `checkin_frequency`, `daily_reflection_enabled`, `daily_reflection_hour`, `updated_at`)을 갖고, `daily_reflection_hour`의 `notnull`이 1이며 `dflt_value`가 `21`이다.
    - Test G: `user_version=3` 상태를 직접 만든 뒤(구버전 DDL 3종 + `CREATE_APP_SETTINGS_TABLE_SQL` 실행 + `PRAGMA user_version = 3`) `app_settings`에 row 1개를 넣고 `migrateDbIfNeeded`를 호출하면, 기존 row가 보존된 채 `daily_reflection_hour`가 `21`로 자동 백필되고 `PRAGMA user_version`이 `4`가 된다.
    - Test H: `migrateDbIfNeeded`를 연속 2회 호출해도 결과가 동일하다(idempotent, v4).
    - 기존 Test 8(보간 1회 게이트)이 계속 통과한다 — 새 블록은 보간을 쓰지 않는다.
  </behavior>
  <action>
    `src/db/schema.ts`의 `AppSettingsRow` 인터페이스에 `daily_reflection_hour: number;`를
    추가한다. `CREATE_APP_SETTINGS_TABLE_SQL` 문자열은 절대 수정하지 않는다 — 이미 출하된
    DDL이고, 새 컬럼은 반드시 마이그레이션으로만 도착해야 한다(07-RESEARCH.md Pitfall 5).
    왜 CREATE문이 아니라 ALTER인지 한 줄 주석으로 남긴다.

    `src/db/migrations.ts`에서 `DATABASE_VERSION`을 `3`에서 `4`로 올리고, 기존
    `if (currentDbVersion === 2)` 블록 **아래에** 새 블록 `if (currentDbVersion === 3)`을
    append한다. 그 블록은 `db.execAsync`로 정확히
    `ALTER TABLE app_settings ADD COLUMN daily_reflection_hour INTEGER NOT NULL DEFAULT 21`
    한 문장만 실행하고 `currentDbVersion = 4`로 올린다. 기존 `=== 0/1/2` 블록의 어느 줄도
    수정하지 않는다(migration_discipline #2 — 이미 그 버전을 통과한 기기는 변경분을 못
    받는다). `DEFAULT 21`이 D-05 Discretion의 "21시가 반드시 포함돼야 창업자 현재 설정이
    깨지지 않는다"를 별도 백필 스크립트 없이 코드 레벨에서 보장한다는 근거를 주석으로 남긴다.
    파일 말미의 "다음 블록은 여기에 append" 안내 주석은 다음 phase를 위해 `=== 4` 예시로
    갱신해 남겨둔다.

    `src/db/migrations.test.ts` 말미에 Test F/G/H를 append한다. Test G는 이 파일이 이미
    쓰는 Test D 패턴(구버전 DDL을 손으로 실행 → `PRAGMA user_version` 설정 → 데이터 삽입 →
    `migrateDbIfNeeded` 호출 → 보존/추가 검증)을 그대로 복제한다. 기존 테스트는 수정하지
    않는다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/db/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/db/migrations.test.ts` 전부 통과(기존 테스트 포함).
    - `grep -n "DATABASE_VERSION = 4" src/db/migrations.ts` 가 1건 반환.
    - `grep -c 'currentDbVersion === 3' src/db/migrations.ts` 가 1 이상(주석 제외 코드 블록에 존재).
    - `git diff src/db/migrations.ts` 에 기존 `if (currentDbVersion === 0)` / `=== 1` / `=== 2` 블록 **내부** 줄의 변경이 없다.
    - `git diff src/db/schema.ts` 에 `CREATE_APP_SETTINGS_TABLE_SQL` 템플릿 리터럴 내부 줄의 변경이 없다.
    - `grep -n 'daily_reflection_hour' src/db/schema.ts` 가 `AppSettingsRow` 인터페이스 안에서 1건 반환.
  </acceptance_criteria>
  <done>새 설치와 v3 업그레이드 두 경로 모두에서 `daily_reflection_hour`가 21로 존재하며, 기존 마이그레이션 테스트가 전부 green이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: settingsRepo가 daily_reflection_hour를 읽고 쓴다</name>
  <files>src/settings/settingsRepo.ts, src/settings/settingsRepo.test.ts</files>
  <read_first>
    - src/settings/settingsRepo.ts (전체 — 헤더의 "SQL은 이 파일에만" 계약, `isNotificationFrequency` fail-safe 관용구, `upsertSettings`의 `?` 바인딩)
    - src/settings/settingsRepo.test.ts (Test 3~9 — `createTestDb` 사용 패턴과 폴백/거부 테스트 스타일)
    - src/notifications/config.ts (`NotificationSettings`, `PHASE2_NOTIFICATION_SETTINGS`)
    - .planning/phases/07-day-end-reflection/07-PATTERNS.md (`src/settings/settingsRepo.ts` 절 — 바꿔야 할 정확한 줄)
  </read_first>
  <behavior>
    - `isDailyReflectionHour(21)` → true, `isDailyReflectionHour(24)` / `(-1)` / `(20.5)` / `('21')` / `(null)` → false.
    - `resolveNotificationSettings(row)`에서 `row.daily_reflection_hour = 19`면 결과 `dailyReflectionHour === 19`.
    - `row.daily_reflection_hour`가 범위 밖(예: `99`)이거나 `undefined`(구버전 row)면 throw하지 않고 `21`로 폴백한다.
    - `upsertSettings` → `getSettingsRow` → `resolveNotificationSettings` 왕복 시 `dailyReflectionHour: 19`가 그대로 보존된다.
    - `upsertSettings`에 `dailyReflectionHour: 99`를 넘기면 쓰기 시점에 rejects한다(`checkinFrequency` 유니온 밖 값 거부와 동일 정신).
  </behavior>
  <action>
    `src/settings/settingsRepo.ts`에 런타임 가드
    `export function isDailyReflectionHour(value: unknown): value is number`를 추가한다 —
    `Number.isInteger(value) && value >= 0 && value <= 23`. 검증 라이브러리를 도입하지
    않는다(06-RESEARCH.md Security Domain V5의 기존 판단 그대로: 닫힌 정수 범위에 zod는 과설계).

    `resolveNotificationSettings`의 `dailyReflectionHour:
    PHASE2_NOTIFICATION_SETTINGS.dailyReflectionHour` 하드코딩 줄을,
    `isDailyReflectionHour(row.daily_reflection_hour)`이면 `row.daily_reflection_hour`,
    아니면 `PHASE2_NOTIFICATION_SETTINGS.dailyReflectionHour`로 폴백하는 형태로 바꾼다 —
    바로 위 `checkinFrequency`가 이미 쓰는 fail-safe 관용구와 동일한 모양으로. 현재 파일
    헤더/함수 주석의 "dailyReflectionHour는 컬럼이 없으므로 항상 상수를 쓴다"는 문장을
    새 동작에 맞게 갱신한다(거짓 주석을 남기지 않는다).

    `upsertSettings`에 `isDailyReflectionHour` 사전 검증을 추가해 실패 시 기존
    `checkinFrequency` 가드와 같은 형태로 `throw new Error`한다. `INSERT OR REPLACE` 문의
    컬럼 목록에 `daily_reflection_hour`를 5번째로 추가하고 `?` 플레이스홀더와 파라미터를
    같은 순서로 추가한다 — 문자열 보간은 절대 쓰지 않는다(T-07-01).

    `src/settings/settingsRepo.test.ts` 말미에 위 behavior 5종을 커버하는 테스트를
    append한다(기존 Test 1~9는 수정하지 않되, Test 4/6/7이 `AppSettingsRow` 리터럴을
    만들고 있다면 새 필수 필드 때문에 타입이 깨지므로 그 리터럴에만 `daily_reflection_hour`를
    추가한다).
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/settings/settingsRepo.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/settings/settingsRepo.test.ts` 전부 통과.
    - `npx tsc --noEmit` 이 0으로 종료.
    - `grep -c 'daily_reflection_hour' src/settings/settingsRepo.ts` 가 2 이상(읽기 1 + INSERT 컬럼 목록 1).
    - `grep -vn '^\s*//' src/settings/settingsRepo.ts | grep -c '\${'` 가 `0` — 이 파일에 템플릿 보간이 없다.
    - `grep -n 'PHASE2_NOTIFICATION_SETTINGS.dailyReflectionHour' src/settings/settingsRepo.ts` 가 폴백 분기 안에서만 등장한다(무조건 반환 경로 없음).
  </acceptance_criteria>
  <done>선택한 회고 알림 시각이 SQLite에 저장되고 앱 재시작 후 그대로 읽히며, 오염된 값은 쓰기 시 거부·읽기 시 21로 폴백한다.</done>
</task>

<task type="auto">
  <name>Task 3: D-05 스코프 확장을 REQUIREMENTS.md/ROADMAP.md에 반영</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (§Day-end reflection — REQ-reflection-notification 항목 원문)
    - .planning/ROADMAP.md (### Phase 7: Day-end Reflection — Requirements 줄과 Success Criteria 4번)
    - .planning/phases/07-day-end-reflection/07-CONTEXT.md (D-05 전문 — 특히 "REQUIREMENTS.md 반영 필요" 문단)
  </read_first>
  <action>
    `.planning/REQUIREMENTS.md`의 `REQ-reflection-notification` 항목 본문을, 토글에 더해
    **설정 화면에서 회고 알림 시각을 직접 선택할 수 있다(후보: 19/20/21/22/23시, 기본 21시)**
    는 조건을 포함하도록 확장한다. 새 requirement ID를 만들지 않고 기존 ID의 경계를 넓히는
    방식을 택한다 — 07-CONTEXT.md Deferred Ideas 절이 "새로운 capability가 아니라 기존
    REQ-reflection-notification의 경계 확장"이라고 이미 확정했다. 원본 문서
    (`day-end-reflection-map.md` Premise #4)가 "시각 변경 UI는 스코프 밖"이었고 창업자가
    2026-09-02 논의에서 명시적으로 뒤집었다는 사실을 항목 안에 괄호로 기록한다.

    `.planning/ROADMAP.md`의 Phase 7 Success Criteria 4번("사용자는 기본으로 켜진 매일 고정
    시각의 회고 리마인더를 받으며, 설정에서 끌 수 있는 토글이 있다")에 "설정에서 알림
    시각(19/20/21/22/23시)을 직접 고를 수 있다"를 덧붙인다. Phase 7의 `**Requirements:**`
    줄은 ID 목록이 바뀌지 않으므로 그대로 둔다.
  </action>
  <verify>
    <automated>grep -c '시각' .planning/REQUIREMENTS.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -q '19' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'REQ-reflection-notification' .planning/REQUIREMENTS.md` 항목 본문에 `시각`과 `21` 문자열이 모두 포함된다.
    - `.planning/REQUIREMENTS.md`에 `REQ-reflection-notification-time` 같은 새 ID가 추가되지 않았다: `grep -c 'REQ-reflection-notification-time' .planning/REQUIREMENTS.md` 가 `0`.
    - `.planning/ROADMAP.md` Phase 7 Success Criteria 4번 줄에 `시각` 선택 문구가 포함된다.
    - `.planning/ROADMAP.md`의 Phase 7 `**Requirements:**` 줄이 기존 7개 ID 그대로다.
  </acceptance_criteria>
  <done>결정 커버리지 게이트가 D-05 스코프를 요구사항 문서에서 찾을 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력(설정 화면 선택값) → settingsRepo → SQLite | 앱 내부 입력이지만 DB 쓰기 경계 — 파라미터 바인딩 규약이 적용되는 지점 |
| 이전 앱 버전이 남긴 DB row → resolveNotificationSettings | 신뢰할 수 없는 과거 데이터(구버전/손상 row)가 런타임 타입 가정을 깨뜨릴 수 있는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `settingsRepo.upsertSettings` / `migrations.ts` v4 블록 | mitigate | 모든 DML은 `?` 플레이스홀더 바인딩만 사용. ALTER TABLE은 사용자 입력이 섞이지 않는 정적 리터럴 1문장. acceptance criteria가 `settingsRepo.ts`의 `${` 출현 0을 게이트하고, 기존 migrations Test 8이 보간 1회 한도를 계속 게이트한다 |
| T-07-02 | Tampering | `daily_reflection_hour` 값 범위 | mitigate | `isDailyReflectionHour`(정수 0~23) 가드를 쓰기 시점에 강제(throw) + 읽기 시점 21 폴백. 범위 밖 값이 `scheduling.ts`의 캘린더 트리거 hour로 흘러들어가 트리거 등록 자체가 실패하는 것을 막는다 |
| T-07-03 | Denial of Service | 앱 부팅 시 마이그레이션 실패 | accept | 추가 전용 ALTER TABLE 1문장이며 기존 블록을 건드리지 않는다. v3→v4 캐스케이드와 idempotency를 Test G/H가 실제 SQLite 엔진으로 검증하므로 잔여 위험이 낮다 |
| T-07-SC | Tampering | npm/pip/cargo 설치 | mitigate | 이 phase는 신규 패키지를 설치하지 않는다(07-RESEARCH.md Package Legitimacy Audit: "해당 없음"). `git diff --exit-code package.json package-lock.json`이 변경 없음을 게이트한다 — 설치가 필요해지면 계획으로 되돌아가 legitimacy 체크포인트를 먼저 만든다 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/db/migrations.test.ts src/settings/settingsRepo.test.ts` 전부 green
- `npx tsc --noEmit` 통과
- `git diff --exit-code package.json` (변경 없음)
</verification>

<success_criteria>
- 신규 설치/v3 업그레이드 두 경로 모두에서 `app_settings.daily_reflection_hour`가 존재하고 기본값 21이다.
- `resolveNotificationSettings`가 DB 값을 반영하고 오염된 값에는 21로 폴백한다.
- `upsertSettings`가 범위 밖 시각을 거부한다.
- REQUIREMENTS.md/ROADMAP.md가 D-05 스코프를 담고 있다.
</success_criteria>

<output>
Create `.planning/phases/07-day-end-reflection/07-01-SUMMARY.md` when done.
커밋 메시지는 한글 Conventional Commits(`feat(07-01): ...`), AI 서명 트레일러 금지.
</output>
