---
phase: 07-day-end-reflection
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/today/TodayBottomSheet.tsx
  - src/app/__tests__/today-wiring.test.ts
autonomous: true
requirements: [REQ-reflection-today-entry, REQ-past-reflection-edit]

must_haves:
  truths:
    - "체크인이 0건이어도 바텀시트가 리스트를 마운트하며, 빈 상태 문구는 리스트 안에서 보인다"
    - "부모 화면이 리스트 최상단/최하단에 임의의 요소를 끼워 넣을 수 있다"
    - "오늘 뷰와 과거 날짜 뷰의 기존 동작(빈 상태 문구, 행 탭, 스와이프 삭제, 시트 강제 접힘)이 그대로다"
  artifacts:
    - path: "src/today/TodayBottomSheet.tsx"
      provides: "항상 마운트되는 리스트 + ListEmptyComponent + 선택적 헤더/푸터 슬롯"
      contains: "ListEmptyComponent"
  key_links:
    - from: "src/today/TodayBottomSheet.tsx"
      to: "BottomSheetFlatList"
      via: "ListHeaderComponent/ListFooterComponent/ListEmptyComponent prop 전달"
      pattern: "ListHeaderComponent"
---

<objective>
`TodayBottomSheet`의 "체크인 0건이면 리스트를 아예 마운트하지 않고 `<Text>`만 그린다"
분기를 제거하고, 항상 `BottomSheetFlatList`를 마운트하되 빈 상태를 `ListEmptyComponent`로
옮긴다. 그리고 부모가 리스트 최상단/최하단에 요소를 끼워 넣을 수 있도록 선택적
`ListHeaderComponent`/`ListFooterComponent` prop을 추가한다.

Purpose: 07-RESEARCH.md Pitfall 1이 짚은 구조적 장애물을 먼저 치운다 — 현재 구조에서는
체크인이 0건일 때 리스트 자체가 없어서 "오늘 돌아보기" 행(REQ-reflection-today-entry:
0건이어도 항상 보여야 함)을 붙일 자리가 없고, 과거 날짜 뷰의 인라인 회고 프롬프트
(REQ-past-reflection-edit)도 리스트 아래에 넣을 자리가 없다. 이 플랜은 두 소비 플랜
(07-07, 07-09)이 붙을 슬롯만 만들고 슬롯을 채우지는 않는다.
Output: 확장된 시트 컴포넌트 + 계약 회귀 가드.

**"오늘 돌아보기" 행 자체를 만들지 않는다** — 슬롯만 만든다(07-09 소관). 이 시트는
`checkins` 배열에 합성 항목을 unshift하지 않는다(타입이 `CheckinRow[]`이고
`CheckinListRow`가 스와이프 삭제/상세 진입을 전제하므로 합성 항목은 잘못된 대상으로
삭제를 시도하게 만든다 — 07-RESEARCH.md Pitfall 1 Warning signs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-day-end-reflection/07-RESEARCH.md
@.planning/phases/07-day-end-reflection/07-PATTERNS.md
@.planning/phases/07-day-end-reflection/07-UI-SPEC.md

<interfaces>
<!-- 기존 계약. 탐색 없이 그대로 쓴다. -->

src/today/TodayBottomSheet.tsx (수정 대상, 현재):
- `TodayBottomSheetProps = { checkins: CheckinRow[]; containerHeight: number;
  animatedPosition: SharedValue<number>; onRowPress: (id: string) => void;
  onDeleteRequest: (checkin: CheckinRow) => void; emptyText?: string; sheetRef?: Ref<BottomSheet> }`
- 선택적 prop 확장 선례: `emptyText`(06-05가 추가, 기본값 `TODAY_COPY.emptyState`),
  `sheetRef`(06-07가 추가) — 둘 다 "기본값이 오늘 뷰의 기존 동작을 그대로 보존한다"는
  주석 규약을 갖고 있다.
- 현재 렌더 분기: `checkins.length === 0 ? <Text style={[typography.helperText, styles.emptyText]}>{emptyText}</Text> : <BottomSheetFlatList data={checkins} keyExtractor renderItem={<CheckinListRow .../>} />`
- `styles.emptyText = { color: colors.textMuted, paddingHorizontal: spacing.lg, minHeight: LIST_ROW_MIN_HEIGHT, textAlignVertical: 'center' }`
- `containerHeight <= 0`이면 컴포넌트가 `null`을 반환하는 기존 계약은 유지한다.

소비처 2곳(이 플랜은 둘 다 수정하지 않는다):
- `src/app/(tabs)/index/index.tsx` — `emptyText`/`sheetRef` 미전달
- `src/calendar/PastDateScreen.tsx` — `emptyText={CALENDAR_COPY.pastDateEmptyState}`, `sheetRef` 전달

@gorhom/bottom-sheet v5 `BottomSheetFlatList`는 `FlatListProps`를 상속한다(무시된다고
문서화된 prop은 `scrollEventThrottle`/`decelerationRate`/`onScrollBeginDrag` 3개뿐).
`ListHeaderComponent`/`ListFooterComponent`/`ListEmptyComponent`가 실제로 동작하는지는
07-RESEARCH.md Assumptions Log A2가 MEDIUM confidence로 표시한 항목이라, 시뮬레이터
검증(07-10)에서 최우선으로 확인한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 리스트 상시 마운트 + 빈 상태/헤더/푸터 슬롯</name>
  <files>src/today/TodayBottomSheet.tsx</files>
  <read_first>
    - src/today/TodayBottomSheet.tsx (전체 — props 주석 규약, 렌더 분기, 스타일)
    - src/calendar/PastDateScreen.tsx (`TodayBottomSheet` 호출부 — `emptyText`/`sheetRef` 전달 형태)
    - src/app/(tabs)/index/index.tsx 1250~1270줄 (`TodayBottomSheet` 호출부)
    - .planning/phases/07-day-end-reflection/07-RESEARCH.md (Pitfall 1 전문, Assumptions Log A2)
  </read_first>
  <action>
    `TodayBottomSheetProps`에 선택적 prop 2개를 추가한다:
    `ListHeaderComponent?: ReactElement | null;`, `ListFooterComponent?: ReactElement | null;`.
    기존 필수 필드는 건드리지 않는다. 두 prop에 `emptyText`/`sheetRef`가 세운 것과 같은
    주석 규약을 적용한다 — "이 prop을 넘기지 않는 호출부(현재 오늘 뷰)의 동작은 완전히
    불변이며, 07-09(오늘 돌아보기 행)와 07-07(과거 날짜 회고 프롬프트)이 이 슬롯을
    채운다"고 명시한다.

    렌더 분기를 다음과 같이 바꾼다: `checkins.length === 0` 삼항 분기를 제거하고 **항상**
    `BottomSheetFlatList`를 마운트한다. 빈 상태 `<Text>`는 `ListEmptyComponent`로 옮기되
    **동일한 스타일(`typography.helperText` + `styles.emptyText`)과 동일한 `emptyText`
    문구**를 그대로 쓴다 — 오늘 뷰/과거 날짜 뷰의 빈 상태 시각이 바뀌면 안 된다.
    `ListHeaderComponent`/`ListFooterComponent`는 받은 값을 그대로 전달한다.
    `containerHeight <= 0` 조기 반환, `snapPoints` 계산, `animationConfigs`,
    `sheetRef`/`animatedPosition` 전달, `CheckinListRow` 렌더는 전부 그대로 둔다.

    변경 이유(0건일 때 리스트가 없으면 헤더/푸터 슬롯이 존재할 수 없어
    REQ-reflection-today-entry/REQ-past-reflection-edit를 만족시킬 수 없다)를 파일 헤더
    주석에 한 문단으로 남긴다. `checkins` 배열에 합성 항목을 넣는 접근을 쓰지 않았다는
    점과 그 이유(`CheckinListRow`의 스와이프 삭제/상세 진입 전제)도 함께 남긴다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 0으로 종료 — `BottomSheetFlatList`가 세 prop을 실제로 타입 수준에서 받아들인다(A2 가정의 1차 확인).
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__` 기존 스위트 전부 green.
    - `grep -c 'ListEmptyComponent' src/today/TodayBottomSheet.tsx` 가 1 이상.
    - `grep -c 'ListHeaderComponent\|ListFooterComponent' src/today/TodayBottomSheet.tsx` 가 4 이상(타입 2 + 구조분해 2 + 전달 2 중 최소).
    - `grep -c 'checkins.length === 0' src/today/TodayBottomSheet.tsx` 가 `0`.
    - `grep -c 'unshift\|prepend' src/today/TodayBottomSheet.tsx` 가 `0`.
    - `git diff src/today/TodayBottomSheet.tsx` 에서 `styles.emptyText` 정의와 `snapPoints` 계산 블록이 변경되지 않았다.
    - `git diff --exit-code src/app/\(tabs\)/index/index.tsx src/calendar/PastDateScreen.tsx` — 소비처 2곳이 이 플랜에서 수정되지 않았다.
  </acceptance_criteria>
  <done>시트가 체크인 0건에서도 리스트를 유지하고, 부모가 최상단/최하단에 요소를 끼워 넣을 슬롯이 열렸다.</done>
</task>

<task type="auto">
  <name>Task 2: 시트 슬롯 계약 회귀 가드</name>
  <files>src/app/__tests__/today-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/today-wiring.test.ts (전체 — `readSource` 헬퍼, `stripComments`, 호출 횟수 단언 스타일)
    - src/today/TodayBottomSheet.tsx (Task 1 결과)
  </read_first>
  <action>
    기존 파일에 `TodayBottomSheet.tsx`를 읽는 헬퍼(`src/today/` 경로 — 이 파일의 기존
    `readSource`가 `src/app/` 기준이므로 `readSrcSource` 스타일 헬퍼를 하나 추가한다.
    `calendar-wiring.test.ts`가 같은 패턴을 이미 쓴다)와 새 describe 블록을 append한다.
    기존 단언은 수정하지 않는다. `stripComments` 적용본에 대해:
    (1) `checkins.length === 0` 삼항 분기가 존재하지 않는다.
    (2) `ListEmptyComponent`가 등장한다.
    (3) `ListHeaderComponent`와 `ListFooterComponent`가 각각 등장한다.
    (4) `BottomSheetFlatList`가 정확히 1회 마운트된다(`<BottomSheetFlatList` 출현 1회).
    (5) `unshift`/`prepend`가 등장하지 않는다(합성 항목 금지 게이트 —
        07-RESEARCH.md Pitfall 1이 지정한 warning sign을 테스트로 고정한다).
    (6) `emptyText` 기본값이 여전히 `TODAY_COPY.emptyState`다.
    (7) `TodayBottomSheetProps`의 필수 필드 6개(`checkins` `containerHeight`
        `animatedPosition` `onRowPress` `onDeleteRequest`와 타입 선언 자체)가 옵셔널로
        바뀌지 않았다 — `checkins: CheckinRow[]`와 `containerHeight: number`가 `?:`가 아닌
        형태로 존재한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/today-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/today-wiring.test.ts` 전부 통과(기존 단언 포함).
    - 새 describe 블록에 최소 7개의 `it(` 이 추가됐다.
    - `git diff src/app/__tests__/today-wiring.test.ts` 에서 기존 `it(` 블록 본문이 변경되지 않았다.
  </acceptance_criteria>
  <done>시트 슬롯 계약과 "합성 항목 금지" 원칙이 회귀 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 부모 화면이 주입하는 헤더/푸터 요소 → 시트 렌더 트리 | 시트가 소유하지 않은 임의 요소가 리스트 안으로 들어오는 경계 |
| 리스트 항목 → 스와이프 삭제 핸들러 | 잘못된 항목이 삭제 대상으로 전달되면 사용자 데이터가 소실되는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Tampering | 리스트 `data` 배열의 항목 타입 | mitigate | 진입 행/프롬프트를 `checkins` 배열에 합성해 넣지 않고 `ListHeaderComponent`/`ListFooterComponent` 슬롯으로 분리한다 — `CheckinListRow`의 `onDeleteRequest`가 체크인이 아닌 합성 객체를 삭제 대상으로 받는 경로가 구조적으로 존재하지 않는다. wiring 테스트가 `unshift`/`prepend` 부재를 게이트 |
| T-07-17 | Denial of Service | 기존 두 소비 화면의 회귀 | mitigate | 새 prop은 전부 선택적이고 기본값이 기존 동작을 보존한다. 소비처 파일을 이 플랜에서 수정하지 않으며(`git diff --exit-code` 게이트), 기존 `src/app/__tests__` 스위트 전체 green을 태스크 verify로 요구한다 |
| T-07-SC | Tampering | npm/pip/cargo 설치 | mitigate | 신규 패키지 없음. `git diff --exit-code package.json`이 변경 없음을 게이트 |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__` green
- `npx tsc --noEmit` 통과
- `git diff --exit-code package.json src/app/\(tabs\)/index/index.tsx src/calendar/PastDateScreen.tsx`
</verification>

<success_criteria>
- 체크인 0건에서도 `BottomSheetFlatList`가 마운트된다.
- 헤더/푸터 슬롯이 선택적 prop으로 열렸고 기존 두 소비 화면 동작이 불변이다.
- 합성 리스트 항목 접근이 코드와 테스트 양쪽에서 차단됐다.
</success_criteria>

<output>
Create `.planning/phases/07-day-end-reflection/07-06-SUMMARY.md` when done.
커밋 메시지는 한글 Conventional Commits(`refactor(07-06): ...`), AI 서명 트레일러 금지.
</output>
