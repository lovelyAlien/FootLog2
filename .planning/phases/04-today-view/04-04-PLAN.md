---
phase: 04-today-view
plan: 04
type: execute
wave: 2
depends_on: [04-02, 04-03]
files_modified:
  - package.json
  - src/checkin/localDate.ts
  - src/checkin/localDate.test.ts
  - src/today/content.ts
  - src/today/CheckinListRow.tsx
  - src/today/TodayBottomSheet.tsx
  - src/today/__tests__/todayUi.test.ts
autonomous: false
requirements: [REQ-today-view]
user_setup: []

must_haves:
  truths:
    - "바텀시트 리스트의 각 행이 모노스페이스 시간과(메모가 있을 때만) 세리프 이탤릭 메모 미리보기 1줄을 보여준다"
    - "리스트 행은 탭할 수 없고 탭 가능함을 암시하는 시각 요소가 없다"
    - "오늘 체크인이 0건이면 리스트 대신 안내 한 줄이 시트 표면에 보인다"
    - "바텀시트는 CLOSED/OPEN 두 스냅 지점 사이를 220ms로 이동하며 드래그 중에는 손가락을 실시간으로 따라간다"
  artifacts:
    - path: "src/today/CheckinListRow.tsx"
      provides: "시간 + 메모 미리보기 리스트 행(비인터랙티브)"
      exports: ["CheckinListRow", "LIST_ROW_MIN_HEIGHT"]
    - path: "src/today/TodayBottomSheet.tsx"
      provides: "@gorhom/bottom-sheet 기반 3단 스냅 시트 + 리스트/빈 상태"
      exports: ["TodayBottomSheet"]
    - path: "src/checkin/localDate.ts"
      provides: "Intl 기반 HH:mm 로컬 시각 포맷"
      exports: ["formatLocalTime"]
    - path: "src/today/__tests__/todayUi.test.ts"
      provides: "리스트 행/시트의 UI 계약 회귀 가드(D-01/D-02/D-03)"
  key_links:
    - from: "src/today/CheckinListRow.tsx"
      to: "src/checkin/localDate.ts formatLocalTime"
      via: "timestamp_utc → 로컬 HH:mm 변환"
      pattern: "formatLocalTime\\("
    - from: "src/today/TodayBottomSheet.tsx"
      to: "@gorhom/bottom-sheet BottomSheetFlatList"
      via: "시트 팬 제스처와 리스트 스크롤 제스처 조율"
      pattern: "BottomSheetFlatList"
---

<objective>
오늘 뷰 바텀시트의 표현 계층 두 컴포넌트(`CheckinListRow`, `TodayBottomSheet`)와 그 둘이 필요로 하는 시각 포맷 헬퍼를 만든다. 화면 배선(어떤 데이터를 넣고 언제 마운트할지)은 이 plan의 범위가 아니다 — 04-05/04-06이 담당한다.

Purpose: REQ-today-view의 "3단 스냅 바텀시트에 오늘의 실제 체크인들을 시간순으로 나열". 04-CONTEXT.md D-01(장소명 필드를 만들지 않음), D-02(시간 + 메모 미리보기만, 사진 아이콘 없음), D-03(행은 탭 불가)이 이 두 컴포넌트의 형태를 정확히 결정한다. 두 컴포넌트를 화면 배선과 분리해 먼저 만드는 이유는 저장소의 기존 계약(`NotificationDeniedBanner`/`CheckinActionCard`) — "재사용 가능한 독립 컴포넌트는 화면별 로직이나 absolute positioning을 내부에 갖지 않는다"를 그대로 유지하기 위해서다.
Output: prop만으로 렌더되는 시트/행 컴포넌트 + 정적 UI 계약 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-today-view/04-CONTEXT.md
@.planning/phases/04-today-view/04-RESEARCH.md
@.planning/phases/04-today-view/04-PATTERNS.md
@.planning/phases/04-today-view/04-UI-SPEC.md
</context>

<interfaces>
<!-- 이 plan이 만들어야 하는 공개 계약 (04-05/04-06이 이 시그니처에 맞춰 배선한다) -->

`src/today/CheckinListRow.tsx`:
`export const LIST_ROW_MIN_HEIGHT = 44;`
`export type CheckinListRowProps = { checkin: CheckinRow };`
`export function CheckinListRow({ checkin }: CheckinListRowProps): JSX.Element;`

`src/today/TodayBottomSheet.tsx`:
`export type TodayBottomSheetProps = { checkins: CheckinRow[]; containerHeight: number; animatedPosition: SharedValue<number> };`
`export function TodayBottomSheet(props: TodayBottomSheetProps): JSX.Element | null;`
 - `containerHeight`: 부모가 `onLayout`으로 잰 오늘 화면 콘텐츠 영역 높이(탭바 제외). `0` 이하면 `null`을 반환해 아직 렌더하지 않는다.
 - `animatedPosition`: **부모가 소유**하는 reanimated `SharedValue<number>`. 시트가 자신의 현재 상단 y좌표를 여기에 계속 써 넣고, 부모(오늘 화면)가 플로팅 버튼 오프셋 계산에 읽는다(D-05).

`src/checkin/localDate.ts`에 추가:
`export function formatLocalTime(isoTimestamp: string, timeZone?: string): string;`  // '09:21' 형태

<!-- 기존 토큰 (재발명 금지) -->
`typography.timestamp` (15px/500, ui-monospace, tabular-nums) · `typography.journalEntry` (세리프 이탤릭, 사용자 작성 텍스트 전용) · `typography.helperText` (13px/400)
`colors.surface` · `colors.line` · `colors.textMuted` · `spacing.sm`(12) · `spacing.lg`(24) · `radius.lg`(16) · `radius.full` · `motion.bottomSheetSnapMs`(220)
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: @gorhom/bottom-sheet 패키지 정당성 확인 게이트</name>
  <files>(리포지토리 파일 변경 없음 — 설치 전 승인 게이트)</files>
  <read_first>
    - .planning/phases/04-today-view/04-RESEARCH.md §Package Legitimacy Audit (감사 표와 `[ASSUMED]` 태그가 남은 이유)
    - .planning/phases/04-today-view/04-RESEARCH.md §Standard Stack (버전/peer 호환성 근거)
  </read_first>
  <action>
    04-RESEARCH.md는 `@gorhom/bottom-sheet`를 slopcheck `OK` + npm 등록일 ~6년 + 주간 다운로드 331만 + 소스 리포 `github.com/gorhom/react-native-bottom-sheet`로 확인했지만, **패키지 이름 자체의 출처가 공식 문서 조회가 아니라 웹검색/훈련지식**이었다는 이유로 엄격 provenance 규칙상 `[ASSUMED]` 태그를 남겼다. 프로젝트 규약상 `[ASSUMED]` 패키지는 설치 전 사람의 확인이 필요하다.

    이 태스크는 자동 승인하지 않는다. 아래 `<how-to-verify>`의 확인 결과를 받은 뒤에만 Task 2의 설치를 진행한다. 사용자가 거부하면 설치하지 않고 중단한 뒤, 대안(기설치된 `react-native-gesture-handler` 2.32.0 + `react-native-reanimated` 4.5.1 위에 커스텀 시트 구현 — 04-RESEARCH.md §Alternatives Considered에서 스냅 물리/키보드 회피/플링 속도 판정을 재구현해야 해 비권장으로 판정됨)을 SUMMARY에 기록하고 재계획을 요청한다.
  </action>
  <verify>
    <human-check>사용자가 npmjs.com 페이지를 확인한 뒤 설치 진행 여부를 명시적으로 응답했다</human-check>
  </verify>
  <what-built>
    아직 아무것도 설치하지 않았다. 3단 스냅 바텀시트를 직접 구현하는 대신 `@gorhom/bottom-sheet` v5(`^5.2.14`)를 도입하려는 시점이다. 이미 설치된 `react-native-gesture-handler@2.32.0` / `react-native-reanimated@4.5.1`이 이 패키지의 peer 요구사항을 만족하며(각각 `>=2.16.1`, `>=4.0.0-`), 앱 루트에는 `GestureHandlerRootView`가 이미 배선돼 있다.
  </what-built>
  <how-to-verify>
    브라우저에서 https://www.npmjs.com/package/@gorhom/bottom-sheet 를 열고 아래를 확인해주세요.

    1. 페이지가 실제로 존재하고, **Repository**가 `github.com/gorhom/react-native-bottom-sheet`를 가리키는지.
    2. 주간 다운로드가 수백만 단위인지(연구 시점 기준 약 331만).
    3. 최신 버전이 `5.x`대이고 최근 1년 내 배포 이력이 있는지.
    4. 패키지 이름의 오탈자 변종(예: `gorhom-bottom-sheet`, `@gorham/bottom-sheet`)이 아니라 정확히 `@gorhom/bottom-sheet`인지.

    확인 후 "설치 진행" 또는 "설치 중단 + 이유"로 답해주세요.
  </how-to-verify>
  <acceptance_criteria>
    - 사용자가 "설치 진행" 또는 "설치 중단"으로 명시적으로 응답했다
    - 응답 내용과 확인된 저장소 URL이 SUMMARY에 기록된다
    - 사용자 응답 없이 Task 2의 `npm install`이 실행되지 않았다
  </acceptance_criteria>
  <resume-signal>"설치 진행" 또는 "설치 중단 + 이유"로 답해주세요.</resume-signal>
  <done>패키지 도입 여부에 대한 사람의 명시적 승인/거부가 기록됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: @gorhom/bottom-sheet 설치 + formatLocalTime + CheckinListRow</name>
  <files>package.json, src/checkin/localDate.ts, src/checkin/localDate.test.ts, src/today/content.ts, src/today/CheckinListRow.tsx, src/today/__tests__/todayUi.test.ts</files>
  <read_first>
    - src/checkin/localDate.ts (`Intl.DateTimeFormat` 단일 출처 규약 — "수동 Date 파싱이나 UTC 오프셋 산수를 절대 쓰지 않는다"는 헤더 계약)
    - src/checkin/localDate.test.ts (기존 `resolveLocalDateKey` 테스트의 타임존 고정 방식)
    - src/components/NotificationDeniedBanner.tsx (가장 단순한 프레젠테이셔널 컴포넌트의 헤더 주석 계약 + prop-driven 렌더 형태)
    - src/theme/tokens.ts (`typography.timestamp`의 `fontVariant`가 `readonly` 튜플이라 `StyleSheet.create` 안에서 그대로 못 쓰는 문제 — `src/app/(tabs)/index.tsx`가 쓰는 얕은 복사 브리징 방식을 참고할 것)
    - src/today/content.ts (04-03이 만든 `TODAY_COPY` — 키를 추가만 하고 기존 키를 바꾸지 않는다)
    - .planning/phases/04-today-view/04-UI-SPEC.md §리스트 행 (CheckinListRow), §Typography, §Spacing Scale
  </read_first>
  <behavior>
    `formatLocalTime`:
    - `formatLocalTime('2026-08-30T00:21:00.000Z', 'Asia/Seoul')` === `'09:21'`
    - `formatLocalTime('2026-08-30T15:00:00.000Z', 'Asia/Seoul')` === `'00:00'` (자정이 `24:00`이 아니라 `00:00`으로 나온다)
    - `formatLocalTime('2026-08-30T04:05:00.000Z', 'UTC')` === `'04:05'` (한 자리 시/분이 0으로 패딩된다)
    - 같은 UTC 타임스탬프가 타임존에 따라 다른 값을 반환한다(`Asia/Seoul` vs `UTC` 비교)
  </behavior>
  <action>
    Task 1이 "설치 진행"으로 승인된 경우에만 `npm install @gorhom/bottom-sheet`를 실행한다(Expo SDK 버전 관리 대상이 아니므로 `npx expo install`이 아니라 일반 `npm install`이다). 설치 후 `npm ls @gorhom/bottom-sheet react-native-gesture-handler react-native-reanimated`로 peer 경고가 없는지 확인한다.

    `src/checkin/localDate.ts`에 `formatLocalTime(isoTimestamp: string, timeZone: string = resolveTimeZone()): string`을 추가한다. 구현은 `new Intl.DateTimeFormat('en-GB', { timeZone, hour: '2-digit', minute: '2-digit', hourCycle: 'h23' }).format(new Date(isoTimestamp))`이다. `hourCycle: 'h23'`을 명시하는 이유(자정을 `24:00`이 아니라 `00:00`으로, 오후를 12시간제가 아니라 24시간제로 고정)를 주석으로 남긴다. 수동 문자열 슬라이싱이나 `getHours()` 산수를 쓰지 않는다 — 이 파일의 기존 계약이다.

    `src/checkin/localDate.test.ts`에 `describe('formatLocalTime', ...)`을 추가하고 위 `<behavior>` 4가지를 각각 별도 `it`으로 검증한다. 타임존은 항상 인자로 명시해 실행 환경 로컬 타임존에 의존하지 않게 한다.

    `src/today/content.ts`의 `TODAY_COPY`에 `emptyState: '아직 기록이 없어요 · 체크인하면 지도가 채워져요'` 키를 추가한다(04-UI-SPEC.md §Copywriting Contract 확정 문구, 가운뎃점은 U+00B7 `·`).

    `src/today/CheckinListRow.tsx`를 만든다:
    - `NotificationDeniedBanner.tsx`의 헤더 주석 계약("이 컴포넌트는 화면별 로직이나 위치 지정(absolute positioning)을 내부에 갖지 않는다 — 배치는 항상 부모가 결정한다")을 그대로 옮겨 적는다.
    - `export const LIST_ROW_MIN_HEIGHT = 44;` — `TodayBottomSheet`가 CLOSED 피크 높이 계산에 재사용하므로 숫자를 시트 쪽에 중복 선언하지 않는다. 주석에 "이 44는 탭 타겟 규칙이 아니라 리스트 밀도 일관성 근거다(행은 비인터랙티브 — D-03, 04-UI-SPEC.md §Spacing Scale Exceptions)"를 남긴다.
    - 순수 `View`로 감싼다. `Pressable`/`TouchableOpacity`/`TouchableHighlight`/`onPress`를 쓰지 않고 chevron·화살표·`>` 문자·밑줄 등 탭 가능함을 암시하는 요소를 넣지 않는다(D-03).
    - 가로 배치: 왼쪽에 `typography.timestamp` 스타일로 `formatLocalTime(checkin.timestamp_utc)` 결과. 오른쪽에 `checkin.note`가 있을 때만 `typography.journalEntry` + `numberOfLines={1}` + `ellipsizeMode="tail"`로 메모 미리보기. 메모가 없으면 두 번째 `Text` 자체를 렌더하지 않는다(placeholder 텍스트 금지).
    - 장소명(`typography.placeName`)을 쓰지 않는다(D-01). 사진 유무 아이콘/뱃지를 만들지 않는다(D-02).
    - 스타일: `minHeight: LIST_ROW_MIN_HEIGHT`, `paddingHorizontal: spacing.lg`, `paddingVertical: spacing.sm`, `borderBottomWidth: 1`, `borderBottomColor: colors.line`, `flexDirection: 'row'`, `alignItems: 'center'`, 시간과 메모 사이 간격 `spacing.xs`. hex 리터럴 금지(foundation-wiring Test 5).
    - `typography.timestamp`의 `fontVariant`가 `readonly` 튜플이라 `StyleSheet.create` 안에서 타입 에러를 낸다 — 오늘 화면이 이미 쓰고 있는 얕은 복사 브리징 방식을 동일하게 적용한다(`as` 캐스트로 우회하지 않는다).

    `src/today/__tests__/todayUi.test.ts`를 `@jest-environment node` + `stripComments` 정적 분석 형태로 만들고(`src/components/__tests__/checkinCardUi.test.ts` 아날로그) `CheckinListRow` 계약을 단언한다: `Pressable`/`TouchableOpacity`/`onPress`/`chevron` 미등장, `typography.placeName` 미등장, `formatLocalTime` 참조 존재, `numberOfLines={1}` 존재, `typography.journalEntry`가 메모 미리보기 한 곳에만 등장, `colors.accent` 미등장, hex 리터럴 미등장.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/localDate.test.ts src/today/__tests__/todayUi.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').dependencies['@gorhom/bottom-sheet'])"` 가 `5.` 을 포함하는 버전 문자열을 출력한다
    - `npm ls @gorhom/bottom-sheet` 가 peer dependency 경고 없이 종료한다
    - `grep -c "hourCycle: 'h23'" src/checkin/localDate.ts` 가 1
    - `grep -c "getHours\|substring\|slice" src/checkin/localDate.ts` 가 0 — 수동 시각 산수 없음
    - `grep -v '^\s*//' src/today/CheckinListRow.tsx | grep -c "Pressable\|TouchableOpacity\|onPress"` 가 0
    - `grep -v '^\s*//' src/today/CheckinListRow.tsx | grep -c "typography.placeName"` 가 0
    - `grep -c "export const LIST_ROW_MIN_HEIGHT = 44" src/today/CheckinListRow.tsx` 가 1
    - `grep -c "emptyState" src/today/content.ts` 가 1
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/localDate.test.ts` 통과, 새 `formatLocalTime` 테스트 4개 green
    - `npm test` 전체 스위트 green (foundation-wiring Test 5 hex 금지 포함)
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>비인터랙티브 리스트 행이 시간+메모만으로 구성되고, 시각 포맷이 Intl 기반 단위 테스트로 검증됐다.</done>
</task>

<task type="auto">
  <name>Task 3: TodayBottomSheet — 3단 스냅 시트 + 리스트/빈 상태</name>
  <files>src/today/TodayBottomSheet.tsx, src/today/__tests__/todayUi.test.ts</files>
  <read_first>
    - node_modules/@gorhom/bottom-sheet 의 TypeScript 선언 파일 (설치된 v5의 정확한 prop 이름 — 특히 `animatedPosition`, `snapPoints`, `animationConfigs`, `handleStyle`, `handleIndicatorStyle`, `backgroundStyle`, `enablePanDownToClose`, 그리고 `BottomSheetFlatList` export 경로. 04-RESEARCH.md Open Question 2/Assumption A2가 "커뮤니티 예제가 v4/v5를 섞어 인용하므로 설치본 타입을 직접 확인하라"고 명시한 지점)
    - src/components/CheckinActionCard.tsx (하단 표면의 `card` 스타일 — `colors.surface` + 상단 모서리만 `radius.lg` + `spacing.lg` 패딩. 시트 표면이 이 시각 언어를 그대로 승계한다)
    - src/today/CheckinListRow.tsx (Task 2 산출물 — `LIST_ROW_MIN_HEIGHT` import)
    - src/today/content.ts (`TODAY_COPY.emptyState`)
    - .planning/phases/04-today-view/04-UI-SPEC.md §바텀시트 (@gorhom/bottom-sheet) — 핸들/표면/CLOSED/DRAGGING/OPEN/애니메이션/리스트 렌더링 6개 항목 전부
    - .planning/phases/04-today-view/04-RESEARCH.md §Anti-Patterns to Avoid (플레인 FlatList 금지, 중첩 GestureHandlerRootView 금지)
  </read_first>
  <action>
    `src/today/TodayBottomSheet.tsx`를 만든다. `<interfaces>`에 적힌 props 시그니처를 정확히 구현한다.

    구현 전 반드시 `node_modules/@gorhom/bottom-sheet`의 타입 선언을 읽어 v5의 실제 prop 이름을 확인한다. 아래 설계는 그 확인 결과에 맞춰 구현하되, 의미(계약)는 바꾸지 않는다.

    스냅 지점: 숫자 2개짜리 배열을 쓴다.
    - CLOSED 피크 = `spacing.sm * 2 + SHEET_HANDLE_HEIGHT + LIST_ROW_MIN_HEIGHT`. `SHEET_HANDLE_HEIGHT = 4`를 이 파일에 상수로 선언하고 주석에 04-UI-SPEC.md의 "핸들 영역 + 리스트 행 1개 = 약 80px" 근거를 남긴다. 리터럴 80을 하드코딩하지 않는다 — 토큰에서 파생한다.
    - OPEN = `containerHeight - insets.top - spacing.xl`. `useSafeAreaInsets()`로 상단 인셋을 읽는다. 이 값이 "OPEN 상태에서도 상단에 지도가 항상 보인다"(04-UI-SPEC.md: 100% 풀스크린 시트를 만들지 않는다)를 구현한다.
    - `containerHeight <= 0`이면 `null`을 반환해 레이아웃 측정 전 렌더를 피한다.
    - 스냅 지점 배열은 `useMemo`로 안정화한다(매 렌더 새 배열을 넘기면 시트가 재계산된다).

    DRAGGING 상태는 별도 스냅 지점이 아니라 두 스냅 사이의 연속 구간이다 — 부모가 넘긴 `animatedPosition` shared value를 시트에 그대로 전달해 드래그 중 실시간 추적이 가능하게 한다. `onChange(index)` 콜백에만 의존하는 구현을 하지 않는다(스냅 도달 시점에만 발화해 버튼이 튀게 된다 — D-05가 요구하는 "실시간 추적"과 배치된다).

    애니메이션: `motion.bottomSheetSnapMs`(220)를 시트의 애니메이션 설정에 전달한다. 새 모션 상수를 발명하지 않는다.

    시각 스타일(전부 토큰만 사용, hex 금지):
    - 표면: `colors.surface` 배경 + 상단 모서리만 `radius.lg`(하단 모서리 없음 — 탭바에 맞닿는다).
    - 핸들 영역: 상하 `spacing.sm` 여백, 배경 `colors.surface`, 상단 모서리 `radius.lg`.
    - 핸들 인디케이터: 너비 36, 높이 `SHEET_HANDLE_HEIGHT`(4), `colors.line`, `radius.full`.
    - 핸들 드래그 히트 영역은 44pt 이상 확보한다(상하 `spacing.sm`×2 + 4 = 28이므로 `hitSlop` 또는 핸들 영역 최소 높이로 44pt를 만든다 — 04-UI-SPEC.md §Spacing Scale Exceptions).

    콘텐츠:
    - `checkins.length === 0`이면 `typography.helperText` + `colors.textMuted`로 `TODAY_COPY.emptyState` 한 줄을 시트 표면 안에 렌더한다(지도 위가 아니라 시트 표면 — 대비 보장). 문구 리터럴을 컴포넌트에 하드코딩하지 않는다.
    - 아니면 `BottomSheetFlatList`로 `checkins`를 렌더하고 `keyExtractor`는 `item.id`, `renderItem`은 `<CheckinListRow checkin={item} />`이다. **플레인 `FlatList`/`ScrollView`를 쓰지 않는다** — 시트의 팬 제스처와 스크롤 제스처가 터치 우선순위를 두고 싸운다(04-RESEARCH.md Anti-Pattern, HIGH confidence).
    - 리스트 최상단 고정 행("오늘 돌아보기")을 만들지 않는다 — Phase 7 소관이다.

    금지 사항:
    - 이 컴포넌트 안에서 `GestureHandlerRootView`를 렌더하지 않는다(앱 루트에 이미 정확히 하나 있고, 중첩은 알려진 제스처 버그 원인이다).
    - `position: 'absolute'`로 자기 위치를 지정하지 않는다(배치는 부모 책임 — 저장소 컴포넌트 계약).
    - SQL 문자열, `expo-*` 네이티브 패키지 직접 import를 두지 않는다.
    - `colors.accent`를 쓰지 않는다(accent 예산은 체크인 버튼 + 지도 마크에만).

    `src/today/__tests__/todayUi.test.ts`에 `TodayBottomSheet` 계약 describe를 추가한다: `BottomSheetFlatList` 사용 + 플레인 `<FlatList`/`<ScrollView` 미등장, `GestureHandlerRootView` 미등장, `motion.bottomSheetSnapMs` 참조 존재, `TODAY_COPY.emptyState` 참조 존재 + 문구 리터럴 미등장, `colors.surface`/`radius.lg` 표면 스타일 존재, `colors.accent` 미등장, `position: 'absolute'` 미등장, 리터럴 `80` 미등장(토큰 파생), `LIST_ROW_MIN_HEIGHT` import 존재. 모든 grep 계열 단언은 `stripComments` 적용본을 대상으로 한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/today/__tests__/todayUi.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/today/TodayBottomSheet.tsx` 가 존재하고 `TodayBottomSheet`와 `TodayBottomSheetProps`를 export한다
    - `grep -v '^\s*//' src/today/TodayBottomSheet.tsx | grep -c "BottomSheetFlatList"` 가 1 이상
    - `grep -v '^\s*//' src/today/TodayBottomSheet.tsx | grep -c "<FlatList\|<ScrollView\|GestureHandlerRootView"` 가 0
    - `grep -v '^\s*//' src/today/TodayBottomSheet.tsx | grep -c "motion.bottomSheetSnapMs"` 가 1 이상
    - `grep -v '^\s*//' src/today/TodayBottomSheet.tsx | grep -c "colors.accent"` 가 0
    - `grep -v '^\s*//' src/today/TodayBottomSheet.tsx | grep -c "position: 'absolute'"` 가 0
    - `grep -c "LIST_ROW_MIN_HEIGHT" src/today/TodayBottomSheet.tsx` 가 1 이상 — CLOSED 높이를 행 높이 상수에서 파생
    - `containerHeight`가 0 이하일 때 `null`을 반환하는 조기 반환이 존재한다
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/today/__tests__/todayUi.test.ts` 통과, `TodayBottomSheet` describe 아래 8개 이상 테스트 green
    - `npm test` 전체 스위트 green
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>부모가 넘긴 데이터/높이/공유값만으로 렌더되는 3단 스냅 시트가 완성되고, 제스처 충돌 안티패턴이 회귀 테스트로 차단됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → 앱 번들 | 새 서드파티 UI 패키지가 앱 코드에 편입되는 지점 |
| SQLite row(사용자 작성 메모) → 화면 텍스트 | 사용자가 입력한 임의 문자열이 리스트에 렌더됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-SC | Tampering | npm 설치 (`@gorhom/bottom-sheet`) | mitigate | 04-RESEARCH.md §Package Legitimacy Audit이 `[ASSUMED]` 태그를 남겼으므로 Task 1의 `gate="blocking-human"` 체크포인트가 설치 전 사람 확인을 강제한다(자동 승인 불가). slopcheck `OK` + 리포 provenance + peer 호환성은 연구 단계에서 이미 확인됨 |
| T-4-10 | Information Disclosure | 메모 미리보기가 긴 텍스트를 그대로 노출해 지도 위 다른 정보를 가림 | mitigate | `numberOfLines={1}` + `ellipsizeMode="tail"` 강제(테스트 게이트). RN `Text`는 문자열을 마크업으로 해석하지 않으므로 인젝션 벡터는 없다 |
| T-4-11 | Denial of Service | 시트 팬 제스처와 리스트 스크롤 제스처가 경합해 터치가 먹통이 됨 | mitigate | `BottomSheetFlatList` 강제 + 플레인 `FlatList`/`ScrollView` 미등장 + 중첩 `GestureHandlerRootView` 미등장을 테스트로 게이트. 실제 제스처 동작은 04-07 시뮬레이터/실기기 검증에서 확인 |
| T-4-12 | Tampering | 확정 문구가 컴포넌트에 중복 하드코딩돼 단일 출처(`TODAY_COPY`)와 드리프트 | mitigate | 문구 상수 참조 존재 + 리터럴 미등장을 동시에 단언(기존 `CheckinActionCard`/`CHECKIN_COPY` 규약 계승) |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/today src/checkin/localDate.test.ts` 전체 통과
- `npm test` 전체 스위트 green
- `npx tsc --noEmit` exit 0
- `src/app/(tabs)/index.tsx`는 이 plan에서 변경되지 않는다 — `git diff --name-only`에 등장하지 않아야 한다
</verification>

<success_criteria>
- 리스트 행이 시간(모노스페이스) + 메모 미리보기(세리프 이탤릭 1줄)로만 구성되고 장소명/사진 아이콘/탭 어포던스가 없다(D-01/D-02/D-03)
- 시트가 CLOSED/OPEN 두 스냅 지점과 220ms 애니메이션을 갖고, 드래그 중 위치를 부모가 읽을 수 있는 공유값으로 노출한다(D-05 준비)
- 체크인 0건일 때 확정된 빈 상태 문구가 시트 표면에 뜬다
- 시트가 `BottomSheetFlatList`를 쓰고 중첩 `GestureHandlerRootView`가 없다
- `@gorhom/bottom-sheet` 도입에 대한 사람의 명시적 승인이 기록됐다
</success_criteria>

<output>
Create `.planning/phases/04-today-view/04-04-SUMMARY.md` when done
</output>
