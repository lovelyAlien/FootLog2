---
phase: 04-today-view
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(tabs)/_layout.tsx
  - src/app/(tabs)/calendar.tsx
  - src/app/(tabs)/index.tsx
  - src/today/content.ts
  - src/app/__tests__/tabs-wiring.test.ts
  - src/app/__tests__/checkin-wiring.test.ts
  - src/app/__tests__/foundation-wiring.test.ts
  - src/app/__tests__/notification-wiring.test.ts
autonomous: true
requirements: [REQ-today-view, REQ-onboarding-empty-state]
user_setup: []

must_haves:
  truths:
    - "앱을 열면 화면 하단에 '오늘'/'캘린더' 2개 탭이 있는 상시 탭바가 보인다"
    - "캘린더 탭을 누르면 '캘린더는 곧 추가돼요' 한 줄만 있는 플레이스홀더 화면이 열린다"
    - "오늘 탭 화면은 Phase 3의 지도/체크인 동작을 그대로 유지한다(회귀 없음)"
    - "알림 priming 게이트와 '첫 체크인 탭 시점 위치 권한 요청' 흐름이 화면 이동 후에도 그대로 동작한다"
  artifacts:
    - path: "src/app/(tabs)/_layout.tsx"
      provides: "RootTabNavigator 셸(오늘/캘린더 2탭, 아이콘 없음, 토큰 기반 색상)"
      contains: "Tabs"
      min_lines: 20
    - path: "src/app/(tabs)/calendar.tsx"
      provides: "캘린더 탭 플레이스홀더 화면(D-07)"
      contains: "TODAY_COPY.calendarPlaceholder"
    - path: "src/app/(tabs)/index.tsx"
      provides: "오늘 탭 화면(Phase 3 지도 화면을 그대로 이동)"
      min_lines: 900
    - path: "src/today/content.ts"
      provides: "오늘 뷰/탭바 확정 문구 단일 출처"
      exports: ["TODAY_COPY"]
    - path: "src/app/__tests__/tabs-wiring.test.ts"
      provides: "탭 셸 UI 계약 회귀 가드(색상 오버라이드/아이콘 없음/햄버거 없음)"
  key_links:
    - from: "src/app/(tabs)/_layout.tsx"
      to: "src/theme/tokens.ts"
      via: "tabBarActiveTintColor/tabBarInactiveTintColor/tabBarStyle 오버라이드"
      pattern: "tabBarActiveTintColor"
    - from: "src/app/(tabs)/index.tsx"
      to: "/priming"
      via: "shouldShowPriming 시 <Redirect>"
      pattern: "<Redirect"
---

<objective>
`expo-router`의 `(tabs)` 라우트 그룹으로 하단 탭바 셸(오늘/캘린더)을 세우고, Phase 3의 임시 전체화면 지도 화면(`src/app/index.tsx`)을 오늘 탭 자리(`src/app/(tabs)/index.tsx`)로 옮긴다. 캘린더 탭은 안내 텍스트 한 줄짜리 플레이스홀더만 연결한다.

Purpose: 04-CONTEXT.md D-06 — 제품 문서가 `RootTabNavigator`를 이미 확정했고 오늘 뷰 레이아웃("하단은 상시 탭바")이 탭바 존재를 전제로 설계돼 있다. 이 셸을 세우지 않으면 뒤 plan들의 바텀시트/버튼 오프셋 좌표계 기준이 확정되지 않는다. D-09에 따라 탭바는 체크인 진행 중에도 항상 보이고 탭 가능해야 하므로, 탭바 레이어는 오늘 뷰 내부 상태와 무관한 상위 레이어여야 한다.
Output: 동작이 변하지 않은 채 라우트 구조만 바뀐 상태 + 새 탭 셸 + 플레이스홀더 화면. 바텀시트/핀/궤적선은 이 plan에서 만들지 않는다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-today-view/04-CONTEXT.md
@.planning/phases/04-today-view/04-RESEARCH.md
@.planning/phases/04-today-view/04-PATTERNS.md
@.planning/phases/04-today-view/04-UI-SPEC.md
</context>

<interfaces>
<!-- 이동 대상 파일이 현재 사용하는 상대 경로 import (전부 `../X` → `../../X` 로 한 단계 깊어진다) -->
`../theme/tokens`, `../notifications/permissions`, `../components/NotificationDeniedBanner`,
`../components/LocationDeniedBanner`, `../components/CheckinActionCard`, `../checkin/checkinFlow`,
`../checkin/permissions`, `../checkin/location`, `../checkin/draftRepo`, `../checkin/checkinRepo`,
`../checkin/deps`, `../checkin/config`, `../checkin/photos`, `../checkin/localDate`, `../db/schema`

<!-- 세 개의 기존 테스트 파일이 공통으로 쓰는 헬퍼 (전부 `src/app/__tests__/` 안에 있고 APP_DIR = src/app) -->
`const APP_DIR = path.join(__dirname, '..');`
`function readSource(relativePath) { return fs.readFileSync(path.join(APP_DIR, relativePath), 'utf-8'); }`
`readSource('index.tsx')` ← 이 호출들이 전부 `(tabs)/index.tsx`를 가리키도록 바뀌어야 한다.

<!-- 기존 테스트 중 이동/신규 코드가 계속 통과시켜야 하는 핵심 게이트 -->
- foundation-wiring Test 5: `src/` 전체 non-test 파일에 hex 컬러 리터럴(`#RRGGBB`) 금지 → 새 파일은 `src/theme/tokens.ts`만 참조
- foundation-wiring Test 6: 오늘 화면의 `colors.accent` 출현 5회 이하
- foundation-wiring Test 7: 오늘 화면에 진행률 패턴(`\d+\s*/\s*\d+`) 금지
- checkin-wiring Test 9/39: 오늘 화면이 `react-native-gesture-handler`를 직접 import하지 않는다
- checkin-wiring Test 12: 오늘 화면에 SQL 키워드 금지
- checkin-wiring Test 53: `await waitForMapReady();` 뒤 `animateToRegion(` 호출이 정확히 6개
- notification-wiring Test 7: 오늘 화면에 `<Redirect`와 `/priming`이 존재
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: (tabs) 라우트 그룹 + 탭 레이아웃 + 캘린더 플레이스홀더</name>
  <files>src/app/(tabs)/_layout.tsx, src/app/(tabs)/calendar.tsx, src/today/content.ts</files>
  <read_first>
    - src/app/_layout.tsx (루트 Stack 구성 — `GestureHandlerRootView` → `SafeAreaProvider` → `SQLiteProvider` → `Stack screenOptions={{ headerShown: false }}`. 이 파일은 이번 task에서 수정하지 않는다)
    - src/app/priming.tsx (가장 단순한 전체화면 컴포넌트의 컨테이너/텍스트 구조 아날로그)
    - src/theme/tokens.ts (`colors.surface`, `colors.line`, `colors.textPrimary`, `colors.textMuted`, `colors.background`, `typography.helperText`의 정확한 이름)
    - src/notifications/content.ts (도메인 문구 상수 파일의 헤더 주석/구조 규약 — `TODAY_COPY`가 따를 형태)
    - .planning/phases/04-today-view/04-UI-SPEC.md §네비게이션 탭바, §Copywriting Contract
  </read_first>
  <action>
    `src/today/content.ts`를 만들어 오늘 뷰/탭바의 확정 문구 단일 출처 `TODAY_COPY`를 export한다. 이번 task에서 필요한 키 3개: `tabToday: '오늘'`, `tabCalendar: '캘린더'`, `calendarPlaceholder: '캘린더는 곧 추가돼요'`. `src/notifications/content.ts`/`CHECKIN_COPY`와 동일 규약으로 "문구를 여기서 발명하지 않는다 — 출처는 04-UI-SPEC.md §Copywriting Contract" 헤더 주석을 남기고 `as const`로 고정한다.

    `src/app/(tabs)/_layout.tsx`를 만든다. `expo-router`의 `Tabs`를 default export 컴포넌트에서 렌더하고 `screenOptions`에 다음을 **명시적으로** 지정한다:
    - `headerShown: false`
    - `tabBarActiveTintColor: colors.textPrimary`
    - `tabBarInactiveTintColor: colors.textMuted`
    - `tabBarStyle: { backgroundColor: colors.surface, borderTopColor: colors.line, borderTopWidth: 1 }`
    자식으로 `<Tabs.Screen name="index" options={{ title: TODAY_COPY.tabToday }} />`와 `<Tabs.Screen name="calendar" options={{ title: TODAY_COPY.tabCalendar }} />` 두 개만 둔다. `tabBarIcon`을 **절대 지정하지 않는다**(04-UI-SPEC.md: 아이콘 없이 라벨만 — 새 SF Symbol 선택이라는 디자인 결정을 이 phase에서 만들지 않기 위한 보수적 기본값).

    색상 오버라이드가 선택 사항이 아닌 이유를 파일 상단 주석으로 남긴다: 미지정 시 활성 탭 틴트가 iOS 시스템 파란색으로 렌더되어 DESIGN.md의 "액센트 1개(올리브그린), 빨강/파랑 계열 배제" 원칙과 정면 충돌한다. 또한 `colors.accent`를 탭 강조에 쓰지 않는다 — accent 예산은 체크인 버튼과 지도 마크에만 쓴다.

    `src/app/(tabs)/calendar.tsx`를 만든다. `flex: 1`, 중앙 정렬(`alignItems`/`justifyContent: 'center'`), `backgroundColor: colors.background`인 `View` 안에 `typography.helperText` + `colors.textMuted`로 `TODAY_COPY.calendarPlaceholder` 한 줄만 렌더한다. D-07에 따라 새 컴포넌트/아이콘/버튼/인터랙션을 만들지 않는다. 월 그리드·날짜 스크러버·과거 날짜 뷰는 Phase 6 소관이므로 관련 식별자나 자리표시 마크업을 두지 않는다.

    D-08: 오늘 뷰 상단 햄버거(≡) 아이콘과 설정 라우트는 이 phase에서 만들지 않는다 — `(tabs)/_layout.tsx`에 세 번째 `Tabs.Screen`이나 `headerRight`를 두지 않는다.

    `src/app/_layout.tsx`는 수정하지 않는다 — `expo-router`가 `(tabs)` 그룹을 루트 `Stack`의 화면으로 자동 인식하고, `priming.tsx`는 `(tabs)` 밖 형제 라우트로 남아 `<Redirect href="/priming">`이 계속 해석된다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(tabs)/_layout.tsx`와 `src/app/(tabs)/calendar.tsx`가 존재한다
    - `grep -c "tabBarActiveTintColor: colors.textPrimary" "src/app/(tabs)/_layout.tsx"` 가 1
    - `grep -c "tabBarInactiveTintColor: colors.textMuted" "src/app/(tabs)/_layout.tsx"` 가 1
    - `grep -c "tabBarIcon" "src/app/(tabs)/_layout.tsx"` 가 0
    - `grep -c "colors.accent" "src/app/(tabs)/_layout.tsx"` 가 0
    - `grep -c "Tabs.Screen" "src/app/(tabs)/_layout.tsx"` 가 정확히 2
    - `grep -c "#" "src/app/(tabs)/_layout.tsx" "src/app/(tabs)/calendar.tsx"` 에서 hex 컬러 리터럴이 나오지 않는다(foundation-wiring Test 5 통과로 확인)
    - `grep -c "TODAY_COPY.calendarPlaceholder" "src/app/(tabs)/calendar.tsx"` 가 1이고, 같은 파일에 문구 리터럴 `캘린더는 곧 추가돼요`가 직접 하드코딩되어 있지 않다
    - `git diff --name-only src/app/_layout.tsx` 가 비어 있다 — 루트 레이아웃 미변경
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>오늘/캘린더 2탭 셸과 플레이스홀더 화면이 존재하고, 탭바 색상이 전부 디자인 토큰으로 오버라이드됐다.</done>
</task>

<task type="auto">
  <name>Task 2: 오늘 화면을 (tabs)/index.tsx로 이동 + import·테스트 경로 갱신</name>
  <files>src/app/(tabs)/index.tsx, src/app/__tests__/checkin-wiring.test.ts, src/app/__tests__/foundation-wiring.test.ts, src/app/__tests__/notification-wiring.test.ts</files>
  <read_first>
    - src/app/index.tsx (이동 대상 — 최소한 상단 import 블록 21~76번째 줄과 파일 헤더 주석 1~20번째 줄. 컴포넌트 본문 로직은 이 task에서 변경하지 않으므로 전체를 정독할 필요는 없다)
    - src/app/__tests__/checkin-wiring.test.ts (`APP_DIR`/`readSource` 헬퍼와 `readSource('index.tsx')` 호출 지점 전부)
    - src/app/__tests__/foundation-wiring.test.ts (동일 헬퍼 + Test 5의 `collectSourceFiles(SRC_DIR)` hex 스캔)
    - src/app/__tests__/notification-wiring.test.ts (동일 헬퍼 + Test 7의 `<Redirect`//`/priming` 단언)
    - .planning/phases/04-today-view/04-RESEARCH.md §Common Pitfalls Pitfall 4 (typedRoutes 캐시 — 이동 직후 라우트 타입이 오래된 상태일 수 있음)
  </read_first>
  <action>
    `git mv src/app/index.tsx "src/app/(tabs)/index.tsx"` 로 파일을 이동한다(내용을 새로 작성하지 말 것 — 히스토리를 잃지 않도록 이동으로 처리한다).

    이동한 파일의 **상대 경로 import만** 한 단계 깊게 고친다: 모든 `from '../X'`를 `from '../../X'`로 바꾼다. 대상은 위 `<interfaces>`에 나열된 15개 모듈 경로다. `expo-router`/`expo-sqlite`/`react-native`/`react-native-maps`/`react-native-safe-area-context`/`expo-symbols` 같은 패키지 import는 그대로 둔다. **컴포넌트 로직·스타일·훅 순서는 한 줄도 바꾸지 않는다** — 이 task는 순수 이동이다.

    파일 헤더 주석(1~20번째 줄)을 현재 상태에 맞게 갱신한다: "Phase 4가 이 자리를 채울 예정" → "Phase 4가 이 화면을 `(tabs)` 그룹의 오늘 탭으로 승격했다"로 바꾸고, 배너 스택 위치에 관한 문장은 04-UI-SPEC.md의 확정(배너는 지도 상단·세이프에어리어 아래에 그대로 유지, 탭바 위로 이관하지 않음)에 맞춰 정정한다. 지도 스타일 토큰 결정(customMapStyle/PROVIDER_GOOGLE 미사용)과 accent 예산에 관한 문장은 그대로 유지한다.

    세 개의 기존 테스트 파일에서 오늘 화면을 읽는 경로를 갱신한다. 각 파일 상단에 `const TODAY_SCREEN_PATH = path.join('(tabs)', 'index.tsx');`를 두고, 모든 `readSource('index.tsx')` 호출을 `readSource(TODAY_SCREEN_PATH)`로 바꾼다. `describe` 제목의 `src/app/index.tsx` 문자열도 `src/app/(tabs)/index.tsx`로 갱신한다. `APP_DIR`/`SRC_DIR`/`collectSourceFiles`/`isExcludedFromWiringContract` 헬퍼 자체는 바꾸지 않는다 — 이동한 파일도 `SRC_DIR` 재귀 스캔 범위 안에 그대로 들어온다.

    `notification-wiring.test.ts`의 "priming 라우트 존재 회귀 가드"(Test 10, `path.join(APP_DIR, 'priming.tsx')`)는 그대로 둔다 — `priming.tsx`는 `(tabs)` 밖 형제로 남는다.

    이동 후 `npx tsc --noEmit`을 실행한다. `expo-router` typedRoutes가 생성한 `.expo/types/router.d.ts`가 이전 파일 트리를 캐시하고 있어 라우트 타입 에러가 나올 수 있다(04-RESEARCH.md Pitfall 4). 그 경우 `npx expo start` 등으로 개발 서버를 한 번 띄워 타입을 재생성한 뒤 다시 검사한다. 재생성 후에도 `/priming`이 유효한 href로 남아 있는지 `.expo/types/router.d.ts`에서 확인한다.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/app/index.tsx` 가 참이고 `test -f "src/app/(tabs)/index.tsx"` 가 참이다
    - `git log --follow --oneline -1 -- "src/app/(tabs)/index.tsx"` 가 이동 이전 커밋 히스토리를 따라간다(rename으로 기록됨)
    - `grep -c "from '\.\./[a-z]" "src/app/(tabs)/index.tsx"` 가 0 — 한 단계짜리 상대 경로가 남아 있지 않다
    - `grep -c "from '\.\./\.\./" "src/app/(tabs)/index.tsx"` 가 15 이상
    - `grep -rn "readSource('index.tsx')" src/app/__tests__/` 결과가 비어 있다
    - `.expo/types/router.d.ts` 에 `/priming` href가 여전히 존재한다
    - `npm test` 전체 스위트 green — 특히 checkin-wiring Test 53(`animateToRegion` 6개), Test 12(SQL 금지), notification-wiring Test 7(`<Redirect` + `/priming`), foundation-wiring Test 5(hex 금지)/Test 6(accent 5회 이하)이 통과
    - `git diff -M --stat` 에서 `(tabs)/index.tsx` 변경이 import 경로 줄과 헤더 주석에 한정된다(로직 줄 변경 없음)
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>오늘 화면이 `(tabs)` 그룹 안으로 이동했고, 기존 배선 회귀 가드 100여 개가 새 경로에서 그대로 통과한다.</done>
</task>

<task type="auto">
  <name>Task 3: 탭 셸 UI 계약 회귀 가드 신규 테스트</name>
  <files>src/app/__tests__/tabs-wiring.test.ts</files>
  <read_first>
    - src/app/__tests__/notification-wiring.test.ts (정적 소스 분석 테스트의 헤더/`@jest-environment node`/`stripComments` 사용 형태)
    - src/components/__tests__/checkinCardUi.test.ts (UI 계약 테스트에서 "확정 문구 상수 참조 + 리터럴 중복 하드코딩 금지"를 함께 단언하는 패턴)
    - src/app/(tabs)/_layout.tsx, src/app/(tabs)/calendar.tsx, src/today/content.ts (Task 1 산출물)
    - .planning/phases/04-today-view/04-UI-SPEC.md §네비게이션 탭바, §Scope Boundary "이 phase가 만들지 않는 것"
  </read_first>
  <action>
    `src/app/__tests__/tabs-wiring.test.ts`를 `@jest-environment node` 헤더로 만들고, `fs.readFileSync` + `stripComments`로 아래 계약을 단언한다(RN 렌더 환경 불필요 — 저장소의 기존 배선 테스트와 동일 기법).

    라우트 구조:
    - `src/app/(tabs)/_layout.tsx`, `src/app/(tabs)/index.tsx`, `src/app/(tabs)/calendar.tsx`가 전부 존재한다.
    - `src/app/index.tsx`가 더 이상 존재하지 않는다(이동 완료 회귀 가드).
    - `src/app/priming.tsx`가 `(tabs)` 밖에 그대로 존재한다.

    탭바 계약(D-06/D-07, 04-UI-SPEC.md):
    - `_layout.tsx`가 `expo-router`에서 `Tabs`를 import하고 렌더한다.
    - `Tabs.Screen`이 정확히 2개이며 `name="index"`와 `name="calendar"`다.
    - 탭 제목이 `TODAY_COPY.tabToday`/`TODAY_COPY.tabCalendar` 상수 참조로 지정되고, 문구 리터럴 `'오늘'`/`'캘린더'`가 `_layout.tsx`에 직접 하드코딩되지 않는다.
    - `tabBarActiveTintColor`가 `colors.textPrimary`, `tabBarInactiveTintColor`가 `colors.textMuted`로 지정된다(iOS 기본 파란 틴트 회귀 가드).
    - `tabBarStyle`이 `colors.surface` 배경과 `colors.line` 상단 보더를 쓴다.
    - `tabBarIcon`이 등장하지 않는다.
    - `colors.accent`가 등장하지 않는다(accent 예산 확장 금지).

    캘린더 플레이스홀더 계약(D-07):
    - `calendar.tsx`가 `TODAY_COPY.calendarPlaceholder`를 참조하고 문구 리터럴을 하드코딩하지 않는다.
    - `typography.helperText`와 `colors.textMuted`를 쓴다.
    - Phase 6 범위 식별자(`Pressable`, `TouchableOpacity`, `FlatList`, `SectionList`, `useState`)가 등장하지 않는다 — 인터랙션 없는 정적 한 줄 화면임을 강제.

    스코프 경계 계약(D-08):
    - `(tabs)/index.tsx`와 `(tabs)/_layout.tsx` 어디에도 설정 진입점 관련 식별자(`headerRight`, `settings`, `Settings`)와 햄버거 문자(`≡`)가 등장하지 않는다.

    문구 단일 출처 계약:
    - `src/today/content.ts`가 `TODAY_COPY`를 `as const`로 export하고 위 3개 키를 갖는다.

    grep 계열 단언을 쓸 때는 주석이 카운트를 오염시키지 않도록 반드시 `stripComments` 적용본(`codeOnly`)을 대상으로 한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/tabs-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/__tests__/tabs-wiring.test.ts` 가 존재하고 `@jest-environment node` 헤더를 갖는다
    - 위 5개 계약 그룹(라우트 구조 / 탭바 / 캘린더 플레이스홀더 / 스코프 경계 / 문구 단일 출처)에 각각 1개 이상의 `it`이 존재한다
    - `src/app/index.tsx` 부재를 단언하는 테스트가 존재한다
    - `tabBarIcon` 미등장을 단언하는 테스트가 존재한다
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/app/__tests__/tabs-wiring.test.ts` 통과, 10개 이상 테스트 green
    - `npm test` 전체 스위트 green
  </acceptance_criteria>
  <done>탭 셸의 색상 오버라이드·아이콘 없음·햄버거 없음·문구 단일 출처가 회귀 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 라우트 파일 트리 → 네비게이션 상태 | 파일 위치가 곧 URL이며 typedRoutes 타입의 근거 |
| 네비게이션 기본 테마 → 화면 색상 | 지정하지 않은 값이 라이브러리 기본값(iOS 시스템 블루)으로 렌더됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-07 | Tampering | 파일 이동이 Phase 3 배선 회귀 가드를 우회(테스트가 사라진 경로를 읽고 조용히 skip/실패) | mitigate | 세 테스트 파일의 경로 상수를 명시적으로 갱신하고, `npm test` 전체 green + `readSource('index.tsx')` 잔존 0을 acceptance criteria로 게이트 |
| T-4-08 | Denial of Service | typedRoutes 캐시가 이동 전 트리를 들고 있어 `/priming` 리다이렉트가 런타임에 깨짐 | mitigate | 타입 재생성 후 `.expo/types/router.d.ts`에 `/priming` 존재 확인 + notification-wiring Test 7 통과를 게이트 |
| T-4-09 | Elevation of Privilege | 아직 존재하지 않는 설정 화면 진입점(햄버거)이 노출돼 죽은 링크/미구현 화면으로 이동 | mitigate | D-08에 따라 진입점 자체를 만들지 않고, `headerRight`/`settings`/`≡` 미등장을 tabs-wiring 테스트로 강제 |
| T-4-SC | Tampering | npm/pip/cargo 설치 | accept | 이 plan은 새 패키지를 설치하지 않는다 — `expo-router`의 `Tabs`는 기설치된 `expo-router@~57.0.16`의 일부다(04-RESEARCH.md §Environment Availability) |
</threat_model>

<verification>
- `npm test` 전체 스위트 green (기존 회귀 가드 전부 새 경로에서 통과)
- `npx tsc --noEmit` exit 0
- `.expo/types/router.d.ts` 에 `/priming` href 존재
- `git status`에 `src/app/index.tsx` 삭제 + `src/app/(tabs)/index.tsx` 추가가 rename으로 기록됨
</verification>

<success_criteria>
- 오늘/캘린더 2탭 셸이 존재하고 탭바 색상이 전부 디자인 토큰으로 지정됐다(iOS 기본 파란색 없음)
- 캘린더 탭에 안내 텍스트 한 줄만 있는 플레이스홀더 화면이 연결됐다
- Phase 3 오늘 화면의 동작/테스트가 하나도 깨지지 않은 채 라우트 위치만 바뀌었다
- 알림 priming 리다이렉트와 "첫 체크인 탭 시점 위치 권한 요청" 경계가 그대로 유지된다(REQ-onboarding-empty-state)
- 설정 화면 진입점(햄버거)은 만들어지지 않았다(D-08)
</success_criteria>

<output>
Create `.planning/phases/04-today-view/04-03-SUMMARY.md` when done
</output>
