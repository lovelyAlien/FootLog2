---
phase: 04-today-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations.ts
  - src/db/testing/nodeSqliteAdapter.ts
  - src/db/testing/nodeSqliteAdapter.test.ts
  - src/checkin/checkinRepo.ts
  - src/checkin/checkinRepo.test.ts
  - src/today/trajectory.ts
  - src/today/trajectory.test.ts
autonomous: true
requirements: [REQ-today-view, REQ-trajectory-line]
user_setup: []

must_haves:
  truths:
    - "오늘 날짜(local_date_key)의 체크인 전체를 시간순(timestamp_utc 오름차순)으로 한 번의 조회로 얻을 수 있다"
    - "리스트와 지도 핀이 소비할 조회 함수가 정확히 하나만 존재한다(D-11)"
    - "오늘 체크인이 0건 또는 1건이면 궤적선 좌표가 만들어지지 않는다"
  artifacts:
    - path: "src/db/migrations.ts"
      provides: "MigratableDb 타입에 getAllAsync 포함(다중 row 조회 가능)"
      contains: "getAllAsync"
    - path: "src/db/testing/nodeSqliteAdapter.ts"
      provides: "node:sqlite 기반 getAllAsync 구현(실엔진 테스트 가능)"
      contains: "getAllAsync"
    - path: "src/checkin/checkinRepo.ts"
      provides: "getTodayCheckins(db, localDateKey) 단일 공유 쿼리"
      exports: ["getTodayCheckins"]
    - path: "src/today/trajectory.ts"
      provides: "CheckinRow[] → Polyline 좌표 파생(순수 함수)"
      exports: ["buildTrajectoryCoordinates"]
  key_links:
    - from: "src/checkin/checkinRepo.ts"
      to: "MigratableDb.getAllAsync"
      via: "파라미터 바인딩(?) 다중 row 조회"
      pattern: "getAllAsync<CheckinRow>"
    - from: "src/today/trajectory.ts"
      to: "src/db/schema.ts CheckinRow"
      via: "type-only import"
      pattern: "import type \\{ CheckinRow \\}"
---

<objective>
오늘 뷰(지도 핀 + 바텀시트 리스트 + 궤적선)가 공유할 데이터 레이어를 만든다: `MigratableDb`에 다중 row 조회(`getAllAsync`)를 열고, `getTodayCheckins`를 `checkinRepo.ts`에 추가하고, 조회 결과를 Polyline 좌표로 바꾸는 순수 함수를 만든다.

Purpose: 04-CONTEXT.md D-11 — "오늘 저장된 체크인들을 조회해 지도에 다시 그리는" 로직은 이 코드베이스에 아직 없다(`getLatestCheckinCoordinate` 하나만 존재, 폴백 좌표용). 리스트와 지도 핀이 **같은 쿼리 결과 하나**를 공유해야 하므로 조회 함수는 정확히 하나만 만든다. 또한 `MigratableDb`가 현재 `getFirstAsync | execAsync | runAsync`로만 좁혀져 있어 다중 row 조회를 쓰면 타입 에러가 난다(04-RESEARCH.md Pitfall 3, 직접 코드 확인으로 검증됨).
Output: 화면 배선 없이도 `npm test`로 검증 가능한 조회/파생 로직 3종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-today-view/04-CONTEXT.md
@.planning/phases/04-today-view/04-RESEARCH.md
@.planning/phases/04-today-view/04-PATTERNS.md
@.planning/phases/04-today-view/04-VALIDATION.md
</context>

<interfaces>
<!-- 실행자가 코드베이스를 탐색하지 않아도 되도록 필요한 기존 계약만 옮겨둔다. -->

`src/db/migrations.ts` (현재 상태, line 19):
`export type MigratableDb = Pick<SQLiteDatabase, 'getFirstAsync' | 'execAsync' | 'runAsync'>;`

`src/db/schema.ts`가 이미 export하는 행 타입(재정의 금지, import해서 쓸 것):
`CheckinRow { id, timestamp_utc, local_date_key, timezone_at_capture, lat, lng, accuracy_meters, location_source, note, photo_path, created_at, updated_at, schema_version }`

`src/db/schema.ts`의 인덱스(이 plan의 WHERE 절이 사용):
`CREATE INDEX IF NOT EXISTS idx_checkins_local_date_key ON checkins(local_date_key);`

`src/db/testing/nodeSqliteAdapter.ts`의 기존 어댑터 메서드 3종(`execAsync`/`getFirstAsync`/`runAsync`)은 전부 `resolveBindArgs(params)` 정규화 헬퍼를 거친다 — 새 메서드도 같은 헬퍼를 재사용해야 한다(재구현 금지).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MigratableDb에 getAllAsync 추가 + node:sqlite 어댑터 구현</name>
  <files>src/db/migrations.ts, src/db/testing/nodeSqliteAdapter.ts, src/db/testing/nodeSqliteAdapter.test.ts</files>
  <read_first>
    - src/db/migrations.ts (현재 `MigratableDb` 정의와 `migrateDbIfNeeded` 본문 — 마이그레이션 블록은 손대지 않는다)
    - src/db/testing/nodeSqliteAdapter.ts (`resolveBindArgs` 헬퍼와 기존 3개 메서드의 정확한 시그니처/캐스트 관례)
    - src/db/testing/nodeSqliteAdapter.test.ts (기존 3개 테스트의 `createTestDb()` + try/finally close() 형태)
    - .planning/phases/04-today-view/04-RESEARCH.md §Common Pitfalls Pitfall 3
  </read_first>
  <behavior>
    - `getAllAsync`가 결과가 없으면 빈 배열 `[]`을 반환한다(null 아님).
    - `getAllAsync`가 여러 row를 SQL이 지정한 순서 그대로 배열로 반환한다.
    - `getAllAsync`가 배열 형태 바인드 파라미터(`['x']`)와 가변인자 형태(`'x'`) 둘 다에서 동일하게 동작한다(기존 `getFirstAsync`/`runAsync`와 같은 `resolveBindArgs` 계약).
  </behavior>
  <action>
    `src/db/migrations.ts` line 19의 `MigratableDb`를 `Pick<SQLiteDatabase, 'getFirstAsync' | 'execAsync' | 'runAsync' | 'getAllAsync'>`로 한 줄 넓힌다. `migrateDbIfNeeded` 본문, `DATABASE_VERSION`(현재 2), 버전별 마이그레이션 if 블록은 **절대 수정하지 않는다** — 이 파일에는 "이전 버전 블록은 사후 수정 금지"(migration_discipline #2) 규율이 명시돼 있고, 이 phase는 스키마 변경이 없다.

    `src/db/testing/nodeSqliteAdapter.ts`의 `adapter` 객체 리터럴에 네 번째 메서드 `getAllAsync`를 추가한다. 시그니처는 `async <T>(sql: string, ...params: unknown[]): Promise<T[]>`이고, 구현은 기존 `getFirstAsync`가 `raw.prepare(sql).get(...)`을 쓰는 것과 동일한 형태로 `raw.prepare(sql).all(...(resolveBindArgs(params) as never[]))`를 쓴 뒤 결과를 `T[]`로 반환한다. `resolveBindArgs`를 재구현하지 말고 기존 헬퍼를 그대로 호출한다. 파일 하단의 `db: adapter as unknown as MigratableDb` 단일 캐스트 경계는 그대로 유지한다.

    `src/db/testing/nodeSqliteAdapter.test.ts`에 `describe('getAllAsync', ...)` 블록을 추가하되 위 `<behavior>` 3가지를 각각 별도 `it`으로 작성한다. 기존 테스트와 동일하게 `createTestDb()` → `try { ... } finally { close(); }` 형태를 쓰고, 임시 테이블(`CREATE TABLE t (...)`)로 검증한다(마이그레이션 스키마에 의존하지 않는다).
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/db/testing/nodeSqliteAdapter.test.ts src/db/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getAllAsync" src/db/migrations.ts` 가 1 이상이고, 해당 줄이 `export type MigratableDb = Pick<SQLiteDatabase,` 로 시작하는 줄이다
    - `grep -v '^\s*//' src/db/migrations.ts | grep -c "DATABASE_VERSION = 2"` 가 1 — 스키마 버전이 변경되지 않았다
    - `git diff src/db/migrations.ts` 의 변경 줄 수가 1줄(`MigratableDb` 정의)뿐이다
    - `src/db/testing/nodeSqliteAdapter.ts`의 `getAllAsync` 구현이 `resolveBindArgs(params)`를 호출한다
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/db/testing/nodeSqliteAdapter.test.ts` 가 통과하고, 새 `getAllAsync` 테스트 3개가 실행 목록에 나타난다
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>다중 row 조회가 프로덕션 타입(`MigratableDb`)과 Node 테스트 어댑터 양쪽에서 가능해졌고, 마이그레이션 러너는 변경되지 않았다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getTodayCheckins — 리스트/지도 핀 공용 단일 쿼리</name>
  <files>src/checkin/checkinRepo.ts, src/checkin/checkinRepo.test.ts</files>
  <read_first>
    - src/checkin/checkinRepo.ts (`getLatestCheckinCoordinate`의 정확한 형태 — 이 함수가 shape 아날로그다. 파일 헤더의 "SQL문은 이 파일에만 존재한다" 규약도 확인)
    - src/checkin/checkinRepo.test.ts (`validParams()` 헬퍼, `createTestDb()` + `migrateDbIfNeeded(db)` + `commitCheckin(...)` 시딩 후 단언하는 Test 6/7 형태)
    - src/db/schema.ts (`CheckinRow` 필드 목록, `idx_checkins_local_date_key` 인덱스)
  </read_first>
  <behavior>
    - `getTodayCheckins(db, '2026-08-30')`이 `local_date_key = '2026-08-30'`인 row만 반환하고 다른 날짜 row는 제외한다.
    - 반환 배열이 `timestamp_utc` 오름차순이다 — 삽입 순서를 일부러 뒤섞어 시딩해도 시간순으로 나온다.
    - 해당 날짜에 체크인이 없으면 빈 배열을 반환한다(null 아님).
    - 반환된 row가 `CheckinRow` 전체 컬럼(`note`, `photo_path`, `lat`, `lng` 포함)을 담는다 — 리스트가 메모 미리보기를, 지도가 좌표를 같은 결과에서 읽어야 하기 때문(D-11).
  </behavior>
  <action>
    `src/checkin/checkinRepo.ts`에 `getTodayCheckins(db: MigratableDb, localDateKey: string): Promise<CheckinRow[]>`를 추가한다. 구현은 `db.getAllAsync<CheckinRow>('SELECT * FROM checkins WHERE local_date_key = ? ORDER BY timestamp_utc ASC', localDateKey)` 한 줄이다. `CheckinRow`는 `src/db/schema.ts`에서 `import type`으로 가져오고 재정의하지 않는다. 문자열 보간(`${...}`)을 절대 쓰지 않고 `?` 플레이스홀더 + 파라미터 바인딩만 쓴다(T-1-01 선례).

    `local_date_key`를 이 함수가 스스로 계산하지 않는다 — 호출자(화면)가 `resolveLocalDateKey(new Date())`로 만들어 넘긴다. 이 함수는 시각/타임존 판단을 하지 않는 순수 조회다.

    "오늘"이라는 이름이지만 임의의 날짜 키를 받는 시그니처를 유지한다 — Phase 6(캘린더 과거 날짜 뷰)이 같은 함수를 재사용할 수 있게 하기 위함이며, 이 plan에서 별도의 과거 날짜용 함수를 추가로 만들지 않는다.

    `src/checkin/checkinRepo.test.ts`에 `describe('getTodayCheckins', ...)` 블록을 추가한다. 기존 Test 6/7과 동일한 `createTestDb()` → `migrateDbIfNeeded(db)` → `commitCheckin(db, validParams({...}))` 시딩 형태를 쓰고, `validParams`의 `id`/`timestampUtc`/`localDateKey`를 override해 위 `<behavior>` 4가지를 각각 별도 `it`으로 검증한다. 정렬 검증 시에는 `timestampUtc`가 늦은 row를 먼저 insert해 "삽입 순서가 아니라 ORDER BY가 순서를 결정한다"를 실제로 확인한다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/checkinRepo.test.ts -t getTodayCheckins</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getTodayCheckins" src/checkin/checkinRepo.ts` 가 1 이상이고 `export async function getTodayCheckins` 형태로 선언된다
    - `src/checkin/checkinRepo.ts` 의 새 SQL 문자열이 `ORDER BY timestamp_utc ASC` 와 `WHERE local_date_key = ?` 를 포함한다
    - `grep -v '^\s*//' src/checkin/checkinRepo.ts | grep -c '\${'` 가 0 — SQL 문자열 보간 없음
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/checkin/checkinRepo.test.ts` 전체 통과, 새 `getTodayCheckins` describe 아래 4개 이상의 테스트가 green
    - 다른 날짜 row를 함께 시딩한 테스트가 존재하고, 그 row가 결과에 포함되지 않음을 단언한다
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>오늘 날짜의 체크인 전체를 시간순으로 반환하는 단일 조회 함수가 실 SQLite 엔진 테스트로 검증됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: buildTrajectoryCoordinates — 궤적선 좌표 파생(순수 함수)</name>
  <files>src/today/trajectory.ts, src/today/trajectory.test.ts</files>
  <read_first>
    - src/checkin/fallbackLocation.ts (좌표만 다루는 순수 모듈의 헤더 주석/export 형태 아날로그)
    - src/checkin/fallbackLocation.test.ts (`@jest-environment node` 헤더 + 표 기반 입출력 단언 형태)
    - src/db/schema.ts (`CheckinRow`의 `lat`/`lng` 필드명 — `latitude`/`longitude`가 아니다)
    - .planning/phases/04-today-view/04-UI-SPEC.md §이동 궤적선 (Polyline) (좌표 계약: 이미 정렬된 쿼리 결과를 그대로 잇는다, 0~1건이면 렌더 안 함)
  </read_first>
  <behavior>
    - 빈 배열 입력 → `[]` 반환.
    - 체크인 1건 입력 → `[]` 반환(선을 그릴 수 없음, REQ-trajectory-line).
    - 체크인 2건 이상 입력 → 입력과 같은 길이/같은 순서의 `{ latitude, longitude }` 배열 반환.
    - 입력 순서를 재정렬하지 않는다 — 일부러 timestamp가 역순인 배열을 넣어도 입력 순서 그대로 매핑된다(정렬 계약은 `getTodayCheckins`의 ORDER BY가 단독으로 소유한다).
  </behavior>
  <action>
    `src/today/` 디렉터리를 새로 만들고 `trajectory.ts`를 추가한다. `export type TrajectoryCoordinate = { latitude: number; longitude: number }`와 `export function buildTrajectoryCoordinates(checkins: CheckinRow[]): TrajectoryCoordinate[]`를 export한다. 구현은 길이가 2 미만이면 빈 배열을 반환하고, 아니면 각 row의 `lat`/`lng`를 `latitude`/`longitude`로 매핑한다(`react-native-maps`의 `Polyline` `coordinates` prop 형태).

    이 파일은 네이티브 모듈도 `react-native-maps`도 import하지 않는다 — `src/db/schema.ts`의 `CheckinRow`만 `import type`으로 가져오는 순수 모듈이어야 Node 테스트 환경에서 그대로 로드된다(`src/checkin/fallbackLocation.ts`와 동일 성격).

    파일 상단 주석에 "정렬은 `getTodayCheckins`의 `ORDER BY timestamp_utc ASC`가 단독으로 책임진다 — 여기서 다시 정렬하지 않는다(D-11 단일 쿼리 계약)"를 명시한다.

    `src/today/trajectory.test.ts`를 `@jest-environment node` 헤더로 만들고 위 `<behavior>` 4가지를 각각 별도 `it`으로 작성한다. `CheckinRow` 픽스처는 테스트 파일 안 로컬 헬퍼(`makeRow(overrides)`)로 만든다.
  </action>
  <verify>
    <automated>NODE_OPTIONS=--experimental-sqlite npx jest src/today/trajectory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/today/trajectory.ts` 가 존재하고 `buildTrajectoryCoordinates`와 `TrajectoryCoordinate`를 export한다
    - `grep -c "react-native-maps\|expo-" src/today/trajectory.ts` 가 0 — 순수 모듈
    - `grep -c "sort\|ORDER BY" src/today/trajectory.ts` 가 0 — 재정렬/SQL 없음
    - `NODE_OPTIONS=--experimental-sqlite npx jest src/today/trajectory.test.ts` 통과, 4개 이상 테스트 green
    - 1건 입력이 `[]`를 반환함을 단언하는 테스트가 존재한다
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>궤적선 좌표 파생이 지도/RN 없이 단위 테스트로 완전히 검증된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 화면(UI) → SQLite | 화면이 넘긴 `localDateKey` 문자열이 SQL WHERE 절에 도달 |
| 테스트 어댑터 → 프로덕션 타입 | `nodeSqliteAdapter`가 `MigratableDb`를 구조적으로 만족한다고 주장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | `getTodayCheckins`의 SQL 문자열 | mitigate | `?` 플레이스홀더 + 파라미터 바인딩만 사용, 템플릿 보간 금지 — acceptance criteria가 `${` 출현 0을 게이트로 강제(T-1-01 선례 계승) |
| T-4-02 | Tampering | `MigratableDb` 확장이 마이그레이션 러너에 부수 변경을 유발 | mitigate | `git diff src/db/migrations.ts` 변경 줄 수 1줄, `DATABASE_VERSION = 2` 불변을 acceptance criteria로 게이트 |
| T-4-03 | Spoofing | 테스트 어댑터가 실제 expo-sqlite와 다르게 동작(테스트-실기기 불일치) | mitigate | `getAllAsync`도 기존 3개 메서드와 동일한 `resolveBindArgs` 정규화를 재사용하도록 강제(재구현 금지) — `lastInsertRowId` 불일치 선례와 같은 계열의 함정 방어 |
| T-4-SC | Tampering | npm/pip/cargo 설치 | accept | 이 plan은 새 패키지를 설치하지 않는다(04-RESEARCH.md §Environment Availability — 필요한 것 전부 기설치) |
</threat_model>

<verification>
- `NODE_OPTIONS=--experimental-sqlite npx jest src/db src/checkin/checkinRepo.test.ts src/today` 전체 통과
- `npx tsc --noEmit` exit 0
- `npm test` 전체 스위트 green (기존 마이그레이션/체크인 테스트 회귀 없음)
</verification>

<success_criteria>
- 리스트와 지도 핀이 공유할 조회 함수가 `checkinRepo.ts`에 정확히 하나 존재한다(D-11)
- 그 함수가 실제 SQLite 엔진 대상 테스트로 필터링/정렬/빈 결과를 검증받았다
- 궤적선 좌표 파생이 0/1건 케이스를 포함해 순수 함수로 검증됐다
- 스키마 버전과 마이그레이션 블록이 변경되지 않았다
</success_criteria>

<output>
Create `.planning/phases/04-today-view/04-01-SUMMARY.md` when done
</output>
