---
phase: 02-notification-infrastructure
plan: 06
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/app/priming.tsx
  - src/components/NotificationDeniedBanner.tsx
  - src/components/__tests__/notificationUi.test.ts
autonomous: true
requirements: [REQ-permission-copy, REQ-notification-denied-flow]
user_setup: []

must_haves:
  truths:
    - "사용자가 OS 알림 다이얼로그가 뜨기 전에 확정된 한국어 안내 문구를 본다"
    - "사용자가 priming 화면에서 '나중에'를 눌러도 앱 사용이 막히지 않는다"
    - "알림 권한이 거부되면 조용한 상태 배너와 설정 딥링크를 본다"
    - "배너가 재사용 가능한 독립 컴포넌트라 Phase 4가 import만으로 오늘 뷰 상단에 재배치할 수 있다"
  artifacts:
    - path: "src/app/priming.tsx"
      provides: "알림 priming 전체화면 라우트 (/priming)"
      min_lines: 60
    - path: "src/components/NotificationDeniedBanner.tsx"
      provides: "재사용 가능한 알림 거부 상태 배너 + 설정 딥링크"
      min_lines: 35
    - path: "src/components/__tests__/notificationUi.test.ts"
      provides: "확정 카피 + 디자인 토큰 규율 + 접근성 최소 기준 회귀 가드"
  key_links:
    - from: "src/app/priming.tsx"
      to: "src/notifications/permissions.ts"
      via: "requestNotificationPermission / markPrimingDismissed"
      pattern: "requestNotificationPermission"
    - from: "src/components/NotificationDeniedBanner.tsx"
      to: "src/notifications/permissions.ts"
      via: "useNotificationPermissionBanner 훅"
      pattern: "useNotificationPermissionBanner"
---

<objective>
Phase 2의 두 UI 표면을 만든다: OS 알림 다이얼로그 전에 뜨는 priming 전체화면 라우트, 그리고 권한 거부 시 뜨는 조용한 상태 배너(재사용 가능한 독립 컴포넌트).

Purpose: REQ-notification-denied-flow의 UI 절반(priming → OS 프롬프트 → 배너 → 설정 딥링크)과 REQ-permission-copy의 알림 항목(D-05 — iOS에 Info.plist 키가 없어 priming 문구가 그 역할을 대신함)을 충족한다. 배선(어느 화면에서 언제 보여줄지)은 Plan 07이 맡고, 여기서는 화면/컴포넌트 자체만 만든다.
Output: `/priming` 라우트 + `NotificationDeniedBanner` 컴포넌트 + UI 계약 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-notification-infrastructure/02-CONTEXT.md
@.planning/phases/02-notification-infrastructure/02-UI-SPEC.md
@.planning/phases/02-notification-infrastructure/02-RESEARCH.md
@.planning/phases/02-notification-infrastructure/02-PATTERNS.md
@.planning/phases/02-notification-infrastructure/02-04-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- Plan 01 디자인 토큰 (Phase 1 산출물 — 값을 새로 발명하지 않는다) -->
src/theme/tokens.ts
  colors: { background:'#F4F1EA', surface:'#FBFAF6', surfaceSoft, textPrimary:'#2F302C', textMuted:'#79786F', textFaint, accent, accentSoft, line, mapLand, mapRoad, mapWater }
  typography: { screenTitle, placeName, timestamp, journalEntry, helperText }
  spacing: { '2xs':4, xs:8, sm:12, md:16, lg:24, xl:32, '2xl':48, '3xl':64 }
  radius: { sm:4, md:8, lg:16, full:9999 }

<!-- Plan 04가 만든 계약 (그대로 소비) -->
src/notifications/permissions.ts
  export async function requestNotificationPermission(deps?): Promise<PermissionSnapshot>;
  export function markPrimingDismissed(): void;
  export function useNotificationPermissionBanner(): { showBanner: boolean; openSettings: () => void };

<!-- 이 plan이 새로 만드는 계약 (Plan 07이 소비) -->
src/app/priming.tsx
  export default function PrimingScreen(): JSX.Element;      // expo-router 라우트 '/priming'
src/components/NotificationDeniedBanner.tsx
  export function NotificationDeniedBanner(): JSX.Element | null;   // showBanner=false면 null 반환

<!-- 교차 plan 계약: 이 plan의 화면은 react-native-safe-area-context의 useSafeAreaInsets()를 사용한다.
     Plan 07이 src/app/_layout.tsx 트리를 <SafeAreaProvider>로 감싸야 런타임에 동작한다. -->
</interfaces>

<!-- Phase 1 참조 구현 (분석 대상 — 토큰 사용 규율, readonly fontVariant 브리징, 헤더 주석) -->
@src/app/index.tsx
@src/app/__tests__/foundation-wiring.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 알림 priming 전체화면 라우트</name>
  <files>src/app/priming.tsx</files>
  <read_first>
    - .planning/phases/02-notification-infrastructure/02-UI-SPEC.md (§Screen & Component Notes의 priming 레이아웃 6단계, §Copywriting Contract, §Color의 "Accent 사용 안 함" 명시적 결정, §Typography의 Newsreader 금지)
    - DESIGN.md (담담한 톤, 시맨틱 컬러 부재, accent 6개 승인 용도 목록 — 편집 전 필독)
    - src/theme/tokens.ts (사용 가능한 토큰 전량 — 새 값을 발명하지 않는다)
    - src/app/index.tsx (헤더 주석 규약 + `StyleSheet.create` 안에서 토큰을 쓰는 방식 + readonly `fontVariant` 브리징 선례)
    - src/notifications/permissions.ts (Plan 04 산출물 — `requestNotificationPermission`, `markPrimingDismissed` 시그니처)
    - src/app/+not-found.tsx (expo-router 라우트 파일의 최소 형태)
  </read_first>
  <action>
`src/app/priming.tsx`를 expo-router 라우트로 만든다(파일 경로가 곧 `/priming` 라우트다. `app.json`의 `experiments.typedRoutes: true`이므로 이 파일이 존재해야 `router.replace('/priming')`이 타입 체크를 통과한다 — Plan 07이 그 호출을 넣는다).

레이아웃은 02-UI-SPEC.md §Screen & Component Notes의 6단계를 정확히 따른다. 세로 스택, 화면 중앙 정렬, 배경 `colors.background` 단색:
1. 상단 여백 `spacing.xl`(32) — 세이프에어리어 아래. `react-native-safe-area-context`의 `useSafeAreaInsets()`로 `insets.top`을 얻어 컨테이너 `paddingTop`에 `insets.top + spacing.xl`로 더한다.
2. 안내 문구: `typography.screenTitle` + `colors.textPrimary`, 좌우 여백 `spacing.lg`(24), `textAlign: 'center'`, 줄바꿈 허용(`numberOfLines` 제한을 걸지 않는다).
3. 세로 간격 `spacing.md`(16)
4. primary CTA 필 버튼: `Pressable`, 배경 `colors.textPrimary`, 라벨 색 `colors.surface`, 라벨 타이포 `typography.placeName`, `borderRadius: radius.full`, `minHeight: 44`, 세로 패딩 `spacing.sm`(12), 좌우 여백 `spacing.lg`(24), 라벨 가운데 정렬.
5. 세로 간격 `spacing.lg`(24)
6. secondary 링크: `Pressable`, 배경 없음, 텍스트 `typography.placeName` + `colors.textMuted`, 밑줄 없음, `minHeight: 44`.

확정 카피를 그대로 쓴다(02-UI-SPEC.md §Copywriting Contract에서 전사, 창작 금지):
- 안내 문구: `매시간 알림으로 지금 어디 있는지 잠깐 기록해요`
- primary CTA: `알림 허용하기`
- secondary: `나중에`

앱 이름("FootLog") 워드마크를 넣지 않는다 — REQ-app-name은 Phase 8 소관이며 02-UI-SPEC.md가 명시적으로 제외했다. 알림 빈도 선택 UI도 넣지 않는다(CONTEXT.md D-01 — Phase 6 소관). 아이콘을 넣지 않는다(`expo-symbols`가 설치돼 있지만 이 화면은 아이콘 없음이 명시 스펙).

`colors.accent`를 사용하지 않는다. 02-UI-SPEC.md §Color가 명시적 결정으로 기록했듯 DESIGN.md는 accent를 정확히 6개 용도(체크인 버튼/현재 위치 링/지도 마크/궤적선/오늘 밑줄/스크러버 표시)로만 제한하고 이 화면은 그 목록에 없다. 이 사실을 헤더 주석에 남긴다. `typography.journalEntry`(Newsreader 이탤릭 세리프)도 사용하지 않는다 — 사용자 작성 텍스트 전용 토큰이며 이 화면 텍스트는 전부 시스템 UI 크롬이다.

동작:
- `알림 허용하기` 탭 → `await requestNotificationPermission()` 실행 후 결과와 무관하게 `router.replace('/')`로 홈으로 돌아간다(승인/거부 모두 앱 사용을 막지 않는다 — product-design.md T8). 진행 중 중복 탭을 막기 위해 `useState`로 in-flight 플래그를 두고 버튼을 `disabled` 처리한다. `.catch`에서 `console.error`로 남기고(프로미스 미삼킴 규약) 역시 `router.replace('/')`로 진행한다.
- `나중에` 탭 → `markPrimingDismissed()` 호출 후 `router.replace('/')`. OS 다이얼로그를 띄우지 않는다.

접근성: 두 `Pressable` 모두 `accessibilityRole="button"`과 라벨 텍스트와 동일한 `accessibilityLabel`을 갖는다. 두 요소 모두 `minHeight: 44`로 44pt 터치 타겟을 확보한다.

hex 리터럴을 어떤 형태로도 쓰지 않는다 — `foundation-wiring.test.ts` Test 5가 `src/` 전역을 스캔해 빌드를 실패시킨다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/app/__tests__/foundation-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 이 exit 0
    - `npm test -- src/app/__tests__/foundation-wiring.test.ts` 이 exit 0 (hex 리터럴 전역 가드 통과)
    - `grep -c "매시간 알림으로 지금 어디 있는지 잠깐 기록해요" src/app/priming.tsx` == 1
    - `grep -c "알림 허용하기" src/app/priming.tsx` >= 1
    - `grep -c "나중에" src/app/priming.tsx` >= 1
    - `grep -c "FootLog" src/app/priming.tsx` == 0
    - `grep -v '^ *//' src/app/priming.tsx | grep -c "colors.accent"` == 0
    - `grep -v '^ *//' src/app/priming.tsx | grep -c "journalEntry"` == 0
    - `grep -c "minHeight: 44" src/app/priming.tsx` == 2
    - `grep -c "accessibilityRole=\"button\"" src/app/priming.tsx` == 2
    - `grep -c "requestNotificationPermission" src/app/priming.tsx` == 1
    - `grep -c "markPrimingDismissed" src/app/priming.tsx` == 1
    - `grep -c "useSafeAreaInsets" src/app/priming.tsx` >= 1
    - `grep -c "radius.full" src/app/priming.tsx` == 1
  </acceptance_criteria>
  <done>`/priming` 라우트가 존재하고, 확정 카피 3종과 UI-SPEC 레이아웃/토큰 계약을 정확히 따르며, 두 액션이 각각 OS 권한 요청과 세션 dismiss로 이어진다.</done>
</task>

<task type="auto">
  <name>Task 2: 알림 거부 배너 컴포넌트 + UI 계약 회귀 테스트</name>
  <files>src/components/NotificationDeniedBanner.tsx, src/components/__tests__/notificationUi.test.ts</files>
  <read_first>
    - .planning/phases/02-notification-infrastructure/02-UI-SPEC.md (§알림 거부 배너(임시 배치) — 레이아웃/카피/재사용 컴포넌트 요구, §Color의 배너 색 지정)
    - .planning/phases/02-notification-infrastructure/02-RESEARCH.md (Common Pitfalls §6 — 배너를 지도 위에 얹으면 명도 대비를 보장할 수 없음)
    - DESIGN.md (오류=muted 텍스트, 빨강 계열 전면 금지)
    - src/theme/tokens.ts (`colors.surface`, `colors.textMuted`, `typography.helperText`, `spacing`)
    - src/notifications/permissions.ts (Plan 04 산출물 — `useNotificationPermissionBanner` 반환 형태)
    - src/app/__tests__/foundation-wiring.test.ts (정적 소스 단언 테스트 패턴 + `stripComments` 사용법)
    - src/test-utils/stripComments.ts (주석 제거 유틸 — 재구현하지 않고 재사용)
    - src/app/priming.tsx (Task 1 산출물 — 같은 테스트가 단언 대상으로 삼는다)
  </read_first>
  <behavior>
    `src/components/__tests__/notificationUi.test.ts`의 테스트(전부 `fs.readFileSync` 기반 정적 소스 단언, `@jest-environment node`):
    - Test 1: `NotificationDeniedBanner.tsx`가 확정 카피 `알림이 꺼져있어요 · 설정에서 켜기`를 정확히 포함한다 (가운뎃점은 U+00B7 `·`)
    - Test 2: `NotificationDeniedBanner.tsx`가 `useNotificationPermissionBanner`를 참조한다
    - Test 3: `NotificationDeniedBanner.tsx`가 `colors.surface`(고정 불투명 배경)와 `colors.textMuted`(텍스트)를 사용한다 (Pitfall 6 — 가변 배경 위 오버레이 금지)
    - Test 4: `NotificationDeniedBanner.tsx`와 `priming.tsx` 어디에도 `colors.accent`가 등장하지 않는다 (`stripComments` 적용 후 판정)
    - Test 5: 두 파일 어디에도 `typography.journalEntry`가 등장하지 않는다 (Newsreader = 사용자 작성 텍스트 전용)
    - Test 6: 두 파일 어디에도 `backgroundColor: 'red'` 류 시맨틱 색 이름(`red`/`orange`/`yellow`/`green`)이 스타일 값으로 등장하지 않는다 (DESIGN.md — 시맨틱 컬러 시스템 자체가 없음)
    - Test 7: `NotificationDeniedBanner.tsx`가 `minHeight: 44`를 포함한다 (44pt 터치 타겟)
    - Test 8: `NotificationDeniedBanner.tsx`가 `accessibilityRole="button"`과 `accessibilityLabel`을 포함한다
    - Test 9: `NotificationDeniedBanner.tsx`가 `expo-symbols`/`Image`/`Icon`을 import 하지 않는다 (아이콘 없음이 명시 스펙)
    - Test 10: `priming.tsx`가 확정 카피 3종(안내 문구 / `알림 허용하기` / `나중에`)을 전부 포함한다
    - Test 11: `priming.tsx`에 문자열 `FootLog`가 등장하지 않는다 (REQ-app-name은 Phase 8 소관)
    - Test 12: `priming.tsx`에 알림 빈도 관련 문자열(`3시간`, `끔`, `빈도`)이 등장하지 않는다 (D-01 — 빈도 UI는 Phase 6 소관)
  </behavior>
  <action>
`src/components/NotificationDeniedBanner.tsx` 생성(`src/components/` 디렉터리를 새로 만든다). 02-UI-SPEC.md가 명시했듯 이 배너는 **재사용 가능한 독립 컴포넌트**여야 한다 — Phase 4가 오늘 뷰를 만들 때 import 경로만 바꿔 지도 상단으로 재배치할 수 있어야 하므로, 화면별 로직이나 위치 지정(absolute positioning)을 컴포넌트 안에 넣지 않는다. 배치는 부모가 결정한다. 이 계약을 헤더 주석에 명시하고, "Phase 2의 `src/app/index.tsx` 배치는 임시이며 최종 위치가 아니다"라는 사실도 함께 적는다.

`useNotificationPermissionBanner()`를 호출해 `{ showBanner, openSettings }`를 얻고, `showBanner`가 `false`면 `null`을 반환한다(조건부 렌더링을 부모에게 미루지 않는다 — 부모는 항상 `<NotificationDeniedBanner />`만 두면 된다).

레이아웃(02-UI-SPEC.md §알림 거부 배너):
- 루트는 `Pressable`이며 `onPress`가 `openSettings`. 텍스트 전체가 탭 영역이다.
- 배경 `colors.surface` 고정 불투명 색. 투명도(`opacity`/`rgba`)를 쓰지 않는다 — 02-RESEARCH.md Pitfall 6이 지적한 "가변 배경 위 대비 보장 불가" 문제를 원천 차단한다. 이 이유를 주석으로 남긴다.
- 좌우 패딩 `spacing.md`(16), 상하 패딩 `spacing.xs`(8), `minHeight: 44`, 내용 세로 중앙 정렬.
- 텍스트: `typography.helperText` + `colors.textMuted`, 확정 카피 `알림이 꺼져있어요 · 설정에서 켜기` 그대로.
- 아이콘 없음, 경고색 없음, 테두리 없음.
- 접근성: `accessibilityRole="button"`, `accessibilityLabel`은 `알림이 꺼져있어요. 설정에서 켜기` (스크린리더에서 가운뎃점이 어색하게 읽히지 않도록 마침표로 치환한 라벨).

hex 리터럴을 쓰지 않는다.

`src/components/__tests__/notificationUi.test.ts` 생성. `foundation-wiring.test.ts`와 동일한 구조: 파일 최상단 `@jest-environment node` docblock, `fs.readFileSync` + `path.join(__dirname, ...)`으로 두 소스 파일을 읽고, `colors.accent`/`journalEntry` 같은 "코드에 등장하면 안 되는" 단언에는 반드시 `stripComments`를 먼저 적용한다(주석에 토큰 이름을 설명으로 적어둘 수 있어야 하므로 — 이것이 이 repo가 `stripComments`를 만든 이유다). 카피 존재 단언(Test 1/10)에는 `stripComments`를 적용하지 않는다.

위 `<behavior>`의 Test 1~12를 `it('Test N: ...')` 한글 넘버링으로 작성한다. Test 6의 시맨틱 색 검사는 단어 경계를 쓴 정규식(`/\b(red|orange|yellow|green)\b/`)으로 하되, 파일 경로/토큰명에 우연히 포함될 수 있는 문자열(`GestureHandler` 등)에 오탐하지 않도록 `stripComments` 적용 후 문자열 리터럴 문맥(따옴표 안)으로 한정한다.

TDD 순서: 테스트를 먼저 작성해 실패를 확인한 뒤(컴포넌트 파일 부재로 `readFileSync` 실패), 컴포넌트를 구현해 통과시킨다. Task 1 산출물을 단언하는 Test 4/5/10/11/12는 즉시 통과하므로, 각 단언의 기대값을 일부러 뒤집어 실패를 확인한 뒤 되돌려 "테스트가 실제로 검증한다"를 확인한다.
  </action>
  <verify>
    <automated>npm test -- src/components && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/components` 이 exit 0 이고 12개 테스트가 통과한다
    - `npx tsc --noEmit` 이 exit 0
    - `npm test` 전체 스위트 exit 0 (특히 `foundation-wiring.test.ts` Test 5 hex 가드)
    - `head -3 src/components/__tests__/notificationUi.test.ts` 에 `@jest-environment node` 가 포함된다
    - `grep -c "it('Test " src/components/__tests__/notificationUi.test.ts` == 12
    - `grep -c "stripComments" src/components/__tests__/notificationUi.test.ts` >= 1
    - `grep -c "알림이 꺼져있어요 · 설정에서 켜기" src/components/NotificationDeniedBanner.tsx` == 1
    - `grep -c "useNotificationPermissionBanner" src/components/NotificationDeniedBanner.tsx` == 1
    - `grep -c "minHeight: 44" src/components/NotificationDeniedBanner.tsx` == 1
    - `grep -v '^ *//' src/components/NotificationDeniedBanner.tsx | grep -c "position: 'absolute'"` == 0 (배치는 부모 책임)
    - `grep -v '^ *//' src/components/NotificationDeniedBanner.tsx | grep -c "rgba\|opacity"` == 0 (Pitfall 6 — 불투명 고정 배경)
    - `grep -c "export function NotificationDeniedBanner" src/components/NotificationDeniedBanner.tsx` == 1
  </acceptance_criteria>
  <done>배너가 재사용 가능한 독립 컴포넌트로 존재하고, 확정 카피/토큰/접근성/불투명 배경 계약이 12개 정적 테스트로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 탭 → OS 권한 다이얼로그 | 사용자가 OS 수준 권한을 부여하는 경계 |
| 사용자 탭 → iOS 설정 앱 전환 | 앱이 OS를 다른 앱으로 전환시키는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Spoofing | priming 화면이 OS 다이얼로그를 흉내 | mitigate | priming 화면은 iOS 시스템 다이얼로그와 시각적으로 명확히 다르다 — 전체화면 `colors.background` 단색 배경 + 앱 자체 타이포/필 버튼이며, 시스템 알림 스타일(중앙 카드 + 파란 텍스트 버튼)을 모사하지 않는다. 다크 패턴 방지: `나중에` 링크가 44pt 터치 타겟을 갖고 시각적으로 숨겨지지 않는다(Test 10 + `minHeight: 44` 게이트) |
| T-02-17 | Spoofing | 설정 딥링크 | mitigate | 배너 탭이 Plan 04의 `openSettings`(= `Linking.openSettings()`)만 호출한다. 임의 URL을 여는 경로가 컴포넌트에 존재하지 않으며, `useNotificationPermissionBanner`가 반환하는 클로저 외에 다른 네비게이션 핸들러를 두지 않는다 |
| T-02-18 | Information Disclosure | 배너/priming 텍스트 | accept | 두 화면의 모든 텍스트가 정적 리터럴이며 위치/메모/체크인 데이터를 포함하지 않는다. 문구 생성 함수나 템플릿 보간이 없다(Test 1/10이 정확한 리터럴 존재를 고정) |
</threat_model>

<verification>
- `npm test -- src/components` green (12 정적 계약 테스트)
- `npm test` 전체 스위트 green — 특히 `foundation-wiring.test.ts` Test 5(hex 리터럴 전역 가드)
- `npx tsc --noEmit` exit 0
- UI-SPEC의 세 가지 UI 표면 중 두 개(priming 화면, 거부 배너)가 존재
</verification>

<success_criteria>
- `/priming` 라우트가 확정 카피 3종과 UI-SPEC 레이아웃/토큰을 정확히 구현
- 배너가 `showBanner=false`일 때 `null`을 반환하는 자기완결 컴포넌트
- accent 미사용, Newsreader 미사용, 시맨틱 컬러 미사용, hex 리터럴 0개
- 인터랙티브 요소 3개 전부 44pt 터치 타겟 + VoiceOver 라벨
</success_criteria>

<output>
Create `.planning/phases/02-notification-infrastructure/02-06-SUMMARY.md` when done
</output>
