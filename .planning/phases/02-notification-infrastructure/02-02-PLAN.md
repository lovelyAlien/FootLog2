---
phase: 02-notification-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - src/notifications/infoPlist.test.ts
  - TODOS.md
autonomous: true
requirements: [REQ-permission-copy]
user_setup: []

must_haves:
  truths:
    - "iOS 위치/카메라/사진 라이브러리 권한 다이얼로그가 확정된 한국어 문구를 보여준다"
    - "후속 phase가 config plugin으로 이 문구를 덮어써도 회귀 테스트가 즉시 잡아낸다"
  artifacts:
    - path: "app.json"
      provides: "3개 iOS usage description 문구"
      contains: "NSLocationWhenInUseUsageDescription"
    - path: "src/notifications/infoPlist.test.ts"
      provides: "권한 문구 회귀 가드"
      contains: "NSPhotoLibraryUsageDescription"
  key_links:
    - from: "src/notifications/infoPlist.test.ts"
      to: "app.json"
      via: "fs.readFileSync + JSON.parse 정적 단언"
      pattern: "expo\\.ios\\.infoPlist"
---

<objective>
CONTEXT.md D-03/D-04가 확정한 iOS 권한 프롬프트 문구 3종을 `app.json`의 `ios.infoPlist`에 넣고, 그 문구가 사라지거나 영어로 되돌아가는 회귀를 자동으로 잡는 테스트를 만든다.

Purpose: REQ-permission-copy의 3/4를 충족한다(나머지 1개인 '알림' 권한은 iOS에 Info.plist 키가 없어 priming 화면 문구로 충족 — D-05, Plan 06 소관). 동시에 02-RESEARCH.md Pitfall 5(Phase 3/4가 `expo-location`/`expo-image-picker` config plugin을 추가하며 이 문구를 영어 기본값으로 덮어쓰는 위험)에 대한 자동 감지 장치를 지금 심어둔다.
Output: `app.json` `ios.infoPlist` 3키 추가 + 정적 회귀 테스트 + Phase 3/4를 위한 TODOS.md 인계 항목.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-notification-infrastructure/02-CONTEXT.md
@.planning/phases/02-notification-infrastructure/02-RESEARCH.md
@.planning/phases/02-notification-infrastructure/02-PATTERNS.md
@.planning/phases/02-notification-infrastructure/02-UI-SPEC.md

<interfaces>
<!-- app.json 목표 상태 (기존 ios.infoPlist 객체에 3키 append — ios 블록을 새로 만들지 않는다) -->
expo.ios.infoPlist:
  ITSAppUsesNonExemptEncryption: false            (기존 — 유지)
  NSLocationWhenInUseUsageDescription: "체크인 위치를 기록하려면 위치 정보가 필요해요."
  NSCameraUsageDescription: "체크인에 사진을 남기려면 카메라 접근이 필요해요."
  NSPhotoLibraryUsageDescription: "체크인에 사진을 첨부하려면 사진 보관함 접근이 필요해요."
</interfaces>

<!-- Phase 1 참조 구현 (분석 대상 — 정적 소스 단언 테스트 패턴) -->
@src/app/__tests__/foundation-wiring.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.json ios.infoPlist에 권한 문구 3종 추가</name>
  <files>app.json</files>
  <read_first>
    - app.json (현재 `expo.ios.infoPlist`에 `ITSAppUsesNonExemptEncryption`만 있는 상태 확인 + `plugins` 배열에 expo-location/expo-image-picker가 아직 없음을 확인)
    - .planning/phases/02-notification-infrastructure/02-CONTEXT.md (D-03 문구 원문, D-04 한국어 단일 언어, D-05 알림 키 부재)
    - .planning/phases/02-notification-infrastructure/02-UI-SPEC.md (§iOS 권한 프롬프트 문구 표 — 문구별 확정 상태)
    - .planning/phases/02-notification-infrastructure/02-RESEARCH.md (Architecture Patterns §Pattern 3, Common Pitfalls §5)
  </read_first>
  <action>
`app.json`의 기존 `expo.ios.infoPlist` 객체 **안에** 아래 3개 키를 추가한다. 새로운 `ios` 블록이나 `plugins` 항목을 만들지 않는다 — 02-RESEARCH.md Pattern 3이 확인했듯 Phase 2 시점에는 `expo-location`/`expo-image-picker`가 설치돼 있지 않아 config plugin 옵션(`locationWhenInUsePermission` 등)이라는 대안 자체가 존재하지 않는다.

- `NSLocationWhenInUseUsageDescription`: `체크인 위치를 기록하려면 위치 정보가 필요해요.`
- `NSCameraUsageDescription`: `체크인에 사진을 남기려면 카메라 접근이 필요해요.`
- `NSPhotoLibraryUsageDescription`: `체크인에 사진을 첨부하려면 사진 보관함 접근이 필요해요.`

세 문구 모두 마침표까지 포함해 D-03 원문과 문자 단위로 동일해야 한다. 영어 병기를 추가하지 않는다(D-04). 알림 권한용 키는 추가하지 않는다 — iOS에는 대응 Info.plist 키가 존재하지 않으며, priming 화면 문구로 충족한다(D-05). 따라서 추가되는 키는 4개가 아니라 정확히 3개다.

`ITSAppUsesNonExemptEncryption: false`는 그대로 둔다. JSON 들여쓰기는 파일의 기존 2-space 스타일을 유지한다.
  </action>
  <verify>
    <automated>node -e "const j=require('./app.json').expo.ios.infoPlist; const e={NSLocationWhenInUseUsageDescription:'체크인 위치를 기록하려면 위치 정보가 필요해요.',NSCameraUsageDescription:'체크인에 사진을 남기려면 카메라 접근이 필요해요.',NSPhotoLibraryUsageDescription:'체크인에 사진을 첨부하려면 사진 보관함 접근이 필요해요.'}; for(const k of Object.keys(e)){if(j[k]!==e[k]){console.error('mismatch',k,JSON.stringify(j[k]));process.exit(1)}} if(j.ITSAppUsesNonExemptEncryption!==false){console.error('lost ITSAppUsesNonExemptEncryption');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(Object.keys(require('./app.json').expo.ios.infoPlist).length)"` 출력이 `4`
    - `node -e "console.log(require('./app.json').expo.ios.infoPlist.NSLocationWhenInUseUsageDescription)"` 출력이 `체크인 위치를 기록하려면 위치 정보가 필요해요.`
    - `node -e "console.log(require('./app.json').expo.ios.infoPlist.NSCameraUsageDescription)"` 출력이 `체크인에 사진을 남기려면 카메라 접근이 필요해요.`
    - `node -e "console.log(require('./app.json').expo.ios.infoPlist.NSPhotoLibraryUsageDescription)"` 출력이 `체크인에 사진을 첨부하려면 사진 보관함 접근이 필요해요.`
    - `node -e "console.log(require('./app.json').expo.ios.infoPlist.ITSAppUsesNonExemptEncryption)"` 출력이 `false`
    - `node -e "const p=require('./app.json').expo.plugins; console.log(JSON.stringify(p).includes('Permission'))"` 출력이 `false` (config plugin 권한 옵션을 도입하지 않음)
    - `git diff --stat app.json` 이 3줄 추가만 보여준다 (기존 키 삭제/변경 없음)
  </acceptance_criteria>
  <done>`app.json`의 `expo.ios.infoPlist`가 정확히 4개 키를 가지며 3개 권한 문구가 D-03 원문과 일치한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 권한 문구 회귀 가드 테스트 + Phase 3/4 인계 항목 기록</name>
  <files>src/notifications/infoPlist.test.ts, TODOS.md</files>
  <read_first>
    - src/app/__tests__/foundation-wiring.test.ts (`@jest-environment node` docblock + `fs.readFileSync` 정적 단언 + `it('Test N: ...')` 한글 넘버링 규약의 원본)
    - app.json (Task 1 산출물 — 단언 대상)
    - TODOS.md (현재 항목 포맷 확인 후 같은 포맷으로 append)
    - .planning/phases/02-notification-infrastructure/02-RESEARCH.md (Common Pitfalls §5 — 후속 phase plugin 덮어쓰기 시나리오)
  </read_first>
  <behavior>
    - Test 1: `app.json`의 `expo.ios.infoPlist.NSLocationWhenInUseUsageDescription`이 D-03 문구와 정확히 일치한다
    - Test 2: `expo.ios.infoPlist.NSCameraUsageDescription`이 D-03 문구와 정확히 일치한다
    - Test 3: `expo.ios.infoPlist.NSPhotoLibraryUsageDescription`이 D-03 문구와 정확히 일치한다
    - Test 4: 세 문구 모두 ASCII 알파벳을 포함하지 않는다 (D-04 한국어 단일 언어 — 영어 병기가 섞여 들어오면 실패)
    - Test 5: `expo.ios.infoPlist.ITSAppUsesNonExemptEncryption`이 `false`로 유지된다 (Phase 1 설정 회귀 가드)
    - Test 6: `expo.plugins`를 JSON 직렬화한 문자열에 `locationWhenInUsePermission`/`cameraPermission`/`photosPermission` 중 어느 것도 등장하지 않는다 (Pitfall 5 — Phase 3/4가 config plugin 옵션을 추가하면 이 테스트가 실패해 문구 이관을 강제한다)
  </behavior>
  <action>
`src/notifications/infoPlist.test.ts` 생성. `foundation-wiring.test.ts`와 동일하게 파일 최상단 `@jest-environment node` docblock을 두고, `fs.readFileSync(path.join(__dirname, '../../app.json'), 'utf-8')` + `JSON.parse`로 `app.json`을 읽는다(`require`가 아니라 `fs`로 읽어 jest 모듈 캐시 영향을 배제한다 — `foundation-wiring.test.ts`가 소스를 읽는 방식과 동일). `stripComments`는 쓰지 않는다 — JSON에는 주석이 없다.

기대 문구 3개를 테스트 파일 상단 상수 객체에 D-03 원문 그대로 하드코딩한다. 이 테스트의 존재 이유(02-RESEARCH.md Pitfall 5 — Phase 3/4의 `expo-location`/`expo-image-picker` config plugin이 이 문구를 영어 기본값으로 덮어쓸 수 있음)를 헤더 주석에 명시하고, Test 6에 "이 테스트가 실패하면 문구를 삭제하는 게 아니라 plugin 옵션으로 이관해야 한다"는 지침 주석을 단다.

위 `<behavior>`의 Test 1~6을 그 순서대로, `it('Test N: ...')` 한글 넘버링으로 작성한다. TDD 순서를 지킨다: Task 1이 이미 app.json을 수정했으므로 Test 1~3/5는 즉시 통과한다 — 대신 각 테스트를 작성한 직후 기대 문구를 일부러 한 글자 바꿔 실패를 확인하고 되돌리는 방식으로 "테스트가 실제로 무언가를 검증한다"는 것을 확인한 뒤 진행한다.

`TODOS.md`에 Phase 3/4 인계 항목 1개를 기존 포맷에 맞춰 추가한다: Phase 3(`expo-location` 설치)와 Phase 4(`expo-image-picker` 설치) 계획 시 각 패키지의 config plugin 옵션(`locationWhenInUsePermission`, `cameraPermission`, `photosPermission`)으로 D-03 한국어 문구를 이관해야 하며, `src/notifications/infoPlist.test.ts` Test 6이 이 이관을 강제하는 게이트라는 사실. 아울러 D-03 문구는 창업자 최종 확인 대기 상태(초안)임을 함께 적는다.
  </action>
  <verify>
    <automated>npm test -- src/notifications/infoPlist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/notifications/infoPlist.test.ts` 이 exit 0 이고 6개 테스트가 통과한다
    - `head -3 src/notifications/infoPlist.test.ts` 에 `@jest-environment node` 가 포함된다
    - `grep -c "it('Test " src/notifications/infoPlist.test.ts` == 6
    - `grep -c "readFileSync" src/notifications/infoPlist.test.ts` >= 1
    - `grep -c "locationWhenInUsePermission" src/notifications/infoPlist.test.ts` >= 1
    - `grep -c "infoPlist.test.ts" TODOS.md` >= 1
    - `npm test` 전체 스위트가 exit 0
  </acceptance_criteria>
  <done>권한 문구 3종이 6개 테스트로 고정되고, Phase 3/4의 plugin 덮어쓰기 시나리오가 자동 게이트로 막히며, 인계 사항이 TODOS.md에 기록됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 빌드 산출물(Info.plist) → iOS 시스템 권한 다이얼로그 | 앱이 OS 다이얼로그 안에 표시할 텍스트를 제공하는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | `app.json` `ios.infoPlist` 문구 | accept | 3개 문구는 전부 개발자가 하드코딩한 정적 문자열이며 런타임 사용자 입력이 보간되는 경로가 없다(02-RESEARCH.md Security Domain). 인젝션 표면 없음 — 별도 완화 불필요 |
| T-02-04 | Repudiation | 후속 phase의 config plugin이 문구를 조용히 영어 기본값으로 교체 | mitigate | `infoPlist.test.ts` Test 6이 `expo.plugins` 직렬화 문자열에서 `locationWhenInUsePermission`/`cameraPermission`/`photosPermission` 등장을 실패로 처리해, 문구 소실이 조용히 배포되지 않고 CI/로컬 테스트에서 즉시 드러나게 한다 |
| T-02-05 | Information Disclosure | 권한 문구가 앱의 데이터 수집 범위를 오도 | accept | 3개 문구 모두 실제 용도(체크인 위치 기록/사진 첨부)와 정확히 일치하며 백그라운드 위치 등 요청하지 않는 권한을 암시하지 않는다. PROJECT.md의 "백그라운드/항상 허용 위치 권한 미사용" 결정과 일관 |
</threat_model>

<verification>
- `node -e` 검증 스크립트로 4개 키 + 정확한 문구 확인
- `npm test -- src/notifications/infoPlist.test.ts` green (6 테스트)
- `npm test` 전체 스위트 green
- `git diff app.json` 이 3줄 추가만
</verification>

<success_criteria>
- REQ-permission-copy의 위치/카메라/사진 라이브러리 3종 문구가 `app.json`에 확정 반영됨
- 문구 회귀와 Phase 3/4 plugin 덮어쓰기가 자동 테스트로 게이트됨
- TODOS.md에 Phase 3/4 이관 지침 기록됨
</success_criteria>

<output>
Create `.planning/phases/02-notification-infrastructure/02-02-SUMMARY.md` when done
</output>
