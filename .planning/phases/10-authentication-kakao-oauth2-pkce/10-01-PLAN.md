---
phase: 10-authentication-kakao-oauth2-pkce
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .gitignore
  - src/auth/envContract.test.ts
autonomous: false
requirements:
  - REQ-auth-kakao-oauth
user_setup:
  - service: kakao-developers
    why: "카카오 로그인 네이티브 SDK(D-13) 초기화에 필요한 네이티브 앱 키 발급 및 iOS 번들 ID/동의항목 등록. 10-RESEARCH.md `## Environment Availability`가 '대체 경로 없음(no fallback)'으로 명시한 유일한 하드 블로커 — 창업자 본인의 카카오 개발자 계정으로만 가능하다"
    env_vars:
      - name: KAKAO_NATIVE_APP_KEY
        source: "developers.kakao.com > 내 애플리케이션 > (앱 선택) > 앱 키 > 네이티브 앱 키"
      - name: JWT_SECRET
        source: "로컬에서 `openssl rand -base64 32` 실행 결과 (외부 서비스 아님, 창업자가 직접 생성)"
    dashboard_config:
      - task: "iOS 플랫폼 등록 — 번들 ID `com.jaeseungchoun.footlog`"
        location: "developers.kakao.com > 내 애플리케이션 > 앱 설정 > 플랫폼 > iOS 플랫폼 등록"
      - task: "카카오 로그인 활성화 설정 ON"
        location: "developers.kakao.com > 내 애플리케이션 > 제품 설정 > 카카오 로그인"
      - task: "동의항목 — 닉네임/프로필 사진만 설정, 카카오계정(이메일)은 설정하지 않음(D-05/D-07)"
        location: "developers.kakao.com > 내 애플리케이션 > 제품 설정 > 카카오 로그인 > 동의항목"

must_haves:
  truths:
    - "창업자가 카카오 개발자 콘솔에 FootLog 앱을 등록했고, iOS 번들 ID com.jaeseungchoun.footlog가 플랫폼으로 등록되어 있다"
    - "카카오 로그인 동의항목에 닉네임/프로필 사진만 있고 이메일은 요청 대상이 아니다(D-05/D-07)"
    - "네이티브 앱 키와 JWT 서명 시크릿이 git에 커밋되지 않는 로컬 .env 파일로만 존재한다"
    - "저장소에는 값이 비어 있는 .env.example만 커밋되어 있으며, 어떤 실제 키도 포함하지 않는다"
    - "expo-secure-store 패키지의 정통성이 npm 레지스트리에서 사람 눈으로 확인됐다"
  artifacts:
    - path: ".env.example"
      provides: "Phase 10이 요구하는 전체 환경변수 계약(값 없음)"
      contains: "KAKAO_NATIVE_APP_KEY"
    - path: "src/auth/envContract.test.ts"
      provides: ".env.example에 실제 시크릿이 커밋되지 않게 막는 회귀 가드"
      min_lines: 30
  key_links:
    - from: ".gitignore"
      to: ".env.example"
      via: "부정 패턴(!)으로 .env* 무시 규칙에서 예외 처리"
      pattern: "!\\.env\\.example"
---

<objective>
Phase 10 전체(백엔드 + 클라이언트)가 시작되기 전에 반드시 사람이 해결해야 하는 두 개의 선행
게이트를 닫는다: (1) 창업자 본인의 카카오 개발자 계정으로만 가능한 카카오 앱 등록 및 네이티브
앱 키 발급, (2) 신규 npm 패키지 `expo-secure-store`의 정통성 확인. 그리고 이후 모든 플랜이
참조할 환경변수 계약 파일(`.env.example`)과, 그 파일에 실제 시크릿이 커밋되는 것을 영구히
막는 회귀 가드를 만든다.

Purpose: 10-RESEARCH.md `## Environment Availability`가 "Missing dependencies with no fallback"
으로 명시한 유일한 항목이 카카오 개발자 콘솔 앱 등록이다. 이 게이트가 열리지 않으면 클라이언트
SDK 초기화(10-07)와 실제 카카오 계정 왕복 검증이 원천적으로 불가능하다. 백엔드 플랜(10-02~10-05)은
이 키에 의존하지 않으므로 병렬로 진행할 수 있지만, 창업자에게 리드타임을 주기 위해 wave 1에 둔다.
Output: `.env.example`, `.gitignore` 갱신, `src/auth/envContract.test.ts`, 그리고 창업자 로컬의
(커밋되지 않는) `.env` 파일.

**중요 — 이번 phase의 백엔드는 카카오 client_secret / REST API 키를 전혀 쓰지 않는다.**
D-14 수정(10-CONTEXT.md, 10-RESEARCH.md ⚠️ AMENDMENT 블록)으로 `kauth.kakao.com/oauth/token`
교환 단계 자체가 백엔드 코드 경로에서 사라졌기 때문이다. 창업자에게 REST API 키나 시크릿 발급을
요청하지 않는다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-authentication-kakao-oauth2-pkce/10-CONTEXT.md
@.planning/phases/10-authentication-kakao-oauth2-pkce/10-RESEARCH.md
@.planning/phases/10-authentication-kakao-oauth2-pkce/10-VALIDATION.md
@app.json
@.gitignore

<interfaces>
<!-- 이 플랜이 확정하는 환경변수 이름. 10-03/10-06/10-07이 이 이름들을 그대로 소비한다. -->

KAKAO_NATIVE_APP_KEY   — Expo config plugin(@react-native-seoul/kakao-login)의 kakaoAppKey prop.
                          10-07의 app.config.js가 process.env로 읽는다.
EXPO_PUBLIC_API_BASE_URL — 클라이언트가 백엔드를 호출할 베이스 URL. 10-06의 src/auth/config.ts가
                          process.env로 읽는다. EXPO_PUBLIC_ 접두사는 Expo가 번들에 인라인하는
                          공개 값 규약이므로 비밀값을 절대 여기 두지 않는다.
JWT_SECRET             — 백엔드 HMAC(HS256) 서명 키. 최소 32바이트.
                          10-03의 application-staging.yml이 기본값 없이 요구한다.
DATABASE_URL / DATABASE_USERNAME / DATABASE_PASSWORD
                       — Phase 9가 이미 도입한 staging DataSource 환경변수
                         (backend/src/main/resources/application-staging.yml).
</interfaces>

**기존 .gitignore 함정:** 현재 `.gitignore`에 `# local env files` 아래 `.env*` 한 줄이 있다.
이 패턴은 `.env.example`까지 무시하므로, 부정 패턴 `!.env.example`을 반드시 함께 추가해야
파일이 실제로 커밋된다.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 카카오 개발자 콘솔 앱 등록 (창업자 전용 — 자동화 불가)</name>
  <files>.env (창업자 로컬 전용 — git이 무시하므로 커밋되지 않는다)</files>
  <read_first>
    - app.json (`expo.ios.bundleIdentifier` — 카카오 콘솔 iOS 플랫폼 등록에 넣을 정확한 번들 ID)
    - .planning/phases/10-authentication-kakao-oauth2-pkce/10-RESEARCH.md (`## Environment Availability` — 이 항목이 "Fallback: 없음"인 이유, 그리고 AMENDMENT 이후 `client_secret`이 불필요해진 근거)
    - .planning/phases/10-authentication-kakao-oauth2-pkce/10-CONTEXT.md (D-05/D-07 — 이메일 동의항목을 설정하지 않는 근거, D-13 — 네이티브 SDK 채택)
  </read_first>
  <what-built>
아직 아무것도 만들어지지 않았다. 이 태스크는 Phase 10에서 유일하게 자동화 경로가 존재하지 않는
선행 조건이다 — 10-RESEARCH.md `## Environment Availability`가 "Fallback: 없음"으로 명시한 항목.
카카오 개발자 콘솔은 창업자 본인의 카카오 계정 로그인을 요구하므로 에이전트가 대신 수행할 수 없다.
  </what-built>
  <how-to-verify>
아래 순서대로 진행해 주세요. (약 5분)

1. https://developers.kakao.com 접속 → 우측 상단 "로그인"(창업자 본인 카카오 계정)
2. 상단 메뉴 "내 애플리케이션" → **"애플리케이션 추가하기"**
   - 앱 이름: `FootLog`
   - 회사명: 아무 값(1인 프로젝트이므로 `FootLog`로 두어도 무방)
   - 저장
3. 생성된 앱 클릭 → 좌측 **"앱 키"** 메뉴
   - **네이티브 앱 키**(Native app key) 값을 복사해 둡니다.
   - ※ REST API 키와 Client Secret은 **이번 phase에서 쓰지 않습니다**(D-14 수정으로 백엔드가
     카카오 토큰 교환을 하지 않음). 발급/활성화하지 않아도 됩니다.
4. 좌측 **"앱 설정 > 플랫폼"** → **"iOS 플랫폼 등록"**
   - 번들 ID: `com.jaeseungchoun.footlog` (app.json의 `expo.ios.bundleIdentifier`와 정확히 동일해야 합니다)
   - 저장
5. 좌측 **"제품 설정 > 카카오 로그인"** → **활성화 설정**을 **ON**으로 변경
   - Redirect URI는 이번 phase에서 쓰지 않습니다(네이티브 SDK가 앱 전환/웹뷰 폴백을 자체 처리, D-13).
     입력을 요구하면 비워두거나 등록하지 않아도 됩니다.
6. 좌측 **"제품 설정 > 카카오 로그인 > 동의항목"**
   - **닉네임(profile_nickname)**: "필수 동의" 또는 "선택 동의"로 설정
   - **프로필 사진(profile_image)**: "선택 동의"로 설정
   - **카카오계정(이메일)**: **설정하지 않음(사용 안 함) 상태로 그대로 둡니다** —
     D-05(이메일 미저장)/D-07(미동의자도 로그인 허용)에 따라 이 앱은 이메일을 애초에 요청하지 않습니다.
7. 저장소 루트에 `.env` 파일을 만들고(이 파일은 git이 무시합니다) 아래 두 줄을 채웁니다:
   - `KAKAO_NATIVE_APP_KEY=` 뒤에 3단계에서 복사한 네이티브 앱 키
   - `JWT_SECRET=` 뒤에 터미널에서 `openssl rand -base64 32`를 실행한 결과 문자열
     (HS256은 최소 256비트=32바이트 키를 요구하며, 짧으면 백엔드가 기동조차 되지 않습니다 —
      10-RESEARCH.md Pitfall 2)
   - `EXPO_PUBLIC_API_BASE_URL=http://localhost:8080` (10-07 실기기 검증 때 LAN IP로 바꿉니다)

**주의:** 네이티브 앱 키와 JWT 시크릿을 채팅창이나 커밋에 붙여넣지 마세요. `.env` 파일에만
넣으면 됩니다. Task 3이 만드는 회귀 가드 테스트가 `.env.example`에 실제 값이 들어가는 것을
자동으로 막습니다.
  </how-to-verify>
  <action>
창업자가 "완료"를 알린 뒤에 수행한다. **창업자가 응답하기 전에 이 태스크를 임의로 완료 처리하지 않는다** —
카카오 콘솔 접근 권한이 없으므로 대신 수행할 방법이 존재하지 않는다.

응답을 받으면: (1) 저장소 루트에 `.env`가 존재하고 `KAKAO_NATIVE_APP_KEY` / `JWT_SECRET` /
`EXPO_PUBLIC_API_BASE_URL` 세 키가 비어 있지 않은지 확인한다. **값 자체를 출력하거나 SUMMARY에
인용하지 않는다** — 존재 여부와 길이만 확인한다. (2) `JWT_SECRET`이 32바이트 미만이면 즉시
창업자에게 알리고 `openssl rand -base64 32` 재생성을 요청한다(짧으면 10-03에서 백엔드가 기동조차
되지 않는다, 10-RESEARCH.md Pitfall 2). (3) 10-01-SUMMARY.md에 등록 완료 사실, 등록한 번들 ID,
그리고 **이메일 동의항목을 설정하지 않았다는 확인**(D-05/D-07)을 기록한다. (4) 10-VALIDATION.md의
Manual-Only Verifications 첫 번째 행("카카오 개발자 콘솔 앱 등록")을 완료 상태로 갱신하되,
AMENDMENT로 불필요해진 `client_secret`/redirect URI 언급을 실제 수행 내용에 맞게 정정한다.
  </action>
  <acceptance_criteria>
    - `test -f .env` 성공
    - `grep -cE '^KAKAO_NATIVE_APP_KEY=.+$' .env` 결과가 1 (값이 비어 있지 않음 — 값 자체는 출력하지 않는다)
    - `grep -E '^JWT_SECRET=' .env | cut -d= -f2- | wc -c` 결과가 33 이상 (32바이트 이상 + 개행 1)
    - `grep -cE '^EXPO_PUBLIC_API_BASE_URL=.+$' .env` 결과가 1
    - 10-01-SUMMARY.md에 등록한 iOS 번들 ID `com.jaeseungchoun.footlog`와 "이메일 동의항목 미설정" 확인이 기록되어 있다
    - 10-01-SUMMARY.md 어디에도 실제 네이티브 앱 키나 JWT 시크릿 값이 인용되어 있지 않다
    - 10-VALIDATION.md의 Manual-Only Verifications 첫 행이 완료 상태로 갱신되어 있다
  </acceptance_criteria>
  <resume-signal>완료했으면 "완료"라고 알려주세요. 진행 중 막히는 지점이 있으면 어느 단계에서 무엇이 다르게 보이는지 알려주세요 (키 값 자체는 붙여넣지 마세요).</resume-signal>
  <verify>
    <automated>test -f .env && grep -qE '^KAKAO_NATIVE_APP_KEY=.+$' .env && grep -qE '^EXPO_PUBLIC_API_BASE_URL=.+$' .env && test "$(grep -E '^JWT_SECRET=' .env | cut -d= -f2- | wc -c)" -ge 33 && echo ENV_CONTRACT_OK</automated>
  </verify>
  <done>창업자가 카카오 앱 등록을 완료했고, 로컬 `.env`에 세 환경변수가 유효한 형태로 존재하며(값은 노출하지 않음), 등록 내역이 SUMMARY와 10-VALIDATION.md에 기록됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: expo-secure-store 패키지 정통성 확인 (자동 승인 불가)</name>
  <files>.planning/phases/10-authentication-kakao-oauth2-pkce/10-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/10-authentication-kakao-oauth2-pkce/10-RESEARCH.md (`## Package Legitimacy Audit` — 이 표가 `@react-native-seoul/kakao-login`만 검증했고 `expo-secure-store`를 다루지 않았다는 사실 확인. 표의 열 형식을 그대로 이어 쓴다)
    - package.json (현재 의존성 — expo-secure-store가 아직 없음을 확인)
  </read_first>
  <what-built>
아직 설치하지 않았다. 10-06(클라이언트 토큰 저장)이 `expo-secure-store`를 새로 설치해야 하는데,
10-RESEARCH.md `## Package Legitimacy Audit` 표는 `@react-native-seoul/kakao-login`만 검증했고
`expo-secure-store`는 다루지 않았다. GSD 패키지 정통성 게이트 규정상 감사 표에 없는 npm 패키지는
`[ASSUMED]`로 취급되며, 설치 전에 사람이 레지스트리를 직접 확인해야 한다(자동 승인 불가).
  </what-built>
  <how-to-verify>
아래 두 곳을 열어 확인해 주세요. (약 1분)

1. https://www.npmjs.com/package/expo-secure-store 접속
   - **Repository**가 `github.com/expo/expo`(Expo 공식 모노레포)를 가리키는지
   - **Weekly Downloads**가 수십만 단위(1st-party Expo 모듈 수준)인지
   - 최근 배포일이 최근 1년 이내인지
2. https://docs.expo.dev/versions/latest/sdk/securestore/ 접속
   - Expo 공식 문서에 SDK 모듈로 등재되어 있는지(= 타입스쿼팅/유사명 패키지가 아님)

세 항목이 모두 맞으면 승인해 주세요. 하나라도 다르면 승인하지 말고 무엇이 달랐는지 알려주세요 —
그 경우 10-06은 SecureStore 대신 다른 저장 경로를 재검토해야 합니다.
  </how-to-verify>
  <action>
창업자의 승인을 받은 뒤에 수행한다. **승인 전에 `expo-secure-store`를 설치하지 않는다** —
설치 자체가 10-06 Task 1의 작업이며, 이 태스크는 그 설치를 허가하는 게이트일 뿐이다.
`workflow.auto_advance` 설정과 무관하게 자동 승인하지 않는다.

승인을 받으면 10-01-SUMMARY.md에 10-RESEARCH.md `## Package Legitimacy Audit` 표와 **동일한 열
형식**으로 한 행을 추가 기록한다: 패키지명 `expo-secure-store`, Registry `npm`,
Verification Method(창업자가 npm 레지스트리 + Expo 공식 문서를 직접 확인), Source Repo
`github.com/expo/expo`, Disposition `Approved [VERIFIED: 창업자 수동 확인 2026-XX-XX]`.
창업자가 이상 징후를 보고했다면 승인 기록 대신 그 내용을 남기고, **10-06을 진행 불가로 보고한다** —
대체 저장 경로(예: `react-native-keychain`) 검토가 선행되어야 하며 그 결정은 창업자 몫이다.
  </action>
  <acceptance_criteria>
    - 10-01-SUMMARY.md에 `expo-secure-store` 행이 10-RESEARCH.md Package Legitimacy Audit 표와 같은 열 구성으로 추가되어 있다
    - 그 행의 Disposition이 `Approved` 또는 (이상 징후 시) 명시적 보류 상태다
    - `grep -c 'expo-secure-store' package.json` 결과가 0 (이 태스크는 게이트일 뿐 설치하지 않는다 — 설치는 10-06 소관)
  </acceptance_criteria>
  <resume-signal>"승인" 또는 이상 징후 설명</resume-signal>
  <verify>
    <automated>grep -c 'expo-secure-store' .planning/phases/10-authentication-kakao-oauth2-pkce/10-01-SUMMARY.md</automated>
  </verify>
  <done>창업자가 패키지 정통성을 직접 확인해 승인했고, 그 판정이 10-RESEARCH.md 감사 표와 동일한 형식으로 SUMMARY에 기록됐다. 설치는 아직 하지 않았다.</done>
</task>

<task type="auto">
  <name>Task 3: .env.example 환경변수 계약 + 시크릿 커밋 회귀 가드</name>
  <files>.env.example, .gitignore, src/auth/envContract.test.ts</files>
  <read_first>
    - .gitignore (현재 `.env*` 한 줄이 `.env.example`까지 무시하고 있다 — 부정 패턴을 추가할 위치 확인)
    - backend/src/main/resources/application-staging.yml (Phase 9가 이미 쓰는 DATABASE_* 환경변수 이름 및 "평문 비밀값 금지" 주석 규약)
    - app.json (expo.ios.bundleIdentifier / expo.plugins 현황 — 10-07이 이어받을 지점)
    - src/notifications/infoPlist.test.ts (fs.readFileSync + 정적 파싱으로 설정 파일을 검사하는 저장소 회귀 가드 관용구 — 이 테스트가 그 형태를 그대로 따른다)
  </read_first>
  <action>
**(1) `.env.example` 생성.** 저장소 루트에 만든다. 모든 값은 **등호 뒤가 비어 있어야 한다**(단
`EXPO_PUBLIC_API_BASE_URL`만 예외로 `http://localhost:8080`을 기본값으로 둔다 — 비밀값이 아니고
로컬 개발 기본값으로 유용하다). 파일 상단에 한글 주석으로 "이 파일은 값이 아니라 *이름*의 계약이다.
실제 값은 git이 무시하는 `.env`에만 넣는다"를 명시한다.

포함할 키와 각 키에 붙일 한 줄 주석:
- `KAKAO_NATIVE_APP_KEY=` — Expo config plugin `@react-native-seoul/kakao-login`의 `kakaoAppKey`
  prop으로 주입됨(10-07의 app.config.js). developers.kakao.com > 앱 키 > 네이티브 앱 키.
- `EXPO_PUBLIC_API_BASE_URL=http://localhost:8080` — 클라이언트가 백엔드를 호출할 베이스 URL
  (10-06의 src/auth/config.ts). `EXPO_PUBLIC_` 접두사 값은 앱 번들에 그대로 인라인되므로
  비밀값을 절대 두지 않는다.
- `JWT_SECRET=` — 백엔드 HS256 서명 키. **최소 32바이트**(`openssl rand -base64 32`).
  10-RESEARCH.md Pitfall 2: 짧으면 앱이 기동조차 되지 않는다. staging에서는 기본값이 없어
  미주입 시 기동 실패한다(10-03).
- `DATABASE_URL=` / `DATABASE_USERNAME=` / `DATABASE_PASSWORD=` — Phase 9 staging 프로파일이
  이미 요구하는 값(application-staging.yml).

이번 phase의 백엔드는 카카오 `client_secret` / REST API 키를 쓰지 않으므로 그 키들은 **넣지 않는다**
(D-14 수정, 10-RESEARCH.md ⚠️ AMENDMENT). 나중에 필요해지면 그때 추가한다.

**(2) `.gitignore` 수정.** 기존 `# local env files` 섹션의 `.env*` 바로 다음 줄에
`!.env.example`을 추가하고, 왜 필요한지(=`.env*` 패턴이 example까지 삼킨다) 한 줄 주석을 남긴다.
기존 줄은 지우지 않는다.

**(3) `src/auth/envContract.test.ts` 작성.** `src/notifications/infoPlist.test.ts`와 동일하게
파일 최상단에 `/** @jest-environment node */`를 두고 `fs.readFileSync`로 `.env.example`과
`.gitignore`를 정적으로 읽어 아래를 단언한다. `require`/`dotenv` 같은 런타임 로더를 쓰지 않는다.
`.env`(실제 값 파일)는 존재 여부를 단언하지 않는다 — CI에는 없기 때문이다.
  </action>
  <behavior>
    - Test 1: `.env.example`이 존재하고, KAKAO_NATIVE_APP_KEY / EXPO_PUBLIC_API_BASE_URL / JWT_SECRET / DATABASE_URL / DATABASE_USERNAME / DATABASE_PASSWORD 6개 키를 모두 정의한다
    - Test 2: `KAKAO_NATIVE_APP_KEY`의 값이 빈 문자열이다 — 실제 네이티브 앱 키가 커밋되는 것을 막는 회귀 가드
    - Test 3: `JWT_SECRET`의 값이 빈 문자열이다 — 실제 서명 키가 커밋되는 것을 막는 회귀 가드
    - Test 4: `DATABASE_PASSWORD`의 값이 빈 문자열이다
    - Test 5: `.env.example` 어디에도 32자 이상의 연속된 hex 문자열(카카오 앱 키 형태)이나 base64 시크릿 형태(`[A-Za-z0-9+/]{32,}={0,2}`)가 등장하지 않는다 — 키 이름을 바꿔 우회하는 경우까지 잡는다
    - Test 6: `.gitignore`가 `.env*`와 `!.env.example`을 모두 포함한다
    - Test 7: `EXPO_PUBLIC_` 접두사를 가진 키는 EXPO_PUBLIC_API_BASE_URL 하나뿐이다 — 비밀값이 공개 접두사로 새어나가는 것을 막는 가드
  </behavior>
  <verify>
    <automated>npm test -- src/auth/envContract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .env.example` 성공
    - `git check-ignore -q .env.example; test $? -eq 1` 성공 (= .env.example이 git에 의해 무시되지 **않는다**)
    - `grep -c '^!\.env\.example$' .gitignore` 결과가 1
    - `grep -c '^KAKAO_NATIVE_APP_KEY=$' .env.example` 결과가 1 (등호 뒤에 아무 값도 없음)
    - `grep -c '^JWT_SECRET=$' .env.example` 결과가 1
    - `grep -c '^DATABASE_PASSWORD=$' .env.example` 결과가 1
    - `grep -c 'client.secret\|CLIENT_SECRET\|REST_API_KEY' .env.example` 결과가 0 (D-14 수정으로 백엔드가 쓰지 않는 값을 요구하지 않음)
    - `grep -cE '^EXPO_PUBLIC_' .env.example` 결과가 1
    - `npm test -- src/auth/envContract.test.ts` 가 7개 단언 모두 통과
  </acceptance_criteria>
  <done>환경변수 이름 계약이 저장소에 커밋되어 있고, 실제 시크릿이 그 파일에 들어가면 테스트가 즉시 실패한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 창업자 로컬 `.env` → git 저장소 | 카카오 네이티브 앱 키와 JWT 서명 키가 커밋 경계를 넘어가면 안 되는 지점 |
| 카카오 개발자 콘솔 동의항목 설정 → 카카오가 앱에 넘겨주는 사용자 개인정보 범위 | 콘솔에서 요청 범위를 넓히면 백엔드 코드 변경 없이도 수집 범위가 커진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | `.env.example` / `.gitignore` | mitigate | `.gitignore`의 `.env*` + `!.env.example` 조합으로 실제 `.env`는 커밋 불가. `envContract.test.ts` Test 2/3/4/5가 `.env.example`에 실제 키·시크릿·base64 형태 문자열이 들어오면 즉시 실패시킨다(ASVS V6 — 시크릿 하드코딩 금지, Phase 9 D-11 계승) |
| T-10-02 | Information Disclosure | 카카오 로그인 동의항목 설정 | mitigate | Task 1 절차 6단계에서 이메일 동의항목을 "설정하지 않음"으로 유지하도록 명시(D-05/D-07). 백엔드는 추가로 `KakaoUserInfoResponse` DTO에 email 필드를 아예 매핑하지 않아 콘솔 설정이 바뀌어도 저장되지 않는다(10-04에서 이중 방어) |
| T-10-03 | Tampering | JWT 서명 키 강도 | mitigate | Task 1이 `openssl rand -base64 32`(≥256비트)를 명시적으로 지시. 키가 짧으면 Nimbus가 기동 시점에 예외로 거부하므로 약한 키가 조용히 운영에 도달할 수 없다(10-RESEARCH.md Pitfall 2) |
| T-10-SC | Tampering | npm 패키지 `expo-secure-store` | mitigate | Task 2의 `blocking-human` 체크포인트 — 10-RESEARCH.md 감사 표에 없는 `[ASSUMED]` 패키지이므로 설치 전 사람이 npm 레지스트리 + Expo 공식 문서에서 1st-party 여부를 직접 확인한다. 자동 승인 불가(`workflow.auto_advance` 무시). `@react-native-seoul/kakao-login`은 10-RESEARCH.md 감사 표에서 이미 `[OK]`/Approved로 검증됨 |
</threat_model>

<verification>
- `npm test -- src/auth/envContract.test.ts` 전체 통과
- `git check-ignore -v .env` 가 `.env`를 무시함을 확인(실제 값 파일은 커밋 불가)
- 창업자가 Task 1 완료를 확인했고, Task 2에서 expo-secure-store 정통성을 승인했다
- `git status --short`에 `.env`가 나타나지 않는다
</verification>

<success_criteria>
- 카카오 개발자 콘솔 앱 등록이라는 Phase 10의 유일한 하드 블로커가 해소되었다(10-RESEARCH.md Environment Availability)
- 이후 모든 플랜(10-03/10-06/10-07)이 참조할 환경변수 이름이 단일 파일에 고정되었다
- 실제 시크릿이 저장소에 커밋되는 경로가 자동 테스트로 차단되었다
</success_criteria>

<output>
Create `.planning/phases/10-authentication-kakao-oauth2-pkce/10-01-SUMMARY.md` when done
</output>
