# Design: FootLog — 재센터 버튼 + 나침반 배지 분리(애플 지도 방식)

Generated by /gsd-capture --list (brainstorming) on 2026-09-01
Branch: main
Repo: FootLog2 (local, no remote yet)
Status: APPROVED
Source todo: `.planning/todos/pending/2026-09-01-recenter-button-apple-maps-parity.md`

## Problem Statement
오늘 뷰의 재센터 버튼(`handleRecenterPress`, `src/app/(tabs)/index/index.tsx` ~696번 줄)이 현재 위치 이동과 헤딩(나침반) 모드 토글을 하나로 겸하고 있다 — 연속 탭하면 north-up → compass(기기 방향에 맞춰 지도 회전) 모드로 전환되고, 아이콘도 `location.fill` ↔ `location.north.line.fill`로 바뀐다.

2026-09-01 사용자 피드백: "애플 지도처럼 현재 위치 버튼과 나침반 버튼이 똑같았으면 좋겠어. 그리고 똑같이 오른쪽 하단에 있었으면 좋겠어." 이 요청을 사용자가 첨부한 실제 지도 앱 스크린샷(우하단에 나침반 배지 → 지도 레이어 버튼 → 위치 버튼 순으로 세로 스택된 구성)으로 재확인함.

## Premises
1. **동작 모델**: 재센터 버튼은 기존 동작(탭 → 위치 이동+follow 진입, 연속 탭 → north↔compass 헤딩 토글)을 그대로 유지한다. 나침반 배지는 이 토글의 대체가 아니라, `orientationMode === 'compass'`일 때만 나타나는 별도의 "북쪽으로 리셋" 전용 컨트롤이다.
2. **배치**: 두 컨트롤 모두 화면 우하단, 기존 재센터 버튼과 같은 `right: spacing.lg` 정렬. 배지는 재센터 버튼 바로 위에 세로로 스택(간격 `spacing.xs` 8px) — 첨부 스크린샷 실측 배치 기준.
3. **가시성**: 배지는 `orientationMode === 'compass'`일 때만 렌더되며 180ms 페이드 인/아웃(기존 "저장 상태 전환" 모션 토큰 재사용, 새 모션 값 추가 안 함). north 모드로 돌아가거나 사용자가 지도를 손으로 팬(`handlePanDrag`)하면 사라진다.
4. **배지 탭 동작**: `handlePanDrag`(678~685번 줄)의 "north로 리셋" 분기 — heading 구독 해제 + `orientationModeRef`/`setOrientationMode('north')` + `animateCamera({heading:0, pitch:0})` — 를 `resetToNorth` 함수로 추출해 배지 `onPress`와 공유한다. 위치 follow 자체는 해제하지 않고 회전만 리셋한다.
5. **색상 — 애플 지도 스타일로 예외 적용(중요 결정)**: 사용자가 "애플 지도와 완전히 동일하게" 색상까지 명시적으로 요청 — 2026-08-31 DESIGN.md 결정(재센터 버튼 아이콘을 Pin 테라코타로 통일)을 이 두 컨트롤에 한해 되돌린다.
   - 재센터 버튼(44×44 유지): 배경 흰색(`#FFFFFF`), 테두리 대신 옅은 그림자, 아이콘은 시스템 블루(`#007AFF`) — `location.fill` / `location.north.line.fill` 아이콘 이름은 그대로 재사용.
   - 나침반 배지(신규, 36×36): 짙은 반투명 배경(`rgba(40,40,40,0.85)`), 안쪽에 작은 빨간 삼각형(북 방향 포인터, `#FF3B30` — iOS systemRed) + "N" 텍스트(9px, 흰색, 500).
   - 이 두 컨트롤은 DESIGN.md "semantic 색상 없음 / 빨강 금지" 원칙의 명시적 예외로 문서화한다(아래 DESIGN.md 갱신 항목 참고).
6. **접근성**: 배지는 `accessibilityRole="button"`, `accessibilityLabel="지도를 북쪽으로 정렬"`. 36×36으로 44pt 터치 타겟 기준에 못 미치므로 `hitSlop`으로 44pt까지 히트 영역만 확장한다(시각적 크기는 애플 지도 실측 기준 유지). 재센터 버튼의 기존 `accessibilityLabel="현재 위치로 이동"`은 변경 없음.

## Approaches Considered (동작 모델)
### Approach A: 기존 토글 유지 + 배지는 리셋 전용 (채택)
재센터 버튼의 연속 탭 토글은 그대로 두고, 배지는 compass 모드일 때만 나타나는 부가 컨트롤로 추가. 실제 애플 지도의 동작 모델(위치 버튼이 follow/heading 토글을 담당하고, 나침반 배지는 회전 상태일 때만 별도로 나타나 리셋만 하는 독립 컨트롤)과 일치. 기존 `handlePanDrag`의 리셋 로직을 그대로 재사용할 수 있어 변경 범위가 가장 작다.

### Approach B: 두 버튼을 완전히 독립적인 단일 목적 컨트롤로 분리 (반려)
재센터 버튼을 "이동 전용"으로, 별도 상시 버튼을 "north/compass 토글 전용"으로 나누는 방식. "두 버튼이 동일한 스타일"이라는 요청에는 더 충실하지만, 실제 애플 지도 동작 모델과 다르고(애플은 위치 버튼 자체가 토글을 담당) 기존 토글 로직의 재설계가 필요해 변경 범위가 크다.

## Recommended Approach
Approach A.

## Visual Design Decisions
목업으로 확인 완료(우하단 세로 스택, north 모드/compass 모드 두 상태) — 사용자 승인.
- 재센터 버튼: 44×44, `#FFFFFF` 배경, 옅은 그림자(`0 1px 4px rgba(0,0,0,0.18)`), 아이콘 `#007AFF`.
- 나침반 배지: 36×36, `rgba(40,40,40,0.85)` 배경, 원형, 내부 빨간 삼각형(북 포인터) + "N" 흰 텍스트.
- 간격: 배지-버튼 사이 `spacing.xs`(8px), 둘 다 화면 우측 `spacing.lg` 정렬.
- 모션: 배지 등장/소멸 180ms 페이드(기존 저장 상태 전환 크로스페이드 토큰 재사용).

## DESIGN.md 갱신 필요 항목
1. **Decisions Log**: 신규 항목 추가 — "2026-09-01 | 재센터 버튼 + 신규 나침반 배지를 2026-08-31 Pin 통일 결정에서 애플 지도 스타일(흰 배경/시스템 블루 아이콘, 배지는 짙은 배경+빨간 삼각형+N)로 되돌림 | 창업자가 애플 지도와 완전히 동일한 스타일을 명시적으로 요청 — 발견성/친숙함을 정체성 색 일관성보다 우선".
2. **Color 섹션**: "Semantic 색상 없음" 원칙 문단에 이 두 컨트롤이 명시적 예외임을 한 줄 추가(빨강 금지 원칙의 유일한 예외 지점).
3. Pin 색상 설명에서 "재센터 버튼 아이콘"을 용도 목록에서 제거(더 이상 Pin을 쓰지 않으므로).

## Open Questions
없음 — 모든 결정 사항이 이 세션에서 확정됨.

## What already exists
`handlePanDrag`의 north 리셋 로직(678~685번 줄), `orientationMode`/`orientationModeRef`/`hasCenteredOnceRef` 상태 관리, 180ms 크로스페이드 모션 토큰, 44×44 원형 버튼 스타일 패턴(`recenterButton`) — 전부 재사용. 새 SF Symbol/애니메이션 라이브러리 불필요.

## NOT in scope (considered and deferred)
- 실제 애플 지도의 3단계 순환(follow → follow+heading → 재탭 시 원위치)이나 pitch 3D 틸트 재현 — 기존 2단계 토글(north/compass) 유지, 이번 범위 아님.
- 배지가 사용자의 수동 2손가락 회전 제스처에도 반응하는 것 — FootLog 지도는 현재 회전 제스처 자체를 별도로 다루지 않음(heading 구독 기반 회전만 존재), 스코프 밖.

## Dependencies
Phase 3/4가 만든 오늘 뷰 지도 컴포넌트(`src/app/(tabs)/index/index.tsx`) 위에 구현. 별도 phase 없이 독립 구현 가능 — Phase 5(체크인 상세/편집)와 무관.
